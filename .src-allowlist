# Paths that are still permitted under repo-root src/.
#
# Format: one repo-relative path per line. Blank lines and lines starting
# with `#` are ignored. Compared with grep -Fx (full-line literal match).
#
# Keep this file small and shrinking. Every line is a deletion target.
# When you remove a path here, also delete the file from src/.
#
# The src/ → packages/daemon/src/ fold completed in F1..F5 (2026-05-12).
# Only the two CLI-edge test fixtures remain — they spawn bin/cli.js as a
# subprocess and resolve it via __dirname/../bin/cli.js, so they live at
# repo-root src/ rather than under the daemon package. Tracked for future
# cleanup once the daemon publishes a bin entry.
src/cli.test.ts
src/integration.test.ts
