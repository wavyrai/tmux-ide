diff --git a/src/common/InputHandler.ts b/src/common/InputHandler.ts
index ba53941..82888c2 100644
--- a/src/common/InputHandler.ts
+++ b/src/common/InputHandler.ts
@@ -554,10 +554,14 @@ export class InputHandler extends Disposable implements IInputHandler {
         this._oscLinkService.addLineToLink(this._getCurrentLinkId(), this._activeBuffer.ybase + this._activeBuffer.y);
       }
 
+      // tmux stores oversized glyph backing even in a one-column pane.
+      // Retain the owner and continuation together; viewport projection clips it.
+      if (chWidth > cols) {
+        if (!wraparoundMode) continue;
+        bufferRow.resize(this._activeBuffer.x + chWidth, this._activeBuffer.getNullCell(curAttr));
+      }
       // goto next line if ch would overflow
-      // NOTE: To avoid costly width checks here,
-      // the terminal does not allow a cols < 2.
-      if (this._activeBuffer.x + chWidth - oldWidth > cols) {
+      if (chWidth <= cols && this._activeBuffer.x + chWidth - oldWidth > cols) {
         // autowrap - DECAWM
         // automatically wraps to the beginning of the next line
         if (wraparoundMode) {
@@ -1152,6 +1156,7 @@ export class InputHandler extends Disposable implements IInputHandler {
       this._activeBuffer.getNullCell(this._eraseAttrData()),
       respectProtect
     );
+    if (!respectProtect && start === 0 && end >= this._bufferService.cols) line.tmuxCellUsed = 0;
     if (clearWrap) {
       line.isWrapped = false;
     }
@@ -1223,7 +1228,46 @@ export class InputHandler extends Disposable implements IInputHandler {
         this._dirtyRowTracker.markDirty(0);
         break;
       case 2:
-        if (this._optionsService.rawOptions.scrollOnEraseInDisplay) {
+        if (this._optionsService.rawOptions.tmuxScrollOnClear && !respectProtect) {
+          // tmux ED2 ignores DECSTBM and moves through the last used physical
+          // row into normal history. An alternate screen never adds history.
+          const buffer = this._activeBuffer;
+          let usedRows = 0;
+          const configuredLimit = this._optionsService.rawOptions.tmuxHistoryLimit;
+          const limit = configuredLimit >= 0 ? configuredLimit : this._optionsService.rawOptions.scrollback;
+          if (limit > 0 && buffer === this._bufferService.buffers.normal && buffer.hasScrollback) {
+            for (let row = this._bufferService.rows - 1; row >= 0; row--) {
+              if (buffer.lines.get(buffer.ybase + row)!.tmuxCellUsed !== 0) {
+                usedRows = row + 1;
+                break;
+              }
+            }
+          }
+          const top = buffer.scrollTop;
+          const bottom = buffer.scrollBottom;
+          buffer.scrollTop = 0;
+          buffer.scrollBottom = this._bufferService.rows - 1;
+          try {
+            for (let row = 0; row < usedRows; row++) {
+              // tmux collects the oldest ten percent before appending at its
+              // history limit, rather than xterm's one-line ring recycling.
+              if (buffer.ybase > 0 && buffer.ybase >= limit) {
+                const trim = Math.min(buffer.ybase, Math.max(1, Math.floor(limit / 10)));
+                buffer.lines.trimStart(trim);
+                buffer.ybase -= trim;
+                buffer.ydisp = Math.max(0, buffer.ydisp - trim);
+              }
+              this._bufferService.scroll(this._eraseAttrData());
+            }
+          } finally {
+            buffer.scrollTop = top;
+            buffer.scrollBottom = bottom;
+          }
+          // Includes the no-used-lines case and clears stale styled blanks.
+          for (let row = 0; row < this._bufferService.rows; row++) this._resetBufferLine(row);
+          this._dirtyRowTracker.markRangeDirty(0, this._bufferService.rows - 1);
+        }
+        else if (this._optionsService.rawOptions.scrollOnEraseInDisplay) {
           j = this._bufferService.rows;
           this._dirtyRowTracker.markRangeDirty(0, j - 1);
           while (j--) {
diff --git a/src/common/Types.ts b/src/common/Types.ts
index 3c147b8..eaf2d5b 100644
--- a/src/common/Types.ts
+++ b/src/common/Types.ts
@@ -223,6 +223,8 @@ export interface ICellData extends IAttributeData {
  * Interface for a line in the terminal buffer.
  */
 export interface IBufferLine {
+  /** tmux cellused: partial erasure retains the written high-water mark. */
+  tmuxCellUsed: number;
   length: number;
   isWrapped: boolean;
   get(index: number): CharData;
diff --git a/src/common/buffer/Buffer.ts b/src/common/buffer/Buffer.ts
index 81ab156..26fbcc9 100644
--- a/src/common/buffer/Buffer.ts
+++ b/src/common/buffer/Buffer.ts
@@ -302,7 +302,9 @@ export class Buffer implements IBuffer {
   }
 
   private _reflow(newCols: number, newRows: number): void {
-    if (this._cols === newCols) {
+    // A tmux owner reseeds one-column transitions from the native grid.
+    // Avoid xterm reflow here: a wide cell cannot fit and yields zero progress.
+    if (this._cols === newCols || this._cols === 1 || newCols === 1) {
       return;
     }
 
diff --git a/src/common/buffer/BufferLine.ts b/src/common/buffer/BufferLine.ts
index ee3481a..28321a7 100644
--- a/src/common/buffer/BufferLine.ts
+++ b/src/common/buffer/BufferLine.ts
@@ -63,6 +63,7 @@ export class BufferLine implements IBufferLine {
   protected _combined: {[index: number]: string} = {};
   protected _extendedAttrs: {[index: number]: IExtendedAttrs | undefined} = {};
   public length: number;
+  public tmuxCellUsed = 0;
 
   constructor(cols: number, fillCellData?: ICellData, public isWrapped: boolean = false) {
     this._data = new Uint32Array(cols * CELL_SIZE);
@@ -97,6 +98,7 @@ export class BufferLine implements IBufferLine {
    * @deprecated
    */
   public set(index: number, value: CharData): void {
+    if (value[CHAR_DATA_CHAR_INDEX]) this.tmuxCellUsed = Math.max(this.tmuxCellUsed, index + Math.max(1, value[CHAR_DATA_WIDTH_INDEX]));
     this._data[index * CELL_SIZE + Cell.FG] = value[CHAR_DATA_ATTR_INDEX];
     if (value[CHAR_DATA_CHAR_INDEX].length > 1) {
       this._combined[index] = value[1];
@@ -196,6 +198,7 @@ export class BufferLine implements IBufferLine {
    * Set data at `index` to `cell`.
    */
   public setCell(index: number, cell: ICellData): void {
+    if (cell.content & Content.HAS_CONTENT_MASK) this.tmuxCellUsed = Math.max(this.tmuxCellUsed, index + Math.max(1, cell.content >> Content.WIDTH_SHIFT));
     if (cell.content & Content.IS_COMBINED_MASK) {
       this._combined[index] = cell.combinedData;
     }
@@ -213,6 +216,7 @@ export class BufferLine implements IBufferLine {
    * it gets an optimized access method.
    */
   public setCellFromCodepoint(index: number, codePoint: number, width: number, attrs: IAttributeData): void {
+    if (codePoint) this.tmuxCellUsed = Math.max(this.tmuxCellUsed, index + Math.max(1, width));
     if (attrs.bg & BgFlags.HAS_EXTENDED) {
       this._extendedAttrs[index] = attrs.extended;
     }
@@ -255,6 +259,7 @@ export class BufferLine implements IBufferLine {
 
   public insertCells(pos: number, n: number, fillCellData: ICellData): void {
     pos %= this.length;
+    if (n > 0 && n < this.length - pos) this.tmuxCellUsed = Math.max(this.tmuxCellUsed, this.length);
 
     // handle fullwidth at pos: reset cell one to the left if pos is second cell of a wide char
     if (pos && this.getWidth(pos - 1) === 2) {
@@ -283,6 +288,8 @@ export class BufferLine implements IBufferLine {
 
   public deleteCells(pos: number, n: number, fillCellData: ICellData): void {
     pos %= this.length;
+    if (n > 0 && n < this.length - pos) this.tmuxCellUsed = Math.max(this.tmuxCellUsed, this.length - n);
+    else if (pos === 0 && n >= this.length) this.tmuxCellUsed = 0;
     if (n < this.length - pos) {
       const cell = new CellData();
       for (let i = 0; i < this.length - pos - n; ++i) {
@@ -416,6 +423,7 @@ export class BufferLine implements IBufferLine {
       }
       return;
     }
+    this.tmuxCellUsed = 0;
     this._combined = {};
     this._extendedAttrs = {};
     for (let i = 0; i < this.length; ++i) {
@@ -441,6 +449,7 @@ export class BufferLine implements IBufferLine {
       this._extendedAttrs[el] = line._extendedAttrs[el];
     }
     this.isWrapped = line.isWrapped;
+    this.tmuxCellUsed = line.tmuxCellUsed;
   }
 
   /** create a new clone */
@@ -455,6 +464,7 @@ export class BufferLine implements IBufferLine {
       newLine._extendedAttrs[el] = this._extendedAttrs[el];
     }
     newLine.isWrapped = this.isWrapped;
+    newLine.tmuxCellUsed = this.tmuxCellUsed;
     return newLine;
   }
 
@@ -477,6 +487,8 @@ export class BufferLine implements IBufferLine {
   }
 
   public copyCellsFrom(src: BufferLine, srcCol: number, destCol: number, length: number, applyInReverse: boolean): void {
+    const copiedUsed = Math.max(0, Math.min(length, src.tmuxCellUsed - srcCol));
+    if (copiedUsed > 0) this.tmuxCellUsed = Math.max(this.tmuxCellUsed, destCol + copiedUsed);
     const srcData = src._data;
     if (applyInReverse) {
       for (let cell = length - 1; cell >= 0; cell--) {
diff --git a/src/common/services/BufferService.ts b/src/common/services/BufferService.ts
index 4cba3c1..bb15ebc 100644
--- a/src/common/services/BufferService.ts
+++ b/src/common/services/BufferService.ts
@@ -10,7 +10,7 @@ import { IBuffer, IBufferSet } from 'common/buffer/Types';
 import { IBufferService, IOptionsService, type IBufferResizeEvent } from 'common/services/Services';
 import { Emitter } from 'vs/base/common/event';
 
-export const MINIMUM_COLS = 2; // Less than 2 can mess with wide chars
+export const MINIMUM_COLS = 1; // tmux permits single-column panes
 export const MINIMUM_ROWS = 1;
 
 export class BufferService extends Disposable implements IBufferService {
diff --git a/src/common/services/OptionsService.ts b/src/common/services/OptionsService.ts
index 6ad48b9..4befd9b 100644
--- a/src/common/services/OptionsService.ts
+++ b/src/common/services/OptionsService.ts
@@ -33,6 +33,8 @@ export const DEFAULT_OPTIONS: Readonly<Required<ITerminalOptions>> = {
   logger: null,
   scrollback: 1000,
   scrollOnEraseInDisplay: false,
+  tmuxScrollOnClear: false,
+  tmuxHistoryLimit: -1,
   scrollOnUserInput: true,
   scrollSensitivity: 1,
   screenReaderMode: false,
diff --git a/src/common/services/Services.ts b/src/common/services/Services.ts
index a2847f1..0fed6ad 100644
--- a/src/common/services/Services.ts
+++ b/src/common/services/Services.ts
@@ -267,6 +267,10 @@ export interface ITerminalOptions {
   wordSeparator?: string;
   overviewRuler?: IOverviewRulerOptions;
   scrollOnEraseInDisplay?: boolean;
+    /** Preserve normal-screen history on ED2 with tmux scroll-on-clear semantics. */
+    tmuxScrollOnClear?: boolean;
+    /** Actual tmux history limit, independent of captured-history capacity; -1 uses scrollback. */
+    tmuxHistoryLimit?: number;
 
   [key: string]: any;
   cancelEvents: boolean;
diff --git a/typings/xterm-headless.d.ts b/typings/xterm-headless.d.ts
index f4f7693..9b4f8e0 100644
--- a/typings/xterm-headless.d.ts
+++ b/typings/xterm-headless.d.ts
@@ -192,6 +192,10 @@ declare module '@xterm/headless' {
      * This emulates PuTTY's default clear screen behavior.
      */
     scrollOnEraseInDisplay?: boolean;
+    /** Preserve normal-screen history on ED2 with tmux scroll-on-clear semantics. */
+    tmuxScrollOnClear?: boolean;
+    /** Actual tmux history limit, independent of captured-history capacity; -1 uses scrollback. */
+    tmuxHistoryLimit?: number;
 
     /**
      * The scrolling speed multiplier used for adjusting normal scrolling speed.
diff --git a/typings/xterm.d.ts b/typings/xterm.d.ts
index 457f161..54d21d8 100644
--- a/typings/xterm.d.ts
+++ b/typings/xterm.d.ts
@@ -256,6 +256,10 @@ declare module '@xterm/xterm' {
      * This emulates PuTTY's default clear screen behavior.
      */
     scrollOnEraseInDisplay?: boolean;
+    /** Preserve normal-screen history on ED2 with tmux scroll-on-clear semantics. */
+    tmuxScrollOnClear?: boolean;
+    /** Actual tmux history limit, independent of captured-history capacity; -1 uses scrollback. */
+    tmuxHistoryLimit?: number;
 
     /**
      * Whether to scroll to the bottom whenever there is some user input. The
