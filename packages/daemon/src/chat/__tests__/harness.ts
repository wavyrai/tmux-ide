/**
 * Re-export of the chat integration harness so future integration tests
 * can land under `__tests__/` without reaching into the parent directory.
 */

export * from "../chat-integration-harness.ts";
