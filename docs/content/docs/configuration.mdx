---
title: ide.yml Layouts
description: The optional ide.yml file for describing tmux layouts that tmux-ide builds and adopts for you
---

## Overview

`ide.yml` is **optional**. `tmux-ide adopt` works on any session you launch
however you like — but if you want tmux-ide to build the layout, describe it in
`ide.yml` and run `tmux-ide`. Sessions launched from a config are adopted
automatically, so the dock and agent detection are on from the start.

Scaffold one from your detected stack:

```bash
tmux-ide init          # or: tmux-ide detect --write
```

## Minimal example

```yaml
name: my-app # tmux session name
sidebar: true # nav column (prefix b / ⌥b), injected at launch

rows:
  - size: 70% # row height
    panes:
      - title: Claude
        command: claude
        focus: true
      - title: Shell
  - panes:
      - title: Dev Server
        command: pnpm dev
      - title: Tests
        command: pnpm test
```

## Fields

### Top level

| Field | Type | Notes |
| --- | --- | --- |
| `name` | string | tmux session name |
| `before` | string | Optional pre-launch shell hook |
| `rows` | array | Row definitions (at least one) |
| `sidebar` | `true` \| `{ width }` | Inject the nav column (`prefix b` / `⌥b`) at launch |
| `theme` | object | Per-session color overrides (see below) |
| `team` | object | Enable Claude Code agent teams (see [Task system](/docs/task-system)) |

### Rows

```yaml
rows:
  - size: 70% # optional row height (percent); rows split evenly if omitted
    panes: [...] # at least one pane
```

### Panes

| Field | Type | Notes |
| --- | --- | --- |
| `title` | string | Pane border label |
| `command` | string | Command to run in the pane |
| `size` | percent | Pane width (e.g. `50%`) |
| `dir` | string | Per-pane working directory |
| `focus` | boolean | Initial focus |
| `env` | map | Environment variables |
| `type` | string | Render a widget instead of a shell (see below) |
| `target` | string | Target path for a widget pane |
| `role`, `specialty`, `skill`, `task` | string | Agent-team metadata |

### Widget panes

Set `type` to render a built-in TUI widget in a pane instead of a shell:

```yaml
panes:
  - title: Explorer
    type: explorer
    target: src/
  - title: Changes
    type: changes
```

Available types include `explorer`, `changes`, `preview`, `config`, `tasks`, and
`costs`. The same widgets are available as floating panels (`prefix e` / `g` /
`v`, or `⌥e` / `⌥g` / `⌥,`) from any adopted session — see
[Home, sidebar & panels](/docs/app-surfaces).

### Sidebar sugar

```yaml
sidebar: true # default width
# or
sidebar:
  width: "30" # tmux columns
```

`sidebar: true` injects a full-height left nav column at launch. Omit it (or set
`false`) for no sidebar. You can also toggle it at runtime with `prefix b` (or `⌥b`).

### Per-session theme

The `theme` block sets colors for **this session's** panes:

```yaml
theme:
  accent: colour75
  border: colour238
  bg: colour235
  fg: colour248
```

For the product-wide palette that colors the dock, chips, panels, and widgets,
use `~/.tmux-ide/config.json` instead — see [Theming & config](/docs/theming).

## Editing config programmatically

Every field is reachable from the CLI with `--json` output, e.g.:

```bash
tmux-ide config set name "my-app"
tmux-ide config add-pane --row 0 --title "Claude" --command "claude"
tmux-ide validate --json
```

See the [CLI reference](/docs/commands) for the full `config` surface, and
[Templates](/docs/templates) for ready-made starting points.

## Agent teams & orchestration

tmux-ide can also enable Claude Code agent teams and run a task/mission
orchestrator from `ide.yml`. That's a deliberately secondary surface now — see
[Task system](/docs/task-system).
