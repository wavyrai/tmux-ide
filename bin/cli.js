#!/usr/bin/env node
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require2() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// packages/daemon/src/lib/yaml-io.ts
var yaml_io_exports = {};
__export(yaml_io_exports, {
  getSessionName: () => getSessionName,
  readConfig: () => readConfig,
  writeConfig: () => writeConfig
});
import { readFileSync, writeFileSync } from "node:fs";
import { resolve, basename } from "node:path";
import yaml from "js-yaml";
function readConfig(dir) {
  const configPath = resolve(dir, "ide.yml");
  const raw = readFileSync(configPath, "utf-8");
  const config2 = yaml.load(raw);
  return { config: config2, configPath };
}
function writeConfig(dir, config2) {
  const configPath = resolve(dir, "ide.yml");
  const out = yaml.dump(config2, { lineWidth: -1, noRefs: true, quotingType: '"' });
  writeFileSync(configPath, out);
  return configPath;
}
function getSessionName(dir) {
  try {
    const { config: config2 } = readConfig(dir);
    return { name: config2.name ?? basename(dir), source: config2.name ? "config" : "fallback" };
  } catch {
    return { name: basename(dir), source: "fallback" };
  }
}
var init_yaml_io = __esm({
  "packages/daemon/src/lib/yaml-io.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/sizes.ts
function computeSizes(items) {
  let claimed = 0;
  let unclaimed = 0;
  for (const item of items) {
    if (item.size) {
      claimed += parseFloat(item.size);
    } else {
      unclaimed++;
    }
  }
  const remaining = Math.max(0, 100 - claimed);
  const defaultSize = unclaimed > 0 ? remaining / unclaimed : 0;
  return items.map((item) => item.size ? parseFloat(item.size) : defaultSize);
}
function toSplitPercents(sizes) {
  const percents = [];
  for (let i = 1; i < sizes.length; i++) {
    const remaining = sizes.slice(i - 1).reduce((a, b) => a + b, 0);
    const topShare = sizes[i - 1];
    percents.push(Math.round((remaining - topShare) / remaining * 100));
  }
  return percents;
}
var init_sizes = __esm({
  "packages/daemon/src/lib/sizes.ts"() {
    "use strict";
  }
});

// packages/tmux-bridge/src/errors.ts
var TmuxError;
var init_errors = __esm({
  "packages/tmux-bridge/src/errors.ts"() {
    "use strict";
    TmuxError = class extends Error {
      code;
      exitCode;
      constructor(message, code, options = {}) {
        super(message, { cause: options.cause });
        this.name = "TmuxError";
        this.code = code;
        this.exitCode = options.exitCode ?? 1;
      }
      toJSON() {
        const out = {
          error: this.message,
          code: this.code
        };
        if (this.cause) out.cause = this.cause.message;
        return out;
      }
    };
  }
});

// packages/tmux-bridge/src/runner.ts
import { execFileSync, spawn } from "node:child_process";
function runTmux(args, options = {}) {
  if (DEBUG || globalThis.__tmuxIdeVerbose) {
    console.error(`  [tmux] ${args.join(" ")}`);
  }
  const execOptions = {
    stdio: ["ignore", "pipe", "pipe"],
    ...options
  };
  try {
    return _executor("tmux", args, execOptions);
  } catch (error) {
    throw classifyTmuxError(error);
  }
}
function classifyTmuxError(error) {
  const detail = getErrorDetail(error).toLowerCase();
  if (SESSION_NOT_FOUND_PATTERNS.some((pattern) => detail.includes(pattern))) {
    return new TmuxError("tmux session was not found", "SESSION_NOT_FOUND", {
      cause: error
    });
  }
  if (TMUX_UNAVAILABLE_PATTERNS.some((pattern) => detail.includes(pattern))) {
    return new TmuxError("tmux is unavailable or its socket is inaccessible", "TMUX_UNAVAILABLE", {
      cause: error
    });
  }
  return new TmuxError("tmux command failed", "TMUX_ERROR", {
    cause: error
  });
}
function getErrorDetail(error) {
  const stderr = error?.stderr;
  if (typeof stderr === "string" && stderr.length > 0) return stderr;
  if (Buffer.isBuffer(stderr) && stderr.length > 0) return stderr.toString("utf-8");
  return error?.message ?? "";
}
var DEBUG, SESSION_NOT_FOUND_PATTERNS, TMUX_UNAVAILABLE_PATTERNS, _executor;
var init_runner = __esm({
  "packages/tmux-bridge/src/runner.ts"() {
    "use strict";
    init_errors();
    DEBUG = process.env.TMUX_IDE_DEBUG === "1";
    SESSION_NOT_FOUND_PATTERNS = ["can't find session", "can't find window", "unknown target"];
    TMUX_UNAVAILABLE_PATTERNS = [
      "failed to connect to server",
      "no server running",
      "error connecting to",
      "connection refused"
    ];
    _executor = execFileSync;
  }
});

// packages/tmux-bridge/src/sessions.ts
function getSessionState(session) {
  try {
    runTmux(["has-session", "-t", session]);
    return { running: true, reason: null };
  } catch (error) {
    if (error instanceof TmuxError) {
      if (error.code === "SESSION_NOT_FOUND") {
        return { running: false, reason: "SESSION_NOT_FOUND" };
      }
      if (error.code === "TMUX_UNAVAILABLE") {
        return { running: false, reason: "TMUX_UNAVAILABLE" };
      }
    }
    throw error;
  }
}
function attachSession(session) {
  runTmux(["attach", "-t", session], { stdio: "inherit" });
}
function hasSession(session) {
  try {
    runTmux(["has-session", "-t", session]);
    return true;
  } catch (error) {
    if (error instanceof TmuxError && (error.code === "SESSION_NOT_FOUND" || error.code === "TMUX_UNAVAILABLE")) {
      return false;
    }
    throw error;
  }
}
function getSessionCwd(session) {
  try {
    const raw = runTmux(["display-message", "-p", "-t", session, "#{pane_current_path}"], {
      encoding: "utf-8"
    });
    const trimmed = raw.trim();
    return trimmed.length > 0 ? trimmed : null;
  } catch {
    return null;
  }
}
function killSession(session) {
  try {
    runTmux(["kill-session", "-t", session]);
    return { stopped: true, reason: null };
  } catch (error) {
    if (error instanceof TmuxError) {
      if (error.code === "SESSION_NOT_FOUND") {
        return { stopped: false, reason: "SESSION_NOT_FOUND" };
      }
      if (error.code === "TMUX_UNAVAILABLE") {
        return { stopped: false, reason: "TMUX_UNAVAILABLE" };
      }
    }
    throw error;
  }
}
function createDetachedSession(session, cwd, { cols, lines } = {}) {
  return runTmux(
    [
      "new-session",
      "-d",
      "-P",
      "-F",
      "#{pane_id}",
      "-s",
      session,
      "-c",
      cwd,
      "-x",
      String(cols ?? 200),
      "-y",
      String(lines ?? 50)
    ],
    { encoding: "utf-8" }
  ).trim();
}
function setSessionEnvironment(session, key, value) {
  runTmux(["set-environment", "-t", session, key, String(value)]);
}
function getSessionVariable(session, name) {
  try {
    const raw = runTmux(["show-option", "-qvt", session, name], {
      encoding: "utf-8"
    });
    return raw.trim() || null;
  } catch {
    return null;
  }
}
function setSessionVariable(session, name, value) {
  runTmux(["set-option", "-t", session, name, value]);
}
function runSessionCommand(args) {
  runTmux(args, { stdio: "inherit" });
}
var init_sessions = __esm({
  "packages/tmux-bridge/src/sessions.ts"() {
    "use strict";
    init_errors();
    init_runner();
  }
});

// packages/tmux-bridge/src/panes.ts
function listPanes(session) {
  const raw = runTmux(
    [
      "list-panes",
      "-t",
      session,
      "-F",
      "#{pane_index}|#{pane_title}|#{pane_width}|#{pane_height}|#{pane_active}"
    ],
    { encoding: "utf-8" }
  ).trim();
  if (!raw) return [];
  return raw.split("\n").map((line) => {
    const [index, title, width, height, active2] = line.split("|");
    return {
      index: Number.parseInt(index, 10),
      title,
      width: Number.parseInt(width, 10),
      height: Number.parseInt(height, 10),
      active: active2 === "1"
    };
  });
}
function splitPane(targetPane, direction, cwd, percent) {
  return runTmux(
    [
      "split-window",
      "-P",
      "-F",
      "#{pane_id}",
      "-t",
      targetPane,
      direction === "vertical" ? "-v" : "-h",
      "-c",
      cwd,
      // `-l N%` (tmux ≥3.1) — the old `-p N` percentage flag is gone from
      // some builds (e.g. 3.4 fails with "size missing").
      "-l",
      `${percent}%`
    ],
    { encoding: "utf-8" }
  ).trim();
}
function sendLiteral(targetPane, text) {
  runTmux(["send-keys", "-t", targetPane, "-l", "--", text], { stdio: "inherit" });
  runTmux(["send-keys", "-t", targetPane, "Enter"], { stdio: "inherit" });
}
function getPaneCurrentCommand(targetPane) {
  return runTmux(["display-message", "-p", "-t", targetPane, "#{pane_current_command}"], {
    encoding: "utf-8"
  }).trim();
}
function selectPane(targetPane) {
  runTmux(["select-pane", "-t", targetPane], { stdio: "inherit" });
}
function setPaneTitle(targetPane, title) {
  runTmux(["select-pane", "-t", targetPane, "-T", title], { stdio: "inherit" });
}
function setPaneOption(targetPane, option, value) {
  runTmux(["set-option", "-pqt", targetPane, option, value]);
}
var init_panes = __esm({
  "packages/tmux-bridge/src/panes.ts"() {
    "use strict";
    init_runner();
  }
});

// packages/tmux-bridge/src/monitor.ts
function isProcessAlive(pid) {
  try {
    process.kill(pid, 0);
    return true;
  } catch {
    return false;
  }
}
function stopSessionMonitor(session) {
  try {
    const pid = runTmux(["show-option", "-qvt", session, "@monitor_pid"], {
      encoding: "utf-8"
    }).trim();
    if (pid) {
      const numPid = parseInt(pid, 10);
      try {
        process.kill(-numPid, "SIGTERM");
      } catch {
        try {
          process.kill(numPid, "SIGTERM");
        } catch {
        }
      }
    }
  } catch {
  }
}
var init_monitor = __esm({
  "packages/tmux-bridge/src/monitor.ts"() {
    "use strict";
    init_runner();
  }
});

// packages/tmux-bridge/src/targeting.ts
var init_targeting = __esm({
  "packages/tmux-bridge/src/targeting.ts"() {
    "use strict";
  }
});

// packages/tmux-bridge/src/index.ts
var init_src = __esm({
  "packages/tmux-bridge/src/index.ts"() {
    "use strict";
    init_errors();
    init_runner();
    init_sessions();
    init_panes();
    init_monitor();
    init_targeting();
  }
});

// packages/daemon/src/lib/errors.ts
var IdeError, DaemonStartupError, DaemonShutdownError;
var init_errors2 = __esm({
  "packages/daemon/src/lib/errors.ts"() {
    "use strict";
    init_src();
    IdeError = class extends Error {
      code;
      exitCode;
      constructor(message, { code, exitCode = 1, cause } = {}) {
        super(message, { cause });
        this.name = "IdeError";
        this.code = code;
        this.exitCode = exitCode;
      }
      toJSON() {
        const obj = {
          error: this.message,
          code: this.code
        };
        if (this.cause) obj.cause = this.cause.message;
        return obj;
      }
    };
    DaemonStartupError = class extends IdeError {
      reason;
      constructor(message, reason, { cause } = {}) {
        super(message, { code: `DAEMON_${reason.toUpperCase()}`, exitCode: 1, cause });
        this.name = "DaemonStartupError";
        this.reason = reason;
      }
    };
    DaemonShutdownError = class extends IdeError {
      constructor(message, { cause } = {}) {
        super(message, { code: "DAEMON_SHUTDOWN_FAILED", exitCode: 1, cause });
        this.name = "DaemonShutdownError";
      }
    };
  }
});

// packages/daemon/src/lib/output.ts
function printLayout(config2) {
  const INNER = 40;
  const rows = config2.rows ?? [];
  if (rows.length === 0) return;
  for (let r = 0; r < rows.length; r++) {
    const panes = rows[r].panes ?? [];
    const count = panes.length || 1;
    const widths = [];
    let remaining = INNER;
    for (let i = 0; i < count; i++) {
      const w = i < count - 1 ? Math.floor(INNER / count) : remaining;
      widths.push(w);
      remaining -= w;
    }
    if (r === 0) {
      let top = "  \u250C";
      for (let i = 0; i < count; i++) {
        top += "\u2500".repeat(widths[i]);
        top += i < count - 1 ? "\u252C" : "\u2510";
      }
      console.log(top);
    } else {
      console.log("  \u251C" + "\u2500".repeat(INNER + count - 1) + "\u2524");
    }
    const sizeLabel = rows[r].size ?? "";
    let line = "  \u2502";
    for (let i = 0; i < count; i++) {
      const title = panes[i]?.title ?? "";
      const w = widths[i];
      const pad = Math.max(0, w - title.length);
      const left = Math.floor(pad / 2);
      const right = pad - left;
      line += " ".repeat(left) + title + " ".repeat(right) + "\u2502";
    }
    if (sizeLabel) line += "  " + sizeLabel;
    console.log(line);
    if (r === rows.length - 1) {
      let bot = "  \u2514";
      for (let i = 0; i < count; i++) {
        bot += "\u2500".repeat(widths[i]);
        bot += i < count - 1 ? "\u2534" : "\u2518";
      }
      console.log(bot);
    }
  }
}
function outputError(message, code, { exitCode = 1 } = {}) {
  throw new IdeError(message, { code, exitCode });
}
function printCommandError(error, { json: json2 = false } = {}) {
  if (json2) {
    console.error(JSON.stringify(error.toJSON(), null, 2));
  } else {
    console.error(error.message);
  }
  process.exit(error.exitCode ?? 1);
}
var init_output = __esm({
  "packages/daemon/src/lib/output.ts"() {
    "use strict";
    init_errors2();
  }
});

// packages/daemon/src/lib/shell.ts
function shellEscape(s) {
  return "'" + s.replace(/'/g, "'\\''") + "'";
}
var init_shell = __esm({
  "packages/daemon/src/lib/shell.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/launch-plan.ts
import { resolve as resolve2 } from "node:path";
function buildPaneCommand(pane) {
  if (!pane.command) return null;
  return pane.command;
}
function collectPaneStartupPlan(rows, paneMap, firstPanesOfRows, dir) {
  let focusPane = paneMap[0][0];
  const paneActions = [];
  for (let rowIdx = 0; rowIdx < rows.length; rowIdx++) {
    const row = rows[rowIdx];
    const panes = row.panes ?? [];
    for (let paneIdx = 0; paneIdx < panes.length; paneIdx++) {
      const pane = panes[paneIdx];
      const tmuxPane = paneMap[rowIdx][paneIdx];
      let paneRole;
      if (pane.role === "lead") {
        paneRole = "lead";
      } else if (pane.role === "teammate" || pane.role === "planner") {
        paneRole = "teammate";
      } else if (pane.type) {
        paneRole = "widget";
      } else {
        paneRole = "shell";
      }
      let paneType;
      if (pane.type) {
        paneType = pane.type;
      } else if (pane.command && /claude|codex/i.test(pane.command)) {
        paneType = "agent";
      } else {
        paneType = "shell";
      }
      const action = {
        targetPane: tmuxPane,
        title: pane.title ?? null,
        chdir: null,
        exports: [],
        command: null,
        widgetType: pane.type ?? null,
        widgetTarget: pane.target ?? null,
        paneRole,
        paneType
      };
      if (pane.dir && firstPanesOfRows.has(tmuxPane)) {
        action.chdir = resolve2(dir, pane.dir);
      }
      if (pane.env && typeof pane.env === "object") {
        action.exports = Object.entries(pane.env).map(
          ([key, value]) => `export ${shellEscape(key)}=${shellEscape(String(value))}`
        );
      }
      let command2 = buildPaneCommand(pane);
      if (command2 && pane.title && /claude|codex/i.test(command2) && !command2.includes("--name")) {
        command2 = `${command2} --name ${shellEscape(pane.title)}`;
      }
      if (command2) {
        action.command = command2;
      }
      if (pane.focus) {
        focusPane = tmuxPane;
      }
      paneActions.push(action);
    }
  }
  return { focusPane, paneActions };
}
var init_launch_plan = __esm({
  "packages/daemon/src/lib/launch-plan.ts"() {
    "use strict";
    init_shell();
  }
});

// packages/daemon/src/lib/session-options.ts
function buildSessionOptions(session, { theme = {} } = {}) {
  return [
    ...themeOptions(session, theme),
    ...borderOptions(session, theme),
    ...behaviorOptions(session),
    ...statusBarOptions(session, theme),
    ...keyBindings()
  ];
}
function themeOptions(session, theme) {
  const accent = theme.accent ?? "colour75";
  const border = theme.border ?? "colour238";
  const bg = theme.bg ?? "colour235";
  const fg = theme.fg ?? "colour248";
  return [
    ["set-option", "-t", session, "status-style", `bg=${bg},fg=${fg}`],
    ["set-option", "-t", session, "pane-border-style", `fg=${border}`],
    ["set-option", "-t", session, "pane-active-border-style", `fg=${accent}`]
  ];
}
function borderOptions(session, theme) {
  const accent = theme.accent ?? "colour75";
  const border = theme.border ?? "colour238";
  const fg = theme.fg ?? "colour248";
  return [
    ["set-option", "-t", session, "pane-border-status", "top"],
    [
      "set-option",
      "-t",
      session,
      "pane-border-format",
      ` #{?pane_active,#[fg=${accent}#,bold]\u25B8 #T  #[fg=${fg}]#{pane_current_path},#[fg=${border}]\xB7 #T  #{pane_current_path}} `
    ]
  ];
}
function behaviorOptions(session) {
  return [
    ["set-option", "-t", session, "mouse", "on"],
    ["set-option", "-t", session, "escape-time", "0"],
    ["set-option", "-t", session, "status-interval", "1"]
  ];
}
function statusBarOptions(session, theme) {
  const accent = theme.accent ?? "colour75";
  const border = theme.border ?? "colour238";
  const fg = theme.fg ?? "colour248";
  const agentIndicator = [
    `#{?#{==:#{@agent_busy},1},#[fg=${accent}]\u23FA ,`,
    `#{?#{==:#{@agent_idle},1},#[fg=${border}]\u25CF ,}}`
  ].join("");
  const portIndicator = `#{?#{==:#{@has_port},1},#[fg=green]\u23FA ,}`;
  const paneStyle = `#{?pane_active,#[fg=${accent}],#[fg=${border}]}`;
  const paneTab = `${agentIndicator}${portIndicator}${paneStyle}#[range=pane|#{pane_id}] #T #[norange]#[default]`;
  const separator = `#{?loop_last_flag,,#[fg=${border}]\u2502}`;
  return [
    [
      "set-option",
      "-t",
      session,
      "status-left",
      `#[fg=colour0,bg=${accent},bold]  ${session.toUpperCase()} IDE #[default] `
    ],
    ["set-option", "-t", session, "status-left-length", "30"],
    [
      "set-option",
      "-t",
      session,
      "status-right",
      `#[fg=colour243]%H:%M #[fg=${accent}]\u2502 #[fg=${fg}]%b %d `
    ],
    ["set-option", "-t", session, "status-justify", "centre"],
    ["set-option", "-t", session, "window-status-current-format", `#[fg=${accent},bold]\u25CF`],
    ["set-option", "-t", session, "window-status-format", `#[fg=${border}]\u25CB`],
    ["set-option", "-t", session, "status", "2"],
    ["set-option", "-t", session, "status-format[1]", `  #{P:${paneTab}${separator}}`]
  ];
}
function keyBindings() {
  return [["bind-key", "-n", "MouseDown1StatusDefault", "select-pane", "-t", "="]];
}
var init_session_options = __esm({
  "packages/daemon/src/lib/session-options.ts"() {
    "use strict";
  }
});

// packages/contracts/src/lib-internal/auth.ts
import { z } from "zod";
var AuthConfigSchema;
var init_auth = __esm({
  "packages/contracts/src/lib-internal/auth.ts"() {
    "use strict";
    AuthConfigSchema = z.object({
      /** Auth method: "none" disables auth (default), "ssh" enables SSH key challenge-response. */
      method: z.enum(["none", "ssh"]).default("none"),
      /** JWT secret — auto-generated if omitted. */
      secret: z.string().optional(),
      /** Token expiry in seconds (default 86400 = 24h). */
      token_expiry: z.number().min(60).default(86400)
    });
  }
});

// packages/contracts/src/lib-internal/hq.ts
import { z as z2 } from "zod";
var RegistrationPayloadSchema, HQConfigSchema;
var init_hq = __esm({
  "packages/contracts/src/lib-internal/hq.ts"() {
    "use strict";
    RegistrationPayloadSchema = z2.object({
      id: z2.string().min(1).max(256),
      name: z2.string().min(1).max(256),
      url: z2.string().url().max(2048),
      token: z2.string().min(1).max(512)
    });
    HQConfigSchema = z2.object({
      enabled: z2.boolean().default(false),
      role: z2.enum(["hq", "remote"]),
      hq_url: z2.string().url().optional(),
      secret: z2.string().optional(),
      heartbeat_interval: z2.number().min(1e3).default(15e3),
      machine_name: z2.string().optional()
    });
  }
});

// packages/contracts/src/ide-config.ts
import { z as z3 } from "zod";
var sizeField, ThemeConfigSchema, PaneSchema, RowSchema, WebhookConfigSchema, OrchestratorYamlConfigSchema, TunnelConfigSchema, CommandCenterConfigSchema, DashboardConfigSchema, IdeConfigSchema, PaneActionSchema, SessionStateSchema;
var init_ide_config = __esm({
  "packages/contracts/src/ide-config.ts"() {
    "use strict";
    init_auth();
    init_hq();
    sizeField = z3.string().regex(/^[1-9]\d*%$/).refine((v) => parseInt(v) <= 100);
    ThemeConfigSchema = z3.object({
      accent: z3.string().optional(),
      border: z3.string().optional(),
      bg: z3.string().optional(),
      fg: z3.string().optional()
    });
    PaneSchema = z3.object({
      title: z3.string().optional(),
      command: z3.string().optional(),
      type: z3.enum(["explorer", "changes", "preview", "tasks", "costs", "config", "mission-control"]).optional(),
      target: z3.string().optional(),
      dir: z3.string().optional(),
      size: sizeField.optional(),
      focus: z3.boolean().optional(),
      env: z3.record(z3.string(), z3.union([z3.string(), z3.number()])).optional(),
      role: z3.enum(["lead", "teammate", "planner", "validator", "researcher"]).optional(),
      task: z3.string().optional(),
      specialty: z3.string().optional(),
      skill: z3.string().optional()
    });
    RowSchema = z3.object({
      size: sizeField.optional(),
      panes: z3.array(PaneSchema).min(1)
    });
    WebhookConfigSchema = z3.object({
      url: z3.string(),
      events: z3.array(z3.string()).optional(),
      secret: z3.string().optional()
    });
    OrchestratorYamlConfigSchema = z3.object({
      enabled: z3.boolean().optional(),
      port: z3.number().int().positive().optional(),
      auto_dispatch: z3.boolean().optional(),
      stall_timeout: z3.number().optional(),
      poll_interval: z3.number().min(100).optional(),
      master_pane: z3.string().optional(),
      before_run: z3.string().optional(),
      after_run: z3.string().optional(),
      dispatch_mode: z3.enum(["tasks", "goals", "missions"]).optional(),
      max_concurrent_agents: z3.number().min(1).max(50).optional(),
      widgets: z3.boolean().optional(),
      webhooks: z3.array(WebhookConfigSchema).optional(),
      services: z3.record(
        z3.string(),
        z3.object({
          command: z3.string(),
          port: z3.number().optional(),
          healthcheck: z3.string().optional()
        })
      ).optional(),
      research: z3.object({
        enabled: z3.boolean().optional(),
        triggers: z3.object({
          mission_start: z3.boolean().optional(),
          milestone_progress: z3.number().min(0).optional(),
          milestone_complete: z3.boolean().optional(),
          periodic_minutes: z3.number().min(0).optional(),
          retry_cluster: z3.boolean().optional(),
          stall_detected: z3.boolean().optional(),
          discovered_issue: z3.boolean().optional()
        }).optional()
      }).optional()
    });
    TunnelConfigSchema = z3.object({
      provider: z3.enum(["tailscale", "ngrok", "cloudflare"]),
      auto_start: z3.boolean().optional(),
      port: z3.number().int().positive().optional(),
      domain: z3.string().optional(),
      authtoken: z3.string().optional()
    });
    CommandCenterConfigSchema = z3.object({
      port: z3.number().optional(),
      enabled: z3.boolean().optional()
    });
    DashboardConfigSchema = z3.object({
      port: z3.number().int().positive().optional()
    });
    IdeConfigSchema = z3.object({
      name: z3.string().optional(),
      before: z3.string().optional(),
      team: z3.object({
        name: z3.string(),
        model: z3.string().optional(),
        permissions: z3.array(z3.string()).optional()
      }).optional(),
      rows: z3.array(RowSchema).min(1),
      theme: ThemeConfigSchema.optional(),
      orchestrator: OrchestratorYamlConfigSchema.optional(),
      command_center: CommandCenterConfigSchema.optional(),
      dashboard: DashboardConfigSchema.optional(),
      auth: AuthConfigSchema.optional(),
      tunnel: TunnelConfigSchema.optional(),
      hq: HQConfigSchema.optional()
    });
    PaneActionSchema = z3.object({
      targetPane: z3.string(),
      title: z3.string().nullable(),
      chdir: z3.string().nullable(),
      exports: z3.array(z3.string()),
      command: z3.string().nullable(),
      widgetType: z3.string().nullable(),
      widgetTarget: z3.string().nullable(),
      paneRole: z3.string().nullable(),
      paneType: z3.string().nullable()
    });
    SessionStateSchema = z3.object({
      running: z3.boolean(),
      reason: z3.string().nullable()
    });
  }
});

// packages/contracts/src/domain.ts
import { z as z4 } from "zod";
var ProofSchemaZ, MilestoneSchemaZ, MissionSchemaZ, GoalSchemaZ, TaskSchemaZ, EventTypeSchemaZ, OrchestratorEventSchemaZ, DispatchEventZ, CompletionEventZ, StallEventZ, RetryEventZ, ReconcileEventZ, ErrorEventZ, TaskCreatedEventZ, StatusChangeEventZ, SendEventZ, NotifyEventZ, MilestoneValidatingEventZ, MilestoneCompleteEventZ, ValidationDispatchEventZ, RemediationEventZ, ValidationFailedEventZ, PlanningEventZ, MissionCompleteEventZ, DiscoveredIssueEventZ, ResearchDispatchEventZ, ResearchFindingEventZ, WebhookTestEventZ, StructuredEventSchemaZ, MarkRangeSchemaZ, MarkSchemaZ, AuthorshipStatsSchemaZ, PlanStatusSchemaZ, PlanMetaSchemaZ, PaneInfoSchemaZ, AgentDetailSchemaZ, SessionStatsSchemaZ, SessionOverviewSchemaZ, ProjectDetailSchemaZ, AgentToolSchemaZ, AgentKindSchemaZ, AgentStatusSchemaZ, AgentRecordSchemaZ, AgentRegisterPayloadSchemaZ, AgentHeartbeatPayloadSchemaZ, AgentListSchemaZ;
var init_domain = __esm({
  "packages/contracts/src/domain.ts"() {
    "use strict";
    ProofSchemaZ = z4.object({
      tests: z4.object({ passed: z4.number(), total: z4.number() }).optional(),
      pr: z4.object({
        number: z4.number(),
        url: z4.string().optional(),
        status: z4.string().optional()
      }).optional(),
      ci: z4.object({ status: z4.string(), url: z4.string().optional() }).optional(),
      notes: z4.string().optional()
    });
    MilestoneSchemaZ = z4.object({
      id: z4.string(),
      title: z4.string(),
      description: z4.string(),
      status: z4.enum(["locked", "active", "done", "validating"]),
      order: z4.number(),
      created: z4.string(),
      updated: z4.string()
    });
    MissionSchemaZ = z4.object({
      title: z4.string(),
      description: z4.string(),
      status: z4.enum(["planning", "active", "validating", "complete"]),
      branch: z4.string().nullable(),
      milestones: z4.array(MilestoneSchemaZ),
      created: z4.string(),
      updated: z4.string()
    });
    GoalSchemaZ = z4.object({
      id: z4.string(),
      title: z4.string(),
      description: z4.string(),
      status: z4.enum(["todo", "in-progress", "done"]),
      acceptance: z4.string(),
      priority: z4.number(),
      created: z4.string(),
      updated: z4.string(),
      assignee: z4.string().nullable(),
      specialty: z4.string().nullable(),
      milestone: z4.string().nullable()
    });
    TaskSchemaZ = z4.object({
      id: z4.string(),
      title: z4.string(),
      description: z4.string(),
      goal: z4.string().nullable(),
      status: z4.enum(["todo", "in-progress", "review", "done"]),
      assignee: z4.string().nullable(),
      priority: z4.number(),
      created: z4.string(),
      updated: z4.string(),
      tags: z4.array(z4.string()),
      proof: ProofSchemaZ.nullable(),
      retryCount: z4.number(),
      maxRetries: z4.number(),
      lastError: z4.string().nullable(),
      nextRetryAt: z4.string().nullable(),
      depends_on: z4.array(z4.string()),
      milestone: z4.string().nullable(),
      specialty: z4.string().nullable(),
      fulfills: z4.array(z4.string()),
      discoveredIssues: z4.array(z4.string()),
      salientSummary: z4.string().nullable()
    });
    EventTypeSchemaZ = z4.enum([
      "dispatch",
      "stall",
      "completion",
      "retry",
      "reconcile",
      "task.dispatched",
      "task.claimed",
      "task.completed",
      "task.failed",
      "task.retried",
      "agent.stalled",
      "agent.recovered",
      "orchestrator.reconciled",
      "error",
      "task_created",
      "status_change",
      "send",
      "notify",
      "milestone_validating",
      "milestone_complete",
      "validation_dispatch",
      "remediation",
      "validation_failed",
      "planning",
      "mission_complete",
      "discovered_issue",
      "research_dispatch",
      "research_finding",
      "agent_heartbeat",
      "session_start",
      "session_end",
      "webhook.test"
    ]);
    OrchestratorEventSchemaZ = z4.object({
      timestamp: z4.string(),
      type: EventTypeSchemaZ,
      taskId: z4.string().optional(),
      agent: z4.string().optional(),
      message: z4.string()
    });
    DispatchEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("dispatch"),
      taskId: z4.string(),
      agent: z4.string()
    });
    CompletionEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("completion"),
      taskId: z4.string(),
      agent: z4.string(),
      durationMs: z4.number().optional()
    });
    StallEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("stall"),
      taskId: z4.string(),
      agent: z4.string(),
      elapsedMs: z4.number()
    });
    RetryEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("retry"),
      taskId: z4.string(),
      attempt: z4.number(),
      maxRetries: z4.number(),
      reason: z4.string()
    });
    ReconcileEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("reconcile"),
      taskId: z4.string(),
      previousAgent: z4.string(),
      action: z4.string()
    });
    ErrorEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("error"),
      taskId: z4.string().optional(),
      agent: z4.string().optional(),
      message: z4.string(),
      code: z4.string().optional()
    });
    TaskCreatedEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("task_created"),
      taskId: z4.string(),
      title: z4.string()
    });
    StatusChangeEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("status_change"),
      taskId: z4.string(),
      from: z4.string(),
      to: z4.string()
    });
    SendEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("send"),
      target: z4.string(),
      paneId: z4.string(),
      message: z4.string()
    });
    NotifyEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("notify"),
      target: z4.string(),
      paneId: z4.string(),
      message: z4.string()
    });
    MilestoneValidatingEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("milestone_validating"),
      milestoneId: z4.string().optional(),
      title: z4.string().optional()
    });
    MilestoneCompleteEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("milestone_complete"),
      milestoneId: z4.string().optional(),
      title: z4.string().optional()
    });
    ValidationDispatchEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("validation_dispatch"),
      milestoneId: z4.string().optional(),
      title: z4.string().optional(),
      target: z4.string().optional()
    });
    RemediationEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("remediation"),
      taskId: z4.string(),
      assertionId: z4.string().optional()
    });
    ValidationFailedEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("validation_failed"),
      milestoneId: z4.string().optional(),
      title: z4.string().optional(),
      failedCount: z4.number().optional()
    });
    PlanningEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("planning"),
      target: z4.string().optional()
    });
    MissionCompleteEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("mission_complete"),
      title: z4.string().optional(),
      milestoneCount: z4.number().optional(),
      taskCount: z4.number().optional(),
      prNumber: z4.number().optional()
    });
    DiscoveredIssueEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("discovered_issue"),
      taskId: z4.string(),
      sourceTaskId: z4.string().optional(),
      issue: z4.string().optional()
    });
    ResearchDispatchEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("research_dispatch"),
      taskId: z4.string(),
      target: z4.string().optional(),
      researchType: z4.string().optional()
    });
    ResearchFindingEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("research_finding"),
      taskId: z4.string(),
      researchType: z4.string().optional(),
      summary: z4.string().optional()
    });
    WebhookTestEventZ = z4.object({
      timestamp: z4.string(),
      type: z4.literal("webhook.test"),
      message: z4.string()
    });
    StructuredEventSchemaZ = z4.union([
      DispatchEventZ,
      CompletionEventZ,
      StallEventZ,
      RetryEventZ,
      ReconcileEventZ,
      ErrorEventZ,
      TaskCreatedEventZ,
      StatusChangeEventZ,
      SendEventZ,
      NotifyEventZ,
      MilestoneValidatingEventZ,
      MilestoneCompleteEventZ,
      ValidationDispatchEventZ,
      RemediationEventZ,
      ValidationFailedEventZ,
      PlanningEventZ,
      MissionCompleteEventZ,
      DiscoveredIssueEventZ,
      ResearchDispatchEventZ,
      ResearchFindingEventZ,
      WebhookTestEventZ
    ]);
    MarkRangeSchemaZ = z4.object({
      from: z4.number(),
      to: z4.number()
    });
    MarkSchemaZ = z4.object({
      id: z4.string(),
      kind: z4.enum(["authored", "approved", "flagged", "comment", "insert", "delete", "replace"]),
      by: z4.string(),
      at: z4.string(),
      range: MarkRangeSchemaZ,
      quote: z4.string(),
      orphaned: z4.boolean().optional()
    });
    AuthorshipStatsSchemaZ = z4.object({
      aiPercent: z4.number(),
      humanPercent: z4.number(),
      totalChars: z4.number()
    });
    PlanStatusSchemaZ = z4.enum(["pending", "in-progress", "done", "archived"]);
    PlanMetaSchemaZ = z4.object({
      name: z4.string(),
      path: z4.string(),
      title: z4.string(),
      status: PlanStatusSchemaZ,
      effort: z4.string().optional(),
      gate: z4.string().optional(),
      completed: z4.string().optional()
    });
    PaneInfoSchemaZ = z4.object({
      id: z4.string(),
      index: z4.number(),
      title: z4.string(),
      currentCommand: z4.string(),
      width: z4.number(),
      height: z4.number(),
      active: z4.boolean(),
      role: z4.enum(["lead", "teammate", "planner", "validator", "researcher", "widget", "shell"]).nullable(),
      name: z4.string().nullable(),
      type: z4.string().nullable()
    });
    AgentDetailSchemaZ = z4.object({
      paneTitle: z4.string(),
      paneId: z4.string(),
      isBusy: z4.boolean(),
      taskTitle: z4.string().nullable(),
      taskId: z4.string().nullable(),
      elapsed: z4.string()
    });
    SessionStatsSchemaZ = z4.object({
      totalTasks: z4.number(),
      doneTasks: z4.number(),
      agents: z4.number(),
      activeAgents: z4.number()
    });
    SessionOverviewSchemaZ = z4.object({
      name: z4.string(),
      dir: z4.string(),
      mission: MissionSchemaZ.nullable(),
      stats: SessionStatsSchemaZ,
      goals: z4.array(
        z4.object({
          id: z4.string(),
          title: z4.string(),
          progress: z4.number()
        })
      )
    });
    ProjectDetailSchemaZ = z4.object({
      session: z4.string(),
      dir: z4.string(),
      mission: MissionSchemaZ.nullable(),
      goals: z4.array(GoalSchemaZ),
      tasks: z4.array(TaskSchemaZ),
      agents: z4.array(AgentDetailSchemaZ)
    });
    AgentToolSchemaZ = z4.enum(["claude", "codex", "unknown"]);
    AgentKindSchemaZ = z4.enum(["managed", "tmux-unmanaged", "external"]);
    AgentStatusSchemaZ = z4.enum(["busy", "idle", "offline"]);
    AgentRecordSchemaZ = z4.object({
      // Stable id. tmux agents (local): `${session}:${paneId}`; HQ namespaces
      // remote ids with `${machineId}:` during aggregation. external agents: the
      // hook-supplied id (typically the Claude session id). Bounds matter because
      // HQ validates untrusted remote responses against this same schema.
      id: z4.string().max(512),
      kind: AgentKindSchemaZ,
      tool: AgentToolSchemaZ,
      name: z4.string().max(256),
      status: AgentStatusSchemaZ,
      session: z4.string().max(256).nullable(),
      paneId: z4.string().max(64).nullable(),
      paneTitle: z4.string().max(512).nullable(),
      cwd: z4.string().max(4096).nullable(),
      taskId: z4.string().max(128).nullable(),
      taskTitle: z4.string().max(512).nullable(),
      pid: z4.number().nullable(),
      // ISO timestamp of last observed activity / heartbeat.
      lastActivity: z4.string().max(64),
      // null = this (local) machine; set by HQ when aggregating remotes.
      machineId: z4.string().max(256).nullable(),
      machineName: z4.string().max(256).nullable()
    });
    AgentRegisterPayloadSchemaZ = z4.object({
      id: z4.string().min(1).max(256),
      tool: AgentToolSchemaZ.default("claude"),
      name: z4.string().max(256).optional(),
      cwd: z4.string().max(4096).optional(),
      session: z4.string().max(256).optional(),
      pid: z4.number().int().positive().optional(),
      status: AgentStatusSchemaZ.optional(),
      taskTitle: z4.string().max(512).optional()
    });
    AgentHeartbeatPayloadSchemaZ = z4.object({
      id: z4.string().min(1).max(256),
      status: AgentStatusSchemaZ.optional(),
      taskTitle: z4.string().max(512).optional()
    });
    AgentListSchemaZ = z4.object({
      agents: z4.array(AgentRecordSchemaZ)
    });
  }
});

// packages/contracts/src/tmux.ts
import { z as z5 } from "zod";
var TmuxPaneSchemaZ, TmuxWindowSchemaZ, TmuxSessionSchemaZ, TmuxPaneTargetSchemaZ;
var init_tmux = __esm({
  "packages/contracts/src/tmux.ts"() {
    "use strict";
    TmuxPaneSchemaZ = z5.object({
      /** Stable tmux pane id (e.g. `%23`). */
      id: z5.string(),
      /** Pane index within its window (zero-based). */
      paneIndex: z5.number().int().nonnegative(),
      /** Window index within the session (zero-based). */
      windowIndex: z5.number().int().nonnegative(),
      title: z5.string().nullable(),
      command: z5.string().nullable(),
      active: z5.boolean()
    });
    TmuxWindowSchemaZ = z5.object({
      index: z5.number().int().nonnegative(),
      name: z5.string(),
      panes: z5.array(TmuxPaneSchemaZ)
    });
    TmuxSessionSchemaZ = z5.object({
      name: z5.string(),
      windows: z5.array(TmuxWindowSchemaZ),
      /** Session creation time (epoch milliseconds). */
      created: z5.number().int().nonnegative(),
      attached: z5.boolean(),
      /** Project directory the session was launched from, when known. */
      projectDir: z5.string().nullable()
    });
    TmuxPaneTargetSchemaZ = z5.discriminatedUnion("kind", [
      z5.object({ kind: z5.literal("byId"), id: z5.string() }),
      z5.object({ kind: z5.literal("byIndex"), index: z5.number().int().nonnegative() }),
      z5.object({ kind: z5.literal("byTitle"), title: z5.string() }),
      z5.object({ kind: z5.literal("byRole"), role: z5.string() })
    ]);
  }
});

// packages/contracts/src/workspace.ts
import { z as z6 } from "zod";
var WorkspaceSchemaZ, WorkspaceListResponseSchemaZ, AddWorkspaceRequestSchemaZ, AddWorkspaceResponseSchemaZ, WorkspaceAddedFrameSchemaZ, WorkspaceRemovedFrameSchemaZ;
var init_workspace = __esm({
  "packages/contracts/src/workspace.ts"() {
    "use strict";
    WorkspaceSchemaZ = z6.object({
      /** Stable workspace name (typically equal to tmux session name). */
      name: z6.string().min(1),
      /** Tmux session this workspace maps to. */
      sessionName: z6.string().min(1),
      /** Absolute project directory. */
      projectDir: z6.string().min(1),
      /** Absolute path to the ide.yml driving this workspace. */
      ideConfigPath: z6.string().nullable(),
      /** ISO timestamp of when the workspace was added. */
      addedAt: z6.string()
    });
    WorkspaceListResponseSchemaZ = z6.object({
      workspaces: z6.array(WorkspaceSchemaZ)
    });
    AddWorkspaceRequestSchemaZ = z6.object({
      /** Absolute path to the project directory. */
      projectDir: z6.string().min(1),
      /** Optional explicit workspace name. Auto-derived from basename when absent. */
      name: z6.string().min(1).optional(),
      /** Optional override for the tmux session name (defaults to `name`). */
      sessionName: z6.string().min(1).optional(),
      /** Optional ide.yml path the workspace was launched with. */
      ideConfigPath: z6.string().min(1).optional()
    });
    AddWorkspaceResponseSchemaZ = z6.object({
      workspace: WorkspaceSchemaZ
    });
    WorkspaceAddedFrameSchemaZ = z6.object({
      type: z6.literal("workspace.added"),
      workspace: WorkspaceSchemaZ
    });
    WorkspaceRemovedFrameSchemaZ = z6.object({
      type: z6.literal("workspace.removed"),
      name: z6.string()
    });
  }
});

// packages/contracts/src/chat-thread.ts
import { z as z7 } from "zod";
var ThreadIdZ, TurnIdZ, CheckpointRefZ, MessageIdZ, EventIdZ, SessionIdZ, ProjectIdZ, IsoDateTimeZ, NonNegativeIntZ, TrimmedNonEmptyStringZ, ProposedPlanIdZ, SessionStatusZ, CheckpointStatusZ, ThreadActivityToneZ, LatestTurnStateZ, MessageRoleZ, RuntimeModeZ, DEFAULT_RUNTIME_MODE, InteractionModeZ, DEFAULT_INTERACTION_MODE, SessionRoleZ, ModelSelectionZ, CHAT_ATTACHMENT_ID_MAX_CHARS, PROVIDER_SEND_TURN_MAX_IMAGE_BYTES, ChatImageAttachmentZ, ChatAttachmentZ, MessageZ, ProposedPlanRejectionZ, ProposedPlanZ, SourceProposedPlanReferenceZ, SessionZ, CheckpointFileZ, CheckpointSummaryZ, ThreadActivityZ, LatestTurnZ, ThreadZ, TurnAbortReasonZ, ThreadActivityAppendedEventZ, TurnStartedEventZ, TurnCompletedEventZ, TurnAbortedEventZ, PlanUpsertedEventZ, CheckpointCreatedEventZ, ThreadRevertedEventZ, SessionAddedEventZ, SessionRemovedEventZ, SessionStatusChangedEventZ, ChatThreadEventZ, CHAT_THREAD_EVENT_TYPES, ProviderKindZ, ProviderInstanceIdZ, NonEmptyTokenZ, AnthropicProviderConfigZ, OpenAIProviderConfigZ, LocalOllamaProviderConfigZ, LocalLmStudioProviderConfigZ, GenericAcpProviderConfigZ, ProviderConfigZ, ProviderInstanceZ, ProvidersFileZ, ProviderInstanceSummaryZ, ProviderTokenUsageZ;
var init_chat_thread = __esm({
  "packages/contracts/src/chat-thread.ts"() {
    "use strict";
    ThreadIdZ = z7.string();
    TurnIdZ = z7.string();
    CheckpointRefZ = z7.string();
    MessageIdZ = z7.string();
    EventIdZ = z7.string();
    SessionIdZ = z7.string();
    ProjectIdZ = z7.string();
    IsoDateTimeZ = z7.string();
    NonNegativeIntZ = z7.number().int().nonnegative();
    TrimmedNonEmptyStringZ = z7.string().trim().min(1);
    ProposedPlanIdZ = TrimmedNonEmptyStringZ;
    SessionStatusZ = z7.enum([
      "idle",
      "starting",
      "running",
      "ready",
      "interrupted",
      "stopped",
      "error"
    ]);
    CheckpointStatusZ = z7.enum(["ready", "missing", "error"]);
    ThreadActivityToneZ = z7.enum(["info", "tool", "approval", "error"]);
    LatestTurnStateZ = z7.enum(["running", "interrupted", "completed", "error"]);
    MessageRoleZ = z7.enum(["user", "assistant", "system"]);
    RuntimeModeZ = z7.enum(["approval-required", "auto-accept-edits", "full-access"]);
    DEFAULT_RUNTIME_MODE = "full-access";
    InteractionModeZ = z7.enum(["default", "plan"]);
    DEFAULT_INTERACTION_MODE = "default";
    SessionRoleZ = z7.enum(["lead", "teammate", "planner", "validator", "researcher"]);
    ModelSelectionZ = z7.object({
      instanceId: TrimmedNonEmptyStringZ,
      model: TrimmedNonEmptyStringZ,
      options: z7.record(z7.string(), z7.unknown()).optional()
    });
    CHAT_ATTACHMENT_ID_MAX_CHARS = 128;
    PROVIDER_SEND_TURN_MAX_IMAGE_BYTES = 10 * 1024 * 1024;
    ChatImageAttachmentZ = z7.object({
      type: z7.literal("image"),
      id: TrimmedNonEmptyStringZ.max(CHAT_ATTACHMENT_ID_MAX_CHARS).regex(/^[a-z0-9_-]+$/i),
      name: TrimmedNonEmptyStringZ.max(255),
      mimeType: TrimmedNonEmptyStringZ.max(100).regex(/^image\//i),
      sizeBytes: NonNegativeIntZ.max(PROVIDER_SEND_TURN_MAX_IMAGE_BYTES)
    });
    ChatAttachmentZ = z7.discriminatedUnion("type", [ChatImageAttachmentZ]);
    MessageZ = z7.object({
      id: MessageIdZ,
      role: MessageRoleZ,
      text: z7.string(),
      attachments: z7.array(ChatAttachmentZ).optional(),
      turnId: TurnIdZ.nullable(),
      streaming: z7.boolean(),
      createdAt: IsoDateTimeZ,
      updatedAt: IsoDateTimeZ
    });
    ProposedPlanRejectionZ = z7.object({
      at: IsoDateTimeZ,
      reason: z7.string().optional()
    });
    ProposedPlanZ = z7.object({
      id: ProposedPlanIdZ,
      turnId: TurnIdZ.nullable(),
      planMarkdown: TrimmedNonEmptyStringZ,
      implementedAt: IsoDateTimeZ.nullable().default(null),
      implementationThreadId: ThreadIdZ.nullable().default(null),
      rejected: ProposedPlanRejectionZ.optional(),
      createdAt: IsoDateTimeZ,
      updatedAt: IsoDateTimeZ
    });
    SourceProposedPlanReferenceZ = z7.object({
      threadId: ThreadIdZ,
      planId: ProposedPlanIdZ
    });
    SessionZ = z7.object({
      id: SessionIdZ.optional(),
      threadId: ThreadIdZ,
      status: SessionStatusZ,
      providerName: TrimmedNonEmptyStringZ.nullable(),
      providerInstanceId: TrimmedNonEmptyStringZ.optional(),
      runtimeMode: RuntimeModeZ.default(DEFAULT_RUNTIME_MODE),
      role: SessionRoleZ.optional(),
      displayName: TrimmedNonEmptyStringZ.optional(),
      activeTurnId: TurnIdZ.nullable(),
      lastError: TrimmedNonEmptyStringZ.nullable(),
      updatedAt: IsoDateTimeZ
    });
    CheckpointFileZ = z7.object({
      path: TrimmedNonEmptyStringZ,
      kind: TrimmedNonEmptyStringZ,
      additions: NonNegativeIntZ,
      deletions: NonNegativeIntZ
    });
    CheckpointSummaryZ = z7.object({
      turnId: TurnIdZ,
      checkpointTurnCount: NonNegativeIntZ,
      checkpointRef: CheckpointRefZ,
      status: CheckpointStatusZ,
      files: z7.array(CheckpointFileZ),
      assistantMessageId: MessageIdZ.nullable(),
      completedAt: IsoDateTimeZ
    });
    ThreadActivityZ = z7.object({
      id: EventIdZ,
      tone: ThreadActivityToneZ,
      kind: TrimmedNonEmptyStringZ,
      summary: TrimmedNonEmptyStringZ,
      payload: z7.unknown(),
      turnId: TurnIdZ.nullable(),
      /**
       * Multi-agent attribution: when a Thread hosts multiple Sessions, the
       * activity stream is interleaved by `sequence` but each entry carries
       * the Session that produced it so the dashboard can render lanes /
       * color-coded chips per agent. Optional for back-compat with single
       * session threads from before T078.
       */
      sessionId: SessionIdZ.optional(),
      sequence: NonNegativeIntZ.optional(),
      createdAt: IsoDateTimeZ
    });
    LatestTurnZ = z7.object({
      turnId: TurnIdZ,
      state: LatestTurnStateZ,
      requestedAt: IsoDateTimeZ,
      startedAt: IsoDateTimeZ.nullable(),
      completedAt: IsoDateTimeZ.nullable(),
      assistantMessageId: MessageIdZ.nullable(),
      /** Owning session when the Thread has multi-agent sessions (T078). */
      sessionId: SessionIdZ.optional(),
      sourceProposedPlan: SourceProposedPlanReferenceZ.optional()
    });
    ThreadZ = z7.object({
      id: ThreadIdZ,
      projectId: ProjectIdZ,
      title: TrimmedNonEmptyStringZ,
      modelSelection: ModelSelectionZ,
      runtimeMode: RuntimeModeZ,
      interactionMode: InteractionModeZ.default(DEFAULT_INTERACTION_MODE),
      branch: TrimmedNonEmptyStringZ.nullable(),
      worktreePath: TrimmedNonEmptyStringZ.nullable(),
      latestTurn: LatestTurnZ.nullable(),
      createdAt: IsoDateTimeZ,
      updatedAt: IsoDateTimeZ,
      archivedAt: IsoDateTimeZ.nullable().default(null),
      deletedAt: IsoDateTimeZ.nullable(),
      messages: z7.array(MessageZ),
      proposedPlans: z7.array(ProposedPlanZ).default([]),
      activities: z7.array(ThreadActivityZ),
      checkpoints: z7.array(CheckpointSummaryZ),
      /**
       * @deprecated since T078 — prefer `sessions[]`. Retained so legacy
       * single-session wire shapes still parse; the canonical multi-agent
       * representation is the `sessions` array below. Consumers that only
       * need a single session can read `sessions[0]` (the compat shim).
       */
      session: SessionZ.nullable(),
      /**
       * Multi-agent (T078): a Thread can host multiple concurrent Sessions,
       * each backed by its own provider/runtimeMode/activeTurnId. Default is
       * empty so previously-persisted Threads continue to round-trip; new
       * Threads should always populate at least one Session.
       */
      sessions: z7.array(SessionZ).default([])
    });
    TurnAbortReasonZ = z7.enum(["cancelled", "interrupted", "error"]);
    ThreadActivityAppendedEventZ = z7.object({
      type: z7.literal("chat.activity.appended"),
      threadId: ThreadIdZ,
      activity: ThreadActivityZ,
      seq: NonNegativeIntZ
    });
    TurnStartedEventZ = z7.object({
      type: z7.literal("chat.turn.started"),
      threadId: ThreadIdZ,
      turnId: TurnIdZ,
      requestedAt: IsoDateTimeZ,
      sourceProposedPlanRef: SourceProposedPlanReferenceZ.optional()
    });
    TurnCompletedEventZ = z7.object({
      type: z7.literal("chat.turn.completed"),
      threadId: ThreadIdZ,
      turnId: TurnIdZ,
      state: LatestTurnStateZ,
      completedAt: IsoDateTimeZ,
      assistantMessageId: MessageIdZ.optional()
    });
    TurnAbortedEventZ = z7.object({
      type: z7.literal("chat.turn.aborted"),
      threadId: ThreadIdZ,
      turnId: TurnIdZ,
      reason: TurnAbortReasonZ
    });
    PlanUpsertedEventZ = z7.object({
      type: z7.literal("chat.plan.upserted"),
      threadId: ThreadIdZ,
      plan: ProposedPlanZ
    });
    CheckpointCreatedEventZ = z7.object({
      type: z7.literal("chat.checkpoint.created"),
      threadId: ThreadIdZ,
      checkpoint: CheckpointSummaryZ
    });
    ThreadRevertedEventZ = z7.object({
      type: z7.literal("chat.thread.reverted"),
      threadId: ThreadIdZ,
      toCheckpointRef: CheckpointRefZ
    });
    SessionAddedEventZ = z7.object({
      type: z7.literal("chat.session.added"),
      threadId: ThreadIdZ,
      session: SessionZ
    });
    SessionRemovedEventZ = z7.object({
      type: z7.literal("chat.session.removed"),
      threadId: ThreadIdZ,
      sessionId: SessionIdZ
    });
    SessionStatusChangedEventZ = z7.object({
      type: z7.literal("chat.session.status-changed"),
      threadId: ThreadIdZ,
      sessionId: SessionIdZ,
      status: SessionStatusZ,
      lastError: TrimmedNonEmptyStringZ.nullable().optional(),
      activeTurnId: TurnIdZ.nullable().optional()
    });
    ChatThreadEventZ = z7.discriminatedUnion("type", [
      ThreadActivityAppendedEventZ,
      TurnStartedEventZ,
      TurnCompletedEventZ,
      TurnAbortedEventZ,
      PlanUpsertedEventZ,
      CheckpointCreatedEventZ,
      ThreadRevertedEventZ,
      SessionAddedEventZ,
      SessionRemovedEventZ,
      SessionStatusChangedEventZ
    ]);
    CHAT_THREAD_EVENT_TYPES = [
      "chat.activity.appended",
      "chat.turn.started",
      "chat.turn.completed",
      "chat.turn.aborted",
      "chat.plan.upserted",
      "chat.checkpoint.created",
      "chat.thread.reverted",
      "chat.session.added",
      "chat.session.removed",
      "chat.session.status-changed"
    ];
    ProviderKindZ = z7.enum([
      "anthropic",
      "openai",
      "local-ollama",
      "local-lmstudio",
      "generic-acp"
    ]);
    ProviderInstanceIdZ = TrimmedNonEmptyStringZ.regex(/^[a-z0-9_-]+$/i);
    NonEmptyTokenZ = TrimmedNonEmptyStringZ;
    AnthropicProviderConfigZ = z7.object({
      kind: z7.literal("anthropic"),
      apiKey: NonEmptyTokenZ,
      model: NonEmptyTokenZ,
      baseUrl: NonEmptyTokenZ.optional()
    });
    OpenAIProviderConfigZ = z7.object({
      kind: z7.literal("openai"),
      apiKey: NonEmptyTokenZ,
      model: NonEmptyTokenZ,
      baseUrl: NonEmptyTokenZ.optional(),
      organization: NonEmptyTokenZ.optional()
    });
    LocalOllamaProviderConfigZ = z7.object({
      kind: z7.literal("local-ollama"),
      baseUrl: NonEmptyTokenZ.default("http://127.0.0.1:11434"),
      model: NonEmptyTokenZ
    });
    LocalLmStudioProviderConfigZ = z7.object({
      kind: z7.literal("local-lmstudio"),
      baseUrl: NonEmptyTokenZ.default("http://127.0.0.1:1234/v1"),
      model: NonEmptyTokenZ,
      apiKey: NonEmptyTokenZ.optional()
      // LM Studio accepts any value but the field is required for some clients.
    });
    GenericAcpProviderConfigZ = z7.object({
      kind: z7.literal("generic-acp"),
      /** Path to the ACP-speaking binary. */
      binary: NonEmptyTokenZ,
      /** Extra args appended to the binary on spawn. */
      args: z7.array(NonEmptyTokenZ).default([])
    });
    ProviderConfigZ = z7.discriminatedUnion("kind", [
      AnthropicProviderConfigZ,
      OpenAIProviderConfigZ,
      LocalOllamaProviderConfigZ,
      LocalLmStudioProviderConfigZ,
      GenericAcpProviderConfigZ
    ]);
    ProviderInstanceZ = z7.object({
      id: ProviderInstanceIdZ,
      kind: ProviderKindZ,
      displayName: TrimmedNonEmptyStringZ,
      config: ProviderConfigZ,
      createdAt: IsoDateTimeZ.optional()
    });
    ProvidersFileZ = z7.object({
      version: z7.literal(1),
      providers: z7.array(ProviderInstanceZ)
    });
    ProviderInstanceSummaryZ = z7.object({
      id: ProviderInstanceIdZ,
      kind: ProviderKindZ,
      displayName: TrimmedNonEmptyStringZ,
      model: TrimmedNonEmptyStringZ.optional(),
      baseUrl: TrimmedNonEmptyStringZ.optional(),
      hasApiKey: z7.boolean(),
      createdAt: IsoDateTimeZ.optional()
    });
    ProviderTokenUsageZ = z7.object({
      providerInstanceId: ProviderInstanceIdZ,
      inputTokens: NonNegativeIntZ.default(0),
      outputTokens: NonNegativeIntZ.default(0),
      totalCostUsd: z7.number().nonnegative().optional()
    });
  }
});

// packages/contracts/src/routes.ts
import { z as z8 } from "zod";
var ValidationSummarySchemaZ, MissionResponseSchemaZ, ProjectFileNodeSchemaZ, ProjectFilesResponseSchemaZ, DiffFileEntrySchemaZ, DiffDataSchemaZ, FileDiffResponseSchemaZ, WidgetSpawnSpecSchemaZ, SessionsListResponseSchemaZ, ProjectEventSchemaZ, ProjectEventsResponseSchemaZ, RawMetricsAgentSchemaZ, RawMetricsResponseSchemaZ, ProjectNameParamsZ, ProjectFileParamsZ, WidgetSpawnQueryZ, WidgetSpawnParamsZ, ThreadIdParamsZ, ThreadPlanParamsZ, PlanListResponseZ, PlanApproveBodyZ, PlanApproveResponseZ, PlanRejectBodyZ, PlanRejectResponseZ, routes;
var init_routes = __esm({
  "packages/contracts/src/routes.ts"() {
    "use strict";
    init_domain();
    init_chat_thread();
    ValidationSummarySchemaZ = z8.object({
      total: z8.number(),
      passing: z8.number(),
      failing: z8.number(),
      pending: z8.number(),
      blocked: z8.number()
    });
    MissionResponseSchemaZ = z8.object({
      mission: MissionSchemaZ.passthrough(),
      validationSummary: ValidationSummarySchemaZ
    });
    ProjectFileNodeSchemaZ = z8.lazy(
      () => z8.object({
        path: z8.string(),
        name: z8.string(),
        isDirectory: z8.boolean(),
        children: z8.array(ProjectFileNodeSchemaZ).optional(),
        truncated: z8.literal(true).optional()
      })
    );
    ProjectFilesResponseSchemaZ = z8.object({
      tree: z8.array(ProjectFileNodeSchemaZ),
      maxDepth: z8.number(),
      truncated: z8.boolean()
    });
    DiffFileEntrySchemaZ = z8.object({
      file: z8.string(),
      additions: z8.number(),
      deletions: z8.number()
    });
    DiffDataSchemaZ = z8.object({
      diff: z8.string(),
      files: z8.array(DiffFileEntrySchemaZ)
    });
    FileDiffResponseSchemaZ = z8.object({
      file: z8.string(),
      diff: z8.string()
    });
    WidgetSpawnSpecSchemaZ = z8.object({
      cwd: z8.string(),
      cmd: z8.array(z8.string())
    });
    SessionsListResponseSchemaZ = z8.object({
      sessions: z8.array(SessionOverviewSchemaZ)
    });
    ProjectEventSchemaZ = z8.object({
      type: z8.string(),
      timestamp: z8.string(),
      relative: z8.string().optional(),
      message: z8.string().optional(),
      agent: z8.string().nullable().optional()
    });
    ProjectEventsResponseSchemaZ = z8.union([
      z8.array(ProjectEventSchemaZ),
      z8.object({ events: z8.array(ProjectEventSchemaZ) })
    ]);
    RawMetricsAgentSchemaZ = z8.object({
      name: z8.string(),
      totalTimeMs: z8.number(),
      activeTimeMs: z8.number(),
      taskCount: z8.number(),
      utilization: z8.number()
    });
    RawMetricsResponseSchemaZ = z8.object({
      session: z8.object({ startedAt: z8.string(), durationMs: z8.number() }),
      tasks: z8.object({ total: z8.number() }),
      agents: z8.array(RawMetricsAgentSchemaZ)
    });
    ProjectNameParamsZ = z8.object({ name: z8.string() });
    ProjectFileParamsZ = z8.object({
      name: z8.string(),
      file: z8.string()
    });
    WidgetSpawnQueryZ = z8.object({
      session: z8.string(),
      dir: z8.string(),
      target: z8.string().optional(),
      /** JSON-encoded theme blob — server JSON.parses if present. */
      theme: z8.string().optional()
    });
    WidgetSpawnParamsZ = z8.object({ name: z8.string() });
    ThreadIdParamsZ = z8.object({ threadId: z8.string().trim().min(1) });
    ThreadPlanParamsZ = z8.object({
      threadId: z8.string().trim().min(1),
      planId: z8.string().trim().min(1)
    });
    PlanListResponseZ = z8.object({
      plans: z8.array(ProposedPlanZ)
    });
    PlanApproveBodyZ = z8.object({
      runtimeMode: RuntimeModeZ.optional()
    });
    PlanApproveResponseZ = z8.object({
      plan: ProposedPlanZ,
      turnId: z8.string()
    });
    PlanRejectBodyZ = z8.object({
      reason: z8.string().trim().min(1).max(2e3).optional()
    });
    PlanRejectResponseZ = z8.object({
      plan: ProposedPlanZ
    });
    routes = {
      "sessions.list": {
        method: "GET",
        path: "/api/sessions",
        res: SessionsListResponseSchemaZ
      },
      "project.detail": {
        method: "GET",
        path: "/api/project/:name",
        params: ProjectNameParamsZ,
        res: ProjectDetailSchemaZ.passthrough(),
        nullableOn404: true
      },
      "project.mission": {
        method: "GET",
        path: "/api/project/:name/mission",
        params: ProjectNameParamsZ,
        res: MissionResponseSchemaZ,
        nullableOn404: true
      },
      "project.events": {
        method: "GET",
        path: "/api/project/:name/events",
        params: ProjectNameParamsZ,
        res: ProjectEventsResponseSchemaZ,
        nullableOn404: true
      },
      "project.files": {
        method: "GET",
        path: "/api/project/:name/files",
        params: ProjectNameParamsZ,
        res: ProjectFilesResponseSchemaZ,
        nullableOn404: true
      },
      "project.diff": {
        method: "GET",
        path: "/api/project/:name/diff",
        params: ProjectNameParamsZ,
        res: DiffDataSchemaZ,
        nullableOn404: true
      },
      "project.fileDiff": {
        method: "GET",
        path: "/api/project/:name/diff/:file",
        params: ProjectFileParamsZ,
        res: FileDiffResponseSchemaZ,
        nullableOn404: true
      },
      "project.metrics": {
        method: "GET",
        path: "/api/project/:name/metrics",
        params: ProjectNameParamsZ,
        res: RawMetricsResponseSchemaZ
      },
      "widget.spawn": {
        method: "GET",
        path: "/api/widget/:name/spawn",
        params: WidgetSpawnParamsZ,
        query: WidgetSpawnQueryZ,
        res: WidgetSpawnSpecSchemaZ
      },
      "threads.plans.list": {
        method: "GET",
        path: "/api/threads/:threadId/plans",
        params: ThreadIdParamsZ,
        res: PlanListResponseZ
      },
      "threads.plans.approve": {
        method: "POST",
        path: "/api/threads/:threadId/plans/:planId/approve",
        params: ThreadPlanParamsZ,
        body: PlanApproveBodyZ,
        res: PlanApproveResponseZ
      },
      "threads.plans.reject": {
        method: "POST",
        path: "/api/threads/:threadId/plans/:planId/reject",
        params: ThreadPlanParamsZ,
        body: PlanRejectBodyZ,
        res: PlanRejectResponseZ
      }
    };
  }
});

// packages/contracts/src/client.ts
var init_client = __esm({
  "packages/contracts/src/client.ts"() {
    "use strict";
    init_routes();
  }
});

// packages/contracts/src/ws-v3-protocol.ts
var init_ws_v3_protocol = __esm({
  "packages/contracts/src/ws-v3-protocol.ts"() {
    "use strict";
  }
});

// packages/contracts/src/chat-timeline.ts
import { z as z9 } from "zod";
var IsoDateTimeZ2, OpaqueBlockZ, TimelineToolCallZ, TimelinePlanEntryZ, TimelineUserMessageZ, TimelineAssistantMessageZ, TimelineMessageZ, TimelineRowZ, ChatResumeSubscribeZ;
var init_chat_timeline = __esm({
  "packages/contracts/src/chat-timeline.ts"() {
    "use strict";
    IsoDateTimeZ2 = z9.string().min(1);
    OpaqueBlockZ = z9.unknown();
    TimelineToolCallZ = z9.object({
      toolCallId: z9.string(),
      title: z9.string(),
      kind: z9.string().optional(),
      status: z9.enum(["pending", "in_progress", "completed", "failed"]),
      content: z9.array(OpaqueBlockZ),
      rawInput: z9.unknown().optional(),
      rawOutput: z9.unknown().optional()
    });
    TimelinePlanEntryZ = z9.object({
      content: z9.string(),
      status: z9.enum(["pending", "in_progress", "completed"]).optional(),
      priority: z9.enum(["low", "medium", "high"]).optional()
    });
    TimelineUserMessageZ = z9.object({
      id: z9.string(),
      role: z9.literal("user"),
      createdAt: IsoDateTimeZ2,
      content: z9.array(OpaqueBlockZ)
    });
    TimelineAssistantMessageZ = z9.object({
      id: z9.string(),
      role: z9.literal("assistant"),
      createdAt: IsoDateTimeZ2,
      completedAt: IsoDateTimeZ2.optional(),
      streaming: z9.boolean(),
      text: z9.string(),
      thoughtText: z9.string().optional(),
      toolCalls: z9.array(TimelineToolCallZ),
      // Loose: passes through ACP stop reasons verbatim.
      stopReason: z9.string().optional()
    });
    TimelineMessageZ = z9.discriminatedUnion("role", [
      TimelineUserMessageZ,
      TimelineAssistantMessageZ
    ]);
    TimelineRowZ = z9.discriminatedUnion("kind", [
      z9.object({
        kind: z9.literal("message"),
        id: z9.string(),
        createdAt: IsoDateTimeZ2,
        message: TimelineMessageZ,
        revertTurnCount: z9.number().int().nonnegative().optional()
      }),
      z9.object({
        kind: z9.literal("plan"),
        id: z9.string(),
        createdAt: IsoDateTimeZ2,
        entries: z9.array(TimelinePlanEntryZ)
      }),
      z9.object({
        kind: z9.literal("working"),
        id: z9.string(),
        createdAt: IsoDateTimeZ2
      })
    ]);
    ChatResumeSubscribeZ = z9.object({
      type: z9.literal("chat.subscribe"),
      threadId: z9.string().min(1),
      lastSeq: z9.number().int().nonnegative().optional()
    }).strict();
  }
});

// packages/contracts/src/actions-contract.ts
import { z as z10 } from "zod";
function isActionName(name) {
  return name in ActionContractsZ;
}
var ProjectScopeInputZ, ProofInputZ, SkillSchemaZ, ProjectOpenTerminalInputZ, ProjectOpenTerminalResultZ, ProjectLaunchInputZ, ProjectLaunchResultZ, ProjectStopInputZ, ProjectStopResultZ, ProjectRestartInputZ, ProjectRestartResultZ, ProjectActivateInputZ, ProjectActivateResultZ, TerminalRespawnInputZ, TerminalRespawnResultZ, TerminalStopInputZ, TerminalStopResultZ, TaskCreateInputZ, TaskCreateResultZ, TaskUpdateInputZ, TaskUpdateResultZ, TaskClaimInputZ, TaskClaimResultZ, TaskDoneInputZ, TaskDoneResultZ, TaskDeleteInputZ, TaskDeleteResultZ, GoalCreateInputZ, GoalCreateResultZ, GoalUpdateInputZ, GoalUpdateResultZ, GoalDoneInputZ, GoalDoneResultZ, GoalDeleteInputZ, GoalDeleteResultZ, MilestoneCreateInputZ, MilestoneCreateResultZ, MilestoneUpdateInputZ, MilestoneUpdateResultZ, MissionSetInputZ, MissionSetResultZ, MissionPlanCompleteInputZ, MissionPlanCompleteResultZ, MissionClearInputZ, MissionClearResultZ, SkillCreateInputZ, SkillCreateResultZ, SkillUpdateInputZ, SkillUpdateResultZ, SkillDeleteInputZ, SkillDeleteResultZ, ConfigSetInputZ, ConfigResultZ, ConfigAddPaneInputZ, ConfigAddPaneResultZ, ConfigRemovePaneInputZ, ConfigRemovePaneResultZ, ConfigAddRowInputZ, ConfigAddRowResultZ, ConfigEnableTeamInputZ, ConfigEnableTeamResultZ, ConfigDisableTeamInputZ, ConfigDisableTeamResultZ, AssertionEntryZ, ValidationReportZ, ValidationAssertInputZ, ValidationAssertResultZ, ValidationReportInputZ, ValidationReportResultZ, WebhookAddInputZ, WebhookAddResultZ, WebhookRemoveInputZ, WebhookRemoveResultZ, WebhookTestInputZ, WebhookTestResultZ, AppSetRemoteAccessInputZ, AppSetRemoteAccessResultZ, DaemonShutdownInputZ, DaemonShutdownResultZ, AgentRegisterInputZ, AgentRegisterResultZ, AgentHeartbeatInputZ, AgentHeartbeatResultZ, AgentUnregisterInputZ, AgentUnregisterResultZ, StopReasonZ, AgentProviderZ, ContentBlockZ, SessionUpdateZ, ChatThreadUsageSummaryZ, ThreadMessageZ, ThreadIndexEntryZ, ThreadStateZ, ChatThreadListInputZ, ChatThreadListResultZ, ProviderOptionSelectionZ, ProviderModelInfoZ, ChatProvidersListInputZ, ChatProvidersListResultZ, ChatThreadCreateInputZ, ChatThreadCreateResultZ, ChatThreadDeleteInputZ, ChatThreadDeleteResultZ, ChatThreadRenameInputZ, ChatThreadRenameResultZ, ChatThreadSetProviderInputZ, ChatThreadSetProviderResultZ, ChatThreadGetInputZ, ChatThreadGetResultZ, ChatThreadUsageInputZ, ChatThreadUsageResultZ, ChatSessionSendInputZ, ChatSessionSendResultZ, ChatSessionCancelInputZ, ChatSessionCancelResultZ, ChatSessionEditFromTurnInputZ, ChatSessionEditFromTurnResultZ, ChatPermissionRespondInputZ, ChatPermissionRespondResultZ, ChatContextCaptureTerminalInputZ, ChatContextCaptureTerminalResultZ, ActionContractsZ, ACTION_NAMES;
var init_actions_contract = __esm({
  "packages/contracts/src/actions-contract.ts"() {
    "use strict";
    init_chat_timeline();
    init_domain();
    init_ide_config();
    ProjectScopeInputZ = z10.object({
      /**
       * Optional project/session scope. Dashboard calls include this so task
       * mutations target the selected project; CLI-style callers may omit it
       * to use the command-center process cwd.
       */
      name: z10.string().min(1).optional(),
      sessionName: z10.string().min(1).optional()
    });
    ProofInputZ = z10.union([ProofSchemaZ, z10.string()]);
    SkillSchemaZ = z10.object({
      name: z10.string(),
      specialties: z10.array(z10.string()),
      role: z10.string(),
      description: z10.string(),
      body: z10.string()
    });
    ProjectOpenTerminalInputZ = z10.object({
      name: z10.string().min(1)
    });
    ProjectOpenTerminalResultZ = z10.object({
      sessionName: z10.string(),
      cwd: z10.string().min(1),
      terminalTabId: z10.string(),
      /**
       * `true` when the dispatcher had to launch the tmux session as part of
       * resolving the terminal. `false` when the session was already running.
       */
      launched: z10.boolean()
    });
    ProjectLaunchInputZ = z10.object({
      name: z10.string().min(1)
    });
    ProjectLaunchResultZ = z10.object({
      sessionName: z10.string(),
      /**
       * `false` when the session was already running (idempotent no-op),
       * `true` when this call started a fresh session.
       */
      started: z10.boolean()
    });
    ProjectStopInputZ = z10.object({
      name: z10.string().min(1)
    });
    ProjectStopResultZ = z10.object({
      sessionName: z10.string(),
      /**
       * `false` when no session was running (idempotent no-op),
       * `true` when this call killed a session.
       */
      stopped: z10.boolean()
    });
    ProjectRestartInputZ = z10.object({
      name: z10.string().min(1)
    });
    ProjectRestartResultZ = z10.object({
      sessionName: z10.string(),
      restarted: z10.literal(true)
    });
    ProjectActivateInputZ = z10.object({
      name: z10.string().min(1),
      orchestrate: z10.boolean().optional()
    });
    ProjectActivateResultZ = z10.object({
      active: z10.boolean(),
      projectName: z10.string()
    });
    TerminalRespawnInputZ = z10.object({
      sessionName: z10.string().min(1),
      terminalId: z10.string().min(1),
      /**
       * Optional cwd override. Omit to respawn at the bridge's current cwd
       * (re-using the `lastCwd` recorded by the PTY bridge).
       */
      cwd: z10.string().min(1).optional()
    });
    TerminalRespawnResultZ = z10.object({
      respawned: z10.literal(true),
      cwd: z10.string().min(1)
    });
    TerminalStopInputZ = z10.object({
      sessionName: z10.string().min(1),
      terminalId: z10.string().min(1)
    });
    TerminalStopResultZ = z10.object({
      stopped: z10.literal(true)
    });
    TaskCreateInputZ = ProjectScopeInputZ.extend({
      title: z10.string().min(1),
      goalId: z10.string().min(1).optional(),
      priority: z10.number().int().positive().optional(),
      assign: z10.string().min(1).optional(),
      tags: z10.array(z10.string()).optional(),
      depends: z10.array(z10.string()).optional(),
      description: z10.string().optional(),
      milestone: z10.string().nullable().optional(),
      specialty: z10.string().nullable().optional(),
      fulfills: z10.array(z10.string()).optional()
    });
    TaskCreateResultZ = z10.object({
      taskId: z10.string(),
      task: TaskSchemaZ
    });
    TaskUpdateInputZ = ProjectScopeInputZ.extend({
      taskId: z10.string().min(1),
      status: z10.enum(["todo", "in-progress", "review", "done"]).optional(),
      proof: ProofInputZ.optional(),
      title: z10.string().optional(),
      description: z10.string().optional(),
      priority: z10.number().int().positive().optional(),
      assign: z10.string().nullable().optional(),
      assignee: z10.string().nullable().optional(),
      goalId: z10.string().nullable().optional(),
      tags: z10.array(z10.string()).optional(),
      depends: z10.array(z10.string()).optional(),
      milestone: z10.string().nullable().optional(),
      specialty: z10.string().nullable().optional(),
      fulfills: z10.array(z10.string()).optional(),
      summary: z10.string().nullable().optional()
    });
    TaskUpdateResultZ = z10.object({
      task: TaskSchemaZ
    });
    TaskClaimInputZ = ProjectScopeInputZ.extend({
      taskId: z10.string().min(1),
      assign: z10.string().min(1)
    });
    TaskClaimResultZ = z10.object({
      task: TaskSchemaZ
    });
    TaskDoneInputZ = ProjectScopeInputZ.extend({
      taskId: z10.string().min(1),
      proof: ProofInputZ.optional()
    });
    TaskDoneResultZ = z10.object({
      task: TaskSchemaZ
    });
    TaskDeleteInputZ = ProjectScopeInputZ.extend({
      taskId: z10.string().min(1)
    });
    TaskDeleteResultZ = z10.object({
      deleted: z10.literal(true)
    });
    GoalCreateInputZ = ProjectScopeInputZ.extend({
      title: z10.string().min(1),
      priority: z10.number().int().positive().optional(),
      acceptance: z10.string().optional(),
      description: z10.string().optional(),
      milestone: z10.string().nullable().optional(),
      specialty: z10.string().nullable().optional()
    });
    GoalCreateResultZ = z10.object({
      goalId: z10.string(),
      goal: GoalSchemaZ
    });
    GoalUpdateInputZ = ProjectScopeInputZ.extend({
      goalId: z10.string().min(1),
      status: z10.enum(["todo", "in-progress", "done"]).optional(),
      title: z10.string().optional(),
      description: z10.string().optional(),
      acceptance: z10.string().optional(),
      priority: z10.number().int().positive().optional(),
      milestone: z10.string().nullable().optional(),
      specialty: z10.string().nullable().optional(),
      assign: z10.string().nullable().optional()
    });
    GoalUpdateResultZ = z10.object({
      goal: GoalSchemaZ
    });
    GoalDoneInputZ = ProjectScopeInputZ.extend({
      goalId: z10.string().min(1)
    });
    GoalDoneResultZ = z10.object({
      goal: GoalSchemaZ
    });
    GoalDeleteInputZ = ProjectScopeInputZ.extend({
      goalId: z10.string().min(1)
    });
    GoalDeleteResultZ = z10.object({
      deleted: z10.literal(true)
    });
    MilestoneCreateInputZ = ProjectScopeInputZ.extend({
      title: z10.string().min(1),
      sequence: z10.number().int().positive().optional(),
      description: z10.string().optional()
    });
    MilestoneCreateResultZ = z10.object({
      milestoneId: z10.string(),
      milestone: MilestoneSchemaZ
    });
    MilestoneUpdateInputZ = ProjectScopeInputZ.extend({
      milestoneId: z10.string().min(1),
      status: z10.enum(["locked", "active", "done", "validating"]).optional()
    });
    MilestoneUpdateResultZ = z10.object({
      milestone: MilestoneSchemaZ
    });
    MissionSetInputZ = ProjectScopeInputZ.extend({
      title: z10.string().min(1),
      description: z10.string().optional()
    });
    MissionSetResultZ = z10.object({
      mission: MissionSchemaZ
    });
    MissionPlanCompleteInputZ = ProjectScopeInputZ;
    MissionPlanCompleteResultZ = z10.object({
      mission: MissionSchemaZ
    });
    MissionClearInputZ = ProjectScopeInputZ;
    MissionClearResultZ = z10.object({
      cleared: z10.literal(true)
    });
    SkillCreateInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      name: z10.string().min(1),
      content: z10.string().min(1)
    });
    SkillCreateResultZ = z10.object({
      skill: SkillSchemaZ
    });
    SkillUpdateInputZ = SkillCreateInputZ;
    SkillUpdateResultZ = SkillCreateResultZ;
    SkillDeleteInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      name: z10.string().min(1)
    });
    SkillDeleteResultZ = z10.object({
      deleted: z10.literal(true)
    });
    ConfigSetInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      path: z10.string().min(1),
      value: z10.unknown()
    });
    ConfigResultZ = z10.object({
      config: IdeConfigSchema
    });
    ConfigAddPaneInputZ = PaneSchema.partial().extend({
      projectName: z10.string().min(1).optional(),
      rowIndex: z10.number().int().min(0)
    });
    ConfigAddPaneResultZ = ConfigResultZ;
    ConfigRemovePaneInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      rowIndex: z10.number().int().min(0),
      paneIndex: z10.number().int().min(0)
    });
    ConfigRemovePaneResultZ = ConfigResultZ;
    ConfigAddRowInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      size: z10.string().optional()
    });
    ConfigAddRowResultZ = ConfigResultZ;
    ConfigEnableTeamInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      name: z10.string().min(1).optional()
    });
    ConfigEnableTeamResultZ = ConfigResultZ;
    ConfigDisableTeamInputZ = z10.object({
      projectName: z10.string().min(1).optional()
    });
    ConfigDisableTeamResultZ = ConfigResultZ;
    AssertionEntryZ = z10.object({
      id: z10.string(),
      status: z10.enum(["pending", "passing", "failing", "blocked"]),
      verifiedBy: z10.string().nullable(),
      verifiedAt: z10.string().nullable(),
      evidence: z10.string().nullable(),
      blockedBy: z10.string().nullable()
    });
    ValidationReportZ = z10.object({
      total: z10.number(),
      passing: z10.number(),
      failing: z10.number(),
      pending: z10.number(),
      blocked: z10.number()
    });
    ValidationAssertInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      assertId: z10.string().min(1),
      status: z10.enum(["pending", "passing", "failing", "blocked"]),
      evidence: z10.string().optional()
    });
    ValidationAssertResultZ = z10.object({
      assertion: AssertionEntryZ
    });
    ValidationReportInputZ = z10.object({
      projectName: z10.string().min(1).optional()
    });
    ValidationReportResultZ = z10.object({
      report: ValidationReportZ
    });
    WebhookAddInputZ = WebhookConfigSchema.extend({
      projectName: z10.string().min(1).optional()
    });
    WebhookAddResultZ = z10.object({
      webhookId: z10.string(),
      webhook: WebhookConfigSchema
    });
    WebhookRemoveInputZ = z10.object({
      projectName: z10.string().min(1).optional(),
      webhookId: z10.string().min(1)
    });
    WebhookRemoveResultZ = z10.object({
      deleted: z10.literal(true)
    });
    WebhookTestInputZ = WebhookRemoveInputZ;
    WebhookTestResultZ = z10.object({
      status: z10.number(),
      ok: z10.literal(true)
    });
    AppSetRemoteAccessInputZ = z10.object({
      enabled: z10.boolean()
    });
    AppSetRemoteAccessResultZ = z10.object({
      enabled: z10.boolean(),
      url: z10.string().nullable(),
      token: z10.string().nullable(),
      qrPayload: z10.string().nullable()
    });
    DaemonShutdownInputZ = z10.object({
      reason: z10.string().optional()
    });
    DaemonShutdownResultZ = z10.object({
      stopping: z10.literal(true)
    });
    AgentRegisterInputZ = AgentRegisterPayloadSchemaZ;
    AgentRegisterResultZ = z10.object({ ok: z10.literal(true) });
    AgentHeartbeatInputZ = AgentHeartbeatPayloadSchemaZ;
    AgentHeartbeatResultZ = z10.object({
      ok: z10.literal(true),
      // `false` when no agent with that id was registered (the hook should
      // re-register). The action still succeeds at the transport level.
      known: z10.boolean()
    });
    AgentUnregisterInputZ = z10.object({
      id: z10.string().min(1).max(256)
    });
    AgentUnregisterResultZ = z10.object({
      ok: z10.literal(true),
      removed: z10.boolean()
    });
    StopReasonZ = z10.enum([
      "end_turn",
      "max_tokens",
      "max_turn_requests",
      "refusal",
      "cancelled"
    ]);
    AgentProviderZ = z10.discriminatedUnion("kind", [
      z10.object({
        kind: z10.literal("claude-code"),
        binary: z10.string().optional(),
        model: z10.string().min(1).optional()
      }).strict(),
      z10.object({
        kind: z10.literal("codex"),
        binary: z10.string().optional(),
        model: z10.string().min(1).optional()
      }).strict(),
      z10.object({
        kind: z10.literal("gemini"),
        binary: z10.string().optional(),
        model: z10.string().min(1).optional()
      }).strict(),
      z10.object({
        kind: z10.literal("custom"),
        command: z10.string().min(1),
        args: z10.array(z10.string()),
        env: z10.record(z10.string(), z10.string()).optional(),
        model: z10.string().min(1).optional()
      }).strict()
    ]);
    ContentBlockZ = z10.discriminatedUnion("type", [
      z10.object({ type: z10.literal("text"), text: z10.string() }).passthrough(),
      z10.object({ type: z10.literal("image") }).passthrough(),
      z10.object({ type: z10.literal("audio") }).passthrough(),
      z10.object({ type: z10.literal("resource") }).passthrough(),
      z10.object({ type: z10.literal("resource_link") }).passthrough()
    ]);
    SessionUpdateZ = z10.object({ sessionUpdate: z10.string().min(1) }).passthrough();
    ChatThreadUsageSummaryZ = z10.object({
      inputTokens: z10.number().int().min(0),
      outputTokens: z10.number().int().min(0),
      cacheReadTokens: z10.number().int().min(0).optional(),
      cacheWriteTokens: z10.number().int().min(0).optional(),
      totalCostUsd: z10.number().min(0).optional(),
      contextWindowMaxTokens: z10.number().int().min(0).optional(),
      contextWindowUsedTokens: z10.number().int().min(0).optional()
    }).strict();
    ThreadMessageZ = z10.discriminatedUnion("_tag", [
      z10.object({
        _tag: z10.literal("UserPrompt"),
        id: z10.string().min(1),
        createdAt: z10.string().min(1),
        content: z10.array(ContentBlockZ)
      }),
      z10.object({
        _tag: z10.literal("AgentUpdate"),
        id: z10.string().min(1),
        createdAt: z10.string().min(1),
        update: SessionUpdateZ
      })
    ]);
    ThreadIndexEntryZ = z10.object({
      id: z10.string().min(1),
      title: z10.string(),
      createdAt: z10.string().min(1),
      updatedAt: z10.string().min(1),
      providerKind: z10.enum(["claude-code", "codex", "gemini", "custom"]),
      projectDir: z10.string().optional(),
      messageCount: z10.number().int().min(0),
      lastStopReason: StopReasonZ.optional()
    }).strict();
    ThreadStateZ = z10.object({
      id: z10.string().min(1),
      title: z10.string(),
      createdAt: z10.string().min(1),
      updatedAt: z10.string().min(1),
      provider: AgentProviderZ,
      projectDir: z10.string().optional(),
      acpSessionId: z10.string().optional(),
      usage: ChatThreadUsageSummaryZ.optional(),
      messages: z10.array(ThreadMessageZ)
    }).strict();
    ChatThreadListInputZ = z10.object({
      /**
       * Filter to threads whose `projectDir` matches. Pass the workspace
       * absolute path. Omit for the legacy global view (returns every
       * thread across every project).
       */
      projectDir: z10.string().optional()
    }).strict();
    ChatThreadListResultZ = z10.object({
      threads: z10.array(ThreadIndexEntryZ)
    }).strict();
    ProviderOptionSelectionZ = z10.object({
      id: z10.string().min(1),
      value: z10.union([z10.string().min(1), z10.boolean()])
    }).strict();
    ProviderModelInfoZ = z10.object({
      slug: z10.string().min(1),
      name: z10.string().min(1),
      description: z10.string().optional(),
      /**
       * Codex-style per-model capabilities mirroring t3's
       * `mapCodexModelCapabilities`
       * (`context/t3code/apps/server/src/provider/Layers/CodexProvider.ts:96`).
       * Picker renders the reasoning-effort select + fast-mode toggle
       * straight from this — present only when the daemon's
       * `provider-discovery` parses non-empty values from the live
       * `model/list` response.
       */
      capabilities: z10.object({
        reasoningEfforts: z10.array(z10.string().min(1)).optional(),
        defaultReasoningEffort: z10.string().min(1).optional(),
        supportsFastMode: z10.boolean().optional()
      }).strict().optional()
    }).strict();
    ChatProvidersListInputZ = z10.object({}).strict();
    ChatProvidersListResultZ = z10.object({
      providers: z10.array(
        z10.object({
          kind: z10.enum(["claude-code", "codex"]),
          name: z10.string().min(1),
          description: z10.string().min(1),
          available: z10.boolean(),
          binary: z10.string().optional(),
          version: z10.string().optional(),
          error: z10.string().optional(),
          /**
           * Real per-provider model list, surfaced by the daemon's
           * `provider-discovery`. Empty when the provider binary is
           * unavailable or when the discovery probe returned nothing
           * usable. The first entry is the recommended default.
           */
          models: z10.array(ProviderModelInfoZ).default([])
        }).strict()
      )
    }).strict();
    ChatThreadCreateInputZ = z10.object({
      provider: AgentProviderZ,
      projectDir: z10.string().optional(),
      title: z10.string().optional()
    }).strict();
    ChatThreadCreateResultZ = z10.object({
      thread: ThreadIndexEntryZ
    }).strict();
    ChatThreadDeleteInputZ = z10.object({ id: z10.string().min(1) }).strict();
    ChatThreadDeleteResultZ = z10.object({ deleted: z10.literal(true) }).strict();
    ChatThreadRenameInputZ = z10.object({
      id: z10.string().min(1),
      title: z10.string().min(1)
    }).strict();
    ChatThreadRenameResultZ = z10.object({ thread: ThreadIndexEntryZ }).strict();
    ChatThreadSetProviderInputZ = z10.object({
      id: z10.string().min(1),
      provider: AgentProviderZ
    }).strict();
    ChatThreadSetProviderResultZ = z10.object({ thread: ThreadIndexEntryZ }).strict();
    ChatThreadGetInputZ = z10.object({ id: z10.string().min(1) }).strict();
    ChatThreadGetResultZ = z10.object({ thread: ThreadStateZ, timeline: z10.array(TimelineRowZ) }).strict();
    ChatThreadUsageInputZ = z10.object({ id: z10.string().min(1) }).strict();
    ChatThreadUsageResultZ = z10.object({ usage: ChatThreadUsageSummaryZ.nullable() }).strict();
    ChatSessionSendInputZ = z10.object({
      threadId: z10.string().min(1),
      content: z10.array(ContentBlockZ).min(1),
      /**
       * Per-turn model override (superset-pattern flat `provider/model`,
       * see docs/learn-from-superset.md §2). The daemon writes this onto
       * the thread's provider record BEFORE dispatch, so picking a
       * different Codex / Claude Code model takes effect on the next
       * turn without any client-side reduction or session teardown.
       * Omit to use the thread's currently-stored selection.
       */
      model: z10.string().min(1).optional(),
      /**
       * Accepted for forward-compat with multi-instance providers; not
       * yet acted on. Today the daemon resolves the live client from
       * `thread.provider.kind` alone — instance routing is a future
       * additive change.
       */
      providerInstanceId: z10.string().min(1).optional(),
      /**
       * Per-turn driver kind (Step 3b — t3-mirror). When supplied, the
       * daemon dispatches THIS turn through this provider regardless of
       * `thread.provider.kind`. Lets the client own the "currently
       * visible" provider without waiting on a `chat.thread.setProvider`
       * round-trip (the persisted thread.provider is updated lazily,
       * fire-and-forget, for reload memory only).
       *
       * If the live client for this thread is bound to a different
       * kind, the daemon disposes it and respawns the right one before
       * dispatching.
       */
      provider: z10.object({ kind: z10.enum(["claude-code", "codex", "gemini", "custom"]) }).strict().optional(),
      /**
       * Per-turn provider options (Codex reasoning effort + fast-mode).
       * Canonical t3 array shape (`Array<{id, value}>`) so future
       * multi-option models migrate additively. Recognized keys today:
       *   - `reasoningEffort` (string: minimal | low | medium | high | xhigh)
       *   - `fastMode` (boolean — forwarded as `serviceTier: "fast"`)
       * Unknown ids are accepted but ignored by the daemon.
       */
      providerOptions: z10.array(ProviderOptionSelectionZ).optional()
    }).strict();
    ChatSessionSendResultZ = z10.object({
      accepted: z10.literal(true),
      promptId: z10.string().min(1)
    }).strict();
    ChatSessionCancelInputZ = z10.object({ threadId: z10.string().min(1) }).strict();
    ChatSessionCancelResultZ = z10.object({ cancelled: z10.literal(true) }).strict();
    ChatSessionEditFromTurnInputZ = z10.object({
      threadId: z10.string().min(1),
      userMessageId: z10.string().min(1),
      content: z10.array(ContentBlockZ).min(1)
    }).strict();
    ChatSessionEditFromTurnResultZ = z10.object({
      accepted: z10.literal(true),
      promptId: z10.string().min(1),
      truncatedCount: z10.number().int().nonnegative()
    }).strict();
    ChatPermissionRespondInputZ = z10.object({
      threadId: z10.string().min(1),
      requestId: z10.string().min(1),
      optionId: z10.string().min(1)
    }).strict();
    ChatPermissionRespondResultZ = z10.object({ responded: z10.literal(true) }).strict();
    ChatContextCaptureTerminalInputZ = z10.object({
      sessionName: z10.string().min(1),
      paneId: z10.string().min(1)
    }).strict();
    ChatContextCaptureTerminalResultZ = z10.object({
      pane: z10.object({
        id: z10.string().min(1),
        title: z10.string()
      }).strict(),
      content: z10.string(),
      capturedAt: z10.string().min(1)
    }).strict();
    ActionContractsZ = {
      "project.openTerminal": {
        input: ProjectOpenTerminalInputZ,
        result: ProjectOpenTerminalResultZ
      },
      "project.launch": {
        input: ProjectLaunchInputZ,
        result: ProjectLaunchResultZ
      },
      "project.stop": {
        input: ProjectStopInputZ,
        result: ProjectStopResultZ
      },
      "project.restart": {
        input: ProjectRestartInputZ,
        result: ProjectRestartResultZ
      },
      "project.activate": {
        input: ProjectActivateInputZ,
        result: ProjectActivateResultZ
      },
      "terminal.respawn": {
        input: TerminalRespawnInputZ,
        result: TerminalRespawnResultZ
      },
      "terminal.stop": {
        input: TerminalStopInputZ,
        result: TerminalStopResultZ
      },
      "task.create": {
        input: TaskCreateInputZ,
        result: TaskCreateResultZ
      },
      "task.update": {
        input: TaskUpdateInputZ,
        result: TaskUpdateResultZ
      },
      "task.claim": {
        input: TaskClaimInputZ,
        result: TaskClaimResultZ
      },
      "task.done": {
        input: TaskDoneInputZ,
        result: TaskDoneResultZ
      },
      "task.delete": {
        input: TaskDeleteInputZ,
        result: TaskDeleteResultZ
      },
      "goal.create": {
        input: GoalCreateInputZ,
        result: GoalCreateResultZ
      },
      "goal.update": {
        input: GoalUpdateInputZ,
        result: GoalUpdateResultZ
      },
      "goal.done": {
        input: GoalDoneInputZ,
        result: GoalDoneResultZ
      },
      "goal.delete": {
        input: GoalDeleteInputZ,
        result: GoalDeleteResultZ
      },
      "milestone.create": {
        input: MilestoneCreateInputZ,
        result: MilestoneCreateResultZ
      },
      "milestone.update": {
        input: MilestoneUpdateInputZ,
        result: MilestoneUpdateResultZ
      },
      "mission.set": {
        input: MissionSetInputZ,
        result: MissionSetResultZ
      },
      "mission.planComplete": {
        input: MissionPlanCompleteInputZ,
        result: MissionPlanCompleteResultZ
      },
      "mission.clear": {
        input: MissionClearInputZ,
        result: MissionClearResultZ
      },
      "skill.create": {
        input: SkillCreateInputZ,
        result: SkillCreateResultZ
      },
      "skill.update": {
        input: SkillUpdateInputZ,
        result: SkillUpdateResultZ
      },
      "skill.delete": {
        input: SkillDeleteInputZ,
        result: SkillDeleteResultZ
      },
      "config.set": {
        input: ConfigSetInputZ,
        result: ConfigResultZ
      },
      "config.addPane": {
        input: ConfigAddPaneInputZ,
        result: ConfigAddPaneResultZ
      },
      "config.removePane": {
        input: ConfigRemovePaneInputZ,
        result: ConfigRemovePaneResultZ
      },
      "config.addRow": {
        input: ConfigAddRowInputZ,
        result: ConfigAddRowResultZ
      },
      "config.enableTeam": {
        input: ConfigEnableTeamInputZ,
        result: ConfigEnableTeamResultZ
      },
      "config.disableTeam": {
        input: ConfigDisableTeamInputZ,
        result: ConfigDisableTeamResultZ
      },
      "validation.assert": {
        input: ValidationAssertInputZ,
        result: ValidationAssertResultZ
      },
      "validation.report": {
        input: ValidationReportInputZ,
        result: ValidationReportResultZ
      },
      "webhook.add": {
        input: WebhookAddInputZ,
        result: WebhookAddResultZ
      },
      "webhook.remove": {
        input: WebhookRemoveInputZ,
        result: WebhookRemoveResultZ
      },
      "webhook.test": {
        input: WebhookTestInputZ,
        result: WebhookTestResultZ
      },
      "app.setRemoteAccess": {
        input: AppSetRemoteAccessInputZ,
        result: AppSetRemoteAccessResultZ
      },
      "daemon.shutdown": {
        input: DaemonShutdownInputZ,
        result: DaemonShutdownResultZ
      },
      "agent.register": {
        input: AgentRegisterInputZ,
        result: AgentRegisterResultZ
      },
      "agent.heartbeat": {
        input: AgentHeartbeatInputZ,
        result: AgentHeartbeatResultZ
      },
      "agent.unregister": {
        input: AgentUnregisterInputZ,
        result: AgentUnregisterResultZ
      },
      "chat.thread.list": {
        input: ChatThreadListInputZ,
        result: ChatThreadListResultZ
      },
      "chat.providers.list": {
        input: ChatProvidersListInputZ,
        result: ChatProvidersListResultZ
      },
      "chat.thread.create": {
        input: ChatThreadCreateInputZ,
        result: ChatThreadCreateResultZ
      },
      "chat.thread.delete": {
        input: ChatThreadDeleteInputZ,
        result: ChatThreadDeleteResultZ
      },
      "chat.thread.rename": {
        input: ChatThreadRenameInputZ,
        result: ChatThreadRenameResultZ
      },
      "chat.thread.setProvider": {
        input: ChatThreadSetProviderInputZ,
        result: ChatThreadSetProviderResultZ
      },
      "chat.thread.get": {
        input: ChatThreadGetInputZ,
        result: ChatThreadGetResultZ
      },
      "chat.thread.usage": {
        input: ChatThreadUsageInputZ,
        result: ChatThreadUsageResultZ
      },
      "chat.session.send": {
        input: ChatSessionSendInputZ,
        result: ChatSessionSendResultZ
      },
      "chat.session.cancel": {
        input: ChatSessionCancelInputZ,
        result: ChatSessionCancelResultZ
      },
      "chat.session.editFromTurn": {
        input: ChatSessionEditFromTurnInputZ,
        result: ChatSessionEditFromTurnResultZ
      },
      "chat.permission.respond": {
        input: ChatPermissionRespondInputZ,
        result: ChatPermissionRespondResultZ
      },
      "chat.context.captureTerminal": {
        input: ChatContextCaptureTerminalInputZ,
        result: ChatContextCaptureTerminalResultZ
      }
    };
    ACTION_NAMES = Object.keys(ActionContractsZ);
  }
});

// packages/contracts/src/actions-errors.ts
var init_actions_errors = __esm({
  "packages/contracts/src/actions-errors.ts"() {
    "use strict";
  }
});

// packages/contracts/src/git.ts
import { z as z11 } from "zod";
var checkoutRequestSchema, commitRequestSchema, stageRequestSchema, unstageRequestSchema, pushRequestSchema;
var init_git = __esm({
  "packages/contracts/src/git.ts"() {
    "use strict";
    checkoutRequestSchema = z11.object({
      branch: z11.string().trim().min(1, "branch is required").max(255, "branch name is too long").refine((s) => !/[\x00-\x1f\x7f \\~^:?*[]/.test(s), "invalid branch name").refine((s) => !s.startsWith("-"), "branch name cannot start with '-'").refine((s) => !s.includes(".."), "branch name cannot contain '..'"),
      create: z11.boolean().optional()
    });
    commitRequestSchema = z11.object({
      message: z11.string().trim().min(1, "commit message is required"),
      /** When true: `git commit -a -m <msg>` (stages tracked changes). */
      all: z11.boolean().optional()
    });
    stageRequestSchema = z11.object({
      paths: z11.array(z11.string().trim().min(1)).min(1, "paths is required")
    });
    unstageRequestSchema = stageRequestSchema;
    pushRequestSchema = z11.object({
      /** When omitted: push the current branch to its upstream. */
      remote: z11.string().trim().min(1).optional(),
      branch: z11.string().trim().min(1).optional(),
      setUpstream: z11.boolean().optional()
    });
  }
});

// packages/contracts/src/github.ts
import { z as z12 } from "zod";
function stripGitSuffix(value) {
  return value.endsWith(".git") ? value.slice(0, -4) : value;
}
function toRepositoryRef(owner, repo) {
  const o = owner?.trim();
  const r = stripGitSuffix(repo?.trim() ?? "");
  if (!o || !r) return null;
  const nameWithOwner = `${o}/${r}`;
  return { owner: o, repo: r, nameWithOwner, repositoryUrl: `https://github.com/${nameWithOwner}` };
}
function parseGitHubRepository(input) {
  const value = input?.trim();
  if (!value) return null;
  const ssh = /^git@github\.com:([^/\s]+)\/([^/\s?#]+?)(?:\.git)?$/i.exec(value);
  if (ssh) return toRepositoryRef(ssh[1], ssh[2]);
  const url = /^https?:\/\/(?:www\.)?github\.com\/([^/\s]+)\/([^/\s?#]+?)(?:\.git)?(?:[/?#].*)?$/i.exec(
    value
  );
  if (url) return toRepositoryRef(url[1], url[2]);
  const canonical = /^([^/\s:]+)\/([^/\s?#]+?)(?:\.git)?$/.exec(value);
  if (canonical) return toRepositoryRef(canonical[1], canonical[2]);
  return null;
}
function summarizeChecks(runs) {
  const out = {
    total: runs.length,
    pending: 0,
    passed: 0,
    failed: 0,
    neutral: 0,
    cancelled: 0,
    skipped: 0
  };
  for (const run2 of runs) {
    if (run2.status !== "completed") {
      out.pending += 1;
      continue;
    }
    switch (run2.conclusion) {
      case "success":
        out.passed += 1;
        break;
      case "failure":
      case "timed_out":
      case "action_required":
        out.failed += 1;
        break;
      case "cancelled":
      case "stale":
        out.cancelled += 1;
        break;
      case "neutral":
        out.neutral += 1;
        break;
      case "skipped":
        out.skipped += 1;
        break;
      default:
        out.pending += 1;
        break;
    }
  }
  return out;
}
var createPrRequestSchema;
var init_github = __esm({
  "packages/contracts/src/github.ts"() {
    "use strict";
    createPrRequestSchema = z12.object({
      title: z12.string().trim().min(1, "title is required").max(256),
      /** Body is markdown. Empty string is OK — GitHub renders the diff
       *  alone — but we strip whitespace so the daemon sees real intent. */
      body: z12.string().optional(),
      /** Target branch. Required because PRs need a base. */
      base: z12.string().trim().min(1, "base branch is required").max(255),
      /** Source branch. Defaults to the current branch when omitted. */
      head: z12.string().trim().min(1).max(255).optional(),
      /** Open as a draft PR. */
      draft: z12.boolean().optional()
    });
  }
});

// packages/contracts/src/terminals.ts
import { z as z13 } from "zod";
async function createScriptTerminalId(args) {
  const scope = args.scopeId ?? args.taskId;
  if (!scope) {
    throw new Error("createScriptTerminalId: scopeId (or taskId) is required");
  }
  const key = `${args.projectId}::${scope}::${args.kind}::${args.script}`;
  const data = new TextEncoder().encode(key);
  const digest = await crypto.subtle.digest("SHA-256", data);
  return Array.from(new Uint8Array(digest)).map((b) => b.toString(16).padStart(2, "0")).join("").slice(0, 32);
}
var terminalKindSchema, terminalCreateRequestSchema, terminalRenameRequestSchema;
var init_terminals = __esm({
  "packages/contracts/src/terminals.ts"() {
    "use strict";
    terminalKindSchema = z13.enum(["shell", "setup", "run", "teardown"]);
    terminalCreateRequestSchema = z13.object({
      scopeId: z13.string().trim().min(1).max(256),
      name: z13.string().trim().min(1).max(120),
      kind: terminalKindSchema.optional(),
      /** Provide for script tabs to opt into deterministic id collapse. */
      script: z13.string().max(2048).optional(),
      /** Explicit id wins. Used by the dashboard to reserve a known id
       *  (e.g. the default shell tab derived from session.dir). */
      id: z13.string().trim().min(8).max(64).regex(/^[A-Za-z0-9_-]+$/u, "id may only contain alphanumerics, '-', '_'").optional()
    }).refine((v) => v.kind !== void 0 || v.script === void 0, {
      message: "script requires kind",
      path: ["script"]
    });
    terminalRenameRequestSchema = z13.object({
      name: z13.string().trim().min(1).max(120)
    });
  }
});

// packages/contracts/src/notes-contract.ts
import { z as z14 } from "zod";
var NoteSchemaZ, NoteResponseSchemaZ, UpdateNoteRequestSchemaZ;
var init_notes_contract = __esm({
  "packages/contracts/src/notes-contract.ts"() {
    "use strict";
    NoteSchemaZ = z14.object({
      /** Session name the note belongs to (matches a tmux-ide project). */
      sessionName: z14.string(),
      /** Markdown body. Empty string when the project has no notes yet. */
      content: z14.string(),
      /** ISO-8601 timestamp of the last write, or null if no note exists. */
      updatedAt: z14.string().nullable()
    });
    NoteResponseSchemaZ = z14.object({
      note: NoteSchemaZ
    });
    UpdateNoteRequestSchemaZ = z14.object({
      content: z14.string().max(1e6, "Note exceeds 1 MB")
    });
  }
});

// packages/contracts/src/index.ts
var init_src2 = __esm({
  "packages/contracts/src/index.ts"() {
    "use strict";
    init_auth();
    init_hq();
    init_ide_config();
    init_domain();
    init_tmux();
    init_workspace();
    init_routes();
    init_client();
    init_ws_v3_protocol();
    init_actions_contract();
    init_actions_errors();
    init_chat_thread();
    init_chat_timeline();
    init_git();
    init_github();
    init_terminals();
    init_notes_contract();
  }
});

// packages/daemon/src/schemas/ide-config.ts
var init_ide_config2 = __esm({
  "packages/daemon/src/schemas/ide-config.ts"() {
    "use strict";
    init_src2();
  }
});

// packages/daemon/src/validate.ts
import { resolve as resolve3 } from "node:path";
function validateConfig(config2) {
  if (config2 == null || typeof config2 !== "object" || Array.isArray(config2)) {
    return ["config must be an object"];
  }
  const result = IdeConfigSchema.safeParse(config2);
  if (!result.success) {
    return result.error.issues.map((issue) => mapZodIssue(issue, config2));
  }
  const errors = [];
  const cfg = result.data;
  const rowSizes = cfg.rows.filter((r) => r.size !== void 0).map((r) => parseInt(r.size, 10));
  const rowSum = rowSizes.reduce((a, b) => a + b, 0);
  if (rowSum > 100) {
    errors.push(`Row sizes sum to ${rowSum}%, which exceeds 100%`);
  }
  for (let i = 0; i < cfg.rows.length; i++) {
    const row = cfg.rows[i];
    const paneSizes = row.panes.filter((p) => p.size !== void 0).map((p) => parseInt(p.size, 10));
    const paneSum = paneSizes.reduce((a, b) => a + b, 0);
    if (paneSum > 100) {
      errors.push(`Row ${i} pane sizes sum to ${paneSum}%, which exceeds 100%`);
    }
    const focusCount = row.panes.filter((p) => p.focus === true).length;
    if (focusCount > 1) {
      errors.push(`Row ${i} has ${focusCount} panes with focus: true (max 1)`);
    }
    for (let j = 0; j < row.panes.length; j++) {
      const pane = row.panes[j];
      if (pane.type !== void 0 && pane.command !== void 0) {
        errors.push(`rows[${i}].panes[${j}] cannot have both 'type' and 'command'`);
      }
    }
  }
  return errors;
}
function formatPath(path4) {
  let result = "";
  for (let i = 0; i < path4.length; i++) {
    const seg = path4[i];
    if (typeof seg === "number") {
      result += `[${seg}]`;
    } else if (i === 0) {
      result += seg;
    } else {
      result += `.${seg}`;
    }
  }
  return result;
}
function shouldQuote(path4) {
  if (path4.length === 1 && typeof path4[0] === "string") return true;
  if (path4[0] === "team") return true;
  return false;
}
function isSizePath(path4) {
  return path4[path4.length - 1] === "size";
}
function isEnvValuePath(path4) {
  const envIdx = path4.indexOf("env");
  return envIdx >= 0 && envIdx < path4.length - 1;
}
function getValueAtPath(obj, path4) {
  let current = obj;
  for (const seg of path4) {
    if (current == null || typeof current !== "object") return void 0;
    current = current[String(seg)];
  }
  return current;
}
function typeDesc(path4, expected) {
  const base = expected.replace(/\s*\|\s*undefined/g, "").trim();
  let desc;
  if (base === "string") desc = "a string";
  else if (base === "boolean") desc = "a boolean";
  else if (base === "number") desc = "a number";
  else if (base === "array") desc = "an array";
  else if (base === "object" || base === "record") desc = "an object";
  else desc = base;
  const field = path4[path4.length - 1];
  if (path4[0] === "orchestrator" && typeof field === "string" && MS_FIELDS.has(field)) {
    return `${desc} (ms)`;
  }
  return desc;
}
function mapZodIssue(issue, config2) {
  const path4 = issue.path ?? [];
  const code = issue.code ?? "";
  const rawPath = formatPath(path4);
  const display = shouldQuote(path4) ? `'${rawPath}'` : rawPath;
  const lastSeg = path4[path4.length - 1];
  if (isEnvValuePath(path4)) {
    return `${formatPath(path4)} must be a string or number`;
  }
  if (isSizePath(path4) && code !== "invalid_type") {
    if (code === "custom") {
      return `${rawPath} must not exceed 100%`;
    }
    const val = getValueAtPath(config2, path4);
    return `${rawPath} "${val}" must be a percentage (e.g. "50%")`;
  }
  if (code === "too_small") {
    return `${display} must not be empty`;
  }
  if (code === "invalid_value" && lastSeg === "type" && path4.includes("panes")) {
    return `${rawPath} must be one of: explorer, changes, preview, tasks, costs, config, mission-control`;
  }
  if (code === "invalid_value" && lastSeg === "role") {
    return `${rawPath} must be "lead", "teammate", or "planner"`;
  }
  if (code === "invalid_value" && lastSeg === "dispatch_mode") {
    return `${rawPath} must be "tasks" or "goals"`;
  }
  if (path4.length === 2 && path4[0] === "team" && path4[1] === "name") {
    if (issue.received === "undefined") {
      return "'team.name' is required when team is specified";
    }
  }
  if (code === "invalid_type") {
    return `${display} must be ${typeDesc(path4, issue.expected ?? "")}`;
  }
  return `${display}: ${issue.message ?? "invalid value"}`;
}
async function validate(targetDir, { json: json2 } = {}) {
  const dir = resolve3(targetDir ?? ".");
  let config2;
  try {
    ({ config: config2 } = readConfig(dir));
  } catch (e) {
    outputError(`Cannot read ide.yml: ${e.message}`, "READ_ERROR");
    return;
  }
  const errors = validateConfig(config2);
  const valid = errors.length === 0;
  if (json2) {
    console.log(JSON.stringify({ valid, errors }, null, 2));
    return;
  }
  if (valid) {
    console.log("\u2713 ide.yml is valid");
  } else {
    console.log("\u2717 ide.yml has errors:");
    for (const e of errors) {
      console.log(`  - ${e}`);
    }
    process.exitCode = 1;
  }
}
var MS_FIELDS;
var init_validate = __esm({
  "packages/daemon/src/validate.ts"() {
    "use strict";
    init_yaml_io();
    init_output();
    init_ide_config2();
    MS_FIELDS = /* @__PURE__ */ new Set(["stall_timeout", "poll_interval"]);
  }
});

// packages/daemon/src/widgets/resolve.ts
var resolve_exports = {};
__export(resolve_exports, {
  WIDGET_TYPES: () => WIDGET_TYPES,
  resolveWidgetCommand: () => resolveWidgetCommand,
  resolveWidgetSpawn: () => resolveWidgetSpawn
});
import { resolve as resolve4, dirname } from "node:path";
import { fileURLToPath } from "node:url";
function resolveWidgetCommand(type, opts) {
  const entry = WIDGET_ENTRY_POINTS[type];
  if (!entry) throw new Error(`Unknown widget type: ${type}`);
  const scriptPath = resolve4(__dirname, entry);
  const args = [`--session=${opts.session}`, `--dir=${opts.dir}`];
  if (opts.target) args.push(`--target=${opts.target}`);
  if (opts.theme) args.push(`--theme=${JSON.stringify(opts.theme)}`);
  const escapedArgs = args.map(shellEscape).join(" ");
  return `cd ${shellEscape(opts.dir)} && bun ${shellEscape(scriptPath)} ${escapedArgs}`;
}
function resolveWidgetSpawn(type, opts) {
  const entry = WIDGET_ENTRY_POINTS[type];
  if (!entry) throw new Error(`Unknown widget type: ${type}`);
  const scriptPath = resolve4(__dirname, entry);
  const args = [`--session=${opts.session}`, `--dir=${opts.dir}`];
  if (opts.target) args.push(`--target=${opts.target}`);
  if (opts.theme) args.push(`--theme=${JSON.stringify(opts.theme)}`);
  return { cwd: opts.dir, cmd: ["bun", scriptPath, ...args] };
}
var __dirname, WIDGET_ENTRY_POINTS, WIDGET_TYPES;
var init_resolve = __esm({
  "packages/daemon/src/widgets/resolve.ts"() {
    "use strict";
    init_shell();
    __dirname = dirname(fileURLToPath(import.meta.url));
    WIDGET_ENTRY_POINTS = {
      explorer: "explorer/index.tsx",
      changes: "changes/index.tsx",
      preview: "preview/index.tsx",
      tasks: "tasks/index.tsx",
      costs: "costs/index.tsx",
      setup: "setup/index.tsx",
      config: "config/index.tsx",
      "mission-control": "mission-control/index.tsx"
    };
    WIDGET_TYPES = Object.keys(WIDGET_ENTRY_POINTS);
  }
});

// packages/daemon/src/lib/canonical-daemon.ts
var canonical_daemon_exports = {};
__export(canonical_daemon_exports, {
  clearCanonicalDaemonInfo: () => clearCanonicalDaemonInfo,
  getCanonicalDaemonInfoPath: () => getCanonicalDaemonInfoPath,
  isCanonicalDaemonAlive: () => isCanonicalDaemonAlive,
  readCanonicalDaemonInfo: () => readCanonicalDaemonInfo,
  warnOnDaemonVersionSkew: () => warnOnDaemonVersionSkew,
  writeCanonicalDaemonInfo: () => writeCanonicalDaemonInfo
});
import { existsSync, mkdirSync, readFileSync as readFileSync2, renameSync, rmSync, writeFileSync as writeFileSync2 } from "node:fs";
import { homedir } from "node:os";
import { dirname as dirname2, join } from "node:path";
function getCanonicalDaemonInfoPath() {
  const dir = process.env[DAEMON_INFO_DIR_ENV] ?? process.env[REGISTRY_DIR_ENV] ?? join(homedir(), ".tmux-ide");
  return join(dir, DAEMON_INFO_FILE);
}
function parseCanonicalDaemonInfo(raw) {
  if (!raw || typeof raw !== "object") return null;
  const info = raw;
  const pid = info.pid;
  const port = info.port;
  if (typeof pid !== "number" || typeof port !== "number") return null;
  if (!Number.isInteger(pid) || !Number.isInteger(port)) return null;
  if (typeof info.version !== "string" || typeof info.startedAt !== "string") return null;
  if (typeof info.bindHostname !== "string") return null;
  if (info.authToken !== null && typeof info.authToken !== "string") return null;
  return {
    pid,
    port,
    version: info.version,
    startedAt: info.startedAt,
    bindHostname: info.bindHostname,
    authToken: info.authToken
  };
}
function writeCanonicalDaemonInfo(info) {
  const path4 = getCanonicalDaemonInfoPath();
  mkdirSync(dirname2(path4), { recursive: true });
  const tmpPath = `${path4}.${process.pid}.${Date.now()}.tmp`;
  const persisted = {
    pid: info.pid,
    port: info.port,
    version: info.version,
    startedAt: info.startedAt,
    bindHostname: info.bindHostname,
    authToken: info.authToken
  };
  writeFileSync2(tmpPath, JSON.stringify(persisted, null, 2) + "\n", "utf-8");
  renameSync(tmpPath, path4);
}
function readCanonicalDaemonInfo() {
  const path4 = getCanonicalDaemonInfoPath();
  if (!existsSync(path4)) return null;
  try {
    return parseCanonicalDaemonInfo(JSON.parse(readFileSync2(path4, "utf-8")));
  } catch {
    return null;
  }
}
function clearCanonicalDaemonInfo() {
  rmSync(getCanonicalDaemonInfoPath(), { force: true });
}
function isPidAlive(pid) {
  try {
    process.kill(pid, 0);
    return true;
  } catch {
    return false;
  }
}
function probeHostname(bindHostname) {
  return bindHostname === "0.0.0.0" ? "127.0.0.1" : bindHostname;
}
function timeoutSignal(ms) {
  const controller = new AbortController();
  setTimeout(() => controller.abort(), ms).unref?.();
  return controller.signal;
}
function warnOnDaemonVersionSkew(info, expectedVersion) {
  if (info.version === expectedVersion) return;
  console.warn(
    `[tmux-ide] canonical daemon version skew: daemon.json reports "${info.version}" but this client expects "${expectedVersion}". The action/WS contract may have drifted \u2014 restart the canonical daemon (tmux-ide) so it matches this client build.`
  );
}
async function isCanonicalDaemonAlive(info) {
  if (!isPidAlive(info.pid)) return false;
  try {
    const res = await fetch(`http://${probeHostname(info.bindHostname)}:${info.port}/health`, {
      signal: timeoutSignal(750)
    });
    return res.ok;
  } catch {
    return false;
  }
}
var DAEMON_INFO_DIR_ENV, REGISTRY_DIR_ENV, DAEMON_INFO_FILE;
var init_canonical_daemon = __esm({
  "packages/daemon/src/lib/canonical-daemon.ts"() {
    "use strict";
    DAEMON_INFO_DIR_ENV = "TMUX_IDE_DAEMON_INFO_DIR";
    REGISTRY_DIR_ENV = "TMUX_IDE_REGISTRY_DIR";
    DAEMON_INFO_FILE = "daemon.json";
  }
});

// packages/daemon/src/launch.ts
var launch_exports = {};
__export(launch_exports, {
  buildMasterAgentPrompt: () => buildMasterAgentPrompt,
  buildPaneMap: () => buildPaneMap,
  ensureTaskDocs: () => ensureTaskDocs,
  launch: () => launch,
  waitForPaneCommand: () => waitForPaneCommand
});
import { resolve as resolve5, join as join2 } from "node:path";
import { execSync } from "node:child_process";
import { createHash } from "node:crypto";
import { readFileSync as readFileSync3, writeFileSync as writeFileSync3, existsSync as existsSync2, mkdirSync as mkdirSync2 } from "node:fs";
function stripWidgetPanes(rows) {
  return rows.map((row) => ({
    ...row,
    panes: row.panes.filter((p) => !p.type)
  })).filter((row) => row.panes.length > 0);
}
function sleepMs(ms) {
  Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, ms);
}
function configHash(config2) {
  return createHash("sha256").update(JSON.stringify(config2)).digest("hex").slice(0, 12);
}
function waitForPaneCommand(targetPane, expectedCommands, {
  attempts = 20,
  delayMs = 100,
  getCurrentCommand = getPaneCurrentCommand,
  sleep = sleepMs
} = {}) {
  const allowed = new Set(expectedCommands.map((command2) => command2.toLowerCase()));
  for (let attempt = 0; attempt < attempts; attempt++) {
    try {
      const current = getCurrentCommand(targetPane)?.trim().toLowerCase();
      if (current && allowed.has(current)) return true;
    } catch {
    }
    if (attempt < attempts - 1) {
      sleep(delayMs);
    }
  }
  return false;
}
function buildPaneMap(rows, dir, rootPaneId, splitPaneFn) {
  const rowSizes = computeSizes(rows);
  const rowSplitPercents = toSplitPercents(rowSizes);
  const rowPaneIds = [rootPaneId];
  for (let rowIdx = 1; rowIdx < rows.length; rowIdx++) {
    const splitFrom = rowPaneIds[rowIdx - 1];
    const newPaneId = splitPaneFn({
      targetPane: splitFrom,
      direction: "vertical",
      cwd: dir,
      percent: rowSplitPercents[rowIdx - 1]
    });
    rowPaneIds.push(newPaneId);
  }
  const paneMap = [];
  const firstPanesOfRows = new Set(rowPaneIds);
  for (let rowIdx = 0; rowIdx < rows.length; rowIdx++) {
    const row = rows[rowIdx];
    const panes = row.panes ?? [];
    const rowPaneId = rowPaneIds[rowIdx];
    const rowPanes = [rowPaneId];
    const paneSizes = computeSizes(panes);
    const paneSplitPercents = toSplitPercents(paneSizes);
    for (let paneIdx = 1; paneIdx < panes.length; paneIdx++) {
      const pane = panes[paneIdx];
      const targetPane = rowPanes[paneIdx - 1];
      const paneDir = pane.dir ? resolve5(dir, pane.dir) : dir;
      const newPaneId = splitPaneFn({
        targetPane,
        direction: "horizontal",
        cwd: paneDir,
        percent: paneSplitPercents[paneIdx - 1]
      });
      rowPanes.push(newPaneId);
    }
    paneMap.push(rowPanes);
  }
  return { paneMap, firstPanesOfRows };
}
function loadLaunchConfig(dir) {
  let config2;
  try {
    ({ config: config2 } = readConfig(dir));
  } catch (error) {
    if (error?.code === "ENOENT") {
      outputError(
        `No ide.yml found in ${dir}. Run "tmux-ide init" or "tmux-ide detect --write" to create one.`,
        "CONFIG_NOT_FOUND"
      );
    }
    outputError(`Cannot read ide.yml: ${error.message}`, "READ_ERROR");
  }
  const errors = validateConfig(config2);
  if (errors.length > 0) {
    outputError(
      `Invalid ide.yml in ${dir}. Run "tmux-ide validate" for details.`,
      "INVALID_CONFIG"
    );
  }
  return config2;
}
function runBeforeHook(command2, dir) {
  if (!command2) return;
  console.log(`Running: ${command2}`);
  try {
    execSync(command2, { cwd: dir, stdio: "inherit", timeout: 6e4 });
  } catch {
    outputError(`The before hook failed: ${command2}`, "BEFORE_HOOK_FAILED");
  }
}
async function launch(targetDir, { json: json2 = false, attach: attach2 = true } = {}) {
  const dir = resolve5(targetDir ?? ".");
  const config2 = loadLaunchConfig(dir);
  const { name: fallbackName } = getSessionName(dir);
  const session = config2.name ?? fallbackName;
  const headless = config2.orchestrator?.widgets === false;
  const rows = headless ? stripWidgetPanes(config2.rows) : config2.rows;
  const theme = config2.theme ?? {};
  const team = config2.team ?? null;
  runBeforeHook(config2.before, dir);
  if (hasSession(session)) {
    const currentHash = configHash(config2);
    const storedHash = getSessionVariable(session, "@config_hash");
    const configChanged = Boolean(storedHash && currentHash !== storedHash);
    if (json2) {
      console.log(JSON.stringify({ session, running: true, configChanged }));
    } else if (configChanged) {
      console.log(`Session "${session}" is running but ide.yml has changed.`);
      console.log(`Run "tmux-ide restart" to apply changes.`);
    } else {
      console.log(`Session "${session}" is already running. Attaching...`);
    }
    if (attach2) {
      attachSession(session);
    }
    return;
  }
  const cols = process.stdout.columns ?? 200;
  const lines = process.stdout.rows ?? 50;
  const rootPaneId = createDetachedSession(session, dir, { cols, lines });
  if (team) {
    setSessionEnvironment(session, "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS", "1");
  }
  const { paneMap, firstPanesOfRows } = buildPaneMap(
    rows,
    dir,
    rootPaneId,
    ({ targetPane, direction, cwd, percent }) => splitPane(targetPane, direction, cwd, percent)
  );
  const { focusPane, paneActions } = collectPaneStartupPlan(rows, paneMap, firstPanesOfRows, dir);
  for (const action of paneActions) {
    if (action.title) {
      setPaneTitle(action.targetPane, action.title);
    }
    setPaneOption(action.targetPane, "@ide_role", action.paneRole ?? "shell");
    setPaneOption(action.targetPane, "@ide_name", action.title ?? "");
    setPaneOption(action.targetPane, "@ide_type", action.paneType ?? "shell");
    if (action.paneRole === "lead" || action.paneRole === "teammate") {
      setPaneOption(action.targetPane, "allow-rename", "off");
    }
    if (action.chdir) {
      sendLiteral(action.targetPane, `cd ${shellEscape(action.chdir)}`);
    }
    for (const exportCommand of action.exports) {
      sendLiteral(action.targetPane, exportCommand);
    }
    if (action.widgetType) {
      const widgetCmd = resolveWidgetCommand(action.widgetType, {
        session,
        dir,
        target: action.widgetTarget ?? null,
        theme: config2.theme ?? null
      });
      sendLiteral(action.targetPane, widgetCmd);
    } else if (action.command) {
      sendLiteral(action.targetPane, action.command);
    }
  }
  for (const command2 of buildSessionOptions(session, { theme })) {
    runSessionCommand(command2);
  }
  setSessionVariable(session, "@config_hash", configHash(config2));
  if (config2.orchestrator?.enabled) {
    ensureTaskDocs(dir);
    const masterPaneTitle = config2.orchestrator.master_pane;
    if (masterPaneTitle) {
      const masterAction = paneActions.find((a) => a.title === masterPaneTitle);
      if (masterAction) {
        const masterPrompt = buildMasterAgentPrompt(config2);
        setTimeout(() => {
          sendLiteral(masterAction.targetPane, masterPrompt);
        }, 5e3);
      }
    }
  }
  selectPane(focusPane);
  const totalPanes = rows.reduce((sum, r) => sum + (r.panes?.length ?? 0), 0);
  console.log(
    `Starting "${session}" (${rows.length} row${rows.length === 1 ? "" : "s"}, ${totalPanes} pane${totalPanes === 1 ? "" : "s"})...`
  );
  try {
    const { readCanonicalDaemonInfo: readCanonicalDaemonInfo2 } = await Promise.resolve().then(() => (init_canonical_daemon(), canonical_daemon_exports));
    const info = readCanonicalDaemonInfo2();
    if (info) {
      console.log(`Dashboard: http://${info.bindHostname}:${info.port}/`);
    }
  } catch {
  }
  if (attach2) {
    attachSession(session);
  }
}
function buildMasterAgentPrompt(config2) {
  const teammatePanes = config2.rows.flatMap((r) => r.panes ?? []).filter((p) => p.role === "teammate" && p.command === "claude").map((p) => p.title).filter(Boolean);
  const isMissionsMode = config2.orchestrator?.dispatch_mode === "missions";
  const milestonesSection = isMissionsMode ? `
## Milestones
Milestones gate execution \u2014 tasks in M2 won't dispatch until M1 is validated.
- tmux-ide milestone create "title" --sequence N
- tmux-ide milestone list --json
- tmux-ide mission plan-complete  (activates milestones, starts dispatch)
` : "";
  return `You are the Lead Agent for this tmux-ide session.

## Your role
You coordinate a team of coding agents. The human gives you high-level goals, and you break them into structured tasks that your teammates execute. You plan, delegate, and review \u2014 you do not implement.

## Your teammates
${teammatePanes.map((t) => `- ${t}`).join("\n")}

## Task management commands
- tmux-ide mission set "title" --description "..."
- tmux-ide goal create "title" --priority N --acceptance "criteria"
- tmux-ide goal list --json
- tmux-ide task create "title" --goal NN --priority N --specialty "type" --fulfills "VAL-001,VAL-002"
- tmux-ide task list --json
- tmux-ide task show NNN --json (shows full mission\u2192goal\u2192task context)
- tmux-ide task done NNN --proof "what was accomplished"
- tmux-ide goal done NN

## How it works
1. Set the mission: tmux-ide mission set "title" --description "..."
2. Create goals with acceptance criteria
3. Create tasks under goals \u2014 use --specialty to hint agent type, --fulfills to link validation assertions
4. The orchestrator automatically dispatches unassigned tasks to idle teammates
5. Teammates work in the project directory
6. When teammates finish, they run tmux-ide task done
7. You get notified and review their work
8. You report progress to the human
${milestonesSection}
## Validation contracts
Define acceptance criteria in .tasks/validation-contract.md using assertion IDs:
  **VAL-001**: All tests pass
  **VAL-002**: No TypeScript errors
Link tasks to assertions: tmux-ide task create "title" --fulfills "VAL-001,VAL-002"
After a milestone's tasks complete, the Validator agent automatically verifies assertions.

## Knowledge library
- .tmux-ide/library/architecture.md \u2014 project context injected into agent prompts
- .tmux-ide/library/learnings.md \u2014 auto-appended by orchestrator after task completion
- AGENTS.md \u2014 project boundaries injected into all agent prompts
Update architecture.md when the project structure changes significantly.

## Important
- Do NOT use --assign when creating tasks \u2014 the orchestrator handles dispatch automatically
- Use --specialty to hint which agent type should pick it up
- Focus on PLANNING and REVIEWING, not implementing
- Break work into small, clear tasks (one per teammate)
- Each task should be completable independently
- Set clear acceptance criteria in goals
- Check progress: tmux-ide task list --json`;
}
function ensureTaskDocs(dir) {
  const claudeMdPath = join2(dir, "CLAUDE.md");
  if (existsSync2(claudeMdPath)) {
    const content = readFileSync3(claudeMdPath, "utf-8");
    if (!content.includes(TASK_DOCS_MARKER)) {
      writeFileSync3(claudeMdPath, content + TASK_DOCS_SECTION);
    }
  } else {
    writeFileSync3(claudeMdPath, `# Project
${TASK_DOCS_SECTION}`);
  }
  const libraryDir = join2(dir, ".tmux-ide", "library");
  if (!existsSync2(libraryDir)) {
    mkdirSync2(libraryDir, { recursive: true });
  }
  const archPath = join2(libraryDir, "architecture.md");
  if (!existsSync2(archPath)) {
    writeFileSync3(
      archPath,
      "# Architecture\n\n<!-- Describe your project architecture here. This is injected into agent dispatch prompts. -->\n"
    );
  }
  const learningsPath = join2(libraryDir, "learnings.md");
  if (!existsSync2(learningsPath)) {
    writeFileSync3(
      learningsPath,
      "# Learnings\n\n<!-- Task summaries are automatically appended here by the orchestrator. -->\n"
    );
  }
  const tasksDir = join2(dir, ".tasks");
  if (!existsSync2(tasksDir)) {
    mkdirSync2(tasksDir, { recursive: true });
  }
  const contractPath = join2(tasksDir, "validation-contract.md");
  if (!existsSync2(contractPath)) {
    writeFileSync3(
      contractPath,
      "# Validation Contract\n\n<!-- Define assertions for the validator agent. Example: -->\n<!-- - VAL-001: All tests pass -->\n<!-- - VAL-002: No TypeScript errors -->\n"
    );
  }
}
var TASK_DOCS_MARKER, TASK_DOCS_SECTION;
var init_launch = __esm({
  "packages/daemon/src/launch.ts"() {
    "use strict";
    init_yaml_io();
    init_sizes();
    init_output();
    init_launch_plan();
    init_session_options();
    init_src();
    init_validate();
    init_resolve();
    init_shell();
    TASK_DOCS_MARKER = "## Task Management";
    TASK_DOCS_SECTION = `
## Task Management

tmux-ide provides structured task management for coordinated multi-agent work.

### Mission & Goals

\`\`\`bash
tmux-ide mission set "title" --description "..."   # Set the project mission
tmux-ide mission show                               # Show current mission
tmux-ide mission clear                              # Clear the mission

tmux-ide goal create "title" --priority N --acceptance "criteria"
tmux-ide goal list [--json]                         # List all goals
tmux-ide goal show <id> [--json]                    # Show goal with tasks
tmux-ide goal update <id> --status done
tmux-ide goal done <id>                             # Mark goal complete
tmux-ide goal delete <id>
\`\`\`

### Tasks

\`\`\`bash
tmux-ide task create "title" --goal NN --priority N --assign "Agent" --tags "a,b" --depends "001,002"
tmux-ide task list [--status todo --goal NN] [--json]
tmux-ide task show <id> [--json]                    # Full mission\u2192goal\u2192task context
tmux-ide task update <id> --status review --proof '{"tests":{"passed":10,"total":10}}'
tmux-ide task claim <id> --assign "Agent Name"      # Claim and start a task
tmux-ide task done <id> --proof "description"       # Mark task complete with proof
tmux-ide task delete <id>
\`\`\`

### Proof Format

The \`--proof\` flag accepts either a plain string (stored as \`notes\`) or a JSON object:

\`\`\`json
{
  "tests": { "passed": 10, "total": 10 },
  "pr": { "number": 42, "url": "https://...", "status": "merged" },
  "ci": { "status": "passing", "url": "https://..." },
  "notes": "Additional context"
}
\`\`\`

### Task Dependencies

Use \`--depends "001,002"\` to declare that a task depends on other tasks. The orchestrator will not dispatch a task until all its dependencies are complete.

### Milestones

\`\`\`bash
tmux-ide milestone create "title" --sequence N
tmux-ide milestone list [--json]
tmux-ide mission plan-complete  # activate milestones and start dispatch
\`\`\`

### Validation

\`\`\`bash
tmux-ide validate show [--json]
tmux-ide validate assert VAL-001 --status passing --evidence "what you verified"
tmux-ide validate coverage [--json]
\`\`\`
`;
  }
});

// packages/daemon/src/detect.ts
import { resolve as resolve6, basename as basename2 } from "node:path";
import { readFileSync as readFileSync4, existsSync as existsSync3 } from "node:fs";
function fileExists(dir, name) {
  return existsSync3(resolve6(dir, name));
}
function readJson(dir, name) {
  try {
    return JSON.parse(readFileSync4(resolve6(dir, name), "utf-8"));
  } catch {
    return null;
  }
}
function detectStack(dir) {
  const detected = {
    packageManager: null,
    frameworks: [],
    devCommand: null,
    language: null,
    reasons: []
  };
  if (fileExists(dir, "pnpm-lock.yaml")) {
    detected.packageManager = "pnpm";
    detected.reasons.push('Detected pnpm from "pnpm-lock.yaml".');
  } else if (fileExists(dir, "bun.lockb") || fileExists(dir, "bun.lock")) {
    detected.packageManager = "bun";
    detected.reasons.push('Detected bun from "bun.lockb" or "bun.lock".');
  } else if (fileExists(dir, "yarn.lock")) {
    detected.packageManager = "yarn";
    detected.reasons.push('Detected yarn from "yarn.lock".');
  } else if (fileExists(dir, "package-lock.json")) {
    detected.packageManager = "npm";
    detected.reasons.push('Detected npm from "package-lock.json".');
  }
  const pkg = readJson(dir, "package.json");
  if (pkg) {
    detected.language = "javascript";
    detected.reasons.push('Detected JavaScript from "package.json".');
    const deps2 = {
      ...pkg.dependencies,
      ...pkg.devDependencies
    };
    if (deps2["next"]) pushFramework(detected, "next", 'Found dependency "next".');
    if (deps2["convex"]) pushFramework(detected, "convex", 'Found dependency "convex".');
    if (deps2["vite"]) pushFramework(detected, "vite", 'Found dependency "vite".');
    if (deps2["remix"] || deps2["@remix-run/node"])
      pushFramework(detected, "remix", "Found Remix dependency.");
    if (deps2["nuxt"]) pushFramework(detected, "nuxt", 'Found dependency "nuxt".');
    if (deps2["astro"]) pushFramework(detected, "astro", 'Found dependency "astro".');
    if (deps2["svelte"] || deps2["@sveltejs/kit"])
      pushFramework(detected, "svelte", "Found Svelte dependency.");
    const pm = detected.packageManager ?? "npm";
    const run2 = pm === "npm" ? "npm run" : pm;
    const scripts = pkg.scripts;
    if (scripts?.dev) {
      detected.devCommand = `${run2} dev`;
      detected.reasons.push(
        `Using dev command "${detected.devCommand}" from package.json scripts.`
      );
    } else if (scripts?.start) {
      detected.devCommand = `${run2} start`;
      detected.reasons.push(
        `Using start command "${detected.devCommand}" from package.json scripts.`
      );
    }
  }
  if (fileExists(dir, "pyproject.toml") || fileExists(dir, "requirements.txt")) {
    detected.language = detected.language ?? "python";
    detected.reasons.push('Detected Python from "pyproject.toml" or "requirements.txt".');
    try {
      const pyproject = readFileSync4(resolve6(dir, "pyproject.toml"), "utf-8");
      if (pyproject.includes("fastapi"))
        pushFramework(detected, "fastapi", 'Found "fastapi" in pyproject.toml.');
      else if (pyproject.includes("django"))
        pushFramework(detected, "django", 'Found "django" in pyproject.toml.');
      else if (pyproject.includes("flask"))
        pushFramework(detected, "flask", 'Found "flask" in pyproject.toml.');
    } catch {
    }
  }
  if (fileExists(dir, "Cargo.toml")) {
    detected.language = detected.language ?? "rust";
    detected.reasons.push('Detected Rust from "Cargo.toml".');
    pushFramework(detected, "cargo", 'Using Cargo workflow from "Cargo.toml".');
  }
  if (fileExists(dir, "go.mod")) {
    detected.language = detected.language ?? "go";
    detected.reasons.push('Detected Go from "go.mod".');
    pushFramework(detected, "go", 'Using Go workflow from "go.mod".');
  }
  if (fileExists(dir, "docker-compose.yml") || fileExists(dir, "docker-compose.yaml")) {
    pushFramework(
      detected,
      "docker",
      'Detected Docker from "docker-compose.yml" or "docker-compose.yaml".'
    );
  }
  if (detected.reasons.length === 0) {
    detected.reasons.push("No framework-specific signals found; using the generic layout.");
  }
  return detected;
}
function suggestConfig(dir, detected) {
  const name = basename2(dir);
  const pm = detected.packageManager ?? "npm";
  const run2 = pm === "npm" ? "npm run" : pm;
  const config2 = {
    name,
    rows: [
      {
        size: "70%",
        panes: [
          { title: "Claude 1", command: "claude" },
          { title: "Claude 2", command: "claude" }
        ]
      },
      {
        panes: []
      }
    ]
  };
  const bottom = config2.rows[1].panes;
  const frameworks = detected.frameworks;
  if (frameworks.length >= 2) {
    config2.rows[0].panes.push({ title: "Claude 3", command: "claude" });
  }
  if (frameworks.includes("next")) {
    bottom.push({ title: "Next.js", command: `${run2} dev` });
  } else if (frameworks.includes("vite")) {
    bottom.push({ title: "Vite", command: `${run2} dev` });
  } else if (frameworks.includes("nuxt")) {
    bottom.push({ title: "Nuxt", command: `${run2} dev` });
  } else if (frameworks.includes("remix")) {
    bottom.push({ title: "Remix", command: `${run2} dev` });
  } else if (frameworks.includes("astro")) {
    bottom.push({ title: "Astro", command: `${run2} dev` });
  } else if (frameworks.includes("svelte")) {
    bottom.push({ title: "SvelteKit", command: `${run2} dev` });
  } else if (frameworks.includes("fastapi")) {
    bottom.push({ title: "FastAPI", command: "uvicorn main:app --reload" });
  } else if (frameworks.includes("django")) {
    bottom.push({ title: "Django", command: "python manage.py runserver" });
  } else if (frameworks.includes("flask")) {
    bottom.push({ title: "Flask", command: "flask run --reload" });
  } else if (frameworks.includes("cargo")) {
    bottom.push({ title: "Cargo", command: "cargo watch -x run" });
  } else if (frameworks.includes("go")) {
    bottom.push({ title: "Go", command: "go run ." });
  } else if (detected.devCommand) {
    bottom.push({ title: "Dev Server", command: detected.devCommand });
  }
  if (frameworks.includes("convex")) {
    bottom.push({ title: "Convex", command: "npx convex dev" });
  }
  bottom.push({ title: "Shell" });
  return config2;
}
async function detect(targetDir, { json: json2, write } = {}) {
  const dir = resolve6(targetDir ?? ".");
  const detected = detectStack(dir);
  const suggested = suggestConfig(dir, detected);
  if (write) {
    writeConfig(dir, suggested);
    if (json2) {
      console.log(JSON.stringify({ detected, suggestedConfig: suggested, written: true }, null, 2));
    } else {
      const desc = detected.frameworks.length > 0 ? detected.frameworks.join(" + ") : detected.language ?? "generic project";
      console.log(`Detected ${desc}. Created ide.yml.`);
      console.log("\nWhy this layout:");
      for (const reason of detected.reasons) {
        console.log(`  - ${reason}`);
      }
    }
    return;
  }
  if (json2) {
    console.log(JSON.stringify({ detected, suggestedConfig: suggested }, null, 2));
    return;
  }
  console.log("Detected stack:");
  if (detected.packageManager) console.log(`  Package manager: ${detected.packageManager}`);
  if (detected.language) console.log(`  Language: ${detected.language}`);
  if (detected.frameworks.length) console.log(`  Frameworks: ${detected.frameworks.join(", ")}`);
  if (detected.devCommand) console.log(`  Dev command: ${detected.devCommand}`);
  console.log("\nReasoning:");
  for (const reason of detected.reasons) {
    console.log(`  - ${reason}`);
  }
  console.log("\nRun with --write to create ide.yml, or --json to see the suggested config.");
}
function pushFramework(detected, framework, reason) {
  if (!detected.frameworks.includes(framework)) {
    detected.frameworks.push(framework);
  }
  detected.reasons.push(reason);
}
var init_detect = __esm({
  "packages/daemon/src/detect.ts"() {
    "use strict";
    init_yaml_io();
  }
});

// packages/daemon/src/lib/skill-registry.ts
import { join as join4 } from "node:path";
import { homedir as homedir2 } from "node:os";
import {
  existsSync as existsSync5,
  mkdirSync as mkdirSync4,
  readdirSync as readdirSync2,
  readFileSync as readFileSync6,
  renameSync as renameSync3,
  rmSync as rmSync2,
  writeFileSync as writeFileSync5
} from "node:fs";
import yaml2 from "js-yaml";
function parseSkillFile(content) {
  const match = content.match(/^---\n([\s\S]*?)\n---\n?([\s\S]*)$/);
  if (!match) return null;
  try {
    const meta = yaml2.load(match[1]);
    const name = meta.name ?? "";
    if (!name) return null;
    const rawSpecialties = meta.specialties;
    const specialties = Array.isArray(rawSpecialties) ? rawSpecialties.map((s) => String(s).toLowerCase()) : typeof rawSpecialties === "string" ? rawSpecialties.split(",").map((s) => s.trim().toLowerCase()) : [];
    return {
      name,
      specialties,
      role: meta.role ?? "teammate",
      description: meta.description ?? "",
      body: (match[2] ?? "").trim()
    };
  } catch {
    return null;
  }
}
function loadSkillsFromDir(skillsDir) {
  if (!existsSync5(skillsDir)) return [];
  const files = readdirSync2(skillsDir).filter((f) => f.endsWith(".md"));
  const skills = [];
  for (const file of files) {
    const content = readFileSync6(join4(skillsDir, file), "utf-8");
    const skill = parseSkillFile(content);
    if (skill) skills.push(skill);
  }
  return skills;
}
function loadSkills(dir) {
  const projectSkills = loadSkillsFromDir(join4(dir, SKILLS_DIR));
  const personalDir = join4(homedir2(), SKILLS_DIR);
  const personalSkills = loadSkillsFromDir(personalDir);
  const nameSet = new Set(projectSkills.map((s) => s.name));
  for (const ps of personalSkills) {
    if (!nameSet.has(ps.name)) {
      projectSkills.push(ps);
    }
  }
  return projectSkills;
}
function loadSkill(dir, name) {
  const skills = loadSkills(dir);
  return skills.find((s) => s.name === name) ?? null;
}
function assertSafeSkillName(name) {
  if (!name.trim() || !SAFE_SKILL_NAME.test(name) || name.includes("..") || name.includes("/")) {
    throw new Error(`Invalid skill name "${name}"`);
  }
}
function projectSkillPath(dir, name) {
  assertSafeSkillName(name);
  return join4(dir, SKILLS_DIR, `${name}.md`);
}
function saveSkill(dir, name, content) {
  const parsed = parseSkillFile(content);
  if (!parsed) {
    throw new Error("Skill content must include valid YAML frontmatter with a name");
  }
  if (parsed.name !== name) {
    throw new Error(`Skill frontmatter name "${parsed.name}" does not match "${name}"`);
  }
  const path4 = projectSkillPath(dir, name);
  mkdirSync4(join4(dir, SKILLS_DIR), { recursive: true });
  const tmpPath = `${path4}.tmp`;
  writeFileSync5(tmpPath, content.endsWith("\n") ? content : `${content}
`);
  renameSync3(tmpPath, path4);
  const saved = parseSkillFile(readFileSync6(path4, "utf-8"));
  if (!saved) {
    throw new Error(`Saved skill "${name}" could not be parsed`);
  }
  return saved;
}
function deleteSkill(dir, name) {
  const path4 = projectSkillPath(dir, name);
  if (!existsSync5(path4)) return false;
  rmSync2(path4);
  return true;
}
function projectSkillExists(dir, name) {
  return existsSync5(projectSkillPath(dir, name));
}
function writeSkillFromFields(dir, fields) {
  const specialties = (fields.specialties ?? []).map((s) => s.trim()).filter(Boolean);
  const meta = {
    name: fields.name,
    specialties,
    role: fields.role && fields.role.trim() ? fields.role.trim() : "teammate",
    description: fields.description ?? ""
  };
  const yamlBlock = yaml2.dump(meta, { lineWidth: 1e3 }).trimEnd();
  const body = (fields.body ?? "").replace(/^\n+/, "").replace(/\n+$/, "");
  const content = `---
${yamlBlock}
---
${body ? `
${body}
` : ""}`;
  return saveSkill(dir, fields.name, content);
}
var SKILLS_DIR, SAFE_SKILL_NAME;
var init_skill_registry = __esm({
  "packages/daemon/src/lib/skill-registry.ts"() {
    "use strict";
    SKILLS_DIR = ".tmux-ide/skills";
    SAFE_SKILL_NAME = /^[A-Za-z0-9._ -]+$/;
  }
});

// packages/daemon/src/lib/slugify.ts
function slugify(text, maxLen = 40) {
  return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, maxLen);
}
var init_slugify = __esm({
  "packages/daemon/src/lib/slugify.ts"() {
    "use strict";
  }
});

// packages/daemon/src/schemas/domain.ts
var init_domain2 = __esm({
  "packages/daemon/src/schemas/domain.ts"() {
    "use strict";
    init_src2();
  }
});

// packages/daemon/src/lib/task-store.ts
import { EventEmitter } from "node:events";
import { createHash as createHash2 } from "node:crypto";
import {
  appendFileSync,
  existsSync as existsSync6,
  mkdirSync as mkdirSync5,
  readdirSync as readdirSync3,
  readFileSync as readFileSync7,
  realpathSync,
  renameSync as renameSync4,
  statSync,
  unlinkSync,
  writeFileSync as writeFileSync6
} from "node:fs";
import { basename as basename4, dirname as dirname4, join as join5, relative, resolve as resolve10 } from "node:path";
import parcelWatcher from "@parcel/watcher";
function hashContent(content) {
  return createHash2("sha256").update(content).digest("hex");
}
function walPathForDir(dir) {
  return join5(getTasksRoot(dir), WAL_FILE);
}
function walPathForFile(filePath) {
  const absolute = resolve10(filePath);
  const parts = absolute.split(/[\\/]/);
  const index = parts.lastIndexOf(TASKS_DIR);
  const root = index >= 0 ? parts.slice(0, index + 1).join("/") : dirname4(absolute);
  return join5(root, WAL_FILE);
}
function appendWalEntry(filePath, entry) {
  const absolute = resolve10(filePath);
  const walPath = walPathForFile(absolute);
  mkdirSync5(dirname4(walPath), { recursive: true });
  appendFileSync(
    walPath,
    JSON.stringify({ ts: (/* @__PURE__ */ new Date()).toISOString(), path: absolute, ...entry }) + "\n"
  );
}
function serializeJson(data) {
  return JSON.stringify(data, null, 2) + "\n";
}
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function zodSummary(error) {
  return error.issues.slice(0, 5).map((issue) => {
    const path4 = issue.path.length > 0 ? issue.path.join(".") : "(root)";
    return `${path4}: ${issue.message}`;
  }).join("; ");
}
function readVersion(raw) {
  if (!isRecord(raw)) return 0;
  return typeof raw._version === "number" ? raw._version : 0;
}
function normalizeMission(raw) {
  const epoch = "1970-01-01T00:00:00.000Z";
  return {
    title: raw.title ?? "",
    description: raw.description ?? "",
    status: raw.status ?? "active",
    branch: raw.branch ?? null,
    milestones: Array.isArray(raw.milestones) ? raw.milestones : [],
    created: raw.created ?? epoch,
    updated: raw.updated ?? epoch
  };
}
function normalizeGoal(raw) {
  const epoch = "1970-01-01T00:00:00.000Z";
  const rest = { ...raw };
  delete rest._version;
  return {
    ...rest,
    created: raw.created ?? epoch,
    updated: raw.updated ?? epoch,
    assignee: raw.assignee ?? null,
    specialty: raw.specialty ?? null,
    milestone: raw.milestone ?? null
  };
}
function normalizeTask(raw) {
  let proof = raw.proof;
  if (isRecord(proof) && "note" in proof && !("notes" in proof)) {
    const { note, ...rest2 } = proof;
    proof = { ...rest2, notes: String(note) };
  }
  const defaults = {
    goal: null,
    assignee: null,
    tags: [],
    retryCount: 0,
    maxRetries: 5,
    lastError: null,
    nextRetryAt: null,
    depends_on: []
  };
  const rest = { ...raw };
  delete rest._version;
  return {
    ...defaults,
    ...rest,
    goal: raw.goal ?? defaults.goal,
    assignee: raw.assignee ?? defaults.assignee,
    tags: Array.isArray(raw.tags) ? raw.tags : defaults.tags,
    proof: proof ?? null,
    retryCount: raw.retryCount ?? defaults.retryCount,
    maxRetries: raw.maxRetries ?? defaults.maxRetries,
    lastError: raw.lastError ?? defaults.lastError,
    nextRetryAt: raw.nextRetryAt ?? defaults.nextRetryAt,
    depends_on: Array.isArray(raw.depends_on) ? raw.depends_on : defaults.depends_on,
    milestone: raw.milestone ?? null,
    specialty: raw.specialty ?? null,
    fulfills: Array.isArray(raw.fulfills) ? raw.fulfills : [],
    discoveredIssues: Array.isArray(raw.discoveredIssues) ? raw.discoveredIssues : [],
    salientSummary: raw.salientSummary ?? null
  };
}
function migrateOnRead(raw, schemaName) {
  if (!isRecord(raw)) return { raw, migrations: [] };
  const version = readVersion(raw);
  const migrations = [];
  let next = { ...raw };
  if (schemaName === "task") {
    const proof = next.proof;
    if (isRecord(proof) && "note" in proof && !("notes" in proof)) {
      next = {
        ...next,
        proof: {
          ...proof,
          notes: proof.note
        }
      };
      delete next.proof.note;
      migrations.push("proof.note -> proof.notes");
    }
  }
  if (version < SCHEMA_VERSION) {
    if (schemaName === "mission")
      next = normalizeMission(next);
    if (schemaName === "goal") next = normalizeGoal(next);
    if (schemaName === "task") next = normalizeTask(next);
    migrations.push(`v${version} -> v${SCHEMA_VERSION}`);
  }
  return { raw: next, migrations };
}
function cacheableFiles(dir) {
  const root = getTasksRoot(dir);
  const files = [];
  const mission = join5(root, "mission.json");
  if (existsSync6(mission)) files.push({ filePath: mission, schemaName: "mission" });
  for (const [subdir, schemaName] of [
    ["goals", "goal"],
    ["tasks", "task"]
  ]) {
    const directory = join5(root, subdir);
    if (!existsSync6(directory)) continue;
    for (const file of readdirSync3(directory).filter((f) => f.endsWith(".json")).sort()) {
      files.push({ filePath: join5(directory, file), schemaName });
    }
  }
  return files;
}
function walkFiles(root) {
  if (!existsSync6(root)) return [];
  const out = [];
  for (const entry of readdirSync3(root, { withFileTypes: true })) {
    const path4 = join5(root, entry.name);
    if (entry.isDirectory()) out.push(...walkFiles(path4));
    else out.push(path4);
  }
  return out;
}
function parseAssertionIds(contract) {
  const ids = /* @__PURE__ */ new Set();
  let match;
  while ((match = ASSERTION_ID_PATTERN.exec(contract)) !== null) {
    ids.add(match[1]);
  }
  return [...ids];
}
function readPositiveIntEnv(name, fallback) {
  const raw = process.env[name];
  if (!raw) return fallback;
  const parsed = Number.parseInt(raw, 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : fallback;
}
function getCacheLimit() {
  return readPositiveIntEnv("TMUX_IDE_TASKSTORE_CACHE_SIZE", DEFAULT_CACHE_SIZE);
}
function getWatcherDebounceMs() {
  return readPositiveIntEnv("TMUX_IDE_WATCHER_DEBOUNCE_MS", DEFAULT_WATCH_DEBOUNCE_MS);
}
function getStoreValueId(value) {
  if ("id" in value) return value.id;
  return void 0;
}
function p95(samples) {
  if (samples.length === 0) return 0;
  const sorted = [...samples].sort((a, b) => a - b);
  const index = Math.min(sorted.length - 1, Math.ceil(sorted.length * 0.95) - 1);
  return sorted[index] ?? 0;
}
function findFileById(directory, id) {
  if (!existsSync6(directory)) return null;
  const files = readdirSync3(directory).filter((f) => f.endsWith(".json"));
  for (const file of files) {
    if (file.startsWith(id + "-") || file === id + ".json") {
      return join5(directory, file);
    }
  }
  return null;
}
function getTasksRoot(dir) {
  return resolve10(dir, TASKS_DIR);
}
function ensureTasksDir(dir) {
  const root = getTasksRoot(dir);
  if (!existsSync6(root)) mkdirSync5(root, { recursive: true });
  const goalsDir = join5(root, "goals");
  if (!existsSync6(goalsDir)) mkdirSync5(goalsDir);
  const tasksDir = join5(root, "tasks");
  if (!existsSync6(tasksDir)) mkdirSync5(tasksDir);
}
function replayTaskStoreWal(dir) {
  ensureTasksDir(dir);
  const walPath = walPathForDir(dir);
  if (!existsSync6(walPath)) return { replayed: 0, skipped: 0 };
  const pending = /* @__PURE__ */ new Map();
  let skipped = 0;
  const raw = readFileSync7(walPath, "utf-8");
  for (const line of raw.split("\n")) {
    if (!line.trim()) continue;
    try {
      const parsed = JSON.parse(line);
      if (!parsed || typeof parsed.path !== "string" || !["write", "delete", "commit"].includes(parsed.op)) {
        skipped++;
        continue;
      }
      const absolute = resolve10(parsed.path);
      if (parsed.op === "commit") {
        pending.delete(absolute);
      } else {
        pending.set(absolute, { ...parsed, path: absolute });
      }
    } catch {
      skipped++;
    }
  }
  let replayed = 0;
  for (const entry of pending.values()) {
    try {
      if (entry.op === "write") {
        const tmpPath = `${entry.path}.tmp`;
        if (!existsSync6(tmpPath)) {
          if (entry.hash && existsSync6(entry.path) && hashContent(readFileSync7(entry.path, "utf-8")) === entry.hash) {
            replayed++;
            continue;
          }
          skipped++;
          continue;
        }
        if (entry.hash && hashContent(readFileSync7(tmpPath, "utf-8")) !== entry.hash) {
          skipped++;
          continue;
        }
        renameSync4(tmpPath, entry.path);
        replayed++;
      } else if (entry.op === "delete") {
        if (existsSync6(entry.path)) unlinkSync(entry.path);
        replayed++;
      }
    } catch (err) {
      skipped++;
      console.warn("[task-store] WAL replay skipped %s: %s", entry.path, err.message);
    }
  }
  writeFileSync6(walPath, "");
  taskStore.invalidateAll();
  return { replayed, skipped };
}
function startTaskStoreWatcher(dir) {
  return taskStore.startWatcher(dir);
}
function flushPendingTaskStore() {
  taskStore.flushPending();
}
function reconcileTaskStore(dir) {
  return taskStore.reconcile(dir);
}
function getTaskStoreHealth() {
  return taskStore.getHealth();
}
function getTaskStoreMetrics() {
  return taskStore.getMetrics();
}
function loadMission(dir) {
  const path4 = join5(getTasksRoot(dir), "mission.json");
  if (!existsSync6(path4)) return null;
  return taskStore.safeRead(path4, "mission");
}
function saveMission(dir, mission) {
  ensureTasksDir(dir);
  taskStore.writeAtomic(
    join5(getTasksRoot(dir), "mission.json"),
    "mission",
    normalizeMission(mission)
  );
}
function clearMission(dir) {
  const path4 = join5(getTasksRoot(dir), "mission.json");
  if (existsSync6(path4)) taskStore.unlink(path4);
}
function nextGoalId(dir) {
  const goalsDir = join5(getTasksRoot(dir), "goals");
  if (!existsSync6(goalsDir)) return "01";
  const files = readdirSync3(goalsDir).filter((f) => f.endsWith(".json"));
  if (files.length === 0) return "01";
  const maxId = Math.max(...files.map((f) => parseInt(f.split("-")[0], 10) || 0));
  return String(maxId + 1).padStart(2, "0");
}
function loadGoals(dir) {
  const goalsDir = join5(getTasksRoot(dir), "goals");
  if (!existsSync6(goalsDir)) return [];
  return readdirSync3(goalsDir).filter((f) => f.endsWith(".json")).sort().map((f) => taskStore.safeRead(join5(goalsDir, f), "goal")).filter((g) => g !== null);
}
function loadGoal(dir, id) {
  const file = findFileById(join5(getTasksRoot(dir), "goals"), id);
  if (!file) return null;
  return taskStore.safeRead(file, "goal");
}
function saveGoal(dir, goal) {
  ensureTasksDir(dir);
  const normalized = normalizeGoal(goal);
  const goalsDir = join5(getTasksRoot(dir), "goals");
  const existing = findFileById(goalsDir, normalized.id);
  const filename = `${normalized.id}-${slugify(normalized.title, 50)}.json`;
  const newPath = join5(goalsDir, filename);
  taskStore.writeAtomic(newPath, "goal", normalized);
  if (existing && existing !== newPath) taskStore.unlink(existing);
}
function deleteGoal(dir, id) {
  const file = findFileById(join5(getTasksRoot(dir), "goals"), id);
  if (!file) return false;
  taskStore.unlink(file);
  return true;
}
function nextTaskId(dir) {
  const tasksDir = join5(getTasksRoot(dir), "tasks");
  if (!existsSync6(tasksDir)) return "001";
  const files = readdirSync3(tasksDir).filter((f) => f.endsWith(".json"));
  if (files.length === 0) return "001";
  const maxId = Math.max(...files.map((f) => parseInt(f.split("-")[0], 10) || 0));
  return String(maxId + 1).padStart(3, "0");
}
function loadTasks(dir) {
  const tasksDir = join5(getTasksRoot(dir), "tasks");
  if (!existsSync6(tasksDir)) return [];
  return readdirSync3(tasksDir).filter((f) => f.endsWith(".json")).sort().map((f) => taskStore.safeRead(join5(tasksDir, f), "task")).filter((t) => t !== null);
}
function loadTask(dir, id) {
  const file = findFileById(join5(getTasksRoot(dir), "tasks"), id);
  if (!file) return null;
  return taskStore.safeRead(file, "task");
}
function saveTask(dir, task) {
  ensureTasksDir(dir);
  const normalized = normalizeTask(task);
  const tasksDir = join5(getTasksRoot(dir), "tasks");
  const existing = findFileById(tasksDir, normalized.id);
  const filename = `${normalized.id}-${slugify(normalized.title, 50)}.json`;
  const newPath = join5(tasksDir, filename);
  taskStore.writeAtomic(newPath, "task", normalized);
  if (existing && existing !== newPath) taskStore.unlink(existing);
}
function deleteTask(dir, id) {
  const file = findFileById(join5(getTasksRoot(dir), "tasks"), id);
  if (!file) return false;
  taskStore.unlink(file);
  return true;
}
function loadTasksForGoal(dir, goalId) {
  return loadTasks(dir).filter((task) => task.goal === goalId);
}
function detectCycle(dir, taskId, newDeps) {
  const tasks = loadTasks(dir);
  const depMap = /* @__PURE__ */ new Map();
  for (const t of tasks) {
    depMap.set(t.id, [...t.depends_on]);
  }
  depMap.set(taskId, newDeps);
  const visited = /* @__PURE__ */ new Set();
  const path4 = [];
  function dfs(id) {
    if (path4.includes(id)) {
      return [...path4.slice(path4.indexOf(id)), id];
    }
    if (visited.has(id)) return null;
    visited.add(id);
    path4.push(id);
    for (const dep of depMap.get(id) ?? []) {
      const cycle = dfs(dep);
      if (cycle) return cycle;
    }
    path4.pop();
    return null;
  }
  return dfs(taskId);
}
function schemaForCacheablePath(filePath) {
  const parts = filePath.split(/[\\/]/);
  if (basename4(filePath) === "mission.json") return "mission";
  if (parts.includes("goals") && filePath.endsWith(".json")) return "goal";
  if (parts.includes("tasks") && filePath.endsWith(".json")) return "task";
  return null;
}
function validateTasksTree(dir, options = {}) {
  const root = getTasksRoot(dir);
  const issues = [];
  const files = walkFiles(root);
  for (const file of files.filter((path4) => path4.endsWith(".json"))) {
    const schemaName = schemaForCacheablePath(file);
    if (!schemaName) continue;
    try {
      taskStore.validateFile(file, schemaName);
    } catch (err) {
      issues.push({
        type: "schema",
        file: relative(dir, file),
        message: err.message
      });
    }
  }
  const goals = loadGoals(dir);
  const goalIds = new Set(goals.map((goal) => goal.id));
  const tasks = loadTasks(dir);
  const taskIds = new Set(tasks.map((task) => task.id));
  for (const task of tasks) {
    const file = findFileById(join5(root, "tasks"), task.id) ?? void 0;
    if (task.goal && !goalIds.has(task.goal)) {
      const issue = {
        type: "orphan-goal",
        file: file ? relative(dir, file) : void 0,
        taskId: task.id,
        message: `Task ${task.id} references missing goal ${task.goal}`
      };
      if (options.fix) {
        task.goal = null;
        task.updated = (/* @__PURE__ */ new Date()).toISOString();
        saveTask(dir, task);
        issue.fixed = true;
      }
      issues.push(issue);
    }
    for (const dep of task.depends_on) {
      if (!taskIds.has(dep)) {
        issues.push({
          type: "orphan-dependency",
          file: file ? relative(dir, file) : void 0,
          taskId: task.id,
          message: `Task ${task.id} depends on missing task ${dep}`
        });
      }
    }
    if (task.status === "done" && !task.proof) {
      issues.push({
        type: "missing-proof",
        file: file ? relative(dir, file) : void 0,
        taskId: task.id,
        message: `Task ${task.id} is done but has no proof`
      });
    }
  }
  const contractPath = join5(root, "validation-contract.md");
  if (existsSync6(contractPath)) {
    const assertionIds = parseAssertionIds(readFileSync7(contractPath, "utf-8"));
    const claimed = new Set(tasks.flatMap((task) => task.fulfills));
    for (const assertionId of assertionIds) {
      if (!claimed.has(assertionId)) {
        issues.push({
          type: "unclaimed-assertion",
          file: relative(dir, contractPath),
          message: `Validation assertion ${assertionId} is claimed by no task`
        });
      }
    }
  }
  const now = Date.now();
  for (const file of files.filter((path4) => path4.endsWith(".lock"))) {
    try {
      const ageMs = now - statSync(file).mtimeMs;
      if (ageMs > 5 * 60 * 1e3) {
        issues.push({
          type: "stale-lock",
          file: relative(dir, file),
          message: `Stale lock file older than 5m: ${relative(dir, file)}`
        });
      }
    } catch {
    }
  }
  const drift = taskStore.reconcile(dir);
  if (drift > 0) {
    issues.push({
      type: "drift",
      file: relative(dir, root),
      message: `Task-store cache drift detected: ${drift} issue(s)`
    });
  }
  return { ok: issues.every((issue) => issue.fixed), issues };
}
var TASKS_DIR, SCHEMA_VERSION, OWN_WRITE_SUPPRESSION_MS, DEFAULT_CACHE_SIZE, DEFAULT_WATCH_DEBOUNCE_MS, RECONCILE_LOG_PREFIX, ASSERTION_ID_PATTERN, WAL_FILE, WATCH_IGNORED_NAMES, WATCH_IGNORE_GLOBS, schemas, TaskStoreValidationError, TaskStore, taskStore;
var init_task_store = __esm({
  "packages/daemon/src/lib/task-store.ts"() {
    "use strict";
    init_slugify();
    init_domain2();
    TASKS_DIR = ".tasks";
    SCHEMA_VERSION = 1;
    OWN_WRITE_SUPPRESSION_MS = 200;
    DEFAULT_CACHE_SIZE = 500;
    DEFAULT_WATCH_DEBOUNCE_MS = 100;
    RECONCILE_LOG_PREFIX = "[task-store]";
    ASSERTION_ID_PATTERN = /\*\*((?:VAL|ASSERT)[A-Z0-9_-]+)\*\*/g;
    WAL_FILE = "_wal.jsonl";
    WATCH_IGNORED_NAMES = [
      ".svn",
      ".hg",
      ".git",
      "node_modules",
      "dist",
      "build",
      "out",
      ".next",
      ".turbo",
      ".cache",
      "__pycache__",
      "target"
    ];
    WATCH_IGNORE_GLOBS = WATCH_IGNORED_NAMES.map((n) => `**/${n}/**`);
    schemas = {
      mission: MissionSchemaZ,
      goal: GoalSchemaZ,
      task: TaskSchemaZ
    };
    TaskStoreValidationError = class extends Error {
      filePath;
      schemaName;
      summary;
      constructor(filePath, schemaName, summary) {
        super(`${filePath}: ${schemaName} schema validation failed: ${summary}`);
        this.name = "TaskStoreValidationError";
        this.filePath = filePath;
        this.schemaName = schemaName;
        this.summary = summary;
      }
    };
    TaskStore = class extends EventEmitter {
      cache = /* @__PURE__ */ new Map();
      ownWriteTimers = /* @__PURE__ */ new Map();
      pendingWatchPaths = /* @__PURE__ */ new Set();
      watchFlushTimer = null;
      subscription = null;
      subscriptionPending = false;
      subscriptionClosed = false;
      watchedRoot = null;
      resolvedWatchRoot = null;
      startedAt = Date.now();
      lastReconcileMs = 0;
      driftCount = 0;
      activeWrites = 0;
      taskById = /* @__PURE__ */ new Map();
      taskPathById = /* @__PURE__ */ new Map();
      tasksByStatus = /* @__PURE__ */ new Map();
      tasksByAgentPane = /* @__PURE__ */ new Map();
      tasksByGoal = /* @__PURE__ */ new Map();
      tasksByMilestone = /* @__PURE__ */ new Map();
      cacheHits = 0;
      cacheMisses = 0;
      cacheEvictions = 0;
      watcherEvents = 0;
      watcherSuppressed = 0;
      watcherBatchedFlushes = 0;
      reconcileRuns = 0;
      writeCount = 0;
      lastWriteMs = 0;
      writeSamples = [];
      read(filePath, schemaName) {
        const absolute = resolve10(filePath);
        const cached = this.cache.get(absolute);
        if (cached) {
          this.cacheHits++;
          this.cache.delete(absolute);
          this.cache.set(absolute, cached);
          return cached.value;
        }
        this.cacheMisses++;
        const content = readFileSync7(absolute, "utf-8");
        const parsed = this.parseContent(absolute, schemaName, content);
        this.seedCache(absolute, schemaName, parsed, content);
        return parsed;
      }
      safeRead(filePath, schemaName) {
        try {
          return this.read(filePath, schemaName);
        } catch (err) {
          console.warn("[task-store] %s", err.message);
          return null;
        }
      }
      writeAtomic(filePath, schemaName, value) {
        const absolute = resolve10(filePath);
        const op = existsSync6(absolute) ? "update" : "create";
        const body = { _version: SCHEMA_VERSION, ...value };
        const content = serializeJson(body);
        const parsed = this.parseContent(absolute, schemaName, content);
        const tmpPath = `${absolute}.tmp`;
        mkdirSync5(dirname4(absolute), { recursive: true });
        this.activeWrites++;
        const started = Date.now();
        try {
          appendWalEntry(absolute, { op: "write", hash: hashContent(content) });
          writeFileSync6(tmpPath, content);
          this.markOwnWrite(absolute);
          renameSync4(tmpPath, absolute);
          appendWalEntry(absolute, { op: "commit" });
          this.seedCache(absolute, schemaName, parsed, content);
          this.emit("change", {
            path: absolute,
            schemaName,
            source: "write",
            op,
            id: getStoreValueId(parsed)
          });
        } finally {
          this.recordWriteSample(Date.now() - started);
          this.activeWrites--;
          if (existsSync6(tmpPath)) {
            try {
              unlinkSync(tmpPath);
            } catch {
            }
          }
        }
      }
      unlink(filePath) {
        const absolute = resolve10(filePath);
        const cached = this.cache.get(absolute);
        const schemaName = cached?.schemaName ?? schemaForCacheablePath(absolute) ?? void 0;
        const id = cached ? getStoreValueId(cached.value) : void 0;
        const hash = existsSync6(absolute) ? hashContent(readFileSync7(absolute, "utf-8")) : void 0;
        appendWalEntry(absolute, { op: "delete", hash });
        this.markOwnWrite(absolute);
        unlinkSync(absolute);
        appendWalEntry(absolute, { op: "commit" });
        this.removeFromCache(absolute);
        this.emit("change", { path: absolute, schemaName, source: "delete", op: "delete", id });
      }
      invalidate(filePath, source = "external") {
        const absolute = resolve10(filePath);
        const removed = this.removeFromCache(absolute);
        if (removed) {
          this.emit("change", {
            path: absolute,
            schemaName: removed.schemaName,
            source,
            op: "invalidate",
            id: getStoreValueId(removed.value)
          });
        }
      }
      invalidateAll() {
        if (this.cache.size === 0) return;
        this.cache.clear();
        this.clearIndexes();
        this.emit("change", { path: null, source: "invalidate-all" });
      }
      getCacheSize() {
        return this.cache.size;
      }
      getHealth() {
        return {
          ok: this.driftCount === 0,
          uptimeMs: Date.now() - this.startedAt,
          lastReconcileMs: this.lastReconcileMs,
          cacheSize: this.cache.size,
          driftCount: this.driftCount,
          watcherActive: this.isWatcherActive(),
          writeQueueDepth: this.activeWrites
        };
      }
      getMetrics() {
        return {
          uptimeMs: Date.now() - this.startedAt,
          cache: {
            size: this.cache.size,
            maxSize: getCacheLimit(),
            hits: this.cacheHits,
            misses: this.cacheMisses,
            evictions: this.cacheEvictions
          },
          watcher: {
            active: this.isWatcherActive(),
            debounceMs: getWatcherDebounceMs(),
            events: this.watcherEvents,
            suppressed: this.watcherSuppressed,
            batchedFlushes: this.watcherBatchedFlushes,
            pending: this.pendingWatchPaths.size
          },
          reconcile: {
            runs: this.reconcileRuns,
            driftCount: this.driftCount,
            lastMs: this.lastReconcileMs
          },
          writes: {
            active: this.activeWrites,
            count: this.writeCount,
            lastMs: this.lastWriteMs,
            p95Ms: p95(this.writeSamples)
          }
        };
      }
      getTasksByStatus(status4) {
        return this.tasksFromIndex(this.tasksByStatus.get(status4));
      }
      getTasksByAgentPane(agentPane) {
        return this.tasksFromIndex(this.tasksByAgentPane.get(agentPane));
      }
      getTasksByGoal(goalId) {
        return this.tasksFromIndex(this.tasksByGoal.get(goalId));
      }
      getTasksByMilestone(milestoneId) {
        return this.tasksFromIndex(this.tasksByMilestone.get(milestoneId));
      }
      startWatcher(dir) {
        const root = getTasksRoot(dir);
        if (this.isWatcherActive() && this.watchedRoot === root) {
          return async () => this.stopWatcher();
        }
        void this.stopWatcher();
        ensureTasksDir(dir);
        let resolvedRoot;
        try {
          resolvedRoot = realpathSync(root);
        } catch {
          resolvedRoot = root;
        }
        this.watchedRoot = root;
        this.resolvedWatchRoot = resolvedRoot;
        this.subscriptionClosed = false;
        this.subscriptionPending = true;
        const invalidateFromWatch = (absolute) => {
          this.watcherEvents++;
          if (this.isOwnWrite(absolute)) {
            this.watcherSuppressed++;
            return;
          }
          this.pendingWatchPaths.add(absolute);
          this.scheduleWatchFlush();
        };
        const remapEventPath = (eventPath) => {
          if (eventPath === resolvedRoot) return root;
          if (eventPath.startsWith(resolvedRoot + "/")) {
            return root + eventPath.slice(resolvedRoot.length);
          }
          return eventPath;
        };
        void parcelWatcher.subscribe(
          resolvedRoot,
          (err, events) => {
            if (err) {
              console.warn("[task-store] watcher error: %s", err.message);
              return;
            }
            for (const event of events) {
              invalidateFromWatch(resolve10(remapEventPath(event.path)));
            }
          },
          { ignore: WATCH_IGNORE_GLOBS }
        ).then((sub) => {
          this.subscriptionPending = false;
          if (this.subscriptionClosed) {
            void sub.unsubscribe().catch(() => {
            });
            return;
          }
          this.subscription = sub;
        }).catch((err) => {
          this.subscriptionPending = false;
          console.warn("[task-store] watcher subscribe failed: %s", err.message);
        });
        return async () => this.stopWatcher();
      }
      async stopWatcher() {
        this.flushPending();
        this.subscriptionClosed = true;
        const sub = this.subscription;
        this.subscription = null;
        this.watchedRoot = null;
        this.resolvedWatchRoot = null;
        if (!sub) return;
        await sub.unsubscribe().catch(() => {
        });
      }
      isWatcherActive() {
        return this.subscription !== null || this.subscriptionPending;
      }
      flushPending() {
        if (this.watchFlushTimer) clearTimeout(this.watchFlushTimer);
        this.watchFlushTimer = null;
        const paths = [...this.pendingWatchPaths];
        this.pendingWatchPaths.clear();
        if (paths.length === 0) return;
        this.watcherBatchedFlushes++;
        for (const path4 of paths) this.invalidate(path4, "watcher");
      }
      reconcile(dir) {
        this.reconcileRuns++;
        const started = Date.now();
        const root = getTasksRoot(dir);
        const diskFiles = cacheableFiles(dir);
        const diskPaths = new Set(diskFiles.map((f) => resolve10(f.filePath)));
        let drift = 0;
        for (const { filePath } of diskFiles) {
          const absolute = resolve10(filePath);
          const cached = this.cache.get(absolute);
          if (!cached) {
            drift++;
            continue;
          }
          const hash = hashContent(readFileSync7(absolute, "utf-8"));
          if (hash !== cached.hash) drift++;
        }
        for (const [filePath] of this.cache) {
          if (!filePath.startsWith(root)) continue;
          if (!diskPaths.has(filePath)) {
            drift++;
          }
        }
        this.lastReconcileMs = Date.now() - started;
        this.driftCount = drift;
        if (drift > 0) {
          console.warn("%s reconcile detected %d cache drift issue(s)", RECONCILE_LOG_PREFIX, drift);
          this.emit("change", {
            path: root,
            source: "reconcile",
            op: "reconcile",
            drift
          });
        }
        return drift;
      }
      validateFile(filePath, schemaName) {
        const absolute = resolve10(filePath);
        const content = readFileSync7(absolute, "utf-8");
        return this.parseContent(absolute, schemaName, content);
      }
      parseContent(filePath, schemaName, content) {
        let raw;
        try {
          raw = JSON.parse(content);
        } catch (err) {
          throw new TaskStoreValidationError(
            filePath,
            schemaName,
            `invalid JSON: ${err.message}`
          );
        }
        const migrated = migrateOnRead(raw, schemaName);
        for (const migration of migrated.migrations) {
          console.warn("[task-store] migrated %s (%s): %s", filePath, schemaName, migration);
        }
        const result = schemas[schemaName].safeParse(migrated.raw);
        if (!result.success) {
          throw new TaskStoreValidationError(filePath, schemaName, zodSummary(result.error));
        }
        return result.data;
      }
      seedCache(filePath, schemaName, value, content) {
        const absolute = resolve10(filePath);
        this.removeFromCache(absolute);
        this.cache.set(absolute, {
          filePath: absolute,
          schemaName,
          value,
          hash: hashContent(content)
        });
        if (schemaName === "task") this.addTaskToIndexes(absolute, value);
        this.enforceCacheLimit();
      }
      removeFromCache(filePath) {
        const absolute = resolve10(filePath);
        const existing = this.cache.get(absolute);
        if (!existing) return null;
        this.cache.delete(absolute);
        if (existing.schemaName === "task") this.removeTaskFromIndexes(existing.value);
        return existing;
      }
      addTaskToIndexes(filePath, task) {
        const oldPath = this.taskPathById.get(task.id);
        if (oldPath && oldPath !== filePath) this.removeFromCache(oldPath);
        this.taskById.set(task.id, task);
        this.taskPathById.set(task.id, filePath);
        this.addIndex(this.tasksByStatus, task.status, task.id);
        if (task.assignee) this.addIndex(this.tasksByAgentPane, task.assignee, task.id);
        if (task.goal) this.addIndex(this.tasksByGoal, task.goal, task.id);
        if (task.milestone) this.addIndex(this.tasksByMilestone, task.milestone, task.id);
      }
      removeTaskFromIndexes(task) {
        this.taskById.delete(task.id);
        this.taskPathById.delete(task.id);
        this.removeIndex(this.tasksByStatus, task.status, task.id);
        if (task.assignee) this.removeIndex(this.tasksByAgentPane, task.assignee, task.id);
        if (task.goal) this.removeIndex(this.tasksByGoal, task.goal, task.id);
        if (task.milestone) this.removeIndex(this.tasksByMilestone, task.milestone, task.id);
      }
      addIndex(map, key, id) {
        let ids = map.get(key);
        if (!ids) {
          ids = /* @__PURE__ */ new Set();
          map.set(key, ids);
        }
        ids.add(id);
      }
      removeIndex(map, key, id) {
        const ids = map.get(key);
        if (!ids) return;
        ids.delete(id);
        if (ids.size === 0) map.delete(key);
      }
      clearIndexes() {
        this.taskById.clear();
        this.taskPathById.clear();
        this.tasksByStatus.clear();
        this.tasksByAgentPane.clear();
        this.tasksByGoal.clear();
        this.tasksByMilestone.clear();
      }
      tasksFromIndex(ids) {
        if (!ids) return [];
        return [...ids].map((id) => this.taskById.get(id)).filter((task) => !!task);
      }
      enforceCacheLimit() {
        const limit = getCacheLimit();
        while (this.cache.size > limit) {
          const oldest = this.cache.keys().next().value;
          if (!oldest) return;
          this.removeFromCache(oldest);
          this.cacheEvictions++;
        }
      }
      scheduleWatchFlush() {
        if (this.watchFlushTimer) return;
        this.watchFlushTimer = setTimeout(() => {
          this.watchFlushTimer = null;
          const paths = [...this.pendingWatchPaths];
          this.pendingWatchPaths.clear();
          if (paths.length === 0) return;
          this.watcherBatchedFlushes++;
          for (const path4 of paths) this.invalidate(path4, "watcher");
        }, getWatcherDebounceMs());
      }
      recordWriteSample(durationMs) {
        this.writeCount++;
        this.lastWriteMs = durationMs;
        this.writeSamples.push(durationMs);
        if (this.writeSamples.length > 100) this.writeSamples.shift();
      }
      markOwnWrite(filePath) {
        const absolute = resolve10(filePath);
        const existing = this.ownWriteTimers.get(absolute);
        if (existing) clearTimeout(existing);
        const timer = setTimeout(() => {
          this.ownWriteTimers.delete(absolute);
        }, OWN_WRITE_SUPPRESSION_MS);
        this.ownWriteTimers.set(absolute, timer);
      }
      isOwnWrite(filePath) {
        return this.ownWriteTimers.has(resolve10(filePath));
      }
    };
    taskStore = new TaskStore();
    taskStore.setMaxListeners(0);
  }
});

// packages/daemon/src/lib/dot-path.ts
function setByPath(obj, path4, value) {
  const keys = path4.split(".");
  const last = keys.pop();
  let i = 0;
  const target = keys.reduce((o, k) => {
    const nextKey = keys[i + 1] ?? last;
    if (o[k] === void 0) o[k] = /^\d+$/.test(nextKey) ? [] : {};
    i++;
    return o[k];
  }, obj);
  target[last] = value;
}
var init_dot_path = __esm({
  "packages/daemon/src/lib/dot-path.ts"() {
    "use strict";
  }
});

// packages/daemon/src/command-center/actions/contract.ts
var init_contract = __esm({
  "packages/daemon/src/command-center/actions/contract.ts"() {
    "use strict";
    init_src2();
  }
});

// packages/daemon/src/lib/session-monitor.ts
import { execFileSync as execFileSync2 } from "node:child_process";
function getListeningPids() {
  try {
    const raw = execFileSync2("lsof", ["-nP", "-iTCP", "-sTCP:LISTEN", "-FpPn"], {
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "ignore"],
      timeout: 2e3
    });
    const pids = /* @__PURE__ */ new Set();
    let currentPid = null;
    for (const line of raw.split("\n")) {
      if (line.startsWith("p")) {
        currentPid = line.slice(1);
      } else if (line.startsWith("n") && currentPid) {
        const match = line.match(/:(\d+)$/);
        if (match) {
          const port = parseInt(match[1], 10);
          if (port >= 1024 && port <= 2e4) pids.add(currentPid);
        }
      }
    }
    return pids;
  } catch {
    return /* @__PURE__ */ new Set();
  }
}
function getProcessTree() {
  try {
    const raw = execFileSync2("ps", ["-axo", "pid=,ppid="], {
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "ignore"],
      timeout: 2e3
    });
    const tree = /* @__PURE__ */ new Map();
    for (const line of raw.trim().split("\n")) {
      const parts = line.trim().split(/\s+/);
      if (parts.length === 2) tree.set(parts[0], parts[1]);
    }
    return tree;
  } catch {
    return /* @__PURE__ */ new Map();
  }
}
function computePortPanes(panes, { listeners, tree } = {}) {
  const resolvedListeners = listeners ?? getListeningPids();
  const resolvedTree = tree ?? getProcessTree();
  if (resolvedListeners.size === 0) return /* @__PURE__ */ new Set();
  const panePids = new Map(panes.map((p) => [p.pid, p.id]));
  const result = /* @__PURE__ */ new Set();
  for (const listenerPid of resolvedListeners) {
    let pid = listenerPid;
    while (pid && pid !== "0") {
      if (panePids.has(pid)) {
        result.add(panePids.get(pid));
        break;
      }
      pid = resolvedTree.get(pid);
    }
  }
  return result;
}
function computeAgentStates(panes) {
  const states = /* @__PURE__ */ new Map();
  for (const pane of panes) {
    const role = pane.role ?? "";
    if (role === "lead" || role === "teammate") {
      states.set(pane.id, SPINNERS.test(pane.title ?? "") ? "busy" : "idle");
      continue;
    }
    const cmd = (pane.cmd ?? "").toLowerCase();
    if (!cmd.includes("claude") && !cmd.includes("codex")) {
      states.set(pane.id, null);
      continue;
    }
    states.set(pane.id, SPINNERS.test(pane.title ?? "") ? "busy" : "idle");
  }
  return states;
}
var SPINNERS;
var init_session_monitor = __esm({
  "packages/daemon/src/lib/session-monitor.ts"() {
    "use strict";
    SPINNERS = /^[⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏⠂⠒⠢⠆⠐⠠⠄◐◓◑◒|/\\-] /;
  }
});

// packages/daemon/src/terminal/PtyAdapter.ts
var PtySpawnError;
var init_PtyAdapter = __esm({
  "packages/daemon/src/terminal/PtyAdapter.ts"() {
    "use strict";
    PtySpawnError = class extends Error {
      adapter;
      code;
      constructor(args) {
        super(args.message, args.cause !== void 0 ? { cause: args.cause } : void 0);
        this.name = "PtySpawnError";
        this.adapter = args.adapter;
        this.code = args.code;
      }
    };
  }
});

// packages/daemon/src/terminal/NodePtyAdapter.ts
import { chmodSync, existsSync as existsSync9, statSync as statSync2 } from "node:fs";
import { dirname as dirname5, join as join6 } from "node:path";
import { createRequire } from "node:module";
import * as pty from "node-pty";
function candidateSpawnHelperPaths() {
  const requireForNodePty = createRequire(import.meta.url);
  let pkgJsonPath;
  try {
    pkgJsonPath = requireForNodePty.resolve("node-pty/package.json");
  } catch {
    return [];
  }
  const pkgDir = dirname5(pkgJsonPath);
  return [
    join6(pkgDir, "build", "Release", "spawn-helper"),
    join6(pkgDir, "build", "Debug", "spawn-helper"),
    join6(pkgDir, "prebuilds", `${process.platform}-${process.arch}`, "spawn-helper")
  ];
}
function ensureNodePtySpawnHelperExecutable(options = {}) {
  if (process.platform === "win32") return;
  if (!options.force && !options.explicitPath && helperEnsured) return;
  const candidates = options.explicitPath ? [options.explicitPath] : candidateSpawnHelperPaths();
  for (const candidate of candidates) {
    if (!existsSync9(candidate)) continue;
    try {
      chmodSync(candidate, 493);
    } catch {
    }
  }
  if (!options.explicitPath) helperEnsured = true;
}
function assertValidCwd(cwd, statFn) {
  let stats;
  try {
    stats = statFn(cwd);
  } catch (err) {
    throw new PtySpawnError({
      adapter: ADAPTER_ID,
      code: "cwd_invalid",
      message: `cwd does not exist or cannot be stat'd: ${cwd}`,
      cause: err
    });
  }
  if (!stats.isDirectory()) {
    throw new PtySpawnError({
      adapter: ADAPTER_ID,
      code: "cwd_invalid",
      message: `cwd is not a directory: ${cwd}`
    });
  }
}
var ADAPTER_ID, helperEnsured, NodePtyProcess, NodePtyAdapter, defaultNodePtyAdapter;
var init_NodePtyAdapter = __esm({
  "packages/daemon/src/terminal/NodePtyAdapter.ts"() {
    "use strict";
    init_PtyAdapter();
    ADAPTER_ID = "node-pty";
    helperEnsured = false;
    NodePtyProcess = class {
      exited = false;
      child;
      dataListeners = /* @__PURE__ */ new Set();
      exitListeners = /* @__PURE__ */ new Set();
      constructor(child) {
        this.child = child;
        this.child.onData((data) => {
          const buf = Buffer.isBuffer(data) ? data : Buffer.from(String(data), "utf8");
          for (const listener of this.dataListeners) listener(buf);
        });
        this.child.onExit((evt) => {
          this.exited = true;
          const event = {
            exitCode: evt.exitCode ?? 0,
            signal: typeof evt.signal === "number" ? evt.signal : null
          };
          for (const listener of this.exitListeners) listener(event);
        });
      }
      get pid() {
        return this.child.pid;
      }
      write(data) {
        if (this.exited) return;
        if (typeof data === "string") this.child.write(data);
        else this.child.write(Buffer.from(data).toString("binary"));
      }
      resize(cols, rows) {
        if (this.exited) return;
        if (!Number.isInteger(cols) || cols <= 0)
          throw new RangeError("cols must be a positive integer");
        if (!Number.isInteger(rows) || rows <= 0)
          throw new RangeError("rows must be a positive integer");
        try {
          this.child.resize(cols, rows);
        } catch {
        }
      }
      kill(signal) {
        if (this.exited) return;
        try {
          this.child.kill(typeof signal === "number" ? String(signal) : signal);
        } catch {
          this.exited = true;
          for (const listener of this.exitListeners) listener({ exitCode: 0, signal: null });
        }
      }
      onData(callback) {
        this.dataListeners.add(callback);
        return () => {
          this.dataListeners.delete(callback);
        };
      }
      onExit(callback) {
        if (this.exited) {
          return () => void 0;
        }
        this.exitListeners.add(callback);
        return () => {
          this.exitListeners.delete(callback);
        };
      }
    };
    NodePtyAdapter = class {
      id = ADAPTER_ID;
      spawnPty;
      statCwd;
      skipHelperEnsure;
      constructor(options = {}) {
        this.spawnPty = options.spawnPty ?? pty.spawn;
        this.statCwd = options.statCwd ?? statSync2;
        this.skipHelperEnsure = options.skipHelperEnsure ?? false;
      }
      async spawn(input) {
        if (!this.skipHelperEnsure) ensureNodePtySpawnHelperExecutable();
        return this.spawnSyncInternal(input);
      }
      spawnSync(input) {
        if (!this.skipHelperEnsure) ensureNodePtySpawnHelperExecutable();
        return this.spawnSyncInternal(input);
      }
      spawnSyncInternal(input) {
        assertValidCwd(input.cwd, this.statCwd);
        if (!Number.isInteger(input.cols) || input.cols <= 0) {
          throw new PtySpawnError({
            adapter: ADAPTER_ID,
            code: "unknown",
            message: `cols must be a positive integer (got ${input.cols})`
          });
        }
        if (!Number.isInteger(input.rows) || input.rows <= 0) {
          throw new PtySpawnError({
            adapter: ADAPTER_ID,
            code: "unknown",
            message: `rows must be a positive integer (got ${input.rows})`
          });
        }
        const env = {};
        for (const [key, value] of Object.entries(input.env)) {
          if (typeof value === "string") env[key] = value;
        }
        let child;
        try {
          child = this.spawnPty(input.shell, [...input.args ?? []], {
            name: input.name ?? "xterm-256color",
            cols: input.cols,
            rows: input.rows,
            cwd: input.cwd,
            env,
            encoding: input.encoding === "utf8" ? "utf8" : null
          });
        } catch (err) {
          const errno = err?.code;
          if (errno === "ENOENT") {
            throw new PtySpawnError({
              adapter: ADAPTER_ID,
              code: "shell_not_found",
              message: `shell not found in PATH: ${input.shell}`,
              cause: err
            });
          }
          if (errno === "EACCES" || errno === "EPERM") {
            throw new PtySpawnError({
              adapter: ADAPTER_ID,
              code: "permission_denied",
              message: `permission denied spawning ${input.shell}`,
              cause: err
            });
          }
          throw new PtySpawnError({
            adapter: ADAPTER_ID,
            code: "unknown",
            message: `node-pty spawn failed: ${err instanceof Error ? err.message : String(err)}`,
            cause: err
          });
        }
        return new NodePtyProcess(child);
      }
    };
    defaultNodePtyAdapter = new NodePtyAdapter();
  }
});

// packages/daemon/src/server/pty-bridge.ts
import { EventEmitter as EventEmitter2 } from "node:events";
import * as fs from "node:fs";
function assertValidCwd2(cwd, statCwd = fs.statSync) {
  let stats;
  try {
    stats = statCwd(cwd);
  } catch (err) {
    const errno = err?.code;
    if (errno === "ENOENT") {
      throw new TerminalCwdError({ cwd, reason: "notFound", cause: err });
    }
    throw new TerminalCwdError({ cwd, reason: "statFailed", cause: err });
  }
  if (!stats.isDirectory()) {
    throw new TerminalCwdError({ cwd, reason: "notDirectory" });
  }
}
function cleanEnv() {
  const env = {};
  for (const [key, value] of Object.entries(process.env)) {
    if (value !== void 0) env[key] = value;
  }
  return env;
}
function outputToBuffer(data) {
  if (Buffer.isBuffer(data)) return data;
  if (data instanceof ArrayBuffer) return Buffer.from(data);
  if (ArrayBuffer.isView(data)) {
    return Buffer.from(data.buffer, data.byteOffset, data.byteLength);
  }
  return Buffer.from(String(data), "utf8");
}
function assertPositiveDimension(name, value) {
  if (!Number.isInteger(value) || value <= 0) {
    throw new Error(`${name} must be a positive integer`);
  }
}
function readPositiveIntEnv2(name, fallback) {
  const parsed = Number.parseInt(process.env[name] ?? "", 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : fallback;
}
var DEFAULT_RING_BUFFER_BYTES, TerminalCwdError, PtyBridge;
var init_pty_bridge = __esm({
  "packages/daemon/src/server/pty-bridge.ts"() {
    "use strict";
    init_NodePtyAdapter();
    DEFAULT_RING_BUFFER_BYTES = 256 * 1024;
    TerminalCwdError = class _TerminalCwdError extends Error {
      cwd;
      reason;
      constructor(args) {
        const message = _TerminalCwdError.formatMessage(args.cwd, args.reason);
        super(message, args.cause !== void 0 ? { cause: args.cause } : void 0);
        this.name = "TerminalCwdError";
        this.cwd = args.cwd;
        this.reason = args.reason;
      }
      static formatMessage(cwd, reason) {
        switch (reason) {
          case "notFound":
            return `cwd does not exist: ${cwd}`;
          case "notDirectory":
            return `cwd is not a directory: ${cwd}`;
          case "statFailed":
            return `cwd stat failed: ${cwd}`;
        }
      }
    };
    PtyBridge = class extends EventEmitter2 {
      ptyProcess = null;
      dataDispose = null;
      exitDispose = null;
      exitPoll = null;
      outputTimer = null;
      outputChunks = [];
      pausedOutputChunks = [];
      outputPaused = false;
      replayChunks = [];
      replayBytes = 0;
      lastCwd = null;
      options;
      adapter;
      ringBufferBytes;
      statCwd;
      constructor(options = {}) {
        super();
        this.options = options;
        if (options.ptyAdapter) {
          this.adapter = options.ptyAdapter;
        } else if (options.pty?.spawn) {
          this.adapter = new NodePtyAdapter({
            spawnPty: options.pty.spawn,
            statCwd: options.statCwd,
            skipHelperEnsure: true
          });
        } else {
          this.adapter = defaultNodePtyAdapter;
        }
        this.ringBufferBytes = options.ringBufferBytes ?? readPositiveIntEnv2("TMUX_IDE_PTY_RING_BUFFER_BYTES", DEFAULT_RING_BUFFER_BYTES);
        this.statCwd = options.statCwd ?? fs.statSync;
      }
      /**
       * Returns the cwd used by the most recent spawn (or restart). `null`
       * if the bridge has never been spawned. Used by ws-route to detect
       * stale-cwd reuse and trigger a respawn.
       */
      getCwd() {
        return this.lastCwd;
      }
      get pid() {
        return this.ptyProcess?.pid ?? null;
      }
      // `cols`/`rows` aren't on the canonical PtyProcess shape — we mirror the
      // most recent value the bridge handed to the adapter so external readers
      // (status views) can still see the size without rummaging in the child.
      lastCols = null;
      lastRows = null;
      get cols() {
        return this.lastCols;
      }
      get rows() {
        return this.lastRows;
      }
      get running() {
        return this.ptyProcess !== null;
      }
      getReplayBuffer() {
        return Buffer.concat(this.replayChunks, this.replayBytes);
      }
      flushReplayBuffer() {
        this.replayChunks = [];
        this.replayBytes = 0;
      }
      spawn(cols, rows, spawnOptions = {}) {
        if (this.running) {
          throw new Error("PTY already spawned");
        }
        assertPositiveDimension("cols", cols);
        assertPositiveDimension("rows", rows);
        const defaultShell = this.options.shell ?? process.env.SHELL ?? "bash";
        let executable = spawnOptions.cmd?.[0] ?? defaultShell;
        let args = spawnOptions.cmd ? spawnOptions.cmd.slice(1) : this.options.args ?? ["-l"];
        if (executable === "__login_shell__" && args.length > 0) {
          const innerCmd = args.map((part) => `'${part.replace(/'/g, "'\\''")}'`).join(" ");
          executable = defaultShell;
          args = ["-l", "-c", `exec ${innerCmd}`];
        }
        const cwd = spawnOptions.cwd ?? this.options.cwd ?? process.env.HOME ?? "/";
        assertValidCwd2(cwd, this.statCwd);
        const env = this.options.env ?? cleanEnv();
        const spawnInput = {
          shell: executable,
          args,
          cwd,
          cols,
          rows,
          env,
          name: this.options.name ?? "xterm-256color",
          encoding: null
        };
        let child;
        try {
          child = this.adapter.spawnSync(spawnInput);
        } catch (err) {
          if (err.code === "ENOENT" && executable === "tmux-ide") {
            throw new Error("tmux-ide not found in PATH", { cause: err });
          }
          throw err;
        }
        this.ptyProcess = child;
        this.lastCwd = cwd;
        this.lastCols = cols;
        this.lastRows = rows;
        this.exitDispose = child.onExit(({ exitCode, signal }) => {
          this.emitExit({ code: exitCode, signal: signal ?? null });
        });
        this.dataDispose = child.onData((data) => {
          this.enqueueOutput(outputToBuffer(data));
        });
        this.startExitPoll(child);
      }
      /**
       * Stop the currently-running PTY process synchronously. Drops listeners
       * and clears replay so a follow-up spawn starts from a clean slate.
       * Idempotent — no-op when no process is running.
       *
       * Used by {@link restartWith} to swap out a sticky bridge whose cwd no
       * longer matches the client request.
       */
      stopProcess(signal = "SIGTERM") {
        if (!this.ptyProcess) return;
        const child = this.ptyProcess;
        this.disposeListeners();
        this.ptyProcess = null;
        this.lastCwd = null;
        this.flushReplayBuffer();
        try {
          child.kill(signal);
        } catch {
        }
      }
      /**
       * Stop the running process (if any) and spawn a new one with the
       * supplied options. Preserves bridge identity (id, registry slot) but
       * resets the replay buffer — the prior process is gone, there is no
       * meaningful output to replay. Used when a reconnect requests a new
       * cwd; modeled on t3code's `stopProcess + spawn` pattern.
       */
      restartWith(cols, rows, spawnOptions = {}) {
        this.stopProcess("SIGTERM");
        this.spawn(cols, rows, spawnOptions);
      }
      pause() {
        this.outputPaused = true;
      }
      resume() {
        if (!this.outputPaused) return;
        this.flushCoalescedOutput();
        this.outputPaused = false;
        this.flushPausedOutput();
      }
      write(bytes) {
        if (!this.ptyProcess) {
          throw new Error("PTY is not running");
        }
        this.ptyProcess.write(typeof bytes === "string" ? bytes : Buffer.from(bytes));
      }
      resize(cols, rows) {
        assertPositiveDimension("cols", cols);
        assertPositiveDimension("rows", rows);
        if (!this.ptyProcess) {
          throw new Error("PTY is not running");
        }
        this.ptyProcess.resize(cols, rows);
        this.lastCols = cols;
        this.lastRows = rows;
      }
      kill(signal = "SIGTERM") {
        if (!this.ptyProcess) return;
        try {
          this.ptyProcess.kill(signal);
        } catch {
        }
      }
      dispose() {
        this.disposeListeners();
        this.kill("SIGTERM");
        this.flushReplayBuffer();
      }
      disposeListeners() {
        this.flushAllOutput();
        this.dataDispose?.();
        this.exitDispose?.();
        if (this.exitPoll) {
          clearInterval(this.exitPoll);
          this.exitPoll = null;
        }
        this.dataDispose = null;
        this.exitDispose = null;
      }
      enqueueOutput(bytes) {
        if (bytes.byteLength === 0) return;
        const coalesceMs = this.options.coalesceMs ?? 8;
        if (coalesceMs <= 0) {
          this.deliverOutput(bytes);
          return;
        }
        this.outputChunks.push(bytes);
        if (this.outputTimer) return;
        this.outputTimer = setTimeout(() => {
          this.outputTimer = null;
          this.flushCoalescedOutput();
        }, coalesceMs);
        this.outputTimer.unref?.();
      }
      flushCoalescedOutput() {
        if (this.outputTimer) {
          clearTimeout(this.outputTimer);
          this.outputTimer = null;
        }
        if (this.outputChunks.length === 0) return;
        const chunks = this.outputChunks;
        this.outputChunks = [];
        this.deliverOutput(Buffer.concat(chunks));
      }
      deliverOutput(bytes) {
        this.appendReplay(bytes);
        if (this.outputPaused) {
          this.pausedOutputChunks.push(bytes);
          return;
        }
        this.emit("output", bytes);
      }
      flushPausedOutput() {
        if (this.pausedOutputChunks.length === 0) return;
        const chunks = this.pausedOutputChunks;
        this.pausedOutputChunks = [];
        this.emit("output", Buffer.concat(chunks));
      }
      flushAllOutput() {
        if (this.outputTimer) {
          clearTimeout(this.outputTimer);
          this.outputTimer = null;
        }
        if (this.outputChunks.length > 0) this.appendReplay(Buffer.concat(this.outputChunks));
        const chunks = [...this.pausedOutputChunks, ...this.outputChunks];
        this.pausedOutputChunks = [];
        this.outputChunks = [];
        if (chunks.length > 0) {
          const bytes = Buffer.concat(chunks);
          this.emit("output", bytes);
        }
      }
      appendReplay(bytes) {
        if (this.ringBufferBytes <= 0 || bytes.byteLength === 0) return;
        if (bytes.byteLength >= this.ringBufferBytes) {
          const tail = bytes.subarray(bytes.byteLength - this.ringBufferBytes);
          this.replayChunks = [Buffer.from(tail)];
          this.replayBytes = tail.byteLength;
          return;
        }
        this.replayChunks.push(Buffer.from(bytes));
        this.replayBytes += bytes.byteLength;
        while (this.replayBytes > this.ringBufferBytes && this.replayChunks.length > 0) {
          const first = this.replayChunks[0];
          const overflow = this.replayBytes - this.ringBufferBytes;
          if (first.byteLength <= overflow) {
            this.replayChunks.shift();
            this.replayBytes -= first.byteLength;
          } else {
            this.replayChunks[0] = first.subarray(overflow);
            this.replayBytes -= overflow;
          }
        }
      }
      startExitPoll(child) {
        this.exitPoll = setInterval(() => {
          if (this.ptyProcess !== child) {
            this.disposeListeners();
            return;
          }
          try {
            process.kill(child.pid, 0);
          } catch (err) {
            if (err.code === "ESRCH") {
              this.emitExit({ code: 0, signal: null });
            }
          }
        }, 100);
        this.exitPoll.unref?.();
      }
      emitExit(exit) {
        if (!this.ptyProcess) return;
        this.flushAllOutput();
        this.disposeListeners();
        this.ptyProcess = null;
        this.lastCwd = null;
        this.flushReplayBuffer();
        this.emit("exit", exit);
      }
    };
  }
});

// packages/daemon/src/server/ws-route.ts
function isPositiveInteger(value) {
  return Number.isInteger(value) && Number(value) > 0;
}
function rawDataToBuffer(data) {
  if (typeof data === "string") return Buffer.from(data, "utf8");
  if (Buffer.isBuffer(data)) return data;
  if (data instanceof ArrayBuffer) return Buffer.from(data);
  if (Array.isArray(data)) return Buffer.concat(data);
  return Buffer.from(data);
}
function rawDataToText(data) {
  return typeof data === "string" ? data : rawDataToBuffer(data).toString("utf8");
}
function isJsonControlFrame(data, isBinary) {
  return !isBinary && rawDataToText(data).startsWith("{");
}
function parseJsonObject(data) {
  const parsed = JSON.parse(rawDataToText(data));
  if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
    throw new Error("control frame must be a JSON object");
  }
  return parsed;
}
function parseInitFrame(data) {
  const frame = parseJsonObject(data);
  if (frame.type !== "init") {
    throw new Error("first frame must be init");
  }
  if (!isPositiveInteger(frame.cols) || !isPositiveInteger(frame.rows)) {
    throw new Error("init requires positive integer cols and rows");
  }
  if (frame.cwd !== void 0 && typeof frame.cwd !== "string") {
    throw new Error("init cwd must be a string");
  }
  if (frame.cmd !== void 0 && (!Array.isArray(frame.cmd) || frame.cmd.length === 0 || !frame.cmd.every((part) => typeof part === "string"))) {
    throw new Error("init cmd must be a non-empty string array");
  }
  return {
    type: "init",
    cols: frame.cols,
    rows: frame.rows,
    ...frame.cwd !== void 0 ? { cwd: frame.cwd } : {},
    ...frame.cmd !== void 0 ? { cmd: frame.cmd } : {}
  };
}
function parseResizeFrame(data) {
  const frame = parseJsonObject(data);
  if (frame.type !== "resize") {
    throw new Error(`unsupported control frame: ${String(frame.type)}`);
  }
  if (!isPositiveInteger(frame.cols) || !isPositiveInteger(frame.rows)) {
    throw new Error("resize requires positive integer cols and rows");
  }
  return { type: "resize", cols: frame.cols, rows: frame.rows };
}
function sendError(ws, message, extras) {
  if (ws.readyState !== WS_OPEN) return;
  const frame = { type: "error", message };
  if (extras?.reason !== void 0) frame.reason = extras.reason;
  if (extras?.cwd !== void 0) frame.cwd = extras.cwd;
  ws.send(JSON.stringify(frame));
}
function sendCwdError(ws, err) {
  sendError(ws, err.message, {
    reason: CWD_ERROR_WIRE_REASON[err.reason],
    cwd: err.cwd
  });
}
function closeWs(ws) {
  if (ws.readyState === WS_OPEN) {
    ws.close();
  }
}
function backpressureBytes() {
  const parsed = Number.parseInt(process.env.TMUX_IDE_PTY_BACKPRESSURE_BYTES ?? "", 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : DEFAULT_BACKPRESSURE_BYTES;
}
function bridgeIdleMs(options) {
  if (options?.idleMs !== void 0) return options.idleMs;
  const parsed = Number.parseInt(process.env.TMUX_IDE_BRIDGE_IDLE_MS ?? "", 10);
  return Number.isFinite(parsed) && parsed >= 0 ? parsed : DEFAULT_BRIDGE_IDLE_MS;
}
function shutdownPtyBridges() {
  defaultPtyBridgeRegistry.shutdown();
}
function handlePtyWebSocket(ws, id, options = {}) {
  const socket = ws;
  let bridge = null;
  let initialized = false;
  let ptyExited = false;
  let killTimer = null;
  let drainTimer = null;
  let releaseBridge = null;
  let outputListener = null;
  let exitListener = null;
  const backpressureThreshold = backpressureBytes();
  const resumeThreshold = Math.floor(backpressureThreshold / 2);
  const clearKillTimer = () => {
    if (!killTimer) return;
    clearTimeout(killTimer);
    killTimer = null;
  };
  const clearDrainTimer = () => {
    if (!drainTimer) return;
    clearTimeout(drainTimer);
    drainTimer = null;
  };
  const scheduleDrainCheck = () => {
    if (drainTimer || !bridge) return;
    drainTimer = setTimeout(() => {
      drainTimer = null;
      if (!bridge || socket.readyState !== WS_OPEN) return;
      if ((socket.bufferedAmount ?? 0) <= resumeThreshold) {
        bridge.resume();
        return;
      }
      scheduleDrainCheck();
    }, DRAIN_POLL_MS);
    drainTimer.unref?.();
  };
  const maybePauseForBackpressure = () => {
    if (!bridge || (socket.bufferedAmount ?? 0) <= backpressureThreshold) return;
    bridge.pause();
    scheduleDrainCheck();
  };
  const closeWithError = (message) => {
    sendError(socket, message);
    closeWs(socket);
  };
  const closeWithCwdError = (err) => {
    sendCwdError(socket, err);
    closeWs(socket);
  };
  const attachBridgeEvents = (ptyBridge) => {
    outputListener = (bytes) => {
      if (socket.readyState === WS_OPEN) {
        maybePauseForBackpressure();
        socket.send(bytes, { binary: true });
        maybePauseForBackpressure();
      }
    };
    exitListener = (exit) => {
      ptyExited = true;
      clearKillTimer();
      clearDrainTimer();
      if (socket.readyState === WS_OPEN) {
        socket.send(JSON.stringify({ type: "exit", code: exit.code, signal: exit.signal }));
        socket.close();
      }
    };
    ptyBridge.on("output", outputListener);
    ptyBridge.on("exit", exitListener);
  };
  const detachBridgeEvents = () => {
    if (bridge && outputListener) bridge.off("output", outputListener);
    if (bridge && exitListener) bridge.off("exit", exitListener);
    outputListener = null;
    exitListener = null;
  };
  socket.on("message", (data, isBinary) => {
    if (!initialized) {
      if (!isJsonControlFrame(data, isBinary)) {
        closeWithError("init frame required before input");
        return;
      }
      let init2;
      try {
        init2 = parseInitFrame(data);
      } catch (err) {
        closeWithError(err instanceof Error ? err.message : String(err));
        return;
      }
      const registry = options.registry ?? defaultPtyBridgeRegistry;
      const acquired = registry.acquire(
        id,
        options.createBridge ?? ((bridgeId) => new PtyBridge({ id: bridgeId })),
        {
          idleMs: options.idleMs
        }
      );
      bridge = acquired.bridge;
      releaseBridge = acquired.release;
      attachBridgeEvents(bridge);
      try {
        const spawnOptions = {};
        if (init2.cwd !== void 0) spawnOptions.cwd = init2.cwd;
        if (init2.cmd !== void 0) spawnOptions.cmd = init2.cmd;
        const currentCwd = bridge.getCwd?.() ?? null;
        const cwdChanged = init2.cwd !== void 0 && currentCwd !== null && currentCwd !== init2.cwd;
        if (!acquired.reused) {
          bridge.spawn(init2.cols, init2.rows, spawnOptions);
        } else if (cwdChanged && bridge.restartWith) {
          bridge.restartWith(init2.cols, init2.rows, spawnOptions);
          if (socket.readyState === WS_OPEN) {
            socket.send(JSON.stringify({ type: "replay-end", bytes: 0 }));
          }
        } else {
          try {
            bridge.resize(init2.cols, init2.rows);
          } catch {
          }
          const replay = bridge.getReplayBuffer?.() ?? Buffer.alloc(0);
          if (replay.byteLength > 0 && socket.readyState === WS_OPEN) {
            socket.send(replay, { binary: true });
          }
          if (socket.readyState === WS_OPEN) {
            socket.send(JSON.stringify({ type: "replay-end", bytes: replay.byteLength }));
          }
        }
        initialized = true;
      } catch (err) {
        detachBridgeEvents();
        releaseBridge?.();
        if (err instanceof TerminalCwdError) {
          closeWithCwdError(err);
        } else {
          closeWithError(`spawn failed: ${err instanceof Error ? err.message : String(err)}`);
        }
      }
      return;
    }
    if (isJsonControlFrame(data, isBinary)) {
      try {
        const resize = parseResizeFrame(data);
        bridge?.resize(resize.cols, resize.rows);
      } catch (err) {
        closeWithError(err instanceof Error ? err.message : String(err));
      }
      return;
    }
    try {
      bridge?.write(rawDataToBuffer(data));
    } catch (err) {
      closeWithError(err instanceof Error ? err.message : String(err));
    }
  });
  socket.on("close", () => {
    clearDrainTimer();
    detachBridgeEvents();
    if (!bridge || ptyExited) return;
    releaseBridge?.();
  });
  socket.on("error", () => {
    closeWs(socket);
  });
  return {
    getBridge: () => bridge
  };
}
var WS_OPEN, DEFAULT_BACKPRESSURE_BYTES, DRAIN_POLL_MS, DEFAULT_BRIDGE_IDLE_MS, CWD_ERROR_WIRE_REASON, PtyBridgeRegistry, defaultPtyBridgeRegistry;
var init_ws_route = __esm({
  "packages/daemon/src/server/ws-route.ts"() {
    "use strict";
    init_pty_bridge();
    WS_OPEN = 1;
    DEFAULT_BACKPRESSURE_BYTES = 1 << 20;
    DRAIN_POLL_MS = 16;
    DEFAULT_BRIDGE_IDLE_MS = 3e5;
    CWD_ERROR_WIRE_REASON = {
      notFound: "cwd-not-found",
      notDirectory: "cwd-not-directory",
      statFailed: "cwd-stat-failed"
    };
    PtyBridgeRegistry = class {
      entries = /* @__PURE__ */ new Map();
      acquire(id, createBridge, options = {}) {
        let entry = this.entries.get(id);
        const reused = !!entry && entry.bridge.running !== false;
        if (!entry || entry.bridge.running === false) {
          entry = { bridge: createBridge(id), clients: 0, idleTimer: null };
          this.entries.set(id, entry);
          entry.bridge.on("exit", () => {
            this.entries.delete(id);
          });
        }
        if (entry.idleTimer) {
          clearTimeout(entry.idleTimer);
          entry.idleTimer = null;
        }
        entry.clients++;
        let released = false;
        const release = () => {
          if (released) return;
          released = true;
          const current = this.entries.get(id);
          if (!current) return;
          current.clients = Math.max(0, current.clients - 1);
          if (current.clients > 0 || current.bridge.running === false) return;
          const idleMs = bridgeIdleMs(options);
          current.idleTimer = setTimeout(() => {
            const latest = this.entries.get(id);
            if (!latest || latest.clients > 0) return;
            latest.bridge.kill("SIGTERM");
            this.entries.delete(id);
          }, idleMs);
          current.idleTimer.unref?.();
        };
        return { bridge: entry.bridge, reused, release };
      }
      /**
       * Look up a bridge by id without acquiring it. Returns `null` when no
       * bridge is registered for the id. Used by server-side action handlers
       * (terminal.respawn, terminal.stop) that need to operate on an existing
       * bridge — they should not bump the client refcount.
       */
      peek(id) {
        const entry = this.entries.get(id);
        if (!entry) return null;
        if (entry.bridge.running === false) return null;
        return entry.bridge;
      }
      /**
       * Drop the bridge for `id`, killing it synchronously. Used by the
       * terminal.stop action handler to release a sticky bridge whose owner
       * has explicitly asked to terminate it.
       */
      delete(id) {
        const entry = this.entries.get(id);
        if (!entry) return false;
        if (entry.idleTimer) {
          clearTimeout(entry.idleTimer);
          entry.idleTimer = null;
        }
        try {
          entry.bridge.kill("SIGTERM");
        } catch {
        }
        this.entries.delete(id);
        return true;
      }
      shutdown() {
        for (const entry of this.entries.values()) {
          if (entry.idleTimer) clearTimeout(entry.idleTimer);
          entry.bridge.kill("SIGTERM");
        }
        this.entries.clear();
      }
      size() {
        return this.entries.size;
      }
    };
    defaultPtyBridgeRegistry = new PtyBridgeRegistry();
  }
});

// packages/daemon/src/lib/tmux-exec.ts
import { execFileSync as execFileSync3 } from "node:child_process";
function runTmux2(args, options) {
  return _executor2("tmux", args, options);
}
function tmux(...args) {
  try {
    return _executor2("tmux", args, { stdio: ["pipe", "pipe", "pipe"] }).trim();
  } catch (error) {
    const stderr = error?.stderr?.toString() ?? "";
    if (stderr.includes("no server running") || stderr.includes("can't find session")) {
      return "";
    }
    throw error;
  }
}
var _executor2;
var init_tmux_exec = __esm({
  "packages/daemon/src/lib/tmux-exec.ts"() {
    "use strict";
    _executor2 = (cmd, args, options) => execFileSync3(cmd, args, { encoding: "utf-8", ...options }).toString();
  }
});

// packages/daemon/src/widgets/lib/pane-comms.ts
function listSessionPanes(session) {
  const format = [
    "#{pane_id}",
    "#{pane_index}",
    "#{pane_title}",
    "#{pane_current_command}",
    "#{pane_width}",
    "#{pane_height}",
    "#{pane_active}",
    "#{@ide_role}",
    "#{@ide_name}",
    "#{@ide_type}"
  ].join("	");
  const output = tmux("list-panes", "-t", session, "-F", format);
  if (!output) return [];
  return output.split("\n").filter(Boolean).map((line) => {
    const [id, index, title, cmd, width, height, active2, role, name, type] = line.split("	");
    return {
      id,
      index: parseInt(index, 10),
      title,
      currentCommand: cmd,
      width: parseInt(width, 10),
      height: parseInt(height, 10),
      active: active2 === "1",
      role: role || null,
      name: name || null,
      type: type || null
    };
  });
}
function getPaneBusyStatus(session, paneId) {
  const panes = listSessionPanes(session);
  const pane = panes.find((p) => p.id === paneId);
  if (!pane) return "busy";
  const cmd = pane.currentCommand.toLowerCase();
  if (cmd.startsWith("claude") || cmd.startsWith("codex")) return "agent";
  if (SHELL_COMMANDS.has(cmd)) return "idle";
  return "busy";
}
function sendText(session, paneId, text) {
  tmux("send-keys", "-t", paneId, "-l", "--", text);
}
function sendLiteralToPane(_session, paneId, text) {
  tmux("send-keys", "-t", paneId, "-l", "--", text);
}
function sendEnterToPane(_session, paneId) {
  tmux("send-keys", "-t", paneId, "Enter");
}
function sleepMs2(ms) {
  Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, ms);
}
function sendCommand(session, paneId, command2) {
  const status4 = getPaneBusyStatus(session, paneId);
  try {
    tmux("send-keys", "-t", paneId, "-l", "--", command2);
  } catch {
    return false;
  }
  if (status4 === "agent") {
    if (command2.length < 200) {
      sleepMs2(150);
    } else {
      sleepMs2(5e3);
      tmux("send-keys", "-t", paneId, "Enter");
      sleepMs2(2e3);
    }
    tmux("send-keys", "-t", paneId, "Enter");
    return true;
  }
  tmux("send-keys", "-t", paneId, "Enter");
  return true;
}
function captureLastLine(paneId) {
  try {
    return runTmux2(["capture-pane", "-t", paneId, "-p", "-S", "-1"], {
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "ignore"]
    }).trim();
  } catch {
    return "";
  }
}
var SHELL_COMMANDS;
var init_pane_comms = __esm({
  "packages/daemon/src/widgets/lib/pane-comms.ts"() {
    "use strict";
    init_tmux_exec();
    SHELL_COMMANDS = /* @__PURE__ */ new Set(["zsh", "bash", "sh", "fish"]);
  }
});

// packages/daemon/src/lib/workspace-registry.ts
import { EventEmitter as EventEmitter3 } from "node:events";
import { existsSync as existsSync10, mkdirSync as mkdirSync6, readFileSync as readFileSync8, renameSync as renameSync5, writeFileSync as writeFileSync7 } from "node:fs";
import { homedir as homedir3 } from "node:os";
import { dirname as dirname6, join as join7 } from "node:path";
import { z as z15 } from "zod";
function getDefaultWorkspaceRegistry() {
  if (!_default) _default = new WorkspaceRegistry();
  return _default;
}
function defaultListSessions() {
  const { execFileSync: execFileSync10 } = __require("node:child_process");
  try {
    const raw = execFileSync10("tmux", ["list-sessions", "-F", "#{session_name}"], {
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "pipe"]
    });
    return raw.split("\n").filter(Boolean);
  } catch {
    return [];
  }
}
var REGISTRY_DIR_ENV2, RegistryFileSchemaZ, WorkspaceAlreadyExistsError, WorkspaceNotFoundError, WorkspaceRegistry, _default;
var init_workspace_registry = __esm({
  "packages/daemon/src/lib/workspace-registry.ts"() {
    "use strict";
    init_src2();
    REGISTRY_DIR_ENV2 = "TMUX_IDE_REGISTRY_DIR";
    RegistryFileSchemaZ = z15.object({
      version: z15.literal(1),
      workspaces: z15.array(WorkspaceSchemaZ)
    });
    WorkspaceAlreadyExistsError = class extends Error {
      code = "ALREADY_EXISTS";
      constructor(name) {
        super(`Workspace "${name}" already exists`);
        this.name = "WorkspaceAlreadyExistsError";
      }
    };
    WorkspaceNotFoundError = class extends Error {
      code = "NOT_FOUND";
      constructor(name) {
        super(`Workspace "${name}" not found`);
        this.name = "WorkspaceNotFoundError";
      }
    };
    WorkspaceRegistry = class {
      dir;
      listSessions;
      emitter = new EventEmitter3();
      workspaces = [];
      loaded = false;
      constructor(options = {}) {
        this.dir = options.dir ?? process.env[REGISTRY_DIR_ENV2] ?? join7(homedir3(), ".tmux-ide");
        this.listSessions = options.listSessions ?? defaultListSessions;
        this.emitter.setMaxListeners(0);
      }
      /**
       * Load workspaces from disk and reconcile against live tmux sessions.
       * Drops entries whose tmux session is gone (silently — they were
       * persisted by a prior daemon invocation that may have crashed).
       *
       * Safe to call repeatedly; subsequent calls re-reconcile.
       */
      async load() {
        const fromDisk = this.readDisk();
        let live;
        try {
          live = new Set(this.listSessions());
        } catch {
          live = new Set(fromDisk.map((w) => w.sessionName));
        }
        const reconciled = fromDisk.filter((w) => live.has(w.sessionName));
        this.workspaces = reconciled;
        this.loaded = true;
        if (reconciled.length !== fromDisk.length) {
          this.writeDisk();
        }
      }
      list() {
        return [...this.workspaces];
      }
      get(name) {
        return this.workspaces.find((w) => w.name === name) ?? null;
      }
      has(name) {
        return this.workspaces.some((w) => w.name === name);
      }
      add(input) {
        if (this.has(input.name)) {
          throw new WorkspaceAlreadyExistsError(input.name);
        }
        const now = (input.now ?? (() => /* @__PURE__ */ new Date()))();
        const workspace = {
          name: input.name,
          sessionName: input.sessionName ?? input.name,
          projectDir: input.projectDir,
          ideConfigPath: input.ideConfigPath ?? null,
          addedAt: now.toISOString()
        };
        this.workspaces = [...this.workspaces, workspace];
        this.writeDisk();
        this.emitter.emit("workspace.added", workspace);
        return workspace;
      }
      remove(name) {
        if (!this.has(name)) {
          throw new WorkspaceNotFoundError(name);
        }
        this.workspaces = this.workspaces.filter((w) => w.name !== name);
        this.writeDisk();
        this.emitter.emit("workspace.removed", name);
      }
      /** Subscribe to workspace.added | workspace.removed events. */
      on(event, handler) {
        this.emitter.on(event, handler);
        return () => this.emitter.off(event, handler);
      }
      // ----------------- io -----------------
      filePath() {
        return join7(this.dir, "workspaces.json");
      }
      readDisk() {
        const path4 = this.filePath();
        if (!existsSync10(path4)) return [];
        let parsed;
        try {
          parsed = JSON.parse(readFileSync8(path4, "utf-8"));
        } catch {
          return [];
        }
        const result = RegistryFileSchemaZ.safeParse(parsed);
        if (!result.success) return [];
        return result.data.workspaces;
      }
      writeDisk() {
        const path4 = this.filePath();
        mkdirSync6(dirname6(path4), { recursive: true });
        const file = { version: 1, workspaces: this.workspaces };
        const tmp = `${path4}.tmp`;
        writeFileSync7(tmp, JSON.stringify(file, null, 2) + "\n");
        renameSync5(tmp, path4);
      }
      /** @internal Test-only: assert the registry is loaded. */
      _isLoaded() {
        return this.loaded;
      }
    };
    _default = null;
  }
});

// packages/daemon/src/lib/token-tracker.ts
import { join as join8 } from "node:path";
import { readFileSync as readFileSync9, writeFileSync as writeFileSync8, existsSync as existsSync11, mkdirSync as mkdirSync7, renameSync as renameSync6 } from "node:fs";
function accountingPath(dir) {
  return join8(dir, ".tasks", "accounting.json");
}
function loadAccounting(dir) {
  const path4 = accountingPath(dir);
  if (existsSync11(path4)) {
    try {
      return JSON.parse(readFileSync9(path4, "utf-8"));
    } catch {
    }
  }
  return {
    agents: {},
    sessionStart: (/* @__PURE__ */ new Date()).toISOString(),
    updated: (/* @__PURE__ */ new Date()).toISOString()
  };
}
function saveAccounting(dir, data) {
  const tasksDir = join8(dir, ".tasks");
  if (!existsSync11(tasksDir)) mkdirSync7(tasksDir, { recursive: true });
  data.updated = (/* @__PURE__ */ new Date()).toISOString();
  const dest = accountingPath(dir);
  const tmpPath = dest + ".tmp";
  writeFileSync8(tmpPath, JSON.stringify(data, null, 2) + "\n");
  renameSync6(tmpPath, dest);
}
function recordTaskTime(dir, agentName, taskId, elapsedMs) {
  const data = loadAccounting(dir);
  if (!data.agents[agentName]) {
    data.agents[agentName] = { totalTimeMs: 0, taskCount: 0, lastTaskId: null };
  }
  const agent = data.agents[agentName];
  agent.totalTimeMs += elapsedMs;
  agent.taskCount += 1;
  agent.lastTaskId = taskId;
  saveAccounting(dir, data);
}
var init_token_tracker = __esm({
  "packages/daemon/src/lib/token-tracker.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/sqlite-adapter.ts
import { createRequire as createRequire2 } from "node:module";
function loadDatabaseCtor() {
  if (DatabaseCtor) return DatabaseCtor;
  if (isBun) {
    const mod = requireFn("bun:sqlite");
    DatabaseCtor = mod.Database;
  } else {
    const mod = requireFn("better-sqlite3");
    DatabaseCtor = mod.default ?? mod;
  }
  return DatabaseCtor;
}
function openDatabase(path4) {
  const Ctor = loadDatabaseCtor();
  return new Ctor(path4);
}
var requireFn, isBun, DatabaseCtor;
var init_sqlite_adapter = __esm({
  "packages/daemon/src/lib/sqlite-adapter.ts"() {
    "use strict";
    requireFn = createRequire2(import.meta.url);
    isBun = typeof globalThis.Bun !== "undefined";
    DatabaseCtor = null;
  }
});

// packages/daemon/src/lib/event-log-sqlite.ts
var event_log_sqlite_exports = {};
__export(event_log_sqlite_exports, {
  __resetEventLogSqliteForTests: () => __resetEventLogSqliteForTests,
  appendEventSqlite: () => appendEventSqlite,
  pruneEventsSqlite: () => pruneEventsSqlite,
  queryEventsSqlite: () => queryEventsSqlite,
  readEventsSqlite: () => readEventsSqlite
});
import { mkdirSync as mkdirSync8 } from "node:fs";
import { basename as basename6, join as join9 } from "node:path";
function dbPathFor(dir) {
  return join9(dir, ".tasks", "events.sqlite");
}
function ensureDb(dir) {
  const cached = dbCache.get(dir);
  if (cached) return cached;
  mkdirSync8(join9(dir, ".tasks"), { recursive: true });
  const db = openDatabase(dbPathFor(dir));
  runMigrations(db);
  dbCache.set(dir, db);
  return db;
}
function runMigrations(db) {
  db.exec(`CREATE TABLE IF NOT EXISTS _migrations (
    id INTEGER PRIMARY KEY,
    applied_at TEXT NOT NULL
  );`);
  const appliedRows = db.prepare("SELECT id FROM _migrations").all();
  const applied = new Set(appliedRows.map((r) => r.id));
  const insert = db.prepare("INSERT INTO _migrations (id, applied_at) VALUES (?, ?)");
  for (const migration of MIGRATIONS) {
    if (applied.has(migration.id)) continue;
    db.exec(migration.sql);
    insert.run(migration.id, (/* @__PURE__ */ new Date()).toISOString());
  }
}
function appendEventSqlite(dir, event) {
  const db = ensureDb(dir);
  const ts = event.timestamp ?? (/* @__PURE__ */ new Date()).toISOString();
  const kind = event.type ?? "unknown";
  db.prepare("INSERT INTO events (ts, kind, payload, session) VALUES (?, ?, ?, ?)").run(
    ts,
    kind,
    JSON.stringify(event),
    basename6(dir)
  );
}
function rehydrate(payload) {
  try {
    const parsed = JSON.parse(payload);
    if ("message" in parsed && typeof parsed.message === "string") {
      return parsed;
    }
    const result = StructuredEventSchemaZ.safeParse(parsed);
    if (result.success) {
      const structured = result.data;
      return {
        timestamp: structured.timestamp,
        type: structured.type,
        taskId: "taskId" in structured ? structured.taskId : void 0,
        agent: "agent" in structured ? structured.agent : void 0,
        message: formatEventMessage(structured)
      };
    }
    if (parsed.type && parsed.timestamp) {
      return { ...parsed, message: parsed.message ?? "" };
    }
    return null;
  } catch {
    return null;
  }
}
function readEventsSqlite(dir) {
  const db = ensureDb(dir);
  const rows = db.prepare("SELECT payload FROM events ORDER BY ts ASC, id ASC").all();
  return rows.map((row) => rehydrate(row.payload)).filter((event) => event !== null);
}
function queryEventsSqlite(dir, query = {}) {
  const db = ensureDb(dir);
  const clauses = [];
  const params = [];
  if (query.session !== void 0) {
    clauses.push("session = ?");
    params.push(query.session);
  }
  if (query.kind !== void 0) {
    clauses.push("kind = ?");
    params.push(query.kind);
  }
  if (query.fromTs !== void 0) {
    clauses.push("ts >= ?");
    params.push(query.fromTs);
  }
  if (query.toTs !== void 0) {
    clauses.push("ts <= ?");
    params.push(query.toTs);
  }
  let sql = "SELECT payload FROM events";
  if (clauses.length > 0) sql += " WHERE " + clauses.join(" AND ");
  sql += " ORDER BY ts ASC, id ASC";
  if (query.limit !== void 0) {
    sql += " LIMIT ?";
    params.push(query.limit);
  }
  const rows = db.prepare(sql).all(...params);
  return rows.map((row) => rehydrate(row.payload)).filter((event) => event !== null);
}
function pruneEventsSqlite(dir, now = Date.now(), retentionDays = RETENTION_DAYS_DEFAULT) {
  const cutoff = new Date(now - retentionDays * 24 * 60 * 60 * 1e3).toISOString();
  ensureDb(dir).prepare("DELETE FROM events WHERE ts < ?").run(cutoff);
}
function __resetEventLogSqliteForTests() {
  for (const db of dbCache.values()) {
    try {
      db.close();
    } catch {
    }
  }
  dbCache.clear();
}
var MIGRATIONS, dbCache, RETENTION_DAYS_DEFAULT;
var init_event_log_sqlite = __esm({
  "packages/daemon/src/lib/event-log-sqlite.ts"() {
    "use strict";
    init_domain2();
    init_sqlite_adapter();
    init_event_log();
    MIGRATIONS = [
      {
        id: 1,
        sql: `
      CREATE TABLE IF NOT EXISTS events (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        ts TEXT NOT NULL,
        kind TEXT NOT NULL,
        payload TEXT NOT NULL,
        session TEXT
      );
      CREATE INDEX IF NOT EXISTS idx_events_session_ts ON events(session, ts);
      CREATE INDEX IF NOT EXISTS idx_events_kind_ts ON events(kind, ts);
    `
      }
    ];
    dbCache = /* @__PURE__ */ new Map();
    RETENTION_DAYS_DEFAULT = 30;
  }
});

// packages/daemon/src/lib/webhook.ts
import { createHmac } from "node:crypto";
function webhookIdForIndex(index) {
  return `webhook-${index}`;
}
function parseWebhookId(webhookId) {
  const match = /^webhook-(\d+)$/.exec(webhookId);
  if (!match) return null;
  return Number.parseInt(match[1], 10);
}
function addWebhookConfig(dir, webhook) {
  const { config: config2 } = readConfig(dir);
  config2.orchestrator = config2.orchestrator ?? {};
  config2.orchestrator.webhooks = config2.orchestrator.webhooks ?? [];
  config2.orchestrator.webhooks.push(webhook);
  writeConfig(dir, config2);
  return {
    webhookId: webhookIdForIndex(config2.orchestrator.webhooks.length - 1),
    ...webhook
  };
}
function removeWebhookConfig(dir, webhookId) {
  const index = parseWebhookId(webhookId);
  const { config: config2 } = readConfig(dir);
  const webhooks = config2.orchestrator?.webhooks ?? [];
  if (index == null || !webhooks[index]) return false;
  webhooks.splice(index, 1);
  writeConfig(dir, config2);
  return true;
}
function getWebhookConfig(dir, webhookId) {
  const index = parseWebhookId(webhookId);
  const { config: config2 } = readConfig(dir);
  const webhook = index == null ? null : (config2.orchestrator?.webhooks ?? [])[index];
  return webhook ? { webhookId, ...webhook } : null;
}
async function testWebhookConfig(webhook, fetchImpl = fetch) {
  const event = {
    type: "webhook.test",
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    message: "Test webhook from tmux-ide"
  };
  const body = JSON.stringify(event);
  const headers = {
    "Content-Type": "application/json",
    "User-Agent": "tmux-ide-webhook/1.0"
  };
  if (webhook.secret) {
    const signature = createHmac("sha256", webhook.secret).update(body).digest("hex");
    headers["X-Signature-256"] = `sha256=${signature}`;
  }
  const res = await fetchImpl(webhook.url, {
    method: "POST",
    headers,
    body,
    signal: AbortSignal.timeout(5e3)
  });
  return { status: res.status, ok: res.ok };
}
function fireWebhooks(webhooks, event) {
  const eventType = event.type;
  for (const hook of webhooks) {
    if (hook.events && hook.events.length > 0 && !hook.events.includes(eventType)) {
      continue;
    }
    const body = JSON.stringify(event);
    const headers = {
      "Content-Type": "application/json",
      "User-Agent": "tmux-ide-webhook/1.0"
    };
    if (hook.secret) {
      const signature = createHmac("sha256", hook.secret).update(body).digest("hex");
      headers["X-Signature-256"] = `sha256=${signature}`;
    }
    fetch(hook.url, {
      method: "POST",
      headers,
      body,
      signal: AbortSignal.timeout(5e3)
    }).catch(() => {
      if (process.env.TMUX_IDE_DEBUG === "1") {
        process.stderr.write(`Webhook failed: ${hook.url}
`);
      }
    });
  }
}
var init_webhook = __esm({
  "packages/daemon/src/lib/webhook.ts"() {
    "use strict";
    init_yaml_io();
  }
});

// packages/daemon/src/lib/event-log.ts
var event_log_exports = {};
__export(event_log_exports, {
  appendEvent: () => appendEvent,
  eventLogEmitter: () => eventLogEmitter,
  formatEventMessage: () => formatEventMessage,
  pruneEventLogs: () => pruneEventLogs,
  queryEvents: () => queryEvents,
  readEvents: () => readEvents,
  setWebhookConfig: () => setWebhookConfig,
  useSqliteEventLog: () => useSqliteEventLog
});
import {
  appendFileSync as appendFileSync2,
  existsSync as existsSync12,
  mkdirSync as mkdirSync9,
  readFileSync as readFileSync10,
  readdirSync as readdirSync4,
  rmSync as rmSync3,
  statSync as statSync4,
  unlinkSync as unlinkSync2,
  writeFileSync as writeFileSync9
} from "node:fs";
import { EventEmitter as EventEmitter4 } from "node:events";
import { gzipSync, gunzipSync } from "node:zlib";
import { join as join10 } from "node:path";
function setWebhookConfig(webhooks) {
  _webhooks = webhooks;
}
function formatEventMessage(event) {
  switch (event.type) {
    case "dispatch":
      return `Dispatched task ${event.taskId} to ${event.agent}`;
    case "completion": {
      const duration = event.durationMs != null ? ` in ${Math.round(event.durationMs / 1e3)}s` : "";
      return `Task ${event.taskId} completed by ${event.agent}${duration}`;
    }
    case "stall":
      return `Stall detected: task ${event.taskId} by ${event.agent} (${Math.floor(event.elapsedMs / 6e4)}m)`;
    case "retry":
      return `Retry ${event.attempt}/${event.maxRetries} for task ${event.taskId}: ${event.reason}`;
    case "reconcile":
      return `Reconcile task ${event.taskId}: agent "${event.previousAgent}" ${event.action}`;
    case "error": {
      const ctx = [event.taskId && `task ${event.taskId}`, event.agent && `agent ${event.agent}`].filter(Boolean).join(", ");
      const code = event.code ? ` [${event.code}]` : "";
      return ctx ? `Error (${ctx}): ${event.message}${code}` : `Error: ${event.message}${code}`;
    }
    case "task_created":
      return `Task ${event.taskId} created: "${event.title}"`;
    case "status_change":
      return `Task ${event.taskId} status: ${event.from} \u2192 ${event.to}`;
    case "send": {
      const preview = event.message.length > 50 ? event.message.slice(0, 50) + "..." : event.message;
      return `Sent to ${event.target}: "${preview}"`;
    }
    case "notify": {
      const preview = event.message.length > 50 ? event.message.slice(0, 50) + "..." : event.message;
      return `Notified ${event.target}: "${preview}"`;
    }
    case "milestone_validating":
      return event.title && event.milestoneId ? `Milestone "${event.title}" (${event.milestoneId}) entered validation` : "Milestone entered validation";
    case "milestone_complete":
      return event.title && event.milestoneId ? `Milestone "${event.title}" (${event.milestoneId}) completed` : "Milestone completed";
    case "validation_dispatch":
      return event.title && event.target ? `Dispatched validation for "${event.title}" to ${event.target}` : "Dispatched milestone validation";
    case "remediation":
      return event.assertionId ? `Created remediation task ${event.taskId} for assertion ${event.assertionId}` : `Created remediation task ${event.taskId}`;
    case "validation_failed":
      return event.title && event.failedCount != null ? `Milestone "${event.title}" validation failed (${event.failedCount} assertion(s))` : "Milestone validation failed";
    case "planning":
      return event.target ? `Dispatched mission planning to ${event.target}` : "Dispatched mission planning";
    case "mission_complete":
      return event.title ? `Mission "${event.title}" completed` : "Mission completed";
    case "discovered_issue":
      return event.issue ? `Created follow-up task ${event.taskId}: ${event.issue}` : `Created follow-up task ${event.taskId} for discovered issue`;
    case "research_dispatch":
      return event.target && event.researchType ? `Dispatched ${event.researchType} research to ${event.target}` : `Dispatched research task ${event.taskId}`;
    case "research_finding":
      return event.summary && event.researchType ? `Research finding (${event.researchType}): ${event.summary}` : `Recorded research finding from task ${event.taskId}`;
    default:
      return event.message ?? `Event: ${event.type}`;
  }
}
function eventLogMaxBytes() {
  const parsed = Number.parseInt(process.env.TMUX_IDE_EVENT_LOG_MAX_BYTES ?? "", 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : MAX_LOG_SIZE;
}
function dateKey(ms = Date.now()) {
  return new Date(ms).toISOString().slice(0, 10);
}
function currentLogPath(dir) {
  return join10(dir, ".tasks", EVENT_LOG_FILE);
}
function rotatedLogPath(tasksDir, date) {
  return join10(tasksDir, `_events-${date}.jsonl.gz`);
}
function rotateEventLogIfNeeded(dir) {
  if (_rotating) return;
  const tasksDir = join10(dir, ".tasks");
  const logPath = currentLogPath(dir);
  if (!existsSync12(logPath)) return;
  try {
    const stat = statSync4(logPath);
    const tooLarge = stat.size >= eventLogMaxBytes();
    const staleDay = dateKey(stat.mtimeMs) !== dateKey();
    if (!tooLarge && !staleDay) return;
    _rotating = true;
    const rotateDate = dateKey(stat.mtimeMs);
    const rotatedPath = rotatedLogPath(tasksDir, rotateDate);
    const content = readFileSync10(logPath);
    const existing = existsSync12(rotatedPath) ? gunzipSync(readFileSync10(rotatedPath)) : Buffer.alloc(0);
    const separator = existing.byteLength > 0 && !existing.toString("utf-8").endsWith("\n") ? "\n" : "";
    writeFileSync9(
      rotatedPath,
      gzipSync(Buffer.concat([existing, Buffer.from(separator), content]))
    );
    unlinkSync2(logPath);
  } catch {
  } finally {
    _rotating = false;
  }
}
function pruneEventLogs(dir, now = Date.now()) {
  if (useSqliteEventLog()) {
    sqliteBackend().pruneEventsSqlite(dir, now, RETENTION_DAYS);
    return;
  }
  const tasksDir = join10(dir, ".tasks");
  if (!existsSync12(tasksDir)) return;
  const cutoff = now - RETENTION_DAYS * 24 * 60 * 60 * 1e3;
  for (const file of readdirSync4(tasksDir)) {
    const match = file.match(/^_events-(\d{4}-\d{2}-\d{2})\.jsonl\.gz$/);
    if (!match) continue;
    const timestamp = Date.parse(`${match[1]}T00:00:00.000Z`);
    if (Number.isFinite(timestamp) && timestamp < cutoff) {
      rmSync3(join10(tasksDir, file), { force: true });
    }
  }
}
function useSqliteEventLog() {
  return process.env.TMUX_IDE_EVENT_LOG === "sqlite";
}
function sqliteBackend() {
  return event_log_sqlite_exports;
}
function appendEvent(dir, event) {
  if (useSqliteEventLog()) {
    const { appendEventSqlite: appendEventSqlite2 } = sqliteBackend();
    appendEventSqlite2(dir, event);
    eventLogEmitter.emit("event", { dir, event });
    if (_webhooks.length > 0) fireWebhooks(_webhooks, event);
    return;
  }
  const tasksDir = join10(dir, ".tasks");
  if (!existsSync12(tasksDir)) {
    mkdirSync9(tasksDir, { recursive: true });
  }
  rotateEventLogIfNeeded(dir);
  pruneEventLogs(dir);
  const logPath = currentLogPath(dir);
  const line = JSON.stringify(event) + "\n";
  const writeEvent = () => {
    appendFileSync2(logPath, line);
    eventLogEmitter.emit("event", { dir, event });
    if (_webhooks.length > 0) fireWebhooks(_webhooks, event);
  };
  if (!("message" in event) || StructuredEventSchemaZ.safeParse(event).success) {
    writeEvent();
    return;
  }
  writeEvent();
}
function queryEvents(dir, query = {}) {
  if (useSqliteEventLog()) {
    return sqliteBackend().queryEventsSqlite(dir, query);
  }
  const session = dir.split("/").pop() ?? "";
  if (query.session !== void 0 && query.session !== session) return [];
  let events = readEvents(dir);
  if (query.kind !== void 0) events = events.filter((e) => e.type === query.kind);
  if (query.fromTs !== void 0) events = events.filter((e) => e.timestamp >= query.fromTs);
  if (query.toTs !== void 0) events = events.filter((e) => e.timestamp <= query.toTs);
  if (query.limit !== void 0) events = events.slice(0, query.limit);
  return events;
}
function readEvents(dir) {
  if (useSqliteEventLog()) {
    return sqliteBackend().readEventsSqlite(dir);
  }
  const tasksDir = join10(dir, ".tasks");
  const logPath = currentLogPath(dir);
  const legacyPath = join10(tasksDir, LEGACY_EVENT_LOG_FILE);
  const legacyRotatedPath = legacyPath + ".1";
  let lines = [];
  if (existsSync12(tasksDir)) {
    const rotations = readdirSync4(tasksDir).filter((file) => /^_events-\d{4}-\d{2}-\d{2}\.jsonl\.gz$/.test(file)).sort().slice(-1);
    for (const file of rotations) {
      const raw = gunzipSync(readFileSync10(join10(tasksDir, file))).toString("utf-8").trim();
      if (raw) lines = lines.concat(raw.split("\n"));
    }
  }
  if (existsSync12(logPath)) {
    const raw = readFileSync10(logPath, "utf-8").trim();
    if (raw) lines = lines.concat(raw.split("\n"));
  }
  if (existsSync12(legacyRotatedPath)) {
    const raw = readFileSync10(legacyRotatedPath, "utf-8").trim();
    if (raw) lines = lines.concat(raw.split("\n"));
  }
  if (existsSync12(legacyPath)) {
    const raw = readFileSync10(legacyPath, "utf-8").trim();
    if (raw) lines = lines.concat(raw.split("\n"));
  }
  if (lines.length === 0) return [];
  return lines.map((line) => {
    try {
      const parsed = JSON.parse(line);
      if ("message" in parsed && typeof parsed.message === "string") {
        return parsed;
      }
      const result = StructuredEventSchemaZ.safeParse(parsed);
      if (result.success) {
        const structured = result.data;
        return {
          timestamp: structured.timestamp,
          type: structured.type,
          taskId: "taskId" in structured ? structured.taskId : void 0,
          agent: "agent" in structured ? structured.agent : void 0,
          message: formatEventMessage(structured)
        };
      }
      if (parsed.type && parsed.timestamp) {
        return { ...parsed, message: parsed.message ?? "" };
      }
      return null;
    } catch {
      return null;
    }
  }).filter((e) => e !== null);
}
var _webhooks, eventLogEmitter, EVENT_LOG_FILE, LEGACY_EVENT_LOG_FILE, MAX_LOG_SIZE, RETENTION_DAYS, _rotating;
var init_event_log = __esm({
  "packages/daemon/src/lib/event-log.ts"() {
    "use strict";
    init_event_log_sqlite();
    init_domain2();
    init_webhook();
    _webhooks = [];
    eventLogEmitter = new EventEmitter4();
    eventLogEmitter.setMaxListeners(0);
    EVENT_LOG_FILE = "_events.jsonl";
    LEGACY_EVENT_LOG_FILE = "events.log";
    MAX_LOG_SIZE = 10 * 1024 * 1024;
    RETENTION_DAYS = 30;
    _rotating = false;
  }
});

// packages/daemon/src/lib/github-pr.ts
import { execFileSync as execFileSync4 } from "node:child_process";
function isGhAvailable() {
  try {
    execFileSync4("gh", ["--version"], {
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "ignore"]
    });
    return true;
  } catch {
    return false;
  }
}
function createTaskPr(task, cwd, baseBranch) {
  let headBranch;
  try {
    headBranch = execFileSync4("git", ["rev-parse", "--abbrev-ref", "HEAD"], {
      cwd,
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "ignore"]
    }).trim();
  } catch {
    return null;
  }
  const bodyParts = [];
  if (task.description) bodyParts.push(task.description);
  if (task.proof) {
    bodyParts.push("");
    bodyParts.push("## Proof");
    const proof = task.proof;
    if (proof.notes) bodyParts.push(proof.notes);
    if (proof.tests) bodyParts.push(`Tests: ${proof.tests.passed}/${proof.tests.total} passed`);
    if (proof.ci)
      bodyParts.push(`CI: ${proof.ci.status}${proof.ci.url ? ` (${proof.ci.url})` : ""}`);
  }
  if (task.tags.length > 0) {
    bodyParts.push("");
    bodyParts.push(`Tags: ${task.tags.join(", ")}`);
  }
  const title = `Task ${task.id}: ${task.title}`;
  const body = bodyParts.join("\n") || task.title;
  try {
    try {
      execFileSync4("git", ["push", "-u", "origin", headBranch], {
        cwd,
        encoding: "utf-8",
        stdio: ["ignore", "pipe", "pipe"]
      });
    } catch {
    }
    const output = execFileSync4(
      "gh",
      [
        "pr",
        "create",
        "--title",
        title,
        "--body",
        body,
        "--head",
        headBranch,
        ...baseBranch ? ["--base", baseBranch] : []
      ],
      {
        cwd,
        encoding: "utf-8",
        stdio: ["ignore", "pipe", "pipe"]
      }
    ).trim();
    const url = output;
    const match = url.match(/\/pull\/(\d+)/);
    const number = match ? parseInt(match[1], 10) : 0;
    return { url, number };
  } catch {
    return null;
  }
}
function createMissionPr(mission, cwd, baseBranch) {
  let headBranch;
  if (mission.branch) {
    headBranch = mission.branch;
  } else {
    try {
      headBranch = execFileSync4("git", ["rev-parse", "--abbrev-ref", "HEAD"], {
        cwd,
        encoding: "utf-8",
        stdio: ["ignore", "pipe", "ignore"]
      }).trim();
    } catch {
      return null;
    }
  }
  const milestonesSummary = mission.milestones.sort((a, b) => a.order - b.order).map((m) => `- ${m.id}: ${m.title} [${m.status}]`).join("\n");
  const title = `Mission: ${mission.title}`;
  const bodyParts = [];
  if (mission.description) bodyParts.push(mission.description);
  bodyParts.push("");
  bodyParts.push("## Milestones");
  bodyParts.push(milestonesSummary || "No milestones");
  const body = bodyParts.join("\n");
  try {
    try {
      execFileSync4("git", ["push", "-u", "origin", headBranch], {
        cwd,
        encoding: "utf-8",
        stdio: ["ignore", "pipe", "pipe"]
      });
    } catch {
    }
    const output = execFileSync4(
      "gh",
      [
        "pr",
        "create",
        "--title",
        title,
        "--body",
        body,
        "--head",
        headBranch,
        ...baseBranch ? ["--base", baseBranch] : []
      ],
      {
        cwd,
        encoding: "utf-8",
        stdio: ["ignore", "pipe", "pipe"]
      }
    ).trim();
    const url = output;
    const match = url.match(/\/pull\/(\d+)/);
    const number = match ? parseInt(match[1], 10) : 0;
    return { url, number };
  } catch {
    return null;
  }
}
var init_github_pr = __esm({
  "packages/daemon/src/lib/github-pr.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/metrics.ts
import { join as join11 } from "node:path";
import {
  existsSync as existsSync13,
  readFileSync as readFileSync11,
  writeFileSync as writeFileSync10,
  appendFileSync as appendFileSync3,
  renameSync as renameSync7,
  mkdirSync as mkdirSync10
} from "node:fs";
function percentile(sorted, p) {
  if (sorted.length === 0) return 0;
  const idx = Math.ceil(p / 100 * sorted.length) - 1;
  return sorted[Math.max(0, idx)];
}
function median(sorted) {
  return percentile(sorted, 50);
}
function computeMetrics(dir) {
  const events = readEvents(dir);
  const tasks = loadTasks(dir);
  const mission = loadMission(dir);
  const sessionStart = events.find((e) => e.type === "session_start");
  const startedAt = sessionStart?.timestamp ?? events[0]?.timestamp ?? null;
  const now = Date.now();
  const durationMs = startedAt ? now - new Date(startedAt).getTime() : 0;
  const completed = tasks.filter((t) => t.status === "done").length;
  const failed = tasks.filter((t) => t.status === "review" && t.lastError).length;
  const retried = tasks.filter((t) => t.retryCount > 0).length;
  const completionRate = tasks.length > 0 ? completed / tasks.length : 0;
  const retryRate = tasks.length > 0 ? retried / tasks.length : 0;
  const durations = [];
  for (const evt of events) {
    const durationMs2 = evt.durationMs;
    if (evt.type === "completion" && typeof durationMs2 === "number") {
      durations.push(durationMs2);
    }
  }
  durations.sort((a, b) => a - b);
  const byMilestone = (mission?.milestones ?? []).map((m) => {
    const mTasks = tasks.filter((t) => t.milestone === m.id);
    const mCompleted = mTasks.filter((t) => t.status === "done").length;
    const timestamps = mTasks.filter((t) => t.status === "done").map((t) => new Date(t.updated).getTime());
    const mDuration = timestamps.length > 0 ? Math.max(...timestamps) - Math.min(...timestamps) : 0;
    return {
      id: m.id,
      title: m.title,
      status: m.status,
      taskCount: mTasks.length,
      completedCount: mCompleted,
      durationMs: mDuration
    };
  });
  const agentBusySamples = /* @__PURE__ */ new Map();
  const agentTaskCounts = /* @__PURE__ */ new Map();
  const agentRetryCounts = /* @__PURE__ */ new Map();
  const agentSpecialties = /* @__PURE__ */ new Map();
  for (const evt of events) {
    if (evt.type === "agent_heartbeat" && evt.message) {
      const parts = evt.message.replace("agents: ", "").split(", ");
      for (const part of parts) {
        const [name, status4] = part.split("=");
        if (!name || !status4) continue;
        const entry = agentBusySamples.get(name) ?? { busy: 0, idle: 0, total: 0 };
        entry.total++;
        if (status4 === "busy") entry.busy++;
        else entry.idle++;
        agentBusySamples.set(name, entry);
      }
    }
  }
  for (const task of tasks) {
    if (task.assignee) {
      agentTaskCounts.set(task.assignee, (agentTaskCounts.get(task.assignee) ?? 0) + 1);
      if (task.retryCount > 0) {
        agentRetryCounts.set(
          task.assignee,
          (agentRetryCounts.get(task.assignee) ?? 0) + task.retryCount
        );
      }
      if (task.specialty) {
        const specs = agentSpecialties.get(task.assignee) ?? /* @__PURE__ */ new Set();
        specs.add(task.specialty);
        agentSpecialties.set(task.assignee, specs);
      }
    }
  }
  const agentNames = /* @__PURE__ */ new Set([...agentBusySamples.keys(), ...agentTaskCounts.keys()]);
  const agents = [...agentNames].map((name) => {
    const samples = agentBusySamples.get(name) ?? { busy: 0, idle: 0, total: 0 };
    const utilization = samples.total > 0 ? samples.busy / samples.total : 0;
    const estimatedTotalMs = durationMs;
    return {
      name,
      totalTimeMs: estimatedTotalMs,
      activeTimeMs: Math.round(estimatedTotalMs * utilization),
      idleTimeMs: Math.round(estimatedTotalMs * (1 - utilization)),
      taskCount: agentTaskCounts.get(name) ?? 0,
      retryCount: agentRetryCounts.get(name) ?? 0,
      utilization: Math.round(utilization * 100) / 100,
      specialties: [...agentSpecialties.get(name) ?? []]
    };
  });
  const milestonesCompleted = (mission?.milestones ?? []).filter((m) => m.status === "done").length;
  const missionWallClock = mission?.created ? now - new Date(mission.created).getTime() : 0;
  const validationEvents = events.filter(
    (e) => e.type === "milestone_complete" || e.type === "validation_failed"
  );
  const passEvents = validationEvents.filter((e) => e.type === "milestone_complete").length;
  const validationPassRate = validationEvents.length > 0 ? passEvents / validationEvents.length : 1;
  const timeline = [];
  if (events.length > 0) {
    const firstTs = new Date(events[0].timestamp).getTime();
    const interval = 5 * 60 * 1e3;
    for (let t = firstTs; t <= now; t += interval) {
      const cutoff = new Date(t).toISOString();
      const completedByNow = events.filter(
        (e) => e.type === "completion" && e.timestamp <= cutoff
      ).length;
      const activeByNow = events.filter((e) => e.type === "dispatch" && e.timestamp <= cutoff).length - completedByNow;
      const heartbeat = events.filter((e) => e.type === "agent_heartbeat" && e.timestamp <= cutoff).pop();
      let busyAgents = 0;
      let idleAgents = 0;
      if (heartbeat?.message) {
        const parts = heartbeat.message.replace("agents: ", "").split(", ");
        for (const p of parts) {
          if (p.includes("=busy")) busyAgents++;
          else if (p.includes("=idle")) idleAgents++;
        }
      }
      timeline.push({
        timestamp: cutoff,
        completedTasks: completedByNow,
        activeTasks: Math.max(0, activeByNow),
        busyAgents,
        idleAgents
      });
      if (timeline.length >= 200) break;
    }
  }
  return {
    session: {
      startedAt,
      durationMs,
      status: mission?.status ?? "unknown",
      agentCount: agents.length
    },
    tasks: {
      total: tasks.length,
      completed,
      failed,
      retried,
      completionRate: Math.round(completionRate * 100) / 100,
      retryRate: Math.round(retryRate * 100) / 100,
      avgDurationMs: durations.length > 0 ? Math.round(durations.reduce((a, b) => a + b, 0) / durations.length) : 0,
      medianDurationMs: median(durations),
      p90DurationMs: percentile(durations, 90),
      byMilestone
    },
    agents,
    mission: {
      title: mission?.title ?? null,
      status: mission?.status ?? null,
      milestonesCompleted,
      validationPassRate: Math.round(validationPassRate * 100) / 100,
      wallClockMs: missionWallClock
    },
    timeline
  };
}
function saveMetrics(dir, snapshot) {
  const tasksDir = join11(dir, ".tasks");
  if (!existsSync13(tasksDir)) mkdirSync10(tasksDir, { recursive: true });
  const path4 = join11(tasksDir, "metrics.json");
  const tmpPath = path4 + ".tmp";
  writeFileSync10(tmpPath, JSON.stringify(snapshot, null, 2) + "\n");
  renameSync7(tmpPath, path4);
}
function computeAndSaveMetrics(dir) {
  const snapshot = computeMetrics(dir);
  saveMetrics(dir, snapshot);
  return snapshot;
}
function appendMissionHistory(dir, mission, tasks) {
  const tasksDir = join11(dir, ".tasks");
  if (!existsSync13(tasksDir)) mkdirSync10(tasksDir, { recursive: true });
  const path4 = join11(tasksDir, "metrics-history.jsonl");
  const completed = tasks.filter((t) => t.status === "done").length;
  const agentNames = new Set(tasks.filter((t) => t.assignee).map((t) => t.assignee));
  const summary = {
    title: mission.title,
    status: mission.status,
    completedAt: (/* @__PURE__ */ new Date()).toISOString(),
    wallClockMs: mission.created ? Date.now() - new Date(mission.created).getTime() : 0,
    taskCount: tasks.length,
    completionRate: tasks.length > 0 ? Math.round(completed / tasks.length * 100) / 100 : 0,
    agentCount: agentNames.size,
    milestonesCompleted: mission.milestones.filter((m) => m.status === "done").length
  };
  appendFileSync3(path4, JSON.stringify(summary) + "\n");
}
function loadMissionHistory(dir) {
  const path4 = join11(dir, ".tasks", "metrics-history.jsonl");
  if (!existsSync13(path4)) return [];
  try {
    return readFileSync11(path4, "utf-8").trim().split("\n").filter(Boolean).map((line) => JSON.parse(line));
  } catch {
    return [];
  }
}
var init_metrics = __esm({
  "packages/daemon/src/lib/metrics.ts"() {
    "use strict";
    init_task_store();
    init_event_log();
  }
});

// packages/daemon/src/lib/validation.ts
import { join as join12 } from "node:path";
import { existsSync as existsSync14, mkdirSync as mkdirSync11, readFileSync as readFileSync12, writeFileSync as writeFileSync11, renameSync as renameSync8 } from "node:fs";
function loadValidationContract(dir) {
  const path4 = join12(dir, TASKS_DIR2, "validation-contract.md");
  if (!existsSync14(path4)) return null;
  return readFileSync12(path4, "utf-8");
}
function loadValidationState(dir) {
  const path4 = join12(dir, TASKS_DIR2, "validation-state.json");
  if (!existsSync14(path4)) return null;
  try {
    return JSON.parse(readFileSync12(path4, "utf-8"));
  } catch {
    return null;
  }
}
function saveValidationState(dir, state) {
  mkdirSync11(join12(dir, TASKS_DIR2), { recursive: true });
  const path4 = join12(dir, TASKS_DIR2, "validation-state.json");
  const tmpPath = path4 + ".tmp";
  writeFileSync11(tmpPath, JSON.stringify(state, null, 2) + "\n");
  renameSync8(tmpPath, path4);
}
function assertValidationStatus(dir, assertId, status4, evidence) {
  const contract = loadValidationContract(dir);
  const assertionIds = contract ? parseAssertionIds2(contract) : [];
  if (!assertionIds.includes(assertId)) {
    throw new Error(`Validation assertion "${assertId}" was not found in the contract`);
  }
  const verifiedAt = (/* @__PURE__ */ new Date()).toISOString();
  const state = loadValidationState(dir) ?? { assertions: {}, lastVerified: null };
  state.assertions[assertId] = {
    status: status4,
    verifiedBy: null,
    verifiedAt,
    evidence: evidence ?? null,
    blockedBy: status4 === "blocked" ? evidence ?? null : null
  };
  state.lastVerified = verifiedAt;
  saveValidationState(dir, state);
  return { id: assertId, ...state.assertions[assertId] };
}
function validationReport(dir) {
  const state = loadValidationState(dir);
  const entries = state ? Object.values(state.assertions) : [];
  return {
    total: entries.length,
    passing: entries.filter((e) => e.status === "passing").length,
    failing: entries.filter((e) => e.status === "failing").length,
    pending: entries.filter((e) => e.status === "pending").length,
    blocked: entries.filter((e) => e.status === "blocked").length
  };
}
function parseAssertionIds2(contract) {
  const ids = /* @__PURE__ */ new Set();
  let match;
  while ((match = ASSERTION_ID_PATTERN2.exec(contract)) !== null) {
    ids.add(match[1]);
  }
  return [...ids];
}
function checkCoverage(dir) {
  const contract = loadValidationContract(dir);
  if (!contract) return { unclaimed: [], duplicates: {} };
  const assertionIds = parseAssertionIds2(contract);
  if (assertionIds.length === 0) return { unclaimed: [], duplicates: {} };
  const tasks = loadTasks(dir);
  const claimMap = /* @__PURE__ */ new Map();
  for (const task of tasks) {
    for (const id of task.fulfills) {
      const existing = claimMap.get(id) ?? [];
      existing.push(task.id);
      claimMap.set(id, existing);
    }
  }
  const unclaimed = assertionIds.filter((id) => !claimMap.has(id));
  const duplicates = {};
  for (const [id, taskIds] of claimMap) {
    if (taskIds.length > 1 && assertionIds.includes(id)) {
      duplicates[id] = taskIds;
    }
  }
  return { unclaimed, duplicates };
}
var TASKS_DIR2, ASSERTION_ID_PATTERN2;
var init_validation = __esm({
  "packages/daemon/src/lib/validation.ts"() {
    "use strict";
    init_task_store();
    TASKS_DIR2 = ".tasks";
    ASSERTION_ID_PATTERN2 = /\*\*((?:VAL|ASSERT)[A-Z0-9_-]+)\*\*/g;
  }
});

// packages/daemon/src/lib/research.ts
import {
  appendFileSync as appendFileSync4,
  existsSync as existsSync15,
  mkdirSync as mkdirSync12,
  readFileSync as readFileSync13,
  renameSync as renameSync9,
  writeFileSync as writeFileSync12
} from "node:fs";
import { join as join13 } from "node:path";
function getResearchStatePath(dir) {
  return join13(dir, RESEARCH_STATE_FILE);
}
function defaultResearchState() {
  return {
    lastResearchAt: {},
    missionStartAnalyzed: false,
    milestoneTaskCounts: {},
    activeResearchTaskId: null,
    retryWindow: []
  };
}
function loadResearchState(dir) {
  const path4 = getResearchStatePath(dir);
  if (!existsSync15(path4)) return defaultResearchState();
  try {
    const parsed = JSON.parse(readFileSync13(path4, "utf-8"));
    return {
      lastResearchAt: parsed.lastResearchAt ?? {},
      missionStartAnalyzed: parsed.missionStartAnalyzed ?? false,
      milestoneTaskCounts: parsed.milestoneTaskCounts ?? {},
      activeResearchTaskId: parsed.activeResearchTaskId ?? null,
      retryWindow: Array.isArray(parsed.retryWindow) ? parsed.retryWindow : []
    };
  } catch {
    return defaultResearchState();
  }
}
function saveResearchState(dir, state) {
  const tasksDir = join13(dir, ".tasks");
  if (!existsSync15(tasksDir)) mkdirSync12(tasksDir, { recursive: true });
  const path4 = getResearchStatePath(dir);
  const tmpPath = path4 + ".tmp";
  writeFileSync12(tmpPath, JSON.stringify(state, null, 2) + "\n");
  renameSync9(tmpPath, path4);
}
function getResearchConfig(config2) {
  return config2.research ?? {};
}
function hasCooldown(researchState, type, minutes = DEFAULT_COOLDOWN_MINUTES) {
  if (minutes <= 0) return false;
  const last = researchState.lastResearchAt[type];
  if (!last) return false;
  return Date.now() - Date.parse(last) < minutes * 6e4;
}
function latestEventAfter(events, type, after) {
  const afterTs = after ? Date.parse(after) : 0;
  const filtered = events.filter((event) => event.type === type && Date.parse(event.timestamp) > afterTs).sort((a, b) => Date.parse(b.timestamp) - Date.parse(a.timestamp));
  return filtered[0] ?? null;
}
function evaluateTriggers(config2, _state, researchState, tasks, events) {
  const research = getResearchConfig(config2);
  if (!research.enabled) return [];
  if (researchState.activeResearchTaskId) return [];
  const mission = loadMission(config2.dir);
  const triggers = research.triggers ?? {};
  const results = [];
  if (triggers.mission_start && mission && !researchState.missionStartAnalyzed) {
    if (!hasCooldown(researchState, "mission_start")) {
      researchState.missionStartAnalyzed = true;
      results.push({
        type: "mission_start",
        reason: `Mission "${mission.title}" started and needs an initial audit`
      });
    }
  }
  const milestoneStep = triggers.milestone_progress ?? 0;
  if (mission && milestoneStep > 0) {
    for (const milestone of mission.milestones) {
      const doneCount = tasks.filter(
        (task) => task.milestone === milestone.id && task.status === "done" && !task.tags.includes("research")
      ).length;
      const previousCount = researchState.milestoneTaskCounts[milestone.id] ?? 0;
      const previousBucket = Math.floor(previousCount / milestoneStep);
      const currentBucket = Math.floor(doneCount / milestoneStep);
      if (doneCount > 0 && currentBucket > previousBucket) {
        results.push({
          type: "milestone_progress",
          reason: `Milestone ${milestone.id} reached ${doneCount} completed task(s)`,
          context: {
            milestoneId: milestone.id,
            completedCount: doneCount,
            milestoneTitle: milestone.title
          }
        });
      }
      researchState.milestoneTaskCounts[milestone.id] = doneCount;
    }
  }
  if (triggers.milestone_complete) {
    const event = latestEventAfter(
      events,
      "milestone_complete",
      researchState.lastResearchAt.milestone_complete
    );
    if (event && !hasCooldown(researchState, "milestone_complete")) {
      results.push({
        type: "milestone_complete",
        reason: event.message
      });
    }
  }
  if ((triggers.periodic_minutes ?? 0) > 0 && !hasCooldown(researchState, "periodic", triggers.periodic_minutes)) {
    results.push({
      type: "periodic",
      reason: `Periodic research interval elapsed (${triggers.periodic_minutes} minute window)`
    });
  }
  if (triggers.retry_cluster) {
    const windowStart = Date.now() - 15 * 6e4;
    researchState.retryWindow = events.filter((event) => event.type === "retry" && Date.parse(event.timestamp) >= windowStart).map((event) => ({ timestamp: event.timestamp, taskId: event.taskId ?? "unknown" }));
    if (researchState.retryWindow.length >= 3 && !hasCooldown(researchState, "retry_cluster")) {
      results.push({
        type: "retry_cluster",
        reason: `${researchState.retryWindow.length} retries clustered in the last 15 minutes`
      });
    }
  }
  if (triggers.stall_detected) {
    const event = latestEventAfter(events, "stall", researchState.lastResearchAt.stall_detected);
    if (event && !hasCooldown(researchState, "stall_detected")) {
      results.push({
        type: "stall_detected",
        reason: event.message
      });
    }
  }
  if (triggers.discovered_issue) {
    const event = latestEventAfter(
      events,
      "discovered_issue",
      researchState.lastResearchAt.discovered_issue
    );
    if (event && !hasCooldown(researchState, "discovered_issue")) {
      results.push({
        type: "discovered_issue",
        reason: event.message
      });
    }
  }
  return results;
}
function promptInstructions(type) {
  switch (type) {
    case "mission_start":
      return `Perform an initial codebase-analysis and architecture-audit.
Identify major risks, missing contracts, and likely execution bottlenecks.`;
    case "milestone_progress":
      return `Perform a progress audit.
Check whether recent completed work is coherent, whether validation coverage still matches implementation, and whether follow-up tasks are needed.`;
    case "milestone_complete":
      return `Perform a completion review.
Look for hidden regressions, weak evidence, and any missing remediation before the next milestone advances.`;
    case "retry_cluster":
      return `Perform an incident-analysis.
Find why multiple retries are clustering and propose concrete corrective actions or task reshaping.`;
    case "stall_detected":
      return `Perform issue-triage on the stalled work.
Identify likely blockers, missing context, or decomposition problems.`;
    case "discovered_issue":
      return `Perform issue-triage on the newly discovered problem.
Clarify severity, scope, and the best next task to create.`;
    case "periodic":
    default:
      return `Perform a continuous internal audit.
Review architecture, tests, contracts, and task quality. Surface actionable findings only.`;
  }
}
function buildResearchPrompt(dir, type, context = {}) {
  const mission = loadMission(dir);
  const taskId = typeof context.taskId === "string" ? context.taskId : "<TASK_ID>";
  let prompt = `# Research Task: ${type}

`;
  if (mission) {
    prompt += `## Mission: ${mission.title}
`;
    if (mission.description) prompt += `${mission.description}
`;
    prompt += "\n";
  }
  if (context.reason) {
    prompt += `## Trigger Context
${String(context.reason)}

`;
  }
  if (context.milestoneId || context.milestoneTitle || context.completedCount != null) {
    prompt += `## Milestone Context
`;
    if (context.milestoneId) prompt += `Milestone: ${String(context.milestoneId)}
`;
    if (context.milestoneTitle) prompt += `Title: ${String(context.milestoneTitle)}
`;
    if (context.completedCount != null)
      prompt += `Completed tasks: ${String(context.completedCount)}
`;
    prompt += "\n";
  }
  prompt += `## Instructions
${promptInstructions(type)}

`;
  prompt += `## Reporting Protocol
`;
  prompt += `1. Investigate only what is necessary to produce actionable findings.
`;
  prompt += `2. If you find concrete issues, update or create follow-up tasks as needed.
`;
  prompt += `3. Finish by recording your result with:
`;
  prompt += `   tmux-ide task done ${taskId} --proof "key findings and evidence" --summary "short research takeaway"
`;
  prompt += `4. Include severity, impact, and the recommended next action in your proof.
`;
  return prompt.trim();
}
function isIdlePane(pane) {
  const cmd = pane.currentCommand.toLowerCase();
  if (SHELL_COMMANDS2.has(cmd)) return true;
  if (AGENT_PREFIXES.some((prefix) => cmd.startsWith(prefix))) return !SPINNERS2.test(pane.title);
  if (VERSION_PATTERN.test(cmd)) return !SPINNERS2.test(pane.title);
  return false;
}
function findResearchPane(panes, masterPane) {
  const explicit = panes.find((pane) => pane.role === "researcher" && isIdlePane(pane));
  if (explicit) return explicit;
  const idleFallback = panes.filter((pane) => {
    if (pane.role === "lead") return false;
    if (masterPane && pane.title === masterPane) return false;
    return isIdlePane(pane);
  });
  if (idleFallback.length < 2) return null;
  return idleFallback[0] ?? null;
}
function activeMilestoneId(dir) {
  const mission = loadMission(dir);
  return mission?.milestones.find((milestone) => milestone.status === "active")?.id ?? null;
}
function dispatchResearch(config2, state, researchState, tasks, panes, triggerOverride) {
  if (researchState.activeResearchTaskId) return null;
  const inProgressCount = tasks.filter((task2) => task2.status === "in-progress").length;
  if (inProgressCount >= config2.maxConcurrentAgents - 1) return null;
  const triggers = triggerOverride ? [triggerOverride] : evaluateTriggers(config2, state, researchState, tasks, readEvents(config2.dir));
  const trigger = triggers[0];
  if (!trigger) return null;
  const pane = findResearchPane(panes, config2.masterPane);
  if (!pane) return null;
  const id = nextTaskId(config2.dir);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const task = {
    id,
    title: `Research: ${trigger.type.replace(/_/g, " ")}`,
    description: trigger.reason,
    goal: null,
    status: "in-progress",
    assignee: pane.name ?? pane.title,
    priority: 3,
    created: now,
    updated: now,
    tags: ["research", trigger.type],
    proof: null,
    retryCount: 0,
    maxRetries: 5,
    lastError: null,
    nextRetryAt: null,
    depends_on: [],
    milestone: typeof trigger.context?.milestoneId === "string" ? trigger.context.milestoneId : activeMilestoneId(config2.dir),
    specialty: "researcher",
    fulfills: [],
    discoveredIssues: [],
    salientSummary: null
  };
  saveTask(config2.dir, task);
  const dispatchDir = join13(config2.dir, ".tasks", "dispatch");
  if (!existsSync15(dispatchDir)) mkdirSync12(dispatchDir, { recursive: true });
  const prompt = buildResearchPrompt(config2.dir, trigger.type, {
    ...trigger.context,
    reason: trigger.reason,
    taskId: id
  });
  const filename = `research-${id}.md`;
  writeFileSync12(join13(dispatchDir, filename), prompt);
  const sent = sendCommand(
    config2.session,
    pane.id,
    `Read and execute: .tasks/dispatch/${filename}`
  );
  if (!sent) {
    task.status = "todo";
    task.assignee = null;
    task.updated = (/* @__PURE__ */ new Date()).toISOString();
    saveTask(config2.dir, task);
    return null;
  }
  researchState.activeResearchTaskId = id;
  researchState.lastResearchAt[trigger.type] = now;
  saveResearchState(config2.dir, researchState);
  state.lastActivity.set(pane.id, Date.now());
  appendEvent(config2.dir, {
    timestamp: now,
    type: "research_dispatch",
    taskId: id,
    target: pane.title,
    researchType: trigger.type,
    message: `Dispatched research task ${id} (${trigger.type}) to ${pane.title}`
  });
  return task;
}
function researchLibraryFile(type) {
  switch (type) {
    case "mission_start":
    case "milestone_progress":
    case "milestone_complete":
    case "periodic":
      return "research-findings.md";
    case "retry_cluster":
    case "stall_detected":
    case "discovered_issue":
    case "issue-triage":
    case "incident-analysis":
      return "learnings.md";
    case "architecture-audit":
    case "codebase-analysis":
      return "architecture.md";
    case "test-coverage":
      return "testing.md";
    case "contract-audit":
      return "validation.md";
    case "code-review":
      return "reviews.md";
    default:
      return "research-findings.md";
  }
}
function processResearchCompletion(config2, researchState, task) {
  if (!task.tags.includes("research")) return;
  const researchType = task.tags.find((tag) => tag !== "research") ?? "research";
  const summary = task.salientSummary ?? task.proof?.notes ?? task.description ?? task.title;
  const libraryDir = join13(config2.dir, ".tmux-ide", "library");
  if (!existsSync15(libraryDir)) mkdirSync12(libraryDir, { recursive: true });
  const filename = researchLibraryFile(researchType);
  const body = [`## ${task.id}: ${task.title}`, `Type: ${researchType}`, summary, "---", ""].join(
    "\n"
  );
  appendFileSync4(join13(libraryDir, filename), body + "\n");
  researchState.activeResearchTaskId = researchState.activeResearchTaskId === task.id ? null : researchState.activeResearchTaskId;
  researchState.lastResearchAt[researchType] = task.updated;
  if (researchType === "mission_start") {
    researchState.missionStartAnalyzed = true;
  }
  saveResearchState(config2.dir, researchState);
  appendEvent(config2.dir, {
    timestamp: task.updated,
    type: "research_finding",
    taskId: task.id,
    researchType,
    summary,
    message: `Recorded research findings for task ${task.id}`
  });
}
var RESEARCH_STATE_FILE, DEFAULT_COOLDOWN_MINUTES, SPINNERS2, SHELL_COMMANDS2, AGENT_PREFIXES, VERSION_PATTERN;
var init_research = __esm({
  "packages/daemon/src/lib/research.ts"() {
    "use strict";
    init_task_store();
    init_event_log();
    init_pane_comms();
    RESEARCH_STATE_FILE = ".tasks/research-state.json";
    DEFAULT_COOLDOWN_MINUTES = 30;
    SPINNERS2 = /^[⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏⠂⠒⠢⠆⠐⠠⠄◐◓◑◒✳|/\\-] /;
    SHELL_COMMANDS2 = /* @__PURE__ */ new Set(["zsh", "bash", "sh", "fish"]);
    AGENT_PREFIXES = ["claude", "codex"];
    VERSION_PATTERN = /^\d+\.\d+/;
  }
});

// packages/daemon/src/lib/orchestrator.ts
var orchestrator_exports = {};
__export(orchestrator_exports, {
  agentIdentifier: () => agentIdentifier,
  buildGoalPrompt: () => buildGoalPrompt,
  buildPlanningPrompt: () => buildPlanningPrompt,
  buildTaskPrompt: () => buildTaskPrompt,
  buildValidationPrompt: () => buildValidationPrompt,
  checkMilestoneCompletion: () => checkMilestoneCompletion,
  checkValidationResults: () => checkValidationResults,
  createOrchestrator: () => createOrchestrator,
  detectCompletions: () => detectCompletions,
  detectStalls: () => detectStalls,
  dispatch: () => dispatch,
  dispatchGoals: () => dispatchGoals,
  dispatchPlanning: () => dispatchPlanning,
  dispatchValidation: () => dispatchValidation,
  findBestAgent: () => findBestAgent,
  getCurrentMilestone: () => getCurrentMilestone,
  getHealth: () => getHealth,
  getOrchestratorHealth: () => getOrchestratorHealth,
  getPaneContentHashMetrics: () => getPaneContentHashMetrics,
  getPaneSpecialties: () => getPaneSpecialties,
  gracefulShutdown: () => gracefulShutdown,
  handleMissionComplete: () => handleMissionComplete,
  isAgentBusy: () => isAgentBusy,
  isAgentPane: () => isAgentPane,
  isIdleForDispatch: () => isIdleForDispatch,
  isMilestoneReady: () => isMilestoneReady,
  loadOrchestratorState: () => loadOrchestratorState,
  normalizePaneTitle: () => normalizePaneTitle,
  reconcile: () => reconcile,
  reloadConfig: () => reloadConfig,
  runHook: () => runHook,
  saveOrchestratorState: () => saveOrchestratorState,
  scheduleRetry: () => scheduleRetry,
  syncClaims: () => syncClaims
});
import { execSync as execSync4 } from "node:child_process";
import { randomUUID } from "node:crypto";
import {
  existsSync as existsSync16,
  readFileSync as readFileSync14,
  writeFileSync as writeFileSync13,
  appendFileSync as appendFileSync5,
  renameSync as renameSync10,
  mkdirSync as mkdirSync13,
  readdirSync as readdirSync5,
  watch
} from "node:fs";
import { join as join14 } from "node:path";
import yaml3 from "js-yaml";
function taskDispatchId(task) {
  return task.dispatchId ?? null;
}
function taskCompletedDispatchId(task) {
  return task.completedDispatchId ?? null;
}
function setTaskDispatch(task, record) {
  const dispatchTask = task;
  dispatchTask.dispatchId = record.dispatchId;
  dispatchTask.dispatchAt = record.at;
  dispatchTask.dispatchAgentPane = record.agentPane;
}
function clearTaskDispatch(task) {
  const dispatchTask = task;
  dispatchTask.dispatchId = null;
  dispatchTask.dispatchAt = null;
  dispatchTask.dispatchAgentPane = null;
}
function makeDispatchId(taskId) {
  return `${taskId}:${Date.now()}:${randomUUID()}`;
}
function getHealth(state) {
  return {
    ticking: state.ticking,
    lastTickMs: state.lastTickMs,
    inflight: state.inflightDispatches.size,
    idleAgents: state.idleAgents,
    queuedDispatches: state.queuedDispatches,
    lastError: state.lastError,
    totalDispatched: state.totalDispatched,
    totalCompleted: state.totalCompleted,
    totalFailed: state.totalFailed
  };
}
function getOrchestratorHealth(session) {
  return healthRegistry.get(session)?.() ?? null;
}
function emitTaskEvent(dir, type, input) {
  const at = (/* @__PURE__ */ new Date()).toISOString();
  appendEvent(dir, {
    timestamp: at,
    type,
    taskId: input.taskId,
    agent: input.agent ?? void 0,
    agentPane: input.agentPane ?? void 0,
    dispatchId: input.dispatchId ?? void 0,
    at,
    reason: input.reason,
    ...input.details ?? {},
    message: input.message
  });
}
function runHook(command2, cwd) {
  try {
    execSync4(command2, { cwd, timeout: 6e4, stdio: "pipe" });
    return { ok: true };
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    return { ok: false, error: message };
  }
}
function hashString(value) {
  let hash = 2166136261;
  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16);
}
function getPaneContentHashMetrics() {
  return { paneContentHashHits, paneContentHashMisses };
}
function normalizePaneTitle(title) {
  return title.replace(SPINNERS3, "").trim();
}
function agentIdentifier(pane) {
  if (pane.name) return pane.name;
  return AGENT_NAMES[pane.index % AGENT_NAMES.length] ?? `Agent ${pane.index}`;
}
function isAgentPane(pane) {
  const cmd = pane.currentCommand.toLowerCase();
  if (AGENT_PREFIXES2.some((p) => cmd.startsWith(p))) return true;
  if (VERSION_PATTERN2.test(cmd)) return true;
  if (/claude\s*code/i.test(pane.title)) return true;
  const name = normalizePaneTitle(pane.title);
  if (AGENT_NAMES.includes(name)) return true;
  return false;
}
function isAgentBusy(pane) {
  return SPINNERS3.test(pane.title);
}
function isAtAgentPrompt(pane) {
  const statusKey = `${pane.currentCommand}\0${pane.title}`;
  const lastLine = captureLastLine(pane.id);
  const hash = hashString(lastLine);
  const cached = paneContentCache.get(pane.id);
  if (cached && cached.statusKey === statusKey && cached.hash === hash) {
    paneContentHashHits += 1;
    return cached.atPrompt;
  }
  paneContentHashMisses += 1;
  const atPrompt = lastLine.includes("\u276F");
  paneContentCache.set(pane.id, { statusKey, hash, atPrompt });
  return atPrompt;
}
function isIdleForDispatch(pane) {
  const cmd = pane.currentCommand.toLowerCase();
  if (SHELL_COMMANDS3.has(cmd)) return true;
  if (isAgentPane(pane)) {
    if (!isAgentBusy(pane)) return true;
    return isAtAgentPrompt(pane);
  }
  return false;
}
function loadLibraryExcerpt(filePath, maxChars = 500) {
  if (!existsSync16(filePath)) return null;
  try {
    const content = readFileSync14(filePath, "utf-8").trim();
    if (!content) return null;
    if (content.length <= maxChars) return content;
    return content.slice(0, maxChars) + `
... (see full file at ${filePath})`;
  } catch {
    return null;
  }
}
function buildTaskPrompt(dir, task, config2) {
  let prompt = "";
  const mission = loadMission(dir);
  if (mission) {
    prompt += `## Mission: ${mission.title}
`;
    if (mission.description) prompt += `${mission.description}
`;
    prompt += "\n";
  }
  if (task.milestone && mission) {
    const milestone = mission.milestones.find((m) => m.id === task.milestone);
    if (milestone) {
      prompt += `## Milestone: ${milestone.title} (${milestone.id})
`;
      if (milestone.description) prompt += `${milestone.description}
`;
      prompt += "\n";
    }
  }
  const agentsMdPath = join14(dir, "AGENTS.md");
  const agentsContent = loadLibraryExcerpt(agentsMdPath, 1e3);
  if (agentsContent) {
    prompt += `## Agent Guidelines
${agentsContent}

`;
  }
  {
    const skillName = task.specialty ?? "general-worker";
    const skill = loadSkill(dir, skillName);
    if (skill?.body) {
      prompt += `## Your Role: ${skill.name}
`;
      prompt += `${skill.body}

`;
    }
  }
  if (task.goal) {
    const goal = loadGoal(dir, task.goal);
    if (goal) {
      prompt += `## Goal: ${goal.title}
`;
      if (goal.acceptance) prompt += `Acceptance: ${goal.acceptance}
`;
      prompt += "\n";
    }
  }
  prompt += `## Your Task: ${task.title}
`;
  if (task.description) prompt += `${task.description}
`;
  prompt += `
Priority: P${task.priority}
`;
  if (task.tags?.length) prompt += `Tags: ${task.tags.join(", ")}
`;
  if (task.fulfills?.length) prompt += `Fulfills assertions: ${task.fulfills.join(", ")}
`;
  prompt += "\n";
  if (task.milestone) {
    const allTasks = loadTasks(dir);
    const recentDone = allTasks.filter((t) => t.milestone === task.milestone && t.status === "done" && t.id !== task.id).slice(-5);
    if (recentDone.length > 0) {
      prompt += `## Recent Completions (${task.milestone})
`;
      for (const t of recentDone) {
        prompt += `- ${t.id}: ${t.title}`;
        if (t.salientSummary) prompt += ` \u2014 ${t.salientSummary}`;
        prompt += "\n";
      }
      prompt += "\n";
    }
  }
  const libraryDir = join14(dir, ".tmux-ide", "library");
  if (existsSync16(libraryDir)) {
    const archExcerpt = loadLibraryExcerpt(join14(libraryDir, "architecture.md"));
    if (archExcerpt) {
      prompt += `## Architecture
${archExcerpt}

`;
    }
    if (task.tags?.length) {
      try {
        const files = readdirSync5(libraryDir).filter(
          (f) => f.endsWith(".md") && f !== "architecture.md"
        );
        const tagSet = new Set(task.tags.map((t) => t.toLowerCase()));
        for (const file of files) {
          const stem = file.replace(/\.md$/, "").toLowerCase();
          if (tagSet.has(stem)) {
            const excerpt = loadLibraryExcerpt(join14(libraryDir, file));
            if (excerpt) {
              prompt += `## Reference: ${file}
${excerpt}

`;
            }
          }
        }
      } catch {
      }
    }
  }
  if (config2?.services && Object.keys(config2.services).length > 0) {
    prompt += `## Available Services
`;
    for (const [name, svc] of Object.entries(config2.services)) {
      prompt += `- **${name}**: \`${svc.command}\``;
      if (svc.port) prompt += ` (port ${svc.port})`;
      if (svc.healthcheck) prompt += ` \u2014 healthcheck: \`${svc.healthcheck}\``;
      prompt += "\n";
    }
    prompt += "\n";
  }
  prompt += `Workspace: ${dir}

`;
  prompt += `When done:
`;
  prompt += `  tmux-ide task done ${task.id} --proof "short summary of what you accomplished"
`;
  return prompt.trim();
}
function getCurrentMilestone(dir) {
  const mission = loadMission(dir);
  if (!mission || mission.milestones.length === 0) return null;
  const sorted = [...mission.milestones].sort((a, b) => a.order - b.order);
  return sorted.find((m) => m.status !== "done") ?? null;
}
function isMilestoneReady(dir, milestoneId) {
  const mission = loadMission(dir);
  if (!mission) return false;
  const target = mission.milestones.find((m) => m.id === milestoneId);
  if (!target) return false;
  return mission.milestones.filter((m) => m.order < target.order).every((m) => m.status === "done");
}
function isTaskMilestoneEligible(dir, task, mission) {
  if (!task.milestone) return true;
  if (!mission) return false;
  const milestone = mission.milestones.find((m) => m.id === task.milestone);
  if (!milestone) return false;
  if (milestone.status !== "active") return false;
  return isMilestoneReady(dir, milestone.id);
}
function checkMilestoneCompletion(config2, state, tasks, panes) {
  const mission = loadMission(config2.dir);
  if (!mission || mission.milestones.length === 0) return;
  let changed = false;
  const sorted = [...mission.milestones].sort((a, b) => a.order - b.order);
  for (const milestone of sorted) {
    if (milestone.status !== "active") continue;
    const milestoneTasks = tasks.filter((t) => t.milestone === milestone.id);
    if (milestoneTasks.length === 0) continue;
    const allDone = milestoneTasks.every((t) => t.status === "done");
    if (!allDone) continue;
    const contract = loadValidationContract(config2.dir);
    if (contract) {
      const dispatched = dispatchValidation(config2, state, milestone, milestoneTasks, panes);
      if (!dispatched) continue;
      milestone.status = "validating";
      milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
      changed = true;
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "milestone_validating",
        milestoneId: milestone.id,
        title: milestone.title,
        message: `Milestone "${milestone.title}" (${milestone.id}) \u2014 all tasks done, dispatching validation`
      });
    } else {
      milestone.status = "done";
      milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
      changed = true;
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "milestone_complete",
        milestoneId: milestone.id,
        title: milestone.title,
        message: `Milestone "${milestone.title}" (${milestone.id}) completed`
      });
      activateNextMilestone(mission, milestone.id);
    }
  }
  if (changed) {
    mission.updated = (/* @__PURE__ */ new Date()).toISOString();
    saveMission(config2.dir, mission);
  }
}
function activateNextMilestone(mission, completedMilestoneId) {
  const sorted = [...mission.milestones].sort((a, b) => a.order - b.order);
  const completed = sorted.find((milestone) => milestone.id === completedMilestoneId);
  if (!completed || completed.status !== "done") return false;
  const next = sorted.find(
    (milestone) => milestone.order > completed.order && milestone.status === "locked"
  );
  if (!next) return false;
  next.status = "active";
  next.updated = (/* @__PURE__ */ new Date()).toISOString();
  return true;
}
function buildValidationPrompt(dir, milestone, completedTasks) {
  const contract = loadValidationContract(dir);
  let prompt = `# Validation: ${milestone.title} (${milestone.id})

`;
  if (contract) {
    prompt += `## Validation Contract

${contract}

`;
  }
  prompt += `## Completed Tasks

`;
  for (const task of completedTasks) {
    prompt += `### ${task.id}: ${task.title}
`;
    if (task.proof) prompt += `Proof: ${JSON.stringify(task.proof)}
`;
    if (task.fulfills.length > 0) prompt += `Fulfills assertions: ${task.fulfills.join(", ")}
`;
    if (task.salientSummary) prompt += `Summary: ${task.salientSummary}
`;
    prompt += "\n";
  }
  prompt += `## Instructions

`;
  prompt += `Verify each assertion in the validation contract. For each assertion:
`;
  prompt += `1. Check that the completed work actually satisfies the requirement
`;
  prompt += `2. Run any relevant tests or checks
`;
  prompt += `3. Report your finding:
`;
  prompt += `   tmux-ide validate assert <ASSERT_ID> --status passing --evidence "description"
`;
  prompt += `   tmux-ide validate assert <ASSERT_ID> --status failing --evidence "what's wrong"

`;
  prompt += `When done verifying all assertions, the orchestrator will automatically detect results.
`;
  return prompt.trim();
}
function dispatchValidation(config2, state, milestone, completedTasks, panes) {
  const validatorPane = panes.find((p) => p.role === "validator" && isIdleForDispatch(p)) ?? panes.find((p) => {
    if (p.role === "lead") return false;
    if (config2.masterPane && normalizePaneTitle(p.title) === config2.masterPane) return false;
    return isIdleForDispatch(p);
  });
  if (!validatorPane) {
    appendEvent(config2.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "error",
      message: `No idle validator pane for milestone "${milestone.title}" \u2014 validation pending`
    });
    return false;
  }
  const valState = loadValidationState(config2.dir) ?? { assertions: {}, lastVerified: null };
  for (const task of completedTasks) {
    for (const assertionId of task.fulfills) {
      if (!valState.assertions[assertionId]) {
        valState.assertions[assertionId] = {
          status: "pending",
          verifiedBy: null,
          verifiedAt: null,
          evidence: null,
          blockedBy: null
        };
      }
    }
  }
  saveValidationState(config2.dir, valState);
  const prompt = buildValidationPrompt(config2.dir, milestone, completedTasks);
  const dispatchDir = join14(config2.dir, ".tasks", "dispatch");
  if (!existsSync16(dispatchDir)) mkdirSync13(dispatchDir, { recursive: true });
  const dispatchFile = join14(dispatchDir, `validate-${milestone.id}.md`);
  writeFileSync13(dispatchFile, prompt);
  try {
    sendCommand(
      config2.session,
      validatorPane.id,
      `Read and execute: .tasks/dispatch/validate-${milestone.id}.md`
    );
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    appendEvent(config2.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "error",
      message: `Failed to dispatch validation for milestone "${milestone.title}": ${message}`
    });
    return false;
  }
  saveValidationState(config2.dir, valState);
  appendEvent(config2.dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "validation_dispatch",
    milestoneId: milestone.id,
    title: milestone.title,
    target: validatorPane.title,
    message: `Dispatched validation for "${milestone.title}" to ${validatorPane.title}`
  });
  state.lastActivity.set(validatorPane.id, Date.now());
  return true;
}
function checkValidationResults(config2, tasks) {
  const mission = loadMission(config2.dir);
  if (!mission) return;
  const valState = loadValidationState(config2.dir);
  if (!valState) return;
  let changed = false;
  const sorted = [...mission.milestones].sort((a, b) => a.order - b.order);
  for (const milestone of sorted) {
    if (milestone.status !== "validating") continue;
    const milestoneTasks = tasks.filter((t) => t.milestone === milestone.id);
    const milestoneAssertionIds = new Set(milestoneTasks.flatMap((t) => t.fulfills));
    if (milestoneAssertionIds.size === 0) {
      milestone.status = "done";
      milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
      changed = true;
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "milestone_complete",
        milestoneId: milestone.id,
        title: milestone.title,
        message: `Milestone "${milestone.title}" (${milestone.id}) \u2014 no assertions to validate`
      });
      activateNextMilestone(mission, milestone.id);
      continue;
    }
    const allVerified = [...milestoneAssertionIds].every((id) => {
      const entry = valState.assertions[id];
      return entry && entry.status !== "pending";
    });
    if (!allVerified) continue;
    const allPassing = [...milestoneAssertionIds].every(
      (id) => valState.assertions[id]?.status === "passing"
    );
    if (allPassing) {
      milestone.status = "done";
      milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
      changed = true;
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "milestone_complete",
        milestoneId: milestone.id,
        title: milestone.title,
        message: `Milestone "${milestone.title}" (${milestone.id}) \u2014 validation passed`
      });
      activateNextMilestone(mission, milestone.id);
    } else {
      const failedIds = [...milestoneAssertionIds].filter(
        (id) => valState.assertions[id]?.status === "failing"
      );
      for (const assertionId of failedIds) {
        const entry = valState.assertions[assertionId];
        if (!entry || entry.status !== "failing") continue;
        const id = nextTaskId(config2.dir);
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const task = {
          id,
          title: `Remediate: assertion ${assertionId} failing`,
          description: `Assertion ${assertionId} failed validation.
Evidence: ${entry.evidence ?? "none"}

Fix the issue and ensure the assertion passes.`,
          goal: null,
          status: "todo",
          assignee: null,
          priority: 1,
          created: now,
          updated: now,
          tags: ["remediation"],
          proof: null,
          depends_on: [],
          retryCount: 0,
          maxRetries: 5,
          lastError: null,
          nextRetryAt: null,
          milestone: milestone.id,
          specialty: null,
          fulfills: [assertionId],
          discoveredIssues: [],
          salientSummary: null
        };
        saveTask(config2.dir, task);
        entry.status = "pending";
        entry.verifiedBy = null;
        entry.verifiedAt = null;
        entry.evidence = null;
        entry.blockedBy = null;
        appendEvent(config2.dir, {
          timestamp: (/* @__PURE__ */ new Date()).toISOString(),
          type: "remediation",
          taskId: id,
          assertionId,
          message: `Created remediation task ${id} for failing assertion ${assertionId}`
        });
      }
      saveValidationState(config2.dir, valState);
      milestone.status = "active";
      milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
      changed = true;
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "validation_failed",
        milestoneId: milestone.id,
        title: milestone.title,
        failedCount: failedIds.length,
        message: `Milestone "${milestone.title}" (${milestone.id}) \u2014 ${failedIds.length} assertion(s) failed, remediation tasks created`
      });
    }
  }
  if (changed) {
    mission.updated = (/* @__PURE__ */ new Date()).toISOString();
    saveMission(config2.dir, mission);
  }
}
function buildPlanningPrompt(dir) {
  const mission = loadMission(dir);
  let prompt = `# Mission Planning

`;
  if (mission) {
    prompt += `## Mission: ${mission.title}
`;
    if (mission.description) prompt += `${mission.description}
`;
    prompt += "\n";
  }
  const agentsMdPath = join14(dir, "AGENTS.md");
  const agentsContent = loadLibraryExcerpt(agentsMdPath, 1e3);
  if (agentsContent) {
    prompt += `## Agent Guidelines
${agentsContent}

`;
  }
  prompt += `## Instructions

`;
  prompt += `You are the mission planner. Analyze the mission scope and create an execution plan.

`;
  prompt += `1. Design milestones (sequential execution phases):
`;
  prompt += `   tmux-ide milestone create "title" --sequence N [-d "description"]

`;
  prompt += `2. Create a validation contract at .tasks/validation-contract.md with assertion IDs:
`;
  prompt += `   Each assertion is a testable claim (e.g., ASSERT01: "Auth endpoint returns 200")

`;
  prompt += `3. Create tasks linked to milestones:
`;
  prompt += `   tmux-ide task create "title" --milestone M1 --specialty frontend --fulfills "ASSERT01,ASSERT02" [-d "description"]

`;
  prompt += `4. Ensure every assertion in the contract is claimed by at least one task's --fulfills

`;
  prompt += `5. When planning is complete, signal:
`;
  prompt += `   tmux-ide mission plan-complete
`;
  return prompt.trim();
}
function dispatchPlanning(config2, state, panes) {
  if (state.claimedTasks.has(PLANNING_CLAIM)) return;
  const plannerPane = panes.find((p) => p.role === "lead") ?? panes.find((p) => config2.masterPane && normalizePaneTitle(p.title) === config2.masterPane) ?? panes.find((p) => isIdleForDispatch(p));
  if (!plannerPane) {
    appendEvent(config2.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "error",
      message: "Unable to dispatch mission planning: no planner pane available"
    });
    return;
  }
  state.claimedTasks.add(PLANNING_CLAIM);
  const prompt = buildPlanningPrompt(config2.dir);
  const dispatchDir = join14(config2.dir, ".tasks", "dispatch");
  if (!existsSync16(dispatchDir)) mkdirSync13(dispatchDir, { recursive: true });
  const dispatchFile = join14(dispatchDir, "planning.md");
  writeFileSync13(dispatchFile, prompt);
  sendCommand(config2.session, plannerPane.id, `Read and execute: .tasks/dispatch/planning.md`);
  appendEvent(config2.dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "planning",
    target: plannerPane.title,
    message: `Dispatched mission planning to ${plannerPane.title}`
  });
  state.lastActivity.set(plannerPane.id, Date.now());
}
function handleMissionComplete(config2, mission, panes) {
  if (!mission.milestones.every((milestone) => milestone.status === "done")) return;
  mission.status = "complete";
  mission.updated = (/* @__PURE__ */ new Date()).toISOString();
  saveMission(config2.dir, mission);
  let prResult = null;
  if (mission.branch && isGhAvailable()) {
    prResult = createMissionPr(mission, config2.dir);
  }
  const totalMilestones = mission.milestones.length;
  const tasks = loadTasks(config2.dir);
  const doneTasks = tasks.filter((t) => t.status === "done").length;
  let summary = `# Mission Complete: ${mission.title}

`;
  summary += `Milestones: ${totalMilestones} completed
`;
  summary += `Tasks: ${doneTasks}/${tasks.length} done
`;
  if (prResult) summary += `PR: ${prResult.url}
`;
  summary += `
Mission "${mission.title}" has been delivered.`;
  const dispatchDir = join14(config2.dir, ".tasks", "dispatch");
  if (!existsSync16(dispatchDir)) mkdirSync13(dispatchDir, { recursive: true });
  writeFileSync13(join14(dispatchDir, "mission-complete.md"), summary);
  if (config2.masterPane) {
    const masterPane = panes.find((p) => p.role === "lead") ?? panes.find((p) => p.title === config2.masterPane);
    if (masterPane) {
      sendCommand(
        config2.session,
        masterPane.id,
        `Mission complete: "${mission.title}". Run: tmux-ide dispatch mission-complete`
      );
    }
  }
  appendMissionHistory(config2.dir, mission, tasks);
  appendEvent(config2.dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "mission_complete",
    title: mission.title,
    milestoneCount: totalMilestones,
    taskCount: doneTasks,
    ...prResult ? { prNumber: prResult.number } : {},
    message: `Mission "${mission.title}" completed \u2014 ${totalMilestones} milestones, ${doneTasks} tasks${prResult ? `, PR #${prResult.number}` : ""}`
  });
}
function isRetryEligible(task, now) {
  if (!task.nextRetryAt || task.retryCount >= (task.maxRetries ?? 5)) return false;
  const retryTime = new Date(task.nextRetryAt).getTime();
  return retryTime <= now;
}
function claimTaskForDispatch(config2, state, candidate, agent, dispatchId) {
  if (state.claimedTasks.has(candidate.id)) return null;
  if (config2.dryRun) {
    const nowMs2 = Date.now();
    if (candidate.status !== "todo" && !isRetryEligible(candidate, nowMs2)) return null;
    const agentName2 = agentIdentifier(agent);
    const at2 = new Date(nowMs2).toISOString();
    const record2 = {
      taskId: candidate.id,
      dispatchId,
      agentPane: agent.id,
      agentName: agentName2,
      at: at2
    };
    const dryTask = {
      ...candidate,
      status: "in-progress",
      assignee: agentName2,
      nextRetryAt: null,
      updated: at2
    };
    setTaskDispatch(dryTask, record2);
    emitTaskEvent(config2.dir, "task.claimed", {
      taskId: dryTask.id,
      agentPane: agent.id,
      agent: agentName2,
      dispatchId,
      message: `Dry-run claim for "${dryTask.title}" by ${agentName2}`
    });
    return { task: dryTask, record: record2 };
  }
  const latest = loadTask(config2.dir, candidate.id);
  if (!latest) return null;
  const nowMs = Date.now();
  if (latest.status !== "todo" && !isRetryEligible(latest, nowMs)) return null;
  if (state.claimedTasks.has(latest.id)) return null;
  const agentName = agentIdentifier(agent);
  const at = new Date(nowMs).toISOString();
  const record = {
    taskId: latest.id,
    dispatchId,
    agentPane: agent.id,
    agentName,
    at
  };
  latest.status = "in-progress";
  latest.assignee = agentName;
  latest.nextRetryAt = null;
  latest.updated = at;
  setTaskDispatch(latest, record);
  saveTask(config2.dir, latest);
  state.claimedTasks.add(latest.id);
  state.inflightDispatches.set(latest.id, record);
  state.taskClaimTimes.set(latest.id, nowMs);
  emitTaskEvent(config2.dir, "task.claimed", {
    taskId: latest.id,
    agentPane: agent.id,
    agent: agentName,
    dispatchId,
    message: config2.dryRun ? `Dry-run claim for "${latest.title}" by ${agentName}` : `Claimed "${latest.title}" for ${agentName}`
  });
  return { task: latest, record };
}
function rollbackDispatchClaim(config2, state, task, dispatchId, reason) {
  if (!config2.dryRun) {
    const latest = loadTask(config2.dir, task.id) ?? task;
    if (latest.status === "in-progress" && latest.id === task.id) {
      latest.status = "todo";
      latest.assignee = null;
      latest.updated = (/* @__PURE__ */ new Date()).toISOString();
      clearTaskDispatch(latest);
      saveTask(config2.dir, latest);
    }
  }
  state.claimedTasks.delete(task.id);
  state.inflightDispatches.delete(task.id);
  state.taskClaimTimes.delete(task.id);
  emitTaskEvent(config2.dir, "task.failed", {
    taskId: task.id,
    dispatchId,
    reason,
    message: `Dispatch for "${task.title}" failed: ${reason}`
  });
}
function dispatch(config2, state, tasks, panes) {
  if (config2.dispatchMode === "missions" && !state.claimedTasks.has("__coverage_warned__")) {
    const { unclaimed } = checkCoverage(config2.dir);
    if (unclaimed.length > 0) {
      state.claimedTasks.add("__coverage_warned__");
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "error",
        message: `Coverage gap: ${unclaimed.length} assertion(s) not claimed by any task: ${unclaimed.join(", ")}`
      });
    }
  }
  const idleAgents = panes.filter((p) => {
    if (p.role === "lead") return false;
    if (!p.role && config2.masterPane && normalizePaneTitle(p.title) === config2.masterPane)
      return false;
    if (!isIdleForDispatch(p)) return false;
    const name = agentIdentifier(p);
    const hasActiveTask = tasks.some(
      (t) => t.assignee === name && (t.status === "in-progress" || t.status === "review")
    );
    if (hasActiveTask) return false;
    return true;
  });
  state.idleAgents = idleAgents.length;
  const maxConcurrent = config2.maxConcurrentAgents ?? 10;
  const inProgressCount = tasks.filter((t) => t.status === "in-progress").length;
  const availableSlots = Math.max(0, maxConcurrent - inProgressCount);
  if (availableSlots === 0) return;
  const now = Date.now();
  const mission = config2.dispatchMode === "missions" ? loadMission(config2.dir) : null;
  const todoTasks = tasks.filter((t) => {
    if (state.claimedTasks.has(t.id)) return false;
    if (t.status === "todo") {
      if (t.nextRetryAt && new Date(t.nextRetryAt).getTime() > now) return false;
      if (t.depends_on && t.depends_on.length > 0) {
        const allDepsDone = t.depends_on.every((depId) => {
          const dep = tasks.find((d) => d.id === depId);
          return dep?.status === "done";
        });
        if (!allDepsDone) return false;
      }
      if (config2.dispatchMode === "missions" && !isTaskMilestoneEligible(config2.dir, t, mission)) {
        return false;
      }
      return true;
    }
    if (t.nextRetryAt && t.retryCount < (t.maxRetries ?? 5)) {
      const retryTime = new Date(t.nextRetryAt).getTime();
      if (retryTime <= now) return true;
    }
    return false;
  }).sort((a, b) => a.priority - b.priority);
  state.queuedDispatches = todoTasks.length;
  const assignedAgents = /* @__PURE__ */ new Set();
  let dispatched = 0;
  for (const task of todoTasks) {
    if (dispatched >= availableSlots) break;
    const agent = findBestAgent(config2, idleAgents, task, assignedAgents);
    if (!agent) continue;
    const dispatchId = makeDispatchId(task.id);
    const currentPanes = listSessionPanes(config2.session);
    if (!currentPanes.find((p) => p.id === agent.id)) {
      emitTaskEvent(config2.dir, "task.failed", {
        taskId: task.id,
        agentPane: agent.id,
        agent: agentIdentifier(agent),
        dispatchId,
        reason: "pane vanished before dispatch",
        message: `Skipped dispatch for "${task.title}" because pane ${agent.id} no longer exists`
      });
      continue;
    }
    const claimed = claimTaskForDispatch(config2, state, task, agent, dispatchId);
    if (!claimed) continue;
    const { task: claimedTask, record } = claimed;
    if (config2.beforeRun && !config2.dryRun) {
      const result = runHook(config2.beforeRun, config2.dir);
      if (!result.ok) {
        rollbackDispatchClaim(config2, state, claimedTask, dispatchId, result.error);
        continue;
      }
    }
    const prompt = buildTaskPrompt(config2.dir, claimedTask, config2);
    const dispatchDir = join14(config2.dir, ".tasks", "dispatch");
    if (!config2.dryRun && !existsSync16(dispatchDir)) mkdirSync13(dispatchDir, { recursive: true });
    if (!/^\d{3,}$/.test(claimedTask.id)) {
      rollbackDispatchClaim(config2, state, claimedTask, dispatchId, "invalid task id");
      continue;
    }
    const dispatchFile = join14(dispatchDir, `${claimedTask.id}.md`);
    if (!config2.dryRun) writeFileSync13(dispatchFile, prompt);
    const shortCmd = `tmux-ide dispatch ${claimedTask.id}`;
    const sent = config2.dryRun ? true : sendCommand(config2.session, agent.id, shortCmd);
    if (!sent) {
      rollbackDispatchClaim(config2, state, claimedTask, dispatchId, "tmux send failed");
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "error",
        taskId: claimedTask.id,
        agent: agent.title,
        dispatchId,
        message: `Failed to send command to ${agent.title} for task "${claimedTask.title}" \u2014 rolled back`
      });
      continue;
    }
    emitTaskEvent(config2.dir, "task.dispatched", {
      taskId: claimedTask.id,
      agentPane: record.agentPane,
      agent: record.agentName,
      dispatchId,
      details: { dryRun: config2.dryRun === true },
      message: config2.dryRun ? `Dry-run dispatch "${claimedTask.title}" to ${agent.title}` : `Dispatched "${claimedTask.title}" to ${agent.title}`
    });
    appendEvent(config2.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "dispatch",
      taskId: claimedTask.id,
      agent: agent.title,
      dispatchId,
      message: config2.dryRun ? `Dry-run dispatch "${claimedTask.title}" to ${agent.title}` : `Dispatched "${claimedTask.title}" to ${agent.title}`
    });
    state.totalDispatched++;
    assignedAgents.add(agent.id);
    dispatched++;
    state.lastActivity.set(agent.id, Date.now());
  }
}
function matchesSpecialty(specialty, agentSpecialties) {
  if (!specialty) return true;
  if (agentSpecialties.length === 0) return true;
  const tags = specialty.split(",").map((s) => s.trim().toLowerCase());
  return tags.some((tag) => agentSpecialties.includes(tag));
}
function findBestAgent(config2, idleAgents, task, excludeIds) {
  const candidates = idleAgents.filter((p) => !excludeIds.has(p.id));
  if (candidates.length === 0) return null;
  if (task.assignee) {
    const preferred = candidates.find((p) => agentIdentifier(p) === task.assignee);
    if (preferred) return preferred;
  }
  if (!task.specialty) return candidates[0] ?? null;
  const specialist = candidates.find((p) => {
    const specs = getPaneSpecialties(config2, p);
    return specs.length > 0 && matchesSpecialty(task.specialty, specs);
  });
  if (specialist) return specialist;
  const generalist = candidates.find((p) => {
    const specs = getPaneSpecialties(config2, p);
    return specs.length === 0;
  });
  return generalist ?? null;
}
function getPaneSpecialties(config2, pane) {
  const key = pane.name ?? pane.title;
  return config2.paneSpecialties.get(key) ?? [];
}
function isPlannerPane(config2, pane) {
  const key = pane.name ?? pane.title;
  if (config2.paneSpecialties.has(key)) return true;
  if (pane.role === "lead") return false;
  return isAgentPane(pane);
}
function buildGoalPrompt(dir, goal, planner) {
  const mission = loadMission(dir);
  const name = agentIdentifier(planner);
  const specialtyLabel = goal.specialty ?? "general";
  let prompt = `You are ${name}, a ${specialtyLabel} planner.

`;
  if (mission) {
    prompt += `## Mission: ${mission.title}
`;
    if (mission.description) prompt += `${mission.description}
`;
    prompt += "\n";
    if (goal.milestone) {
      const milestone = mission.milestones.find((m) => m.id === goal.milestone);
      if (milestone) {
        prompt += `## Milestone: ${milestone.title} (${milestone.id})
`;
        if (milestone.description) prompt += `${milestone.description}
`;
        prompt += "\n";
      }
    }
  }
  const agentsMdPath = join14(dir, "AGENTS.md");
  const agentsContent = loadLibraryExcerpt(agentsMdPath, 1e3);
  if (agentsContent) {
    prompt += `## Agent Guidelines
${agentsContent}

`;
  }
  prompt += `Your Goal: ${goal.title}
`;
  if (goal.acceptance) prompt += `Acceptance Criteria: ${goal.acceptance}
`;
  prompt += `Priority: P${goal.priority}

`;
  prompt += `Instructions:
`;
  prompt += `1. Read plans/MISSION.md for architectural context (if it exists)
`;
  prompt += `2. Create your plan file: plans/goal-${goal.id}-${slugify(goal.title, 30)}.md
`;
  prompt += `   Include: analysis, approach, task breakdown, risks
`;
  prompt += `3. Create tasks for your goal:
`;
  prompt += `   tmux-ide task create "task title" --goal ${goal.id} --priority N
`;
  prompt += `4. Execute tasks using Claude Code subagents for parallel work
`;
  prompt += `5. Update your plan file as tasks complete
`;
  prompt += `6. Review all completed work for quality
`;
  prompt += `7. When done: tmux-ide goal done ${goal.id} --proof "summary of what was accomplished"

`;
  prompt += `You own this goal end-to-end. Plan it, execute it, deliver it.
`;
  return prompt.trim();
}
function dispatchGoals(config2, state, goals, tasks, panes) {
  const idlePlanners = panes.filter((p) => {
    if (p.index === 0) return false;
    if (p.role === "lead") return false;
    if (!p.role && config2.masterPane && normalizePaneTitle(p.title) === config2.masterPane)
      return false;
    if (!isPlannerPane(config2, p)) return false;
    if (!isIdleForDispatch(p)) return false;
    const name = agentIdentifier(p);
    const hasActiveGoal = goals.some((g) => g.assignee === name && g.status === "in-progress");
    if (hasActiveGoal) return false;
    return true;
  });
  const todoGoals = goals.filter((g) => g.status === "todo" && !g.assignee).sort((a, b) => a.priority - b.priority);
  for (const planner of idlePlanners) {
    const plannerSpecs = getPaneSpecialties(config2, planner);
    const goal = todoGoals.find((g) => matchesSpecialty(g.specialty, plannerSpecs));
    if (!goal) continue;
    todoGoals.splice(todoGoals.indexOf(goal), 1);
    goal.assignee = agentIdentifier(planner);
    goal.status = "in-progress";
    goal.updated = (/* @__PURE__ */ new Date()).toISOString();
    saveGoal(config2.dir, goal);
    const prompt = buildGoalPrompt(config2.dir, goal, planner);
    const goalDispatchDir = join14(config2.dir, ".tasks", "dispatch");
    if (!existsSync16(goalDispatchDir)) mkdirSync13(goalDispatchDir, { recursive: true });
    const goalDispatchFile = join14(goalDispatchDir, `goal-${goal.id}.md`);
    writeFileSync13(goalDispatchFile, prompt);
    sendCommand(
      config2.session,
      planner.id,
      `Read and execute the goal in .tasks/dispatch/goal-${goal.id}.md`
    );
    appendEvent(config2.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "dispatch",
      taskId: goal.id,
      agent: planner.title,
      message: `Dispatched goal "${goal.title}" to ${planner.title}`
    });
    state.lastActivity.set(planner.id, Date.now());
  }
}
function detectStalls(config2, state, tasks, panes, researchState) {
  const now = Date.now();
  for (const task of tasks.filter((t) => t.status === "in-progress" && t.assignee)) {
    if (task.tags.includes("research") && (!researchState?.activeResearchTaskId || researchState.activeResearchTaskId !== task.id)) {
      continue;
    }
    const agentPane = panes.find((p) => agentIdentifier(p) === task.assignee);
    if (!agentPane) continue;
    const lastSeen = state.lastActivity.get(agentPane.id) ?? now;
    const elapsed = now - lastSeen;
    if (elapsed > config2.stallTimeout) {
      const lastNudge = state.stallNudges.get(task.id) ?? 0;
      if (now - lastNudge < STALL_NUDGE_COOLDOWN_MS) continue;
      const dispatchId = taskDispatchId(task) ?? taskCompletedDispatchId(task);
      sendCommand(
        config2.session,
        agentPane.id,
        `You've been working on "${task.title}" for ${Math.floor(elapsed / 6e4)} minutes. If done, run: tmux-ide task done ${task.id} --proof "describe what you did". If stuck, run: tmux-ide task update ${task.id} --status review`
      );
      state.stallNudges.set(task.id, now);
      emitTaskEvent(config2.dir, "agent.stalled", {
        taskId: task.id,
        agentPane: agentPane.id,
        agent: task.assignee,
        dispatchId,
        details: { elapsedMs: elapsed },
        message: `Stall detected: "${task.title}" (${Math.floor(elapsed / 6e4)}m)`
      });
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "stall",
        taskId: task.id,
        agent: task.assignee,
        dispatchId: dispatchId ?? void 0,
        agentPane: agentPane.id,
        message: `Stall detected: "${task.title}" (${Math.floor(elapsed / 6e4)}m)`
      });
      state.lastActivity.set(agentPane.id, now);
    }
  }
}
function detectCompletions(config2, state, tasks, panes, researchState) {
  for (const task of tasks) {
    const prev = state.previousTasks.get(task.id);
    if (prev === "in-progress" && task.status === "review" && task.lastError) {
      const dispatchId = taskDispatchId(task);
      const retryKey = `${task.id}:retry:${dispatchId ?? "legacy"}:${task.retryCount}`;
      if (state.completedDispatches.has(retryKey)) continue;
      state.completedDispatches.add(retryKey);
      state.taskClaimTimes.delete(task.id);
      const retried = scheduleRetry(config2.dir, state, task, task.lastError);
      if (!retried && config2.masterPane) {
        const masterPaneInfo = panes.find((p) => p.role === "lead") ?? panes.find((p) => p.title === config2.masterPane);
        if (masterPaneInfo) {
          const retryDispDir = join14(config2.dir, ".tasks", "dispatch");
          if (!existsSync16(retryDispDir)) mkdirSync13(retryDispDir, { recursive: true });
          const msgFile = join14(retryDispDir, `retry-exhausted-${task.id}.md`);
          const msg = `# Task Failed: ${task.title}

Failed after ${task.maxRetries ?? 5} retries.
Last error: ${task.lastError}

Manual intervention required.`;
          writeFileSync13(msgFile, msg);
          sendCommand(
            config2.session,
            masterPaneInfo.id,
            `Task ${task.id} failed after retries. Run: tmux-ide dispatch retry-exhausted-${task.id}`
          );
        }
      }
    }
    if (prev === "in-progress" && task.status === "done") {
      const dispatchId = taskDispatchId(task) ?? taskCompletedDispatchId(task);
      const inflight = state.inflightDispatches.get(task.id);
      if (inflight && dispatchId && inflight.dispatchId !== dispatchId) {
        appendEvent(config2.dir, {
          timestamp: (/* @__PURE__ */ new Date()).toISOString(),
          type: "error",
          taskId: task.id,
          dispatchId,
          message: `Ignored stale completion for "${task.title}" from dispatch ${dispatchId}`
        });
        continue;
      }
      const completionKey = `${task.id}:${dispatchId ?? "legacy"}`;
      if (state.completedDispatches.has(completionKey)) continue;
      state.completedDispatches.add(completionKey);
      const claimTime = state.taskClaimTimes.get(task.id);
      const elapsedMs = claimTime ? Date.now() - claimTime : void 0;
      if (claimTime && task.assignee) {
        recordTaskTime(config2.dir, task.assignee, task.id, elapsedMs);
      }
      state.taskClaimTimes.delete(task.id);
      state.claimedTasks.delete(task.id);
      state.inflightDispatches.delete(task.id);
      state.stallNudges.delete(task.id);
      state.totalCompleted++;
      task.completedDispatchId = dispatchId;
      clearTaskDispatch(task);
      const latest = loadTask(config2.dir, task.id);
      if (latest?.status === "done") {
        latest.completedDispatchId = dispatchId;
        clearTaskDispatch(latest);
        saveTask(config2.dir, latest);
      }
      emitTaskEvent(config2.dir, "task.completed", {
        taskId: task.id,
        agentPane: inflight?.agentPane ?? null,
        agent: task.assignee,
        dispatchId,
        details: { durationMs: elapsedMs },
        message: `Completed "${task.title}" by ${task.assignee ?? "unknown"}${task.salientSummary ? ` \u2014 ${task.salientSummary}` : ""}`
      });
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "completion",
        taskId: task.id,
        agent: task.assignee ?? void 0,
        dispatchId: dispatchId ?? void 0,
        durationMs: elapsedMs,
        message: `Completed "${task.title}" by ${task.assignee ?? "unknown"}${task.salientSummary ? ` \u2014 ${task.salientSummary}` : ""}`
      });
      if (task.tags.includes("research") && researchState) {
        processResearchCompletion(config2, researchState, task);
      }
      if (task.salientSummary) {
        try {
          const libraryDir = join14(config2.dir, ".tmux-ide", "library");
          if (!existsSync16(libraryDir)) mkdirSync13(libraryDir, { recursive: true });
          const learningsPath = join14(libraryDir, "learnings.md");
          const entry = `## Task ${task.id}: ${task.title}
${task.salientSummary}
---

`;
          appendFileSync5(learningsPath, entry);
        } catch (err) {
          const msg = err instanceof Error ? err.message : String(err);
          console.error(`[orchestrator] Failed to write learnings.md: ${msg}`);
          appendEvent(config2.dir, {
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            type: "error",
            taskId: task.id,
            message: `Failed to append salientSummary to learnings.md: ${msg}`
          });
        }
      }
      if (task.discoveredIssues && task.discoveredIssues.length > 0) {
        for (const issue of task.discoveredIssues) {
          const followUpId = nextTaskId(config2.dir);
          const now = (/* @__PURE__ */ new Date()).toISOString();
          const followUp = {
            id: followUpId,
            title: issue.length > 80 ? issue.slice(0, 77) + "..." : issue,
            description: `Discovered during task ${task.id} ("${task.title}"):

${issue}`,
            goal: task.goal,
            status: "todo",
            assignee: null,
            priority: 2,
            created: now,
            updated: now,
            tags: ["discovered-issue"],
            proof: null,
            depends_on: [],
            retryCount: 0,
            maxRetries: 5,
            lastError: null,
            nextRetryAt: null,
            milestone: task.milestone,
            specialty: null,
            fulfills: [],
            discoveredIssues: [],
            salientSummary: null
          };
          saveTask(config2.dir, followUp);
          appendEvent(config2.dir, {
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            type: "discovered_issue",
            taskId: followUpId,
            sourceTaskId: task.id,
            issue,
            message: `Auto-created follow-up task ${followUpId} from ${task.id}: ${issue}`
          });
        }
      }
      if (config2.afterRun) {
        runHook(config2.afterRun, config2.dir);
      }
      if (config2.masterPane) {
        const masterPaneInfo = panes.find((p) => p.role === "lead") ?? panes.find((p) => p.title === config2.masterPane);
        if (masterPaneInfo) {
          const proofStr = task.proof ? JSON.stringify(task.proof, null, 2) : "no proof provided";
          const dispDir = join14(config2.dir, ".tasks", "dispatch");
          if (!existsSync16(dispDir)) mkdirSync13(dispDir, { recursive: true });
          const msgFile = join14(dispDir, `completed-${task.id}.md`);
          const msg = `# Task Completed: ${task.title}

By: ${task.assignee}
Proof:
${proofStr}

Review and approve, or request changes.`;
          writeFileSync13(msgFile, msg);
          const shortTitle = task.title.length > 60 ? task.title.slice(0, 57) + "..." : task.title;
          sendCommand(
            config2.session,
            masterPaneInfo.id,
            `Task done: "${shortTitle}" by ${task.assignee}. Run: tmux-ide dispatch completed-${task.id}`
          );
        }
      }
    }
  }
}
function scheduleRetry(dir, state, task, reason) {
  const maxRetries = task.maxRetries ?? 5;
  const dispatchId = taskDispatchId(task);
  if (task.retryCount >= maxRetries) {
    task.status = "review";
    task.lastError = reason;
    task.assignee = null;
    task.updated = (/* @__PURE__ */ new Date()).toISOString();
    clearTaskDispatch(task);
    saveTask(dir, task);
    state.claimedTasks.delete(task.id);
    state.inflightDispatches.delete(task.id);
    state.taskClaimTimes.delete(task.id);
    state.stallNudges.delete(task.id);
    state.totalFailed++;
    emitTaskEvent(dir, "task.failed", {
      taskId: task.id,
      dispatchId,
      reason,
      details: { maxRetries },
      message: `Max retries (${maxRetries}) exceeded for "${task.title}": ${reason}`
    });
    appendEvent(dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "error",
      taskId: task.id,
      dispatchId: dispatchId ?? void 0,
      message: `Max retries (${maxRetries}) exceeded for "${task.title}": ${reason}`
    });
    return false;
  }
  const backoffMs = Math.min(1e4 * Math.pow(2, task.retryCount), 3e5);
  const nextRetryAt = new Date(Date.now() + backoffMs).toISOString();
  task.retryCount += 1;
  task.lastError = reason;
  task.nextRetryAt = nextRetryAt;
  task.status = "todo";
  task.assignee = null;
  task.updated = (/* @__PURE__ */ new Date()).toISOString();
  clearTaskDispatch(task);
  saveTask(dir, task);
  state.claimedTasks.delete(task.id);
  state.inflightDispatches.delete(task.id);
  state.taskClaimTimes.delete(task.id);
  state.stallNudges.delete(task.id);
  emitTaskEvent(dir, "task.retried", {
    taskId: task.id,
    dispatchId,
    reason,
    details: { attempt: task.retryCount, maxRetries, nextRetryAt, backoffMs },
    message: `Retry ${task.retryCount}/${maxRetries} scheduled for "${task.title}" in ${backoffMs / 1e3}s: ${reason}`
  });
  appendEvent(dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "retry",
    taskId: task.id,
    dispatchId: dispatchId ?? void 0,
    message: `Retry ${task.retryCount}/${maxRetries} scheduled for "${task.title}" in ${backoffMs / 1e3}s: ${reason}`
  });
  return true;
}
function reconcile(config2, state, tasks, panes) {
  const agentIds = new Set(panes.map((p) => agentIdentifier(p)));
  const taskIds = new Set(tasks.map((task) => task.id));
  let orphans = 0;
  let dropped = 0;
  for (const [taskId] of state.inflightDispatches) {
    if (!taskIds.has(taskId)) {
      state.inflightDispatches.delete(taskId);
      state.claimedTasks.delete(taskId);
      state.taskClaimTimes.delete(taskId);
      state.stallNudges.delete(taskId);
      dropped++;
    }
  }
  for (const task of tasks.filter((t) => t.status === "in-progress" && t.assignee)) {
    const dispatchId = taskDispatchId(task);
    const inflight = state.inflightDispatches.get(task.id);
    if (!dispatchId) {
      orphans++;
      const agent = task.assignee;
      const agentMissing = agent ? !agentIds.has(agent) : false;
      const reason = agentMissing ? `Agent "${agent}" vanished (pane closed or crashed); task also lacked dispatchId` : `Task "${task.title}" is in-progress without dispatchId`;
      scheduleRetry(config2.dir, state, task, reason);
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "reconcile",
        taskId: task.id,
        agent: agent ?? void 0,
        message: agentMissing ? `Agent "${agent}" vanished, scheduled retry for "${task.title}"` : `Orphaned task "${task.title}" missing dispatchId, scheduled retry`
      });
      continue;
    }
    if (!inflight) {
      const assignee = task.assignee;
      if (!assignee) continue;
      const pane = panes.find((p) => agentIdentifier(p) === assignee);
      if (pane) {
        state.inflightDispatches.set(task.id, {
          taskId: task.id,
          dispatchId,
          agentPane: pane.id,
          agentName: assignee,
          at: task.dispatchAt ?? task.updated
        });
        state.claimedTasks.add(task.id);
      } else {
        orphans++;
      }
    }
    if (!agentIds.has(task.assignee)) {
      const agent = task.assignee;
      const reason = `Agent "${agent}" vanished (pane closed or crashed)`;
      scheduleRetry(config2.dir, state, task, reason);
      appendEvent(config2.dir, {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        type: "reconcile",
        taskId: task.id,
        agent,
        dispatchId,
        message: `Agent "${agent}" vanished, scheduled retry for "${task.title}"`
      });
    }
  }
  if (orphans > 0 || dropped > 0) {
    const at = (/* @__PURE__ */ new Date()).toISOString();
    appendEvent(config2.dir, {
      timestamp: at,
      type: "orchestrator.reconciled",
      at,
      orphans,
      dropped,
      message: `Orchestrator reconciled drift: ${orphans} orphan(s), ${dropped} dropped dispatch(es)`
    });
  }
}
function stateFilePath(dir) {
  return join14(dir, ".tasks", "orchestrator-state.json");
}
function saveOrchestratorState(dir, state) {
  const tasksDir = join14(dir, ".tasks");
  if (!existsSync16(tasksDir)) mkdirSync13(tasksDir, { recursive: true });
  const data = {
    claimedTasks: [...state.claimedTasks],
    taskClaimTimes: Object.fromEntries(state.taskClaimTimes),
    lastActivity: Object.fromEntries(state.lastActivity),
    inflightDispatches: [...state.inflightDispatches.values()],
    completedDispatches: [...state.completedDispatches],
    stallNudges: Object.fromEntries(state.stallNudges)
  };
  const filePath = stateFilePath(dir);
  const tmpPath = filePath + ".tmp";
  writeFileSync13(tmpPath, JSON.stringify(data, null, 2) + "\n");
  renameSync10(tmpPath, filePath);
}
function loadOrchestratorState(dir, state) {
  const path4 = stateFilePath(dir);
  if (!existsSync16(path4)) return;
  try {
    const data = JSON.parse(readFileSync14(path4, "utf-8"));
    if (Array.isArray(data.claimedTasks)) {
      for (const id of data.claimedTasks) state.claimedTasks.add(id);
    }
    if (data.taskClaimTimes && typeof data.taskClaimTimes === "object") {
      for (const [id, time] of Object.entries(data.taskClaimTimes)) {
        state.taskClaimTimes.set(id, time);
      }
    }
    if (data.lastActivity && typeof data.lastActivity === "object") {
      for (const [id, time] of Object.entries(data.lastActivity)) {
        state.lastActivity.set(id, time);
      }
    }
    if (Array.isArray(data.inflightDispatches)) {
      for (const record of data.inflightDispatches) {
        state.inflightDispatches.set(record.taskId, record);
      }
    }
    if (Array.isArray(data.completedDispatches)) {
      for (const id of data.completedDispatches) state.completedDispatches.add(id);
    }
    if (data.stallNudges && typeof data.stallNudges === "object") {
      for (const [id, time] of Object.entries(data.stallNudges)) {
        state.stallNudges.set(id, time);
      }
    }
  } catch {
  }
}
function syncClaims(dir, state) {
  const tasks = loadTasks(dir);
  const inProgressIds = new Set(tasks.filter((t) => t.status === "in-progress").map((t) => t.id));
  for (const id of state.claimedTasks) {
    if (!inProgressIds.has(id)) {
      state.claimedTasks.delete(id);
      state.inflightDispatches.delete(id);
      state.taskClaimTimes.delete(id);
      state.stallNudges.delete(id);
    }
  }
  for (const id of inProgressIds) {
    state.claimedTasks.add(id);
  }
}
function gracefulShutdown(config2, state) {
  const tasks = loadTasks(config2.dir);
  for (const task of tasks) {
    if (task.status === "in-progress" && task.assignee) {
      task.assignee = null;
      task.status = "todo";
      task.updated = (/* @__PURE__ */ new Date()).toISOString();
      clearTaskDispatch(task);
      saveTask(config2.dir, task);
    }
  }
  appendEvent(config2.dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "session_end",
    message: `Orchestrator shutting down`
  });
  try {
    computeAndSaveMetrics(config2.dir);
  } catch {
  }
  saveOrchestratorState(config2.dir, state);
}
function reloadConfig(target, patch) {
  if (patch.pollInterval !== void 0) target.pollInterval = patch.pollInterval;
  if (patch.stallTimeout !== void 0) target.stallTimeout = patch.stallTimeout;
  if (patch.maxConcurrentAgents !== void 0)
    target.maxConcurrentAgents = patch.maxConcurrentAgents;
  if (patch.autoDispatch !== void 0) target.autoDispatch = patch.autoDispatch;
  if (patch.beforeRun !== void 0) target.beforeRun = patch.beforeRun;
  if (patch.afterRun !== void 0) target.afterRun = patch.afterRun;
  if (patch.research !== void 0) target.research = patch.research;
  if (patch.dryRun !== void 0) target.dryRun = patch.dryRun;
}
function createOrchestrator(initialConfig) {
  const config2 = {
    ...initialConfig,
    dryRun: initialConfig.dryRun ?? process.env.TMUX_IDE_ORCHESTRATOR_DRY_RUN === "1"
  };
  const state = {
    lastActivity: /* @__PURE__ */ new Map(),
    previousTasks: /* @__PURE__ */ new Map(),
    claimedTasks: /* @__PURE__ */ new Set(),
    taskClaimTimes: /* @__PURE__ */ new Map(),
    inflightDispatches: /* @__PURE__ */ new Map(),
    completedDispatches: /* @__PURE__ */ new Set(),
    stallNudges: /* @__PURE__ */ new Map(),
    totalDispatched: 0,
    totalCompleted: 0,
    totalFailed: 0,
    lastTickMs: 0,
    ticking: false,
    idleAgents: 0,
    queuedDispatches: 0,
    lastError: null,
    lastReconcileMs: 0
  };
  const researchState = loadResearchState(config2.dir);
  loadOrchestratorState(config2.dir, state);
  syncClaims(config2.dir, state);
  for (const task of loadTasks(config2.dir)) {
    state.previousTasks.set(task.id, task.status);
  }
  appendEvent(config2.dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "session_start",
    message: `Orchestrator started \u2014 mode: ${config2.dispatchMode}, max agents: ${config2.maxConcurrentAgents}`
  });
  let tickCount = 0;
  healthRegistry.set(config2.session, () => getHealth(state));
  function tick() {
    const started = Date.now();
    state.ticking = true;
    try {
      tickCount++;
      let tasks = loadTasks(config2.dir);
      const panes = listSessionPanes(config2.session);
      if (state.lastReconcileMs === 0 || started - state.lastReconcileMs >= RECONCILE_INTERVAL_MS) {
        reconcile(config2, state, tasks, panes);
        state.lastReconcileMs = started;
        tasks = loadTasks(config2.dir);
      }
      if (config2.autoDispatch) {
        if (config2.dispatchMode === "goals") {
          const goals = loadGoals(config2.dir);
          dispatchGoals(config2, state, goals, tasks, panes);
        } else if (config2.dispatchMode === "missions") {
          const mission = loadMission(config2.dir);
          if (mission?.status === "planning" && mission.milestones.length === 0) {
            dispatchPlanning(config2, state, panes);
          } else if (mission?.status === "active") {
            dispatch(config2, state, tasks, panes);
          }
        } else {
          dispatch(config2, state, tasks, panes);
        }
      }
      detectStalls(config2, state, tasks, panes, researchState);
      detectCompletions(config2, state, tasks, panes, researchState);
      tasks = loadTasks(config2.dir);
      if (config2.dispatchMode === "missions" && config2.research?.enabled) {
        const triggers = evaluateTriggers(
          config2,
          state,
          researchState,
          tasks,
          readEvents(config2.dir)
        );
        if (triggers.length > 0) {
          dispatchResearch(config2, state, researchState, tasks, panes, triggers[0]);
          tasks = loadTasks(config2.dir);
        }
      }
      if (config2.dispatchMode === "missions") {
        checkMilestoneCompletion(config2, state, tasks, panes);
        checkValidationResults(config2, tasks);
        const mission = loadMission(config2.dir);
        if (mission?.status === "active" && mission.milestones.length > 0) {
          const allMilestonesDone = mission.milestones.every((m) => m.status === "done");
          if (allMilestonesDone) {
            handleMissionComplete(config2, mission, panes);
          }
        }
      }
      if (tickCount % 6 === 0) {
        const agentPanes = panes.filter((p) => {
          if (p.role === "lead") return false;
          return isAgentPane(p);
        });
        if (agentPanes.length > 0) {
          const statuses = agentPanes.map((p) => `${agentIdentifier(p)}=${isAgentBusy(p) ? "busy" : "idle"}`).join(", ");
          appendEvent(config2.dir, {
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            type: "agent_heartbeat",
            message: `agents: ${statuses}`
          });
        }
      }
      for (const task of tasks) {
        state.previousTasks.set(task.id, task.status);
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      state.lastError = { message, at: (/* @__PURE__ */ new Date()).toISOString() };
      appendEvent(config2.dir, {
        timestamp: state.lastError.at,
        type: "error",
        message: `Orchestrator tick failed: ${message}`
      });
    } finally {
      state.lastTickMs = Date.now() - started;
      state.ticking = false;
    }
  }
  let interval = setInterval(tick, config2.pollInterval);
  let watcher = null;
  const configPath = join14(config2.dir, "ide.yml");
  if (existsSync16(configPath)) {
    let debounce = null;
    watcher = watch(configPath, () => {
      if (debounce) clearTimeout(debounce);
      debounce = setTimeout(() => {
        try {
          const raw = readFileSync14(configPath, "utf-8");
          const parsed = yaml3.load(raw);
          const orch = parsed.orchestrator ?? {};
          const oldPollInterval = config2.pollInterval;
          reloadConfig(config2, {
            pollInterval: orch.poll_interval ?? config2.pollInterval,
            stallTimeout: orch.stall_timeout ?? config2.stallTimeout,
            maxConcurrentAgents: orch.max_concurrent_agents ?? config2.maxConcurrentAgents,
            autoDispatch: orch.auto_dispatch ?? config2.autoDispatch,
            beforeRun: orch.before_run ?? config2.beforeRun,
            afterRun: orch.after_run ?? config2.afterRun,
            research: orch.research ?? config2.research,
            dryRun: orch.dry_run ?? (process.env.TMUX_IDE_ORCHESTRATOR_DRY_RUN === "1" ? true : config2.dryRun)
          });
          if (config2.pollInterval !== oldPollInterval) {
            clearInterval(interval);
            interval = setInterval(tick, config2.pollInterval);
          }
        } catch {
        }
      }, 300);
    });
  }
  function onSignal() {
    clearInterval(interval);
    if (watcher) watcher.close();
    healthRegistry.delete(config2.session);
    gracefulShutdown(config2, state);
    saveResearchState(config2.dir, researchState);
    process.exit(0);
  }
  process.on("SIGTERM", onSignal);
  process.on("SIGINT", onSignal);
  return () => {
    clearInterval(interval);
    if (watcher) watcher.close();
    healthRegistry.delete(config2.session);
    saveResearchState(config2.dir, researchState);
    process.removeListener("SIGTERM", onSignal);
    process.removeListener("SIGINT", onSignal);
  };
}
var STALL_NUDGE_COOLDOWN_MS, RECONCILE_INTERVAL_MS, healthRegistry, SHELL_COMMANDS3, AGENT_PREFIXES2, SPINNERS3, VERSION_PATTERN2, paneContentCache, paneContentHashHits, paneContentHashMisses, AGENT_NAMES, PLANNING_CLAIM;
var init_orchestrator = __esm({
  "packages/daemon/src/lib/orchestrator.ts"() {
    "use strict";
    init_task_store();
    init_slugify();
    init_token_tracker();
    init_pane_comms();
    init_event_log();
    init_skill_registry();
    init_github_pr();
    init_metrics();
    init_validation();
    init_research();
    STALL_NUDGE_COOLDOWN_MS = 3e4;
    RECONCILE_INTERVAL_MS = 6e4;
    healthRegistry = /* @__PURE__ */ new Map();
    SHELL_COMMANDS3 = /* @__PURE__ */ new Set(["zsh", "bash", "sh", "fish"]);
    AGENT_PREFIXES2 = ["claude", "codex"];
    SPINNERS3 = /^[⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏⠂⠒⠢⠆⠐⠠⠄◐◓◑◒✳|/\\-] /;
    VERSION_PATTERN2 = /^\d+\.\d+/;
    paneContentCache = /* @__PURE__ */ new Map();
    paneContentHashHits = 0;
    paneContentHashMisses = 0;
    AGENT_NAMES = [
      "Fran\xE7ois",
      "Am\xE9lie",
      "Louis",
      "Camille",
      "Marcel",
      "Colette",
      "Henri",
      "Margaux",
      "Ren\xE9",
      "L\xE9onie",
      "\xC9tienne",
      "Fleur",
      "Gaston",
      "Isabelle",
      "Jacques",
      "Lucienne",
      "Nicolas",
      "Odette",
      "Pierre",
      "Rosalie"
    ];
    PLANNING_CLAIM = "__planning__";
  }
});

// packages/daemon/src/command-center/discovery.ts
import { execFileSync as execFileSync5 } from "node:child_process";
import { existsSync as existsSync17 } from "node:fs";
import { join as join15 } from "node:path";
function tmuxSilent(args) {
  try {
    return _tmuxRunner(args);
  } catch {
    return "";
  }
}
function listTmuxSessions() {
  const raw = tmuxSilent(["list-sessions", "-F", "#{session_name}"]);
  if (!raw) return [];
  return raw.split("\n").filter(Boolean);
}
function getSessionCwd2(session) {
  return tmuxSilent(["display-message", "-t", session, "-p", "#{pane_current_path}"]);
}
function formatElapsed(isoDate) {
  const ms = Date.now() - new Date(isoDate).getTime();
  const mins = Math.floor(ms / 6e4);
  if (mins < 1) return "<1m";
  if (mins < 60) return `${mins}m`;
  return `${Math.floor(mins / 60)}h ${mins % 60}m`;
}
function discoverSessions() {
  const sessionNames = listTmuxSessions();
  const results = [];
  const registry = getDefaultWorkspaceRegistry();
  const enforceRegistry = registry._isLoaded();
  for (const name of sessionNames) {
    if (enforceRegistry && !registry.has(name)) continue;
    const dir = getSessionCwd2(name);
    if (!dir) continue;
    const tasksDir = join15(dir, ".tasks");
    const hasTasks = existsSync17(tasksDir);
    const mission = hasTasks ? loadMission(dir) : null;
    const goals = hasTasks ? loadGoals(dir) : [];
    const tasks = hasTasks ? loadTasks(dir) : [];
    let panes = [];
    try {
      panes = listSessionPanes(name);
    } catch {
    }
    results.push({ name, dir, mission, goals, tasks, panes });
  }
  return results;
}
function computeStats(info) {
  const agentPanes = info.panes.filter((p) => isAgentPane(p));
  const activeAgents = agentPanes.filter((p) => isAgentBusy(p)).length;
  return {
    totalTasks: info.tasks.length,
    doneTasks: info.tasks.filter((t) => t.status === "done").length,
    agents: agentPanes.length,
    activeAgents
  };
}
function computeGoalProgress(goals, tasks) {
  return goals.map((g) => {
    const goalTasks = tasks.filter((t) => t.goal === g.id);
    const done = goalTasks.filter((t) => t.status === "done").length;
    const progress = goalTasks.length > 0 ? Math.round(done / goalTasks.length * 100) : 0;
    return { id: g.id, title: g.title, progress };
  });
}
function buildOverviews(sessions) {
  return sessions.map((s) => ({
    name: s.name,
    dir: s.dir,
    mission: s.mission,
    stats: computeStats(s),
    goals: computeGoalProgress(s.goals, s.tasks)
  }));
}
function buildProjectDetail(info) {
  const agents = info.panes.filter((p) => isAgentPane(p)).map((pane) => {
    const name = agentIdentifier(pane);
    const assignedTask = info.tasks.find(
      (t) => t.assignee === name && t.status === "in-progress"
    );
    return {
      paneTitle: name,
      paneId: pane.id,
      isBusy: isAgentBusy(pane),
      taskTitle: assignedTask?.title ?? null,
      taskId: assignedTask?.id ?? null,
      elapsed: assignedTask ? formatElapsed(assignedTask.updated) : ""
    };
  });
  const valState = loadValidationState(info.dir);
  const assertions = valState ? Object.values(valState.assertions) : [];
  const validationSummary2 = {
    total: assertions.length,
    passing: assertions.filter((a) => a.status === "passing").length,
    failing: assertions.filter((a) => a.status === "failing").length,
    pending: assertions.filter((a) => a.status === "pending").length,
    blocked: assertions.filter((a) => a.status === "blocked").length
  };
  const skills = loadSkills(info.dir).map((s) => ({
    name: s.name,
    specialties: s.specialties
  }));
  return {
    session: info.name,
    dir: info.dir,
    mission: info.mission,
    goals: info.goals,
    tasks: info.tasks,
    agents,
    milestones: info.mission?.milestones ?? [],
    validationSummary: validationSummary2,
    skills
  };
}
function buildOrchestratorSnapshot(info) {
  const agents = info.panes.filter((p) => isAgentPane(p)).map((pane) => {
    const name = agentIdentifier(pane);
    const assignedTask = info.tasks.find(
      (t) => t.assignee === name && t.status === "in-progress"
    );
    return {
      paneTitle: name,
      paneId: pane.id,
      isBusy: isAgentBusy(pane),
      taskTitle: assignedTask?.title ?? null,
      taskId: assignedTask?.id ?? null,
      elapsed: assignedTask ? formatElapsed(assignedTask.updated) : ""
    };
  });
  return {
    running: agents.length > 0,
    claimedCount: info.tasks.filter((t) => t.status === "in-progress").length,
    agents,
    inProgressCount: info.tasks.filter((t) => t.status === "in-progress").length,
    pendingCount: info.tasks.filter((t) => t.status === "todo").length
  };
}
var _tmuxRunner;
var init_discovery = __esm({
  "packages/daemon/src/command-center/discovery.ts"() {
    "use strict";
    init_task_store();
    init_pane_comms();
    init_workspace_registry();
    init_orchestrator();
    init_validation();
    init_skill_registry();
    _tmuxRunner = (args) => execFileSync5("tmux", args, {
      encoding: "utf-8",
      stdio: ["ignore", "pipe", "ignore"]
    }).trim();
  }
});

// packages/daemon/src/command-center/fs-watch.ts
import parcelWatcher2 from "@parcel/watcher";
import { realpathSync as realpathSync2 } from "node:fs";
import { relative as relative2 } from "node:path";
function buildRelativePath(resolvedRoot, absPath) {
  const rel = relative2(resolvedRoot, absPath);
  if (!rel || rel.startsWith("..")) return null;
  return rel.split("\\").join("/");
}
function subscribeFsChanges(sessionDir, listener) {
  let resolvedRoot;
  try {
    resolvedRoot = realpathSync2(sessionDir);
  } catch {
    return () => {
    };
  }
  let entry = watchers.get(resolvedRoot);
  if (!entry) {
    entry = { subscription: null, closed: false, resolvedRoot, listeners: /* @__PURE__ */ new Set() };
    watchers.set(resolvedRoot, entry);
    const fire = (kind, abs) => {
      const localEntry = watchers.get(resolvedRoot);
      if (!localEntry) return;
      const rel = buildRelativePath(resolvedRoot, abs);
      if (!rel) return;
      const event = { path: rel, kind };
      for (const l of localEntry.listeners) {
        try {
          l(event);
        } catch {
        }
      }
    };
    void parcelWatcher2.subscribe(
      resolvedRoot,
      (err, events) => {
        if (err) return;
        for (const e of events) {
          if (e.type === "delete") fire("delete", e.path);
          else fire("modify", e.path);
        }
      },
      { ignore: WATCH_IGNORE_GLOBS2 }
    ).then((sub) => {
      const current = watchers.get(resolvedRoot);
      if (!current || current.closed) {
        void sub.unsubscribe().catch(() => {
        });
        return;
      }
      current.subscription = sub;
    }).catch(() => {
    });
  }
  entry.listeners.add(listener);
  return () => {
    const current = watchers.get(resolvedRoot);
    if (!current) return;
    current.listeners.delete(listener);
    if (current.listeners.size === 0) {
      current.closed = true;
      const sub = current.subscription;
      watchers.delete(resolvedRoot);
      if (sub) void sub.unsubscribe().catch(() => {
      });
    }
  };
}
var watchers, WATCH_IGNORED_NAMES2, WATCH_IGNORE_GLOBS2;
var init_fs_watch = __esm({
  "packages/daemon/src/command-center/fs-watch.ts"() {
    "use strict";
    watchers = /* @__PURE__ */ new Map();
    WATCH_IGNORED_NAMES2 = [
      ".svn",
      ".hg",
      ".git",
      "node_modules",
      "dist",
      "build",
      "out",
      "release",
      ".next",
      ".nuxt",
      ".turbo",
      ".cache",
      ".parcel-cache",
      ".vite",
      ".svelte-kit",
      ".output",
      ".expo",
      "coverage",
      "__pycache__",
      ".pytest_cache",
      "venv",
      ".venv",
      "target",
      ".terraform",
      ".serverless",
      "worktrees",
      ".tmux-ide",
      ".tasks",
      ".claude",
      ".cursor",
      ".aider",
      ".continue",
      "context"
    ];
    WATCH_IGNORE_GLOBS2 = WATCH_IGNORED_NAMES2.map((n) => `**/${n}/**`);
  }
});

// packages/daemon/src/schemas/registry.ts
import { z as z16 } from "zod";
var RegisteredProjectSchemaZ, RegisterProjectRequestSchemaZ, InitProjectRequestSchemaZ, ProjectTemplateSchemaZ;
var init_registry = __esm({
  "packages/daemon/src/schemas/registry.ts"() {
    "use strict";
    RegisteredProjectSchemaZ = z16.object({
      /** Unique registry key. Defaults to `basename(dir)`; collisions resolved by appending `-2`, `-3`, … */
      name: z16.string(),
      /** Absolute path to the project directory. */
      dir: z16.string(),
      /** Whether `<dir>/ide.yml` exists; refreshed on register and on `probe()`. */
      hasIdeYml: z16.boolean(),
      /** Git remote origin URL, or `null` if not a git repo / no origin / probe failed. */
      gitOrigin: z16.string().nullable(),
      /** Current git branch, or `null` if not a git repo / detached HEAD / probe failed. */
      gitBranch: z16.string().nullable(),
      /** ISO-8601 timestamp the project was first registered. */
      registeredAt: z16.string()
    });
    RegisterProjectRequestSchemaZ = z16.object({
      dir: z16.string().min(1),
      name: z16.string().min(1).optional()
    });
    InitProjectRequestSchemaZ = z16.object({
      dir: z16.string().min(1),
      template: z16.string().min(1).optional()
    });
    ProjectTemplateSchemaZ = z16.object({
      id: z16.string(),
      label: z16.string(),
      description: z16.string()
    });
  }
});

// packages/daemon/src/lib/project-probe.ts
import { execFile } from "node:child_process";
import { existsSync as existsSync18 } from "node:fs";
import { basename as basename7, isAbsolute, resolve as resolve14 } from "node:path";
function sanitizeName(raw) {
  return raw.trim().replace(/\s+/g, "-").replace(/[^A-Za-z0-9._-]/g, "").replace(/^-+|-+$/g, "");
}
async function probeProject(dir, io = realIo) {
  const absoluteDir = isAbsolute(dir) ? dir : resolve14(dir);
  const rawName = basename7(absoluteDir);
  const sanitized = sanitizeName(rawName);
  const name = sanitized.length > 0 ? sanitized : "project";
  const hasIdeYml = io.exists(`${absoluteDir}/ide.yml`);
  const [gitOrigin, gitBranch] = await Promise.all([
    io.runGit(["config", "--get", "remote.origin.url"], absoluteDir),
    io.runGit(["branch", "--show-current"], absoluteDir)
  ]);
  return {
    name,
    dir: absoluteDir,
    hasIdeYml,
    // Treat empty string as null — branch --show-current returns "" on a
    // detached HEAD.
    gitOrigin: gitOrigin && gitOrigin.length > 0 ? gitOrigin : null,
    gitBranch: gitBranch && gitBranch.length > 0 ? gitBranch : null
  };
}
var GIT_TIMEOUT_MS, realIo;
var init_project_probe = __esm({
  "packages/daemon/src/lib/project-probe.ts"() {
    "use strict";
    GIT_TIMEOUT_MS = 2e3;
    realIo = {
      exists: existsSync18,
      runGit: (args, cwd) => new Promise((resolveResult) => {
        execFile(
          "git",
          ["-C", cwd, ...args],
          { timeout: GIT_TIMEOUT_MS, encoding: "utf-8" },
          (err, stdout) => {
            if (err) {
              resolveResult(null);
              return;
            }
            resolveResult(stdout.trim());
          }
        );
      })
    };
  }
});

// packages/daemon/src/lib/project-registry.ts
import { EventEmitter as EventEmitter5 } from "node:events";
import { existsSync as existsSync19, mkdirSync as mkdirSync14, readFileSync as readFileSync15, renameSync as renameSync11, writeFileSync as writeFileSync14 } from "node:fs";
import { homedir as homedir4 } from "node:os";
import { dirname as dirname7, isAbsolute as isAbsolute2, join as join16, resolve as resolve15 } from "node:path";
import { z as z17 } from "zod";
function applyAction(state, action) {
  switch (action.type) {
    case "register":
      return [...state, action.project];
    case "unregister":
      return state.filter((p) => p.name !== action.name);
    case "replace":
      return state.map((p) => p.name === action.project.name ? action.project : p);
  }
}
function resolveUniqueName(state, desired) {
  const used = new Set(state.map((p) => p.name));
  if (!used.has(desired)) return desired;
  let counter = 2;
  while (used.has(`${desired}-${counter}`)) counter++;
  return `${desired}-${counter}`;
}
function buildRegisteredProject(probe, name, registeredAt) {
  return {
    name,
    dir: probe.dir,
    hasIdeYml: probe.hasIdeYml,
    gitOrigin: probe.gitOrigin,
    gitBranch: probe.gitBranch,
    registeredAt
  };
}
function registryDir() {
  const override = process.env[REGISTRY_DIR_ENV3];
  if (override && override.length > 0) return override;
  return join16(homedir4(), ".tmux-ide");
}
function registryPath() {
  return join16(registryDir(), "projects.json");
}
function readDisk() {
  const path4 = registryPath();
  if (!existsSync19(path4)) return [];
  const raw = readFileSync15(path4, "utf-8");
  if (raw.trim().length === 0) return [];
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    console.warn("[project-registry] %s contains invalid JSON; ignoring", path4);
    return [];
  }
  const result = RegistryFileSchemaZ2.safeParse(parsed);
  if (!result.success) {
    console.warn(
      "[project-registry] %s failed schema validation; ignoring (%s)",
      path4,
      result.error.issues.slice(0, 3).map((i) => `${i.path.join(".")}: ${i.message}`).join("; ")
    );
    return [];
  }
  return result.data.projects;
}
function writeDisk(projects) {
  const path4 = registryPath();
  const dir = dirname7(path4);
  mkdirSync14(dir, { recursive: true });
  const file = { version: 1, projects };
  const tmpPath = `${path4}.tmp`;
  writeFileSync14(tmpPath, JSON.stringify(file, null, 2) + "\n");
  renameSync11(tmpPath, path4);
}
function ensureCache() {
  if (cache !== null) return cache;
  cache = readDisk();
  return cache;
}
function commit(next) {
  cache = next;
  writeDisk(next);
  projectRegistryEmitter.emit("change");
}
function listProjects() {
  return [...ensureCache()];
}
function getProject(name) {
  return ensureCache().find((p) => p.name === name) ?? null;
}
async function registerProject(input) {
  const exists = input.exists ?? existsSync19;
  const absoluteDir = isAbsolute2(input.dir) ? input.dir : resolve15(input.dir);
  if (!exists(absoluteDir)) {
    throw new ProjectDirNotFoundError(absoluteDir);
  }
  const probe = await probeProject(absoluteDir, input.io);
  const state = ensureCache();
  const desired = input.name ? sanitizeName(input.name) : probe.name;
  const cleaned = desired.length > 0 ? desired : probe.name;
  let resolvedName;
  if (input.name) {
    if (state.some((p) => p.name === cleaned)) {
      throw new ProjectAlreadyRegisteredError(cleaned, resolveUniqueName(state, cleaned));
    }
    resolvedName = cleaned;
  } else {
    resolvedName = resolveUniqueName(state, cleaned);
  }
  const dupDir = state.find((p) => p.dir === probe.dir);
  if (dupDir) {
    throw new ProjectAlreadyRegisteredError(dupDir.name, dupDir.name);
  }
  const now = (input.now ?? (() => /* @__PURE__ */ new Date()))();
  const project = buildRegisteredProject(probe, resolvedName, now.toISOString());
  commit(applyAction(state, { type: "register", project }));
  return project;
}
function unregisterProject(name) {
  const state = ensureCache();
  if (!state.some((p) => p.name === name)) {
    throw new ProjectNotFoundError(name);
  }
  commit(applyAction(state, { type: "unregister", name }));
}
async function refreshProject(name, options = {}) {
  const state = ensureCache();
  const existing = state.find((p) => p.name === name);
  if (!existing) throw new ProjectNotFoundError(name);
  const probe = await probeProject(existing.dir, options.io);
  const refreshed = buildRegisteredProject(probe, existing.name, existing.registeredAt);
  commit(applyAction(state, { type: "replace", project: refreshed }));
  return refreshed;
}
var REGISTRY_DIR_ENV3, RegistryFileSchemaZ2, ProjectRegistryError, ProjectAlreadyRegisteredError, ProjectNotFoundError, ProjectDirNotFoundError, projectRegistryEmitter, cache;
var init_project_registry = __esm({
  "packages/daemon/src/lib/project-registry.ts"() {
    "use strict";
    init_registry();
    init_project_probe();
    REGISTRY_DIR_ENV3 = "TMUX_IDE_REGISTRY_DIR";
    RegistryFileSchemaZ2 = z17.object({
      version: z17.literal(1),
      projects: z17.array(RegisteredProjectSchemaZ)
    });
    ProjectRegistryError = class extends Error {
      code;
      constructor(message, code) {
        super(message);
        this.name = "ProjectRegistryError";
        this.code = code;
      }
    };
    ProjectAlreadyRegisteredError = class extends ProjectRegistryError {
      suggestion;
      constructor(name, suggestion) {
        super(`Project "${name}" is already registered`, "ALREADY_REGISTERED");
        this.name = "ProjectAlreadyRegisteredError";
        this.suggestion = suggestion;
      }
    };
    ProjectNotFoundError = class extends ProjectRegistryError {
      constructor(name) {
        super(`Project "${name}" not found in registry`, "NOT_FOUND");
        this.name = "ProjectNotFoundError";
      }
    };
    ProjectDirNotFoundError = class extends ProjectRegistryError {
      constructor(dir) {
        super(`Directory "${dir}" does not exist`, "DIR_NOT_FOUND");
        this.name = "ProjectDirNotFoundError";
      }
    };
    projectRegistryEmitter = new EventEmitter5();
    projectRegistryEmitter.setMaxListeners(0);
    cache = null;
  }
});

// packages/daemon/src/command-center/ws-events.ts
function isChatTimelineFrame(event) {
  return event.type === "chat.timeline.upsert" || event.type === "chat.timeline.reset";
}
function recordChatTimelineFrame(event) {
  if (!isChatTimelineFrame(event)) return;
  const threadId = event.threadId;
  const seq = (chatSeqByThread.get(threadId) ?? 0) + 1;
  chatSeqByThread.set(threadId, seq);
  event.seq = seq;
  let buf = chatReplayByThread.get(threadId);
  if (!buf) {
    buf = [];
    chatReplayByThread.set(threadId, buf);
  }
  if (event.type === "chat.timeline.reset") buf.length = 0;
  buf.push({ seq, frame: event });
  if (buf.length > CHAT_REPLAY_CAP_PER_THREAD) {
    buf.splice(0, buf.length - CHAT_REPLAY_CAP_PER_THREAD);
  }
}
function replayChatTimelineSince(threadId, lastSeq, send2) {
  const buf = chatReplayByThread.get(threadId);
  if (!buf) return;
  for (const entry of buf) {
    if (entry.seq > lastSeq) send2(entry.frame);
  }
}
function snapshotSessionsHash() {
  try {
    return JSON.stringify(
      discoverSessions().map((s) => s.name).sort()
    );
  } catch {
    return "";
  }
}
function ensureSessionsPoller() {
  if (sessionsPollTimer) return;
  lastSessionsHash = snapshotSessionsHash();
  sessionsPollTimer = setInterval(() => {
    const hash = snapshotSessionsHash();
    if (hash === lastSessionsHash) return;
    lastSessionsHash = hash;
    for (const client of allClients) client.broadcastSessionsChanged();
  }, SESSIONS_POLL_MS);
  sessionsPollTimer.unref?.();
}
function maybeStopSessionsPoller() {
  if (allClients.size > 0 || !sessionsPollTimer) return;
  clearInterval(sessionsPollTimer);
  sessionsPollTimer = null;
}
function ensureProjectRegistryListener() {
  if (projectRegistryListener) return;
  const listener = () => {
    for (const client of allClients) client.broadcastProjectsChanged();
  };
  projectRegistryListener = listener;
  projectRegistryEmitter.on("change", listener);
}
function maybeStopProjectRegistryListener() {
  if (allClients.size > 0 || !projectRegistryListener) return;
  projectRegistryEmitter.off("change", projectRegistryListener);
  projectRegistryListener = null;
}
function broadcastInitOutput(jobId, chunk, done) {
  for (const client of allClients) client.broadcastInitOutput(jobId, chunk, done);
}
function broadcastInitError(jobId, message) {
  for (const client of allClients) client.broadcastInitError(jobId, message);
}
function broadcastActionComplete(name, result) {
  for (const client of allClients) client.broadcastActionComplete(name, result);
}
function broadcastSkillsChanged(sessionName) {
  for (const client of allClients) client.broadcastSkillsChanged(sessionName);
}
function broadcastValidationChanged(sessionName) {
  for (const client of allClients) client.broadcastValidationChanged(sessionName);
}
function broadcastConfigChanged(sessionName) {
  for (const client of allClients) client.broadcastConfigChanged(sessionName);
}
function broadcastTerminalsChanged(sessionName) {
  for (const client of allClients) client.broadcastTerminalsChanged(sessionName);
}
function broadcastChatEvent(event) {
  recordChatTimelineFrame(event);
  for (const client of allClients) client.broadcastChatEvent(event);
}
function rawDataToText2(data) {
  if (typeof data === "string") return data;
  if (Buffer.isBuffer(data)) return data.toString("utf8");
  if (data instanceof ArrayBuffer) return Buffer.from(data).toString("utf8");
  if (Array.isArray(data)) return Buffer.concat(data).toString("utf8");
  return Buffer.from(data).toString("utf8");
}
function isPathInside(path4, root) {
  if (!path4) return false;
  return path4 === root || path4.startsWith(root + "/");
}
function classifyTaskStorePath(change, sessionDir) {
  const path4 = change.path;
  if (!path4) return null;
  if (!isPathInside(path4, sessionDir)) return null;
  if (change.schemaName === "task") return "task";
  if (change.schemaName === "goal") return "goal";
  if (change.schemaName === "mission") return "mission";
  if (path4.includes("/.tasks/tasks/")) return "task";
  if (path4.includes("/.tasks/goals/")) return "goal";
  if (path4.endsWith("/.tasks/mission.json")) return "mission";
  return null;
}
function buildSessionSnapshot(sessionName) {
  const session = discoverSessions().find((s) => s.name === sessionName);
  if (!session) return null;
  const project = buildProjectDetail(session);
  const mission = loadMission(session.dir);
  const tasks = loadTasks(session.dir);
  const milestones = mission ? [...mission.milestones].sort((a, b) => a.order - b.order).map((milestone) => {
    const milestoneTasks = tasks.filter((t) => t.milestone === milestone.id);
    return {
      ...milestone,
      taskCount: milestoneTasks.length,
      tasksDone: milestoneTasks.filter((t) => t.status === "done").length
    };
  }) : [];
  const valState = loadValidationState(session.dir);
  const assertions = valState ? Object.values(valState.assertions) : [];
  const validationSummary2 = {
    total: assertions.length,
    passing: assertions.filter((a) => a.status === "passing").length,
    failing: assertions.filter((a) => a.status === "failing").length,
    pending: assertions.filter((a) => a.status === "pending").length,
    blocked: assertions.filter((a) => a.status === "blocked").length
  };
  return {
    project,
    mission: mission ? { mission, validationSummary: validationSummary2 } : null,
    milestones,
    goals: project.goals,
    tasks: project.tasks,
    skills: loadSkills(session.dir),
    agents: project.agents,
    events: readEvents(session.dir).slice(-100).reverse()
  };
}
function handleWsEventsConnection(socket) {
  const ws = socket;
  const subscriptions = /* @__PURE__ */ new Map();
  const sessionDirs = /* @__PURE__ */ new Map();
  let closed = false;
  const send2 = (frame) => {
    if (closed || ws.readyState !== WS_OPEN2) return;
    try {
      ws.send(JSON.stringify(frame));
    } catch {
    }
  };
  const broadcastSessionsChanged = () => {
    send2({ type: "sessions.changed" });
  };
  const broadcastProjectsChanged = () => {
    send2({ type: "projects.changed" });
  };
  const broadcastInitOutputForClient = (jobId, chunk, done) => {
    const frame = done === void 0 ? { type: "init.output", jobId, chunk } : { type: "init.output", jobId, chunk, done };
    send2(frame);
  };
  const broadcastInitErrorForClient = (jobId, message) => {
    send2({ type: "init.error", jobId, message });
  };
  const broadcastActionCompleteForClient = (name, result) => {
    send2({ type: "action.complete", name, result });
  };
  const broadcastSkillsChangedForClient = (sessionName) => {
    send2({ type: "skills.changed", sessionName });
  };
  const broadcastValidationChangedForClient = (sessionName) => {
    send2({ type: "validation.changed", sessionName });
  };
  const broadcastConfigChangedForClient = (sessionName) => {
    send2({ type: "config.changed", sessionName });
  };
  const broadcastChatEventForClient = (event) => {
    send2(event);
  };
  const broadcastTerminalsChangedForClient = (sessionName) => {
    send2({ type: "terminals.changed", sessionName });
  };
  const workspaceRegistry = getDefaultWorkspaceRegistry();
  const unsubWorkspaceAdded = workspaceRegistry.on(
    "workspace.added",
    (workspace) => send2({ type: "workspace.added", workspace })
  );
  const unsubWorkspaceRemoved = workspaceRegistry.on(
    "workspace.removed",
    (name) => send2({ type: "workspace.removed", name })
  );
  const clientHandle = {
    broadcastSessionsChanged,
    broadcastProjectsChanged,
    broadcastInitOutput: broadcastInitOutputForClient,
    broadcastInitError: broadcastInitErrorForClient,
    broadcastActionComplete: broadcastActionCompleteForClient,
    broadcastSkillsChanged: broadcastSkillsChangedForClient,
    broadcastValidationChanged: broadcastValidationChangedForClient,
    broadcastConfigChanged: broadcastConfigChangedForClient,
    broadcastChatEvent: broadcastChatEventForClient,
    broadcastTerminalsChanged: broadcastTerminalsChangedForClient
  };
  allClients.add(clientHandle);
  ensureSessionsPoller();
  ensureProjectRegistryListener();
  const keepalive = setInterval(() => {
    send2({ type: "pong" });
  }, KEEPALIVE_INTERVAL_MS);
  keepalive.unref?.();
  const subscribe = (sessionName) => {
    if (subscriptions.has(sessionName)) return;
    const session = discoverSessions().find((s) => s.name === sessionName);
    const dir = session?.dir ?? null;
    if (dir) sessionDirs.set(sessionName, dir);
    const taskListener = (change) => {
      const knownDir = sessionDirs.get(sessionName);
      if (!knownDir) return;
      const kind = classifyTaskStorePath(change, knownDir);
      if (!kind) return;
      if (kind === "mission") {
        send2({ type: "mission.changed", sessionName });
        send2({ type: "milestone.changed", sessionName });
        return;
      }
      if (kind === "task") send2({ type: "task.changed", sessionName });
      else if (kind === "goal") send2({ type: "goal.changed", sessionName });
      else if (kind === "milestone") send2({ type: "milestone.changed", sessionName });
    };
    const eventListener = (message) => {
      const knownDir = sessionDirs.get(sessionName);
      if (!knownDir || message.dir !== knownDir) return;
      send2({ type: "event.appended", sessionName, event: message.event });
    };
    taskStore.on("change", taskListener);
    eventLogEmitter.on("event", eventListener);
    const fsUnsubscribe = dir ? subscribeFsChanges(dir, (event) => {
      send2({
        type: "file.changed",
        sessionName,
        path: event.path,
        kind: event.kind
      });
    }) : () => {
    };
    subscriptions.set(sessionName, { taskListener, eventListener, fsUnsubscribe });
    if (session) {
      const data = buildSessionSnapshot(sessionName);
      if (data) {
        send2({
          type: "snapshot",
          sessionName,
          data
        });
      }
    }
  };
  const unsubscribe = (sessionName) => {
    const entry = subscriptions.get(sessionName);
    if (!entry) return;
    taskStore.off("change", entry.taskListener);
    eventLogEmitter.off("event", entry.eventListener);
    try {
      entry.fsUnsubscribe();
    } catch {
    }
    subscriptions.delete(sessionName);
    sessionDirs.delete(sessionName);
  };
  const cleanup = () => {
    if (closed) return;
    closed = true;
    clearInterval(keepalive);
    allClients.delete(clientHandle);
    for (const [name, entry] of subscriptions) {
      taskStore.off("change", entry.taskListener);
      eventLogEmitter.off("event", entry.eventListener);
      try {
        entry.fsUnsubscribe();
      } catch {
      }
      sessionDirs.delete(name);
    }
    subscriptions.clear();
    unsubWorkspaceAdded();
    unsubWorkspaceRemoved();
    maybeStopSessionsPoller();
    maybeStopProjectRegistryListener();
  };
  ws.on("message", (data) => {
    if (closed) return;
    let parsed = null;
    try {
      const obj = JSON.parse(rawDataToText2(data));
      if (obj && typeof obj === "object" && typeof obj.type === "string") {
        parsed = obj;
      }
    } catch {
      return;
    }
    if (!parsed) return;
    if (parsed.type === "subscribe") {
      for (const name of parsed.sessions) subscribe(name);
      return;
    }
    if (parsed.type === "unsubscribe") {
      for (const name of parsed.sessions) unsubscribe(name);
      return;
    }
    if (parsed.type === "ping") {
      send2({ type: "pong" });
      return;
    }
    if (parsed.type === "chat.subscribe") {
      replayChatTimelineSince(parsed.threadId, parsed.lastSeq ?? 0, send2);
      return;
    }
  });
  ws.on("close", cleanup);
  ws.on("error", cleanup);
  try {
    const sessions = discoverSessions();
    send2({ type: "hello", sessions: buildOverviews(sessions) });
  } catch {
    send2({ type: "hello", sessions: [] });
  }
}
var WS_OPEN2, KEEPALIVE_INTERVAL_MS, SESSIONS_POLL_MS, allClients, CHAT_REPLAY_CAP_PER_THREAD, chatSeqByThread, chatReplayByThread, sessionsPollTimer, lastSessionsHash, projectRegistryListener;
var init_ws_events = __esm({
  "packages/daemon/src/command-center/ws-events.ts"() {
    "use strict";
    init_discovery();
    init_fs_watch();
    init_task_store();
    init_event_log();
    init_validation();
    init_skill_registry();
    init_project_registry();
    init_workspace_registry();
    WS_OPEN2 = 1;
    KEEPALIVE_INTERVAL_MS = 25e3;
    SESSIONS_POLL_MS = 2e3;
    allClients = /* @__PURE__ */ new Set();
    CHAT_REPLAY_CAP_PER_THREAD = 1024;
    chatSeqByThread = /* @__PURE__ */ new Map();
    chatReplayByThread = /* @__PURE__ */ new Map();
    sessionsPollTimer = null;
    lastSessionsHash = "";
    projectRegistryListener = null;
  }
});

// packages/daemon/src/lib/auth-token.ts
import { randomBytes } from "node:crypto";
function generateAuthToken() {
  return randomBytes(32).toString("base64url");
}
var init_auth_token = __esm({
  "packages/daemon/src/lib/auth-token.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/app-settings.ts
import { existsSync as existsSync20, mkdirSync as mkdirSync15, readFileSync as readFileSync16, renameSync as renameSync12, writeFileSync as writeFileSync15 } from "node:fs";
import { dirname as dirname8, join as join17 } from "node:path";
import { homedir as homedir5 } from "node:os";
function settingsDir() {
  return process.env.TMUX_IDE_SETTINGS_DIR ?? join17(homedir5(), ".tmux-ide");
}
function appSettingsPath() {
  return join17(settingsDir(), "app-settings.json");
}
function normalizeSettings(value) {
  if (!value || typeof value !== "object") return structuredClone(DEFAULT_SETTINGS);
  const remote = value.remoteAccess;
  if (!remote || typeof remote !== "object") return structuredClone(DEFAULT_SETTINGS);
  const enabled = remote.enabled === true;
  const rawToken = remote.token;
  const token = typeof rawToken === "string" && rawToken.length > 0 ? rawToken : null;
  return { remoteAccess: { enabled, token } };
}
function readAppSettings() {
  const path4 = appSettingsPath();
  if (!existsSync20(path4)) return structuredClone(DEFAULT_SETTINGS);
  try {
    return normalizeSettings(JSON.parse(readFileSync16(path4, "utf-8")));
  } catch {
    return structuredClone(DEFAULT_SETTINGS);
  }
}
function writeAppSettings(next) {
  const path4 = appSettingsPath();
  mkdirSync15(dirname8(path4), { recursive: true });
  const tmp = `${path4}.${process.pid}.${Date.now()}.tmp`;
  writeFileSync15(tmp, `${JSON.stringify(normalizeSettings(next), null, 2)}
`, "utf-8");
  renameSync12(tmp, path4);
}
var DEFAULT_SETTINGS;
var init_app_settings = __esm({
  "packages/daemon/src/lib/app-settings.ts"() {
    "use strict";
    DEFAULT_SETTINGS = {
      remoteAccess: {
        enabled: false,
        token: null
      }
    };
  }
});

// packages/daemon/src/command-center/actions/handlers/app-set-remote-access.ts
import { hostname, networkInterfaces } from "node:os";
function setRemoteAccessRestartBackend(backend2) {
  remoteAccessRestartBackend = backend2;
}
function currentPort(deps2) {
  const envPort = Number(process.env.TMUX_IDE_DAEMON_PORT);
  return deps2.port ?? (Number.isInteger(envPort) && envPort > 0 ? envPort : 6060);
}
function primaryLanHost() {
  const interfaces = networkInterfaces();
  for (const entries of Object.values(interfaces)) {
    for (const entry of entries ?? []) {
      if (entry.family === "IPv4" && !entry.internal) return entry.address;
    }
  }
  return hostname();
}
function buildUrl(host, port) {
  return `http://${host}:${port}`;
}
function defaultDeferRestart(restart2) {
  setImmediate(restart2);
}
async function appSetRemoteAccessHandler(input, deps2 = {}) {
  const readSettings = deps2.readSettings ?? readAppSettings;
  const writeSettings = deps2.writeSettings ?? writeAppSettings;
  const nextEnabled = input.enabled;
  const current = readSettings();
  const token = nextEnabled ? current.remoteAccess.token ?? (deps2.generateToken ?? generateAuthToken)() : null;
  const next = {
    ...current,
    remoteAccess: { enabled: nextEnabled, token }
  };
  writeSettings(next);
  const port = currentPort(deps2);
  const request = {
    enabled: nextEnabled,
    bindHostname: nextEnabled ? "0.0.0.0" : "127.0.0.1",
    token,
    port
  };
  const restartDaemon = deps2.restartDaemon ?? remoteAccessRestartBackend;
  if (restartDaemon) {
    (deps2.deferRestart ?? defaultDeferRestart)(() => {
      void Promise.resolve(restartDaemon(request)).catch((err) => {
        console.error(
          `[actions] Failed to restart daemon for remote access: ${err.message ?? String(err)}`
        );
      });
    });
  }
  if (!nextEnabled) {
    return { enabled: false, url: null, token: null, qrPayload: null };
  }
  const host = deps2.host ?? primaryLanHost();
  const url = buildUrl(host, port);
  return {
    enabled: true,
    url,
    token,
    qrPayload: `${url}?token=${encodeURIComponent(token ?? "")}`
  };
}
var remoteAccessRestartBackend;
var init_app_set_remote_access = __esm({
  "packages/daemon/src/command-center/actions/handlers/app-set-remote-access.ts"() {
    "use strict";
    init_auth_token();
    init_app_settings();
    remoteAccessRestartBackend = null;
  }
});

// packages/daemon/src/command-center/actions/errors.ts
function actionErrorFromCwdError(err) {
  return new ActionError({
    code: CWD_REASON_TO_CODE[err.reason],
    message: err.message,
    details: { cwd: err.cwd, reason: err.reason },
    cause: err
  });
}
function wrapInternalError(err) {
  if (err instanceof ActionError) return err;
  if (err instanceof TerminalCwdError) return actionErrorFromCwdError(err);
  const message = err instanceof Error ? err.message : String(err);
  return new ActionError({ code: "internal", message, cause: err });
}
var ActionError, CWD_REASON_TO_CODE;
var init_errors3 = __esm({
  "packages/daemon/src/command-center/actions/errors.ts"() {
    "use strict";
    init_pty_bridge();
    ActionError = class extends Error {
      code;
      details;
      constructor(args) {
        super(
          args.message,
          args.cause !== void 0 ? { cause: args.cause } : void 0
        );
        this.name = "ActionError";
        this.code = args.code;
        this.details = args.details;
      }
      toEnvelope() {
        return this.details !== void 0 ? { code: this.code, message: this.message, details: this.details } : { code: this.code, message: this.message };
      }
    };
    CWD_REASON_TO_CODE = {
      notFound: "cwd_not_found",
      notDirectory: "cwd_not_directory",
      statFailed: "cwd_stat_failed"
    };
  }
});

// packages/daemon/src/acp/errors.ts
var AcpError, AcpProtocolError, AcpRpcError, AcpAgentExitedError, AcpAgentSpawnError;
var init_errors4 = __esm({
  "packages/daemon/src/acp/errors.ts"() {
    "use strict";
    AcpError = class extends Error {
      constructor(message, options) {
        super(message, options);
        this.name = new.target.name;
      }
    };
    AcpProtocolError = class extends AcpError {
    };
    AcpRpcError = class extends AcpError {
      code;
      data;
      constructor(payload) {
        super(payload.message);
        this.code = payload.code;
        this.data = payload.data;
      }
    };
    AcpAgentExitedError = class extends AcpError {
      code;
      signal;
      constructor(message = "ACP agent exited", options) {
        super(message);
        this.code = options?.code ?? null;
        this.signal = options?.signal ?? null;
      }
    };
    AcpAgentSpawnError = class extends AcpError {
    };
  }
});

// packages/daemon/src/acp/methods.ts
var AGENT_METHODS, CLIENT_METHODS;
var init_methods = __esm({
  "packages/daemon/src/acp/methods.ts"() {
    "use strict";
    AGENT_METHODS = {
      authenticate: "authenticate",
      initialize: "initialize",
      logout: "logout",
      session_cancel: "session/cancel",
      session_close: "session/close",
      session_fork: "session/fork",
      session_list: "session/list",
      session_load: "session/load",
      session_new: "session/new",
      session_prompt: "session/prompt",
      session_resume: "session/resume",
      session_set_config_option: "session/set_config_option",
      session_set_mode: "session/set_mode",
      session_set_model: "session/set_model"
    };
    CLIENT_METHODS = {
      fs_read_text_file: "fs/read_text_file",
      fs_write_text_file: "fs/write_text_file",
      session_elicitation: "session/elicitation",
      session_elicitation_complete: "session/elicitation/complete",
      session_request_permission: "session/request_permission",
      session_update: "session/update",
      terminal_create: "terminal/create",
      terminal_kill: "terminal/kill",
      terminal_output: "terminal/output",
      terminal_release: "terminal/release",
      terminal_wait_for_exit: "terminal/wait_for_exit"
    };
  }
});

// packages/daemon/src/acp/protocol.ts
function isObject(value) {
  return typeof value === "object" && value !== null;
}
function isJsonRpcRequest(value) {
  return isObject(value) && value.jsonrpc === "2.0" && (typeof value.id === "number" || typeof value.id === "string") && typeof value.method === "string";
}
function isJsonRpcNotification(value) {
  return isObject(value) && value.jsonrpc === "2.0" && value.id === void 0 && typeof value.method === "string";
}
function isJsonRpcResponse(value) {
  return isObject(value) && value.jsonrpc === "2.0" && (typeof value.id === "number" || typeof value.id === "string") && value.method === void 0;
}
function makeJsonRpcEndpoint(opts) {
  let nextId = 1;
  let buffer = "";
  let isClosed = false;
  let incomingRequestHandler = null;
  let resolveClosed;
  const pending = /* @__PURE__ */ new Map();
  const notificationHandlers = /* @__PURE__ */ new Set();
  const closed = new Promise((resolve35) => {
    resolveClosed = resolve35;
  });
  function log2(direction, payload) {
    opts.logger?.({ direction, payload });
  }
  function write(payload) {
    if (isClosed) return;
    log2("out", payload);
    opts.output.write(`${JSON.stringify(payload)}
`);
  }
  function finish(reason = new AcpAgentExitedError()) {
    if (isClosed) return;
    isClosed = true;
    for (const request of pending.values()) request.reject(reason);
    pending.clear();
    resolveClosed();
  }
  function respond(id, response) {
    write({ jsonrpc: "2.0", id, ...response });
  }
  async function handleIncomingRequest(request) {
    try {
      if (!incomingRequestHandler) {
        respond(request.id, {
          error: { code: -32601, message: `Method not found: ${request.method}` }
        });
        return;
      }
      const result = await incomingRequestHandler(request);
      respond(request.id, { result });
    } catch (err) {
      if (err instanceof AcpRpcError) {
        respond(request.id, {
          error: { code: err.code, message: err.message, data: err.data }
        });
        return;
      }
      const message = err instanceof Error ? err.message : String(err);
      respond(request.id, { error: { code: -32603, message } });
    }
  }
  function handleFrame(payload) {
    log2("in", payload);
    if (isJsonRpcResponse(payload)) {
      const request = pending.get(payload.id);
      if (!request) return;
      pending.delete(payload.id);
      if (payload.error) request.reject(new AcpRpcError(payload.error));
      else request.resolve(payload.result);
      return;
    }
    if (isJsonRpcNotification(payload)) {
      for (const handler of notificationHandlers) handler(payload);
      return;
    }
    if (isJsonRpcRequest(payload)) {
      void handleIncomingRequest(payload);
      return;
    }
    finish(new AcpProtocolError("Invalid JSON-RPC frame"));
  }
  function handleChunk(chunk) {
    buffer += Buffer.isBuffer(chunk) ? chunk.toString("utf8") : String(chunk);
    for (; ; ) {
      const newline = buffer.indexOf("\n");
      if (newline === -1) break;
      const line = buffer.slice(0, newline).trim();
      buffer = buffer.slice(newline + 1);
      if (!line) continue;
      try {
        handleFrame(JSON.parse(line));
      } catch (err) {
        finish(new AcpProtocolError("Invalid JSON-RPC JSON", { cause: err }));
      }
    }
  }
  opts.input.on("data", handleChunk);
  opts.input.on(
    "error",
    (err) => finish(new AcpProtocolError("ACP input stream failed", { cause: err }))
  );
  opts.input.on("close", () => finish(new AcpAgentExitedError()));
  opts.input.on("end", () => finish(new AcpAgentExitedError()));
  opts.output.on?.(
    "error",
    (err) => finish(new AcpProtocolError("ACP output stream failed", { cause: err }))
  );
  return {
    request(method, params) {
      if (isClosed) return Promise.reject(new AcpAgentExitedError());
      const id = nextId++;
      const envelope = { jsonrpc: "2.0", id, method };
      if (params !== void 0) envelope.params = params;
      const promise = new Promise((resolve35, reject) => {
        pending.set(id, { resolve: resolve35, reject });
      });
      write(envelope);
      return promise;
    },
    notify(method, params) {
      const envelope = { jsonrpc: "2.0", method };
      if (params !== void 0) envelope.params = params;
      write(envelope);
    },
    onNotification(handler) {
      notificationHandlers.add(handler);
      return () => notificationHandlers.delete(handler);
    },
    onIncomingRequest(handler) {
      incomingRequestHandler = handler;
      return () => {
        if (incomingRequestHandler === handler) incomingRequestHandler = null;
      };
    },
    async close(reason) {
      finish(reason);
      opts.input.off?.("data", handleChunk);
      const output = opts.output;
      if (!output.destroyed) output.end?.();
      await closed;
    },
    closed
  };
}
var init_protocol = __esm({
  "packages/daemon/src/acp/protocol.ts"() {
    "use strict";
    init_errors4();
  }
});

// packages/daemon/src/acp/schema.ts
import { z as z18 } from "zod";
var MetaZ, EmbeddedResourceResourceZ, ContentBlockZ2, ToolKindZ, ToolCallStatusZ, ToolCallLocationZ, ToolCallContentZ, PermissionOptionZ, ToolCallUpdateRawZ, ToolCallUpdateZ, PlanEntryZ, AvailableCommandInputZ, AvailableCommandZ, RequestPermissionRequestZ, SessionNotificationZ;
var init_schema = __esm({
  "packages/daemon/src/acp/schema.ts"() {
    "use strict";
    MetaZ = z18.record(z18.string(), z18.unknown()).nullable().optional();
    EmbeddedResourceResourceZ = z18.union([
      z18.object({
        _meta: MetaZ,
        mimeType: z18.string().nullable().optional(),
        text: z18.string(),
        uri: z18.string()
      }),
      z18.object({
        _meta: MetaZ,
        blob: z18.string(),
        mimeType: z18.string().nullable().optional(),
        uri: z18.string()
      })
    ]);
    ContentBlockZ2 = z18.discriminatedUnion("type", [
      z18.object({
        type: z18.literal("text"),
        _meta: MetaZ,
        annotations: z18.unknown().optional(),
        text: z18.string()
      }),
      z18.object({
        type: z18.literal("image"),
        _meta: MetaZ,
        annotations: z18.unknown().optional(),
        data: z18.string(),
        mimeType: z18.string(),
        uri: z18.string().nullable().optional()
      }),
      z18.object({
        type: z18.literal("audio"),
        _meta: MetaZ,
        annotations: z18.unknown().optional(),
        data: z18.string(),
        mimeType: z18.string()
      }),
      z18.object({
        type: z18.literal("resource"),
        _meta: MetaZ,
        annotations: z18.unknown().optional(),
        resource: EmbeddedResourceResourceZ
      }),
      z18.object({
        type: z18.literal("resource_link"),
        _meta: MetaZ,
        annotations: z18.unknown().optional(),
        description: z18.string().nullable().optional(),
        mimeType: z18.string().nullable().optional(),
        name: z18.string(),
        size: z18.number().int().nullable().optional(),
        title: z18.string().nullable().optional(),
        uri: z18.string()
      })
    ]);
    ToolKindZ = z18.enum([
      "read",
      "edit",
      "delete",
      "move",
      "search",
      "execute",
      "think",
      "fetch",
      "switch_mode",
      "other"
    ]);
    ToolCallStatusZ = z18.enum(["pending", "in_progress", "completed", "failed"]);
    ToolCallLocationZ = z18.object({
      _meta: MetaZ,
      line: z18.number().int().nullable().optional(),
      path: z18.string()
    });
    ToolCallContentZ = z18.discriminatedUnion("type", [
      z18.object({ type: z18.literal("content"), _meta: MetaZ, content: ContentBlockZ2 }),
      z18.object({
        type: z18.literal("diff"),
        _meta: MetaZ,
        newText: z18.string(),
        oldText: z18.string().nullable().optional(),
        path: z18.string()
      }),
      z18.object({ type: z18.literal("terminal"), _meta: MetaZ, terminalId: z18.string() })
    ]);
    PermissionOptionZ = z18.object({
      _meta: MetaZ,
      kind: z18.enum(["allow_once", "allow_always", "reject_once", "reject_always"]),
      name: z18.string(),
      optionId: z18.string()
    });
    ToolCallUpdateRawZ = z18.object({
      _meta: MetaZ,
      content: z18.array(ToolCallContentZ).nullable().optional(),
      kind: ToolKindZ.nullable().optional(),
      locations: z18.array(ToolCallLocationZ).nullable().optional(),
      rawInput: z18.unknown().optional(),
      rawOutput: z18.unknown().optional(),
      status: ToolCallStatusZ.nullable().optional(),
      title: z18.string().nullable().optional(),
      toolCallId: z18.string()
    });
    ToolCallUpdateZ = ToolCallUpdateRawZ;
    PlanEntryZ = z18.object({
      _meta: MetaZ,
      content: z18.string(),
      priority: z18.enum(["high", "medium", "low"]),
      status: z18.enum(["pending", "in_progress", "completed"])
    });
    AvailableCommandInputZ = z18.object({
      _meta: MetaZ,
      hint: z18.string()
    });
    AvailableCommandZ = z18.object({
      _meta: MetaZ,
      description: z18.string(),
      input: AvailableCommandInputZ.nullable().optional(),
      name: z18.string()
    });
    RequestPermissionRequestZ = z18.object({
      _meta: MetaZ,
      options: z18.array(PermissionOptionZ),
      sessionId: z18.string(),
      toolCall: ToolCallUpdateZ
    });
    SessionNotificationZ = z18.object({
      _meta: MetaZ,
      sessionId: z18.string(),
      update: z18.discriminatedUnion("sessionUpdate", [
        z18.object({
          sessionUpdate: z18.literal("user_message_chunk"),
          _meta: MetaZ,
          content: ContentBlockZ2,
          messageId: z18.string().nullable().optional()
        }),
        z18.object({
          sessionUpdate: z18.literal("agent_message_chunk"),
          _meta: MetaZ,
          content: ContentBlockZ2,
          messageId: z18.string().nullable().optional()
        }),
        z18.object({
          sessionUpdate: z18.literal("agent_thought_chunk"),
          _meta: MetaZ,
          content: ContentBlockZ2,
          messageId: z18.string().nullable().optional()
        }),
        z18.object({
          sessionUpdate: z18.literal("tool_call"),
          _meta: MetaZ,
          content: z18.array(ToolCallContentZ).optional(),
          kind: ToolKindZ.optional(),
          locations: z18.array(ToolCallLocationZ).optional(),
          rawInput: z18.unknown().optional(),
          rawOutput: z18.unknown().optional(),
          status: ToolCallStatusZ.optional(),
          title: z18.string(),
          toolCallId: z18.string()
        }),
        ToolCallUpdateRawZ.extend({ sessionUpdate: z18.literal("tool_call_update") }),
        z18.object({
          sessionUpdate: z18.literal("plan"),
          _meta: MetaZ,
          entries: z18.array(PlanEntryZ)
        }),
        z18.object({
          sessionUpdate: z18.literal("available_commands_update"),
          _meta: MetaZ,
          availableCommands: z18.array(AvailableCommandZ)
        }),
        z18.object({
          sessionUpdate: z18.literal("current_mode_update"),
          _meta: MetaZ,
          currentModeId: z18.string()
        }),
        z18.object({
          sessionUpdate: z18.literal("config_option_update"),
          _meta: MetaZ,
          configOptions: z18.array(z18.unknown())
        }),
        z18.object({
          sessionUpdate: z18.literal("session_info_update"),
          _meta: MetaZ,
          title: z18.string().nullable().optional(),
          updatedAt: z18.string().nullable().optional()
        }),
        z18.object({
          sessionUpdate: z18.literal("usage_update"),
          _meta: MetaZ,
          cost: z18.unknown().optional(),
          size: z18.number().int().nonnegative(),
          used: z18.number().int().nonnegative()
        })
      ])
    });
  }
});

// packages/daemon/src/acp/client.ts
import { accessSync, constants } from "node:fs";
import { spawn as spawn3 } from "node:child_process";
import { once } from "node:events";
function resolveFromPath(binary) {
  const paths = process.env.PATH?.split(":") ?? [];
  for (const dir of paths) {
    const candidate = `${dir}/${binary}`;
    try {
      accessSync(candidate, constants.X_OK);
      return candidate;
    } catch {
    }
  }
  return null;
}
function assertExecutable(binary) {
  if (binary.includes("/")) {
    try {
      accessSync(binary, constants.X_OK);
      return binary;
    } catch (err) {
      throw new AcpAgentSpawnError(`ACP agent binary is not executable: ${binary}`, { cause: err });
    }
  }
  const resolved2 = resolveFromPath(binary);
  if (!resolved2) throw new AcpAgentSpawnError(`ACP agent binary not found on PATH: ${binary}`);
  return resolved2;
}
function resolveProvider(provider) {
  if (provider.kind === "custom") {
    return { command: provider.command, args: provider.args, env: provider.env };
  }
  if (provider.kind === "claude-code") {
    if (provider.binary) {
      return { command: assertExecutable(provider.binary), args: [] };
    }
    const direct = resolveFromPath("claude-code-acp");
    if (direct) return { command: direct, args: [] };
    const npx = resolveFromPath("npx");
    if (npx) {
      return { command: npx, args: ["-y", "@zed-industries/claude-code-acp"] };
    }
    throw new AcpAgentSpawnError(
      "ACP agent binary not found: install @zed-industries/claude-code-acp globally, or ensure `npx` is on PATH."
    );
  }
  throw new AcpAgentSpawnError(`ACP provider is not implemented: ${provider.kind}`);
}
function writeStderrLines(child, logger2) {
  let buffer = "";
  child.stderr.on("data", (chunk) => {
    buffer += Buffer.isBuffer(chunk) ? chunk.toString("utf8") : String(chunk);
    for (; ; ) {
      const newline = buffer.indexOf("\n");
      if (newline === -1) break;
      const line = buffer.slice(0, newline);
      buffer = buffer.slice(newline + 1);
      if (line) logger2?.({ direction: "in", payload: { _stderr: true, line } });
    }
  });
  child.stderr.on("close", () => {
    if (buffer) logger2?.({ direction: "in", payload: { _stderr: true, line: buffer } });
  });
}
async function spawnAcpClient(opts) {
  const resolved2 = resolveProvider(opts.provider);
  const mergedEnv = {
    ...process.env,
    ...opts.env,
    ...resolved2.env
  };
  if (opts.provider.kind === "claude-code") {
    delete mergedEnv.CLAUDECODE;
    delete mergedEnv.CLAUDE_CODE;
    delete mergedEnv.CLAUDE_CODE_ENTRYPOINT;
  }
  const child = spawn3(resolved2.command, resolved2.args, {
    cwd: opts.cwd,
    env: mergedEnv,
    stdio: ["pipe", "pipe", "pipe"]
  });
  const spawnFailure = await Promise.race([
    once(child, "spawn").then(() => null),
    once(child, "error").then(([err]) => err),
    once(child, "exit").then(
      ([code, signal]) => new AcpAgentExitedError("ACP agent exited during spawn", {
        code,
        signal
      })
    )
  ]);
  if (spawnFailure) {
    throw spawnFailure instanceof AcpAgentExitedError ? spawnFailure : new AcpAgentSpawnError(`Failed to spawn ACP agent: ${spawnFailure.message}`, {
      cause: spawnFailure
    });
  }
  writeStderrLines(child, opts.logger);
  const endpoint = makeJsonRpcEndpoint({
    input: child.stdout,
    output: child.stdin,
    logger: opts.logger
  });
  const sessionUpdateHandlers = /* @__PURE__ */ new Set();
  let permissionHandler = null;
  endpoint.onNotification((notification) => {
    if (notification.method !== CLIENT_METHODS.session_update) return;
    const parsed = SessionNotificationZ.parse(notification.params);
    for (const handler of sessionUpdateHandlers) handler(parsed);
  });
  endpoint.onIncomingRequest(async (request) => {
    if (request.method !== CLIENT_METHODS.session_request_permission) {
      throw new AcpRpcError({ code: -32601, message: `Method not found: ${request.method}` });
    }
    if (!permissionHandler) throw new AcpProtocolError("No permission request handler registered");
    return await permissionHandler(RequestPermissionRequestZ.parse(request.params));
  });
  const closed = once(child, "exit").then(([code, signal]) => ({
    code,
    signal
  }));
  void closed.then(({ code, signal }) => {
    void endpoint.close(new AcpAgentExitedError("ACP agent exited", { code, signal }));
  });
  return {
    async initialize() {
      return await endpoint.request(AGENT_METHODS.initialize, {
        protocolVersion: 1,
        clientInfo: { name: "tmux-ide", version: "0.0.1" }
      });
    },
    async newSession(req) {
      return await endpoint.request(AGENT_METHODS.session_new, req);
    },
    async loadSession(req) {
      return await endpoint.request(AGENT_METHODS.session_load, req);
    },
    async prompt(req) {
      return await endpoint.request(AGENT_METHODS.session_prompt, req);
    },
    async cancel(notif) {
      endpoint.notify(AGENT_METHODS.session_cancel, notif);
    },
    onSessionUpdate(handler) {
      sessionUpdateHandlers.add(handler);
      return () => sessionUpdateHandlers.delete(handler);
    },
    onPermissionRequest(handler) {
      permissionHandler = handler;
      return () => {
        if (permissionHandler === handler) permissionHandler = null;
      };
    },
    async close() {
      if (!child.killed) child.kill("SIGTERM");
      await endpoint.close(new AcpAgentExitedError("ACP client closed"));
    },
    closed
  };
}
var init_client2 = __esm({
  "packages/daemon/src/acp/client.ts"() {
    "use strict";
    init_errors4();
    init_methods();
    init_protocol();
    init_schema();
  }
});

// packages/daemon/src/acp/index.ts
var init_acp = __esm({
  "packages/daemon/src/acp/index.ts"() {
    "use strict";
    init_client2();
    init_errors4();
    init_methods();
    init_protocol();
    init_schema();
  }
});

// packages/daemon/src/chat/legacy-to-v2.ts
function isChatThreadUpdate(event) {
  return event.type === "chat.thread.update";
}
function isChatThreadStop(event) {
  return event.type === "chat.thread.stop";
}
function summarizeUpdate(update) {
  const sessionUpdate = update.sessionUpdate;
  switch (sessionUpdate) {
    case "agent_message_chunk": {
      const block = update.content;
      const text = typeof block?.text === "string" ? block.text : "";
      return { kind: "agent_message", tone: "info", summary: text || "(streaming)" };
    }
    case "agent_thought_chunk":
      return { kind: "agent_thought", tone: "info", summary: "(thought)" };
    case "user_message_chunk": {
      const block = update.content;
      return { kind: "user_message", tone: "info", summary: block?.text ?? "" };
    }
    case "tool_call": {
      const title = update.title ?? "tool";
      return { kind: "tool_call", tone: "tool", summary: title };
    }
    case "tool_call_update": {
      const title = update.title ?? "tool update";
      const status4 = update.status;
      return {
        kind: "tool_call_update",
        tone: status4 === "failed" ? "error" : "tool",
        summary: status4 ? `${title} \xB7 ${status4}` : title
      };
    }
    case "plan":
      return { kind: "plan", tone: "info", summary: "plan updated" };
    case "available_commands_update":
      return { kind: "commands", tone: "info", summary: "commands updated" };
    case "current_mode_update":
      return { kind: "mode", tone: "info", summary: "mode updated" };
    default:
      return { kind: sessionUpdate || "update", tone: "info", summary: "" };
  }
}
function translateLegacyToV2(event) {
  if (isChatThreadUpdate(event)) {
    const meta = summarizeUpdate(event.update);
    const activity = {
      id: `legacy:${event.threadId}:${event.seq}`,
      tone: meta.tone,
      kind: meta.kind,
      summary: meta.summary,
      payload: event.update,
      // turnId stays null until thread-manager grows native turn
      // tracking — the dashboard's group-by-turn renders these as
      // ambient activities, which is acceptable until turn-store is
      // wired in.
      turnId: null,
      sequence: event.seq,
      createdAt: (/* @__PURE__ */ new Date()).toISOString()
    };
    return [
      {
        type: "chat.activity.appended",
        threadId: event.threadId,
        activity,
        seq: event.seq
      }
    ];
  }
  if (isChatThreadStop(event)) {
    return [];
  }
  return [];
}
var init_legacy_to_v2 = __esm({
  "packages/daemon/src/chat/legacy-to-v2.ts"() {
    "use strict";
  }
});

// packages/daemon/src/persistence/chat-event-store.ts
import { randomUUID as randomUUID2 } from "node:crypto";
function applyChatStorePragmas(db) {
  db.exec("PRAGMA journal_mode = WAL;");
  db.exec("PRAGMA synchronous = NORMAL;");
  db.exec("PRAGMA foreign_keys = ON;");
}
function runChatStoreMigrations(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS _chat_migrations (
      id          INTEGER PRIMARY KEY,
      name        TEXT NOT NULL,
      applied_at  TEXT NOT NULL
    );
  `);
  const applied = new Set(
    db.prepare("SELECT id FROM _chat_migrations").all().map((r) => r.id)
  );
  const insert = db.prepare("INSERT INTO _chat_migrations (id, name, applied_at) VALUES (?, ?, ?)");
  const now = (/* @__PURE__ */ new Date()).toISOString();
  for (const migration of CHAT_MIGRATIONS) {
    if (applied.has(migration.id)) continue;
    db.exec(migration.sql);
    insert.run(migration.id, migration.name, now);
  }
}
function aggregateKindForEvent(event) {
  switch (event.type) {
    case "chat.turn.started":
    case "chat.turn.completed":
    case "chat.turn.aborted":
      return "turn";
    case "chat.plan.upserted":
      return "plan";
    case "chat.checkpoint.created":
      return "checkpoint";
    case "chat.session.added":
    case "chat.session.removed":
    case "chat.session.status-changed":
      return "session";
    case "chat.activity.appended":
    case "chat.thread.reverted":
      return "thread";
  }
}
function streamIdForEvent(event) {
  return event.threadId;
}
function rowToPersisted(row) {
  let payload;
  try {
    payload = JSON.parse(row.payload_json);
  } catch (err) {
    throw new Error(
      `chat-event-store: payload_json for sequence ${row.sequence} is not valid JSON`,
      { cause: err }
    );
  }
  const parsed = ChatThreadEventZ.safeParse(payload);
  if (!parsed.success) {
    throw new Error(
      `chat-event-store: payload at sequence ${row.sequence} failed ChatThreadEventZ validation: ${parsed.error.message}`
    );
  }
  let metadata = {};
  if (row.metadata_json && row.metadata_json !== "{}") {
    try {
      const meta = JSON.parse(row.metadata_json);
      if (meta && typeof meta === "object" && !Array.isArray(meta)) {
        metadata = meta;
      }
    } catch {
    }
  }
  const persisted = {
    sequence: row.sequence,
    eventId: row.event_id,
    aggregateKind: row.aggregate_kind,
    streamId: row.stream_id,
    streamVersion: row.stream_version,
    eventType: row.event_type,
    occurredAt: row.occurred_at,
    actorKind: row.actor_kind,
    event: parsed.data,
    metadata
  };
  if (row.command_id !== null) persisted.commandId = row.command_id;
  if (row.causation_event_id !== null) persisted.causationEventId = row.causation_event_id;
  if (row.correlation_id !== null) persisted.correlationId = row.correlation_id;
  if (row.session_id !== null) persisted.sessionId = row.session_id;
  return persisted;
}
function makeChatEventStore(db) {
  applyChatStorePragmas(db);
  runChatStoreMigrations(db);
  const subscribers2 = /* @__PURE__ */ new Set();
  const appendStmt = db.prepare(`
    INSERT INTO chat_events (
      event_id, aggregate_kind, stream_id, stream_version,
      event_type, occurred_at, command_id, causation_event_id,
      correlation_id, actor_kind, session_id, payload_json, metadata_json
    ) VALUES (
      ?, ?, ?,
      (SELECT COALESCE(MAX(stream_version), 0) + 1
       FROM chat_events
       WHERE aggregate_kind = ? AND stream_id = ?),
      ?, ?, ?, ?, ?, ?, ?, ?, ?
    )
  `);
  const readBackStmt = db.prepare(`
    SELECT sequence, event_id, aggregate_kind, stream_id, stream_version,
           event_type, occurred_at, command_id, causation_event_id,
           correlation_id, actor_kind, session_id, payload_json, metadata_json
    FROM chat_events
    WHERE event_id = ?
  `);
  const readFromSeqStmt = db.prepare(`
    SELECT sequence, event_id, aggregate_kind, stream_id, stream_version,
           event_type, occurred_at, command_id, causation_event_id,
           correlation_id, actor_kind, session_id, payload_json, metadata_json
    FROM chat_events
    WHERE sequence > ?
    ORDER BY sequence ASC
    LIMIT ?
  `);
  const readAllStmt = db.prepare(`
    SELECT sequence, event_id, aggregate_kind, stream_id, stream_version,
           event_type, occurred_at, command_id, causation_event_id,
           correlation_id, actor_kind, session_id, payload_json, metadata_json
    FROM chat_events
    ORDER BY sequence ASC
  `);
  function append(input) {
    const validated = ChatThreadEventZ.safeParse(input.event);
    if (!validated.success) {
      throw new Error(
        `chat-event-store.append: event failed ChatThreadEventZ validation: ${validated.error.message}`
      );
    }
    const event = validated.data;
    const aggregateKind = aggregateKindForEvent(event);
    const streamId = streamIdForEvent(event);
    const eventId = input.eventId ?? randomUUID2();
    const occurredAt = input.occurredAt ?? (/* @__PURE__ */ new Date()).toISOString();
    const payloadJson = JSON.stringify(event);
    const metadataJson = JSON.stringify(input.metadata ?? {});
    appendStmt.run(
      eventId,
      aggregateKind,
      streamId,
      // correlated subquery params
      aggregateKind,
      streamId,
      // remaining columns
      event.type,
      occurredAt,
      input.commandId ?? null,
      input.causationEventId ?? null,
      input.correlationId ?? null,
      input.actorKind,
      input.sessionId ?? null,
      payloadJson,
      metadataJson
    );
    const row = readBackStmt.get?.(eventId);
    if (!row) {
      throw new Error(
        `chat-event-store.append: insert succeeded but read-back returned no row for event_id=${eventId}`
      );
    }
    const persisted = rowToPersisted(row);
    for (const handler of subscribers2) {
      try {
        handler(persisted);
      } catch (err) {
        console.error("[chat-event-store] subscriber threw:", err);
      }
    }
    return persisted;
  }
  function subscribe(handler) {
    subscribers2.add(handler);
    return () => {
      subscribers2.delete(handler);
    };
  }
  function readFromSequence(seqExclusive, limit = 1e3) {
    const rows = readFromSeqStmt.all(seqExclusive, limit);
    return rows.map(rowToPersisted);
  }
  function readByStream(streamId, options = {}) {
    const since = options.sinceVersion ?? 0;
    const limit = options.limit ?? 1e4;
    if (options.aggregateKind) {
      const stmt2 = db.prepare(`
        SELECT sequence, event_id, aggregate_kind, stream_id, stream_version,
               event_type, occurred_at, command_id, causation_event_id,
               correlation_id, actor_kind, session_id, payload_json, metadata_json
        FROM chat_events
        WHERE aggregate_kind = ? AND stream_id = ? AND stream_version > ?
        ORDER BY stream_version ASC
        LIMIT ?
      `);
      return stmt2.all(options.aggregateKind, streamId, since, limit).map(rowToPersisted);
    }
    const stmt = db.prepare(`
      SELECT sequence, event_id, aggregate_kind, stream_id, stream_version,
             event_type, occurred_at, command_id, causation_event_id,
             correlation_id, actor_kind, session_id, payload_json, metadata_json
      FROM chat_events
      WHERE stream_id = ? AND sequence > ?
      ORDER BY sequence ASC
      LIMIT ?
    `);
    return stmt.all(streamId, since, limit).map(rowToPersisted);
  }
  function* readAll() {
    const rows = readAllStmt.all();
    for (const row of rows) yield rowToPersisted(row);
  }
  return { append, subscribe, readFromSequence, readByStream, readAll };
}
var CHAT_MIGRATIONS;
var init_chat_event_store = __esm({
  "packages/daemon/src/persistence/chat-event-store.ts"() {
    "use strict";
    init_src2();
    CHAT_MIGRATIONS = [
      {
        id: 1,
        name: "chat_events",
        sql: `
      CREATE TABLE IF NOT EXISTS chat_events (
        sequence            INTEGER PRIMARY KEY AUTOINCREMENT,
        event_id            TEXT NOT NULL UNIQUE,
        aggregate_kind      TEXT NOT NULL,
        stream_id           TEXT NOT NULL,
        stream_version      INTEGER NOT NULL,
        event_type          TEXT NOT NULL,
        occurred_at         TEXT NOT NULL,
        command_id          TEXT,
        causation_event_id  TEXT,
        correlation_id      TEXT,
        actor_kind          TEXT NOT NULL,
        session_id          TEXT,
        payload_json        TEXT NOT NULL,
        metadata_json       TEXT NOT NULL
      );
      CREATE UNIQUE INDEX IF NOT EXISTS idx_chat_events_stream_version
        ON chat_events(aggregate_kind, stream_id, stream_version);
      CREATE INDEX IF NOT EXISTS idx_chat_events_stream_seq
        ON chat_events(aggregate_kind, stream_id, sequence);
      CREATE INDEX IF NOT EXISTS idx_chat_events_correlation
        ON chat_events(correlation_id);
      CREATE INDEX IF NOT EXISTS idx_chat_events_session
        ON chat_events(session_id);
    `
      },
      {
        id: 2,
        name: "chat_commands",
        sql: `
      CREATE TABLE IF NOT EXISTS chat_commands (
        command_id      TEXT PRIMARY KEY,
        command_type    TEXT NOT NULL,
        received_at     TEXT NOT NULL,
        decided_at      TEXT NOT NULL,
        outcome         TEXT NOT NULL,
        rejection_code  TEXT,
        input_json      TEXT NOT NULL,
        actor_kind      TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_chat_commands_received
        ON chat_commands(received_at);
    `
      },
      {
        id: 3,
        name: "projection_state",
        sql: `
      CREATE TABLE IF NOT EXISTS projection_state (
        projection_name        TEXT PRIMARY KEY,
        last_applied_sequence  INTEGER NOT NULL,
        updated_at             TEXT NOT NULL
      );
    `
      },
      {
        id: 4,
        name: "projection_turns",
        sql: `
      CREATE TABLE IF NOT EXISTS projection_turns (
        turn_id                TEXT PRIMARY KEY,
        thread_id              TEXT NOT NULL,
        session_id             TEXT,
        state                  TEXT NOT NULL,
        requested_at           TEXT NOT NULL,
        started_at             TEXT,
        completed_at           TEXT,
        assistant_message_id   TEXT,
        source_plan_id         TEXT,
        source_plan_thread_id  TEXT
      );
      CREATE INDEX IF NOT EXISTS idx_proj_turns_thread
        ON projection_turns(thread_id);
    `
      },
      {
        id: 5,
        name: "projection_activities",
        sql: `
      CREATE TABLE IF NOT EXISTS projection_activities (
        activity_id     TEXT PRIMARY KEY,
        thread_id       TEXT NOT NULL,
        turn_id         TEXT,
        session_id      TEXT,
        sequence        INTEGER NOT NULL,
        tone            TEXT NOT NULL,
        kind            TEXT NOT NULL,
        summary         TEXT NOT NULL,
        payload_json    TEXT NOT NULL,
        created_at      TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_proj_act_thread_seq
        ON projection_activities(thread_id, sequence);
      CREATE INDEX IF NOT EXISTS idx_proj_act_thread_turn
        ON projection_activities(thread_id, turn_id);
      CREATE INDEX IF NOT EXISTS idx_proj_act_thread_session
        ON projection_activities(thread_id, session_id);
    `
      },
      {
        id: 6,
        name: "projection_checkpoints",
        sql: `
      CREATE TABLE IF NOT EXISTS projection_checkpoints (
        thread_id              TEXT NOT NULL,
        turn_id                TEXT NOT NULL,
        checkpoint_ref         TEXT NOT NULL,
        status                 TEXT NOT NULL,
        files_json             TEXT NOT NULL,
        assistant_message_id   TEXT,
        completed_at           TEXT NOT NULL,
        checkpoint_turn_count  INTEGER NOT NULL,
        PRIMARY KEY (thread_id, turn_id)
      );
    `
      },
      {
        id: 7,
        name: "projection_sessions",
        sql: `
      CREATE TABLE IF NOT EXISTS projection_sessions (
        session_id              TEXT NOT NULL,
        thread_id               TEXT NOT NULL,
        status                  TEXT NOT NULL,
        provider_name           TEXT,
        provider_instance_id    TEXT,
        role                    TEXT,
        display_name            TEXT,
        runtime_mode            TEXT NOT NULL,
        active_turn_id          TEXT,
        last_error              TEXT,
        updated_at              TEXT NOT NULL,
        PRIMARY KEY (thread_id, session_id)
      );
    `
      }
    ];
  }
});

// packages/daemon/src/persistence/types.ts
function makeInMemoryCursorStore() {
  const map = /* @__PURE__ */ new Map();
  return {
    load(name) {
      return map.get(name) ?? 0;
    },
    save(name, lastAppliedSequence) {
      map.set(name, lastAppliedSequence);
    }
  };
}
var ProjectionGapError;
var init_types = __esm({
  "packages/daemon/src/persistence/types.ts"() {
    "use strict";
    ProjectionGapError = class extends Error {
      constructor(projectionName, expectedSequence, observedSequence) {
        super(
          `Projection "${projectionName}" gap: expected sequence ${expectedSequence}, observed ${observedSequence}`
        );
        this.projectionName = projectionName;
        this.expectedSequence = expectedSequence;
        this.observedSequence = observedSequence;
        this.name = "ProjectionGapError";
      }
    };
  }
});

// packages/daemon/src/persistence/projections/turn-projection.ts
function isLegalTransition(from, to) {
  if (from === to) return true;
  if (TERMINAL_STATES.has(from)) return false;
  return TERMINAL_STATES.has(to);
}
function stripThreadId(record) {
  const { threadId: _omit, ...rest } = record;
  void _omit;
  return rest;
}
function makeTurnProjection(opts) {
  const name = opts.name ?? DEFAULT_NAME;
  const batchSize = opts.batchSize ?? DEFAULT_BATCH_SIZE;
  const log2 = opts.logger ?? (() => void 0);
  const byThread = /* @__PURE__ */ new Map();
  const latestByThread = /* @__PURE__ */ new Map();
  const latestByThreadSession = /* @__PURE__ */ new Map();
  const sessionKey = (threadId, sessionId) => `${threadId} ${sessionId}`;
  let cursor = 0;
  let unsubscribe = null;
  let started = false;
  function bucket(threadId) {
    let b = byThread.get(threadId);
    if (!b) {
      b = /* @__PURE__ */ new Map();
      byThread.set(threadId, b);
    }
    return b;
  }
  function applyTurnStarted(threadId, turnId, requestedAt, sourceProposedPlan) {
    const b = bucket(threadId);
    if (b.has(turnId)) {
      log2({
        level: "warn",
        msg: `turn projection: duplicate chat.turn.started for ${threadId}/${turnId}; ignoring`
      });
      return;
    }
    const record = {
      threadId,
      turnId,
      state: "running",
      requestedAt,
      startedAt: requestedAt,
      completedAt: null,
      assistantMessageId: null,
      ...sourceProposedPlan ? { sourceProposedPlan } : {}
    };
    b.set(turnId, record);
    latestByThread.set(threadId, turnId);
  }
  function applyTurnTransition(threadId, turnId, nextState, completedAt, assistantMessageId) {
    const b = byThread.get(threadId);
    const existing = b?.get(turnId);
    if (!existing) {
      log2({
        level: "warn",
        msg: `turn projection: ${nextState} event for unknown turn ${threadId}/${turnId}; skipping`
      });
      return;
    }
    if (!isLegalTransition(existing.state, nextState)) {
      log2({
        level: "warn",
        msg: `turn projection: illegal transition ${existing.state}\u2192${nextState} for ${threadId}/${turnId}; skipping`
      });
      return;
    }
    const next = {
      ...existing,
      state: nextState,
      completedAt: TERMINAL_STATES.has(nextState) ? completedAt : existing.completedAt,
      assistantMessageId: assistantMessageId !== void 0 ? assistantMessageId : existing.assistantMessageId
    };
    b.set(turnId, next);
  }
  function applySessionRemoved(threadId, sessionId) {
    latestByThreadSession.delete(sessionKey(threadId, sessionId));
  }
  function project(event, occurredAt) {
    switch (event.type) {
      case "chat.turn.started":
        applyTurnStarted(
          event.threadId,
          event.turnId,
          event.requestedAt,
          event.sourceProposedPlanRef
        );
        return;
      case "chat.turn.completed":
        applyTurnTransition(
          event.threadId,
          event.turnId,
          event.state,
          event.completedAt,
          event.assistantMessageId ?? null
        );
        return;
      case "chat.turn.aborted": {
        const nextState = ABORT_TO_STATE[event.reason];
        applyTurnTransition(event.threadId, event.turnId, nextState, occurredAt, void 0);
        return;
      }
      case "chat.session.removed":
        applySessionRemoved(event.threadId, event.sessionId);
        return;
      // All other event types are irrelevant to the turn projection.
      default:
        return;
    }
  }
  function ingest(persisted) {
    if (persisted.sequence !== cursor + 1) {
      throw new ProjectionGapError(name, cursor + 1, persisted.sequence);
    }
    project(persisted.event, persisted.occurredAt);
    cursor = persisted.sequence;
    opts.cursorStore.save(name, cursor);
  }
  function bootstrap() {
    cursor = opts.cursorStore.load(name);
    let batch = opts.reader.readFromSequence(cursor, batchSize);
    while (batch.length > 0) {
      for (const persisted of batch) {
        ingest(persisted);
      }
      batch = opts.reader.readFromSequence(cursor, batchSize);
    }
  }
  return {
    start() {
      if (started) return;
      started = true;
      bootstrap();
      unsubscribe = opts.reader.subscribe((event) => {
        ingest(event);
      });
    },
    stop() {
      if (unsubscribe) {
        unsubscribe();
        unsubscribe = null;
      }
      started = false;
    },
    cursor() {
      return cursor;
    },
    get(threadId, turnId) {
      return byThread.get(threadId)?.get(turnId) ?? null;
    },
    list(threadId) {
      const b = byThread.get(threadId);
      return b ? [...b.values()] : [];
    },
    listBySession(threadId, sessionId) {
      const b = byThread.get(threadId);
      if (!b) return [];
      return [...b.values()].filter((t) => t.sessionId === sessionId);
    },
    latest(threadId) {
      const turnId = latestByThread.get(threadId);
      if (!turnId) return null;
      const record = byThread.get(threadId)?.get(turnId);
      return record ? stripThreadId(record) : null;
    },
    latestForSession(threadId, sessionId) {
      const turnId = latestByThreadSession.get(sessionKey(threadId, sessionId));
      if (!turnId) return null;
      const record = byThread.get(threadId)?.get(turnId);
      return record ? stripThreadId(record) : null;
    }
  };
}
var DEFAULT_NAME, DEFAULT_BATCH_SIZE, TERMINAL_STATES, ABORT_TO_STATE;
var init_turn_projection = __esm({
  "packages/daemon/src/persistence/projections/turn-projection.ts"() {
    "use strict";
    init_types();
    DEFAULT_NAME = "turn";
    DEFAULT_BATCH_SIZE = 1e3;
    TERMINAL_STATES = /* @__PURE__ */ new Set([
      "completed",
      "interrupted",
      "error"
    ]);
    ABORT_TO_STATE = {
      cancelled: "interrupted",
      interrupted: "interrupted",
      error: "error"
    };
  }
});

// packages/daemon/src/persistence/projections/turn-diff-projection.ts
function normaliseDiffStatus(kind) {
  const k = kind.trim().toLowerCase();
  if (k === "added" || k === "add" || k === "create" || k === "created" || k === "a" || k === "new") {
    return "added";
  }
  if (k === "deleted" || k === "delete" || k === "remove" || k === "removed" || k === "d") {
    return "deleted";
  }
  if (k === "renamed" || k === "rename" || k === "moved" || k === "r" || k === "moved-to") {
    return "renamed";
  }
  return "modified";
}
function makeEntry(threadId, turnId, fileIndex, file) {
  return {
    threadId,
    turnId,
    fileIndex,
    path: file.path,
    status: normaliseDiffStatus(file.kind),
    additions: file.additions,
    deletions: file.deletions,
    rawKind: file.kind
  };
}
function makeTurnDiffProjection(opts) {
  const name = opts.name ?? DEFAULT_NAME2;
  const batchSize = opts.batchSize ?? DEFAULT_BATCH_SIZE2;
  const log2 = opts.logger ?? (() => void 0);
  const byTurn = /* @__PURE__ */ new Map();
  const turnsByThread = /* @__PURE__ */ new Map();
  let cursor = 0;
  let unsubscribe = null;
  let started = false;
  function applyCheckpointCreated(threadId, turnId, files) {
    if (byTurn.has(turnId)) {
      log2({
        level: "warn",
        msg: `turn-diff projection: duplicate chat.checkpoint.created for ${threadId}/${turnId}; ignoring`
      });
      return;
    }
    const entries = files.map((file, index) => makeEntry(threadId, turnId, index, file));
    byTurn.set(turnId, entries);
    let set = turnsByThread.get(threadId);
    if (!set) {
      set = /* @__PURE__ */ new Set();
      turnsByThread.set(threadId, set);
    }
    set.add(turnId);
  }
  function project(event) {
    if (event.type === "chat.checkpoint.created") {
      applyCheckpointCreated(event.threadId, event.checkpoint.turnId, event.checkpoint.files);
      return;
    }
  }
  function ingest(persisted) {
    if (persisted.sequence !== cursor + 1) {
      throw new ProjectionGapError(name, cursor + 1, persisted.sequence);
    }
    project(persisted.event);
    cursor = persisted.sequence;
    opts.cursorStore.save(name, cursor);
  }
  function bootstrap() {
    cursor = opts.cursorStore.load(name);
    let batch = opts.reader.readFromSequence(cursor, batchSize);
    while (batch.length > 0) {
      for (const persisted of batch) ingest(persisted);
      batch = opts.reader.readFromSequence(cursor, batchSize);
    }
  }
  return {
    start() {
      if (started) return;
      started = true;
      bootstrap();
      unsubscribe = opts.reader.subscribe((event) => {
        ingest(event);
      });
    },
    stop() {
      if (unsubscribe) {
        unsubscribe();
        unsubscribe = null;
      }
      started = false;
    },
    cursor() {
      return cursor;
    },
    listForTurn(turnId) {
      const entries = byTurn.get(turnId);
      return entries ? entries.slice() : [];
    },
    listForThread(threadId) {
      const result = {};
      const turnIds = turnsByThread.get(threadId);
      if (!turnIds) return result;
      for (const turnId of turnIds) {
        const entries = byTurn.get(turnId);
        if (entries) result[turnId] = entries.slice();
      }
      return result;
    },
    aggregateForThread(threadId) {
      const turnIds = turnsByThread.get(threadId);
      if (!turnIds || turnIds.size === 0) {
        return { totalAdditions: 0, totalDeletions: 0, filesChanged: 0 };
      }
      let totalAdditions = 0;
      let totalDeletions = 0;
      let filesChanged = 0;
      for (const turnId of turnIds) {
        const entries = byTurn.get(turnId);
        if (!entries) continue;
        for (const entry of entries) {
          totalAdditions += entry.additions;
          totalDeletions += entry.deletions;
          filesChanged += 1;
        }
      }
      return { totalAdditions, totalDeletions, filesChanged };
    }
  };
}
var DEFAULT_NAME2, DEFAULT_BATCH_SIZE2;
var init_turn_diff_projection = __esm({
  "packages/daemon/src/persistence/projections/turn-diff-projection.ts"() {
    "use strict";
    init_types();
    DEFAULT_NAME2 = "turn-diff";
    DEFAULT_BATCH_SIZE2 = 1e3;
  }
});

// packages/daemon/src/codex/errors.ts
var CodexError, CodexProtocolError, CodexRpcError, CodexAgentExitedError, CodexAgentSpawnError;
var init_errors5 = __esm({
  "packages/daemon/src/codex/errors.ts"() {
    "use strict";
    CodexError = class extends Error {
      constructor(message, options) {
        super(message, options);
        this.name = new.target.name;
      }
    };
    CodexProtocolError = class extends CodexError {
    };
    CodexRpcError = class extends CodexError {
      code;
      data;
      constructor(payload) {
        super(payload.message);
        this.code = payload.code;
        this.data = payload.data;
      }
    };
    CodexAgentExitedError = class extends CodexError {
      code;
      signal;
      constructor(message = "Codex agent exited", options) {
        super(message);
        this.code = options?.code ?? null;
        this.signal = options?.signal ?? null;
      }
    };
    CodexAgentSpawnError = class extends CodexError {
    };
  }
});

// packages/daemon/src/codex/methods.ts
var CODEX_METHODS, AGENT_METHODS2, CLIENT_METHODS2;
var init_methods2 = __esm({
  "packages/daemon/src/codex/methods.ts"() {
    "use strict";
    CODEX_METHODS = {
      initialize: "initialize",
      initialized: "initialized",
      thread_start: "thread/start",
      thread_resume: "thread/resume",
      thread_fork: "thread/fork",
      thread_archive: "thread/archive",
      thread_unsubscribe: "thread/unsubscribe",
      thread_name_set: "thread/name/set",
      thread_metadata_update: "thread/metadata/update",
      thread_unarchive: "thread/unarchive",
      thread_compact_start: "thread/compact/start",
      thread_shell_command: "thread/shellCommand",
      thread_rollback: "thread/rollback",
      thread_list: "thread/list",
      thread_loaded_list: "thread/loaded/list",
      thread_read: "thread/read",
      thread_turns_list: "thread/turns/list",
      thread_inject_items: "thread/inject_items",
      skills_list: "skills/list",
      marketplace_add: "marketplace/add",
      marketplace_remove: "marketplace/remove",
      plugin_list: "plugin/list",
      plugin_read: "plugin/read",
      app_list: "app/list",
      device_key_create: "device/key/create",
      device_key_public: "device/key/public",
      device_key_sign: "device/key/sign",
      fs_read_file: "fs/readFile",
      fs_write_file: "fs/writeFile",
      fs_create_directory: "fs/createDirectory",
      fs_get_metadata: "fs/getMetadata",
      fs_read_directory: "fs/readDirectory",
      fs_remove: "fs/remove",
      fs_copy: "fs/copy",
      fs_watch: "fs/watch",
      fs_unwatch: "fs/unwatch",
      skills_config_write: "skills/config/write",
      plugin_install: "plugin/install",
      plugin_uninstall: "plugin/uninstall",
      turn_start: "turn/start",
      turn_steer: "turn/steer",
      turn_interrupt: "turn/interrupt",
      review_start: "review/start",
      model_list: "model/list",
      experimental_feature_list: "experimentalFeature/list",
      experimental_feature_enablement_set: "experimentalFeature/enablement/set",
      mcp_server_oauth_login: "mcpServer/oauth/login",
      config_mcp_server_reload: "config/mcpServer/reload",
      mcp_server_status_list: "mcpServerStatus/list",
      mcp_server_resource_read: "mcpServer/resource/read",
      mcp_server_tool_call: "mcpServer/tool/call",
      windows_sandbox_setup_start: "windowsSandbox/setupStart",
      account_login_start: "account/login/start",
      account_login_cancel: "account/login/cancel",
      account_logout: "account/logout",
      account_rate_limits_read: "account/rateLimits/read",
      account_send_add_credits_nudge_email: "account/sendAddCreditsNudgeEmail",
      feedback_upload: "feedback/upload",
      command_exec: "command/exec",
      command_exec_write: "command/exec/write",
      command_exec_terminate: "command/exec/terminate",
      command_exec_resize: "command/exec/resize",
      config_read: "config/read",
      external_agent_config_detect: "externalAgentConfig/detect",
      external_agent_config_import: "externalAgentConfig/import",
      config_value_write: "config/value/write",
      config_batch_write: "config/batchWrite",
      config_requirements_read: "configRequirements/read",
      account_read: "account/read",
      get_conversation_summary: "getConversationSummary",
      git_diff_to_remote: "gitDiffToRemote",
      get_auth_status: "getAuthStatus",
      fuzzy_file_search: "fuzzyFileSearch",
      item_command_execution_request_approval: "item/commandExecution/requestApproval",
      item_file_change_request_approval: "item/fileChange/requestApproval",
      item_tool_request_user_input: "item/tool/requestUserInput",
      mcp_server_elicitation_request: "mcpServer/elicitation/request",
      item_permissions_request_approval: "item/permissions/requestApproval",
      item_tool_call: "item/tool/call",
      account_chatgpt_auth_tokens_refresh: "account/chatgptAuthTokens/refresh",
      apply_patch_approval: "applyPatchApproval",
      exec_command_approval: "execCommandApproval",
      error: "error",
      thread_started: "thread/started",
      thread_status_changed: "thread/status/changed",
      thread_archived: "thread/archived",
      thread_unarchived: "thread/unarchived",
      thread_closed: "thread/closed",
      skills_changed: "skills/changed",
      thread_name_updated: "thread/name/updated",
      thread_token_usage_updated: "thread/tokenUsage/updated",
      turn_started: "turn/started",
      hook_started: "hook/started",
      turn_completed: "turn/completed",
      hook_completed: "hook/completed",
      turn_diff_updated: "turn/diff/updated",
      turn_plan_updated: "turn/plan/updated",
      item_started: "item/started",
      item_auto_approval_review_started: "item/autoApprovalReview/started",
      item_auto_approval_review_completed: "item/autoApprovalReview/completed",
      item_completed: "item/completed",
      raw_response_item_completed: "rawResponseItem/completed",
      item_agent_message_delta: "item/agentMessage/delta",
      item_plan_delta: "item/plan/delta",
      command_exec_output_delta: "command/exec/outputDelta",
      item_command_execution_output_delta: "item/commandExecution/outputDelta",
      item_command_execution_terminal_interaction: "item/commandExecution/terminalInteraction",
      item_file_change_output_delta: "item/fileChange/outputDelta",
      item_file_change_patch_updated: "item/fileChange/patchUpdated",
      server_request_resolved: "serverRequest/resolved",
      item_mcp_tool_call_progress: "item/mcpToolCall/progress",
      mcp_server_oauth_login_completed: "mcpServer/oauthLogin/completed",
      mcp_server_startup_status_updated: "mcpServer/startupStatus/updated",
      account_updated: "account/updated",
      account_rate_limits_updated: "account/rateLimits/updated",
      app_list_updated: "app/list/updated",
      external_agent_config_import_completed: "externalAgentConfig/import/completed",
      fs_changed: "fs/changed",
      item_reasoning_summary_text_delta: "item/reasoning/summaryTextDelta",
      item_reasoning_summary_part_added: "item/reasoning/summaryPartAdded",
      item_reasoning_text_delta: "item/reasoning/textDelta",
      thread_compacted: "thread/compacted",
      model_rerouted: "model/rerouted",
      warning: "warning",
      deprecation_notice: "deprecationNotice",
      config_warning: "configWarning",
      fuzzy_file_search_session_updated: "fuzzyFileSearch/sessionUpdated",
      fuzzy_file_search_session_completed: "fuzzyFileSearch/sessionCompleted",
      thread_realtime_started: "thread/realtime/started",
      thread_realtime_item_added: "thread/realtime/itemAdded",
      thread_realtime_transcript_delta: "thread/realtime/transcript/delta",
      thread_realtime_transcript_done: "thread/realtime/transcript/done",
      thread_realtime_output_audio_delta: "thread/realtime/outputAudio/delta",
      thread_realtime_sdp: "thread/realtime/sdp",
      thread_realtime_error: "thread/realtime/error",
      thread_realtime_closed: "thread/realtime/closed",
      windows_world_writable_warning: "windows/worldWritableWarning",
      windows_sandbox_setup_completed: "windowsSandbox/setupCompleted",
      account_login_completed: "account/login/completed"
    };
    AGENT_METHODS2 = {
      initialize: CODEX_METHODS.initialize,
      thread_start: CODEX_METHODS.thread_start,
      thread_resume: CODEX_METHODS.thread_resume,
      thread_fork: CODEX_METHODS.thread_fork,
      thread_archive: CODEX_METHODS.thread_archive,
      thread_unsubscribe: CODEX_METHODS.thread_unsubscribe,
      thread_name_set: CODEX_METHODS.thread_name_set,
      thread_metadata_update: CODEX_METHODS.thread_metadata_update,
      thread_unarchive: CODEX_METHODS.thread_unarchive,
      thread_compact_start: CODEX_METHODS.thread_compact_start,
      thread_shell_command: CODEX_METHODS.thread_shell_command,
      thread_rollback: CODEX_METHODS.thread_rollback,
      thread_list: CODEX_METHODS.thread_list,
      thread_loaded_list: CODEX_METHODS.thread_loaded_list,
      thread_read: CODEX_METHODS.thread_read,
      thread_turns_list: CODEX_METHODS.thread_turns_list,
      thread_inject_items: CODEX_METHODS.thread_inject_items,
      skills_list: CODEX_METHODS.skills_list,
      marketplace_add: CODEX_METHODS.marketplace_add,
      marketplace_remove: CODEX_METHODS.marketplace_remove,
      plugin_list: CODEX_METHODS.plugin_list,
      plugin_read: CODEX_METHODS.plugin_read,
      app_list: CODEX_METHODS.app_list,
      device_key_create: CODEX_METHODS.device_key_create,
      device_key_public: CODEX_METHODS.device_key_public,
      device_key_sign: CODEX_METHODS.device_key_sign,
      fs_read_file: CODEX_METHODS.fs_read_file,
      fs_write_file: CODEX_METHODS.fs_write_file,
      fs_create_directory: CODEX_METHODS.fs_create_directory,
      fs_get_metadata: CODEX_METHODS.fs_get_metadata,
      fs_read_directory: CODEX_METHODS.fs_read_directory,
      fs_remove: CODEX_METHODS.fs_remove,
      fs_copy: CODEX_METHODS.fs_copy,
      fs_watch: CODEX_METHODS.fs_watch,
      fs_unwatch: CODEX_METHODS.fs_unwatch,
      skills_config_write: CODEX_METHODS.skills_config_write,
      plugin_install: CODEX_METHODS.plugin_install,
      plugin_uninstall: CODEX_METHODS.plugin_uninstall,
      turn_start: CODEX_METHODS.turn_start,
      turn_steer: CODEX_METHODS.turn_steer,
      turn_interrupt: CODEX_METHODS.turn_interrupt,
      review_start: CODEX_METHODS.review_start,
      model_list: CODEX_METHODS.model_list,
      experimental_feature_list: CODEX_METHODS.experimental_feature_list,
      experimental_feature_enablement_set: CODEX_METHODS.experimental_feature_enablement_set,
      mcp_server_oauth_login: CODEX_METHODS.mcp_server_oauth_login,
      config_mcp_server_reload: CODEX_METHODS.config_mcp_server_reload,
      mcp_server_status_list: CODEX_METHODS.mcp_server_status_list,
      mcp_server_resource_read: CODEX_METHODS.mcp_server_resource_read,
      mcp_server_tool_call: CODEX_METHODS.mcp_server_tool_call,
      windows_sandbox_setup_start: CODEX_METHODS.windows_sandbox_setup_start,
      account_login_start: CODEX_METHODS.account_login_start,
      account_login_cancel: CODEX_METHODS.account_login_cancel,
      account_logout: CODEX_METHODS.account_logout,
      account_rate_limits_read: CODEX_METHODS.account_rate_limits_read,
      account_send_add_credits_nudge_email: CODEX_METHODS.account_send_add_credits_nudge_email,
      feedback_upload: CODEX_METHODS.feedback_upload,
      command_exec: CODEX_METHODS.command_exec,
      command_exec_write: CODEX_METHODS.command_exec_write,
      command_exec_terminate: CODEX_METHODS.command_exec_terminate,
      command_exec_resize: CODEX_METHODS.command_exec_resize,
      config_read: CODEX_METHODS.config_read,
      external_agent_config_detect: CODEX_METHODS.external_agent_config_detect,
      external_agent_config_import: CODEX_METHODS.external_agent_config_import,
      config_value_write: CODEX_METHODS.config_value_write,
      config_batch_write: CODEX_METHODS.config_batch_write,
      config_requirements_read: CODEX_METHODS.config_requirements_read,
      account_read: CODEX_METHODS.account_read,
      get_conversation_summary: CODEX_METHODS.get_conversation_summary,
      git_diff_to_remote: CODEX_METHODS.git_diff_to_remote,
      get_auth_status: CODEX_METHODS.get_auth_status,
      fuzzy_file_search: CODEX_METHODS.fuzzy_file_search
    };
    CLIENT_METHODS2 = {
      initialized: CODEX_METHODS.initialized,
      item_command_execution_request_approval: CODEX_METHODS.item_command_execution_request_approval,
      item_file_change_request_approval: CODEX_METHODS.item_file_change_request_approval,
      item_tool_request_user_input: CODEX_METHODS.item_tool_request_user_input,
      mcp_server_elicitation_request: CODEX_METHODS.mcp_server_elicitation_request,
      item_permissions_request_approval: CODEX_METHODS.item_permissions_request_approval,
      item_tool_call: CODEX_METHODS.item_tool_call,
      account_chatgpt_auth_tokens_refresh: CODEX_METHODS.account_chatgpt_auth_tokens_refresh,
      apply_patch_approval: CODEX_METHODS.apply_patch_approval,
      exec_command_approval: CODEX_METHODS.exec_command_approval,
      error: CODEX_METHODS.error,
      thread_started: CODEX_METHODS.thread_started,
      thread_status_changed: CODEX_METHODS.thread_status_changed,
      thread_archived: CODEX_METHODS.thread_archived,
      thread_unarchived: CODEX_METHODS.thread_unarchived,
      thread_closed: CODEX_METHODS.thread_closed,
      skills_changed: CODEX_METHODS.skills_changed,
      thread_name_updated: CODEX_METHODS.thread_name_updated,
      thread_token_usage_updated: CODEX_METHODS.thread_token_usage_updated,
      turn_started: CODEX_METHODS.turn_started,
      hook_started: CODEX_METHODS.hook_started,
      turn_completed: CODEX_METHODS.turn_completed,
      hook_completed: CODEX_METHODS.hook_completed,
      turn_diff_updated: CODEX_METHODS.turn_diff_updated,
      turn_plan_updated: CODEX_METHODS.turn_plan_updated,
      item_started: CODEX_METHODS.item_started,
      item_auto_approval_review_started: CODEX_METHODS.item_auto_approval_review_started,
      item_auto_approval_review_completed: CODEX_METHODS.item_auto_approval_review_completed,
      item_completed: CODEX_METHODS.item_completed,
      raw_response_item_completed: CODEX_METHODS.raw_response_item_completed,
      item_agent_message_delta: CODEX_METHODS.item_agent_message_delta,
      item_plan_delta: CODEX_METHODS.item_plan_delta,
      command_exec_output_delta: CODEX_METHODS.command_exec_output_delta,
      item_command_execution_output_delta: CODEX_METHODS.item_command_execution_output_delta,
      item_command_execution_terminal_interaction: CODEX_METHODS.item_command_execution_terminal_interaction,
      item_file_change_output_delta: CODEX_METHODS.item_file_change_output_delta,
      item_file_change_patch_updated: CODEX_METHODS.item_file_change_patch_updated,
      server_request_resolved: CODEX_METHODS.server_request_resolved,
      item_mcp_tool_call_progress: CODEX_METHODS.item_mcp_tool_call_progress,
      mcp_server_oauth_login_completed: CODEX_METHODS.mcp_server_oauth_login_completed,
      mcp_server_startup_status_updated: CODEX_METHODS.mcp_server_startup_status_updated,
      account_updated: CODEX_METHODS.account_updated,
      account_rate_limits_updated: CODEX_METHODS.account_rate_limits_updated,
      app_list_updated: CODEX_METHODS.app_list_updated,
      external_agent_config_import_completed: CODEX_METHODS.external_agent_config_import_completed,
      fs_changed: CODEX_METHODS.fs_changed,
      item_reasoning_summary_text_delta: CODEX_METHODS.item_reasoning_summary_text_delta,
      item_reasoning_summary_part_added: CODEX_METHODS.item_reasoning_summary_part_added,
      item_reasoning_text_delta: CODEX_METHODS.item_reasoning_text_delta,
      thread_compacted: CODEX_METHODS.thread_compacted,
      model_rerouted: CODEX_METHODS.model_rerouted,
      warning: CODEX_METHODS.warning,
      deprecation_notice: CODEX_METHODS.deprecation_notice,
      config_warning: CODEX_METHODS.config_warning,
      fuzzy_file_search_session_updated: CODEX_METHODS.fuzzy_file_search_session_updated,
      fuzzy_file_search_session_completed: CODEX_METHODS.fuzzy_file_search_session_completed,
      thread_realtime_started: CODEX_METHODS.thread_realtime_started,
      thread_realtime_item_added: CODEX_METHODS.thread_realtime_item_added,
      thread_realtime_transcript_delta: CODEX_METHODS.thread_realtime_transcript_delta,
      thread_realtime_transcript_done: CODEX_METHODS.thread_realtime_transcript_done,
      thread_realtime_output_audio_delta: CODEX_METHODS.thread_realtime_output_audio_delta,
      thread_realtime_sdp: CODEX_METHODS.thread_realtime_sdp,
      thread_realtime_error: CODEX_METHODS.thread_realtime_error,
      thread_realtime_closed: CODEX_METHODS.thread_realtime_closed,
      windows_world_writable_warning: CODEX_METHODS.windows_world_writable_warning,
      windows_sandbox_setup_completed: CODEX_METHODS.windows_sandbox_setup_completed,
      account_login_completed: CODEX_METHODS.account_login_completed
    };
  }
});

// packages/daemon/src/codex/protocol.ts
function isObject2(value) {
  return typeof value === "object" && value !== null;
}
function isJsonRpcId(value) {
  return typeof value === "number" || typeof value === "string";
}
function isErrorPayload(value) {
  return isObject2(value) && typeof value.code === "number" && typeof value.message === "string" && (value.data === void 0 || true);
}
function isCodexRequest(value) {
  return isObject2(value) && isJsonRpcId(value.id) && typeof value.method === "string";
}
function isCodexNotification(value) {
  return isObject2(value) && value.id === void 0 && typeof value.method === "string";
}
function isCodexResponse(value) {
  return isObject2(value) && isJsonRpcId(value.id) && value.method === void 0;
}
function makeJsonRpcEndpoint2(opts) {
  let nextId = 1;
  let buffer = "";
  let isClosed = false;
  let incomingRequestHandler = null;
  let resolveClosed;
  const pending = /* @__PURE__ */ new Map();
  const notificationHandlers = /* @__PURE__ */ new Set();
  const closed = new Promise((resolve35) => {
    resolveClosed = resolve35;
  });
  function log2(direction, payload) {
    opts.logger?.({ direction, payload });
  }
  function finish(reason = new CodexAgentExitedError()) {
    if (isClosed) return;
    isClosed = true;
    for (const request of pending.values()) request.reject(reason);
    pending.clear();
    resolveClosed();
  }
  function write(payload) {
    if (isClosed) return;
    log2("out", payload);
    try {
      opts.output.write(`${JSON.stringify(payload)}
`);
    } catch (err) {
      finish(new CodexProtocolError("Failed to encode Codex protocol message", { cause: err }));
    }
  }
  function respond(id, response) {
    write({ id, ...response });
  }
  async function handleIncomingRequest(request) {
    try {
      if (!incomingRequestHandler) {
        respond(request.id, {
          error: { code: -32601, message: `Method not found: ${request.method}` }
        });
        return;
      }
      const result = await incomingRequestHandler(request);
      respond(request.id, { result });
    } catch (err) {
      if (err instanceof CodexRpcError) {
        respond(request.id, { error: { code: err.code, message: err.message, data: err.data } });
        return;
      }
      const message = err instanceof Error ? err.message : String(err);
      respond(request.id, { error: { code: -32603, message } });
    }
  }
  function handleFrame(payload) {
    log2("in", payload);
    if (isCodexResponse(payload)) {
      const request = pending.get(payload.id);
      if (!request) return;
      pending.delete(payload.id);
      if (payload.error !== void 0) {
        if (!isErrorPayload(payload.error)) {
          request.reject(new CodexProtocolError("Invalid Codex error response"));
          return;
        }
        request.reject(new CodexRpcError(payload.error));
      } else {
        request.resolve(payload.result);
      }
      return;
    }
    if (isCodexNotification(payload)) {
      for (const handler of notificationHandlers) handler(payload);
      return;
    }
    if (isCodexRequest(payload)) {
      void handleIncomingRequest(payload);
      return;
    }
    finish(new CodexProtocolError("Invalid Codex protocol frame"));
  }
  function handleChunk(chunk) {
    buffer += Buffer.isBuffer(chunk) ? chunk.toString("utf8") : String(chunk);
    for (; ; ) {
      const newline = buffer.indexOf("\n");
      if (newline === -1) break;
      const line = buffer.slice(0, newline).trim();
      buffer = buffer.slice(newline + 1);
      if (!line) continue;
      try {
        handleFrame(JSON.parse(line));
      } catch (err) {
        finish(new CodexProtocolError("Invalid Codex protocol JSON", { cause: err }));
      }
    }
  }
  opts.input.on("data", handleChunk);
  opts.input.on(
    "error",
    (err) => finish(new CodexProtocolError("Codex input stream failed", { cause: err }))
  );
  opts.input.on("close", () => finish(new CodexAgentExitedError()));
  opts.input.on("end", () => finish(new CodexAgentExitedError()));
  opts.output.on?.(
    "error",
    (err) => finish(new CodexProtocolError("Codex output stream failed", { cause: err }))
  );
  return {
    request(method, params) {
      if (isClosed) return Promise.reject(new CodexAgentExitedError());
      const id = nextId++;
      const envelope = { id, method };
      if (params !== void 0) envelope.params = params;
      const promise = new Promise((resolve35, reject) => {
        pending.set(id, { resolve: resolve35, reject });
      });
      write(envelope);
      return promise;
    },
    notify(method, params) {
      const envelope = { method };
      if (params !== void 0) envelope.params = params;
      write(envelope);
    },
    onNotification(handler) {
      notificationHandlers.add(handler);
      return () => notificationHandlers.delete(handler);
    },
    onIncomingRequest(handler) {
      incomingRequestHandler = handler;
      return () => {
        if (incomingRequestHandler === handler) incomingRequestHandler = null;
      };
    },
    async close(reason) {
      finish(reason);
      opts.input.off?.("data", handleChunk);
      const output = opts.output;
      if (!output.destroyed) output.end?.();
      await closed;
    },
    closed
  };
}
var init_protocol2 = __esm({
  "packages/daemon/src/codex/protocol.ts"() {
    "use strict";
    init_errors5();
  }
});

// packages/daemon/src/codex/schema.ts
import { z as z19 } from "zod";
function defaultInitializeRequest() {
  return {
    clientInfo: { name: "tmux-ide", title: "tmux-ide", version: "0.0.1" },
    capabilities: { experimentalApi: true, optOutNotificationMethods: null }
  };
}
var JsonObjectZ, ApplyPatchFileChangeZ, ApplyPatchApprovalRequestZ, ApplyPatchApprovalDecisionZ, ApplyPatchApprovalResponseZ, ChatgptAuthTokensRefreshRequestZ, ChatgptAuthTokensRefreshResponseZ, TurnSummaryZ, AgentMessageDeltaZ, ReasoningTextDeltaZ, ReasoningSummaryTextDeltaZ, ReasoningSummaryPartAddedZ, TurnNotificationZ, ItemCompletedZ, CodexAgentEventZ;
var init_schema2 = __esm({
  "packages/daemon/src/codex/schema.ts"() {
    "use strict";
    init_methods2();
    JsonObjectZ = z19.record(z19.string(), z19.unknown());
    ApplyPatchFileChangeZ = z19.discriminatedUnion("type", [
      z19.object({ type: z19.literal("add"), content: z19.string() }),
      z19.object({ type: z19.literal("delete"), content: z19.string() }),
      z19.object({
        type: z19.literal("update"),
        unified_diff: z19.string(),
        move_path: z19.string().nullable().optional()
      })
    ]);
    ApplyPatchApprovalRequestZ = z19.object({
      callId: z19.string(),
      conversationId: z19.string(),
      fileChanges: z19.record(z19.string(), ApplyPatchFileChangeZ),
      grantRoot: z19.string().nullable().optional(),
      reason: z19.string().nullable().optional()
    });
    ApplyPatchApprovalDecisionZ = z19.union([
      z19.literal("approved"),
      z19.literal("approved_for_session"),
      z19.literal("denied"),
      z19.literal("timed_out"),
      z19.literal("abort"),
      z19.object({
        approved_execpolicy_amendment: z19.object({
          proposed_execpolicy_amendment: z19.array(z19.string())
        })
      }),
      z19.object({
        network_policy_amendment: z19.object({
          network_policy_amendment: z19.object({
            action: z19.enum(["allow", "deny"]),
            host: z19.string()
          })
        })
      })
    ]);
    ApplyPatchApprovalResponseZ = z19.object({
      decision: ApplyPatchApprovalDecisionZ
    });
    ChatgptAuthTokensRefreshRequestZ = z19.object({
      previousAccountId: z19.string().nullable().optional(),
      reason: z19.literal("unauthorized")
    });
    ChatgptAuthTokensRefreshResponseZ = z19.object({
      accessToken: z19.string(),
      chatgptAccountId: z19.string(),
      chatgptPlanType: z19.string().nullable().optional()
    });
    TurnSummaryZ = z19.object({ id: z19.string(), status: z19.string().optional() }).catchall(z19.unknown());
    AgentMessageDeltaZ = z19.object({
      delta: z19.string(),
      itemId: z19.string(),
      threadId: z19.string(),
      turnId: z19.string()
    });
    ReasoningTextDeltaZ = AgentMessageDeltaZ.extend({ contentIndex: z19.number().int() });
    ReasoningSummaryTextDeltaZ = AgentMessageDeltaZ.extend({ summaryIndex: z19.number().int() });
    ReasoningSummaryPartAddedZ = z19.object({
      itemId: z19.string(),
      summaryIndex: z19.number().int(),
      threadId: z19.string(),
      turnId: z19.string()
    });
    TurnNotificationZ = z19.object({ threadId: z19.string(), turn: TurnSummaryZ });
    ItemCompletedZ = z19.object({ item: JsonObjectZ, threadId: z19.string(), turnId: z19.string() });
    CodexAgentEventZ = z19.discriminatedUnion("method", [
      z19.object({
        method: z19.literal(CLIENT_METHODS2.item_agent_message_delta),
        params: AgentMessageDeltaZ
      }),
      z19.object({
        method: z19.literal(CLIENT_METHODS2.item_reasoning_summary_text_delta),
        params: ReasoningSummaryTextDeltaZ
      }),
      z19.object({
        method: z19.literal(CLIENT_METHODS2.item_reasoning_text_delta),
        params: ReasoningTextDeltaZ
      }),
      z19.object({
        method: z19.literal(CLIENT_METHODS2.item_reasoning_summary_part_added),
        params: ReasoningSummaryPartAddedZ
      }),
      z19.object({ method: z19.literal(CLIENT_METHODS2.turn_started), params: TurnNotificationZ }),
      z19.object({ method: z19.literal(CLIENT_METHODS2.turn_completed), params: TurnNotificationZ }),
      z19.object({ method: z19.literal(CLIENT_METHODS2.item_completed), params: ItemCompletedZ }),
      z19.object({ method: z19.literal(CLIENT_METHODS2.error), params: JsonObjectZ })
    ]);
  }
});

// packages/daemon/src/codex/client.ts
import { spawn as spawn4 } from "node:child_process";
import { once as once2 } from "node:events";
import { accessSync as accessSync2, constants as constants2 } from "node:fs";
function resolveFromPath2(binary) {
  const paths = process.env.PATH?.split(":") ?? [];
  for (const dir of paths) {
    const candidate = `${dir}/${binary}`;
    try {
      accessSync2(candidate, constants2.X_OK);
      return candidate;
    } catch {
    }
  }
  return null;
}
function assertExecutable2(binary) {
  if (binary.includes("/")) {
    try {
      accessSync2(binary, constants2.X_OK);
      return binary;
    } catch (err) {
      throw new CodexAgentSpawnError(`Codex binary is not executable: ${binary}`, { cause: err });
    }
  }
  const resolved2 = resolveFromPath2(binary);
  if (!resolved2) throw new CodexAgentSpawnError(`Codex binary not found on PATH: ${binary}`);
  return resolved2;
}
function resolveProvider2(provider) {
  if (provider.binary) return { command: assertExecutable2(provider.binary), args: [] };
  return { command: assertExecutable2("codex"), args: ["app-server"] };
}
function writeStderrLines2(child, logger2) {
  let buffer = "";
  child.stderr.on("data", (chunk) => {
    buffer += Buffer.isBuffer(chunk) ? chunk.toString("utf8") : String(chunk);
    for (; ; ) {
      const newline = buffer.indexOf("\n");
      if (newline === -1) break;
      const line = buffer.slice(0, newline);
      buffer = buffer.slice(newline + 1);
      if (line) logger2?.({ direction: "in", payload: { _stderr: true, line } });
    }
  });
  child.stderr.on("close", () => {
    if (buffer) logger2?.({ direction: "in", payload: { _stderr: true, line: buffer } });
  });
}
function isAgentEventMethod(method) {
  return method === CLIENT_METHODS2.item_agent_message_delta || method === CLIENT_METHODS2.item_reasoning_summary_text_delta || method === CLIENT_METHODS2.item_reasoning_summary_part_added || method === CLIENT_METHODS2.item_reasoning_text_delta || method === CLIENT_METHODS2.turn_started || method === CLIENT_METHODS2.turn_completed || method === CLIENT_METHODS2.item_completed || method === CLIENT_METHODS2.error;
}
async function spawnCodexClient(opts) {
  const resolved2 = resolveProvider2(opts.provider);
  const child = spawn4(resolved2.command, resolved2.args, {
    cwd: opts.cwd,
    env: { ...process.env, ...opts.env },
    stdio: ["pipe", "pipe", "pipe"]
  });
  const spawnFailure = await Promise.race([
    once2(child, "spawn").then(() => null),
    once2(child, "error").then(([err]) => err),
    once2(child, "exit").then(
      ([code, signal]) => new CodexAgentExitedError("Codex agent exited during spawn", {
        code,
        signal
      })
    )
  ]);
  if (spawnFailure) {
    throw spawnFailure instanceof CodexAgentExitedError ? spawnFailure : new CodexAgentSpawnError(`Failed to spawn Codex agent: ${spawnFailure.message}`, {
      cause: spawnFailure
    });
  }
  writeStderrLines2(child, opts.logger);
  const endpoint = makeJsonRpcEndpoint2({
    input: child.stdout,
    output: child.stdin,
    logger: opts.logger
  });
  const agentEventHandlers = /* @__PURE__ */ new Set();
  let applyPatchApprovalHandler = null;
  let chatgptTokenRefreshHandler = null;
  endpoint.onNotification((notification) => {
    if (!isAgentEventMethod(notification.method)) return;
    const event = CodexAgentEventZ.parse({
      method: notification.method,
      params: notification.params
    });
    for (const handler of agentEventHandlers) handler(event);
  });
  endpoint.onIncomingRequest(async (request) => {
    if (request.method === CLIENT_METHODS2.apply_patch_approval) {
      if (!applyPatchApprovalHandler) {
        throw new CodexProtocolError("No apply-patch approval handler registered");
      }
      const parsed = ApplyPatchApprovalRequestZ.parse(request.params);
      return ApplyPatchApprovalResponseZ.parse(await applyPatchApprovalHandler(parsed));
    }
    if (request.method === CLIENT_METHODS2.account_chatgpt_auth_tokens_refresh) {
      if (!chatgptTokenRefreshHandler) {
        throw new CodexRpcError({
          code: -32603,
          message: "ChatGPT token refresh is not available"
        });
      }
      const parsed = ChatgptAuthTokensRefreshRequestZ.parse(request.params);
      return ChatgptAuthTokensRefreshResponseZ.parse(await chatgptTokenRefreshHandler(parsed));
    }
    throw new CodexRpcError({ code: -32601, message: `Method not found: ${request.method}` });
  });
  const closed = once2(child, "exit").then(([code, signal]) => ({
    code,
    signal
  }));
  void closed.then(({ code, signal }) => {
    void endpoint.close(new CodexAgentExitedError("Codex agent exited", { code, signal }));
  });
  return {
    async initialize() {
      return await endpoint.request(
        AGENT_METHODS2.initialize,
        defaultInitializeRequest()
      );
    },
    async newConversation(req) {
      return await endpoint.request(AGENT_METHODS2.thread_start, req);
    },
    async sendUserMessage(req) {
      return await endpoint.request(AGENT_METHODS2.turn_start, req);
    },
    async interrupt(req) {
      await endpoint.request(AGENT_METHODS2.turn_interrupt, req);
    },
    onAgentEvent(handler) {
      agentEventHandlers.add(handler);
      return () => agentEventHandlers.delete(handler);
    },
    onApplyPatchApproval(handler) {
      applyPatchApprovalHandler = handler;
      return () => {
        if (applyPatchApprovalHandler === handler) applyPatchApprovalHandler = null;
      };
    },
    onChatgptTokenRefresh(handler) {
      chatgptTokenRefreshHandler = handler;
      return () => {
        if (chatgptTokenRefreshHandler === handler) chatgptTokenRefreshHandler = null;
      };
    },
    async close() {
      if (!child.killed) child.kill("SIGTERM");
      await endpoint.close(new CodexAgentExitedError("Codex client closed"));
      await closed;
    },
    closed
  };
}
var init_client3 = __esm({
  "packages/daemon/src/codex/client.ts"() {
    "use strict";
    init_errors5();
    init_methods2();
    init_protocol2();
    init_schema2();
  }
});

// packages/daemon/src/codex/index.ts
var init_codex = __esm({
  "packages/daemon/src/codex/index.ts"() {
    "use strict";
    init_client3();
    init_errors5();
    init_methods2();
    init_protocol2();
    init_schema2();
  }
});

// packages/daemon/src/codex/debug-log.ts
import { appendFileSync as appendFileSync6, mkdirSync as mkdirSync16 } from "node:fs";
import { homedir as homedir6 } from "node:os";
import { join as join18 } from "node:path";
function isRecord2(value) {
  return typeof value === "object" && value !== null;
}
function createCodexDebugLogger(threadId) {
  if (process.env.TMUX_IDE_CODEX_LOG !== "1") return void 0;
  const dir = join18(homedir6(), ".tmux-ide", "logs");
  const file = join18(dir, "codex-events.ndjson");
  mkdirSync16(dir, { recursive: true });
  return (event) => {
    const payload = event.payload;
    const record = {
      ts: (/* @__PURE__ */ new Date()).toISOString(),
      threadId,
      direction: event.direction
    };
    if (isRecord2(payload)) {
      if (typeof payload.method === "string") record.method = payload.method;
      if ("params" in payload) record.params = payload.params;
      if (typeof payload.line === "string") record.raw = payload.line;
      if ("_stderr" in payload && typeof payload.line === "string") record.raw = payload.line;
      if (!("raw" in record) && typeof payload.method !== "string") {
        record.raw = JSON.stringify(payload);
      }
    } else if (typeof payload === "string") {
      record.raw = payload;
    }
    appendFileSync6(file, `${JSON.stringify(record)}
`, "utf-8");
  };
}
var init_debug_log = __esm({
  "packages/daemon/src/codex/debug-log.ts"() {
    "use strict";
  }
});

// packages/daemon/src/chat/session-store.ts
import { randomUUID as randomUUID3 } from "node:crypto";
function makeSessionStore(opts = {}) {
  const now = opts.now ?? (() => /* @__PURE__ */ new Date());
  const randomId = opts.randomId ?? randomUUID3;
  const emit = opts.emit ?? (() => void 0);
  const byThread = /* @__PURE__ */ new Map();
  function bucket(threadId) {
    let b = byThread.get(threadId);
    if (!b) {
      b = /* @__PURE__ */ new Map();
      byThread.set(threadId, b);
    }
    return b;
  }
  return {
    add(input) {
      const b = bucket(input.threadId);
      const id = input.id ?? randomId();
      if (b.has(id)) {
        throw new SessionStoreError(
          `Session ${id} already exists on thread ${input.threadId}`,
          "duplicate"
        );
      }
      const session = {
        id,
        threadId: input.threadId,
        status: input.status ?? "idle",
        providerName: input.providerName,
        ...input.providerInstanceId ? { providerInstanceId: input.providerInstanceId } : {},
        runtimeMode: input.runtimeMode ?? "full-access",
        ...input.role ? { role: input.role } : {},
        ...input.displayName ? { displayName: input.displayName } : {},
        activeTurnId: input.activeTurnId ?? null,
        lastError: null,
        updatedAt: now().toISOString()
      };
      b.set(id, session);
      emit({ type: "chat.session.added", threadId: input.threadId, session });
      return session;
    },
    remove(threadId, sessionId) {
      const b = byThread.get(threadId);
      const existing = b?.get(sessionId) ?? null;
      if (!existing) return null;
      b.delete(sessionId);
      emit({ type: "chat.session.removed", threadId, sessionId });
      return existing;
    },
    get(threadId, sessionId) {
      return byThread.get(threadId)?.get(sessionId) ?? null;
    },
    list(threadId) {
      const b = byThread.get(threadId);
      return b ? [...b.values()] : [];
    },
    updateStatus(input) {
      const b = byThread.get(input.threadId);
      const existing = b?.get(input.sessionId);
      if (!existing) {
        throw new SessionStoreError(
          `Session ${input.sessionId} not found on thread ${input.threadId}`,
          "not_found"
        );
      }
      const next = {
        ...existing,
        status: input.status,
        lastError: input.lastError !== void 0 ? input.lastError : existing.lastError,
        activeTurnId: input.activeTurnId !== void 0 ? input.activeTurnId : existing.activeTurnId,
        updatedAt: now().toISOString()
      };
      b.set(input.sessionId, next);
      emit({
        type: "chat.session.status-changed",
        threadId: input.threadId,
        sessionId: input.sessionId,
        status: next.status,
        ...next.lastError !== void 0 ? { lastError: next.lastError } : {},
        ...next.activeTurnId !== void 0 ? { activeTurnId: next.activeTurnId } : {}
      });
      return next;
    },
    clear(threadId) {
      byThread.delete(threadId);
    }
  };
}
var SessionStoreError;
var init_session_store = __esm({
  "packages/daemon/src/chat/session-store.ts"() {
    "use strict";
    SessionStoreError = class extends Error {
      constructor(message, code) {
        super(message);
        this.code = code;
        this.name = "SessionStoreError";
      }
    };
  }
});

// packages/daemon/src/chat/usage-extraction.ts
function isRecord3(value) {
  return typeof value === "object" && value !== null;
}
function numberValue(value) {
  return typeof value === "number" && Number.isFinite(value) && value >= 0 ? value : void 0;
}
function firstNumber(source, keys) {
  for (const key of keys) {
    const value = numberValue(source[key]);
    if (value !== void 0) return value;
  }
  return void 0;
}
function costUsd(value) {
  if (numberValue(value) !== void 0) return numberValue(value);
  if (!isRecord3(value)) return void 0;
  const amount = firstNumber(value, ["totalCostUsd", "total_cost_usd", "costUsd", "cost_usd", "amount"]) ?? void 0;
  const currency = typeof value.currency === "string" ? value.currency.toUpperCase() : "USD";
  return amount !== void 0 && currency === "USD" ? amount : void 0;
}
function extractTokenUsage(value, tokensAreCumulative = false) {
  if (!isRecord3(value)) return null;
  const total = isRecord3(value.total) ? value.total : null;
  if (total) {
    return {
      inputTokens: firstNumber(total, ["inputTokens", "input_tokens"]),
      outputTokens: firstNumber(total, ["outputTokens", "output_tokens"]),
      cacheReadTokens: firstNumber(total, [
        "cachedInputTokens",
        "cacheReadTokens",
        "cache_read_tokens"
      ]),
      cacheWriteTokens: firstNumber(total, ["cacheWriteTokens", "cache_write_tokens"]),
      contextWindowMaxTokens: firstNumber(value, ["modelContextWindow", "contextWindowMaxTokens"]),
      contextWindowUsedTokens: firstNumber(total, [
        "totalTokens",
        "total_tokens",
        "contextWindowUsedTokens"
      ]),
      tokensAreCumulative: true
    };
  }
  const patch = {
    inputTokens: firstNumber(value, ["inputTokens", "input_tokens", "promptTokens"]),
    outputTokens: firstNumber(value, [
      "outputTokens",
      "output_tokens",
      "completionTokens",
      "reasoningOutputTokens"
    ]),
    cacheReadTokens: firstNumber(value, [
      "cacheReadTokens",
      "cache_read_tokens",
      "cachedInputTokens",
      "cache_read_input_tokens"
    ]),
    cacheWriteTokens: firstNumber(value, [
      "cacheWriteTokens",
      "cache_write_tokens",
      "cacheCreationInputTokens",
      "cache_creation_input_tokens"
    ]),
    contextWindowMaxTokens: firstNumber(value, [
      "contextWindowMaxTokens",
      "context_window_max_tokens",
      "modelContextWindow",
      "size"
    ]),
    contextWindowUsedTokens: firstNumber(value, [
      "contextWindowUsedTokens",
      "context_window_used_tokens",
      "used",
      "totalTokens",
      "total_tokens"
    ]),
    totalCostUsd: costUsd(value.cost) ?? costUsd(value.totalCostUsd) ?? costUsd(value.total_cost_usd),
    ...tokensAreCumulative ? { tokensAreCumulative: true } : {}
  };
  return [
    patch.inputTokens,
    patch.outputTokens,
    patch.cacheReadTokens,
    patch.cacheWriteTokens,
    patch.totalCostUsd,
    patch.contextWindowMaxTokens,
    patch.contextWindowUsedTokens
  ].some((candidate) => candidate !== void 0) ? patch : null;
}
function mergeUsage(previous, patch) {
  const addOrSet = (current, next) => {
    if (next === void 0) return current;
    return patch.tokensAreCumulative ? next : (current ?? 0) + next;
  };
  const merged = {
    inputTokens: addOrSet(previous?.inputTokens, patch.inputTokens) ?? previous?.inputTokens ?? 0,
    outputTokens: addOrSet(previous?.outputTokens, patch.outputTokens) ?? previous?.outputTokens ?? 0
  };
  const cacheReadTokens = addOrSet(previous?.cacheReadTokens, patch.cacheReadTokens);
  const cacheWriteTokens = addOrSet(previous?.cacheWriteTokens, patch.cacheWriteTokens);
  if (cacheReadTokens !== void 0) merged.cacheReadTokens = cacheReadTokens;
  if (cacheWriteTokens !== void 0) merged.cacheWriteTokens = cacheWriteTokens;
  if (patch.totalCostUsd !== void 0) merged.totalCostUsd = patch.totalCostUsd;
  else if (previous?.totalCostUsd !== void 0) merged.totalCostUsd = previous.totalCostUsd;
  if (patch.contextWindowMaxTokens !== void 0) {
    merged.contextWindowMaxTokens = patch.contextWindowMaxTokens;
  } else if (previous?.contextWindowMaxTokens !== void 0) {
    merged.contextWindowMaxTokens = previous.contextWindowMaxTokens;
  }
  if (patch.contextWindowUsedTokens !== void 0) {
    merged.contextWindowUsedTokens = patch.contextWindowUsedTokens;
  } else if (previous?.contextWindowUsedTokens !== void 0) {
    merged.contextWindowUsedTokens = previous.contextWindowUsedTokens;
  }
  return merged;
}
function usageChanged(left, right) {
  return JSON.stringify(left ?? null) !== JSON.stringify(right);
}
function extractUsagePatch(value) {
  if (!isRecord3(value)) return null;
  if (value.sessionUpdate === "usage_update") {
    const patch = extractTokenUsage(value);
    const usd2 = costUsd(value.cost);
    if (!patch && usd2 === void 0) return null;
    return { ...patch ?? {}, ...usd2 !== void 0 ? { totalCostUsd: usd2 } : {} };
  }
  for (const candidate of [
    value.usage,
    value.tokenUsage,
    value.tokens,
    isRecord3(value._meta) ? value._meta.usage : void 0,
    isRecord3(value._meta) ? value._meta.tokenUsage : void 0,
    isRecord3(value._meta) ? value._meta.tokens : void 0
  ]) {
    const patch = extractTokenUsage(candidate);
    if (patch) return patch;
  }
  const usd = costUsd(value.cost) ?? (isRecord3(value._meta) ? costUsd(value._meta.cost) : void 0);
  return usd !== void 0 ? { totalCostUsd: usd } : null;
}
var init_usage_extraction = __esm({
  "packages/daemon/src/chat/usage-extraction.ts"() {
    "use strict";
  }
});

// packages/daemon/src/chat/codex-helpers.ts
function stopReasonFromResponse(value) {
  const response = value;
  const reason = response.stopReason ?? response.stop_reason;
  if (reason === "end_turn" || reason === "max_tokens" || reason === "max_turn_requests" || reason === "refusal" || reason === "cancelled") {
    return reason;
  }
  return "end_turn";
}
function codexInputFromContent(content) {
  return content.map((block) => {
    if (block.type === "text") return { type: "text", text: block.text };
    if (block.type === "image" && block.uri) return { type: "image", url: block.uri };
    if (block.type === "resource_link") {
      return { type: "mention", name: block.name, path: block.uri };
    }
    return { type: "text", text: JSON.stringify(block) };
  });
}
function codexMeta(value) {
  return { provider: "codex", ...value };
}
function codexApplyPatchToPermission(threadId, req) {
  const paths = Object.keys(req.fileChanges);
  const title = req.reason ?? (paths.length === 1 ? `Edit ${paths[0]}` : `Edit ${paths.length} files`);
  return {
    sessionId: threadId,
    _meta: codexMeta({ codexRequest: req }),
    toolCall: {
      toolCallId: req.callId,
      title,
      kind: "edit",
      status: "pending",
      rawInput: req,
      _meta: codexMeta({ conversationId: req.conversationId }),
      content: paths.map((path4) => {
        const change = req.fileChanges[path4];
        if (!change) {
          return {
            type: "content",
            content: { type: "text", text: `Unknown change for ${path4}` }
          };
        }
        if (change.type === "add") {
          return { type: "diff", path: path4, oldText: "", newText: change.content };
        }
        if (change.type === "delete") {
          return { type: "diff", path: path4, oldText: change.content, newText: "" };
        }
        return {
          type: "diff",
          path: change.move_path ?? path4,
          oldText: "",
          newText: change.unified_diff
        };
      })
    },
    options: [
      { optionId: "allow_once", name: "Allow once", kind: "allow_once" },
      { optionId: "allow_always", name: "Allow for session", kind: "allow_always" },
      { optionId: "reject_once", name: "Reject", kind: "reject_once" }
    ]
  };
}
function codexApplyPatchResponseFromPermission(response) {
  if (response.outcome.outcome === "cancelled") return { decision: "abort" };
  if (response.outcome.optionId === "allow_once") return { decision: "approved" };
  if (response.outcome.optionId === "allow_always") return { decision: "approved_for_session" };
  return { decision: "denied" };
}
function codexErrorMessage(params) {
  if (typeof params.message === "string") return params.message;
  const error = params.error;
  if (typeof error === "string") return error;
  if (typeof error === "object" && error !== null && "message" in error) {
    const message = error.message;
    if (typeof message === "string") return message;
  }
  return "Codex returned an error.";
}
function classifyCodexToolKind(item) {
  const raw = String(item.type ?? item.name ?? item.toolName ?? item.tool ?? "").toLowerCase();
  if (raw.includes("patch") || raw.includes("file") || raw.includes("edit")) return "edit";
  if (raw.includes("shell") || raw.includes("exec") || raw.includes("command")) return "execute";
  if (raw.includes("read")) return "read";
  if (raw.includes("search")) return "search";
  return "other";
}
function codexToolTitle(item) {
  for (const key of ["title", "name", "toolName", "tool", "type"]) {
    const value = item[key];
    if (typeof value === "string" && value.trim()) return value;
  }
  return "Codex tool call";
}
function translateCodexItemCompleted(event) {
  const item = event.params.item;
  const toolCallId = typeof item.id === "string" ? item.id : typeof item.callId === "string" ? item.callId : typeof item.toolCallId === "string" ? item.toolCallId : `${event.params.turnId}:item`;
  const output = typeof item.output === "string" ? item.output : typeof item.result === "string" ? item.result : null;
  return {
    sessionUpdate: "tool_call",
    toolCallId,
    title: codexToolTitle(item),
    kind: classifyCodexToolKind(item),
    status: item.status === "failed" ? "failed" : "completed",
    rawInput: item,
    _meta: codexMeta({ threadId: event.params.threadId, turnId: event.params.turnId, item }),
    ...output ? { content: [{ type: "content", content: { type: "text", text: output } }] } : {}
  };
}
function isCodexFinalAgentMessageItem(item) {
  if (item.type !== "agentMessage") return false;
  const phase = typeof item.phase === "string" ? item.phase : null;
  const status4 = typeof item.status === "string" ? item.status : null;
  return phase === "final" || phase === "final_answer" || phase === "completed" || status4 === "completed";
}
var init_codex_helpers = __esm({
  "packages/daemon/src/chat/codex-helpers.ts"() {
    "use strict";
  }
});

// packages/daemon/src/chat/codex-event-handler.ts
function handleCodexAgentEvent(event, deps2) {
  if (event.method === CLIENT_METHODS2.turn_started) return;
  if (event.method === CLIENT_METHODS2.turn_completed) {
    deps2.recordUsage(extractUsagePatch(event.params.turn));
    deps2.resolveActivePrompt("end_turn", event.params.turn.id);
    return;
  }
  if (event.method === CLIENT_METHODS2.error) {
    deps2.emitUpdate({
      sessionUpdate: "agent_message_chunk",
      content: { type: "text", text: codexErrorMessage(event.params) },
      _meta: codexMeta({ error: event.params })
    });
    deps2.resolveActivePrompt("refusal");
    return;
  }
  if (event.method === CLIENT_METHODS2.item_agent_message_delta) {
    deps2.emitUpdate({
      sessionUpdate: "agent_message_chunk",
      content: { type: "text", text: event.params.delta },
      messageId: event.params.itemId,
      _meta: codexMeta({ threadId: event.params.threadId, turnId: event.params.turnId })
    });
    return;
  }
  if (event.method === CLIENT_METHODS2.item_reasoning_summary_text_delta || event.method === CLIENT_METHODS2.item_reasoning_text_delta) {
    deps2.emitUpdate({
      sessionUpdate: "agent_thought_chunk",
      content: { type: "text", text: event.params.delta },
      messageId: event.params.itemId,
      _meta: codexMeta({ threadId: event.params.threadId, turnId: event.params.turnId })
    });
    return;
  }
  if (event.method === CLIENT_METHODS2.item_reasoning_summary_part_added) {
    deps2.emitUpdate({
      sessionUpdate: "agent_thought_chunk",
      content: { type: "text", text: "" },
      messageId: event.params.itemId,
      _meta: codexMeta({
        threadId: event.params.threadId,
        turnId: event.params.turnId,
        summaryIndex: event.params.summaryIndex
      })
    });
    return;
  }
  if (event.method === CLIENT_METHODS2.item_completed) {
    const itemType = event.params.item.type;
    if (itemType === "userMessage" || itemType === "agentReasoning") return;
    if (itemType === "agentMessage") {
      if (isCodexFinalAgentMessageItem(event.params.item)) {
        deps2.resolveActivePrompt("end_turn", event.params.turnId);
      }
      return;
    }
    deps2.emitUpdate(translateCodexItemCompleted(event));
  }
}
var init_codex_event_handler = __esm({
  "packages/daemon/src/chat/codex-event-handler.ts"() {
    "use strict";
    init_methods2();
    init_codex_helpers();
    init_usage_extraction();
  }
});

// packages/daemon/src/chat/permission-helpers.ts
function normalizePermissionToolCall(update) {
  return {
    toolCallId: update.toolCallId,
    title: update.title ?? "Permission request",
    ...update.kind ? { kind: update.kind } : {},
    ...update.status ? { status: update.status } : {},
    ...update.content ? { content: update.content } : {},
    ...update.locations ? { locations: update.locations } : {},
    ...update.rawInput !== void 0 ? { rawInput: update.rawInput } : {},
    ...update.rawOutput !== void 0 ? { rawOutput: update.rawOutput } : {},
    ...update._meta !== void 0 ? { _meta: update._meta } : {}
  };
}
function autoRejectResponse(options) {
  const option = options.find((candidate) => candidate.optionId === "reject_once") ?? options.find((candidate) => candidate.kind.startsWith("reject_")) ?? options[0];
  return option ? { outcome: { outcome: "selected", optionId: option.optionId } } : { outcome: { outcome: "cancelled" } };
}
function cancelledPermissionResponse() {
  return { outcome: { outcome: "cancelled" } };
}
var PermissionRequestNotFoundError, InvalidPermissionOptionError;
var init_permission_helpers = __esm({
  "packages/daemon/src/chat/permission-helpers.ts"() {
    "use strict";
    PermissionRequestNotFoundError = class extends Error {
      threadId;
      requestId;
      constructor(threadId, requestId) {
        super(`Permission request ${requestId} not found for thread ${threadId}`);
        this.name = "PermissionRequestNotFoundError";
        this.threadId = threadId;
        this.requestId = requestId;
      }
    };
    InvalidPermissionOptionError = class extends Error {
      requestId;
      optionId;
      constructor(requestId, optionId) {
        super(`Permission option ${optionId} is not valid for request ${requestId}`);
        this.name = "InvalidPermissionOptionError";
        this.requestId = requestId;
        this.optionId = optionId;
      }
    };
  }
});

// packages/daemon/src/chat/permission-coordinator.ts
import { randomUUID as randomUUID4 } from "node:crypto";
function makePermissionCoordinator(opts) {
  const pending = /* @__PURE__ */ new Map();
  function remove(requestId) {
    const entry = pending.get(requestId);
    if (!entry) return null;
    if (entry.timer) clearTimeout(entry.timer);
    pending.delete(requestId);
    return entry;
  }
  return {
    async request(threadId, req) {
      await opts.beforeEmit?.(threadId);
      const requestId = randomUUID4();
      return new Promise((resolve35) => {
        const timer = setTimeout(() => {
          remove(requestId)?.resolve(autoRejectResponse(req.options));
        }, opts.permissionTimeoutMs);
        timer.unref?.();
        pending.set(requestId, { threadId, options: req.options, resolve: resolve35, timer });
        opts.busEmit({
          type: "chat.permission.request",
          threadId,
          requestId,
          toolCall: normalizePermissionToolCall(req.toolCall),
          options: req.options
        });
      });
    },
    respond(threadId, requestId, optionId) {
      const entry = pending.get(requestId);
      if (!entry || entry.threadId !== threadId) {
        throw new PermissionRequestNotFoundError(threadId, requestId);
      }
      if (!entry.options.some((option) => option.optionId === optionId)) {
        throw new InvalidPermissionOptionError(requestId, optionId);
      }
      remove(requestId)?.resolve({ outcome: { outcome: "selected", optionId } });
    },
    cancelForThread(threadId) {
      for (const [requestId, entry] of pending) {
        if (entry.threadId !== threadId) continue;
        remove(requestId)?.resolve(cancelledPermissionResponse());
      }
    },
    cancelAll() {
      for (const requestId of pending.keys()) {
        remove(requestId)?.resolve(cancelledPermissionResponse());
      }
    },
    isKnown(threadId, requestId) {
      const entry = pending.get(requestId);
      return !!entry && entry.threadId === threadId;
    }
  };
}
var init_permission_coordinator = __esm({
  "packages/daemon/src/chat/permission-coordinator.ts"() {
    "use strict";
    init_permission_helpers();
  }
});

// packages/daemon/src/chat/materialize.ts
function clone(value) {
  return structuredClone(value);
}
function mergeToolCallUpdate(toolCalls, update) {
  let toolCall = toolCalls.find((candidate) => candidate.toolCallId === update.toolCallId);
  if (!toolCall) {
    toolCall = {
      toolCallId: update.toolCallId,
      title: update.title ?? update.toolCallId,
      ...update.kind ? { kind: update.kind } : {},
      status: update.status ?? "pending",
      content: []
    };
    toolCalls.push(toolCall);
  }
  if (update.title) toolCall.title = update.title;
  if (update.kind) toolCall.kind = update.kind;
  if (update.status) toolCall.status = update.status;
  if (update.content?.length) toolCall.content = [...toolCall.content, ...update.content];
  if (update.rawInput !== void 0) toolCall.rawInput = update.rawInput;
  if (update.rawOutput !== void 0) toolCall.rawOutput = update.rawOutput;
}
function assistantHasVisibleContent(message) {
  return message.text.length > 0 || Boolean(message.thoughtText && message.thoughtText.length > 0) || message.toolCalls.length > 0;
}
function assignRevertTurnCounts(rows) {
  let trailingUserTurns = 0;
  for (let i = rows.length - 1; i >= 0; i -= 1) {
    const row = rows[i];
    if (!row || row.kind !== "message" || row.message.role !== "user") continue;
    if (trailingUserTurns > 0) row.revertTurnCount = trailingUserTurns;
    trailingUserTurns += 1;
  }
}
function freshCursor() {
  return {
    turnIndex: -1,
    currentAssistantRowId: null,
    currentPlanRowId: null,
    latestUserRowId: null,
    activePromptId: null
  };
}
function materializeRows(messages) {
  const timeline = new ThreadTimeline();
  timeline.bootstrap(messages);
  return timeline.snapshot();
}
var ThreadTimeline;
var init_materialize = __esm({
  "packages/daemon/src/chat/materialize.ts"() {
    "use strict";
    ThreadTimeline = class {
      rows = [];
      cursor = freshCursor();
      dirty = /* @__PURE__ */ new Set();
      /**
       * Rebuild from the raw event log. Settled by default; pass
       * `activePromptId` to re-open the latest turn as the live streaming
       * one (used after a user prompt / truncation so the next agent
       * chunk streams into a fresh assistant row).
       */
      bootstrap(messages, activePromptId) {
        this.rows = [];
        this.cursor = freshCursor();
        for (const message of messages) {
          if (message._tag === "UserPrompt") {
            this.applyUserPrompt({
              id: message.id,
              content: message.content,
              createdAt: message.createdAt
            });
          } else {
            this.applyAgentUpdate(message.id, message.createdAt, message.update);
          }
        }
        this.cursor.activePromptId = activePromptId ?? null;
        this.dirty.clear();
      }
      snapshot() {
        return clone(this.rows);
      }
      get activePromptId() {
        return this.cursor.activePromptId;
      }
      rowById(id) {
        if (!id) return null;
        for (const row of this.rows) {
          if (row.kind === "message" && row.id === id) return row;
        }
        return null;
      }
      workingId(assistantId) {
        return `${assistantId}:working`;
      }
      removeWorkingRow(assistantId) {
        const id = this.workingId(assistantId);
        const index = this.rows.findIndex((row) => row.kind === "working" && row.id === id);
        if (index !== -1) {
          this.rows.splice(index, 1);
          this.dirty.add(id);
        }
      }
      syncStreaming() {
        if (!this.cursor.activePromptId) return;
        const row = this.rowById(this.cursor.currentAssistantRowId);
        if (!row || row.message.role !== "assistant") return;
        const assistant = row.message;
        assistant.streaming = true;
        this.dirty.add(row.id);
        if (assistantHasVisibleContent(assistant)) {
          this.removeWorkingRow(assistant.id);
          return;
        }
        const id = this.workingId(assistant.id);
        if (!this.rows.some((candidate) => candidate.kind === "working" && candidate.id === id)) {
          this.rows.push({ kind: "working", id, createdAt: assistant.createdAt });
          this.dirty.add(id);
        }
      }
      ensureAssistantRow(sourceId, createdAt) {
        const existing = this.rowById(this.cursor.currentAssistantRowId);
        if (existing && existing.message.role === "assistant") return existing.message;
        const id = `assistant:${this.cursor.turnIndex < 0 ? "orphan" : this.cursor.turnIndex}:${sourceId}`;
        const message = {
          id,
          role: "assistant",
          createdAt,
          streaming: false,
          text: "",
          toolCalls: []
        };
        this.rows.push({ kind: "message", id, createdAt, message });
        this.cursor.currentAssistantRowId = id;
        this.dirty.add(id);
        return message;
      }
      /**
       * Open a new turn. `activePromptId` ties the live streaming caret to
       * a specific prompt; omitted (bootstrap replay) leaves the turn
       * settled.
       */
      applyUserPrompt(input) {
        this.cursor.turnIndex += 1;
        this.cursor.currentAssistantRowId = null;
        this.cursor.currentPlanRowId = null;
        this.cursor.latestUserRowId = input.id;
        this.cursor.activePromptId = input.activePromptId ?? null;
        this.rows.push({
          kind: "message",
          id: input.id,
          createdAt: input.createdAt,
          message: {
            id: input.id,
            role: "user",
            createdAt: input.createdAt,
            content: [...input.content]
          }
        });
        this.dirty.add(input.id);
        assignRevertTurnCounts(this.rows);
      }
      /** Fold one ACP session update into the transcript. */
      applyAgentUpdate(sourceId, createdAt, update) {
        const assistant = this.ensureAssistantRow(sourceId, createdAt);
        const assistantRowId = this.cursor.currentAssistantRowId;
        switch (update.sessionUpdate) {
          case "agent_message_chunk": {
            const content = update.content;
            if (content && content.type === "text") assistant.text += content.text;
            if (assistantRowId) this.dirty.add(assistantRowId);
            break;
          }
          case "agent_thought_chunk": {
            const content = update.content;
            if (content && content.type === "text") {
              assistant.thoughtText = `${assistant.thoughtText ?? ""}${content.text}`;
            }
            if (assistantRowId) this.dirty.add(assistantRowId);
            break;
          }
          case "user_message_chunk": {
            const content = update.content;
            const userRow = this.rowById(this.cursor.latestUserRowId);
            if (content && userRow && userRow.message.role === "user") {
              userRow.message.content = [...userRow.message.content, content];
              this.dirty.add(userRow.id);
            }
            break;
          }
          case "tool_call": {
            const u = update;
            assistant.toolCalls.push({
              toolCallId: u.toolCallId,
              title: u.title,
              ...u.kind ? { kind: u.kind } : {},
              status: u.status ?? "pending",
              content: [...u.content ?? []],
              ...u.rawInput !== void 0 ? { rawInput: u.rawInput } : {},
              ...u.rawOutput !== void 0 ? { rawOutput: u.rawOutput } : {}
            });
            if (assistantRowId) this.dirty.add(assistantRowId);
            break;
          }
          case "tool_call_update": {
            const u = update;
            mergeToolCallUpdate(assistant.toolCalls, {
              toolCallId: u.toolCallId,
              title: u.title ?? void 0,
              kind: u.kind ?? void 0,
              status: u.status ?? void 0,
              content: u.content ?? void 0,
              rawInput: u.rawInput,
              rawOutput: u.rawOutput
            });
            if (assistantRowId) this.dirty.add(assistantRowId);
            break;
          }
          case "plan": {
            const rawEntries = update.entries ?? [];
            const entries = rawEntries.map((entry) => ({
              content: entry.content,
              ...entry.status ? { status: entry.status } : {},
              ...entry.priority ? { priority: entry.priority } : {}
            }));
            const planId = this.cursor.currentPlanRowId ?? `plan:${sourceId}`;
            const planRow = {
              kind: "plan",
              id: planId,
              createdAt,
              entries
            };
            if (this.cursor.currentPlanRowId) {
              const index = this.rows.findIndex((row) => row.id === this.cursor.currentPlanRowId);
              if (index !== -1) this.rows[index] = planRow;
            } else {
              this.cursor.currentPlanRowId = planId;
              this.rows.push(planRow);
            }
            this.dirty.add(planId);
            break;
          }
          case "available_commands_update":
          case "current_mode_update":
            break;
          default:
            break;
        }
        this.syncStreaming();
      }
      /**
       * Close the active turn's streaming row. Idempotent: a second call
       * (real stop after an optimistic cancel) is a no-op once the active
       * prompt is cleared.
       */
      finish(promptId, stopReason, completedAt) {
        if (this.cursor.activePromptId && promptId && this.cursor.activePromptId !== promptId) return;
        const row = this.rowById(this.cursor.currentAssistantRowId);
        if (row && row.message.role === "assistant") {
          const assistant = row.message;
          assistant.streaming = false;
          assistant.stopReason = stopReason;
          assistant.completedAt = completedAt;
          this.dirty.add(row.id);
          this.removeWorkingRow(assistant.id);
        }
        this.cursor.activePromptId = null;
      }
      /** Drain the pending delta (changed rows + authoritative order). */
      drainDelta() {
        const order = this.rows.map((row) => row.id);
        const present = new Map(this.rows.map((row) => [row.id, row]));
        const rows = [];
        for (const id of this.dirty) {
          const row = present.get(id);
          if (row) rows.push(clone(row));
        }
        this.dirty.clear();
        return { rows, order };
      }
    };
  }
});

// packages/daemon/src/chat/message-pipe.ts
import { randomUUID as randomUUID5 } from "node:crypto";
function makeMessagePipe(opts) {
  const { threadId, store, busEmit, logger: logger2 } = opts;
  let seq = opts.initialSeq;
  let usage = opts.initialUsage;
  const persist = { messages: [], timer: null, chain: Promise.resolve() };
  let pendingText = null;
  const timeline = new ThreadTimeline();
  timeline.bootstrap(opts.initialMessages ?? []);
  function broadcastTimelineUpsert() {
    const delta = timeline.drainDelta();
    if (delta.rows.length === 0) return;
    busEmit({ type: "chat.timeline.upsert", threadId, rows: delta.rows, order: delta.order });
  }
  function schedulePersist() {
    if (persist.timer) clearTimeout(persist.timer);
    persist.timer = setTimeout(() => {
      persist.timer = null;
      void flushPersist();
    }, opts.persistDebounceMs);
    persist.timer.unref?.();
  }
  function queuePersist(update) {
    persist.messages.push({
      _tag: "AgentUpdate",
      id: randomUUID5(),
      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
      update
    });
    schedulePersist();
  }
  async function flushPersist() {
    if (persist.timer) {
      clearTimeout(persist.timer);
      persist.timer = null;
    }
    const batch = persist.messages.splice(0);
    if (batch.length === 0) {
      await persist.chain;
      return;
    }
    const run2 = persist.chain.then(() => store.appendMessages(threadId, batch)).catch((err) => {
      logger2({
        level: "warn",
        msg: "Failed to persist ACP session updates",
        data: { threadId, error: err.message ?? String(err) }
      });
    });
    persist.chain = run2;
    await run2;
  }
  function emitUpdate(update) {
    seq += 1;
    busEmit({ type: "chat.thread.update", threadId, update, seq });
    queuePersist(update);
    timeline.applyAgentUpdate(`agent-update:${seq}`, (/* @__PURE__ */ new Date()).toISOString(), update);
    broadcastTimelineUpsert();
  }
  function textChunkKind(update) {
    if (update.sessionUpdate === "agent_message_chunk" && typeof update.content === "object" && update.content !== null && update.content.type === "text") {
      return "agent_message_chunk";
    }
    if (update.sessionUpdate === "agent_thought_chunk" && typeof update.content === "object" && update.content !== null && update.content.type === "text") {
      return "agent_thought_chunk";
    }
    return null;
  }
  function flushText() {
    const pending = pendingText;
    if (!pending) return;
    if (pending.timer) clearTimeout(pending.timer);
    pendingText = null;
    const update = {
      sessionUpdate: pending.sessionUpdate,
      content: { type: "text", text: pending.text },
      ...pending.messageId ? { messageId: pending.messageId } : {},
      ...pending.meta !== void 0 ? { _meta: pending.meta } : {}
    };
    emitUpdate(update);
  }
  function scheduleTextFlush(pending) {
    if (pending.timer) clearTimeout(pending.timer);
    pending.timer = setTimeout(() => {
      pending.timer = null;
      flushText();
    }, opts.textCoalesceWindowMs);
    pending.timer.unref?.();
  }
  function bufferText(update, kind) {
    const content = update.content;
    const messageId = typeof update.messageId === "string" || update.messageId === null ? update.messageId : void 0;
    const meta = typeof update._meta === "object" || update._meta === null ? update._meta : void 0;
    if (pendingText && pendingText.sessionUpdate !== kind) flushText();
    const pending = pendingText ?? {
      sessionUpdate: kind,
      text: "",
      messageId,
      meta,
      timer: null
    };
    pending.text += content.text;
    pending.messageId = pending.messageId ?? messageId;
    pending.meta = pending.meta ?? meta;
    pendingText = pending;
    scheduleTextFlush(pending);
  }
  return {
    emit(update) {
      if (opts.disableCoalescing) {
        emitUpdate(update);
        return;
      }
      const kind = textChunkKind(update);
      if (!kind) {
        flushText();
        emitUpdate(update);
        return;
      }
      bufferText(update, kind);
    },
    recordUsage(patch) {
      if (!patch) return;
      const next = mergeUsage(usage, patch);
      if (!usageChanged(usage, next)) return;
      usage = next;
      busEmit({ type: "chat.thread.usage", threadId, usage: next });
      void store.recordUsage(threadId, next).catch((err) => {
        logger2({
          level: "warn",
          msg: "Failed to persist chat thread usage",
          data: { threadId, error: err.message ?? String(err) }
        });
      });
    },
    async forceFlush() {
      flushText();
      await flushPersist();
    },
    resyncTimeline(messages, activePromptId) {
      timeline.bootstrap(messages, activePromptId);
      busEmit({ type: "chat.timeline.reset", threadId, rows: timeline.snapshot() });
    },
    finishTimeline(promptId, stopReason) {
      timeline.finish(promptId, stopReason, (/* @__PURE__ */ new Date()).toISOString());
      broadcastTimelineUpsert();
    }
  };
}
var init_message_pipe = __esm({
  "packages/daemon/src/chat/message-pipe.ts"() {
    "use strict";
    init_usage_extraction();
    init_materialize();
  }
});

// packages/daemon/src/chat/dispatch-prompt.ts
function asReasoningEffort(value) {
  return KNOWN_REASONING_EFFORTS.includes(value) ? value : null;
}
async function dispatchCodexPrompt(input) {
  const completed = new Promise((resolve35) => {
    input.bindActivePrompt({ promptId: input.promptId, turnId: null, resolve: resolve35 });
  });
  try {
    const effortLevel = input.reasoningEffort ? asReasoningEffort(input.reasoningEffort) : null;
    const response = await input.client.sendUserMessage({
      threadId: input.codexThreadId,
      input: codexInputFromContent(input.content),
      ...input.model ? { model: input.model } : {},
      ...effortLevel ? { effort: effortLevel } : {},
      ...input.fastMode ? { serviceTier: "fast" } : {}
    });
    const active2 = input.readActivePrompt();
    if (active2?.promptId === input.promptId) {
      input.bindActivePrompt({ ...active2, turnId: response.turn.id });
    }
    const stopReason = await completed;
    await input.pipe.forceFlush();
    input.busEmit({
      type: "chat.thread.stop",
      threadId: input.threadId,
      promptId: input.promptId,
      stopReason
    });
    input.pipe.finishTimeline(input.promptId, stopReason);
    await input.store.recordStopReason(input.threadId, stopReason);
  } catch (err) {
    if (input.readActivePrompt()?.promptId === input.promptId) input.bindActivePrompt(null);
    input.logger({
      level: "warn",
      msg: "Codex prompt failed",
      data: { threadId: input.threadId, error: err.message ?? String(err) }
    });
    await input.pipe.forceFlush();
    input.busEmit({
      type: "chat.thread.stop",
      threadId: input.threadId,
      promptId: input.promptId,
      stopReason: "refusal"
    });
    input.pipe.finishTimeline(input.promptId, "refusal");
    await input.store.recordStopReason(input.threadId, "refusal").catch(() => void 0);
  }
}
async function dispatchAcpPrompt(input) {
  try {
    const meta = {};
    if (input.model) meta.model = input.model;
    if (input.reasoningEffort) meta.reasoningEffort = input.reasoningEffort;
    if (input.fastMode) meta.fastMode = true;
    const response = await input.client.prompt({
      sessionId: input.sessionId,
      prompt: input.content,
      ...Object.keys(meta).length > 0 ? { _meta: meta } : {}
    });
    const stopReason = stopReasonFromResponse(response);
    await input.pipe.forceFlush();
    input.busEmit({
      type: "chat.thread.stop",
      threadId: input.threadId,
      promptId: input.promptId,
      stopReason
    });
    input.pipe.finishTimeline(input.promptId, stopReason);
    await input.store.recordStopReason(input.threadId, stopReason);
  } catch (err) {
    input.logger({
      level: "warn",
      msg: "ACP prompt failed",
      data: { threadId: input.threadId, error: err.message ?? String(err) }
    });
    await input.pipe.forceFlush();
    input.busEmit({
      type: "chat.thread.stop",
      threadId: input.threadId,
      promptId: input.promptId,
      stopReason: "refusal"
    });
    input.pipe.finishTimeline(input.promptId, "refusal");
    await input.store.recordStopReason(input.threadId, "refusal").catch(() => void 0);
  }
}
var KNOWN_REASONING_EFFORTS;
var init_dispatch_prompt = __esm({
  "packages/daemon/src/chat/dispatch-prompt.ts"() {
    "use strict";
    init_codex_helpers();
    KNOWN_REASONING_EFFORTS = ["minimal", "low", "medium", "high", "xhigh"];
  }
});

// packages/daemon/src/chat/provider-discovery.ts
import { execFile as execFile2, spawn as spawn5 } from "node:child_process";
import { once as once3 } from "node:events";
import { constants as constants3 } from "node:fs";
import { access } from "node:fs/promises";
import { delimiter, isAbsolute as isAbsolute3, join as join19 } from "node:path";
function isCodexCompatibleSlug(slug) {
  return /-codex(?:-|$)/.test(slug);
}
function extractCodexCapabilities(model) {
  const efforts = [];
  if (Array.isArray(model.supportedReasoningEfforts)) {
    for (const entry of model.supportedReasoningEfforts) {
      if (typeof entry === "string" && entry.length > 0) {
        efforts.push(entry);
      } else if (entry && typeof entry === "object" && typeof entry.reasoningEffort === "string") {
        efforts.push(entry.reasoningEffort);
      }
    }
  }
  const defaultEffort = typeof model.defaultReasoningEffort === "string" && model.defaultReasoningEffort.length > 0 ? model.defaultReasoningEffort : void 0;
  const speedTiers = Array.isArray(model.additionalSpeedTiers) ? model.additionalSpeedTiers.filter((t) => typeof t === "string") : [];
  const supportsFastMode = speedTiers.includes("fast");
  if (efforts.length === 0 && !defaultEffort && !supportsFastMode) return void 0;
  const out = {};
  if (efforts.length > 0) out.reasoningEfforts = efforts;
  if (defaultEffort) out.defaultReasoningEffort = defaultEffort;
  if (supportsFastMode) out.supportsFastMode = true;
  return out;
}
function parseCodexModelListResponse(response) {
  const data = Array.isArray(response.data) ? response.data : [];
  const out = [];
  for (const model of data) {
    const slug = typeof model.model === "string" ? model.model : null;
    if (!slug) continue;
    if (model.hidden === true) continue;
    if (!isCodexCompatibleSlug(slug)) continue;
    const displayName = typeof model.displayName === "string" ? model.displayName : slug;
    const description = typeof model.description === "string" ? model.description : void 0;
    const capabilities = extractCodexCapabilities(model);
    const entry = {
      slug,
      name: prettifyCodexDisplayName(displayName),
      ...description ? { description } : {},
      ...capabilities ? { capabilities } : {}
    };
    out.push(entry);
  }
  const defaultSlug = data.find((m) => m.isDefault === true && typeof m.model === "string")?.model;
  if (defaultSlug) {
    const idx = out.findIndex((m) => m.slug === defaultSlug);
    if (idx > 0) {
      const [item] = out.splice(idx, 1);
      if (item) out.unshift(item);
    }
  }
  return out;
}
function prettifyCodexDisplayName(name) {
  return name.replace(/^gpt/i, "GPT").replace(/-([a-z])/g, (_, c) => "-" + c.toUpperCase());
}
async function defaultProbeCodexModels(binaryPath, opts = {}) {
  const timeoutMs = opts.timeoutMs ?? CODEX_PROBE_TIMEOUT_MS;
  let timer = null;
  let child = null;
  try {
    child = spawn5(binaryPath, ["app-server"], { stdio: ["pipe", "pipe", "pipe"] });
    const spawnedChild = child;
    if (!spawnedChild.stdin || !spawnedChild.stdout) return null;
    spawnedChild.stderr?.on("data", () => void 0);
    const spawnFailure = await Promise.race([
      once3(spawnedChild, "spawn").then(() => null),
      once3(spawnedChild, "error").then(([err]) => err),
      once3(spawnedChild, "exit").then(
        ([code, signal]) => new Error(`codex app-server exited during spawn (code=${code}, signal=${signal})`)
      )
    ]);
    if (spawnFailure) return null;
    const endpoint = makeJsonRpcEndpoint2({
      input: spawnedChild.stdout,
      output: spawnedChild.stdin
    });
    const work = (async () => {
      await endpoint.request(AGENT_METHODS2.initialize, defaultInitializeRequest());
      const accumulated = [];
      let cursor = void 0;
      for (let page = 0; page < 16; page += 1) {
        const params = cursor ? { cursor } : {};
        const response = await endpoint.request(
          CODEX_METHODS.model_list,
          params
        );
        accumulated.push(...parseCodexModelListResponse(response));
        cursor = response.nextCursor ?? null;
        if (!cursor) break;
      }
      return accumulated;
    })();
    const timeout = new Promise((resolve35) => {
      timer = setTimeout(() => resolve35(null), timeoutMs);
    });
    const result = await Promise.race([work, timeout]);
    if (result === null) return null;
    return result.length > 0 ? result : null;
  } catch {
    return null;
  } finally {
    if (timer) clearTimeout(timer);
    if (child && !child.killed) {
      try {
        child.kill("SIGTERM");
      } catch {
      }
    }
  }
}
async function getCodexModelsCached(binaryPath, version, now, probe) {
  if (codexModelsCache && codexModelsCache.binaryPath === binaryPath && codexModelsCache.version === version && codexModelsCache.expiresAt > now) {
    return codexModelsCache.models;
  }
  const probed = await probe(binaryPath, { timeoutMs: CODEX_PROBE_TIMEOUT_MS }).catch(() => null);
  const models = probed ?? CODEX_MODELS;
  codexModelsCache = {
    binaryPath,
    version,
    expiresAt: now + CODEX_MODELS_CACHE_TTL_MS,
    models
  };
  return models;
}
async function isExecutable(path4) {
  try {
    await access(path4, constants3.X_OK);
    return true;
  } catch {
    return false;
  }
}
async function resolveFromPath3(binary) {
  if (isAbsolute3(binary)) return await isExecutable(binary) ? binary : null;
  if (binary.includes("/")) return await isExecutable(binary) ? binary : null;
  for (const dir of (process.env.PATH ?? "").split(delimiter)) {
    if (!dir) continue;
    const candidate = join19(dir, binary);
    if (await isExecutable(candidate)) return candidate;
  }
  return null;
}
async function defaultExec(cmd, args, opts = {}) {
  return await new Promise((resolve35) => {
    execFile2(cmd, args, { timeout: opts.timeoutMs }, (err, stdout, stderr) => {
      const exitCode = err && typeof err.code === "number" ? err.code : err ? null : 0;
      resolve35({ stdout, stderr, code: exitCode });
    });
  });
}
function firstStdoutLine(result) {
  return result.code === 0 ? result.stdout.split(/\r?\n/).find((line) => line.trim())?.trim() : void 0;
}
async function bestEffortVersion(exec, binary) {
  try {
    return firstStdoutLine(await exec(binary, ["--version"], { timeoutMs: VERSION_TIMEOUT_MS }));
  } catch {
    return void 0;
  }
}
async function discoverClaudeCode(pathLookup, exec) {
  const direct = await pathLookup("claude-code-acp");
  if (direct) {
    const version = await bestEffortVersion(exec, direct);
    return {
      kind: "claude-code",
      name: "Claude Code",
      description: "Claude Code via claude-code-acp",
      available: true,
      binary: direct,
      ...version ? { version } : {},
      models: CLAUDE_CODE_MODELS
    };
  }
  const npx = await pathLookup("npx");
  if (npx) {
    return {
      kind: "claude-code",
      name: "Claude Code",
      description: "Claude Code via npx",
      available: true,
      binary: npx,
      models: CLAUDE_CODE_MODELS
    };
  }
  return {
    kind: "claude-code",
    name: "Claude Code",
    description: "Claude Code via claude-code-acp",
    available: false,
    error: "neither claude-code-acp nor npx on PATH",
    models: []
  };
}
async function discoverCodex(pathLookup, exec, probe, now) {
  const binary = await pathLookup("codex");
  if (!binary) {
    return {
      kind: "codex",
      name: "Codex",
      description: "Codex app-server proxy",
      available: false,
      error: "codex not on PATH",
      models: []
    };
  }
  const version = await bestEffortVersion(exec, binary);
  const models = await getCodexModelsCached(binary, version, now, probe);
  return {
    kind: "codex",
    name: "Codex",
    description: "Codex app-server proxy",
    available: true,
    binary,
    ...version ? { version } : {},
    models
  };
}
async function discoverProviders(opts = {}) {
  const pathLookup = opts.pathLookup ?? resolveFromPath3;
  const exec = opts.exec ?? defaultExec;
  const probe = opts.probeCodexModels ?? defaultProbeCodexModels;
  const now = opts.now?.() ?? Date.now();
  return [
    await discoverClaudeCode(pathLookup, exec),
    await discoverCodex(pathLookup, exec, probe, now)
  ];
}
var MODEL_SLUG_ALIASES_BY_KIND, CLAUDE_CODE_MODELS, CODEX_STATIC_DEFAULT_REASONING, CODEX_STATIC_REASONING_EFFORTS, CODEX_MODELS, VERSION_TIMEOUT_MS, CODEX_PROBE_TIMEOUT_MS, CODEX_MODELS_CACHE_TTL_MS, codexModelsCache;
var init_provider_discovery = __esm({
  "packages/daemon/src/chat/provider-discovery.ts"() {
    "use strict";
    init_protocol2();
    init_methods2();
    init_schema2();
    MODEL_SLUG_ALIASES_BY_KIND = {
      codex: {
        "gpt-5-codex": "gpt-5.4",
        "5.4": "gpt-5.4",
        "5.3": "gpt-5.3-codex",
        "gpt-5.3": "gpt-5.3-codex",
        "5.3-spark": "gpt-5.3-codex-spark",
        "gpt-5.3-spark": "gpt-5.3-codex-spark"
      },
      "claude-code": {
        opus: "claude-opus-4-7",
        "opus-4.7": "claude-opus-4-7",
        "claude-opus-4.7": "claude-opus-4-7",
        sonnet: "claude-sonnet-4-6",
        "sonnet-4.6": "claude-sonnet-4-6",
        "claude-sonnet-4.6": "claude-sonnet-4-6",
        haiku: "claude-haiku-4-5",
        "haiku-4.5": "claude-haiku-4-5",
        "claude-haiku-4-5-20251001": "claude-haiku-4-5"
      }
    };
    CLAUDE_CODE_MODELS = [
      {
        slug: "claude-opus-4-7",
        name: "Claude Opus 4.7",
        description: "1M context \xB7 highest capability"
      },
      {
        slug: "claude-sonnet-4-6",
        name: "Claude Sonnet 4.6",
        description: "Balanced speed + quality"
      },
      {
        slug: "claude-haiku-4-5",
        name: "Claude Haiku 4.5",
        description: "Fastest \xB7 low cost"
      }
    ];
    CODEX_STATIC_DEFAULT_REASONING = "medium";
    CODEX_STATIC_REASONING_EFFORTS = ["minimal", "low", "medium", "high", "xhigh"];
    CODEX_MODELS = [
      {
        slug: "gpt-5.4",
        name: "GPT-5.4",
        description: "Current default \xB7 reasoning + fast mode",
        capabilities: {
          reasoningEfforts: CODEX_STATIC_REASONING_EFFORTS,
          defaultReasoningEffort: CODEX_STATIC_DEFAULT_REASONING,
          supportsFastMode: true
        }
      },
      {
        slug: "gpt-5.3-codex",
        name: "GPT-5.3 Codex",
        description: "Newer code-tuned",
        capabilities: {
          reasoningEfforts: CODEX_STATIC_REASONING_EFFORTS,
          defaultReasoningEffort: CODEX_STATIC_DEFAULT_REASONING,
          supportsFastMode: true
        }
      },
      {
        slug: "gpt-5.3-codex-spark",
        name: "GPT-5.3 Codex Spark",
        description: "Faster code-tuned",
        capabilities: {
          reasoningEfforts: CODEX_STATIC_REASONING_EFFORTS,
          defaultReasoningEffort: CODEX_STATIC_DEFAULT_REASONING,
          supportsFastMode: true
        }
      },
      {
        slug: "gpt-5-codex",
        name: "GPT-5 Codex",
        description: "Code-tuned (legacy)"
      }
    ];
    VERSION_TIMEOUT_MS = 1500;
    CODEX_PROBE_TIMEOUT_MS = 5e3;
    CODEX_MODELS_CACHE_TTL_MS = 6e4;
    codexModelsCache = null;
  }
});

// packages/daemon/src/chat/thread-manager.ts
import { randomUUID as randomUUID6 } from "node:crypto";
function extractKnownOptions(options) {
  if (!options) return {};
  const out = {};
  for (const sel of options) {
    if (sel.id === "reasoningEffort" && typeof sel.value === "string") {
      out.reasoningEffort = sel.value;
    } else if (sel.id === "fastMode" && typeof sel.value === "boolean") {
      out.fastMode = sel.value;
    }
  }
  return out;
}
function aliasModelSlug(kind, slug) {
  if (kind !== "claude-code" && kind !== "codex") return slug;
  return MODEL_SLUG_ALIASES_BY_KIND[kind]?.[slug] ?? slug;
}
function initialSeq(messages) {
  return messages.filter((msg) => msg._tag === "AgentUpdate").length;
}
function resolveCodexPrompt(liveThread, stopReason, turnId) {
  const active2 = liveThread.activePrompt;
  if (!active2) return;
  if (turnId && active2.turnId && active2.turnId !== turnId) return;
  liveThread.activePrompt = null;
  active2.resolve(stopReason);
}
function makeThreadManager(opts) {
  const live = /* @__PURE__ */ new Map();
  const starting = /* @__PURE__ */ new Map();
  const lastProviderOptions = /* @__PURE__ */ new Map();
  const logger2 = opts.logger ?? (() => void 0);
  const permissionTimeoutMs = opts.permissionTimeoutMs ?? DEFAULT_PERMISSION_TIMEOUT_MS;
  const persistDebounceMs = opts.persistDebounceMs ?? DEFAULT_PERSIST_DEBOUNCE_MS;
  const textCoalesceWindowMs = opts.textCoalesceWindowMs ?? DEFAULT_TEXT_COALESCE_WINDOW_MS;
  const disableCoalescing = opts.disableCoalescing ?? false;
  const spawnCodexClient2 = opts.spawnCodexClient ?? ((provider, spawnOpts) => spawnCodexClient({ provider, cwd: spawnOpts.cwd, logger: spawnOpts.logger }));
  const sessionStore = opts.sessionStore ?? makeSessionStore({
    emit: (event) => opts.busEmit(event)
  });
  const permissions = makePermissionCoordinator({
    busEmit: opts.busEmit,
    permissionTimeoutMs,
    beforeEmit: async (threadId) => {
      const liveThread = live.get(threadId);
      if (liveThread) await liveThread.pipe.forceFlush();
    }
  });
  function makePipeFor(threadId, initialUsage, seq, initialMessages) {
    return makeMessagePipe({
      threadId,
      initialSeq: seq,
      ...initialUsage ? { initialUsage } : {},
      initialMessages,
      store: opts.store,
      busEmit: opts.busEmit,
      persistDebounceMs,
      textCoalesceWindowMs,
      disableCoalescing,
      logger: logger2
    });
  }
  async function spawnCodexLive(threadId, effectiveProvider) {
    const thread = await opts.store.get(threadId);
    if (!thread) throw new ThreadNotFoundError(threadId);
    const provider = effectiveProvider ?? thread.provider;
    if (provider.kind !== "codex") {
      throw new Error(`Expected codex provider on thread ${threadId}`);
    }
    const pipe = makePipeFor(threadId, thread.usage, initialSeq(thread.messages), thread.messages);
    const client = await spawnCodexClient2(provider, {
      cwd: thread.projectDir,
      logger: createCodexDebugLogger(threadId)
    });
    await client.initialize();
    const liveThread = {
      kind: "codex",
      client,
      threadId,
      pipe,
      unsubs: [],
      activePrompt: null
    };
    liveThread.unsubs.push(
      client.onAgentEvent(
        (event) => handleCodexAgentEvent(event, {
          sessionId: liveThread.threadId,
          emitUpdate: (update) => pipe.emit(update),
          recordUsage: (patch) => pipe.recordUsage(patch),
          resolveActivePrompt: (stopReason, turnId) => resolveCodexPrompt(liveThread, stopReason, turnId)
        })
      )
    );
    liveThread.unsubs.push(
      client.onApplyPatchApproval(
        async (req) => codexApplyPatchResponseFromPermission(
          await permissions.request(
            threadId,
            codexApplyPatchToPermission(liveThread.threadId, req)
          )
        )
      )
    );
    liveThread.unsubs.push(
      client.onChatgptTokenRefresh(async (req) => {
        logger2({
          level: "warn",
          msg: "Codex ChatGPT token refresh requested but auth refresh is not implemented",
          data: { threadId, previousAccountId: req.previousAccountId ?? null }
        });
        return { accessToken: "", chatgptAccountId: "", chatgptPlanType: null };
      })
    );
    live.set(threadId, liveThread);
    try {
      const conversation = await client.newConversation({
        cwd: thread.projectDir ?? process.cwd(),
        ...provider.model ? { model: provider.model } : {}
      });
      liveThread.threadId = conversation.thread.id;
      return liveThread;
    } catch (err) {
      live.delete(threadId);
      for (const unsub of liveThread.unsubs.splice(0)) unsub();
      await client.close().catch(() => void 0);
      throw err;
    }
  }
  async function spawnAcpLive(threadId, effectiveProvider) {
    const thread = await opts.store.get(threadId);
    if (!thread) throw new ThreadNotFoundError(threadId);
    const provider = effectiveProvider ?? thread.provider;
    const pipe = makePipeFor(threadId, thread.usage, initialSeq(thread.messages), thread.messages);
    const client = await opts.spawnClient(provider, { cwd: thread.projectDir });
    await client.initialize();
    const session = await client.newSession({
      cwd: thread.projectDir ?? process.cwd(),
      mcpServers: []
    });
    await opts.store.recordAcpSessionId(threadId, session.sessionId);
    const liveThread = {
      kind: "acp",
      client,
      sessionId: session.sessionId,
      pipe,
      unsubs: []
    };
    liveThread.unsubs.push(
      client.onSessionUpdate((n) => {
        pipe.recordUsage(extractUsagePatch(n.update));
        pipe.emit(n.update);
      })
    );
    liveThread.unsubs.push(
      client.onPermissionRequest(
        (req) => permissions.request(threadId, req)
      )
    );
    live.set(threadId, liveThread);
    return liveThread;
  }
  async function disposeLiveImmediate(threadId) {
    const existing = live.get(threadId);
    if (!existing) return;
    permissions.cancelForThread(threadId);
    await existing.pipe.forceFlush();
    live.delete(threadId);
    lastProviderOptions.delete(threadId);
    for (const unsub of existing.unsubs.splice(0)) {
      try {
        unsub();
      } catch {
      }
    }
    await existing.client.close().catch(() => void 0);
  }
  async function ensureLive(threadId, effectiveProvider) {
    let target = effectiveProvider;
    if (!target) {
      const thread = await opts.store.get(threadId);
      if (!thread) throw new ThreadNotFoundError(threadId);
      target = thread.provider;
    }
    const targetIsCodex = target.kind === "codex";
    const existing = live.get(threadId);
    if (existing) {
      const existingIsCodex = existing.kind === "codex";
      if (existingIsCodex === targetIsCodex) return existing;
      await disposeLiveImmediate(threadId);
    }
    const existingStart = starting.get(threadId);
    if (existingStart) return existingStart;
    const start2 = (async () => targetIsCodex ? spawnCodexLive(threadId, target) : spawnAcpLive(threadId, target))();
    starting.set(threadId, start2);
    try {
      return await start2;
    } finally {
      starting.delete(threadId);
    }
  }
  return {
    async send(input) {
      const stored = await opts.store.get(input.threadId);
      if (!stored) throw new ThreadNotFoundError(input.threadId);
      const overrideKind = input.providerKind;
      const persistedKind = stored.provider.kind;
      const sameKind = !overrideKind || overrideKind === persistedKind;
      const aliasKind = overrideKind ?? persistedKind;
      const normalizedInputModel = input.model ? aliasModelSlug(aliasKind, input.model) : void 0;
      if (normalizedInputModel && sameKind && stored.provider.model !== normalizedInputModel) {
        await opts.store.setProvider(input.threadId, {
          ...stored.provider,
          model: normalizedInputModel
        });
      }
      const thread = await opts.store.get(input.threadId) ?? stored;
      const effectiveKind = overrideKind ?? thread.provider.kind;
      const effectiveModel = normalizedInputModel ?? thread.provider.model;
      const effectiveProvider = effectiveKind === thread.provider.kind ? { ...thread.provider, ...effectiveModel ? { model: effectiveModel } : {} } : effectiveKind === "custom" ? thread.provider : { kind: effectiveKind, ...effectiveModel ? { model: effectiveModel } : {} };
      const liveThread = await ensureLive(input.threadId, effectiveProvider);
      const promptId = randomUUID6();
      await opts.store.appendMessage(input.threadId, {
        _tag: "UserPrompt",
        id: promptId,
        createdAt: (/* @__PURE__ */ new Date()).toISOString(),
        content: input.content
      });
      const persisted = await opts.store.get(input.threadId);
      const dispatchModel = effectiveModel;
      const effectiveOptions = input.providerOptions ?? lastProviderOptions.get(input.threadId);
      if (input.providerOptions) {
        lastProviderOptions.set(input.threadId, [...input.providerOptions]);
      }
      const known = extractKnownOptions(effectiveOptions);
      liveThread.pipe.resyncTimeline(persisted?.messages ?? [], promptId);
      if (liveThread.kind === "codex") {
        void dispatchCodexPrompt({
          threadId: input.threadId,
          codexThreadId: liveThread.threadId,
          pipe: liveThread.pipe,
          client: liveThread.client,
          busEmit: opts.busEmit,
          store: opts.store,
          logger: logger2,
          bindActivePrompt: (active2) => {
            liveThread.activePrompt = active2;
          },
          readActivePrompt: () => liveThread.activePrompt,
          promptId,
          content: input.content,
          ...dispatchModel ? { model: dispatchModel } : {},
          ...known.reasoningEffort ? { reasoningEffort: known.reasoningEffort } : {},
          ...known.fastMode ? { fastMode: known.fastMode } : {}
        });
      } else {
        void dispatchAcpPrompt({
          threadId: input.threadId,
          sessionId: liveThread.sessionId,
          pipe: liveThread.pipe,
          client: liveThread.client,
          busEmit: opts.busEmit,
          store: opts.store,
          logger: logger2,
          ...dispatchModel ? { model: dispatchModel } : {},
          ...known.reasoningEffort ? { reasoningEffort: known.reasoningEffort } : {},
          ...known.fastMode ? { fastMode: known.fastMode } : {},
          promptId,
          content: input.content
        });
      }
      return { promptId };
    },
    async cancel(input) {
      permissions.cancelForThread(input.threadId);
      const liveThread = live.get(input.threadId);
      if (!liveThread) return;
      await liveThread.pipe.forceFlush();
      if (liveThread.kind === "codex") {
        const turnId = liveThread.activePrompt?.turnId;
        if (turnId) {
          await liveThread.client.interrupt({ threadId: liveThread.threadId, turnId });
        }
        resolveCodexPrompt(liveThread, "cancelled");
        return;
      }
      await liveThread.client.cancel({ sessionId: liveThread.sessionId });
    },
    async respondPermission(input) {
      if (!permissions.isKnown(input.threadId, input.requestId)) {
        throw new PermissionRequestNotFoundError(input.threadId, input.requestId);
      }
      const thread = await opts.store.get(input.threadId);
      if (!thread) throw new ThreadNotFoundError(input.threadId);
      permissions.respond(input.threadId, input.requestId, input.optionId);
      return { responded: true };
    },
    async revert(input) {
      opts.busEmit({
        type: "chat.thread.reverted",
        threadId: input.threadId,
        toCheckpointRef: input.toCheckpointRef
      });
    },
    createSession(input) {
      return sessionStore.add({
        threadId: input.threadId,
        ...input.id ? { id: input.id } : {},
        providerName: input.provider.kind,
        ...input.providerInstanceId ? { providerInstanceId: input.providerInstanceId } : {},
        ...input.role ? { role: input.role } : {},
        ...input.displayName ? { displayName: input.displayName } : {},
        status: "idle"
      });
    },
    removeSession(input) {
      return sessionStore.remove(input.threadId, input.sessionId);
    },
    listSessions(threadId) {
      return sessionStore.list(threadId);
    },
    async getResolvedProvider(threadId) {
      if (!opts.providerStore) return null;
      const thread = await opts.store.get(threadId);
      if (!thread) return null;
      const instanceId = thread.providerInstanceId;
      const sessionInstanceId = instanceId ? null : sessionStore.list(threadId).find((s) => s.providerInstanceId)?.providerInstanceId ?? null;
      const id = instanceId ?? sessionInstanceId;
      if (!id) return null;
      return opts.providerStore.get(id);
    },
    async disposeLive(threadId) {
      await disposeLiveImmediate(threadId);
    },
    async shutdown() {
      const closing = [...live.entries()].map(async ([threadId, liveThread]) => {
        permissions.cancelForThread(threadId);
        await liveThread.pipe.forceFlush();
        live.delete(threadId);
        for (const unsub of liveThread.unsubs.splice(0)) {
          try {
            unsub();
          } catch {
          }
        }
        await liveThread.client.close();
      });
      await Promise.allSettled(closing);
      permissions.cancelAll();
    }
  };
}
var DEFAULT_PERMISSION_TIMEOUT_MS, DEFAULT_PERSIST_DEBOUNCE_MS, DEFAULT_TEXT_COALESCE_WINDOW_MS, ThreadNotFoundError;
var init_thread_manager = __esm({
  "packages/daemon/src/chat/thread-manager.ts"() {
    "use strict";
    init_codex();
    init_debug_log();
    init_session_store();
    init_usage_extraction();
    init_codex_helpers();
    init_codex_event_handler();
    init_permission_helpers();
    init_permission_coordinator();
    init_message_pipe();
    init_dispatch_prompt();
    init_provider_discovery();
    DEFAULT_PERMISSION_TIMEOUT_MS = 6e4;
    DEFAULT_PERSIST_DEBOUNCE_MS = 750;
    DEFAULT_TEXT_COALESCE_WINDOW_MS = 16;
    ThreadNotFoundError = class extends Error {
      threadId;
      constructor(threadId) {
        super(`Thread ${threadId} not found`);
        this.name = "ThreadNotFoundError";
        this.threadId = threadId;
      }
    };
  }
});

// packages/daemon/src/chat/thread-store.ts
import { randomUUID as randomUUID7 } from "node:crypto";
import { mkdir, readFile, rename, rm, writeFile } from "node:fs/promises";
import { dirname as dirname9, join as join20 } from "node:path";
function clone2(value) {
  return structuredClone(value);
}
function cleanTitle(title) {
  const trimmed = title?.trim();
  return trimmed ? trimmed.slice(0, 80) : "New chat";
}
function titleFromFirstPrompt(msg) {
  if (msg._tag !== "UserPrompt") return null;
  const text = msg.content.find(
    (block) => block.type === "text" && block.text.trim().length > 0
  );
  return text ? cleanTitle(text.text) : null;
}
function threadPath(rootDir, id) {
  return join20(rootDir, "threads", `${id}.json`);
}
function indexPath(rootDir) {
  return join20(rootDir, "threads.json");
}
async function readJson2(path4) {
  try {
    return JSON.parse(await readFile(path4, "utf-8"));
  } catch (err) {
    if (err.code === "ENOENT") return null;
    throw err;
  }
}
async function writeJsonAtomic(path4, value, randomId) {
  await mkdir(dirname9(path4), { recursive: true });
  const tmp = `${path4}.tmp.${process.pid}.${randomId()}`;
  try {
    await writeFile(tmp, `${JSON.stringify(value, null, 2)}
`, "utf-8");
    await rename(tmp, path4);
  } finally {
    await rm(tmp, { force: true }).catch(() => void 0);
  }
}
function makeThreadStore(opts) {
  const now = opts.now ?? (() => /* @__PURE__ */ new Date());
  const randomId = opts.randomId ?? randomUUID7;
  const rootDir = opts.rootDir;
  const threadsDir = join20(rootDir, "threads");
  let hydrated = false;
  let index = [];
  const states = /* @__PURE__ */ new Map();
  let writeQueue = Promise.resolve();
  async function hydrate() {
    if (hydrated) return;
    await mkdir(threadsDir, { recursive: true });
    const rawIndex = await readJson2(indexPath(rootDir));
    index = rawIndex?.version === 1 && Array.isArray(rawIndex.threads) ? rawIndex.threads : [];
    states.clear();
    for (const entry of index) {
      const state = await readJson2(threadPath(rootDir, entry.id));
      if (state) states.set(entry.id, state);
    }
    index = index.filter((entry) => states.has(entry.id));
    hydrated = true;
  }
  async function persistIndex() {
    await writeJsonAtomic(indexPath(rootDir), { version: 1, threads: index }, randomId);
  }
  async function persistState(state) {
    await writeJsonAtomic(threadPath(rootDir, state.id), state, randomId);
  }
  function enqueue(op) {
    const run2 = writeQueue.then(op, op);
    writeQueue = run2.then(
      () => void 0,
      () => void 0
    );
    return run2;
  }
  function entryFromState(state, existing) {
    const entry = {
      id: state.id,
      title: state.title,
      createdAt: state.createdAt,
      updatedAt: state.updatedAt,
      providerKind: state.provider.kind,
      messageCount: state.messages.length,
      ...state.projectDir ? { projectDir: state.projectDir } : {},
      ...existing?.lastStopReason ? { lastStopReason: existing.lastStopReason } : {}
    };
    return entry;
  }
  function findEntry(id) {
    return index.find((entry) => entry.id === id);
  }
  function appendMessages(id, messages) {
    return enqueue(async () => {
      if (messages.length === 0) return;
      const { state, entry } = await updateThread(id, (nextState, nextEntry) => {
        const updatedAt = now().toISOString();
        const firstPromptTitle = nextState.messages.length === 0 && nextState.title === "New chat" ? messages.reduce(
          (found, message) => found ?? titleFromFirstPrompt(message),
          null
        ) : null;
        if (firstPromptTitle) {
          nextState.title = firstPromptTitle;
          nextEntry.title = firstPromptTitle;
        }
        nextState.messages.push(...messages.map(clone2));
        nextState.updatedAt = updatedAt;
        nextEntry.updatedAt = updatedAt;
        nextEntry.messageCount = nextState.messages.length;
      });
      states.set(id, state);
      index = index.map((candidate) => candidate.id === id ? entry : candidate);
    });
  }
  async function updateThread(id, update) {
    await hydrate();
    const state = states.get(id);
    const entry = findEntry(id);
    if (!state || !entry) throw new Error(`Thread ${id} not found`);
    update(state, entry);
    await persistState(state);
    await persistIndex();
    return { state, entry };
  }
  return {
    async list() {
      await hydrate();
      return clone2(index);
    },
    async get(id) {
      await hydrate();
      const state = states.get(id);
      return state ? clone2(state) : null;
    },
    create(input) {
      return enqueue(async () => {
        await hydrate();
        const id = randomId();
        const createdAt = now().toISOString();
        const state = {
          id,
          title: cleanTitle(input.title),
          createdAt,
          updatedAt: createdAt,
          provider: clone2(input.provider),
          ...input.providerInstanceId ? { providerInstanceId: input.providerInstanceId } : {},
          ...input.projectDir ? { projectDir: input.projectDir } : {},
          messages: []
        };
        const entry = entryFromState(state);
        states.set(id, state);
        index = [entry, ...index];
        await persistState(state);
        await persistIndex();
        return clone2(state);
      });
    },
    rename(id, title) {
      return enqueue(async () => {
        const { state, entry } = await updateThread(id, (nextState, nextEntry) => {
          const updatedAt = now().toISOString();
          nextState.title = cleanTitle(title);
          nextState.updatedAt = updatedAt;
          nextEntry.title = nextState.title;
          nextEntry.updatedAt = updatedAt;
        });
        states.set(id, state);
        return clone2(entry);
      });
    },
    setProvider(id, provider) {
      return enqueue(async () => {
        const { state, entry } = await updateThread(id, (nextState, nextEntry) => {
          const updatedAt = now().toISOString();
          nextState.provider = clone2(provider);
          nextState.acpSessionId = void 0;
          nextState.updatedAt = updatedAt;
          nextEntry.providerKind = provider.kind;
          nextEntry.updatedAt = updatedAt;
        });
        states.set(id, state);
        return clone2(entry);
      });
    },
    delete(id) {
      return enqueue(async () => {
        await hydrate();
        states.delete(id);
        index = index.filter((entry) => entry.id !== id);
        await rm(threadPath(rootDir, id), { force: true });
        await persistIndex();
      });
    },
    truncateFromUserMessage(id, userMessageId) {
      return enqueue(async () => {
        const result = { truncatedCount: 0 };
        const { state, entry } = await updateThread(id, (nextState, nextEntry) => {
          const idx = nextState.messages.findIndex((message) => {
            if (message._tag !== "UserPrompt") return false;
            return message.id === userMessageId;
          });
          if (idx === -1) {
            const exists = nextState.messages.some(
              (message) => message._tag === "UserPrompt" ? message.id === userMessageId : message.id === userMessageId
            );
            if (exists) {
              throw new Error(`Message ${userMessageId} in thread ${id} is not a user prompt`);
            }
            throw new Error(`User message ${userMessageId} not found in thread ${id}`);
          }
          const dropped = nextState.messages.length - idx;
          nextState.messages.splice(idx);
          const updatedAt = now().toISOString();
          nextState.updatedAt = updatedAt;
          nextEntry.updatedAt = updatedAt;
          nextEntry.messageCount = nextState.messages.length;
          result.truncatedCount = dropped;
        });
        states.set(id, state);
        index = index.map((candidate) => candidate.id === id ? entry : candidate);
        return result;
      });
    },
    appendMessage(id, msg) {
      return appendMessages(id, [msg]);
    },
    appendMessages,
    recordAcpSessionId(id, acpSessionId) {
      return enqueue(async () => {
        const { state } = await updateThread(id, (nextState) => {
          nextState.acpSessionId = acpSessionId;
        });
        states.set(id, state);
      });
    },
    recordUsage(id, usage) {
      return enqueue(async () => {
        const { state } = await updateThread(id, (nextState, nextEntry) => {
          const updatedAt = now().toISOString();
          nextState.usage = clone2(usage);
          nextState.updatedAt = updatedAt;
          nextEntry.updatedAt = updatedAt;
        });
        states.set(id, state);
      });
    },
    recordStopReason(id, reason) {
      return enqueue(async () => {
        const { state } = await updateThread(id, (nextState, nextEntry) => {
          const updatedAt = now().toISOString();
          nextState.updatedAt = updatedAt;
          nextEntry.updatedAt = updatedAt;
          nextEntry.lastStopReason = reason;
        });
        states.set(id, state);
      });
    }
  };
}
var init_thread_store = __esm({
  "packages/daemon/src/chat/thread-store.ts"() {
    "use strict";
  }
});

// packages/daemon/src/chat/plan-store.ts
function makePlanStore(opts = {}) {
  const byThread = /* @__PURE__ */ new Map();
  const emit = opts.emit ?? (() => void 0);
  const now = opts.now ?? (() => /* @__PURE__ */ new Date());
  function bucket(threadId) {
    let b = byThread.get(threadId);
    if (!b) {
      b = /* @__PURE__ */ new Map();
      byThread.set(threadId, b);
    }
    return b;
  }
  function getOrThrow(threadId, planId) {
    const b = byThread.get(threadId);
    const existing = b?.get(planId);
    if (!existing) throw new PlanNotFoundError(threadId, planId);
    return existing;
  }
  function sortedList(threadId) {
    const b = byThread.get(threadId);
    if (!b) return [];
    return [...b.values()].sort((a, b2) => a.createdAt.localeCompare(b2.createdAt));
  }
  return {
    upsert(threadId, plan) {
      bucket(threadId).set(plan.id, plan);
      emit({ type: "chat.plan.upserted", threadId, plan });
      return plan;
    },
    get(threadId, planId) {
      return byThread.get(threadId)?.get(planId) ?? null;
    },
    list(threadId) {
      return sortedList(threadId);
    },
    listForThread(threadId) {
      return sortedList(threadId);
    },
    markImplemented(threadId, planId, implementationThreadId, implementedAt) {
      const existing = getOrThrow(threadId, planId);
      const ts = implementedAt ?? now().toISOString();
      const next = {
        ...existing,
        implementedAt: ts,
        implementationThreadId,
        updatedAt: ts
      };
      bucket(threadId).set(planId, next);
      emit({ type: "chat.plan.upserted", threadId, plan: next });
      return next;
    },
    markRejected(threadId, planId, options = {}) {
      const existing = getOrThrow(threadId, planId);
      const ts = options.at ?? now().toISOString();
      const rejection = options.reason !== void 0 ? { at: ts, reason: options.reason } : { at: ts };
      const next = {
        ...existing,
        rejected: rejection,
        updatedAt: ts
      };
      bucket(threadId).set(planId, next);
      emit({ type: "chat.plan.upserted", threadId, plan: next });
      return next;
    },
    remove(threadId, planId) {
      return byThread.get(threadId)?.delete(planId) ?? false;
    },
    clear(threadId) {
      byThread.delete(threadId);
    }
  };
}
var PlanNotFoundError;
var init_plan_store = __esm({
  "packages/daemon/src/chat/plan-store.ts"() {
    "use strict";
    PlanNotFoundError = class extends Error {
      threadId;
      planId;
      constructor(threadId, planId) {
        super(`Plan ${planId} not found on thread ${threadId}`);
        this.name = "PlanNotFoundError";
        this.threadId = threadId;
        this.planId = planId;
      }
    };
  }
});

// packages/daemon/src/chat/plan-orchestrator.ts
function makePlanOrchestrator(opts) {
  const isTurnRunning = opts.isTurnRunning ?? (() => false);
  return {
    list(threadId) {
      return opts.planStore.list(threadId);
    },
    get(threadId, planId) {
      return opts.planStore.get(threadId, planId);
    },
    async approve(input) {
      const existing = opts.planStore.get(input.threadId, input.planId);
      if (!existing) throw new PlanNotFoundError(input.threadId, input.planId);
      if (existing.implementedAt) {
        throw new PlanAlreadyImplementedError(input.threadId, input.planId);
      }
      if (existing.rejected) {
        throw new PlanAlreadyRejectedError(input.threadId, input.planId);
      }
      if (isTurnRunning(input.threadId)) {
        throw new TurnAlreadyRunningError(input.threadId);
      }
      const { turnId } = await opts.sendTurn({
        threadId: input.threadId,
        planMarkdown: existing.planMarkdown,
        sourceProposedPlan: { threadId: input.threadId, planId: input.planId },
        ...input.runtimeMode ? { runtimeMode: input.runtimeMode } : {}
      });
      const stamped = opts.planStore.markImplemented(input.threadId, input.planId, input.threadId);
      return { plan: stamped, turnId };
    },
    reject(input) {
      const existing = opts.planStore.get(input.threadId, input.planId);
      if (!existing) throw new PlanNotFoundError(input.threadId, input.planId);
      if (existing.implementedAt) {
        throw new PlanAlreadyImplementedError(input.threadId, input.planId);
      }
      if (existing.rejected) {
        throw new PlanAlreadyRejectedError(input.threadId, input.planId);
      }
      return opts.planStore.markRejected(input.threadId, input.planId, {
        ...input.reason !== void 0 ? { reason: input.reason } : {}
      });
    }
  };
}
var PlanAlreadyImplementedError, PlanAlreadyRejectedError, TurnAlreadyRunningError;
var init_plan_orchestrator = __esm({
  "packages/daemon/src/chat/plan-orchestrator.ts"() {
    "use strict";
    init_plan_store();
    PlanAlreadyImplementedError = class extends Error {
      threadId;
      planId;
      constructor(threadId, planId) {
        super(`Plan ${planId} on thread ${threadId} is already implemented`);
        this.name = "PlanAlreadyImplementedError";
        this.threadId = threadId;
        this.planId = planId;
      }
    };
    PlanAlreadyRejectedError = class extends Error {
      threadId;
      planId;
      constructor(threadId, planId) {
        super(`Plan ${planId} on thread ${threadId} is already rejected`);
        this.name = "PlanAlreadyRejectedError";
        this.threadId = threadId;
        this.planId = planId;
      }
    };
    TurnAlreadyRunningError = class extends Error {
      threadId;
      constructor(threadId) {
        super(`A turn is already running on thread ${threadId}`);
        this.name = "TurnAlreadyRunningError";
        this.threadId = threadId;
      }
    };
  }
});

// packages/daemon/src/chat/checkpoint-store.ts
function makeCheckpointStore(opts = {}) {
  const byThread = /* @__PURE__ */ new Map();
  const emit = opts.emit ?? (() => void 0);
  function bucket(threadId) {
    let b = byThread.get(threadId);
    if (!b) {
      b = /* @__PURE__ */ new Map();
      byThread.set(threadId, b);
    }
    return b;
  }
  return {
    upsert(threadId, summary) {
      const b = bucket(threadId);
      const existed = b.has(summary.turnId);
      b.set(summary.turnId, summary);
      if (!existed) {
        emit({ type: "chat.checkpoint.created", threadId, checkpoint: summary });
      }
      return summary;
    },
    get(threadId, turnId) {
      return byThread.get(threadId)?.get(turnId) ?? null;
    },
    list(threadId) {
      const b = byThread.get(threadId);
      if (!b) return [];
      return [...b.values()].sort((a, b2) => a.checkpointTurnCount - b2.checkpointTurnCount);
    },
    remove(threadId, turnId) {
      const b = byThread.get(threadId);
      if (!b) return false;
      return b.delete(turnId);
    },
    updateStatus(threadId, turnId, status4) {
      const b = bucket(threadId);
      const existing = b.get(turnId);
      if (!existing) {
        throw new CheckpointStoreError(
          `Checkpoint for turn ${turnId} not found in thread ${threadId}`,
          "not_found"
        );
      }
      const next = { ...existing, status: status4 };
      b.set(turnId, next);
      return next;
    },
    clear(threadId) {
      byThread.delete(threadId);
    }
  };
}
var CheckpointStoreError;
var init_checkpoint_store = __esm({
  "packages/daemon/src/chat/checkpoint-store.ts"() {
    "use strict";
    CheckpointStoreError = class extends Error {
      constructor(message, code) {
        super(message);
        this.code = code;
        this.name = "CheckpointStoreError";
      }
    };
  }
});

// packages/daemon/src/chat/defaults.ts
var defaults_exports = {};
__export(defaults_exports, {
  _resetDefaultChatRuntimeForTests: () => _resetDefaultChatRuntimeForTests,
  defaultChatRootDir: () => defaultChatRootDir,
  getDefaultChatEventStore: () => getDefaultChatEventStore,
  getDefaultCheckpointStore: () => getDefaultCheckpointStore,
  getDefaultPlanOrchestrator: () => getDefaultPlanOrchestrator,
  getDefaultPlanStore: () => getDefaultPlanStore,
  getDefaultSessionStore: () => getDefaultSessionStore,
  getDefaultThreadManager: () => getDefaultThreadManager,
  getDefaultThreadStore: () => getDefaultThreadStore,
  getDefaultTurnDiffProjection: () => getDefaultTurnDiffProjection,
  getDefaultTurnProjection: () => getDefaultTurnProjection,
  initializeDefaultChatRuntime: () => initializeDefaultChatRuntime,
  shutdownDefaultChatRuntime: () => shutdownDefaultChatRuntime
});
import { mkdirSync as mkdirSync17 } from "node:fs";
import { join as join21 } from "node:path";
import { homedir as homedir7 } from "node:os";
import { randomUUID as randomUUID8 } from "node:crypto";
function isChatThreadEvent(event) {
  return CHAT_THREAD_EVENT_TYPE_SET.has(event.type);
}
function inferActorKind(event) {
  switch (event.type) {
    case "chat.activity.appended":
      return "user";
    case "chat.turn.started":
    case "chat.turn.completed":
    case "chat.turn.aborted":
      return "provider";
    case "chat.session.added":
    case "chat.session.removed":
    case "chat.session.status-changed":
      return "system";
    case "chat.plan.upserted":
    case "chat.checkpoint.created":
    case "chat.thread.reverted":
      return "system";
  }
}
function defaultChatRootDir() {
  return process.env.TMUX_IDE_CHATS_DIR ?? join21(homedir7(), ".tmux-ide", "chats");
}
function getDefaultEventDb() {
  if (defaultEventDb) return defaultEventDb;
  const dir = defaultChatRootDir();
  mkdirSync17(dir, { recursive: true });
  const path4 = process.env.TMUX_IDE_CHAT_EVENTS_DB ?? join21(dir, "events.sqlite");
  defaultEventDb = openDatabase(path4);
  return defaultEventDb;
}
function getDefaultChatEventStore() {
  defaultEventStore ??= makeChatEventStore(getDefaultEventDb());
  return defaultEventStore;
}
function getDefaultTurnProjection() {
  if (defaultTurnProjection) return defaultTurnProjection;
  const store = getDefaultChatEventStore();
  const projection = makeTurnProjection({
    reader: store,
    cursorStore: makeInMemoryCursorStore()
  });
  projection.start();
  defaultTurnProjection = projection;
  return projection;
}
function getDefaultTurnDiffProjection() {
  if (defaultTurnDiffProjection) return defaultTurnDiffProjection;
  const store = getDefaultChatEventStore();
  const projection = makeTurnDiffProjection({
    reader: store,
    cursorStore: makeInMemoryCursorStore()
  });
  projection.start();
  defaultTurnDiffProjection = projection;
  return projection;
}
function persistAndBroadcast(event) {
  if (isChatThreadEvent(event)) {
    try {
      getDefaultChatEventStore().append({
        event,
        actorKind: inferActorKind(event)
      });
    } catch (err) {
      console.error("[chat-event-store] persist failed:", err);
    }
  }
  broadcastChatEvent(event);
  for (const derived of translateLegacyToV2(event)) {
    broadcastChatEvent(derived);
  }
}
function getDefaultThreadStore() {
  defaultStore ??= makeThreadStore({ rootDir: defaultChatRootDir() });
  return defaultStore;
}
function getDefaultThreadManager() {
  defaultManager ??= makeThreadManager({
    store: getDefaultThreadStore(),
    spawnClient: (provider, opts) => spawnAcpClient({ provider, cwd: opts.cwd }),
    busEmit: (event) => persistAndBroadcast(event),
    logger: (event) => {
      if (event.level === "error") console.error("[chat]", event.msg, event.data ?? "");
      else if (event.level === "warn") console.warn("[chat]", event.msg, event.data ?? "");
    }
  });
  return defaultManager;
}
function getDefaultPlanStore() {
  defaultPlanStore ??= makePlanStore({
    emit: (event) => persistAndBroadcast(event)
  });
  return defaultPlanStore;
}
function getDefaultSessionStore() {
  defaultSessionStore ??= makeSessionStore({
    emit: (event) => persistAndBroadcast(event)
  });
  return defaultSessionStore;
}
function getDefaultCheckpointStore() {
  defaultCheckpointStore ??= makeCheckpointStore({
    emit: (event) => persistAndBroadcast(event)
  });
  return defaultCheckpointStore;
}
function getDefaultPlanOrchestrator() {
  if (defaultPlanOrchestrator) return defaultPlanOrchestrator;
  const planStore = getDefaultPlanStore();
  const manager2 = getDefaultThreadManager();
  defaultPlanOrchestrator = makePlanOrchestrator({
    planStore,
    isTurnRunning: (threadId) => activeTurns.has(threadId),
    sendTurn: async ({ threadId, planMarkdown }) => {
      const turnId = randomUUID8();
      activeTurns.set(threadId, turnId);
      try {
        await manager2.send({
          threadId,
          content: [{ type: "text", text: planMarkdown }]
        });
      } finally {
        activeTurns.delete(threadId);
      }
      return { turnId };
    }
  });
  return defaultPlanOrchestrator;
}
function initializeDefaultChatRuntime() {
  const store = getDefaultThreadStore();
  getDefaultThreadManager();
  getDefaultPlanStore();
  getDefaultPlanOrchestrator();
  getDefaultChatEventStore();
  getDefaultTurnProjection();
  if (bootIndexEmitted) return;
  bootIndexEmitted = true;
  void store.list().then((threads) => {
    persistAndBroadcast({ type: "chat.thread.index", threads });
  });
}
async function shutdownDefaultChatRuntime() {
  await defaultManager?.shutdown();
  defaultManager = null;
  defaultTurnProjection?.stop();
  defaultTurnProjection = null;
  defaultTurnDiffProjection?.stop();
  defaultTurnDiffProjection = null;
  try {
    defaultEventDb?.close();
  } catch {
  }
  defaultEventDb = null;
  defaultEventStore = null;
}
function _resetDefaultChatRuntimeForTests() {
  defaultStore = null;
  defaultManager = null;
  defaultPlanStore = null;
  defaultPlanOrchestrator = null;
  defaultSessionStore = null;
  defaultCheckpointStore = null;
  defaultTurnProjection?.stop();
  defaultTurnProjection = null;
  defaultTurnDiffProjection?.stop();
  defaultTurnDiffProjection = null;
  try {
    defaultEventDb?.close();
  } catch {
  }
  defaultEventDb = null;
  defaultEventStore = null;
  bootIndexEmitted = false;
  activeTurns.clear();
}
var defaultStore, defaultManager, defaultPlanStore, defaultPlanOrchestrator, defaultSessionStore, defaultCheckpointStore, defaultEventDb, defaultEventStore, defaultTurnProjection, defaultTurnDiffProjection, bootIndexEmitted, activeTurns, CHAT_THREAD_EVENT_TYPE_SET;
var init_defaults = __esm({
  "packages/daemon/src/chat/defaults.ts"() {
    "use strict";
    init_src2();
    init_acp();
    init_ws_events();
    init_legacy_to_v2();
    init_sqlite_adapter();
    init_chat_event_store();
    init_turn_projection();
    init_turn_diff_projection();
    init_types();
    init_thread_manager();
    init_thread_store();
    init_plan_store();
    init_plan_orchestrator();
    init_session_store();
    init_checkpoint_store();
    defaultStore = null;
    defaultManager = null;
    defaultPlanStore = null;
    defaultPlanOrchestrator = null;
    defaultSessionStore = null;
    defaultCheckpointStore = null;
    defaultEventDb = null;
    defaultEventStore = null;
    defaultTurnProjection = null;
    defaultTurnDiffProjection = null;
    bootIndexEmitted = false;
    activeTurns = /* @__PURE__ */ new Map();
    CHAT_THREAD_EVENT_TYPE_SET = new Set(
      CHAT_THREAD_EVENT_TYPES
    );
  }
});

// packages/daemon/src/command-center/actions/handlers/chat-actions.ts
import { isAbsolute as isAbsolute4 } from "node:path";
function resetChatProvidersListCache() {
  providerCache = null;
  providerCachePending = null;
}
function storeFrom(deps2) {
  return deps2.store ?? getDefaultThreadStore();
}
function sessionStoreFrom(deps2) {
  return deps2.sessionStore ?? getDefaultSessionStore();
}
function checkpointStoreFrom(deps2) {
  return deps2.checkpointStore ?? getDefaultCheckpointStore();
}
function managerFrom(deps2) {
  return deps2.manager ?? getDefaultThreadManager();
}
function busFrom(deps2) {
  return deps2.busEmit ?? broadcastChatEvent;
}
function badRequest(message, details) {
  return new ActionError({ code: "bad_request", message, details });
}
function threadNotFound(id) {
  return new ActionError({
    code: "thread_not_found",
    message: `Chat thread "${id}" not found`,
    details: { id }
  });
}
function permissionRequestNotFound(threadId, requestId) {
  return new ActionError({
    code: "permission_request_not_found",
    message: `Permission request "${requestId}" not found for thread "${threadId}"`,
    details: { threadId, requestId }
  });
}
function mapPermissionRespondError(err, input) {
  if (err instanceof ThreadNotFoundError) {
    throw threadNotFound(err.threadId);
  }
  if (err instanceof PermissionRequestNotFoundError) {
    throw permissionRequestNotFound(err.threadId, err.requestId);
  }
  if (err instanceof InvalidPermissionOptionError) {
    throw badRequest(err.message, {
      threadId: input.threadId,
      requestId: err.requestId,
      optionId: err.optionId
    });
  }
  throw err;
}
async function requireThread(store, id) {
  const thread = await store.get(id);
  if (!thread) throw threadNotFound(id);
  return thread;
}
async function entryFor(store, id) {
  const entry = (await store.list()).find((candidate) => candidate.id === id);
  if (!entry) throw threadNotFound(id);
  return entry;
}
async function emitIndex(store, emit) {
  emit({ type: "chat.thread.index", threads: await store.list() });
}
async function chatThreadListHandler(input, deps2 = {}) {
  const all = await storeFrom(deps2).list();
  if (input.projectDir === void 0) return { threads: all };
  const threads = all.filter((t) => t.projectDir === input.projectDir);
  return { threads };
}
async function chatProvidersListHandler(_input, deps2 = {}) {
  const now = deps2.now?.() ?? Date.now();
  if (providerCache && providerCache.expiresAt > now) {
    return { providers: providerCache.providers };
  }
  providerCachePending ??= discoverProviders(deps2.discover);
  let providers;
  try {
    providers = await providerCachePending;
  } finally {
    providerCachePending = null;
  }
  providerCache = { providers, expiresAt: now + PROVIDER_CACHE_TTL_MS };
  return { providers };
}
async function chatThreadCreateHandler(input, deps2 = {}) {
  if (input.projectDir && !isAbsolute4(input.projectDir)) {
    throw badRequest("projectDir must be an absolute path", { projectDir: input.projectDir });
  }
  const store = storeFrom(deps2);
  const state = await store.create(input);
  const thread = await entryFor(store, state.id);
  await emitIndex(store, busFrom(deps2));
  return { thread };
}
async function chatThreadDeleteHandler(input, deps2 = {}) {
  const store = storeFrom(deps2);
  await requireThread(store, input.id);
  sessionStoreFrom(deps2).clear(input.id);
  checkpointStoreFrom(deps2).clear(input.id);
  await store.delete(input.id);
  await emitIndex(store, busFrom(deps2));
  return { deleted: true };
}
async function chatThreadRenameHandler(input, deps2 = {}) {
  const title = input.title.trim();
  if (!title) throw badRequest("title must not be blank");
  const store = storeFrom(deps2);
  await requireThread(store, input.id);
  const thread = await store.rename(input.id, title);
  await emitIndex(store, busFrom(deps2));
  return { thread };
}
async function chatThreadSetProviderHandler(input, deps2 = {}) {
  const store = storeFrom(deps2);
  await requireThread(store, input.id);
  const thread = await store.setProvider(input.id, input.provider);
  await managerFrom(deps2).disposeLive(input.id).catch(() => void 0);
  await emitIndex(store, busFrom(deps2));
  return { thread };
}
async function chatThreadGetHandler(input, deps2 = {}) {
  const thread = await requireThread(storeFrom(deps2), input.id);
  return { thread, timeline: materializeRows(thread.messages) };
}
async function chatThreadUsageHandler(input, deps2 = {}) {
  const thread = await requireThread(storeFrom(deps2), input.id);
  return { usage: thread.usage ?? null };
}
async function chatSessionSendHandler(input, deps2 = {}) {
  await requireThread(storeFrom(deps2), input.threadId);
  const { promptId } = await managerFrom(deps2).send({
    threadId: input.threadId,
    content: input.content,
    ...input.model ? { model: input.model } : {},
    ...input.provider?.kind ? { providerKind: input.provider.kind } : {},
    ...input.providerOptions ? { providerOptions: input.providerOptions } : {}
  });
  return { accepted: true, promptId };
}
async function chatSessionCancelHandler(input, deps2 = {}) {
  await requireThread(storeFrom(deps2), input.threadId);
  await managerFrom(deps2).cancel(input);
  return { cancelled: true };
}
async function chatSessionEditFromTurnHandler(input, deps2 = {}) {
  const store = storeFrom(deps2);
  const manager2 = managerFrom(deps2);
  await requireThread(store, input.threadId);
  try {
    await manager2.cancel({ threadId: input.threadId });
  } catch (err) {
    if (!(err instanceof ThreadNotFoundError)) throw err;
  }
  let truncatedCount;
  try {
    const result = await store.truncateFromUserMessage(input.threadId, input.userMessageId);
    truncatedCount = result.truncatedCount;
  } catch (err) {
    if (err instanceof Error) {
      if (err.message.includes("not found")) {
        throw badRequest(err.message, {
          threadId: input.threadId,
          userMessageId: input.userMessageId
        });
      }
      if (err.message.includes("is not a user prompt")) {
        throw badRequest(err.message, {
          threadId: input.threadId,
          userMessageId: input.userMessageId
        });
      }
    }
    throw err;
  }
  const { promptId } = await manager2.send({
    threadId: input.threadId,
    content: input.content
  });
  return { accepted: true, promptId, truncatedCount };
}
async function chatPermissionRespondHandler(input, deps2 = {}) {
  try {
    return await managerFrom(deps2).respondPermission(input);
  } catch (err) {
    mapPermissionRespondError(err, input);
  }
}
var PROVIDER_CACHE_TTL_MS, providerCache, providerCachePending;
var init_chat_actions = __esm({
  "packages/daemon/src/command-center/actions/handlers/chat-actions.ts"() {
    "use strict";
    init_defaults();
    init_provider_discovery();
    init_thread_manager();
    init_materialize();
    init_ws_events();
    init_errors3();
    PROVIDER_CACHE_TTL_MS = 5e3;
    providerCache = null;
    providerCachePending = null;
  }
});

// packages/daemon/src/command-center/actions/handlers/daemon-shutdown.ts
function setDaemonShutdownBackend(backend2) {
  shutdownBackend = backend2;
  if (!backend2) shutdownInProgress = false;
}
function daemonShutdownHandler(input, deps2 = {}) {
  if (shutdownInProgress) {
    throw new ActionError({
      code: "shutdown_already_in_progress",
      message: "Daemon shutdown is already in progress"
    });
  }
  shutdownInProgress = true;
  resetChatProvidersListCache();
  const shutdown = deps2.shutdown ?? shutdownBackend;
  process.nextTick(() => {
    void Promise.resolve(shutdown?.(input.reason ?? null)).catch((err) => {
      console.error("[daemon] shutdown action failed:", err);
    });
  });
  return { stopping: true };
}
var shutdownBackend, shutdownInProgress;
var init_daemon_shutdown = __esm({
  "packages/daemon/src/command-center/actions/handlers/daemon-shutdown.ts"() {
    "use strict";
    init_errors3();
    init_chat_actions();
    shutdownBackend = null;
    shutdownInProgress = false;
  }
});

// packages/daemon/src/lib/active-projects.ts
function setActivationBackend(next) {
  backend = next;
  active.clear();
}
async function activateProject(name, options = {}) {
  if (active.has(name) && !options.orchestrate) return;
  if (!backend) {
    throw new Error("No active-project backend is registered");
  }
  await backend.activateProject(name, options);
  active.add(name);
}
var backend, active;
var init_active_projects = __esm({
  "packages/daemon/src/lib/active-projects.ts"() {
    "use strict";
    backend = null;
    active = /* @__PURE__ */ new Set();
  }
});

// packages/daemon/src/send.ts
import { randomUUID as randomUUID9 } from "node:crypto";
import { resolve as resolve16, join as join22 } from "node:path";
import { existsSync as existsSync21, mkdirSync as mkdirSync18, writeFileSync as writeFileSync16 } from "node:fs";
function writeDispatchFile(dir, paneId, message) {
  if (message.length <= LONG_MESSAGE_THRESHOLD) return null;
  const dispatchDir = join22(dir, ".tasks", "dispatch");
  if (!existsSync21(dispatchDir)) mkdirSync18(dispatchDir, { recursive: true });
  const paneSlug = paneId.replace("%", "");
  const filename = `send-${paneSlug}-${Date.now()}-${randomUUID9().slice(0, 8)}.md`;
  const filePath = join22(dispatchDir, filename);
  writeFileSync16(filePath, message);
  return { filePath, triggerCmd: `Read and execute: .tasks/dispatch/${filename}` };
}
function resolvePane(panes, target) {
  if (target.startsWith("%")) {
    return panes.find((p) => p.id === target) ?? null;
  }
  const byName = panes.find((p) => p.name === target);
  if (byName) return byName;
  const byTitle = panes.find((p) => p.title === target);
  if (byTitle) return byTitle;
  const lower = target.toLowerCase();
  if (["lead", "teammate", "planner"].includes(lower)) {
    const byRole = panes.find((p) => p.role === lower);
    if (byRole) return byRole;
  }
  const byPattern = panes.find((p) => p.title.toLowerCase().includes(lower));
  if (byPattern) return byPattern;
  return null;
}
function prepareMessage(message, busyStatus) {
  if (busyStatus === "agent") {
    return message.replace(/\n+/g, " ").trim();
  }
  return message;
}
async function send(targetDir, opts) {
  const dir = resolve16(targetDir ?? ".");
  const { name: session } = getSessionName(dir);
  const { json: json2, to: target, message: rawMessage, noEnter } = opts;
  if (!target) {
    throw new IdeError("Missing target. Usage: tmux-ide send <target> <message>", {
      code: "USAGE"
    });
  }
  if (!rawMessage) {
    throw new IdeError("Missing message. Usage: tmux-ide send <target> <message>", {
      code: "USAGE"
    });
  }
  const state = getSessionState(session);
  if (!state.running) {
    throw new IdeError(`Session "${session}" is not running`, {
      code: "SESSION_NOT_FOUND"
    });
  }
  const panes = listSessionPanes(session);
  const pane = resolvePane(panes, target);
  if (!pane) {
    const available = panes.map((p) => {
      const label2 = p.name ?? p.title;
      return `  ${p.id}  ${label2}${p.role ? ` (${p.role})` : ""}`;
    }).join("\n");
    throw new IdeError(`Pane "${target}" not found.

Available panes:
${available}`, {
      code: "PANE_NOT_FOUND"
    });
  }
  const busyStatus = getPaneBusyStatus(session, pane.id);
  const message = prepareMessage(rawMessage, busyStatus);
  let sentViaFile = false;
  if (noEnter) {
    sendText(session, pane.id, message);
  } else {
    const dispatch3 = writeDispatchFile(dir, pane.id, message);
    if (dispatch3) {
      sendCommand(session, pane.id, dispatch3.triggerCmd);
      sentViaFile = true;
    } else {
      sendCommand(session, pane.id, message);
    }
  }
  appendEvent(dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "send",
    target: pane.name ?? pane.title,
    paneId: pane.id,
    message: message.length > 100 ? message.slice(0, 100) + "..." : message
  });
  const result = {
    ok: true,
    session,
    target: {
      paneId: pane.id,
      name: pane.name,
      title: pane.title,
      role: pane.role
    },
    message,
    busyStatus,
    sentViaFile,
    ...busyStatus === "agent" ? { warning: "agent_busy" } : {}
  };
  if (json2) {
    console.log(JSON.stringify(result, null, 2));
    return;
  }
  const label = pane.name ?? pane.title;
  const preview = message.length > 60 ? message.slice(0, 60) + "..." : message;
  console.log(`Sent to "${label}" (${pane.id}): ${preview}`);
  if (busyStatus === "agent") {
    console.log("Warning: agent appears busy. Message sent anyway.");
  }
}
var LONG_MESSAGE_THRESHOLD;
var init_send = __esm({
  "packages/daemon/src/send.ts"() {
    "use strict";
    init_yaml_io();
    init_src();
    init_pane_comms();
    init_event_log();
    init_errors2();
    LONG_MESSAGE_THRESHOLD = 150;
  }
});

// packages/daemon/src/lib/task-actions.ts
function parseProof(raw, existing) {
  if (raw.startsWith("{")) {
    try {
      const parsed = JSON.parse(raw);
      return mergeProof(parsed, existing);
    } catch {
    }
  }
  return { ...existing, notes: raw };
}
function mergeProof(raw, existing) {
  const proof = { ...existing };
  if (raw.tests && typeof raw.tests === "object") {
    const t = raw.tests;
    if (typeof t.passed === "number" && typeof t.total === "number") {
      proof.tests = { passed: t.passed, total: t.total };
    }
  }
  if (raw.pr && typeof raw.pr === "object") {
    const p = raw.pr;
    if (typeof p.number === "number") {
      proof.pr = { number: p.number };
      if (typeof p.url === "string") proof.pr.url = p.url;
      if (typeof p.status === "string") proof.pr.status = p.status;
    }
  }
  if (raw.ci && typeof raw.ci === "object") {
    const c = raw.ci;
    if (typeof c.status === "string") {
      proof.ci = { status: c.status };
      if (typeof c.url === "string") proof.ci.url = c.url;
    }
  }
  if (typeof raw.notes === "string") proof.notes = raw.notes;
  return proof;
}
function normalizeProof(input, existing) {
  return typeof input === "string" ? parseProof(input, existing) : mergeProof(input, existing);
}
function cleanList(values2) {
  if (values2 === void 0) return void 0;
  const raw = Array.isArray(values2) ? values2 : values2.split(",");
  return raw.map((value) => value.trim()).filter(Boolean);
}
function validTaskStatus(status4) {
  return status4 === "todo" || status4 === "in-progress" || status4 === "review" || status4 === "done";
}
function validGoalStatus(status4) {
  return status4 === "todo" || status4 === "in-progress" || status4 === "done";
}
function validMilestoneStatus(status4) {
  return status4 === "locked" || status4 === "active" || status4 === "validating" || status4 === "done";
}
function createTaskRecord(dir, input) {
  const title = input.title.trim();
  if (!title) {
    throw new TaskActionError({ code: "validation_failed", message: "Task title is required" });
  }
  ensureTasksDir(dir);
  const id = nextTaskId(dir);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const depends = cleanList(input.depends) ?? [];
  const task = {
    id,
    title,
    description: input.description ?? "",
    goal: input.goalId ?? null,
    status: "todo",
    assignee: input.assign ?? null,
    priority: input.priority ?? 2,
    created: now,
    updated: now,
    tags: cleanList(input.tags) ?? [],
    proof: null,
    depends_on: depends,
    retryCount: 0,
    maxRetries: 5,
    lastError: null,
    nextRetryAt: null,
    milestone: input.milestone ?? null,
    specialty: input.specialty ?? null,
    fulfills: cleanList(input.fulfills) ?? [],
    discoveredIssues: [],
    salientSummary: null
  };
  if (depends.length > 0) {
    const cycle = detectCycle(dir, task.id, depends);
    if (cycle) {
      throw new TaskActionError({
        code: "validation_failed",
        message: `Dependency cycle detected: ${cycle.join(" -> ")}`,
        details: { cycle }
      });
    }
  }
  saveTask(dir, task);
  return { taskId: id, task };
}
function updateTaskRecord(dir, input) {
  const task = loadTask(dir, input.taskId);
  if (!task) {
    throw new TaskActionError({
      code: "task_not_found",
      message: `Task ${input.taskId} not found`,
      details: { taskId: input.taskId }
    });
  }
  if (input.status !== void 0) {
    if (!validTaskStatus(input.status)) {
      throw new TaskActionError({
        code: "validation_failed",
        message: `Invalid task status: ${input.status}`,
        details: { status: input.status }
      });
    }
    task.status = input.status;
  }
  if (input.title !== void 0) task.title = input.title;
  if (input.description !== void 0) task.description = input.description;
  if (input.priority !== void 0) task.priority = input.priority;
  if (input.assign !== void 0) task.assignee = input.assign || null;
  if (input.assignee !== void 0) task.assignee = input.assignee || null;
  if (input.goalId !== void 0) task.goal = input.goalId;
  if (input.tags !== void 0) task.tags = cleanList(input.tags) ?? [];
  if (input.depends !== void 0) {
    const newDeps = cleanList(input.depends) ?? [];
    const cycle = detectCycle(dir, task.id, newDeps);
    if (cycle) {
      throw new TaskActionError({
        code: "validation_failed",
        message: `Dependency cycle detected: ${cycle.join(" -> ")}`,
        details: { cycle }
      });
    }
    task.depends_on = newDeps;
  }
  if (input.proof !== void 0) task.proof = normalizeProof(input.proof, task.proof);
  if (input.milestone !== void 0) task.milestone = input.milestone;
  if (input.specialty !== void 0) task.specialty = input.specialty;
  if (input.fulfills !== void 0) task.fulfills = cleanList(input.fulfills) ?? [];
  if (input.summary !== void 0) task.salientSummary = input.summary;
  task.updated = (/* @__PURE__ */ new Date()).toISOString();
  saveTask(dir, task);
  return { task };
}
function claimTaskRecord(dir, taskId, assign) {
  const task = loadTask(dir, taskId);
  if (!task) {
    throw new TaskActionError({
      code: "task_not_found",
      message: `Task ${taskId} not found`,
      details: { taskId }
    });
  }
  if (task.assignee && task.assignee !== assign) {
    throw new TaskActionError({
      code: "task_already_assigned",
      message: `Task ${taskId} is already assigned to ${task.assignee}`,
      details: { taskId, assignee: task.assignee }
    });
  }
  const allTasks = loadTasks(dir);
  const unmet = task.depends_on.filter((depId) => {
    const dep = allTasks.find((candidate) => candidate.id === depId);
    return !dep || dep.status !== "done";
  });
  if (unmet.length > 0) {
    throw new TaskActionError({
      code: "task_dependency_unmet",
      message: `Task ${taskId} has unmet dependencies: ${unmet.join(", ")}`,
      details: { taskId, unmet }
    });
  }
  task.assignee = assign;
  task.status = "in-progress";
  task.updated = (/* @__PURE__ */ new Date()).toISOString();
  saveTask(dir, task);
  return { task };
}
function doneTaskRecord(dir, taskId, proof) {
  const task = loadTask(dir, taskId);
  if (!task) {
    throw new TaskActionError({
      code: "task_not_found",
      message: `Task ${taskId} not found`,
      details: { taskId }
    });
  }
  task.status = "done";
  if (proof !== void 0) task.proof = normalizeProof(proof, task.proof);
  task.updated = (/* @__PURE__ */ new Date()).toISOString();
  saveTask(dir, task);
  return { task };
}
function deleteTaskRecord(dir, taskId) {
  if (!deleteTask(dir, taskId)) {
    throw new TaskActionError({
      code: "task_not_found",
      message: `Task ${taskId} not found`,
      details: { taskId }
    });
  }
  return { deleted: true };
}
function createGoalRecord(dir, input) {
  const title = input.title.trim();
  if (!title) {
    throw new TaskActionError({ code: "validation_failed", message: "Goal title is required" });
  }
  ensureTasksDir(dir);
  const id = nextGoalId(dir);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const goal = {
    id,
    title,
    description: input.description ?? "",
    status: "todo",
    acceptance: input.acceptance ?? "",
    priority: input.priority ?? 2,
    created: now,
    updated: now,
    assignee: null,
    specialty: input.specialty ?? null,
    milestone: input.milestone ?? null
  };
  saveGoal(dir, goal);
  return { goalId: id, goal };
}
function updateGoalRecord(dir, input) {
  const goal = loadGoal(dir, input.goalId);
  if (!goal) {
    throw new TaskActionError({
      code: "goal_not_found",
      message: `Goal ${input.goalId} not found`,
      details: { goalId: input.goalId }
    });
  }
  if (input.status !== void 0) {
    if (!validGoalStatus(input.status)) {
      throw new TaskActionError({
        code: "validation_failed",
        message: `Invalid goal status: ${input.status}`,
        details: { status: input.status }
      });
    }
    goal.status = input.status;
  }
  if (input.title !== void 0) goal.title = input.title;
  if (input.description !== void 0) goal.description = input.description;
  if (input.acceptance !== void 0) goal.acceptance = input.acceptance;
  if (input.priority !== void 0) goal.priority = input.priority;
  if (input.milestone !== void 0) goal.milestone = input.milestone;
  if (input.specialty !== void 0) goal.specialty = input.specialty;
  if (input.assign !== void 0) goal.assignee = input.assign || null;
  goal.updated = (/* @__PURE__ */ new Date()).toISOString();
  saveGoal(dir, goal);
  return { goal };
}
function doneGoalRecord(dir, goalId) {
  return updateGoalRecord(dir, { goalId, status: "done" });
}
function deleteGoalRecord(dir, goalId) {
  if (!deleteGoal(dir, goalId)) {
    throw new TaskActionError({
      code: "goal_not_found",
      message: `Goal ${goalId} not found`,
      details: { goalId }
    });
  }
  return { deleted: true };
}
function createMilestoneRecord(dir, input) {
  const title = input.title.trim();
  if (!title) {
    throw new TaskActionError({
      code: "validation_failed",
      message: "Milestone title is required"
    });
  }
  const mission = loadMission(dir);
  if (!mission) {
    throw new TaskActionError({ code: "mission_not_set", message: "No mission set" });
  }
  const sequence = input.sequence ?? mission.milestones.length + 1;
  const id = `M${sequence}`;
  if (mission.milestones.some((milestone2) => milestone2.id === id)) {
    throw new TaskActionError({
      code: "validation_failed",
      message: `Milestone ${id} already exists`,
      details: { milestoneId: id }
    });
  }
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const hasActive = mission.milestones.some(
    (milestone2) => milestone2.status === "active" || milestone2.status === "done"
  );
  const milestone = {
    id,
    title,
    description: input.description ?? "",
    status: hasActive ? "locked" : "active",
    order: sequence,
    created: now,
    updated: now
  };
  mission.milestones.push(milestone);
  mission.milestones.sort((a, b) => a.order - b.order);
  mission.updated = now;
  saveMission(dir, mission);
  return { milestoneId: id, milestone };
}
function updateMilestoneRecord(dir, input) {
  const mission = loadMission(dir);
  if (!mission) {
    throw new TaskActionError({ code: "mission_not_set", message: "No mission set" });
  }
  const milestone = mission.milestones.find((candidate) => candidate.id === input.milestoneId);
  if (!milestone) {
    throw new TaskActionError({
      code: "milestone_not_found",
      message: `Milestone ${input.milestoneId} not found`,
      details: { milestoneId: input.milestoneId }
    });
  }
  if (input.status !== void 0) {
    if (!validMilestoneStatus(input.status)) {
      throw new TaskActionError({
        code: "validation_failed",
        message: `Invalid milestone status: ${input.status}`,
        details: { status: input.status }
      });
    }
    milestone.status = input.status;
  }
  if (input.title !== void 0) milestone.title = input.title;
  if (input.description !== void 0) milestone.description = input.description;
  milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
  mission.updated = milestone.updated;
  saveMission(dir, mission);
  return { milestone };
}
function setMissionRecord(dir, input) {
  const title = input.title.trim();
  if (!title) {
    throw new TaskActionError({ code: "validation_failed", message: "Mission title is required" });
  }
  ensureTasksDir(dir);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const mission = {
    title,
    description: input.description ?? "",
    status: "active",
    branch: null,
    milestones: [],
    created: now,
    updated: now
  };
  saveMission(dir, mission);
  return { mission };
}
function completeMissionPlanRecord(dir) {
  const mission = loadMission(dir);
  if (!mission) {
    throw new TaskActionError({ code: "mission_not_set", message: "No mission set" });
  }
  if (mission.status !== "planning") {
    throw new TaskActionError({
      code: "validation_failed",
      message: `Mission is "${mission.status}", expected "planning"`,
      details: { status: mission.status }
    });
  }
  mission.status = "active";
  const sorted = [...mission.milestones].sort((a, b) => a.order - b.order);
  const first = sorted[0] ?? null;
  const now = (/* @__PURE__ */ new Date()).toISOString();
  for (const milestone of sorted) {
    milestone.status = first && milestone.id === first.id ? "active" : "locked";
    milestone.updated = now;
  }
  mission.updated = now;
  saveMission(dir, mission);
  const { unclaimed } = checkCoverage(dir);
  return { mission, coverageGaps: unclaimed };
}
function clearMissionRecord(dir) {
  clearMission(dir);
  return { cleared: true };
}
function areGoalTasksDone(dir, goalId) {
  const tasks = loadTasksForGoal(dir, goalId);
  return tasks.length > 0 && tasks.every((task) => task.status === "done");
}
var TaskActionError;
var init_task_actions = __esm({
  "packages/daemon/src/lib/task-actions.ts"() {
    "use strict";
    init_task_store();
    init_validation();
    TaskActionError = class extends Error {
      code;
      details;
      constructor(args) {
        super(args.message);
        this.name = "TaskActionError";
        this.code = args.code;
        this.details = args.details;
      }
    };
  }
});

// packages/daemon/src/lib/log.ts
function getLogBuffer() {
  return logBuffer.slice();
}
function subscribeLogs(handler) {
  subscribers.add(handler);
  return () => {
    subscribers.delete(handler);
  };
}
function writeStructuredLog(level, component, message, data) {
  if (LEVEL_RANK[level] < LEVEL_RANK[minLevel]) return;
  const entry = {
    ts: (/* @__PURE__ */ new Date()).toISOString(),
    level,
    component,
    msg: message,
    ...data ? { data } : {}
  };
  logBuffer.push(entry);
  if (logBuffer.length > LOG_BUFFER_SIZE) logBuffer.shift();
  for (const sub of subscribers) {
    try {
      sub(entry);
    } catch (err) {
      process.stderr.write(
        `[log.ts] subscriber threw: ${err instanceof Error ? err.message : String(err)}
`
      );
    }
  }
  const wire = {
    ts: entry.ts,
    level: entry.level,
    component: entry.component,
    msg: entry.msg
  };
  if (data) Object.assign(wire, data);
  const out = level === "error" ? process.stderr : process.stdout;
  out.write(JSON.stringify(wire) + "\n");
}
var LEVEL_RANK, minLevel, LOG_BUFFER_SIZE, logBuffer, subscribers, logger, log;
var init_log = __esm({
  "packages/daemon/src/lib/log.ts"() {
    "use strict";
    LEVEL_RANK = { debug: 0, info: 1, warn: 2, error: 3 };
    minLevel = process.env.LOG_LEVEL ?? "info";
    LOG_BUFFER_SIZE = 1e3;
    logBuffer = [];
    subscribers = /* @__PURE__ */ new Set();
    logger = {
      debug: (component, msg, data) => writeStructuredLog("debug", component, msg, data),
      info: (component, msg, data) => writeStructuredLog("info", component, msg, data),
      warn: (component, msg, data) => writeStructuredLog("warn", component, msg, data),
      error: (component, msg, data) => writeStructuredLog("error", component, msg, data)
    };
    log = {
      /** Alias for `info` / console.log (VibeTunnel `createLogger` compatibility). */
      log: (...args) => {
        console.log(...args);
      },
      info: (...args) => {
        console.log(...args);
      },
      warn: (...args) => {
        console.warn(...args);
      },
      error: (...args) => {
        console.error(...args);
      },
      debug: (...args) => {
        console.debug(...args);
      }
    };
  }
});

// packages/daemon/src/lib/authorship.ts
function generateMarkId() {
  return `m${Date.now()}_${++markIdCounter}`;
}
function normalizeQuote(text) {
  return text.replace(/\s+/g, " ").trim();
}
function extractMarks(markdown) {
  const startIdx = markdown.lastIndexOf(MARKS_START);
  if (startIdx === -1) {
    return { content: markdown, marks: null };
  }
  const endIdx = markdown.indexOf(MARKS_END, startIdx + MARKS_START.length);
  if (endIdx === -1) {
    return { content: markdown, marks: null };
  }
  const jsonStr = markdown.slice(startIdx + MARKS_START.length, endIdx).trim();
  const content = (markdown.slice(0, startIdx) + markdown.slice(endIdx + MARKS_END.length)).trimEnd();
  try {
    const data = JSON.parse(jsonStr);
    return { content, marks: data };
  } catch {
    return { content, marks: null };
  }
}
function embedMarks(markdown, doc) {
  const { content } = extractMarks(markdown);
  const json2 = JSON.stringify(doc, null, 2);
  return `${content}

${MARKS_START}
${json2}
${MARKS_END}
`;
}
function createAuthored(by, range, quote) {
  return {
    id: generateMarkId(),
    kind: "authored",
    by,
    at: (/* @__PURE__ */ new Date()).toISOString(),
    range,
    quote: normalizeQuote(quote)
  };
}
function calculateStats(marks) {
  let aiChars = 0;
  let humanChars = 0;
  for (const mark of Object.values(marks)) {
    if (mark.kind !== "authored") continue;
    if (mark.orphaned) continue;
    const chars = mark.range.to - mark.range.from;
    if (mark.by.startsWith("ai:") || mark.by === "ai") {
      aiChars += chars;
    } else {
      humanChars += chars;
    }
  }
  const totalChars = aiChars + humanChars;
  if (totalChars === 0) {
    return { aiPercent: 0, humanPercent: 0, totalChars: 0 };
  }
  return {
    aiPercent: Math.round(aiChars / totalChars * 100),
    humanPercent: Math.round(humanChars / totalChars * 100),
    totalChars
  };
}
function findQuotePosition(content, quote) {
  if (!quote) return null;
  const normalized = normalizeQuote(quote);
  const normalizedContent = normalizeQuote(content);
  const idx = normalizedContent.indexOf(normalized);
  if (idx === -1) return null;
  let origFrom = -1;
  let normalizedPos = 0;
  for (let i = 0; i < content.length; i++) {
    if (/\s/.test(content[i]) && (i === 0 || /\s/.test(content[i - 1]))) continue;
    if (normalizedPos === idx && origFrom === -1) origFrom = i;
    if (normalizedPos === idx + normalized.length) {
      return { from: origFrom, to: i };
    }
    normalizedPos++;
  }
  const simpleIdx = content.indexOf(quote);
  if (simpleIdx !== -1) {
    return { from: simpleIdx, to: simpleIdx + quote.length };
  }
  return null;
}
function reanchorMarks(content, marks) {
  const result = {};
  for (const [id, mark] of Object.entries(marks)) {
    const currentText = content.slice(mark.range.from, mark.range.to);
    if (normalizeQuote(currentText) === normalizeQuote(mark.quote)) {
      result[id] = mark;
      continue;
    }
    const newRange = findQuotePosition(content, mark.quote);
    if (newRange) {
      result[id] = { ...mark, range: newRange, orphaned: false };
    } else {
      result[id] = { ...mark, orphaned: true };
    }
  }
  return result;
}
function tagContent(markdown, by) {
  const { content, marks: existingDoc } = extractMarks(markdown);
  const existingMarks = existingDoc?.marks ?? {};
  const anchored = Object.keys(existingMarks).length > 0 ? reanchorMarks(content, existingMarks) : {};
  const covered = /* @__PURE__ */ new Set();
  for (const mark of Object.values(anchored)) {
    if (mark.kind !== "authored" || mark.orphaned) continue;
    for (let i = mark.range.from; i < mark.range.to && i < content.length; i++) {
      covered.add(i);
    }
  }
  const newMarks = { ...anchored };
  let rangeStart = -1;
  for (let i = 0; i <= content.length; i++) {
    const isCovered = covered.has(i);
    const atEnd = i === content.length;
    if (!isCovered && !atEnd && rangeStart === -1) {
      rangeStart = i;
    } else if ((isCovered || atEnd) && rangeStart !== -1) {
      const quote = content.slice(rangeStart, i);
      if (quote.trim().length > 0) {
        const mark = createAuthored(by, { from: rangeStart, to: i }, quote);
        newMarks[mark.id] = mark;
      }
      rangeStart = -1;
    }
  }
  const doc = { version: 2, marks: newMarks };
  return embedMarks(content, doc);
}
var MARKS_START, MARKS_END, markIdCounter;
var init_authorship = __esm({
  "packages/daemon/src/lib/authorship.ts"() {
    "use strict";
    MARKS_START = "<!-- TMUX-IDE:MARKS";
    MARKS_END = "-->";
    markIdCounter = 0;
  }
});

// packages/daemon/src/lib/plan-store.ts
import { readdirSync as readdirSync6, readFileSync as readFileSync17, writeFileSync as writeFileSync17, existsSync as existsSync22 } from "node:fs";
import { join as join23 } from "node:path";
function parseInlineStatus(content) {
  const match = content.match(/\*\*Status:\*\*\s*`([^`]+)`/);
  if (!match) return "pending";
  const raw = match[1].toLowerCase().trim();
  if (raw === "done" || raw === "completed") return "done";
  if (raw === "in-progress" || raw === "in progress" || raw === "active") return "in-progress";
  if (raw === "archived") return "archived";
  return "pending";
}
function parseInlineField(content, field) {
  const re = new RegExp(`\\*\\*${field}:\\*\\*\\s*(.+?)\\s*$`, "m");
  const match = content.match(re);
  return match ? match[1].replace(/^`|`$/g, "").trim() : void 0;
}
function parseTitle(content) {
  const match = content.match(/^#\s+(.+)/m);
  return match ? match[1].trim() : "(untitled)";
}
function loadPlans(dir) {
  const plansDir = join23(dir, "plans");
  if (!existsSync22(plansDir)) return [];
  return readdirSync6(plansDir).filter((f) => f.endsWith(".md") && f !== "ROADMAP.md").sort().map((f) => {
    const content = readFileSync17(join23(plansDir, f), "utf-8");
    return {
      name: f.replace(/\.md$/, ""),
      path: `plans/${f}`,
      title: parseTitle(content),
      status: parseInlineStatus(content),
      effort: parseInlineField(content, "Effort"),
      gate: parseInlineField(content, "Gate"),
      completed: parseInlineField(content, "Completed")
    };
  });
}
function listPlans(dir, filter) {
  const plans = loadPlans(dir);
  if (filter?.status) {
    return plans.filter((p) => p.status === filter.status);
  }
  return plans;
}
function markPlanDone(dir, nameOrNumber) {
  const plansDir = join23(dir, "plans");
  if (!existsSync22(plansDir)) return null;
  const files = readdirSync6(plansDir).filter((f) => f.endsWith(".md"));
  const target = files.find(
    (f) => f === `${nameOrNumber}.md` || f.replace(/\.md$/, "") === nameOrNumber || f.startsWith(`${nameOrNumber}-`)
  );
  if (!target) return null;
  const filePath = join23(plansDir, target);
  let content = readFileSync17(filePath, "utf-8");
  const today = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
  if (content.match(/\*\*Status:\*\*\s*`[^`]+`/)) {
    content = content.replace(/\*\*Status:\*\*\s*`[^`]+`/, "**Status:** `done`");
  }
  if (content.match(/\*\*Completed:\*\*/)) {
    content = content.replace(/\*\*Completed:\*\*\s*.+/, `**Completed:** ${today}`);
  } else {
    content = content.replace(/(\*\*Status:\*\*\s*`done`)/, `$1
**Completed:** ${today}`);
  }
  writeFileSync17(filePath, content);
  return {
    name: target.replace(/\.md$/, ""),
    path: `plans/${target}`,
    title: parseTitle(content),
    status: "done",
    effort: parseInlineField(content, "Effort"),
    gate: parseInlineField(content, "Gate"),
    completed: today
  };
}
function getPlan(dir, nameOrNumber) {
  const plans = loadPlans(dir);
  return plans.find((p) => p.name === nameOrNumber || p.name.startsWith(`${nameOrNumber}-`)) ?? null;
}
var init_plan_store2 = __esm({
  "packages/daemon/src/lib/plan-store.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/workflow-store.ts
import { join as join24 } from "node:path";
import {
  readdirSync as readdirSync7,
  readFileSync as readFileSync18,
  writeFileSync as writeFileSync18,
  mkdirSync as mkdirSync19,
  existsSync as existsSync23,
  unlinkSync as unlinkSync3,
  renameSync as renameSync13
} from "node:fs";
function atomicWriteJSON(filePath, data) {
  const tmpPath = filePath + ".tmp";
  writeFileSync18(tmpPath, JSON.stringify(data, null, 2) + "\n");
  renameSync13(tmpPath, filePath);
}
function checkpointsDir(dir) {
  return join24(getTasksRoot(dir), "checkpoints");
}
function ensureCheckpointsDir(dir) {
  const d = checkpointsDir(dir);
  if (!existsSync23(d)) mkdirSync19(d, { recursive: true });
}
function nextCheckpointId(dir) {
  const d = checkpointsDir(dir);
  if (!existsSync23(d)) return "001";
  const files = readdirSync7(d).filter((f) => f.endsWith(".json"));
  if (files.length === 0) return "001";
  const maxId = Math.max(...files.map((f) => parseInt(f.split("-")[0], 10) || 0));
  return String(maxId + 1).padStart(3, "0");
}
function slugify2(text) {
  return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, 50);
}
function loadCheckpoints(dir) {
  const d = checkpointsDir(dir);
  if (!existsSync23(d)) return [];
  return readdirSync7(d).filter((f) => f.endsWith(".json")).sort().map((f) => {
    try {
      return JSON.parse(readFileSync18(join24(d, f), "utf-8"));
    } catch {
      return null;
    }
  }).filter((c) => c !== null);
}
function loadCheckpoint(dir, id) {
  const d = checkpointsDir(dir);
  if (!existsSync23(d)) return null;
  const file = readdirSync7(d).find((f) => f.startsWith(id + "-") || f === id + ".json");
  if (!file) return null;
  try {
    return JSON.parse(readFileSync18(join24(d, file), "utf-8"));
  } catch {
    return null;
  }
}
function loadCheckpointsForTask(dir, taskId) {
  return loadCheckpoints(dir).filter((c) => c.taskId === taskId);
}
function saveCheckpoint(dir, checkpoint) {
  ensureCheckpointsDir(dir);
  const d = checkpointsDir(dir);
  const filename = `${checkpoint.id}-${slugify2(checkpoint.title)}.json`;
  const newPath = join24(d, filename);
  const existing = readdirSync7(d).find(
    (f) => f.startsWith(checkpoint.id + "-") || f === checkpoint.id + ".json"
  );
  atomicWriteJSON(newPath, { _version: SCHEMA_VERSION2, ...checkpoint });
  if (existing && join24(d, existing) !== newPath) unlinkSync3(join24(d, existing));
}
function deleteCheckpoint(dir, id) {
  const d = checkpointsDir(dir);
  if (!existsSync23(d)) return false;
  const file = readdirSync7(d).find((f) => f.startsWith(id + "-") || f === id + ".json");
  if (!file) return false;
  unlinkSync3(join24(d, file));
  return true;
}
function reviewsDir(dir) {
  return join24(getTasksRoot(dir), "reviews");
}
function ensureReviewsDir(dir) {
  const d = reviewsDir(dir);
  if (!existsSync23(d)) mkdirSync19(d, { recursive: true });
}
function nextReviewId(dir) {
  const d = reviewsDir(dir);
  if (!existsSync23(d)) return "001";
  const files = readdirSync7(d).filter((f) => f.endsWith(".json"));
  if (files.length === 0) return "001";
  const maxId = Math.max(...files.map((f) => parseInt(f.split("-")[0], 10) || 0));
  return String(maxId + 1).padStart(3, "0");
}
function loadReviews(dir) {
  const d = reviewsDir(dir);
  if (!existsSync23(d)) return [];
  return readdirSync7(d).filter((f) => f.endsWith(".json")).sort().map((f) => {
    try {
      return JSON.parse(readFileSync18(join24(d, f), "utf-8"));
    } catch {
      return null;
    }
  }).filter((r) => r !== null);
}
function loadReview(dir, id) {
  const d = reviewsDir(dir);
  if (!existsSync23(d)) return null;
  const file = readdirSync7(d).find((f) => f.startsWith(id + "-") || f === id + ".json");
  if (!file) return null;
  try {
    return JSON.parse(readFileSync18(join24(d, file), "utf-8"));
  } catch {
    return null;
  }
}
function loadReviewsForTask(dir, taskId) {
  return loadReviews(dir).filter((r) => r.taskId === taskId);
}
function saveReview(dir, review) {
  ensureReviewsDir(dir);
  const d = reviewsDir(dir);
  const filename = `${review.id}-${slugify2(review.title)}.json`;
  const newPath = join24(d, filename);
  const existing = readdirSync7(d).find(
    (f) => f.startsWith(review.id + "-") || f === review.id + ".json"
  );
  atomicWriteJSON(newPath, { _version: SCHEMA_VERSION2, ...review });
  if (existing && join24(d, existing) !== newPath) unlinkSync3(join24(d, existing));
}
function deleteReview(dir, id) {
  const d = reviewsDir(dir);
  if (!existsSync23(d)) return false;
  const file = readdirSync7(d).find((f) => f.startsWith(id + "-") || f === id + ".json");
  if (!file) return false;
  unlinkSync3(join24(d, file));
  return true;
}
var SCHEMA_VERSION2;
var init_workflow_store = __esm({
  "packages/daemon/src/lib/workflow-store.ts"() {
    "use strict";
    init_task_store();
    SCHEMA_VERSION2 = 1;
  }
});

// packages/daemon/src/lib/agent-discovery.ts
function listAllTmuxPanes() {
  const output = tmux("list-panes", "-a", "-F", FIELDS.join("	"));
  if (!output) return [];
  const panes = [];
  for (const line of output.split("\n")) {
    if (!line) continue;
    const parts = line.split("	");
    if (parts.length < FIXED_FIELDS) continue;
    const [session, id, index, cmd, cwd, pid, width, height, active2, role, name, type] = parts;
    if (!session || !id) continue;
    panes.push({
      session,
      id,
      index: parseInt(index, 10) || 0,
      title: parts.slice(FIXED_FIELDS).join("	"),
      currentCommand: cmd ?? "",
      cwd: cwd || null,
      pid: pid ? parseInt(pid, 10) || null : null,
      width: parseInt(width, 10) || 0,
      height: parseInt(height, 10) || 0,
      active: active2 === "1",
      role: role || null,
      name: name || null,
      type: type || null
    });
  }
  return panes;
}
function inferAgentTool(currentCommand) {
  const cmd = currentCommand.toLowerCase();
  if (cmd.startsWith("codex")) return "codex";
  if (cmd.startsWith("claude")) return "claude";
  if (/^\d+\.\d+/.test(cmd)) return "claude";
  return "unknown";
}
function discoverTmuxAgents(managedSessions) {
  return listAllTmuxPanes().filter((pane) => isAgentPane(pane)).map((pane) => {
    const managed = managedSessions.has(pane.session);
    return {
      id: `${pane.session}:${pane.id}`,
      kind: managed ? "managed" : "tmux-unmanaged",
      tool: inferAgentTool(pane.currentCommand),
      name: agentIdentifier(pane),
      status: isAgentBusy(pane) ? "busy" : "idle",
      session: pane.session,
      paneId: pane.id,
      paneTitle: pane.title,
      cwd: pane.cwd,
      taskId: null,
      taskTitle: null,
      pid: pane.pid,
      lastActivity: (/* @__PURE__ */ new Date()).toISOString(),
      machineId: null,
      machineName: null
    };
  });
}
var FIELDS, FIXED_FIELDS;
var init_agent_discovery = __esm({
  "packages/daemon/src/lib/agent-discovery.ts"() {
    "use strict";
    init_orchestrator();
    init_tmux_exec();
    FIELDS = [
      "#{session_name}",
      "#{pane_id}",
      "#{pane_index}",
      "#{pane_current_command}",
      "#{pane_current_path}",
      "#{pane_pid}",
      "#{pane_width}",
      "#{pane_height}",
      "#{pane_active}",
      "#{@ide_role}",
      "#{@ide_name}",
      "#{@ide_type}",
      "#{pane_title}"
    ];
    FIXED_FIELDS = FIELDS.length - 1;
  }
});

// packages/daemon/src/ssh-remote.ts
var ssh_remote_exports = {};
__export(ssh_remote_exports, {
  buildRemoteServeScript: () => buildRemoteServeScript,
  buildSshForwardArgs: () => buildSshForwardArgs,
  parseSshConfigHosts: () => parseSshConfigHosts,
  readSshRemotes: () => readSshRemotes,
  remoteServeCommand: () => remoteServeCommand,
  remotesFilePath: () => remotesFilePath,
  shellQuote: () => shellQuote,
  sshRemoteCommand: () => sshRemoteCommand,
  validateRemoteName: () => validateRemoteName,
  validateRemotePath: () => validateRemotePath,
  validateSshAlias: () => validateSshAlias
});
import { spawn as spawn6, execFile as execFile3 } from "node:child_process";
import { mkdir as mkdir2, readFile as readFile2, writeFile as writeFile2, chmod } from "node:fs/promises";
import { existsSync as existsSync24 } from "node:fs";
import { homedir as homedir8 } from "node:os";
import { dirname as dirname10, join as join25, resolve as resolve17 } from "node:path";
import { promisify } from "node:util";
import { createServer } from "node:net";
function remotesFilePath() {
  return process.env.TMUX_IDE_SSH_REMOTES_FILE ?? join25(homedir8(), ".tmux-ide", "ssh-remotes.json");
}
async function readSshRemotes() {
  return (await readStore()).remotes;
}
function sshConfigPath() {
  return process.env.TMUX_IDE_SSH_CONFIG ?? join25(homedir8(), ".ssh", "config");
}
function emptyStore() {
  return { version: 1, remotes: [] };
}
async function readStore() {
  const file = remotesFilePath();
  if (!existsSync24(file)) return emptyStore();
  try {
    const parsed = JSON.parse(await readFile2(file, "utf-8"));
    if (parsed.version !== 1 || !Array.isArray(parsed.remotes)) return emptyStore();
    return {
      version: 1,
      remotes: parsed.remotes.filter(isSshRemote)
    };
  } catch {
    return emptyStore();
  }
}
async function writeStore(store) {
  const file = remotesFilePath();
  await mkdir2(dirname10(file), { recursive: true, mode: 448 });
  const tmp = `${file}.${process.pid}.${Date.now()}.tmp`;
  await writeFile2(tmp, JSON.stringify(store, null, 2) + "\n", { encoding: "utf-8", mode: 384 });
  await chmod(tmp, 384);
  await import("node:fs/promises").then(({ rename: rename2 }) => rename2(tmp, file));
}
function isSshRemote(value) {
  if (!value || typeof value !== "object") return false;
  const remote = value;
  if (typeof remote.name !== "string") return false;
  if (typeof remote.host !== "string") return false;
  if (typeof remote.path !== "string") return false;
  if (typeof remote.addedAt !== "string") return false;
  if (remote.localPort !== void 0 && !isValidPort(remote.localPort)) return false;
  if (remote.remotePort !== void 0 && !isValidPort(remote.remotePort)) return false;
  if (remote.lastLocalPort !== void 0 && !isValidPort(remote.lastLocalPort)) return false;
  if (remote.lastLaunchedAt !== void 0 && typeof remote.lastLaunchedAt !== "string")
    return false;
  return true;
}
function isValidPort(port) {
  return Number.isInteger(port) && port >= 1 && port <= 65535;
}
function parsePort(value, label) {
  if (value === void 0 || value === false) return void 0;
  if (value === true || value.trim() === "") {
    throw new IdeError(`${label} requires a numeric value`, { code: "USAGE" });
  }
  const port = Number(value);
  if (!isValidPort(port)) {
    throw new IdeError(`${label} must be an integer from 1 to 65535`, { code: "USAGE" });
  }
  return port;
}
function parseSshConfigHosts(contents) {
  const hosts = /* @__PURE__ */ new Set();
  for (const rawLine of contents.split(/\r?\n/)) {
    const line = rawLine.trim();
    if (!line || line.startsWith("#")) continue;
    const match = line.match(/^Host\s+(.+)$/i);
    if (!match) continue;
    for (const host of match[1].split(/\s+/)) {
      if (!host || host.includes("*") || host.includes("?") || host.startsWith("!")) continue;
      hosts.add(host);
    }
  }
  return [...hosts].sort((a, b) => a.localeCompare(b));
}
function validateRemoteName(name) {
  if (!/^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$/.test(name)) {
    throw new IdeError(
      "Remote name must start with a letter or number and contain only letters, numbers, dot, underscore, or dash.",
      { code: "USAGE" }
    );
  }
}
function validateSshAlias(host) {
  if (!/^[A-Za-z0-9][A-Za-z0-9._:@+-]{0,127}$/.test(host)) {
    throw new IdeError(
      "SSH host must be a config alias or host string without whitespace or shell metacharacters.",
      { code: "USAGE" }
    );
  }
  if (host.startsWith("-")) {
    throw new IdeError("SSH host cannot start with '-'", { code: "USAGE" });
  }
}
function validateRemotePath(path4) {
  if (!path4 || /[\0\r\n]/.test(path4)) {
    throw new IdeError("Remote path must be a single non-empty path.", { code: "USAGE" });
  }
}
function shellQuote(value) {
  return `'${value.replace(/'/g, `'\\''`)}'`;
}
function buildRemoteServeScript(remotePath, remotePort) {
  validateRemotePath(remotePath);
  if (!isValidPort(remotePort)) {
    throw new IdeError("Remote port must be an integer from 1 to 65535", { code: "USAGE" });
  }
  const quotedPath = shellQuote(remotePath);
  return [
    `cd ${quotedPath}`,
    "mkdir -p .tmux-ide",
    'if ! command -v tmux-ide >/dev/null 2>&1; then echo "tmux-ide not found on remote PATH" >&2; exit 127; fi',
    `nohup tmux-ide __remote-serve --port ${remotePort} > .tmux-ide/remote-daemon.log 2>&1 < /dev/null &`
  ].join(" && ");
}
function buildSshForwardArgs(opts) {
  validateSshAlias(opts.host);
  if (!isValidPort(opts.localPort) || !isValidPort(opts.remotePort)) {
    throw new IdeError("SSH tunnel ports must be integers from 1 to 65535", { code: "USAGE" });
  }
  return [
    "-N",
    "-L",
    `${DEFAULT_LOCAL_HOST}:${opts.localPort}:127.0.0.1:${opts.remotePort}`,
    "-o",
    "ExitOnForwardFailure=yes",
    "-o",
    "ServerAliveInterval=15",
    // The tunnel must be a dedicated connection we own. Under ControlMaster
    // multiplexing (common with SSM ProxyCommand setups) the `-N` client can
    // exit immediately after handing off to the persistent master, taking its
    // forward down with it. Disable mux for this one connection.
    "-o",
    "ControlMaster=no",
    "-o",
    "ControlPath=none",
    opts.host
  ];
}
async function pickFreeLocalPort() {
  const server = createServer();
  return await new Promise((resolvePort2, reject) => {
    server.once("error", reject);
    server.listen(0, DEFAULT_LOCAL_HOST, () => {
      const address = server.address();
      const port = typeof address === "object" && address ? address.port : null;
      server.close(() => {
        if (port) resolvePort2(port);
        else reject(new IdeError("Could not allocate a local tunnel port", { code: "PORT_ERROR" }));
      });
    });
  });
}
async function assertLocalPortFree(port) {
  const server = createServer();
  await new Promise((resolvePort2, reject) => {
    server.once(
      "error",
      () => reject(new IdeError(`Local port ${port} is already in use`, { code: "PORT_IN_USE" }))
    );
    server.listen(port, DEFAULT_LOCAL_HOST, () => {
      server.close(() => resolvePort2());
    });
  });
}
function openInBrowser(url) {
  const cmd = process.platform === "darwin" ? "open" : process.platform === "win32" ? "start" : "xdg-open";
  try {
    const child = spawn6(cmd, [url], { detached: true, stdio: "ignore" });
    child.unref();
  } catch {
  }
}
async function waitForHealth(url, timeoutMs = 15e3) {
  const deadline = Date.now() + timeoutMs;
  let lastError = "";
  while (Date.now() < deadline) {
    try {
      const controller = new AbortController();
      setTimeout(() => controller.abort(), 750).unref?.();
      const res = await fetch(`${url}healthz`, { signal: controller.signal });
      if (res.ok) return;
      lastError = `HTTP ${res.status}`;
    } catch (err) {
      lastError = err instanceof Error ? err.message : String(err);
    }
    await new Promise((resolveTimer) => setTimeout(resolveTimer, 250));
  }
  throw new IdeError(
    `Remote dashboard did not become healthy through the SSH tunnel: ${lastError}`,
    {
      code: "REMOTE_UNHEALTHY"
    }
  );
}
async function runRemoteServe(remote, remotePort) {
  const script = buildRemoteServeScript(remote.path, remotePort);
  try {
    await execFileAsync("ssh", [remote.host, script], { timeout: 1e4 });
  } catch (err) {
    const stderr = err instanceof Error && "stderr" in err && typeof err.stderr === "string" ? err.stderr.trim() : "";
    throw new IdeError(`Failed to start tmux-ide on ${remote.host}${stderr ? `: ${stderr}` : ""}`, {
      code: "SSH_REMOTE_START_FAILED"
    });
  }
}
async function startTunnel(remote, localPort, remotePort) {
  const args = buildSshForwardArgs({ host: remote.host, localPort, remotePort });
  const child = spawn6("ssh", args, { detached: true, stdio: "ignore" });
  child.unref();
  await new Promise((resolveStart, reject) => {
    const timer = setTimeout(resolveStart, 500);
    child.once("exit", (code, signal) => {
      clearTimeout(timer);
      reject(
        new IdeError(
          `SSH tunnel exited before it was ready (${signal ?? `code ${code ?? "unknown"}`})`,
          { code: "SSH_TUNNEL_FAILED" }
        )
      );
    });
    child.once("error", (err) => {
      clearTimeout(timer);
      reject(
        new IdeError(`Failed to start ssh tunnel: ${err.message}`, { code: "SSH_TUNNEL_FAILED" })
      );
    });
  });
  return {
    pid: child.pid ?? 0,
    stop: () => {
      if (!child.pid) return;
      try {
        process.kill(-child.pid, "SIGTERM");
      } catch {
        try {
          child.kill("SIGTERM");
        } catch {
        }
      }
    }
  };
}
function printRemotes(remotes) {
  if (remotes.length === 0) {
    console.log("No SSH remotes configured");
    return;
  }
  for (const remote of remotes) {
    const local = remote.localPort ? ` local:${remote.localPort}` : "";
    const remotePort = remote.remotePort ? ` remote:${remote.remotePort}` : "";
    console.log(`  ${remote.name} \u2014 ${remote.host}:${remote.path}${local}${remotePort}`);
  }
}
async function listHosts(json2) {
  const file = sshConfigPath();
  const hosts = existsSync24(file) ? parseSshConfigHosts(await readFile2(file, "utf-8")) : [];
  if (json2) {
    console.log(JSON.stringify({ hosts }));
  } else if (hosts.length === 0) {
    console.log("No concrete SSH hosts found");
  } else {
    for (const host of hosts) console.log(host);
  }
}
async function addRemote(opts) {
  const name = opts.args?.[0];
  const host = opts.values?.host;
  const path4 = opts.values?.path;
  if (!name || typeof host !== "string" || typeof path4 !== "string") {
    throw new IdeError(
      "Usage: tmux-ide remote ssh add <name> --host <ssh-host> --path <remote-project-dir> [--local-port N] [--remote-port N]",
      { code: "USAGE" }
    );
  }
  validateRemoteName(name);
  validateSshAlias(host);
  validateRemotePath(path4);
  const localPort = parsePort(opts.values?.["local-port"], "--local-port");
  const remotePort = parsePort(opts.values?.["remote-port"], "--remote-port");
  const store = await readStore();
  const next = {
    name,
    host,
    path: path4,
    localPort,
    remotePort,
    addedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
  const index = store.remotes.findIndex((remote) => remote.name === name);
  if (index >= 0) store.remotes[index] = next;
  else store.remotes.push(next);
  await writeStore(store);
  if (opts.json) console.log(JSON.stringify({ ok: true, remote: next }));
  else console.log(`Saved SSH remote ${name} (${host}:${path4})`);
}
async function removeRemote(opts) {
  const name = opts.args?.[0];
  if (!name) {
    throw new IdeError("Usage: tmux-ide remote ssh remove <name>", { code: "USAGE" });
  }
  const store = await readStore();
  const before = store.remotes.length;
  store.remotes = store.remotes.filter((remote) => remote.name !== name);
  await writeStore(store);
  const removed = store.remotes.length !== before;
  if (opts.json) console.log(JSON.stringify({ ok: true, removed }));
  else
    console.log(removed ? `Removed SSH remote ${name}` : `SSH remote ${name} was not configured`);
}
async function getConfiguredRemote(name) {
  if (!name) {
    throw new IdeError("Usage: tmux-ide remote ssh launch <name>", { code: "USAGE" });
  }
  validateRemoteName(name);
  const store = await readStore();
  const remote = store.remotes.find((candidate) => candidate.name === name);
  if (!remote) {
    throw new IdeError(`SSH remote ${name} is not configured`, { code: "REMOTE_NOT_FOUND" });
  }
  return remote;
}
async function launchRemote(opts) {
  const remote = await getConfiguredRemote(opts.args?.[0]);
  const remotePort = remote.remotePort ?? DEFAULT_REMOTE_PORT;
  const localPort = remote.localPort ?? await pickFreeLocalPort();
  if (remote.localPort) await assertLocalPortFree(localPort);
  await runRemoteServe(remote, remotePort);
  const tunnel = await startTunnel(remote, localPort, remotePort);
  const url = `http://${DEFAULT_LOCAL_HOST}:${localPort}/`;
  try {
    await waitForHealth(url);
  } catch (err) {
    tunnel.stop();
    throw err;
  }
  const store = await readStore();
  const stored = store.remotes.find((candidate) => candidate.name === remote.name);
  if (stored) {
    stored.lastLocalPort = localPort;
    stored.lastLaunchedAt = (/* @__PURE__ */ new Date()).toISOString();
    await writeStore(store);
  }
  if (opts.json) {
    console.log(
      JSON.stringify({ ok: true, url, localPort, remotePort, tunnelPid: tunnel.pid, remote })
    );
  } else {
    console.log(`Dashboard: ${url}`);
    console.log(`Tunnel: ${remote.host} 127.0.0.1:${localPort} -> 127.0.0.1:${remotePort}`);
  }
  if (opts.values?.["no-open"] !== true) openInBrowser(url);
}
async function statusRemote(opts) {
  const remote = await getConfiguredRemote(opts.args?.[0]);
  const localPort = remote.localPort;
  const url = localPort ? `http://${DEFAULT_LOCAL_HOST}:${localPort}/` : null;
  let healthy = false;
  if (url) {
    try {
      const res = await fetch(`${url}healthz`);
      healthy = res.ok;
    } catch {
      healthy = false;
    }
  }
  if (opts.json) {
    console.log(JSON.stringify({ remote, url, healthy }));
  } else {
    console.log(`Remote: ${remote.name}`);
    console.log(`SSH host: ${remote.host}`);
    console.log(`Path: ${remote.path}`);
    console.log(`Remote daemon bind: 127.0.0.1:${remote.remotePort ?? DEFAULT_REMOTE_PORT}`);
    if (url) console.log(`Local URL: ${url} (${healthy ? "healthy" : "not reachable"})`);
    else console.log("Local URL: dynamic; run launch to allocate a tunnel port");
  }
}
async function sshRemoteCommand(opts) {
  const sub = opts.args?.[0];
  const args = opts.args?.slice(1) ?? [];
  switch (sub) {
    case "hosts":
      await listHosts(opts.json);
      break;
    case "list": {
      const store = await readStore();
      if (opts.json) console.log(JSON.stringify({ remotes: store.remotes }));
      else printRemotes(store.remotes);
      break;
    }
    case "add":
      await addRemote({ ...opts, args });
      break;
    case "remove":
      await removeRemote({ ...opts, args });
      break;
    case "launch":
    case "dashboard":
      await launchRemote({ ...opts, args });
      break;
    case "status":
      await statusRemote({ ...opts, args });
      break;
    default:
      throw new IdeError(
        "Usage: tmux-ide remote ssh hosts|list|add|remove|launch|dashboard|status",
        { code: "USAGE" }
      );
  }
}
async function remoteServeCommand(opts) {
  const port = parsePort(opts.port, "--port") ?? DEFAULT_REMOTE_PORT;
  const dir = resolve17(".");
  const { readConfig: readConfig2, getSessionName: getSessionName2 } = await Promise.resolve().then(() => (init_yaml_io(), yaml_io_exports));
  const { launch: launch2 } = await Promise.resolve().then(() => (init_launch(), launch_exports));
  const { startEmbeddedDaemon: startEmbeddedDaemon2 } = await Promise.resolve().then(() => (init_daemon_embed(), daemon_embed_exports));
  const { readCanonicalDaemonInfo: readCanonicalDaemonInfo2, isCanonicalDaemonAlive: isCanonicalDaemonAlive2 } = await Promise.resolve().then(() => (init_canonical_daemon(), canonical_daemon_exports));
  const existing = readCanonicalDaemonInfo2();
  if (existing && existing.port === port && existing.bindHostname === "127.0.0.1") {
    if (await isCanonicalDaemonAlive2(existing)) return;
  }
  const { config: config2 } = readConfig2(dir);
  const { name: fallbackName } = getSessionName2(dir);
  const sessionName = config2.name ?? fallbackName;
  await launch2(dir, { attach: false });
  const handle = await startEmbeddedDaemon2({
    sessionName,
    port,
    bindHostname: "127.0.0.1"
  });
  const stop2 = async () => {
    await handle.stop({ gracefulMs: 500 });
  };
  process.once("SIGINT", () => void stop2().finally(() => process.exit(0)));
  process.once("SIGTERM", () => void stop2().finally(() => process.exit(0)));
  await new Promise(() => void 0);
}
var execFileAsync, DEFAULT_REMOTE_PORT, DEFAULT_LOCAL_HOST;
var init_ssh_remote = __esm({
  "packages/daemon/src/ssh-remote.ts"() {
    "use strict";
    init_errors2();
    execFileAsync = promisify(execFile3);
    DEFAULT_REMOTE_PORT = 6060;
    DEFAULT_LOCAL_HOST = "127.0.0.1";
  }
});

// packages/daemon/src/lib/agent-remotes.ts
function sshMachineId(remoteName) {
  return `${SSH_MACHINE_PREFIX}${remoteName}`;
}
function tunnelPort(remote) {
  return remote.localPort ?? remote.lastLocalPort ?? null;
}
function tunnelBaseUrl(remote) {
  const port = tunnelPort(remote);
  return port ? `http://127.0.0.1:${port}` : null;
}
async function resolveSshMachineUrl(machineId) {
  if (!machineId.startsWith(SSH_MACHINE_PREFIX)) return null;
  const name = machineId.slice(SSH_MACHINE_PREFIX.length);
  const remote = (await readSshRemotes()).find((candidate) => candidate.name === name);
  return remote ? tunnelBaseUrl(remote) : null;
}
async function fetchAgents(baseUrl) {
  try {
    const controller = new AbortController();
    const tid = setTimeout(() => controller.abort(), PROBE_TIMEOUT_MS);
    const res = await fetch(`${baseUrl}/api/agents`, {
      signal: controller.signal,
      redirect: "manual"
    });
    clearTimeout(tid);
    if (!res.ok) return null;
    const parsed = AgentListSchemaZ.safeParse(await res.json());
    return parsed.success ? parsed.data.agents : null;
  } catch {
    return null;
  }
}
async function listSshAgentSources() {
  const byUrl = /* @__PURE__ */ new Map();
  for (const remote of await readSshRemotes()) {
    const url = tunnelBaseUrl(remote);
    if (!url) continue;
    const current = byUrl.get(url);
    const launched = (candidate) => candidate.lastLaunchedAt ?? "";
    if (!current || launched(remote) > launched(current)) byUrl.set(url, remote);
  }
  const probes = [...byUrl.entries()].map(
    async ([url, remote]) => {
      const agents = await fetchAgents(url);
      if (!agents) return null;
      return { machineId: sshMachineId(remote.name), machineName: remote.name, agents };
    }
  );
  return (await Promise.all(probes)).filter((source) => !!source);
}
var SSH_MACHINE_PREFIX, PROBE_TIMEOUT_MS;
var init_agent_remotes = __esm({
  "packages/daemon/src/lib/agent-remotes.ts"() {
    "use strict";
    init_src2();
    init_ssh_remote();
    SSH_MACHINE_PREFIX = "ssh:";
    PROBE_TIMEOUT_MS = 2500;
  }
});

// packages/daemon/src/lib/agent-registry.ts
function getDefaultExternalAgentRegistry() {
  if (!_default2) _default2 = new ExternalAgentRegistry();
  return _default2;
}
function sanitizeDisplay(value) {
  const chars = [];
  for (const ch of value) {
    const code = ch.codePointAt(0);
    if (code < 32 || code === 127 || code >= 128 && code <= 159) continue;
    chars.push(ch);
    if (chars.length >= 512) break;
  }
  return chars.join("");
}
function sanitizeRemoteAgent(a, remote) {
  const machineId = sanitizeDisplay(remote.machineId);
  return {
    ...a,
    id: `${machineId}:${sanitizeDisplay(a.id)}`,
    name: sanitizeDisplay(a.name),
    session: a.session === null ? null : sanitizeDisplay(a.session),
    paneTitle: a.paneTitle === null ? null : sanitizeDisplay(a.paneTitle),
    cwd: a.cwd === null ? null : sanitizeDisplay(a.cwd),
    taskTitle: a.taskTitle === null ? null : sanitizeDisplay(a.taskTitle),
    machineId,
    machineName: sanitizeDisplay(remote.machineName)
  };
}
function aggregateHqAgents(localAgents, selfMachineName, remotes) {
  const selfName = sanitizeDisplay(selfMachineName);
  const out = localAgents.map((a) => ({
    ...a,
    machineId: null,
    machineName: selfName
  }));
  for (const remote of remotes) {
    for (const a of remote.agents) {
      out.push(sanitizeRemoteAgent(a, remote));
    }
  }
  return out;
}
function mergeLocalAgents(tmuxAgents, externalAgents) {
  const tmuxPids = new Set(
    tmuxAgents.map((a) => a.pid).filter((pid) => pid !== null)
  );
  const externalOnly = externalAgents.filter((a) => a.pid === null || !tmuxPids.has(a.pid));
  return [...tmuxAgents, ...externalOnly];
}
var ExternalAgentRegistry, _default2;
var init_agent_registry = __esm({
  "packages/daemon/src/lib/agent-registry.ts"() {
    "use strict";
    ExternalAgentRegistry = class {
      agents = /* @__PURE__ */ new Map();
      offlineAfterMs;
      evictAfterMs;
      maxEntries;
      constructor(opts) {
        this.offlineAfterMs = opts?.offlineAfterMs ?? 5 * 6e4;
        this.evictAfterMs = opts?.evictAfterMs ?? 30 * 6e4;
        this.maxEntries = opts?.maxEntries ?? 1e3;
      }
      evictOldest() {
        let oldestId = null;
        let oldestSeen = Infinity;
        for (const [id, entry] of this.agents) {
          if (entry.lastSeen < oldestSeen) {
            oldestSeen = entry.lastSeen;
            oldestId = id;
          }
        }
        if (oldestId !== null) this.agents.delete(oldestId);
      }
      register(payload, now = Date.now()) {
        const existing = this.agents.get(payload.id);
        if (!existing && this.agents.size >= this.maxEntries) this.evictOldest();
        this.agents.set(payload.id, {
          id: payload.id,
          tool: payload.tool,
          name: payload.name ?? existing?.name ?? payload.id.slice(0, 12),
          cwd: payload.cwd ?? existing?.cwd ?? null,
          session: payload.session ?? existing?.session ?? null,
          pid: payload.pid ?? existing?.pid ?? null,
          status: payload.status ?? existing?.status ?? "idle",
          taskTitle: payload.taskTitle ?? existing?.taskTitle ?? null,
          registeredAt: existing?.registeredAt ?? now,
          lastSeen: now
        });
      }
      heartbeat(payload, now = Date.now()) {
        const entry = this.agents.get(payload.id);
        if (!entry) return false;
        entry.lastSeen = now;
        if (payload.status) entry.status = payload.status;
        if (payload.taskTitle !== void 0) entry.taskTitle = payload.taskTitle;
        return true;
      }
      unregister(id) {
        return this.agents.delete(id);
      }
      /** Drop entries that have been silent past the eviction window. */
      prune(now = Date.now()) {
        for (const [id, entry] of this.agents) {
          if (now - entry.lastSeen > this.evictAfterMs) this.agents.delete(id);
        }
      }
      /** Snapshot as unified AgentRecords; stale entries surface as `offline`. */
      list(now = Date.now()) {
        this.prune(now);
        return Array.from(this.agents.values()).map((entry) => {
          const stale = now - entry.lastSeen > this.offlineAfterMs;
          return {
            id: entry.id,
            kind: "external",
            tool: entry.tool,
            name: entry.name,
            status: stale ? "offline" : entry.status,
            session: entry.session,
            paneId: null,
            paneTitle: null,
            cwd: entry.cwd,
            taskId: null,
            taskTitle: entry.taskTitle,
            pid: entry.pid,
            lastActivity: new Date(entry.lastSeen).toISOString(),
            machineId: null,
            machineName: null
          };
        });
      }
    };
    _default2 = null;
  }
});

// packages/daemon/src/command-center/schemas.ts
import { z as z20 } from "zod";
var updateTaskSchema, createTaskSchema, savePlanSchema, savePlanContentSchema, sendCommandSchema, agentSendSchema, createMilestoneSchema, updateMilestoneSchema, updateAssertionSchema, triggerResearchSchema, launchSchema, stopSchema, skillNameRegex, createSkillSchema, updateSkillSchema;
var init_schemas = __esm({
  "packages/daemon/src/command-center/schemas.ts"() {
    "use strict";
    updateTaskSchema = z20.object({
      status: z20.enum(["todo", "in-progress", "review", "done"]).optional(),
      assignee: z20.string().optional(),
      title: z20.string().optional(),
      description: z20.string().optional(),
      priority: z20.number().optional()
    });
    createTaskSchema = z20.object({
      title: z20.string().trim().min(1, "Title is required"),
      description: z20.string().optional(),
      priority: z20.number().optional(),
      goal: z20.string().optional(),
      tags: z20.array(z20.string()).optional()
    });
    savePlanSchema = z20.object({
      content: z20.string().max(1e6, "Plan content is too large")
    });
    savePlanContentSchema = z20.object({
      content: z20.string().max(1e6, "Plan content is too large")
    });
    sendCommandSchema = z20.object({
      target: z20.string().min(1, "Target pane is required"),
      message: z20.string().min(1, "Message is required"),
      noEnter: z20.boolean().optional()
    });
    agentSendSchema = z20.object({
      id: z20.string().min(1).max(512),
      machineId: z20.string().min(1).max(256).nullish(),
      message: z20.string().min(1, "Message is required").max(1e4),
      noEnter: z20.boolean().optional()
    });
    createMilestoneSchema = z20.object({
      title: z20.string().trim().min(1, "Title is required"),
      sequence: z20.number().int().positive(),
      description: z20.string().optional()
    });
    updateMilestoneSchema = z20.object({
      status: z20.enum(["locked", "active", "done", "validating"]).optional(),
      title: z20.string().optional(),
      description: z20.string().optional()
    });
    updateAssertionSchema = z20.object({
      status: z20.enum(["pending", "passing", "failing", "blocked"]),
      evidence: z20.string().optional(),
      verifiedBy: z20.string().optional()
    });
    triggerResearchSchema = z20.object({
      type: z20.string().trim().min(1, "Research type is required")
    });
    launchSchema = z20.object({
      attach: z20.boolean().optional()
    }).optional();
    stopSchema = z20.object({}).optional();
    skillNameRegex = /^[A-Za-z0-9._ -]+$/;
    createSkillSchema = z20.object({
      name: z20.string().trim().min(1, "Skill name is required").regex(
        skillNameRegex,
        "Skill name may only contain letters, digits, dot, dash, underscore, or space"
      ),
      role: z20.string().trim().optional(),
      description: z20.string().optional(),
      specialties: z20.array(z20.string()).optional(),
      body: z20.string().optional()
    });
    updateSkillSchema = z20.object({
      role: z20.string().trim().optional(),
      description: z20.string().optional(),
      specialties: z20.array(z20.string()).optional(),
      body: z20.string().optional()
    });
  }
});

// packages/daemon/src/git/status-parser.ts
function tokenizeRecords(buf) {
  const out = [];
  let i = 0;
  while (i < buf.length) {
    const nul = buf.indexOf("\0", i);
    if (nul === -1) {
      const tail = buf.slice(i);
      if (tail) out.push(toRecord(tail));
      break;
    }
    const record = buf.slice(i, nul);
    i = nul + 1;
    if (!record) continue;
    const rec = toRecord(record);
    if (rec.kind === "renamed") {
      const orig = buf.indexOf("\0", i);
      if (orig !== -1) {
        rec.origPath = buf.slice(i, orig);
        i = orig + 1;
      }
    }
    out.push(rec);
  }
  return out;
}
function toRecord(raw) {
  switch (raw[0]) {
    case "#":
      return { kind: "header", raw };
    case "1":
      return { kind: "ordinary", raw };
    case "2":
      return { kind: "renamed", raw };
    case "u":
      return { kind: "unmerged", raw };
    case "?":
      return { kind: "untracked", raw };
    case "!":
      return { kind: "ignored", raw };
    default:
      return { kind: "ordinary", raw };
  }
}
function mapStatusCode(letter) {
  switch (letter) {
    case "U":
      return "conflicted";
    case "R":
    case "C":
      return "renamed";
    case "D":
      return "deleted";
    case "A":
      return "added";
    case "M":
    case "T":
      return "modified";
    default:
      return "modified";
  }
}
function parseHeaders(records) {
  let branch = null;
  let upstream = null;
  let ahead = 0;
  let behind = 0;
  let isUnborn = false;
  for (const rec of records) {
    if (rec.kind !== "header") continue;
    const parts = rec.raw.split(" ");
    if (parts[1] === "branch.head") {
      const value = parts.slice(2).join(" ");
      if (value === "(detached)") {
        branch = null;
      } else {
        branch = value;
      }
    } else if (parts[1] === "branch.upstream") {
      upstream = parts.slice(2).join(" ") || null;
    } else if (parts[1] === "branch.ab" && parts[2] && parts[3]) {
      ahead = parseInt(parts[2].replace("+", ""), 10) || 0;
      behind = parseInt(parts[3].replace("-", ""), 10) || 0;
    } else if (parts[1] === "branch.oid" && parts[2] === "(initial)") {
      isUnborn = true;
    }
  }
  return { branch, upstream, ahead, behind, isUnborn };
}
function parseChanges(records) {
  const staged = [];
  const unstaged = [];
  for (const rec of records) {
    if (rec.kind === "ordinary") {
      const parts = rec.raw.split(" ");
      const xy = parts[1] ?? "..";
      const path4 = parts.slice(8).join(" ");
      addBuckets(staged, unstaged, path4, xy);
    } else if (rec.kind === "renamed") {
      const parts = rec.raw.split(" ");
      const xy = parts[1] ?? "..";
      const path4 = parts.slice(9).join(" ");
      addBuckets(staged, unstaged, path4, xy);
    } else if (rec.kind === "untracked") {
      const path4 = rec.raw.slice(2);
      unstaged.push({ path: path4, status: "added", additions: 0, deletions: 0 });
    } else if (rec.kind === "unmerged") {
      const parts = rec.raw.split(" ");
      const path4 = parts.slice(10).join(" ");
      const entry = { path: path4, status: "conflicted", additions: 0, deletions: 0 };
      unstaged.push(entry);
    }
  }
  return { staged, unstaged };
}
function addBuckets(staged, unstaged, path4, xy) {
  const X = xy[0] ?? ".";
  const Y = xy[1] ?? ".";
  if (X !== "." && X !== "?") {
    staged.push({ path: path4, status: mapStatusCode(X), additions: 0, deletions: 0 });
  }
  if (Y !== "." && Y !== "?") {
    unstaged.push({ path: path4, status: mapStatusCode(Y), additions: 0, deletions: 0 });
  }
}
function parseStatus(rawZ) {
  const records = tokenizeRecords(rawZ);
  const header = parseHeaders(records);
  const { staged, unstaged } = parseChanges(records);
  return {
    staged,
    unstaged,
    currentBranch: header.branch,
    ahead: header.ahead,
    behind: header.behind,
    isUnborn: header.isUnborn,
    // `numstat` is plumbed separately (status -z doesn't carry add/del
    // counts). For now we leave the totals at zero — UI surfaces show
    // a separate diff endpoint when it needs per-file numbers.
    totalAdded: 0,
    totalDeleted: 0
  };
}
function parseBranchList(raw) {
  const lines = raw.split("\n").filter((l) => l.trim().length > 0);
  const branches2 = [];
  let current = null;
  for (const line of lines) {
    const [headFlag, name, upstream, track] = line.split("\0");
    if (!name) continue;
    const isCurrent = headFlag === "*";
    const entry = {
      name,
      isCurrent
    };
    if (upstream) entry.upstream = upstream;
    if (track) {
      const ahead = /ahead (\d+)/.exec(track);
      const behind = /behind (\d+)/.exec(track);
      if (ahead) entry.ahead = parseInt(ahead[1], 10);
      if (behind) entry.behind = parseInt(behind[1], 10);
    }
    if (isCurrent) current = name;
    branches2.push(entry);
  }
  return { current, branches: branches2 };
}
function parseRemotes(raw) {
  const seen = /* @__PURE__ */ new Map();
  for (const line of raw.split("\n")) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    const tab = trimmed.indexOf("	");
    if (tab === -1) continue;
    const name = trimmed.slice(0, tab);
    const rest = trimmed.slice(tab + 1);
    const space = rest.indexOf(" ");
    const url = space === -1 ? rest : rest.slice(0, space);
    if (!seen.has(name)) seen.set(name, url);
  }
  return Array.from(seen, ([name, url]) => ({ name, url }));
}
function parseRemoteBranches(raw, remotes) {
  const out = [];
  const remoteByName = new Map(remotes.map((r) => [r.name, r]));
  for (const line of raw.split("\n")) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    if (trimmed.includes(" -> ")) continue;
    const slash = trimmed.indexOf("/");
    if (slash === -1) continue;
    const remoteName = trimmed.slice(0, slash);
    const branch = trimmed.slice(slash + 1);
    const remote = remoteByName.get(remoteName) ?? { name: remoteName, url: "" };
    out.push({ type: "remote", branch, remote });
  }
  return out;
}
var init_status_parser = __esm({
  "packages/daemon/src/git/status-parser.ts"() {
    "use strict";
  }
});

// packages/daemon/src/git/errors.ts
import { Data } from "effect";
function toPayload(err) {
  switch (err._tag) {
    case "NotGitRepo":
      return { type: "not_git_repo" };
    case "NoRemote":
      return { type: "no_remote" };
    case "AuthFailed":
      return { type: "auth_failed", message: err.message };
    case "NetworkError":
      return { type: "network_error", message: err.message };
    case "PushRejected":
      return { type: "rejected", message: err.message };
    case "HookRejected":
      return { type: "hook_rejected", message: err.message };
    case "NothingToCommit":
      return { type: "nothing_to_commit" };
    case "BranchExists":
      return { type: "branch_exists", name: err.name };
    case "BranchNotFound":
      return { type: "branch_not_found", name: err.name };
    case "InvalidBranchName":
      return { type: "invalid_branch_name", name: err.name };
    case "UncommittedChanges":
      return { type: "uncommitted_changes", message: err.message };
    case "GitError":
      return { type: "error", message: err.message };
  }
}
function classifyExitError(stderr) {
  const text = stderr || "";
  if (/Authentication failed|could not read Username|Permission denied|publickey|HTTP\s+401/i.test(
    text
  )) {
    return new AuthFailed({ message: text.trim() });
  }
  if (/Could not resolve host|Connection refused|Connection timed out|repository .*not found|Failed to connect|Operation timed out/i.test(
    text
  )) {
    return new NetworkError({ message: text.trim() });
  }
  if (/\[rejected\]|! \[remote rejected\]|non-fast-forward|protected branch hook declined/i.test(text)) {
    return new PushRejected({ message: text.trim() });
  }
  if (/hook declined|pre-commit hook failed|pre-push hook/i.test(text)) {
    return new HookRejected({ message: text.trim() });
  }
  if (/No configured push destination|does not appear to be a git repository.*remote|no upstream/i.test(
    text
  )) {
    return new NoRemote({});
  }
  if (/nothing to commit|no changes added to commit/i.test(text)) {
    return new NothingToCommit({});
  }
  if (/Your local changes to the following files would be overwritten|would be overwritten by checkout/i.test(
    text
  )) {
    return new UncommittedChanges({ message: text.trim() });
  }
  if (/A branch named '.*' already exists/i.test(text)) {
    const m = /A branch named '(.*?)' already exists/i.exec(text);
    return new BranchExists({ name: m?.[1] ?? "" });
  }
  if (/pathspec '.*' did not match|did not match any file\(s\) known to git|invalid reference: /i.test(
    text
  )) {
    return new BranchNotFound({ name: "" });
  }
  if (/not a git repository/i.test(text)) {
    return new NotGitRepo({});
  }
  return new GitError({ message: text.trim() || "git failed" });
}
var NotGitRepo, NoRemote, AuthFailed, NetworkError, PushRejected, HookRejected, NothingToCommit, BranchExists, BranchNotFound, InvalidBranchName, UncommittedChanges, GitError;
var init_errors6 = __esm({
  "packages/daemon/src/git/errors.ts"() {
    "use strict";
    NotGitRepo = class extends Data.TaggedError("NotGitRepo") {
    };
    NoRemote = class extends Data.TaggedError("NoRemote") {
    };
    AuthFailed = class extends Data.TaggedError("AuthFailed") {
    };
    NetworkError = class extends Data.TaggedError("NetworkError") {
    };
    PushRejected = class extends Data.TaggedError("PushRejected") {
    };
    HookRejected = class extends Data.TaggedError("HookRejected") {
    };
    NothingToCommit = class extends Data.TaggedError("NothingToCommit") {
    };
    BranchExists = class extends Data.TaggedError("BranchExists") {
    };
    BranchNotFound = class extends Data.TaggedError("BranchNotFound") {
    };
    InvalidBranchName = class extends Data.TaggedError("InvalidBranchName") {
    };
    UncommittedChanges = class extends Data.TaggedError("UncommittedChanges") {
    };
    GitError = class extends Data.TaggedError("GitError") {
    };
  }
});

// packages/daemon/src/git/git-service.ts
import { execFile as execFile4 } from "node:child_process";
import { Effect } from "effect";
function runRaw(cwd, args) {
  return new Promise((resolve35, reject) => {
    execFile4(
      "git",
      args,
      {
        cwd,
        encoding: "utf-8",
        maxBuffer: GIT_MAX_BUFFER
      },
      (err, stdout, stderr) => {
        const out = String(stdout ?? "");
        const errOut = String(stderr ?? "");
        if (err) {
          const exit = err.code ?? 1;
          const failure = {
            code: typeof exit === "number" ? exit : 1,
            stdout: out,
            stderr: errOut
          };
          reject(failure);
          return;
        }
        resolve35({ stdout: out, stderr: errOut });
      }
    );
  });
}
function run(cwd, args) {
  return Effect.tryPromise({
    try: () => runRaw(cwd, args),
    catch: (cause) => {
      if (cause && typeof cause === "object" && "stderr" in cause) {
        const fail2 = cause;
        return classifyExitError(fail2.stderr || fail2.stdout);
      }
      const msg = cause instanceof Error ? cause.message : String(cause);
      return new GitError({ message: msg });
    }
  });
}
function ensureRepo(cwd) {
  return Effect.matchEffect(run(cwd, ["rev-parse", "--is-inside-work-tree"]), {
    onSuccess: () => Effect.void,
    onFailure: () => Effect.fail(new NotGitRepo({}))
  });
}
function status2(cwd) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const { stdout } = yield* run(cwd, ["status", "--porcelain=v2", "--branch", "-z"]);
    return parseStatus(stdout);
  });
}
function branches(cwd) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const [local, remotesRaw, remoteRaw] = yield* Effect.all(
      [
        run(cwd, [
          "branch",
          "--list",
          "--format=%(HEAD)%00%(refname:short)%00%(upstream:short)%00%(upstream:track,nobracket)"
        ]),
        run(cwd, ["remote", "-v"]),
        run(cwd, ["branch", "-r", "--format=%(refname:short)"])
      ],
      { concurrency: "unbounded" }
    );
    const parsedLocal = parseBranchList(local.stdout);
    const parsedRemotes = parseRemotes(remotesRaw.stdout);
    const parsedRemote = parseRemoteBranches(remoteRaw.stdout, parsedRemotes);
    const localBranches = parsedLocal.branches.map((b) => {
      const out = { type: "local", branch: b.name };
      if (b.upstream !== void 0) out.upstream = b.upstream;
      if (b.ahead !== void 0) out.ahead = b.ahead;
      if (b.behind !== void 0) out.behind = b.behind;
      return out;
    });
    return {
      local: localBranches,
      remote: parsedRemote,
      remotes: parsedRemotes,
      currentBranch: parsedLocal.current,
      isUnborn: parsedLocal.current === null && localBranches.length === 0
    };
  });
}
function checkout(cwd, opts) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const args = opts.create ? ["checkout", "-b", opts.branch] : ["checkout", opts.branch];
    yield* run(cwd, args);
    const { stdout } = yield* run(cwd, ["symbolic-ref", "--short", "HEAD"]);
    return { currentBranch: stdout.trim() };
  });
}
function commit2(cwd, opts) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const args = opts.all ? ["commit", "-a", "-m", opts.message] : ["commit", "-m", opts.message];
    yield* run(cwd, args);
    const { stdout } = yield* run(cwd, ["rev-parse", "HEAD"]);
    return { sha: stdout.trim() };
  });
}
function assertSafePaths(paths) {
  for (const p of paths) {
    if (!p || p.startsWith("/") || p.startsWith("\\") || p.split("/").includes("..")) {
      return Effect.fail(new GitError({ message: `unsafe path: ${p}` }));
    }
  }
  return Effect.void;
}
function stage(cwd, paths) {
  return Effect.gen(function* () {
    yield* assertSafePaths(paths);
    yield* ensureRepo(cwd);
    yield* run(cwd, ["add", "--", ...paths]);
  });
}
function unstage(cwd, paths) {
  return Effect.gen(function* () {
    yield* assertSafePaths(paths);
    yield* ensureRepo(cwd);
    yield* run(cwd, ["restore", "--staged", "--", ...paths]);
  });
}
function push(cwd, opts = {}) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const headRes = yield* run(cwd, ["symbolic-ref", "--short", "HEAD"]);
    const currentBranch = headRes.stdout.trim();
    const remote = opts.remote ?? "origin";
    const branch = opts.branch ?? currentBranch;
    const args = ["push"];
    if (opts.setUpstream) args.push("-u");
    args.push(remote, branch);
    yield* Effect.catchTag(
      run(cwd, args),
      "AuthFailed",
      // Bubble auth as-is — UI maps to a sign-in CTA.
      (e) => Effect.fail(new AuthFailed({ message: e.message }))
    );
    return { remote, branch };
  });
}
function parseNumstat(out) {
  return out.split("\n").map((l) => l.trim()).filter(Boolean).map((line) => {
    const [added, removed, ...rest] = line.split("	");
    const file = rest.join("	");
    return {
      file,
      // Binary files report "-" for both columns — coerce to 0.
      additions: Number.parseInt(added ?? "", 10) || 0,
      deletions: Number.parseInt(removed ?? "", 10) || 0
    };
  }).filter((f) => f.file.length > 0);
}
function revParse(cwd, ref) {
  return run(cwd, ["rev-parse", "--verify", "--quiet", `${ref}^{commit}`]).pipe(
    Effect.map((r) => r.stdout.trim())
  );
}
function commits(cwd, opts = {}) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const limit = Math.min(Math.max(opts.limit ?? 100, 1), 1e3);
    const headSha = (yield* run(cwd, ["rev-parse", "HEAD"])).stdout.trim();
    let base = opts.base?.trim() || null;
    let baseSha = null;
    let aheadShas = /* @__PURE__ */ new Set();
    let aheadCount = 0;
    if (base) {
      const baseResolved = yield* Effect.either(revParse(cwd, base));
      if (baseResolved._tag === "Right") {
        const mb = yield* Effect.either(run(cwd, ["merge-base", baseResolved.right, "HEAD"]));
        baseSha = mb._tag === "Right" ? mb.right.stdout.trim() || baseResolved.right : baseResolved.right;
        const aheadOut = yield* Effect.either(run(cwd, ["rev-list", `${baseSha}..HEAD`]));
        if (aheadOut._tag === "Right") {
          const shas = aheadOut.right.stdout.split("\n").map((s) => s.trim()).filter(Boolean);
          aheadShas = new Set(shas);
          aheadCount = shas.length;
        }
      } else {
        base = null;
      }
    }
    const fmt = ["%H", "%h", "%s", "%an", "%ae", "%cI", "%P"].join(LOG_FIELD_SEP);
    const logOut = yield* run(cwd, [
      "log",
      `--max-count=${limit}`,
      `--format=${fmt}${LOG_RECORD_SEP}`,
      "HEAD"
    ]);
    const commitList = logOut.stdout.split(LOG_RECORD_SEP).map((r) => r.replace(/^\n+/, "")).filter(Boolean).map((record) => {
      const [sha, shortSha, subject, author, authorEmail, date, parents] = record.split(LOG_FIELD_SEP);
      return {
        sha: sha ?? "",
        shortSha: shortSha ?? "",
        subject: subject ?? "",
        author: author ?? "",
        authorEmail: authorEmail ?? "",
        date: date ?? "",
        parents: (parents ?? "").split(" ").map((s) => s.trim()).filter(Boolean),
        ahead: aheadShas.has((sha ?? "").trim())
      };
    }).filter((c) => c.sha.length > 0);
    return { commits: commitList, base, baseSha, headSha, aheadCount };
  });
}
function commitDiff(cwd, sha) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    if (!/^[A-Za-z0-9_./-]+$/.test(sha)) {
      return yield* Effect.fail(new GitError({ message: `invalid commit ref: ${sha}` }));
    }
    const fullSha = yield* revParse(cwd, sha);
    const meta = yield* run(cwd, [
      "show",
      "--no-patch",
      `--format=%H${LOG_FIELD_SEP}%h${LOG_FIELD_SEP}%s${LOG_FIELD_SEP}%an${LOG_FIELD_SEP}%cI${LOG_FIELD_SEP}%P`,
      fullSha
    ]);
    const [, shortSha, subject, author, date, parentsRaw] = meta.stdout.trim().split(LOG_FIELD_SEP);
    const parents = (parentsRaw ?? "").split(" ").map((s) => s.trim()).filter(Boolean);
    const parent = parents[0] ?? null;
    const beforeRef = parent ?? EMPTY_TREE_SHA;
    const diff = (yield* run(cwd, ["diff", beforeRef, fullSha])).stdout;
    const files = parseNumstat((yield* run(cwd, ["diff", "--numstat", beforeRef, fullSha])).stdout);
    return {
      sha: fullSha,
      shortSha: shortSha ?? fullSha.slice(0, 7),
      parent,
      subject: subject ?? "",
      author: author ?? "",
      date: date ?? "",
      diff,
      files
    };
  });
}
function rangeDiff(cwd, base) {
  return Effect.gen(function* () {
    yield* ensureRepo(cwd);
    const trimmed = base.trim();
    if (!/^[A-Za-z0-9_./-]+$/.test(trimmed)) {
      return yield* Effect.fail(new GitError({ message: `invalid base ref: ${base}` }));
    }
    const headSha = (yield* run(cwd, ["rev-parse", "HEAD"])).stdout.trim();
    const baseResolved = yield* revParse(cwd, trimmed);
    const mb = yield* Effect.either(run(cwd, ["merge-base", baseResolved, "HEAD"]));
    const baseSha = mb._tag === "Right" ? mb.right.stdout.trim() || baseResolved : baseResolved;
    const diff = (yield* run(cwd, ["diff", `${baseSha}...HEAD`])).stdout;
    const files = parseNumstat(
      (yield* run(cwd, ["diff", "--numstat", `${baseSha}...HEAD`])).stdout
    );
    const aheadOut = yield* Effect.either(run(cwd, ["rev-list", "--count", `${baseSha}..HEAD`]));
    const aheadCount = aheadOut._tag === "Right" ? Number.parseInt(aheadOut.right.stdout.trim(), 10) || 0 : 0;
    return { base: trimmed, baseSha, headSha, diff, files, aheadCount };
  });
}
var GIT_MAX_BUFFER, EMPTY_TREE_SHA, LOG_FIELD_SEP, LOG_RECORD_SEP;
var init_git_service = __esm({
  "packages/daemon/src/git/git-service.ts"() {
    "use strict";
    init_status_parser();
    init_errors6();
    GIT_MAX_BUFFER = 32 * 1024 * 1024;
    EMPTY_TREE_SHA = "4b825dc642cb6eb9a060e54bf8d69288fbee4904";
    LOG_FIELD_SEP = "";
    LOG_RECORD_SEP = "";
  }
});

// packages/daemon/src/git/github-service.ts
import { execFile as execFile5 } from "node:child_process";
import { Effect as Effect2, Data as Data2 } from "effect";
function toPayload2(err) {
  switch (err._tag) {
    case "GhUnavailable":
      return { type: "gh_unavailable" };
    case "NotAuthenticated":
      return { type: "not_authenticated" };
    case "NoGithubRemote":
      return { type: "no_github_remote" };
    case "HeadNotPushed":
      return { type: "head_not_pushed", branch: err.branch };
    case "PrAlreadyExists":
      return err.url ? { type: "pr_already_exists", url: err.url } : { type: "pr_already_exists" };
    case "GithubValidation":
      return { type: "validation_failed", message: err.message };
    case "GithubNetwork":
      return { type: "network_error", message: err.message };
    case "GithubError":
      return { type: "error", message: err.message };
  }
}
function runCli(cmd, args, cwd) {
  return new Promise((resolve35, reject) => {
    execFile5(
      cmd,
      args,
      {
        ...cwd ? { cwd } : {},
        encoding: "utf-8",
        maxBuffer: 8 * 1024 * 1024
      },
      (err, stdout, stderr) => {
        const out = String(stdout ?? "");
        const errOut = String(stderr ?? "");
        if (err) {
          const code = err.code;
          if (code === "ENOENT") {
            reject({ code: -1, stdout: out, stderr: "ENOENT" });
            return;
          }
          const exit = err.code ?? 1;
          reject({
            code: typeof exit === "number" ? exit : 1,
            stdout: out,
            stderr: errOut
          });
          return;
        }
        resolve35({ stdout: out, stderr: errOut });
      }
    );
  });
}
function classifyGhFailure(failure) {
  if (failure.stderr === "ENOENT") return new GhUnavailable({});
  const text = failure.stderr || failure.stdout || "";
  if (/not logged into|gh auth login|authentication required/i.test(text)) {
    return new NotAuthenticated({});
  }
  if (/no such ref|src refspec .* does not match|head ref .* does not exist on the remote/i.test(text)) {
    const m = /\b([^\s/]+\/[^\s/]+)\b/.exec(text);
    return new HeadNotPushed({ branch: m?.[1] ?? "" });
  }
  if (/already exists\.|a pull request for branch .* already exists/i.test(text)) {
    const m = /https:\/\/github\.com\/[^\s]+\/pull\/\d+/.exec(text);
    return new PrAlreadyExists(m ? { url: m[0] } : {});
  }
  if (/validation failed|Validation Failed/i.test(text)) {
    return new GithubValidation({ message: text.trim() });
  }
  if (/network|connection refused|getaddrinfo|timed out/i.test(text)) {
    return new GithubNetwork({ message: text.trim() });
  }
  return new GithubError({ message: text.trim() || "gh failed" });
}
function ghAvailable() {
  return Effect2.tryPromise({
    try: () => runCli("gh", ["--version"]),
    catch: () => null
  }).pipe(
    Effect2.match({
      onFailure: () => false,
      onSuccess: () => true
    })
  );
}
function readGhToken() {
  return Effect2.tryPromise({
    try: () => runCli("gh", ["auth", "token"]),
    catch: (cause) => classifyGhFailure(cause)
  }).pipe(
    Effect2.flatMap(({ stdout }) => {
      const tok = stdout.trim();
      if (!tok) return Effect2.fail(new NotAuthenticated({}));
      return Effect2.succeed(tok);
    })
  );
}
function fetchAuthenticatedUser(token) {
  return Effect2.tryPromise({
    try: async () => {
      const res = await fetch("https://api.github.com/user", {
        headers: {
          Authorization: `token ${token}`,
          Accept: "application/vnd.github+json",
          "User-Agent": "tmux-ide-daemon"
        }
      });
      if (res.status === 401) throw new NotAuthenticated({});
      if (!res.ok) {
        throw new GithubError({
          message: `GET /user failed (${res.status})`
        });
      }
      const body = await res.json();
      return {
        id: typeof body.id === "number" ? body.id : 0,
        login: body.login ?? "",
        name: body.name ?? "",
        email: body.email ?? "",
        avatar_url: body.avatar_url ?? ""
      };
    },
    catch: (cause) => {
      if (cause instanceof NotAuthenticated || cause instanceof GithubError) return cause;
      const msg = cause instanceof Error ? cause.message : String(cause);
      return new GithubNetwork({ message: msg });
    }
  });
}
function status3() {
  return Effect2.gen(function* () {
    const available = yield* ghAvailable();
    if (!available) {
      return {
        authenticated: false,
        user: null,
        tokenSource: null,
        ghAvailable: false
      };
    }
    const result = yield* Effect2.either(readGhToken());
    if (result._tag === "Left") {
      return {
        authenticated: false,
        user: null,
        tokenSource: null,
        ghAvailable: true
      };
    }
    const userResult = yield* Effect2.either(fetchAuthenticatedUser(result.right));
    if (userResult._tag === "Left") {
      return {
        authenticated: false,
        user: null,
        tokenSource: "cli",
        ghAvailable: true
      };
    }
    return {
      authenticated: true,
      user: userResult.right,
      tokenSource: "cli",
      ghAvailable: true
    };
  });
}
function detectRepo(cwd) {
  return Effect2.tryPromise({
    try: () => runCli("git", ["remote", "get-url", "--push", "origin"], cwd),
    catch: () => new NoGithubRemote({})
  }).pipe(
    Effect2.flatMap(({ stdout }) => {
      const parsed = parseGitHubRepository(stdout.trim());
      if (!parsed) return Effect2.fail(new NoGithubRemote({}));
      return Effect2.succeed(parsed);
    })
  );
}
function createPullRequest(cwd, input) {
  return Effect2.gen(function* () {
    const repo = yield* detectRepo(cwd);
    const available = yield* ghAvailable();
    if (available) {
      return yield* createPrWithGh(cwd, repo, input);
    }
    return yield* createPrWithRest(repo, input);
  });
}
function createPrWithGh(cwd, repo, input) {
  return Effect2.gen(function* () {
    const args = [
      "pr",
      "create",
      "--repo",
      repo.nameWithOwner,
      "--title",
      input.title,
      "--body",
      input.body ?? "",
      "--base",
      input.base
    ];
    if (input.head) args.push("--head", input.head);
    if (input.draft) args.push("--draft");
    const { stdout } = yield* Effect2.tryPromise({
      try: () => runCli("gh", args, cwd),
      catch: (cause) => classifyGhFailure(cause)
    });
    const url = (stdout.split("\n").find((l) => l.startsWith("https://")) ?? "").trim();
    if (!url)
      return yield* Effect2.fail(new GithubError({ message: "gh pr create did not return a URL" }));
    const number = parseInt(url.split("/").pop() ?? "0", 10) || 0;
    return {
      url,
      number,
      title: input.title,
      base: input.base,
      head: input.head ?? "",
      isDraft: Boolean(input.draft)
    };
  });
}
function toCheckRun(raw, fallbackSha) {
  const status4 = raw.status === "in_progress" || raw.status === "queued" || raw.status === "completed" ? raw.status : "queued";
  const conclusionRaw = raw.conclusion;
  const conclusion = conclusionRaw === "success" || conclusionRaw === "failure" || conclusionRaw === "neutral" || conclusionRaw === "cancelled" || conclusionRaw === "timed_out" || conclusionRaw === "action_required" || conclusionRaw === "stale" || conclusionRaw === "skipped" ? conclusionRaw : null;
  return {
    id: String(raw.id ?? `${raw.name}-${raw.head_sha ?? fallbackSha}`),
    name: raw.name ?? "(unnamed check)",
    status: status4,
    conclusion,
    detailsUrl: raw.details_url ?? raw.html_url ?? null,
    headSha: raw.head_sha ?? fallbackSha,
    startedAt: raw.started_at ?? null,
    completedAt: raw.completed_at ?? null,
    appName: raw.app?.name ?? null,
    appAvatarUrl: raw.app?.owner?.avatar_url ?? null,
    workflowName: raw.app?.slug === "github-actions" ? raw.app?.name ?? null : null
  };
}
function resolveSha(cwd, ref) {
  return Effect2.tryPromise({
    try: () => runCli("git", ["rev-parse", ref], cwd),
    catch: () => new GithubError({ message: `failed to resolve ref "${ref}"` })
  }).pipe(Effect2.map(({ stdout }) => stdout.trim()));
}
function listChecks(cwd, ref) {
  return Effect2.gen(function* () {
    const repo = yield* detectRepo(cwd);
    const headRef = ref?.trim() || "HEAD";
    const sha = yield* resolveSha(cwd, headRef);
    const available = yield* ghAvailable();
    const raw = available ? yield* fetchChecksViaGh(cwd, repo, sha) : yield* fetchChecksViaRest(repo, sha);
    const runs = (raw.check_runs ?? []).map((r) => toCheckRun(r, sha));
    return { ref: sha, runs, summary: summarizeChecks(runs) };
  });
}
function fetchChecksViaGh(cwd, repo, sha) {
  const path4 = `repos/${repo.owner}/${repo.repo}/commits/${sha}/check-runs?per_page=100`;
  return Effect2.tryPromise({
    try: () => runCli("gh", ["api", "-H", "Accept: application/vnd.github+json", path4], cwd),
    catch: (cause) => classifyGhFailure(cause)
  }).pipe(
    Effect2.flatMap(
      ({ stdout }) => Effect2.try({
        try: () => JSON.parse(stdout),
        catch: () => new GithubError({ message: "could not parse check-runs JSON" })
      })
    )
  );
}
function fetchChecksViaRest(repo, sha) {
  return Effect2.gen(function* () {
    const token = yield* readGhToken();
    const res = yield* Effect2.tryPromise({
      try: () => fetch(
        `https://api.github.com/repos/${repo.owner}/${repo.repo}/commits/${sha}/check-runs?per_page=100`,
        {
          headers: {
            Authorization: `token ${token}`,
            Accept: "application/vnd.github+json",
            "User-Agent": "tmux-ide-daemon"
          }
        }
      ),
      catch: (cause) => new GithubNetwork({ message: cause instanceof Error ? cause.message : String(cause) })
    });
    if (res.status === 401 || res.status === 403) {
      return yield* Effect2.fail(new NotAuthenticated({}));
    }
    if (res.status === 404) {
      return { check_runs: [] };
    }
    if (!res.ok) {
      return yield* Effect2.fail(
        new GithubError({ message: `GET check-runs failed (${res.status})` })
      );
    }
    return yield* Effect2.tryPromise({
      try: () => res.json(),
      catch: () => new GithubError({ message: "could not parse check-runs body" })
    });
  });
}
function createPrWithRest(repo, input) {
  return Effect2.gen(function* () {
    const token = yield* readGhToken();
    const res = yield* Effect2.tryPromise({
      try: () => fetch(`https://api.github.com/repos/${repo.owner}/${repo.repo}/pulls`, {
        method: "POST",
        headers: {
          Authorization: `token ${token}`,
          Accept: "application/vnd.github+json",
          "Content-Type": "application/json",
          "User-Agent": "tmux-ide-daemon"
        },
        body: JSON.stringify({
          title: input.title,
          body: input.body ?? "",
          base: input.base,
          head: input.head,
          draft: Boolean(input.draft)
        })
      }),
      catch: (cause) => new GithubNetwork({ message: cause instanceof Error ? cause.message : String(cause) })
    });
    const body = yield* Effect2.tryPromise({
      try: () => res.json(),
      catch: () => new GithubError({ message: "could not parse PR response body" })
    });
    if (res.status === 401 || res.status === 403) {
      return yield* Effect2.fail(new NotAuthenticated({}));
    }
    if (res.status === 422) {
      const msg = String(body.message ?? "Validation failed");
      if (/already exists/i.test(msg)) return yield* Effect2.fail(new PrAlreadyExists({}));
      return yield* Effect2.fail(new GithubValidation({ message: msg }));
    }
    if (!res.ok || !body.html_url || typeof body.number !== "number") {
      return yield* Effect2.fail(
        new GithubError({
          message: String(body.message ?? `POST /pulls failed (${res.status})`)
        })
      );
    }
    return {
      url: body.html_url,
      number: body.number,
      title: input.title,
      base: input.base,
      head: input.head ?? "",
      isDraft: Boolean(input.draft)
    };
  });
}
var GhUnavailable, NotAuthenticated, NoGithubRemote, HeadNotPushed, PrAlreadyExists, GithubValidation, GithubNetwork, GithubError;
var init_github_service = __esm({
  "packages/daemon/src/git/github-service.ts"() {
    "use strict";
    init_src2();
    GhUnavailable = class extends Data2.TaggedError("GhUnavailable") {
    };
    NotAuthenticated = class extends Data2.TaggedError("NotAuthenticated") {
    };
    NoGithubRemote = class extends Data2.TaggedError("NoGithubRemote") {
    };
    HeadNotPushed = class extends Data2.TaggedError("HeadNotPushed") {
    };
    PrAlreadyExists = class extends Data2.TaggedError("PrAlreadyExists") {
    };
    GithubValidation = class extends Data2.TaggedError("GithubValidation") {
    };
    GithubNetwork = class extends Data2.TaggedError("GithubNetwork") {
    };
    GithubError = class extends Data2.TaggedError("GithubError") {
    };
  }
});

// packages/daemon/src/lib/terminals-store.ts
import { existsSync as existsSync25, mkdirSync as mkdirSync20, readFileSync as readFileSync19, renameSync as renameSync14, writeFileSync as writeFileSync19 } from "node:fs";
import { dirname as dirname11, join as join26 } from "node:path";
function path(dir) {
  return join26(dir, TERMINALS_FILE);
}
function ensureDir(dir) {
  mkdirSync20(dirname11(path(dir)), { recursive: true });
}
function loadTerminals(dir) {
  const file = path(dir);
  if (!existsSync25(file)) return [];
  try {
    const body = readFileSync19(file, "utf-8");
    const parsed = JSON.parse(body);
    if (!parsed.terminals || !Array.isArray(parsed.terminals)) return [];
    return parsed.terminals.filter((t) => isTerminal(t)).map((t) => ({ ...t }));
  } catch {
    return [];
  }
}
function isTerminal(value) {
  if (!value || typeof value !== "object") return false;
  const v = value;
  return typeof v.id === "string" && SAFE_ID.test(v.id) && typeof v.projectId === "string" && typeof v.scopeId === "string" && typeof v.name === "string" && (v.kind === "shell" || v.kind === "setup" || v.kind === "run" || v.kind === "teardown") && typeof v.createdAt === "string" && typeof v.updatedAt === "string";
}
function writeAtomic(dir, terminals) {
  ensureDir(dir);
  const file = path(dir);
  const tmp = `${file}.tmp`;
  writeFileSync19(tmp, JSON.stringify({ terminals }, null, 2) + "\n");
  renameSync14(tmp, file);
}
function upsertTerminal(dir, input) {
  if (!SAFE_ID.test(input.id)) {
    throw new Error(`invalid terminal id "${input.id}"`);
  }
  const existing = loadTerminals(dir);
  const idx = existing.findIndex((t) => t.id === input.id);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const next = {
    id: input.id,
    projectId: input.projectId,
    scopeId: input.scopeId,
    name: input.name,
    kind: input.kind,
    createdAt: existing[idx]?.createdAt ?? now,
    updatedAt: now,
    ...input.scripted ? { scripted: true } : {}
  };
  if (idx === -1) existing.push(next);
  else existing[idx] = next;
  writeAtomic(dir, existing);
  return next;
}
function renameTerminal(dir, id, name) {
  const all = loadTerminals(dir);
  const idx = all.findIndex((t) => t.id === id);
  if (idx === -1) return null;
  const trimmed = name.trim();
  if (!trimmed) throw new Error("name required");
  const next = {
    ...all[idx],
    name: trimmed,
    updatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
  all[idx] = next;
  writeAtomic(dir, all);
  return next;
}
function deleteTerminal(dir, id) {
  const all = loadTerminals(dir);
  const next = all.filter((t) => t.id !== id);
  if (next.length === all.length) return false;
  writeAtomic(dir, next);
  return true;
}
var TERMINALS_FILE, SAFE_ID;
var init_terminals_store = __esm({
  "packages/daemon/src/lib/terminals-store.ts"() {
    "use strict";
    TERMINALS_FILE = ".tmux-ide/terminals.json";
    SAFE_ID = /^[A-Za-z0-9_-]+$/u;
  }
});

// packages/daemon/src/lib/auth/auth-service.ts
var auth_service_exports = {};
__export(auth_service_exports, {
  AuthService: () => AuthService
});
import * as crypto2 from "node:crypto";
import { readFileSync as readFileSync20, existsSync as existsSync26 } from "node:fs";
import { join as join27 } from "node:path";
import { homedir as homedir9 } from "node:os";
function base64url(buf) {
  const b = typeof buf === "string" ? Buffer.from(buf) : buf;
  return b.toString("base64url");
}
function decodeBase64url(str) {
  return Buffer.from(str, "base64url");
}
function signJwt(payload, secret, expiresInSec) {
  const header = { alg: "HS256", typ: "JWT" };
  const now = Math.floor(Date.now() / 1e3);
  const body = { ...payload, iat: now, exp: now + expiresInSec };
  const segments = [base64url(JSON.stringify(header)), base64url(JSON.stringify(body))];
  const sigInput = segments.join(".");
  const sig = crypto2.createHmac("sha256", secret).update(sigInput).digest();
  segments.push(base64url(sig));
  return segments.join(".");
}
function verifyJwt(token, secret) {
  const parts = token.split(".");
  if (parts.length !== 3) return { valid: false };
  const sigInput = parts[0] + "." + parts[1];
  const expected = crypto2.createHmac("sha256", secret).update(sigInput).digest();
  const actual = decodeBase64url(parts[2]);
  if (expected.length !== actual.length || !crypto2.timingSafeEqual(expected, actual)) {
    return { valid: false };
  }
  try {
    const payload = JSON.parse(decodeBase64url(parts[1]).toString());
    if (typeof payload.exp === "number" && payload.exp < Math.floor(Date.now() / 1e3)) {
      return { valid: false };
    }
    return { valid: true, payload };
  } catch {
    return { valid: false };
  }
}
var TOKEN_EXPIRY_SEC, CHALLENGE_TIMEOUT_MS, AuthService;
var init_auth_service = __esm({
  "packages/daemon/src/lib/auth/auth-service.ts"() {
    "use strict";
    init_log();
    TOKEN_EXPIRY_SEC = 24 * 60 * 60;
    CHALLENGE_TIMEOUT_MS = 5 * 60 * 1e3;
    AuthService = class {
      challenges = /* @__PURE__ */ new Map();
      jwtSecret;
      cleanupTimer;
      constructor(secret) {
        this.jwtSecret = secret ?? process.env.JWT_SECRET ?? crypto2.randomBytes(64).toString("hex");
        this.cleanupTimer = setInterval(() => this.cleanupExpiredChallenges(), 6e4);
      }
      dispose() {
        clearInterval(this.cleanupTimer);
      }
      cleanupExpiredChallenges() {
        const now = Date.now();
        for (const [id, ch] of this.challenges) {
          if (now - ch.timestamp > CHALLENGE_TIMEOUT_MS) {
            this.challenges.delete(id);
          }
        }
      }
      // ---- JWT ----------------------------------------------------------------
      generateToken(userId) {
        return signJwt({ userId }, this.jwtSecret, TOKEN_EXPIRY_SEC);
      }
      verifyToken(token) {
        const result = verifyJwt(token, this.jwtSecret);
        if (!result.valid) return { valid: false };
        return { valid: true, userId: result.payload.userId };
      }
      // ---- SSH challenge-response --------------------------------------------
      createChallenge(userId) {
        const challengeId = crypto2.randomUUID();
        const challenge = crypto2.randomBytes(32);
        this.challenges.set(challengeId, {
          challengeId,
          challenge,
          timestamp: Date.now(),
          userId
        });
        return { challengeId, challenge: challenge.toString("base64") };
      }
      async authenticateWithSSHKey(auth) {
        const challenge = this.challenges.get(auth.challengeId);
        if (!challenge) {
          return { success: false, error: "Invalid or expired challenge" };
        }
        const sigBuf = Buffer.from(auth.signature, "base64");
        if (!this.verifySSHSignature(challenge.challenge, sigBuf, auth.publicKey)) {
          return { success: false, error: "Invalid SSH key signature" };
        }
        const authorized = this.checkSSHKeyAuthorization(challenge.userId, auth.publicKey);
        if (!authorized) {
          return { success: false, error: "SSH key not authorized for this user" };
        }
        this.challenges.delete(auth.challengeId);
        const token = this.generateToken(challenge.userId);
        return { success: true, userId: challenge.userId, token };
      }
      // ---- SSH helpers -------------------------------------------------------
      verifySSHSignature(challenge, signature, publicKeyStr) {
        try {
          const parts = publicKeyStr.trim().split(" ");
          if (parts.length < 2) return false;
          const keyType = parts[0];
          const keyData = parts[1];
          if (keyType !== "ssh-ed25519") {
            logger.warn("auth", `Unsupported key type: ${keyType}`);
            return false;
          }
          if (signature.length !== 64) return false;
          const sshBuf = Buffer.from(keyData, "base64");
          let offset = 0;
          const algLen = sshBuf.readUInt32BE(offset);
          offset += 4 + algLen;
          const keyLen = sshBuf.readUInt32BE(offset);
          offset += 4;
          if (keyLen !== 32) return false;
          const rawPub = sshBuf.subarray(offset, offset + 32);
          const pubKey = crypto2.createPublicKey({
            key: Buffer.concat([
              Buffer.from([48, 42]),
              Buffer.from([48, 5]),
              Buffer.from([6, 3, 43, 101, 112]),
              Buffer.from([3, 33, 0]),
              rawPub
            ]),
            format: "der",
            type: "spki"
          });
          return crypto2.verify(null, challenge, pubKey, signature);
        } catch (err) {
          logger.error("auth", "SSH signature verification failed", {
            error: String(err)
          });
          return false;
        }
      }
      checkSSHKeyAuthorization(userId, publicKey) {
        try {
          const home = userId === process.env.USER ? homedir9() : `/home/${userId}`;
          const authKeysPath = join27(home, ".ssh", "authorized_keys");
          if (!existsSync26(authKeysPath)) return false;
          const authorizedKeys = readFileSync20(authKeysPath, "utf-8");
          const parts = publicKey.trim().split(" ");
          const keyData = parts.length > 1 ? parts[1] : parts[0];
          return authorizedKeys.includes(keyData);
        } catch {
          return false;
        }
      }
      getCurrentUser() {
        return process.env.USER ?? process.env.USERNAME ?? "unknown";
      }
    };
  }
});

// packages/daemon/src/lib/auth/middleware.ts
function authMiddleware(authService, config2) {
  return async (c, next) => {
    if (config2.method === "none") {
      return next();
    }
    const path4 = new URL(c.req.url).pathname;
    if (path4 === "/health" || path4.startsWith("/api/auth/")) {
      return next();
    }
    const authHeader = c.req.header("Authorization");
    if (!authHeader?.startsWith("Bearer ")) {
      return c.json({ error: "Missing or invalid Authorization header" }, 401);
    }
    const token = authHeader.slice(7);
    const result = authService.verifyToken(token);
    if (!result.valid) {
      return c.json({ error: "Invalid or expired token" }, 401);
    }
    c.set("userId", result.userId);
    return next();
  };
}
var init_middleware = __esm({
  "packages/daemon/src/lib/auth/middleware.ts"() {
    "use strict";
  }
});

// packages/daemon/src/lib/hq/types.ts
var types_exports = {};
__export(types_exports, {
  HQConfigSchema: () => HQConfigSchema,
  RegistrationPayloadSchema: () => RegistrationPayloadSchema
});
var init_types2 = __esm({
  "packages/daemon/src/lib/hq/types.ts"() {
    "use strict";
    init_src2();
  }
});

// packages/daemon/src/command-center/search.ts
import { spawn as spawn7 } from "node:child_process";
import { createInterface } from "node:readline";
import { z as z21 } from "zod";
async function resolveRipgrepPath() {
  const override = process.env["TMUX_IDE_RIPGREP_PATH"]?.trim();
  if (override) return override;
  try {
    const mod = await import("@vscode/ripgrep");
    if (mod.rgPath) return mod.rgPath;
  } catch {
  }
  return "rg";
}
function parseSearchQuery(raw) {
  const q = (raw["q"] ?? "").trim();
  if (!q) return { ok: false, error: "Missing q= query parameter" };
  const include = splitGlobs(raw["include"]);
  const exclude = splitGlobs(raw["exclude"]);
  const bad = [...include, ...exclude].find((glob) => !isSafeGlob(glob));
  if (bad) return { ok: false, error: `Glob escapes workspace: ${bad}` };
  const caseMode = CaseModeZ.safeParse(raw["case"]);
  const regex = BoolFlagZ.safeParse(raw["regex"]);
  const context = clampInt(raw["context"], 0, 10, DEFAULT_CONTEXT);
  const maxResults = clampInt(raw["maxResults"], 1, MAX_RESULTS_CEILING, DEFAULT_MAX_RESULTS);
  const maxFileSize = clampInt(
    raw["maxFileSize"],
    1024,
    MAX_FILESIZE_BYTES_CEILING,
    DEFAULT_MAX_FILESIZE_BYTES
  );
  return {
    ok: true,
    query: {
      q,
      include,
      exclude,
      case: caseMode.success ? caseMode.data : "smart",
      regex: regex.success ? regex.data : false,
      context,
      maxResults,
      maxFileSize
    }
  };
}
function splitGlobs(raw) {
  if (!raw) return [];
  return raw.split(",").map((s) => s.trim()).filter((s) => s.length > 0);
}
function isSafeGlob(glob) {
  if (!glob) return false;
  if (glob.startsWith("/")) return false;
  const segs = glob.split("/");
  return !segs.some((seg) => seg === "..");
}
function clampInt(raw, min, max, fallback) {
  if (raw === void 0) return fallback;
  const n = Number.parseInt(raw, 10);
  if (!Number.isFinite(n)) return fallback;
  return Math.max(min, Math.min(max, n));
}
function buildRgArgs(query, searchRoot) {
  const args = [
    "--json",
    `--max-count=${PER_FILE_MAX_COUNT}`,
    `--max-filesize=${query.maxFileSize}`,
    "--hidden",
    "--no-messages",
    "--color=never"
  ];
  switch (query.case) {
    case "sensitive":
      args.push("--case-sensitive");
      break;
    case "insensitive":
      args.push("--ignore-case");
      break;
    case "smart":
      args.push("--smart-case");
      break;
  }
  if (query.context > 0) {
    args.push(`--context=${query.context}`);
  }
  for (const include of query.include) {
    args.push("--glob", include);
  }
  for (const exclude of query.exclude) {
    args.push("--glob", `!${exclude}`);
  }
  if (query.regex) {
    args.push("--regexp", query.q);
  } else {
    args.push("--fixed-strings", query.q);
  }
  args.push("--", searchRoot);
  return args;
}
function reshapeRgFrame(line, searchRoot) {
  let raw;
  try {
    raw = JSON.parse(line);
  } catch {
    return null;
  }
  if (!raw || typeof raw !== "object" || !("type" in raw)) return null;
  switch (raw.type) {
    case "begin":
      return { type: "begin", path: relativizePath(raw.data?.path?.text ?? "", searchRoot) };
    case "match": {
      const rel = relativizePath(raw.data?.path?.text ?? "", searchRoot);
      const text = raw.data?.lines?.text ?? "";
      const submatches = (raw.data?.submatches ?? []).map((sm) => ({
        start: sm.start,
        end: sm.end
      }));
      return {
        type: "match",
        path: rel,
        line: raw.data?.line_number ?? 0,
        text,
        submatches
      };
    }
    case "context":
      return {
        type: "context",
        path: relativizePath(raw.data?.path?.text ?? "", searchRoot),
        line: raw.data?.line_number ?? 0,
        text: raw.data?.lines?.text ?? ""
      };
    case "end":
      return { type: "end", path: relativizePath(raw.data?.path?.text ?? "", searchRoot) };
    case "summary":
      return null;
    default:
      return null;
  }
}
function relativizePath(absolute, searchRoot) {
  if (!absolute) return "";
  if (absolute === searchRoot) return ".";
  const prefix = searchRoot.endsWith("/") ? searchRoot : searchRoot + "/";
  return absolute.startsWith(prefix) ? absolute.slice(prefix.length) : absolute;
}
async function* runSearch(opts) {
  const args = buildRgArgs(opts.query, opts.searchRoot);
  const spawnFn = opts.spawn ?? spawn7;
  const startedAt = Date.now();
  let child;
  try {
    child = spawnFn(opts.rgPath, args, {
      cwd: opts.searchRoot,
      stdio: ["ignore", "pipe", "pipe"],
      // Don't inherit env beyond what rg actually needs; rg only reads
      // a couple of vars and a clean env is cheaper / more predictable.
      env: { PATH: process.env["PATH"] ?? "" }
    });
  } catch (err) {
    yield {
      type: "error",
      message: err instanceof Error ? err.message : "Failed to spawn ripgrep",
      fatal: true
    };
    return;
  }
  const onAbort = () => {
    if (!child.killed) {
      try {
        child.kill("SIGTERM");
      } catch {
      }
    }
  };
  opts.signal.addEventListener("abort", onAbort);
  let stderr = "";
  child.stderr.setEncoding("utf-8");
  child.stderr.on("data", (chunk) => {
    stderr += chunk;
    if (stderr.length > 4096) stderr = stderr.slice(-4096);
  });
  let filesSearched = 0;
  let matches = 0;
  let truncated = false;
  const rl = createInterface({ input: child.stdout, crlfDelay: Infinity });
  try {
    for await (const line of rl) {
      if (opts.signal.aborted) break;
      const frame = reshapeRgFrame(line, opts.searchRoot);
      if (!frame) continue;
      if (frame.type === "begin") filesSearched += 1;
      if (frame.type === "match") {
        matches += 1;
        if (matches > opts.query.maxResults) {
          truncated = true;
          if (!child.killed) child.kill("SIGTERM");
          break;
        }
      }
      yield frame;
    }
  } finally {
    opts.signal.removeEventListener("abort", onAbort);
    rl.close();
  }
  const exit = await new Promise(
    (resolve35) => {
      if (child.exitCode !== null || child.signalCode !== null) {
        resolve35({ code: child.exitCode, signal: child.signalCode });
        return;
      }
      child.once("exit", (code, sig) => resolve35({ code, signal: sig }));
    }
  );
  const aborted = opts.signal.aborted;
  if (exit.code === 2 && !aborted && !truncated) {
    yield {
      type: "error",
      message: stderr.trim() || "ripgrep exited with code 2",
      fatal: true
    };
    return;
  }
  if (aborted) return;
  yield {
    type: "summary",
    matches,
    filesSearched,
    elapsedMs: Date.now() - startedAt,
    truncated
  };
}
var MAX_RESULTS_CEILING, MAX_FILESIZE_BYTES_CEILING, DEFAULT_MAX_RESULTS, DEFAULT_MAX_FILESIZE_BYTES, DEFAULT_CONTEXT, PER_FILE_MAX_COUNT, CaseModeZ, BoolFlagZ;
var init_search = __esm({
  "packages/daemon/src/command-center/search.ts"() {
    "use strict";
    MAX_RESULTS_CEILING = 1e4;
    MAX_FILESIZE_BYTES_CEILING = 50 * 1024 * 1024;
    DEFAULT_MAX_RESULTS = 1e3;
    DEFAULT_MAX_FILESIZE_BYTES = 5 * 1024 * 1024;
    DEFAULT_CONTEXT = 0;
    PER_FILE_MAX_COUNT = 50;
    CaseModeZ = z21.enum(["smart", "sensitive", "insensitive"]).default("smart");
    BoolFlagZ = z21.string().optional().transform((v) => v === "true" || v === "1");
  }
});

// packages/daemon/src/command-center/search-replace.ts
import { readFileSync as readFileSync21, renameSync as renameSync15, rmSync as rmSync4, statSync as statSync5, writeFileSync as writeFileSync20 } from "node:fs";
import { dirname as dirname12, resolve as pathResolve } from "node:path";
import { z as z22 } from "zod";
function resolveSandboxedPath(sessionDir, relPath) {
  if (!relPath) return null;
  if (relPath.startsWith("/")) return null;
  if (relPath.split("/").some((seg) => seg === "..")) return null;
  const resolved2 = pathResolve(sessionDir, relPath);
  const root = sessionDir.endsWith("/") ? sessionDir : sessionDir + "/";
  if (!(resolved2 === sessionDir || resolved2.startsWith(root))) return null;
  return resolved2;
}
function applyReplacementsToContent(content, replacements, replacement) {
  const lines = content.split("\n");
  const sorted = [...replacements].sort((a, b) => {
    if (a.line !== b.line) return b.line - a.line;
    return b.column - a.column;
  });
  let replaced = 0;
  for (const r of sorted) {
    if (r.line < 1 || r.line > lines.length) return { ok: false, reason: "out_of_bounds" };
    const lineIndex = r.line - 1;
    const original = lines[lineIndex] ?? "";
    if (r.column < 0 || r.column + r.length > original.length) {
      return { ok: false, reason: "out_of_bounds" };
    }
    lines[lineIndex] = original.slice(0, r.column) + replacement + original.slice(r.column + r.length);
    replaced += 1;
  }
  return { ok: true, content: lines.join("\n"), replaced };
}
function writeAtomic2(target, content) {
  const tmp = `${target}.tmp.${process.pid}.${Math.random().toString(36).slice(2, 10)}`;
  try {
    writeFileSync20(tmp, content, { encoding: "utf-8" });
    renameSync15(tmp, target);
  } catch (err) {
    try {
      rmSync4(tmp, { force: true });
    } catch {
    }
    throw err;
  }
}
function executeReplace(sessionDir, body, deps2 = {}) {
  const tolerance = deps2.mtimeToleranceMs ?? DEFAULT_MTIME_TOLERANCE_MS;
  let filesUpdated = 0;
  let matchesReplaced = 0;
  const skipped = [];
  for (const file of body.files) {
    const target = resolveSandboxedPath(sessionDir, file.path);
    if (!target) {
      skipped.push({ path: file.path, reason: "path_escapes_workspace" });
      continue;
    }
    let stat;
    try {
      stat = statSync5(target);
    } catch {
      skipped.push({ path: file.path, reason: "file_not_found" });
      continue;
    }
    if (file.expectedMtimeMs !== void 0) {
      const drift = Math.abs(stat.mtimeMs - file.expectedMtimeMs);
      if (drift > tolerance) {
        skipped.push({
          path: file.path,
          reason: "file_modified_since_search",
          details: `mtime drift ${drift.toFixed(0)}ms`
        });
        continue;
      }
    }
    let original;
    try {
      original = readFileSync21(target, "utf-8");
    } catch (err) {
      skipped.push({
        path: file.path,
        reason: "read_failed",
        details: err instanceof Error ? err.message : String(err)
      });
      continue;
    }
    const applied = applyReplacementsToContent(original, file.replacements, body.replacement);
    if (!applied.ok) {
      skipped.push({ path: file.path, reason: applied.reason });
      continue;
    }
    try {
      const parent = dirname12(target);
      if (!parent) {
        skipped.push({ path: file.path, reason: "write_failed" });
        continue;
      }
      writeAtomic2(target, applied.content);
    } catch (err) {
      skipped.push({
        path: file.path,
        reason: "write_failed",
        details: err instanceof Error ? err.message : String(err)
      });
      continue;
    }
    filesUpdated += 1;
    matchesReplaced += applied.replaced;
  }
  return { filesUpdated, matchesReplaced, skipped };
}
var ReplacementZ, FileTargetZ, ReplaceRequestZ, DEFAULT_MTIME_TOLERANCE_MS;
var init_search_replace = __esm({
  "packages/daemon/src/command-center/search-replace.ts"() {
    "use strict";
    ReplacementZ = z22.object({
      line: z22.number().int().min(1),
      column: z22.number().int().min(0),
      length: z22.number().int().min(0)
    }).strict();
    FileTargetZ = z22.object({
      path: z22.string().min(1),
      expectedMtimeMs: z22.number().int().nonnegative().optional(),
      replacements: z22.array(ReplacementZ).min(1)
    }).strict();
    ReplaceRequestZ = z22.object({
      query: z22.string(),
      regex: z22.boolean().default(false),
      caseMode: z22.enum(["smart", "sensitive", "insensitive"]).default("smart"),
      replacement: z22.string(),
      files: z22.array(FileTargetZ).min(1)
    }).strict();
    DEFAULT_MTIME_TOLERANCE_MS = 10;
  }
});

// packages/daemon/src/command-center/static.ts
import { existsSync as existsSync27, readFileSync as readFileSync22, statSync as statSync6 } from "node:fs";
import { resolve as resolve18, dirname as dirname13, join as join28, extname } from "node:path";
import { fileURLToPath as fileURLToPath3 } from "node:url";
function resolveDashboardOut() {
  const override = process.env.TMUX_IDE_DASHBOARD_OUT;
  if (override) return existsSync27(override) ? override : null;
  const here = dirname13(fileURLToPath3(import.meta.url));
  let current = here;
  let workspaceMatch = null;
  let firstMatch = null;
  for (let i = 0; i < 10; i += 1) {
    const candidate = join28(current, "dashboard", "dist");
    if (existsSync27(candidate)) {
      if (firstMatch === null) firstMatch = candidate;
      if (existsSync27(join28(current, "pnpm-workspace.yaml"))) {
        workspaceMatch = candidate;
        break;
      }
    }
    const parent = resolve18(current, "..");
    if (parent === current) break;
    current = parent;
  }
  return workspaceMatch ?? firstMatch;
}
function readCached(filePath) {
  const cached = fileCache.get(filePath);
  if (cached) return cached;
  if (!existsSync27(filePath) || !statSync6(filePath).isFile()) return null;
  const ext = extname(filePath);
  const mimeType = MIME_TYPES[ext] ?? "application/octet-stream";
  const content = new Uint8Array(readFileSync22(filePath));
  const entry = { content, mimeType };
  fileCache.set(filePath, entry);
  return entry;
}
function cacheControl(_urlPath, mimeType) {
  if (mimeType.startsWith("text/html")) {
    return "no-cache";
  }
  return "public, max-age=31536000, immutable";
}
function serveDashboard() {
  const outDir = resolveDashboardOut();
  if (!outDir) {
    return async (_c, next) => {
      await next();
    };
  }
  return async (c, next) => {
    const path4 = new URL(c.req.url).pathname;
    if (path4.startsWith("/api/") || path4.startsWith("/ws/") || path4 === "/health") {
      await next();
      return;
    }
    const normalized = path4 !== "/" && path4.endsWith("/") ? path4.slice(0, -1) : path4;
    const serve = (file) => new Response(new Uint8Array(file.content), {
      status: 200,
      headers: {
        "Content-Type": file.mimeType,
        "Cache-Control": cacheControl(path4, file.mimeType)
      }
    });
    const exactPath = join28(outDir, normalized);
    const exactFile = readCached(exactPath);
    if (exactFile) return serve(exactFile);
    const rootIndex = join28(outDir, "index.html");
    const rootFile = readCached(rootIndex);
    if (rootFile) return serve(rootFile);
    await next();
  };
}
var MIME_TYPES, fileCache;
var init_static = __esm({
  "packages/daemon/src/command-center/static.ts"() {
    "use strict";
    MIME_TYPES = {
      ".html": "text/html; charset=utf-8",
      ".js": "application/javascript; charset=utf-8",
      ".css": "text/css; charset=utf-8",
      ".json": "application/json; charset=utf-8",
      ".svg": "image/svg+xml",
      ".png": "image/png",
      ".ico": "image/x-icon",
      ".woff2": "font/woff2",
      ".txt": "text/plain; charset=utf-8"
    };
    fileCache = /* @__PURE__ */ new Map();
  }
});

// packages/daemon/src/notes/service.ts
import { existsSync as existsSync28, mkdirSync as mkdirSync21, readFileSync as readFileSync23, renameSync as renameSync16, statSync as statSync7, writeFileSync as writeFileSync21 } from "node:fs";
import { dirname as dirname14, join as join29 } from "node:path";
function notePath(sessionDir) {
  return join29(sessionDir, NOTES_REL_PATH);
}
function readNote(sessionDir) {
  const path4 = notePath(sessionDir);
  if (!existsSync28(path4)) return { content: "", updatedAt: null };
  try {
    const content = readFileSync23(path4, "utf8");
    const stat = statSync7(path4);
    return { content, updatedAt: stat.mtime.toISOString() };
  } catch {
    return { content: "", updatedAt: null };
  }
}
function writeNote(sessionDir, content) {
  const path4 = notePath(sessionDir);
  mkdirSync21(dirname14(path4), { recursive: true });
  const tmp = `${path4}.${process.pid}.${Date.now()}.tmp`;
  writeFileSync21(tmp, content, "utf8");
  renameSync16(tmp, path4);
  const stat = statSync7(path4);
  return { content, updatedAt: stat.mtime.toISOString() };
}
var NOTES_REL_PATH;
var init_service = __esm({
  "packages/daemon/src/notes/service.ts"() {
    "use strict";
    NOTES_REL_PATH = ".tmux-ide/notes.md";
  }
});

// packages/daemon/src/notes/handlers.ts
import { zValidator } from "@hono/zod-validator";
function attachNotesRoutes(app, deps2) {
  app.get("/api/project/:name/notes", (c) => {
    const name = c.req.param("name");
    const session = deps2.resolveSession(name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const record = readNote(session.dir);
    const note = {
      sessionName: name,
      content: record.content,
      updatedAt: record.updatedAt
    };
    return c.json({ note });
  });
  app.put("/api/project/:name/notes", zValidator("json", UpdateNoteRequestSchemaZ), (c) => {
    const name = c.req.param("name");
    const session = deps2.resolveSession(name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = c.req.valid("json");
    const record = writeNote(session.dir, body.content);
    deps2.onChanged?.(name);
    const note = {
      sessionName: name,
      content: record.content,
      updatedAt: record.updatedAt
    };
    return c.json({ note });
  });
}
var init_handlers = __esm({
  "packages/daemon/src/notes/handlers.ts"() {
    "use strict";
    init_src2();
    init_service();
  }
});

// packages/daemon/src/command-center/actions/handlers/_resolve-project.ts
function resolveProject(name, deps2 = {}) {
  const lookup = deps2.getProject ?? getProject;
  const project = lookup(name);
  if (project) {
    return {
      name: project.name,
      dir: project.dir,
      sessionName: project.name,
      fromLiveSession: false
    };
  }
  const hasSession2 = deps2.hasSession ?? hasSession;
  if (hasSession2(name)) {
    const cwd = (deps2.getSessionCwd ?? getSessionCwd)(name);
    if (cwd) {
      return { name, dir: cwd, sessionName: name, fromLiveSession: true };
    }
  }
  throw new ActionError({
    code: "project_not_found",
    message: `Project "${name}" not found in registry or as a live tmux session`,
    details: { name }
  });
}
var init_resolve_project = __esm({
  "packages/daemon/src/command-center/actions/handlers/_resolve-project.ts"() {
    "use strict";
    init_project_registry();
    init_src();
    init_errors3();
  }
});

// packages/daemon/src/command-center/actions/handlers/project-open-terminal.ts
function defaultTerminalTabId(sessionName) {
  return `${TERMINAL_TAB_ID_PREFIX}:${sessionName}:${TERMINAL_TAB_ID_SUFFIX}`;
}
async function projectOpenTerminalHandler(input, deps2 = {}) {
  const project = resolveProject(input.name, deps2);
  const activateProject2 = deps2.activateProject ?? activateProject;
  await activateProject2(project.name);
  try {
    assertValidCwd2(project.dir, deps2.statCwd);
  } catch (err) {
    if (err instanceof TerminalCwdError) {
      throw actionErrorFromCwdError(err);
    }
    throw err;
  }
  const hasSession2 = deps2.hasSession ?? hasSession;
  const launch2 = deps2.launch ?? launch;
  let launched = false;
  if (!hasSession2(project.sessionName)) {
    try {
      await launch2(project.dir, { json: false, attach: false });
      launched = true;
    } catch (err) {
      throw new ActionError({
        code: "launch_failed",
        message: `Failed to launch session "${project.sessionName}": ${err.message ?? String(err)}`,
        details: { sessionName: project.sessionName, dir: project.dir },
        cause: err
      });
    }
  }
  return {
    sessionName: project.sessionName,
    cwd: project.dir,
    terminalTabId: defaultTerminalTabId(project.sessionName),
    launched
  };
}
var TERMINAL_TAB_ID_PREFIX, TERMINAL_TAB_ID_SUFFIX;
var init_project_open_terminal = __esm({
  "packages/daemon/src/command-center/actions/handlers/project-open-terminal.ts"() {
    "use strict";
    init_src();
    init_launch();
    init_active_projects();
    init_pty_bridge();
    init_errors3();
    init_resolve_project();
    TERMINAL_TAB_ID_PREFIX = "terminal";
    TERMINAL_TAB_ID_SUFFIX = "default";
  }
});

// packages/daemon/src/command-center/actions/handlers/project-activate.ts
async function projectActivateHandler(input, deps2 = {}) {
  const project = resolveProject(input.name, deps2);
  const activateProject2 = deps2.activateProject ?? activateProject;
  try {
    await activateProject2(project.name, { orchestrate: input.orchestrate ?? false });
  } catch (err) {
    throw new ActionError({
      code: "internal",
      message: `Failed to activate project "${project.name}": ${err.message ?? String(err)}`,
      details: { projectName: project.name },
      cause: err
    });
  }
  return { active: true, projectName: project.name };
}
var init_project_activate = __esm({
  "packages/daemon/src/command-center/actions/handlers/project-activate.ts"() {
    "use strict";
    init_active_projects();
    init_errors3();
    init_resolve_project();
  }
});

// packages/daemon/src/command-center/actions/handlers/project-launch.ts
function ensureWorkspaceRegistered(name, sessionName, dir) {
  const reg = getDefaultWorkspaceRegistry();
  if (reg.has(name)) return;
  try {
    reg.add({ name, sessionName, projectDir: dir });
  } catch {
  }
}
async function projectLaunchHandler(input, deps2 = {}) {
  const project = resolveProject(input.name, deps2);
  const hasSession2 = deps2.hasSession ?? hasSession;
  if (hasSession2(project.sessionName)) {
    ensureWorkspaceRegistered(project.name, project.sessionName, project.dir);
    return { sessionName: project.sessionName, started: false };
  }
  const launch2 = deps2.launch ?? launch;
  try {
    await launch2(project.dir, { json: false, attach: false });
  } catch (err) {
    throw new ActionError({
      code: "launch_failed",
      message: `Failed to launch session "${project.sessionName}": ${err.message ?? String(err)}`,
      details: { sessionName: project.sessionName, dir: project.dir },
      cause: err
    });
  }
  ensureWorkspaceRegistered(project.name, project.sessionName, project.dir);
  return { sessionName: project.sessionName, started: true };
}
var init_project_launch = __esm({
  "packages/daemon/src/command-center/actions/handlers/project-launch.ts"() {
    "use strict";
    init_src();
    init_launch();
    init_errors3();
    init_resolve_project();
    init_workspace_registry();
  }
});

// packages/daemon/src/command-center/actions/handlers/project-stop.ts
function defaultKillOrphanDaemons(_session) {
}
async function projectStopHandler(input, deps2 = {}) {
  const project = resolveProject(input.name, deps2);
  const hasSession2 = deps2.hasSession ?? hasSession;
  if (!hasSession2(project.sessionName)) {
    return { sessionName: project.sessionName, stopped: false };
  }
  const stopSessionMonitor2 = deps2.stopSessionMonitor ?? stopSessionMonitor;
  const killSession2 = deps2.killSession ?? killSession;
  const killOrphanDaemons = deps2.killOrphanDaemons ?? defaultKillOrphanDaemons;
  try {
    stopSessionMonitor2(project.sessionName);
    killOrphanDaemons(project.sessionName);
    const result = killSession2(project.sessionName);
    return { sessionName: project.sessionName, stopped: result.stopped };
  } catch (err) {
    throw new ActionError({
      code: "stop_failed",
      message: `Failed to stop session "${project.sessionName}": ${err.message ?? String(err)}`,
      details: { sessionName: project.sessionName },
      cause: err
    });
  }
}
var init_project_stop = __esm({
  "packages/daemon/src/command-center/actions/handlers/project-stop.ts"() {
    "use strict";
    init_src();
    init_errors3();
    init_resolve_project();
  }
});

// packages/daemon/src/restart.ts
import { resolve as resolve19 } from "node:path";
async function restart(targetDir, { json: json2, attach: attach2 } = {}) {
  const dir = resolve19(targetDir ?? ".");
  const { name: session } = getSessionName(dir);
  stopSessionMonitor(session);
  const result = killSession(session);
  if (result.stopped) {
    console.log(`Stopped session "${session}"`);
  }
  await launch(dir, { json: json2, attach: attach2 });
}
var init_restart = __esm({
  "packages/daemon/src/restart.ts"() {
    "use strict";
    init_yaml_io();
    init_launch();
    init_src();
  }
});

// packages/daemon/src/command-center/actions/handlers/project-restart.ts
async function projectRestartHandler(input, deps2 = {}) {
  const project = resolveProject(input.name, deps2);
  const restart2 = deps2.restart ?? restart;
  try {
    await restart2(project.dir, { json: false, attach: false });
  } catch (err) {
    throw new ActionError({
      code: "launch_failed",
      message: `Failed to restart session "${project.sessionName}": ${err.message ?? String(err)}`,
      details: { sessionName: project.sessionName, dir: project.dir },
      cause: err
    });
  }
  return { sessionName: project.sessionName, restarted: true };
}
var init_project_restart = __esm({
  "packages/daemon/src/command-center/actions/handlers/project-restart.ts"() {
    "use strict";
    init_restart();
    init_errors3();
    init_resolve_project();
  }
});

// packages/daemon/src/command-center/actions/handlers/terminal-respawn.ts
function terminalRespawnHandler(input, deps2 = {}) {
  const registry = deps2.registry ?? defaultPtyBridgeRegistry;
  const bridge = registry.peek(input.terminalId);
  if (!bridge) {
    throw new ActionError({
      code: "terminal_not_found",
      message: `No running terminal bridge for id "${input.terminalId}"`,
      details: { terminalId: input.terminalId, sessionName: input.sessionName }
    });
  }
  if (!bridge.restartWith) {
    throw new ActionError({
      code: "internal",
      message: "Bridge does not support restartWith",
      details: { terminalId: input.terminalId }
    });
  }
  const cwd = resolveRespawnCwd(input, bridge, deps2.statCwd);
  const cols = deps2.cols ?? bridge.cols ?? DEFAULT_RESPAWN_COLS;
  const rows = deps2.rows ?? bridge.rows ?? DEFAULT_RESPAWN_ROWS;
  try {
    bridge.restartWith(cols, rows, { cwd });
  } catch (err) {
    if (err instanceof TerminalCwdError) {
      throw actionErrorFromCwdError(err);
    }
    throw new ActionError({
      code: "internal",
      message: `Failed to respawn terminal "${input.terminalId}": ${err.message ?? String(err)}`,
      details: { terminalId: input.terminalId },
      cause: err
    });
  }
  return { respawned: true, cwd };
}
function resolveRespawnCwd(input, bridge, statCwd) {
  if (input.cwd) {
    try {
      assertValidCwd2(input.cwd, statCwd);
    } catch (err) {
      if (err instanceof TerminalCwdError) {
        throw actionErrorFromCwdError(err);
      }
      throw err;
    }
    return input.cwd;
  }
  const last = bridge.getCwd?.() ?? null;
  if (!last) {
    throw new ActionError({
      code: "internal",
      message: "Cannot respawn terminal without an explicit cwd: bridge has no recorded cwd",
      details: { terminalId: input.terminalId }
    });
  }
  try {
    assertValidCwd2(last, statCwd);
  } catch (err) {
    if (err instanceof TerminalCwdError) {
      throw actionErrorFromCwdError(err);
    }
    throw err;
  }
  return last;
}
var DEFAULT_RESPAWN_COLS, DEFAULT_RESPAWN_ROWS;
var init_terminal_respawn = __esm({
  "packages/daemon/src/command-center/actions/handlers/terminal-respawn.ts"() {
    "use strict";
    init_ws_route();
    init_pty_bridge();
    init_errors3();
    DEFAULT_RESPAWN_COLS = 80;
    DEFAULT_RESPAWN_ROWS = 24;
  }
});

// packages/daemon/src/command-center/actions/handlers/terminal-stop.ts
function terminalStopHandler(input, deps2 = {}) {
  const registry = deps2.registry ?? defaultPtyBridgeRegistry;
  const ok = registry.delete(input.terminalId);
  if (!ok) {
    throw new ActionError({
      code: "terminal_not_found",
      message: `No terminal bridge for id "${input.terminalId}"`,
      details: { terminalId: input.terminalId, sessionName: input.sessionName }
    });
  }
  return { stopped: true };
}
var init_terminal_stop = __esm({
  "packages/daemon/src/command-center/actions/handlers/terminal-stop.ts"() {
    "use strict";
    init_ws_route();
    init_errors3();
  }
});

// packages/daemon/src/command-center/actions/handlers/task-system.ts
import { resolve as resolve20 } from "node:path";
function resolveActionDir(input, deps2 = {}) {
  if (deps2.dir) return deps2.dir;
  const name = input.name ?? input.sessionName;
  if (!name) return resolve20(".");
  const sessions = (deps2.discoverSessions ?? discoverSessions)();
  const session = sessions.find((candidate) => candidate.name === name);
  if (!session) {
    throw new ActionError({
      code: "project_not_found",
      message: `Project "${name}" not found`,
      details: { name }
    });
  }
  return session.dir;
}
function mapTaskActionError(err) {
  if (err instanceof TaskActionError) {
    throw new ActionError({
      code: err.code,
      message: err.message,
      details: err.details,
      cause: err
    });
  }
  throw err;
}
function runMutation(input, deps2, fn) {
  try {
    return fn(resolveActionDir(input, deps2));
  } catch (err) {
    mapTaskActionError(err);
  }
}
function taskCreateHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => createTaskRecord(dir, {
      title: input.title,
      description: input.description,
      goalId: input.goalId,
      priority: input.priority,
      assign: input.assign,
      tags: input.tags,
      depends: input.depends,
      milestone: input.milestone,
      specialty: input.specialty,
      fulfills: input.fulfills
    })
  );
}
function taskUpdateHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => updateTaskRecord(dir, {
      taskId: input.taskId,
      status: input.status,
      proof: input.proof,
      title: input.title,
      description: input.description,
      priority: input.priority,
      assign: input.assign,
      assignee: input.assignee,
      goalId: input.goalId,
      tags: input.tags,
      depends: input.depends,
      milestone: input.milestone,
      specialty: input.specialty,
      fulfills: input.fulfills,
      summary: input.summary
    })
  );
}
function taskClaimHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => claimTaskRecord(dir, input.taskId, input.assign));
}
function taskDoneHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => doneTaskRecord(dir, input.taskId, input.proof));
}
function taskDeleteHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => deleteTaskRecord(dir, input.taskId));
}
function goalCreateHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => createGoalRecord(dir, {
      title: input.title,
      priority: input.priority,
      acceptance: input.acceptance,
      description: input.description,
      milestone: input.milestone,
      specialty: input.specialty
    })
  );
}
function goalUpdateHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => updateGoalRecord(dir, {
      goalId: input.goalId,
      status: input.status,
      title: input.title,
      description: input.description,
      acceptance: input.acceptance,
      priority: input.priority,
      milestone: input.milestone,
      specialty: input.specialty,
      assign: input.assign
    })
  );
}
function goalDoneHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => doneGoalRecord(dir, input.goalId));
}
function goalDeleteHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => deleteGoalRecord(dir, input.goalId));
}
function milestoneCreateHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => createMilestoneRecord(dir, {
      title: input.title,
      sequence: input.sequence,
      description: input.description
    })
  );
}
function milestoneUpdateHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => updateMilestoneRecord(dir, { milestoneId: input.milestoneId, status: input.status })
  );
}
function missionSetHandler(input, deps2 = {}) {
  return runMutation(
    input,
    deps2,
    (dir) => setMissionRecord(dir, { title: input.title, description: input.description })
  );
}
function missionPlanCompleteHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => {
    const { mission } = completeMissionPlanRecord(dir);
    return { mission };
  });
}
function missionClearHandler(input, deps2 = {}) {
  return runMutation(input, deps2, (dir) => clearMissionRecord(dir));
}
var init_task_system = __esm({
  "packages/daemon/src/command-center/actions/handlers/task-system.ts"() {
    "use strict";
    init_discovery();
    init_task_actions();
    init_errors3();
  }
});

// packages/daemon/src/command-center/actions/handlers/_project-context.ts
import { basename as basename8 } from "node:path";
function resolveProjectContext(input, deps2 = {}) {
  if (input.projectName) {
    const project = resolveProject(input.projectName, deps2);
    return { dir: project.dir, sessionName: project.sessionName };
  }
  const dir = deps2.cwd ?? process.cwd();
  const sessionName = getSessionName(dir).name || basename8(dir);
  return { dir, sessionName };
}
var init_project_context = __esm({
  "packages/daemon/src/command-center/actions/handlers/_project-context.ts"() {
    "use strict";
    init_yaml_io();
    init_resolve_project();
  }
});

// packages/daemon/src/command-center/actions/handlers/skill-actions.ts
function broadcast(sessionName, deps2) {
  (deps2.broadcastSkillsChanged ?? broadcastSkillsChanged)(sessionName);
}
function skillError(err) {
  return new ActionError({
    code: "skill_invalid",
    message: err.message ?? String(err),
    cause: err
  });
}
function skillCreateHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  try {
    const skill = saveSkill(context.dir, input.name, input.content);
    broadcast(context.sessionName, deps2);
    return { skill };
  } catch (err) {
    throw skillError(err);
  }
}
function skillUpdateHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  if (!loadSkill(context.dir, input.name)) {
    throw new ActionError({
      code: "skill_not_found",
      message: `Skill "${input.name}" not found`,
      details: { name: input.name }
    });
  }
  try {
    const skill = saveSkill(context.dir, input.name, input.content);
    broadcast(context.sessionName, deps2);
    return { skill };
  } catch (err) {
    throw skillError(err);
  }
}
function skillDeleteHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  const deleted = deleteSkill(context.dir, input.name);
  if (!deleted) {
    throw new ActionError({
      code: "skill_not_found",
      message: `Skill "${input.name}" not found`,
      details: { name: input.name }
    });
  }
  broadcast(context.sessionName, deps2);
  return { deleted: true };
}
var init_skill_actions = __esm({
  "packages/daemon/src/command-center/actions/handlers/skill-actions.ts"() {
    "use strict";
    init_skill_registry();
    init_ws_events();
    init_errors3();
    init_project_context();
  }
});

// packages/daemon/src/command-center/actions/handlers/config-actions.ts
import { existsSync as existsSync29 } from "node:fs";
import { join as join30 } from "node:path";
function mutateConfigAction(input, deps2, fn) {
  const context = resolveProjectContext(input, deps2);
  if (!existsSync29(join30(context.dir, "ide.yml"))) {
    throw new ActionError({
      code: "ide_yml_missing",
      message: "ide.yml was not found",
      details: { dir: context.dir }
    });
  }
  try {
    const result = fn(context.dir);
    (deps2.broadcastConfigChanged ?? broadcastConfigChanged)(context.sessionName);
    return result;
  } catch (err) {
    const message = err.message ?? String(err);
    throw new ActionError({
      code: message.toLowerCase().includes("path") ? "config_path_invalid" : "config_validation_failed",
      message,
      cause: err
    });
  }
}
function configSetHandler(input, deps2 = {}) {
  const config2 = mutateConfigAction(
    input,
    deps2,
    (dir) => configSetValue(dir, input.path, input.value)
  );
  return { config: config2 };
}
function configAddPaneHandler(input, deps2 = {}) {
  const pane = {
    title: input.title,
    command: input.command,
    type: input.type,
    target: input.target,
    dir: input.dir,
    size: input.size,
    focus: input.focus,
    env: input.env,
    role: input.role,
    task: input.task,
    specialty: input.specialty,
    skill: input.skill
  };
  const config2 = mutateConfigAction(input, deps2, (dir) => configAddPane(dir, input.rowIndex, pane));
  return { config: config2 };
}
function configRemovePaneHandler(input, deps2 = {}) {
  const config2 = mutateConfigAction(
    input,
    deps2,
    (dir) => configRemovePane(dir, input.rowIndex, input.paneIndex)
  ).config;
  return { config: config2 };
}
function configAddRowHandler(input, deps2 = {}) {
  const config2 = mutateConfigAction(input, deps2, (dir) => configAddRow(dir, input.size));
  return { config: config2 };
}
function configEnableTeamHandler(input, deps2 = {}) {
  const config2 = mutateConfigAction(input, deps2, (dir) => configEnableTeam(dir, input.name));
  return { config: config2 };
}
function configDisableTeamHandler(input, deps2 = {}) {
  const config2 = mutateConfigAction(input, deps2, (dir) => configDisableTeam(dir));
  return { config: config2 };
}
var init_config_actions = __esm({
  "packages/daemon/src/command-center/actions/handlers/config-actions.ts"() {
    "use strict";
    init_config();
    init_ws_events();
    init_errors3();
    init_project_context();
  }
});

// packages/daemon/src/command-center/actions/handlers/validation-actions.ts
function validationAssertHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  try {
    const assertion = assertValidationStatus(
      context.dir,
      input.assertId,
      input.status,
      input.evidence
    );
    (deps2.broadcastValidationChanged ?? broadcastValidationChanged)(context.sessionName);
    return { assertion };
  } catch (err) {
    throw new ActionError({
      code: "validation_assertion_not_found",
      message: err.message ?? String(err),
      details: { assertId: input.assertId },
      cause: err
    });
  }
}
function validationReportHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  return { report: validationReport(context.dir) };
}
var init_validation_actions = __esm({
  "packages/daemon/src/command-center/actions/handlers/validation-actions.ts"() {
    "use strict";
    init_validation();
    init_ws_events();
    init_errors3();
    init_project_context();
  }
});

// packages/daemon/src/command-center/actions/handlers/webhook-actions.ts
import { existsSync as existsSync30 } from "node:fs";
import { join as join31 } from "node:path";
function assertIdeYml(dir) {
  if (!existsSync30(join31(dir, "ide.yml"))) {
    throw new ActionError({
      code: "ide_yml_missing",
      message: "ide.yml was not found",
      details: { dir }
    });
  }
}
function webhookAddHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  assertIdeYml(context.dir);
  const { webhookId, ...webhook } = addWebhookConfig(context.dir, {
    url: input.url,
    events: input.events,
    secret: input.secret
  });
  (deps2.broadcastConfigChanged ?? broadcastConfigChanged)(context.sessionName);
  return { webhookId, webhook };
}
function webhookRemoveHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  assertIdeYml(context.dir);
  if (!removeWebhookConfig(context.dir, input.webhookId)) {
    throw new ActionError({
      code: "webhook_not_found",
      message: `Webhook "${input.webhookId}" not found`,
      details: { webhookId: input.webhookId }
    });
  }
  (deps2.broadcastConfigChanged ?? broadcastConfigChanged)(context.sessionName);
  return { deleted: true };
}
async function webhookTestHandler(input, deps2 = {}) {
  const context = resolveProjectContext(input, deps2);
  assertIdeYml(context.dir);
  const webhook = getWebhookConfig(context.dir, input.webhookId);
  if (!webhook) {
    throw new ActionError({
      code: "webhook_not_found",
      message: `Webhook "${input.webhookId}" not found`,
      details: { webhookId: input.webhookId }
    });
  }
  try {
    const result = await testWebhookConfig(webhook, deps2.fetch);
    if (!result.ok) {
      throw new ActionError({
        code: "webhook_test_failed",
        message: `Webhook test failed with HTTP ${result.status}`,
        details: { webhookId: input.webhookId, status: result.status }
      });
    }
    return { status: result.status, ok: true };
  } catch (err) {
    if (err instanceof ActionError) throw err;
    throw new ActionError({
      code: "webhook_test_failed",
      message: `Webhook test failed: ${err.message ?? String(err)}`,
      details: { webhookId: input.webhookId },
      cause: err
    });
  }
}
var init_webhook_actions = __esm({
  "packages/daemon/src/command-center/actions/handlers/webhook-actions.ts"() {
    "use strict";
    init_webhook();
    init_ws_events();
    init_errors3();
    init_project_context();
  }
});

// packages/daemon/src/command-center/actions/handlers/agent-actions.ts
function registryFrom(deps2) {
  return deps2.registry ?? getDefaultExternalAgentRegistry();
}
function agentRegisterHandler(input, deps2 = {}) {
  registryFrom(deps2).register(input);
  return { ok: true };
}
function agentHeartbeatHandler(input, deps2 = {}) {
  const known = registryFrom(deps2).heartbeat(input);
  return { ok: true, known };
}
function agentUnregisterHandler(input, deps2 = {}) {
  const removed = registryFrom(deps2).unregister(input.id);
  return { ok: true, removed };
}
var init_agent_actions = __esm({
  "packages/daemon/src/command-center/actions/handlers/agent-actions.ts"() {
    "use strict";
    init_agent_registry();
  }
});

// packages/daemon/src/chat/context-actions.ts
import { execFile as execFile6 } from "node:child_process";
import { promisify as promisify2 } from "node:util";
function badRequest2(message, details) {
  return new ActionError({ code: "bad_request", message, details });
}
async function tmux2(args, deps2, input) {
  const exec = deps2.execFile ?? execFileAsync2;
  try {
    const result = await exec("tmux", args, {
      encoding: "utf8",
      maxBuffer: CAPTURE_MAX_BUFFER
    });
    return result.stdout;
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    throw badRequest2(`Unable to capture tmux pane "${input.paneId}"`, {
      sessionName: input.sessionName,
      paneId: input.paneId,
      reason: message
    });
  }
}
async function chatContextCaptureTerminalHandler(input, deps2 = {}) {
  const target = `${input.sessionName}:${input.paneId}`;
  const title = (await tmux2(["display-message", "-p", "-t", target, "#{pane_title}"], deps2, input)).trim();
  const content = await tmux2(
    ["capture-pane", "-t", target, "-p", "-e", "-S", "-5000"],
    deps2,
    input
  );
  return {
    pane: { id: input.paneId, title: title || input.paneId },
    content,
    capturedAt: (deps2.now?.() ?? /* @__PURE__ */ new Date()).toISOString()
  };
}
var execFileAsync2, CAPTURE_MAX_BUFFER;
var init_context_actions = __esm({
  "packages/daemon/src/chat/context-actions.ts"() {
    "use strict";
    init_errors3();
    execFileAsync2 = promisify2(execFile6);
    CAPTURE_MAX_BUFFER = 10 * 1024 * 1024;
  }
});

// packages/daemon/src/command-center/actions/registry.ts
function getLooseActionEntry(name) {
  return actionRegistry[name];
}
var actionRegistry;
var init_registry2 = __esm({
  "packages/daemon/src/command-center/actions/registry.ts"() {
    "use strict";
    init_contract();
    init_project_open_terminal();
    init_project_activate();
    init_project_launch();
    init_project_stop();
    init_project_restart();
    init_terminal_respawn();
    init_terminal_stop();
    init_task_system();
    init_skill_actions();
    init_config_actions();
    init_validation_actions();
    init_webhook_actions();
    init_app_set_remote_access();
    init_daemon_shutdown();
    init_agent_actions();
    init_context_actions();
    init_chat_actions();
    actionRegistry = {
      "project.openTerminal": {
        inputSchema: ActionContractsZ["project.openTerminal"].input,
        resultSchema: ActionContractsZ["project.openTerminal"].result,
        handler: projectOpenTerminalHandler
      },
      "project.launch": {
        inputSchema: ActionContractsZ["project.launch"].input,
        resultSchema: ActionContractsZ["project.launch"].result,
        handler: projectLaunchHandler
      },
      "project.stop": {
        inputSchema: ActionContractsZ["project.stop"].input,
        resultSchema: ActionContractsZ["project.stop"].result,
        handler: projectStopHandler
      },
      "project.restart": {
        inputSchema: ActionContractsZ["project.restart"].input,
        resultSchema: ActionContractsZ["project.restart"].result,
        handler: projectRestartHandler
      },
      "project.activate": {
        inputSchema: ActionContractsZ["project.activate"].input,
        resultSchema: ActionContractsZ["project.activate"].result,
        handler: projectActivateHandler
      },
      "terminal.respawn": {
        inputSchema: ActionContractsZ["terminal.respawn"].input,
        resultSchema: ActionContractsZ["terminal.respawn"].result,
        handler: terminalRespawnHandler
      },
      "terminal.stop": {
        inputSchema: ActionContractsZ["terminal.stop"].input,
        resultSchema: ActionContractsZ["terminal.stop"].result,
        handler: terminalStopHandler
      },
      "task.create": {
        inputSchema: ActionContractsZ["task.create"].input,
        resultSchema: ActionContractsZ["task.create"].result,
        handler: taskCreateHandler
      },
      "task.update": {
        inputSchema: ActionContractsZ["task.update"].input,
        resultSchema: ActionContractsZ["task.update"].result,
        handler: taskUpdateHandler
      },
      "task.claim": {
        inputSchema: ActionContractsZ["task.claim"].input,
        resultSchema: ActionContractsZ["task.claim"].result,
        handler: taskClaimHandler
      },
      "task.done": {
        inputSchema: ActionContractsZ["task.done"].input,
        resultSchema: ActionContractsZ["task.done"].result,
        handler: taskDoneHandler
      },
      "task.delete": {
        inputSchema: ActionContractsZ["task.delete"].input,
        resultSchema: ActionContractsZ["task.delete"].result,
        handler: taskDeleteHandler
      },
      "goal.create": {
        inputSchema: ActionContractsZ["goal.create"].input,
        resultSchema: ActionContractsZ["goal.create"].result,
        handler: goalCreateHandler
      },
      "goal.update": {
        inputSchema: ActionContractsZ["goal.update"].input,
        resultSchema: ActionContractsZ["goal.update"].result,
        handler: goalUpdateHandler
      },
      "goal.done": {
        inputSchema: ActionContractsZ["goal.done"].input,
        resultSchema: ActionContractsZ["goal.done"].result,
        handler: goalDoneHandler
      },
      "goal.delete": {
        inputSchema: ActionContractsZ["goal.delete"].input,
        resultSchema: ActionContractsZ["goal.delete"].result,
        handler: goalDeleteHandler
      },
      "milestone.create": {
        inputSchema: ActionContractsZ["milestone.create"].input,
        resultSchema: ActionContractsZ["milestone.create"].result,
        handler: milestoneCreateHandler
      },
      "milestone.update": {
        inputSchema: ActionContractsZ["milestone.update"].input,
        resultSchema: ActionContractsZ["milestone.update"].result,
        handler: milestoneUpdateHandler
      },
      "mission.set": {
        inputSchema: ActionContractsZ["mission.set"].input,
        resultSchema: ActionContractsZ["mission.set"].result,
        handler: missionSetHandler
      },
      "mission.planComplete": {
        inputSchema: ActionContractsZ["mission.planComplete"].input,
        resultSchema: ActionContractsZ["mission.planComplete"].result,
        handler: missionPlanCompleteHandler
      },
      "mission.clear": {
        inputSchema: ActionContractsZ["mission.clear"].input,
        resultSchema: ActionContractsZ["mission.clear"].result,
        handler: missionClearHandler
      },
      "skill.create": {
        inputSchema: ActionContractsZ["skill.create"].input,
        resultSchema: ActionContractsZ["skill.create"].result,
        handler: skillCreateHandler
      },
      "skill.update": {
        inputSchema: ActionContractsZ["skill.update"].input,
        resultSchema: ActionContractsZ["skill.update"].result,
        handler: skillUpdateHandler
      },
      "skill.delete": {
        inputSchema: ActionContractsZ["skill.delete"].input,
        resultSchema: ActionContractsZ["skill.delete"].result,
        handler: skillDeleteHandler
      },
      "config.set": {
        inputSchema: ActionContractsZ["config.set"].input,
        resultSchema: ActionContractsZ["config.set"].result,
        handler: configSetHandler
      },
      "config.addPane": {
        inputSchema: ActionContractsZ["config.addPane"].input,
        resultSchema: ActionContractsZ["config.addPane"].result,
        handler: configAddPaneHandler
      },
      "config.removePane": {
        inputSchema: ActionContractsZ["config.removePane"].input,
        resultSchema: ActionContractsZ["config.removePane"].result,
        handler: configRemovePaneHandler
      },
      "config.addRow": {
        inputSchema: ActionContractsZ["config.addRow"].input,
        resultSchema: ActionContractsZ["config.addRow"].result,
        handler: configAddRowHandler
      },
      "config.enableTeam": {
        inputSchema: ActionContractsZ["config.enableTeam"].input,
        resultSchema: ActionContractsZ["config.enableTeam"].result,
        handler: configEnableTeamHandler
      },
      "config.disableTeam": {
        inputSchema: ActionContractsZ["config.disableTeam"].input,
        resultSchema: ActionContractsZ["config.disableTeam"].result,
        handler: configDisableTeamHandler
      },
      "validation.assert": {
        inputSchema: ActionContractsZ["validation.assert"].input,
        resultSchema: ActionContractsZ["validation.assert"].result,
        handler: validationAssertHandler
      },
      "validation.report": {
        inputSchema: ActionContractsZ["validation.report"].input,
        resultSchema: ActionContractsZ["validation.report"].result,
        handler: validationReportHandler
      },
      "webhook.add": {
        inputSchema: ActionContractsZ["webhook.add"].input,
        resultSchema: ActionContractsZ["webhook.add"].result,
        handler: webhookAddHandler
      },
      "webhook.remove": {
        inputSchema: ActionContractsZ["webhook.remove"].input,
        resultSchema: ActionContractsZ["webhook.remove"].result,
        handler: webhookRemoveHandler
      },
      "webhook.test": {
        inputSchema: ActionContractsZ["webhook.test"].input,
        resultSchema: ActionContractsZ["webhook.test"].result,
        handler: webhookTestHandler
      },
      "app.setRemoteAccess": {
        inputSchema: ActionContractsZ["app.setRemoteAccess"].input,
        resultSchema: ActionContractsZ["app.setRemoteAccess"].result,
        handler: appSetRemoteAccessHandler
      },
      "daemon.shutdown": {
        inputSchema: ActionContractsZ["daemon.shutdown"].input,
        resultSchema: ActionContractsZ["daemon.shutdown"].result,
        handler: daemonShutdownHandler
      },
      "agent.register": {
        inputSchema: ActionContractsZ["agent.register"].input,
        resultSchema: ActionContractsZ["agent.register"].result,
        handler: agentRegisterHandler
      },
      "agent.heartbeat": {
        inputSchema: ActionContractsZ["agent.heartbeat"].input,
        resultSchema: ActionContractsZ["agent.heartbeat"].result,
        handler: agentHeartbeatHandler
      },
      "agent.unregister": {
        inputSchema: ActionContractsZ["agent.unregister"].input,
        resultSchema: ActionContractsZ["agent.unregister"].result,
        handler: agentUnregisterHandler
      },
      "chat.thread.list": {
        inputSchema: ActionContractsZ["chat.thread.list"].input,
        resultSchema: ActionContractsZ["chat.thread.list"].result,
        handler: chatThreadListHandler
      },
      "chat.providers.list": {
        inputSchema: ActionContractsZ["chat.providers.list"].input,
        resultSchema: ActionContractsZ["chat.providers.list"].result,
        handler: chatProvidersListHandler
      },
      "chat.thread.create": {
        inputSchema: ActionContractsZ["chat.thread.create"].input,
        resultSchema: ActionContractsZ["chat.thread.create"].result,
        handler: chatThreadCreateHandler
      },
      "chat.thread.delete": {
        inputSchema: ActionContractsZ["chat.thread.delete"].input,
        resultSchema: ActionContractsZ["chat.thread.delete"].result,
        handler: chatThreadDeleteHandler
      },
      "chat.thread.rename": {
        inputSchema: ActionContractsZ["chat.thread.rename"].input,
        resultSchema: ActionContractsZ["chat.thread.rename"].result,
        handler: chatThreadRenameHandler
      },
      "chat.thread.setProvider": {
        inputSchema: ActionContractsZ["chat.thread.setProvider"].input,
        resultSchema: ActionContractsZ["chat.thread.setProvider"].result,
        handler: chatThreadSetProviderHandler
      },
      "chat.thread.get": {
        inputSchema: ActionContractsZ["chat.thread.get"].input,
        resultSchema: ActionContractsZ["chat.thread.get"].result,
        handler: chatThreadGetHandler
      },
      "chat.thread.usage": {
        inputSchema: ActionContractsZ["chat.thread.usage"].input,
        resultSchema: ActionContractsZ["chat.thread.usage"].result,
        handler: chatThreadUsageHandler
      },
      "chat.session.send": {
        inputSchema: ActionContractsZ["chat.session.send"].input,
        resultSchema: ActionContractsZ["chat.session.send"].result,
        handler: chatSessionSendHandler
      },
      "chat.session.cancel": {
        inputSchema: ActionContractsZ["chat.session.cancel"].input,
        resultSchema: ActionContractsZ["chat.session.cancel"].result,
        handler: chatSessionCancelHandler
      },
      "chat.session.editFromTurn": {
        inputSchema: ActionContractsZ["chat.session.editFromTurn"].input,
        resultSchema: ActionContractsZ["chat.session.editFromTurn"].result,
        handler: chatSessionEditFromTurnHandler
      },
      "chat.permission.respond": {
        inputSchema: ActionContractsZ["chat.permission.respond"].input,
        resultSchema: ActionContractsZ["chat.permission.respond"].result,
        handler: chatPermissionRespondHandler
      },
      "chat.context.captureTerminal": {
        inputSchema: ActionContractsZ["chat.context.captureTerminal"].input,
        resultSchema: ActionContractsZ["chat.context.captureTerminal"].result,
        handler: chatContextCaptureTerminalHandler
      }
    };
  }
});

// packages/daemon/src/command-center/actions/dispatcher.ts
function errorEnvelope(err) {
  return { ok: false, error: err.toEnvelope() };
}
function zodErrorEnvelope(err) {
  return {
    ok: false,
    error: {
      code: "validation_failed",
      message: "Input failed schema validation",
      details: { issues: err.issues }
    }
  };
}
function outputZodErrorEnvelope(err) {
  console.error("[actions] handler output failed schema validation", err.issues);
  return {
    ok: false,
    error: {
      code: "internal",
      message: "Handler returned an invalid result",
      details: { issues: err.issues }
    }
  };
}
function createActionDispatcher(deps2 = {}) {
  const broadcast2 = deps2.broadcast ?? broadcastActionComplete;
  return async function dispatcher(c) {
    const name = c.req.param("name");
    if (!name || !isActionName(name)) {
      return c.json(
        {
          ok: false,
          error: {
            code: "validation_failed",
            message: `Unknown action: ${name}`,
            details: { name }
          }
        },
        404
      );
    }
    let body;
    try {
      body = await c.req.json();
    } catch (err) {
      return c.json(
        {
          ok: false,
          error: {
            code: "validation_failed",
            message: `Invalid JSON body: ${err.message ?? String(err)}`
          }
        },
        400
      );
    }
    const actionName = name;
    const entry = getLooseActionEntry(actionName);
    const inputParsed = entry.inputSchema.safeParse(body);
    if (!inputParsed.success) {
      return c.json(zodErrorEnvelope(inputParsed.error), 200);
    }
    let result;
    try {
      result = await entry.handler(inputParsed.data);
    } catch (err) {
      const wrapped = wrapInternalError(err);
      return c.json(errorEnvelope(wrapped), 200);
    }
    const outputParsed = entry.resultSchema.safeParse(result);
    if (!outputParsed.success) {
      return c.json(outputZodErrorEnvelope(outputParsed.error), 200);
    }
    try {
      broadcast2(actionName, outputParsed.data);
    } catch (err) {
      console.error("[actions] broadcast failed:", err);
    }
    return c.json({ ok: true, result: outputParsed.data }, 200);
  };
}
var init_dispatcher = __esm({
  "packages/daemon/src/command-center/actions/dispatcher.ts"() {
    "use strict";
    init_contract();
    init_errors3();
    init_registry2();
    init_ws_events();
  }
});

// packages/daemon/src/lib/project-init-runner.ts
import { spawn as spawn8 } from "node:child_process";
function lineStreamer(onChunk) {
  let pending = "";
  return {
    push(text) {
      pending += text;
      let newline = pending.indexOf("\n");
      while (newline >= 0) {
        const line = pending.slice(0, newline).replace(/\r$/, "");
        pending = pending.slice(newline + 1);
        onChunk(line);
        newline = pending.indexOf("\n");
      }
    },
    flush() {
      if (pending.length === 0) return;
      onChunk(pending.replace(/\r$/, ""));
      pending = "";
    }
  };
}
async function runInit(options) {
  const spawnFn = options.spawnFn ?? spawn8;
  const command2 = options.command ?? "tmux-ide";
  const timeoutMs = options.timeoutMs ?? DEFAULT_TIMEOUT_MS;
  const args = ["init"];
  if (options.template) args.push("--template", options.template);
  const child = spawnFn(command2, args, { cwd: options.cwd });
  const stderrStreamer = lineStreamer(options.onChunk);
  const stdoutStreamer = lineStreamer(options.onChunk);
  let stderrBuffer = "";
  child.stdout.setEncoding?.("utf-8");
  child.stderr.setEncoding?.("utf-8");
  child.stdout.on("data", (data) => {
    stdoutStreamer.push(typeof data === "string" ? data : data.toString("utf-8"));
  });
  child.stderr.on("data", (data) => {
    const text = typeof data === "string" ? data : data.toString("utf-8");
    stderrBuffer += text;
    stderrStreamer.push(text);
  });
  return new Promise((resolveResult, reject) => {
    let timer = null;
    let settled = false;
    const settle = (fn) => {
      if (settled) return;
      settled = true;
      if (timer) clearTimeout(timer);
      stdoutStreamer.flush();
      stderrStreamer.flush();
      fn();
    };
    timer = setTimeout(() => {
      try {
        child.kill("SIGKILL");
      } catch {
      }
      settle(() => reject(new ProjectInitTimeoutError(timeoutMs)));
    }, timeoutMs);
    timer.unref?.();
    child.on("error", (err) => {
      settle(() => reject(err));
    });
    child.on("close", (code) => {
      if (code === 0) {
        settle(() => resolveResult({ ok: true }));
      } else {
        settle(() => reject(new ProjectInitFailedError(code, stderrBuffer.trim())));
      }
    });
  });
}
var DEFAULT_TIMEOUT_MS, ProjectInitTimeoutError, ProjectInitFailedError;
var init_project_init_runner = __esm({
  "packages/daemon/src/lib/project-init-runner.ts"() {
    "use strict";
    DEFAULT_TIMEOUT_MS = 3e4;
    ProjectInitTimeoutError = class extends Error {
      timeoutMs;
      constructor(timeoutMs) {
        super(`tmux-ide init timed out after ${timeoutMs}ms`);
        this.name = "ProjectInitTimeoutError";
        this.timeoutMs = timeoutMs;
      }
    };
    ProjectInitFailedError = class extends Error {
      exitCode;
      stderr;
      constructor(exitCode, stderr) {
        super(`tmux-ide init exited with code ${exitCode ?? "(killed)"}: ${stderr || "no stderr"}`);
        this.name = "ProjectInitFailedError";
        this.exitCode = exitCode;
        this.stderr = stderr;
      }
    };
  }
});

// packages/daemon/src/schemas/inspect.ts
import { z as z23 } from "zod";
var ProjectInspectDetectedSchemaZ, ProjectInspectSchemaZ, InspectFilesystemRequestSchemaZ, OnboardProjectRequestSchemaZ;
var init_inspect = __esm({
  "packages/daemon/src/schemas/inspect.ts"() {
    "use strict";
    ProjectInspectDetectedSchemaZ = z23.object({
      /** Detected package manager from lockfile, or `null`. */
      packageManager: z23.enum(["pnpm", "npm", "yarn", "bun"]).nullable(),
      /** Detected frameworks (e.g. `["next", "convex"]`). Empty array when none. */
      frameworks: z23.array(z23.string()),
      /** Suggested dev command (e.g. `pnpm dev`). `null` if no dev script found. */
      devCommand: z23.string().nullable(),
      /** Suggested test command (e.g. `pnpm test`). `null` if no test script found. */
      testCommand: z23.string().nullable()
    });
    ProjectInspectSchemaZ = z23.object({
      /** Sanitized basename of the directory — safe to use as a tmux session name. */
      name: z23.string(),
      /** Absolute, canonical path to the directory. */
      dir: z23.string(),
      /** Whether `<dir>/ide.yml` exists. */
      hasIdeYml: z23.boolean(),
      /** Git remote origin URL, or `null` if not a git repo / no origin / probe failed. */
      gitOrigin: z23.string().nullable(),
      /** Current git branch, or `null` if not a git repo / detached HEAD / probe failed. */
      gitBranch: z23.string().nullable(),
      /** Detected stack signals (reuses `tmux-ide detect` logic). */
      detected: ProjectInspectDetectedSchemaZ
    });
    InspectFilesystemRequestSchemaZ = z23.object({
      dir: z23.string().min(1)
    });
    OnboardProjectRequestSchemaZ = z23.object({
      dir: z23.string().min(1),
      /** Optional override for the project name — defaults to inspect.name. */
      name: z23.string().min(1).optional(),
      /** 1, 2, or 3 — how many Claude panes to scaffold in the top row. */
      agents: z23.number().int().min(1).max(3),
      /**
       * Optional per-agent pane titles. When provided, length must equal
       * `agents`; the server uses these as `title:` for the Claude panes
       * instead of the canonical `Lead`/`Teammate N`/`Claude N` defaults.
       */
      agentNames: z23.array(z23.string().min(1)).optional(),
      /** Dev server command (e.g. `pnpm dev`). Omit / null to skip the dev pane. */
      devCommand: z23.string().min(1).nullable().optional(),
      /** Test command (e.g. `pnpm test`). Currently informational; stored for later. */
      testCommand: z23.string().min(1).nullable().optional(),
      /** Lint command (e.g. `pnpm lint`). Currently informational; stored for later. */
      lintCommand: z23.string().min(1).nullable().optional()
    });
  }
});

// packages/daemon/src/lib/filesystem-browser.ts
import { realpathSync as realpathSync3, readdirSync as readdirSync8, statSync as statSync8 } from "node:fs";
import { homedir as homedir10 } from "node:os";
import { isAbsolute as isAbsolute5, join as join32, resolve as resolve21, sep } from "node:path";
function resolveHome() {
  const override = process.env.TMUX_IDE_HOME_OVERRIDE;
  if (override && override.trim().length > 0) return override;
  return homedir10();
}
function preflight(rawPath, io) {
  const trimmed = rawPath.trim();
  if (!trimmed) {
    throw new InvalidPathError("Path must not be empty");
  }
  if (trimmed.includes("\0")) {
    throw new InvalidPathError("Path contains a null byte");
  }
  let candidate = trimmed;
  if (candidate === "~") {
    candidate = io.home();
  } else if (candidate.startsWith("~/")) {
    candidate = join32(io.home(), candidate.slice(2));
  }
  if (!isAbsolute5(candidate)) {
    throw new InvalidPathError("Path must be absolute");
  }
  return resolve21(candidate);
}
function isUnderRoot(canonical, root) {
  if (canonical === root) return true;
  const prefix = root.endsWith(sep) ? root : root + sep;
  return canonical.startsWith(prefix);
}
function assertInsideSandbox(canonical, home) {
  if (isUnderRoot(canonical, home)) return;
  for (const root of ALLOWED_PLATFORM_ROOTS) {
    if (isUnderRoot(canonical, root)) return;
  }
  throw new SandboxViolationError(canonical);
}
function computeParentPath(canonical, home) {
  if (canonical === "/" || canonical === sep) return null;
  const parent = resolve21(canonical, "..");
  if (parent === canonical) return null;
  try {
    assertInsideSandbox(parent, home);
  } catch {
    return null;
  }
  return parent;
}
function sortEntries(entries) {
  return [...entries].sort((a, b) => {
    if (a.isDir !== b.isDir) return a.isDir ? -1 : 1;
    return a.name.localeCompare(b.name, void 0, { sensitivity: "base" });
  });
}
function classifyDirent(dirent, fullPath, io) {
  const isSymlink = dirent.isSymbolicLink();
  if (!isSymlink) {
    return {
      entry: {
        name: dirent.name,
        fullPath,
        isDir: dirent.isDirectory(),
        isSymlink: false
      }
    };
  }
  const start2 = io.now();
  let isDir;
  try {
    const stat = io.stat(fullPath);
    if (io.now() - start2 > STAT_BUDGET_MS) {
      return { entry: null };
    }
    isDir = stat.isDirectory();
  } catch {
    return {
      entry: {
        name: dirent.name,
        fullPath,
        isDir: false,
        isSymlink: true
      }
    };
  }
  return {
    entry: {
      name: dirent.name,
      fullPath,
      isDir,
      isSymlink: true
    }
  };
}
function browseDirectory(input, io = realIo2) {
  const home = io.home();
  const requestedPath = input.path?.trim() ? input.path.trim() : home;
  let preflightPath;
  try {
    preflightPath = preflight(requestedPath, io);
  } catch (err) {
    if (err instanceof InvalidPathError) throw err;
    throw new InvalidPathError("Invalid path");
  }
  let canonical;
  try {
    canonical = io.realpath(preflightPath);
  } catch (err) {
    const code = err.code;
    if (code === "ENOENT" || code === "ENOTDIR") {
      throw new PathNotFoundError(preflightPath);
    }
    throw err;
  }
  assertInsideSandbox(canonical, home);
  let dirents;
  try {
    dirents = io.readdir(canonical);
  } catch (err) {
    const code = err.code;
    if (code === "ENOENT") {
      throw new PathNotFoundError(canonical);
    }
    if (code === "ENOTDIR") {
      throw new InvalidPathError(`"${canonical}" is not a directory`);
    }
    throw err;
  }
  const showHidden = Boolean(input.showHidden);
  const entries = [];
  for (const dirent of dirents) {
    if (!showHidden && dirent.name.startsWith(".")) continue;
    const fullPath = join32(canonical, dirent.name);
    const { entry } = classifyDirent(dirent, fullPath, io);
    if (entry !== null) entries.push(entry);
  }
  return {
    path: canonical,
    parentPath: computeParentPath(canonical, home),
    entries: sortEntries(entries)
  };
}
var ALLOWED_PLATFORM_ROOTS, STAT_BUDGET_MS, SandboxViolationError, PathNotFoundError, InvalidPathError, realIo2;
var init_filesystem_browser = __esm({
  "packages/daemon/src/lib/filesystem-browser.ts"() {
    "use strict";
    ALLOWED_PLATFORM_ROOTS = ["/Users", "/home", "/Volumes"];
    STAT_BUDGET_MS = 100;
    SandboxViolationError = class extends Error {
      code = "outside-sandbox";
      constructor(path4) {
        super(`Path "${path4}" is outside the allowed sandbox`);
        this.name = "SandboxViolationError";
      }
    };
    PathNotFoundError = class extends Error {
      code = "not-found";
      constructor(path4) {
        super(`Path "${path4}" does not exist`);
        this.name = "PathNotFoundError";
      }
    };
    InvalidPathError = class extends Error {
      code = "invalid-path";
      constructor(message) {
        super(message);
        this.name = "InvalidPathError";
      }
    };
    realIo2 = {
      realpath: realpathSync3,
      stat: statSync8,
      readdir: (path4) => readdirSync8(path4, { withFileTypes: true }),
      home: resolveHome,
      now: () => Date.now()
    };
  }
});

// packages/daemon/src/lib/project-inspect.ts
import { existsSync as existsSync31 } from "node:fs";
import { isAbsolute as isAbsolute6, resolve as resolve22 } from "node:path";
function narrowPackageManager(raw) {
  if (!raw) return null;
  return KNOWN_PACKAGE_MANAGERS.has(raw) ? raw : null;
}
function inferTestCommand(packageManager) {
  if (!packageManager) return null;
  return packageManager === "npm" ? "npm test" : `${packageManager} test`;
}
async function inspectProject(dir, io = {}) {
  const exists = io.exists ?? existsSync31;
  const absoluteDir = isAbsolute6(dir) ? dir : resolve22(dir);
  if (!exists(absoluteDir)) {
    throw new InspectDirNotFoundError(absoluteDir);
  }
  const probe = await probeProject(absoluteDir, io.probeIo);
  const stack = detectStack(absoluteDir);
  const detected = {
    packageManager: narrowPackageManager(stack.packageManager),
    frameworks: stack.frameworks,
    devCommand: stack.devCommand,
    testCommand: inferTestCommand(stack.packageManager)
  };
  return {
    name: probe.name,
    dir: probe.dir,
    hasIdeYml: probe.hasIdeYml,
    gitOrigin: probe.gitOrigin,
    gitBranch: probe.gitBranch,
    detected
  };
}
var InspectDirNotFoundError, KNOWN_PACKAGE_MANAGERS;
var init_project_inspect = __esm({
  "packages/daemon/src/lib/project-inspect.ts"() {
    "use strict";
    init_detect();
    init_project_probe();
    InspectDirNotFoundError = class extends Error {
      code = "DIR_NOT_FOUND";
      constructor(dir) {
        super(`Directory "${dir}" does not exist`);
        this.name = "InspectDirNotFoundError";
      }
    };
    KNOWN_PACKAGE_MANAGERS = /* @__PURE__ */ new Set(["pnpm", "npm", "yarn", "bun"]);
  }
});

// packages/daemon/src/lib/project-onboard.ts
import yaml4 from "js-yaml";
import { existsSync as existsSync32 } from "node:fs";
import { join as join33 } from "node:path";
function composeIdeYmlConfig(input) {
  if (!Number.isInteger(input.agents) || input.agents < 1 || input.agents > 3) {
    throw new OnboardInvalidInputError(
      `agents must be an integer between 1 and 3 (got ${input.agents})`
    );
  }
  const cleanName = input.name.trim();
  if (!cleanName) {
    throw new OnboardInvalidInputError("name must be a non-empty string");
  }
  const agentsCount = input.agents;
  const useTeam = agentsCount > 1;
  const customNames = input.agentNames;
  if (customNames !== void 0) {
    if (customNames.length !== agentsCount) {
      throw new OnboardInvalidInputError(
        `agentNames length (${customNames.length}) must equal agents (${agentsCount})`
      );
    }
    for (const name of customNames) {
      if (typeof name !== "string" || name.trim() === "") {
        throw new OnboardInvalidInputError("agentNames entries must be non-empty strings");
      }
    }
  }
  const topPanes = [];
  for (let i = 0; i < agentsCount; i++) {
    const fallback = useTeam ? i === 0 ? "Lead" : `Teammate ${i}` : `Claude ${i + 1}`;
    const customTitle = customNames?.[i]?.trim();
    const pane = {
      title: customTitle && customTitle.length > 0 ? customTitle : fallback,
      command: "claude"
    };
    if (useTeam) {
      pane.role = i === 0 ? "lead" : "teammate";
    }
    if (i === 0) {
      pane.focus = true;
    }
    topPanes.push(pane);
  }
  const bottomPanes = [];
  const devCommand = input.devCommand?.trim();
  if (devCommand) {
    bottomPanes.push({ title: "Dev", command: devCommand });
  }
  bottomPanes.push({ title: "Shell" });
  const rows = [{ size: "70%", panes: topPanes }, { panes: bottomPanes }];
  const config2 = {
    name: cleanName,
    rows
  };
  if (useTeam) {
    config2.team = { name: cleanName };
  }
  return config2;
}
function assertNoExistingIdeYml(dir, exists = existsSync32) {
  const path4 = join33(dir, "ide.yml");
  if (exists(path4)) {
    throw new OnboardConflictError(path4);
  }
}
var OnboardConflictError, OnboardInvalidInputError;
var init_project_onboard = __esm({
  "packages/daemon/src/lib/project-onboard.ts"() {
    "use strict";
    OnboardConflictError = class extends Error {
      code = "IDE_YML_EXISTS";
      constructor(path4) {
        super(`ide.yml already exists at ${path4}`);
        this.name = "OnboardConflictError";
      }
    };
    OnboardInvalidInputError = class extends Error {
      code = "INVALID_INPUT";
      constructor(message) {
        super(message);
        this.name = "OnboardInvalidInputError";
      }
    };
  }
});

// packages/daemon/src/lsp/launch.ts
import { spawn as spawn9 } from "node:child_process";
import { existsSync as existsSync33 } from "node:fs";
import { delimiter as delimiter2, extname as extname2, join as join34 } from "node:path";
function findOnPath(binary) {
  const envPath = process.env.PATH ?? "";
  for (const dir of envPath.split(PATH_DELIM)) {
    if (!dir) continue;
    const candidate = join34(dir, binary);
    if (existsSync33(candidate)) return candidate;
  }
  return null;
}
function resolveBinary(config2, workspaceRoot) {
  const override = config2.binaryEnvVar ? process.env[config2.binaryEnvVar] : void 0;
  if (override && override.trim().length > 0) {
    return override;
  }
  for (const rel of config2.workspaceLookups ?? []) {
    const candidate = join34(workspaceRoot, rel);
    if (existsSync33(candidate)) return candidate;
  }
  return findOnPath(config2.defaultBinary);
}
function languageServerConfig(language) {
  const config2 = CONFIG_BY_LANGUAGE.get(language);
  if (!config2) throw new Error(`Unknown LSP language: ${language}`);
  return config2;
}
function languageServerConfigForFile(file) {
  return CONFIG_BY_EXTENSION.get(extname2(file).toLowerCase());
}
function launchLanguageServer(language, workspaceRoot) {
  const config2 = languageServerConfig(language);
  const resolved2 = resolveBinary(config2, workspaceRoot);
  if (!resolved2) return null;
  const proc = spawn9(resolved2, [...config2.args], {
    cwd: workspaceRoot,
    stdio: ["pipe", "pipe", "pipe"],
    env: { ...process.env }
  });
  if (!proc.stdin || !proc.stdout || !proc.stderr) {
    throw new Error(`${config2.defaultBinary} stdio pipes unavailable`);
  }
  return { process: proc, command: resolved2 };
}
var PATH_DELIM, LANGUAGE_SERVERS, CONFIG_BY_LANGUAGE, CONFIG_BY_EXTENSION;
var init_launch2 = __esm({
  "packages/daemon/src/lsp/launch.ts"() {
    "use strict";
    PATH_DELIM = delimiter2;
    LANGUAGE_SERVERS = [
      {
        language: "typescript",
        extensions: [".ts", ".tsx", ".mts", ".cts", ".js", ".jsx", ".mjs", ".cjs"],
        languageIdFor: (file) => {
          const ext = extname2(file).toLowerCase();
          switch (ext) {
            case ".ts":
            case ".mts":
            case ".cts":
              return "typescript";
            case ".tsx":
              return "typescriptreact";
            case ".jsx":
              return "javascriptreact";
            default:
              return "javascript";
          }
        },
        defaultBinary: "typescript-language-server",
        binaryEnvVar: "TMUX_IDE_LSP_TYPESCRIPT",
        args: ["--stdio"],
        workspaceLookups: [join34("node_modules", ".bin", "typescript-language-server")]
      },
      {
        language: "python",
        // Pyright's stock LSP id is "python" — same as VS Code. `.pyi` is
        // a stub-only file but pyright handles it under the same id.
        extensions: [".py", ".pyi"],
        languageIdFor: () => "python",
        defaultBinary: "pyright-langserver",
        binaryEnvVar: "TMUX_IDE_LSP_PYRIGHT",
        args: ["--stdio"]
      },
      {
        language: "rust",
        extensions: [".rs"],
        languageIdFor: () => "rust",
        defaultBinary: "rust-analyzer",
        binaryEnvVar: "TMUX_IDE_LSP_RUST_ANALYZER",
        // rust-analyzer reads stdio by default; no args needed.
        args: []
      },
      {
        language: "go",
        extensions: [".go"],
        languageIdFor: () => "go",
        defaultBinary: "gopls",
        binaryEnvVar: "TMUX_IDE_LSP_GOPLS",
        args: []
      }
    ];
    CONFIG_BY_LANGUAGE = new Map(
      LANGUAGE_SERVERS.map((c) => [c.language, c])
    );
    CONFIG_BY_EXTENSION = /* @__PURE__ */ new Map();
    for (const config2 of LANGUAGE_SERVERS) {
      for (const ext of config2.extensions) {
        CONFIG_BY_EXTENSION.set(ext.toLowerCase(), config2);
      }
    }
  }
});

// packages/daemon/src/lsp/client.ts
import { pathToFileURL, fileURLToPath as fileURLToPath4 } from "node:url";
import { readFile as readFile3 } from "node:fs/promises";
import {
  createProtocolConnection,
  StreamMessageReader,
  StreamMessageWriter,
  InitializeRequest,
  InitializedNotification,
  DidOpenTextDocumentNotification,
  DidChangeTextDocumentNotification,
  HoverRequest,
  DefinitionRequest,
  ReferencesRequest,
  WorkspaceSymbolRequest,
  RenameRequest,
  CodeActionRequest,
  PublishDiagnosticsNotification,
  ShutdownRequest,
  ExitNotification
} from "vscode-languageserver-protocol/node.js";
function languageForFile(file) {
  return languageServerConfigForFile(file)?.language;
}
function languageIdFor(file) {
  const config2 = languageServerConfigForFile(file);
  return config2 ? config2.languageIdFor(file) : "plaintext";
}
async function createLspClient(input) {
  languageServerConfig(input.language);
  const handle = launchLanguageServer(input.language, input.root);
  if (!handle) {
    return makeNoOpLspClient(input.root, input.language);
  }
  handle.process.stderr.on("data", () => {
  });
  const connection = createProtocolConnection(
    new StreamMessageReader(handle.process.stdout),
    new StreamMessageWriter(handle.process.stdin)
  );
  const diagnosticsByFile = /* @__PURE__ */ new Map();
  const openDocs = /* @__PURE__ */ new Map();
  const diagnosticsWaiters = /* @__PURE__ */ new Map();
  connection.onNotification(PublishDiagnosticsNotification.type, (params) => {
    let file;
    try {
      file = fileURLToPath4(params.uri);
    } catch {
      return;
    }
    diagnosticsByFile.set(file, params.diagnostics);
    const waiters = diagnosticsWaiters.get(file);
    if (waiters?.length) {
      diagnosticsWaiters.delete(file);
      for (const w of waiters) w();
    }
  });
  connection.onRequest("workspace/configuration", () => []);
  connection.onRequest("window/workDoneProgress/create", () => null);
  connection.onRequest("client/registerCapability", () => null);
  connection.onRequest("client/unregisterCapability", () => null);
  connection.listen();
  const initializeResult = await Promise.race([
    connection.sendRequest(InitializeRequest.type, {
      processId: process.pid,
      rootUri: pathToFileURL(input.root).href,
      workspaceFolders: [{ uri: pathToFileURL(input.root).href, name: "workspace" }],
      capabilities: {
        textDocument: {
          synchronization: { didOpen: true, didChange: true },
          hover: { contentFormat: ["markdown", "plaintext"] },
          definition: { linkSupport: true },
          references: {},
          rename: { prepareSupport: false },
          codeAction: {
            codeActionLiteralSupport: {
              codeActionKind: {
                valueSet: [
                  "",
                  "quickfix",
                  "refactor",
                  "refactor.extract",
                  "refactor.inline",
                  "refactor.rewrite",
                  "source",
                  "source.organizeImports"
                ]
              }
            }
          },
          publishDiagnostics: { versionSupport: false }
        },
        workspace: {
          configuration: true,
          workspaceFolders: true,
          symbol: {},
          applyEdit: false
        }
      },
      initializationOptions: {}
    }),
    new Promise(
      (_, reject) => setTimeout(() => reject(new Error("LSP initialize timeout")), INITIALIZE_TIMEOUT_MS)
    )
  ]);
  void initializeResult;
  await connection.sendNotification(InitializedNotification.type, {});
  async function ensureOpen(file) {
    if (openDocs.has(file)) return;
    const text = await readFile3(file, "utf-8").catch(() => "");
    openDocs.set(file, { version: 0, text });
    await connection.sendNotification(DidOpenTextDocumentNotification.type, {
      textDocument: {
        uri: pathToFileURL(file).href,
        languageId: languageIdFor(file),
        version: 0,
        text
      }
    });
  }
  async function touch(file) {
    const current = openDocs.get(file);
    if (!current) {
      await ensureOpen(file);
      return;
    }
    const text = await readFile3(file, "utf-8").catch(() => current.text);
    if (text === current.text) return;
    const nextVersion = current.version + 1;
    openDocs.set(file, { version: nextVersion, text });
    await connection.sendNotification(DidChangeTextDocumentNotification.type, {
      textDocument: { uri: pathToFileURL(file).href, version: nextVersion },
      contentChanges: [{ text }]
    });
  }
  return {
    root: input.root,
    language: input.language,
    ensureOpen,
    async hover(file, line, character) {
      await ensureOpen(file);
      await touch(file);
      return connection.sendRequest(HoverRequest.type, {
        textDocument: { uri: pathToFileURL(file).href },
        position: { line, character }
      }).catch(() => null);
    },
    async definition(file, line, character) {
      await ensureOpen(file);
      await touch(file);
      return connection.sendRequest(DefinitionRequest.type, {
        textDocument: { uri: pathToFileURL(file).href },
        position: { line, character }
      }).catch(() => null);
    },
    async references(file, line, character) {
      await ensureOpen(file);
      await touch(file);
      return connection.sendRequest(ReferencesRequest.type, {
        textDocument: { uri: pathToFileURL(file).href },
        position: { line, character },
        context: { includeDeclaration: true }
      }).catch(() => null);
    },
    diagnostics(file) {
      return diagnosticsByFile.get(file) ?? [];
    },
    async workspaceSymbols(query) {
      return connection.sendRequest(WorkspaceSymbolRequest.type, { query }).catch(() => null);
    },
    async rename(file, line, character, newName) {
      await ensureOpen(file);
      await touch(file);
      return connection.sendRequest(RenameRequest.type, {
        textDocument: { uri: pathToFileURL(file).href },
        position: { line, character },
        newName
      }).catch(() => null);
    },
    async codeActions(file, range, diagnostics) {
      await ensureOpen(file);
      await touch(file);
      return connection.sendRequest(CodeActionRequest.type, {
        textDocument: { uri: pathToFileURL(file).href },
        range,
        context: { diagnostics: diagnostics ?? [] }
      }).catch(() => null);
    },
    async waitForDiagnostics(file, timeoutMs) {
      const existing = diagnosticsByFile.get(file);
      if (existing) return existing;
      return new Promise((resolve35) => {
        const timer = setTimeout(() => {
          const waiters2 = diagnosticsWaiters.get(file);
          if (waiters2) {
            const idx = waiters2.indexOf(done);
            if (idx >= 0) waiters2.splice(idx, 1);
            if (waiters2.length === 0) diagnosticsWaiters.delete(file);
          }
          resolve35(diagnosticsByFile.get(file) ?? []);
        }, timeoutMs);
        const done = () => {
          clearTimeout(timer);
          resolve35(diagnosticsByFile.get(file) ?? []);
        };
        const waiters = diagnosticsWaiters.get(file) ?? [];
        waiters.push(done);
        diagnosticsWaiters.set(file, waiters);
      });
    },
    async shutdown() {
      try {
        await connection.sendRequest(ShutdownRequest.type);
        await connection.sendNotification(ExitNotification.type);
      } catch {
      }
      try {
        connection.dispose();
      } catch {
      }
      if (!handle.process.killed) {
        handle.process.kill();
      }
    }
  };
}
function makeNoOpLspClient(root, language) {
  return {
    root,
    language,
    async ensureOpen() {
    },
    async hover() {
      return null;
    },
    async definition() {
      return null;
    },
    async references() {
      return null;
    },
    diagnostics() {
      return [];
    },
    async waitForDiagnostics() {
      return [];
    },
    async workspaceSymbols() {
      return null;
    },
    async rename() {
      return null;
    },
    async codeActions() {
      return null;
    },
    async shutdown() {
    }
  };
}
var INITIALIZE_TIMEOUT_MS;
var init_client4 = __esm({
  "packages/daemon/src/lsp/client.ts"() {
    "use strict";
    init_launch2();
    INITIALIZE_TIMEOUT_MS = 3e4;
  }
});

// packages/daemon/src/lsp/registry.ts
function keyFor(root, language) {
  return root + "::" + language;
}
function getLspClient(root, language) {
  const key = keyFor(root, language);
  const existing = clients.get(key);
  if (existing) return existing;
  const pending = createLspClient({ root, language }).catch((err) => {
    clients.delete(key);
    throw err;
  });
  clients.set(key, pending);
  return pending;
}
async function getLspClientForFile(root, file) {
  const language = languageForFile(file);
  if (!language) return void 0;
  return getLspClient(root, language);
}
var clients;
var init_registry3 = __esm({
  "packages/daemon/src/lsp/registry.ts"() {
    "use strict";
    init_client4();
    init_client4();
    clients = /* @__PURE__ */ new Map();
  }
});

// packages/daemon/src/chat/provider-capabilities.ts
function capabilitiesFor(instance, override) {
  const base = BUILT_IN_CAPABILITIES[instance.kind];
  if (!override || Object.keys(override).length === 0) return base;
  return { ...base, ...override };
}
var BUILT_IN_CAPABILITIES;
var init_provider_capabilities = __esm({
  "packages/daemon/src/chat/provider-capabilities.ts"() {
    "use strict";
    BUILT_IN_CAPABILITIES = {
      anthropic: {
        streaming: true,
        toolCalls: true,
        vision: true,
        promptCaching: true,
        systemPrompts: true,
        reasoningEffort: false,
        contextWindowTokens: 2e5,
        maxOutputTokens: 8192,
        toolApprovalPolicy: "ask"
      },
      openai: {
        streaming: true,
        toolCalls: true,
        vision: true,
        promptCaching: true,
        // gpt-4o-mini onwards; default to true for the modern lineup
        systemPrompts: true,
        reasoningEffort: "medium",
        // o1/o3 surface; non-reasoning models ignore
        contextWindowTokens: 128e3,
        maxOutputTokens: 16384,
        toolApprovalPolicy: "ask"
      },
      "local-ollama": {
        streaming: true,
        toolCalls: false,
        // most local quantized models lack the structured surface
        vision: false,
        promptCaching: false,
        systemPrompts: true,
        reasoningEffort: false,
        contextWindowTokens: 8192,
        maxOutputTokens: 2048,
        toolApprovalPolicy: "auto"
        // local-only — trust by default
      },
      "local-lmstudio": {
        streaming: true,
        toolCalls: false,
        vision: false,
        promptCaching: false,
        systemPrompts: true,
        reasoningEffort: false,
        contextWindowTokens: 8192,
        maxOutputTokens: 4096,
        toolApprovalPolicy: "auto"
      },
      "generic-acp": {
        // ACP is a transport, not a provider: the inner agent's capabilities
        // are opaque to the daemon until the binary identifies itself.
        // Start permissive on tool-calls because the whole point of ACP is to
        // run an agent that owns tooling.
        streaming: true,
        toolCalls: true,
        vision: false,
        promptCaching: false,
        systemPrompts: true,
        reasoningEffort: false,
        contextWindowTokens: 2e5,
        maxOutputTokens: 8192,
        toolApprovalPolicy: "ask"
      }
    };
  }
});

// packages/daemon/src/chat/provider-registry.ts
function makeProviderRegistry(opts = {}) {
  const factories = /* @__PURE__ */ new Map();
  for (const [kind, factory] of Object.entries(BUILT_IN_FACTORIES)) {
    factories.set(kind, factory);
  }
  for (const [kind, factory] of Object.entries(opts.factories ?? {})) {
    if (factory) factories.set(kind, factory);
  }
  const instances = /* @__PURE__ */ new Map();
  return {
    registerKind(kind, factory) {
      factories.set(kind, factory);
    },
    hasKind(kind) {
      return factories.has(kind);
    },
    registerInstance(instance) {
      const parsed = ProviderInstanceZ.safeParse(instance);
      if (!parsed.success) {
        throw new ProviderRegistryError(
          `Invalid provider instance: ${parsed.error.message}`,
          "invalid_config"
        );
      }
      if (instances.has(parsed.data.id)) {
        throw new ProviderRegistryError(
          `Provider instance already registered: ${parsed.data.id}`,
          "duplicate"
        );
      }
      instances.set(parsed.data.id, parsed.data);
    },
    unregisterInstance(id) {
      return instances.delete(id);
    },
    listInstances() {
      return [...instances.values()];
    },
    getInstance(id) {
      return instances.get(id) ?? null;
    },
    adapterFor(id, deps2) {
      const instance = instances.get(id);
      if (!instance) {
        throw new ProviderRegistryError(`Provider instance not found: ${id}`, "instance_not_found");
      }
      const factory = factories.get(instance.kind);
      if (!factory) {
        throw new ProviderRegistryError(
          `No adapter registered for provider kind: ${instance.kind}`,
          "unknown_kind"
        );
      }
      return factory(instance, deps2);
    },
    validateConfig(config2) {
      const parsed = ProviderConfigZ.safeParse(config2);
      if (!parsed.success) {
        throw new ProviderRegistryError(
          `Invalid provider config: ${parsed.error.message}`,
          "invalid_config"
        );
      }
      return parsed.data;
    }
  };
}
function resolveFetch(deps2) {
  const candidate = deps2?.fetch ?? globalThis.fetch;
  if (!candidate) {
    throw new ProviderRegistryError(
      "No fetch implementation available \u2014 pass deps.fetch in test or run on Node 18+.",
      "invalid_config"
    );
  }
  return candidate;
}
function makeAnthropicAdapter(instance, deps2) {
  const config2 = instance.config;
  return {
    kind: "anthropic",
    instance,
    capabilities: capabilitiesFor(instance),
    async *sendTurn({ messages, signal }) {
      const fetchFn = resolveFetch(deps2);
      const url = `${config2.baseUrl ?? "https://api.anthropic.com"}/v1/messages`;
      const res = await fetchFn(url, {
        method: "POST",
        signal,
        headers: {
          "content-type": "application/json",
          "x-api-key": config2.apiKey,
          "anthropic-version": "2023-06-01"
        },
        body: JSON.stringify({
          model: config2.model,
          max_tokens: 4096,
          messages: messages.filter((m) => m.role !== "system"),
          system: messages.find((m) => m.role === "system")?.content
        })
      });
      if (!res.ok) {
        yield { type: "error", message: `Anthropic ${res.status}: ${await safeText(res)}` };
        yield { type: "end", reason: "refusal" };
        return;
      }
      const body = await res.json();
      for (const block of body.content ?? []) {
        if (block.type === "text" && typeof block.text === "string") {
          yield { type: "text", text: block.text };
        }
      }
      if (body.usage) {
        yield {
          type: "usage",
          usage: {
            inputTokens: body.usage.input_tokens,
            outputTokens: body.usage.output_tokens
          }
        };
      }
      yield { type: "end", reason: mapStopReason(body.stop_reason) };
    }
  };
}
function makeOpenAIAdapter(instance, deps2) {
  const config2 = instance.config;
  return {
    kind: "openai",
    instance,
    capabilities: capabilitiesFor(instance),
    async *sendTurn({ messages, signal }) {
      const fetchFn = resolveFetch(deps2);
      const url = `${config2.baseUrl ?? "https://api.openai.com/v1"}/chat/completions`;
      const headers = {
        "content-type": "application/json",
        authorization: `Bearer ${config2.apiKey}`
      };
      if (config2.organization) headers["openai-organization"] = config2.organization;
      const res = await fetchFn(url, {
        method: "POST",
        signal,
        headers,
        body: JSON.stringify({ model: config2.model, messages })
      });
      if (!res.ok) {
        yield { type: "error", message: `OpenAI ${res.status}: ${await safeText(res)}` };
        yield { type: "end", reason: "refusal" };
        return;
      }
      const body = await res.json();
      const choice = body.choices?.[0];
      if (choice?.message?.content) {
        yield { type: "text", text: choice.message.content };
      }
      if (body.usage) {
        yield {
          type: "usage",
          usage: {
            inputTokens: body.usage.prompt_tokens,
            outputTokens: body.usage.completion_tokens
          }
        };
      }
      yield { type: "end", reason: mapStopReason(choice?.finish_reason) };
    }
  };
}
function makeOllamaAdapter(instance, deps2) {
  const config2 = instance.config;
  return {
    kind: "local-ollama",
    instance,
    capabilities: capabilitiesFor(instance),
    async *sendTurn({ messages, signal }) {
      const fetchFn = resolveFetch(deps2);
      const url = `${config2.baseUrl}/api/chat`;
      const res = await fetchFn(url, {
        method: "POST",
        signal,
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ model: config2.model, messages, stream: false })
      });
      if (!res.ok) {
        yield { type: "error", message: `Ollama ${res.status}: ${await safeText(res)}` };
        yield { type: "end", reason: "refusal" };
        return;
      }
      const body = await res.json();
      if (body.message?.content) yield { type: "text", text: body.message.content };
      if (body.eval_count !== void 0 || body.prompt_eval_count !== void 0) {
        yield {
          type: "usage",
          usage: {
            inputTokens: body.prompt_eval_count,
            outputTokens: body.eval_count
          }
        };
      }
      yield { type: "end", reason: mapStopReason(body.done_reason) };
    }
  };
}
function makeLmStudioAdapter(instance, deps2) {
  const config2 = instance.config;
  const openAiInstance = {
    ...instance,
    kind: "openai",
    config: {
      kind: "openai",
      apiKey: config2.apiKey ?? "lm-studio",
      model: config2.model,
      baseUrl: config2.baseUrl
    }
  };
  const inner = makeOpenAIAdapter(openAiInstance, deps2);
  return {
    ...inner,
    kind: "local-lmstudio",
    instance,
    capabilities: capabilitiesFor(instance)
  };
}
function makeGenericAcpAdapter(instance) {
  const config2 = instance.config;
  return {
    kind: "generic-acp",
    instance,
    capabilities: capabilitiesFor(instance),
    async *sendTurn() {
      yield {
        type: "error",
        message: `Generic ACP adapter not yet wired (binary=${config2.binary}). Wired in T080.`
      };
      yield { type: "end", reason: "refusal" };
    }
  };
}
async function safeText(res) {
  try {
    return (await res.text()).slice(0, 500);
  } catch {
    return "";
  }
}
function mapStopReason(raw) {
  if (typeof raw !== "string") return "completed";
  const lower = raw.toLowerCase();
  if (lower === "max_tokens" || lower === "length") return "max_tokens";
  if (lower === "refusal" || lower === "stopped") return "refusal";
  if (lower === "cancelled" || lower === "aborted") return "cancelled";
  return "completed";
}
var ProviderRegistryError, BUILT_IN_FACTORIES;
var init_provider_registry = __esm({
  "packages/daemon/src/chat/provider-registry.ts"() {
    "use strict";
    init_src2();
    init_provider_capabilities();
    ProviderRegistryError = class extends Error {
      constructor(message, code) {
        super(message);
        this.code = code;
        this.name = "ProviderRegistryError";
      }
    };
    BUILT_IN_FACTORIES = {
      anthropic: makeAnthropicAdapter,
      openai: makeOpenAIAdapter,
      "local-ollama": makeOllamaAdapter,
      "local-lmstudio": makeLmStudioAdapter,
      "generic-acp": (instance) => makeGenericAcpAdapter(instance)
    };
  }
});

// packages/daemon/src/chat/provider-store.ts
import { existsSync as existsSync34, mkdirSync as mkdirSync22, readFileSync as readFileSync24, renameSync as renameSync17, writeFileSync as writeFileSync22 } from "node:fs";
import { dirname as dirname15, join as join35 } from "node:path";
import { homedir as homedir11 } from "node:os";
import { randomBytes as randomBytes3 } from "node:crypto";
function toSummary(instance) {
  const cfg = instance.config;
  const hasApiKey = cfg.kind === "anthropic" && Boolean(cfg.apiKey) || cfg.kind === "openai" && Boolean(cfg.apiKey) || cfg.kind === "local-lmstudio" && Boolean(cfg.apiKey);
  const summary = {
    id: instance.id,
    kind: instance.kind,
    displayName: instance.displayName,
    hasApiKey
  };
  if ("model" in cfg && cfg.model) summary.model = cfg.model;
  if ("baseUrl" in cfg && cfg.baseUrl) summary.baseUrl = cfg.baseUrl;
  if (instance.createdAt) summary.createdAt = instance.createdAt;
  return summary;
}
function defaultProvidersFilePath() {
  return process.env.TMUX_IDE_PROVIDERS_FILE ?? join35(homedir11(), ".tmux-ide", "providers.json");
}
function readProvidersFile(filePath) {
  if (!existsSync34(filePath)) {
    return { version: 1, providers: [] };
  }
  let raw;
  try {
    raw = readFileSync24(filePath, "utf8");
  } catch (err) {
    throw new ProviderStoreError(
      `Cannot read providers file ${filePath}: ${err.message}`,
      "io_error"
    );
  }
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch (err) {
    throw new ProviderStoreError(
      `Providers file ${filePath} is not valid JSON: ${err.message}`,
      "invalid_file"
    );
  }
  const validated = ProvidersFileZ.safeParse(parsed);
  if (!validated.success) {
    throw new ProviderStoreError(
      `Providers file ${filePath} failed schema: ${validated.error.issues.map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`).join("; ")}`,
      "invalid_file"
    );
  }
  return validated.data;
}
function writeProvidersFile(filePath, data) {
  const dir = dirname15(filePath);
  if (!existsSync34(dir)) {
    mkdirSync22(dir, { recursive: true });
  }
  const tmp = `${filePath}.${randomBytes3(4).toString("hex")}.tmp`;
  writeFileSync22(tmp, JSON.stringify(data, null, 2), "utf8");
  renameSync17(tmp, filePath);
}
function makeProviderStore(opts = {}) {
  const filePath = opts.filePath ?? defaultProvidersFilePath();
  const now = opts.now ?? (() => /* @__PURE__ */ new Date());
  const randomId = opts.randomId ?? (() => randomBytes3(6).toString("hex"));
  const registry = opts.registry ?? makeProviderRegistry();
  let cache2 = readProvidersFile(filePath);
  function commit3(next) {
    writeProvidersFile(filePath, next);
    cache2 = next;
  }
  return {
    list() {
      return cache2.providers.map((p) => ({ ...p }));
    },
    summaries() {
      return cache2.providers.map((p) => toSummary(p));
    },
    get(id) {
      const found = cache2.providers.find((p) => p.id === id);
      return found ? { ...found } : null;
    },
    add(input) {
      const seed = {
        ...input,
        createdAt: input.createdAt ?? now().toISOString()
      };
      const validated = ProviderInstanceZ.safeParse(seed);
      if (!validated.success) {
        throw new ProviderStoreError(
          `Invalid provider instance: ${validated.error.issues.map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`).join("; ")}`,
          "invalid_file"
        );
      }
      const id = validated.data.id || randomId();
      if (cache2.providers.some((p) => p.id === id)) {
        throw new ProviderStoreError(`Provider id already exists: ${id}`, "duplicate_id");
      }
      try {
        registry.validateConfig(validated.data.config);
      } catch (err) {
        if (err instanceof ProviderRegistryError) {
          throw new ProviderStoreError(err.message, "invalid_file");
        }
        throw err;
      }
      const record = {
        ...validated.data,
        id,
        createdAt: validated.data.createdAt ?? now().toISOString()
      };
      commit3({ ...cache2, providers: [...cache2.providers, record] });
      return { ...record };
    },
    update(id, patch) {
      const idx = cache2.providers.findIndex((p) => p.id === id);
      if (idx < 0) {
        throw new ProviderStoreError(`Provider not found: ${id}`, "not_found");
      }
      const existing = cache2.providers[idx];
      const candidate = {
        ...existing,
        ...patch,
        id: existing.id
      };
      const validated = ProviderInstanceZ.safeParse(candidate);
      if (!validated.success) {
        throw new ProviderStoreError(
          `Invalid provider patch: ${validated.error.issues.map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`).join("; ")}`,
          "invalid_file"
        );
      }
      try {
        registry.validateConfig(validated.data.config);
      } catch (err) {
        if (err instanceof ProviderRegistryError) {
          throw new ProviderStoreError(err.message, "invalid_file");
        }
        throw err;
      }
      const nextProviders = cache2.providers.slice();
      nextProviders[idx] = validated.data;
      commit3({ ...cache2, providers: nextProviders });
      return { ...validated.data };
    },
    remove(id) {
      const before = cache2.providers.length;
      const next = cache2.providers.filter((p) => p.id !== id);
      if (next.length === before) return false;
      commit3({ ...cache2, providers: next });
      return true;
    },
    reload() {
      cache2 = readProvidersFile(filePath);
    }
  };
}
var ProviderStoreError;
var init_provider_store = __esm({
  "packages/daemon/src/chat/provider-store.ts"() {
    "use strict";
    init_src2();
    init_provider_registry();
    ProviderStoreError = class extends Error {
      code;
      constructor(message, code) {
        super(message);
        this.name = "ProviderStoreError";
        this.code = code;
      }
    };
  }
});

// packages/daemon/src/widgets/lib/files.ts
var files_exports = {};
__export(files_exports, {
  createIgnoreFilter: () => createIgnoreFilter,
  readDirectory: () => readDirectory
});
import { readdirSync as readdirSync9, readFileSync as readFileSync25, existsSync as existsSync35 } from "node:fs";
import { join as join36, relative as relative3 } from "node:path";
import ignore from "ignore";
function createIgnoreFilter(rootDir) {
  const ig = ignore();
  const gitignorePath = join36(rootDir, ".gitignore");
  if (existsSync35(gitignorePath)) {
    ig.add(readFileSync25(gitignorePath, "utf-8"));
  }
  return ig;
}
function readDirectory(dir, rootDir, ig, showHidden, showIgnored = false) {
  let entries;
  try {
    entries = readdirSync9(dir, { withFileTypes: true });
  } catch {
    return [];
  }
  return entries.filter((e) => {
    if (ALWAYS_IGNORE.has(e.name)) return false;
    if (!showHidden && e.name.startsWith(".")) return false;
    if (showIgnored) return true;
    const rel = relative3(rootDir, join36(dir, e.name));
    try {
      return !ig.ignores(e.isDirectory() ? rel + "/" : rel);
    } catch {
      return true;
    }
  }).map((e) => {
    const rel = relative3(rootDir, join36(dir, e.name));
    let isIgnored = false;
    try {
      isIgnored = ig.ignores(e.isDirectory() ? rel + "/" : rel);
    } catch {
    }
    return {
      name: e.name,
      path: rel,
      absolutePath: join36(dir, e.name),
      isDir: e.isDirectory(),
      ignored: isIgnored
    };
  }).sort((a, b) => {
    if (a.isDir !== b.isDir) return a.isDir ? -1 : 1;
    return a.name.localeCompare(b.name);
  });
}
var ALWAYS_IGNORE;
var init_files = __esm({
  "packages/daemon/src/widgets/lib/files.ts"() {
    "use strict";
    ALWAYS_IGNORE = /* @__PURE__ */ new Set([
      "node_modules",
      ".git",
      ".svn",
      ".hg",
      "dist",
      "build",
      "out",
      ".next",
      ".turbo",
      ".cache",
      "__pycache__",
      "coverage",
      ".nyc_output",
      "target",
      "vendor",
      "bower_components"
    ]);
  }
});

// packages/daemon/src/lib/tunnels/types.ts
var types_exports2 = {};
__export(types_exports2, {
  tunnelConfigSchema: () => tunnelConfigSchema
});
import { z as z24 } from "zod";
var tunnelConfigSchema;
var init_types3 = __esm({
  "packages/daemon/src/lib/tunnels/types.ts"() {
    "use strict";
    tunnelConfigSchema = z24.discriminatedUnion("provider", [
      z24.object({
        provider: z24.literal("tailscale"),
        port: z24.number().int().positive(),
        enableFunnel: z24.boolean().optional()
      }),
      z24.object({
        provider: z24.literal("ngrok"),
        port: z24.number().int().positive(),
        authToken: z24.string().optional(),
        domain: z24.string().optional(),
        region: z24.string().optional(),
        startupTimeoutMs: z24.number().positive().optional()
      }),
      z24.object({
        provider: z24.literal("cloudflare"),
        port: z24.number().int().positive(),
        startupTimeoutMs: z24.number().positive().optional()
      })
    ]);
  }
});

// packages/daemon/src/command-center/server.ts
var server_exports = {};
__export(server_exports, {
  attachWsEvents: () => attachWsEvents,
  createApp: () => createApp,
  getSseMetrics: () => getSseMetrics
});
import { execFileSync as execFileSync6, execFile as execFile7 } from "node:child_process";
import { promisify as promisify3 } from "node:util";
import {
  existsSync as existsSync36,
  readFileSync as readFileSync26,
  writeFileSync as writeFileSync23,
  mkdirSync as mkdirSync23,
  unlinkSync as unlinkSync4,
  statSync as statSync9,
  renameSync as renameSync18,
  readdirSync as readdirSync10,
  openSync,
  readSync,
  closeSync
} from "node:fs";
import { join as join37, dirname as dirname16, basename as basename9 } from "node:path";
import { fileURLToPath as fileURLToPath5 } from "node:url";
import { Hono } from "hono";
import { streamSSE, stream as streamResponse } from "hono/streaming";
import { cors } from "hono/cors";
import { zValidator as zValidator2 } from "@hono/zod-validator";
import { Effect as Effect3 } from "effect";
import { realpathSync as realpathSync4 } from "node:fs";
import { homedir as homedir12, hostname as hostname2 } from "node:os";
import { isAbsolute as isAbsolute7, resolve as pathResolve2 } from "node:path";
import { randomUUID as randomUUID11 } from "node:crypto";
import { WebSocketServer } from "ws";
function resolvePackageVersion() {
  const candidates = [join37(__dirname3, "../../package.json"), join37(__dirname3, "../package.json")];
  for (const candidate of candidates) {
    try {
      const parsed = JSON.parse(readFileSync26(candidate, "utf-8"));
      if (typeof parsed.version === "string") return parsed.version;
    } catch {
    }
  }
  return "0.0.0";
}
function bearerToken(authHeader) {
  if (!authHeader?.startsWith("Bearer ")) return null;
  return authHeader.slice("Bearer ".length);
}
function requireAuth(token, localBypassToken) {
  return async (c, next) => {
    if (!token) return next();
    const url = new URL(c.req.url);
    const suppliedToken = bearerToken(c.req.header("Authorization")) ?? url.searchParams.get("token");
    if (suppliedToken === token || localBypassToken && suppliedToken === localBypassToken) {
      return next();
    }
    return c.json({ error: "Remote access token required" }, 401);
  };
}
function remoteAccessAuth(options) {
  const bindHostname = options.remoteAccess?.bindHostname ?? "127.0.0.1";
  return {
    token: bindHostname === "127.0.0.1" ? null : options.remoteAccess?.token ?? null,
    localBypassToken: options.remoteAccess?.localBypassToken ?? null
  };
}
function getSseMetrics() {
  return { ...sseMetrics };
}
function matchLogChannel(channel) {
  switch (channel) {
    case "daemon":
      return () => true;
    case "hq":
      return (entry) => entry.component.startsWith("hq") || entry.component.startsWith("remote");
    case "watchdog":
      return (entry) => entry.component.startsWith("watchdog");
    default:
      return null;
  }
}
function freezePayload(payload) {
  if (payload && typeof payload === "object") {
    for (const value of Object.values(payload)) {
      freezePayload(value);
    }
    Object.freeze(payload);
  }
  return payload;
}
function isPathInside2(path4, root) {
  if (!path4) return false;
  return path4 === root || path4.startsWith(root + "/");
}
function safePlanName(filename) {
  return filename.replace(/[^a-zA-Z0-9_\-. ]/g, "");
}
function planFilePath(projectDir, filename) {
  const safeName = safePlanName(filename);
  return join37(projectDir, "plans", safeName.endsWith(".md") ? safeName : `${safeName}.md`);
}
function writePlanAtomic(filePath, content) {
  const dir = dirname16(filePath);
  mkdirSync23(dir, { recursive: true });
  const tmpPath = join37(dir, `.${Date.now()}-${Math.random().toString(36).slice(2)}.tmp`);
  try {
    writeFileSync23(tmpPath, content);
    renameSync18(tmpPath, filePath);
  } finally {
    if (existsSync36(tmpPath)) {
      try {
        unlinkSync4(tmpPath);
      } catch {
      }
    }
  }
  return statSync9(filePath).mtimeMs;
}
function parsePlanFrontmatterSummary(content) {
  if (!content.startsWith("---\n")) return { owner: null, status: null };
  const end = content.indexOf("\n---", 4);
  if (end === -1) return { owner: null, status: null };
  let owner = null;
  let status4 = null;
  for (const line of content.slice(4, end).split("\n")) {
    const match = line.match(/^([A-Za-z][A-Za-z0-9_-]*):\s*(.+)$/);
    if (!match) continue;
    const key = match[1].toLowerCase();
    const value = match[2].trim().replace(/^["']|["']$/g, "");
    if (key === "owner") owner = value;
    if (key === "status") status4 = value.toLowerCase().replace(/\s+/g, "-");
  }
  return { owner, status: status4 };
}
function patchPlanStatus(content, status4) {
  if (content.startsWith("---\n")) {
    const end = content.indexOf("\n---", 4);
    if (end !== -1) {
      const block = content.slice(4, end);
      const rest = content.slice(end);
      if (/^status:\s*.*$/im.test(block)) {
        return `---
${block.replace(/^status:\s*.*$/im, `status: ${status4}`)}${rest}`;
      }
      return `---
status: ${status4}
${block}${rest}`;
    }
  }
  if (/\*\*Status:\*\*\s*`[^`]+`/.test(content)) {
    return content.replace(/\*\*Status:\*\*\s*`[^`]+`/, `**Status:** \`${status4}\``);
  }
  return `---
status: ${status4}
---
${content}`;
}
function isValidMilestoneTransition(from, to) {
  if (from === to) return true;
  return ALLOWED_MILESTONE_TRANSITIONS.get(from)?.has(to) ?? false;
}
function validationSummary(dir) {
  const valState = loadValidationState(dir);
  const assertions = valState ? Object.values(valState.assertions) : [];
  return {
    total: assertions.length,
    passing: assertions.filter((a) => a.status === "passing").length,
    failing: assertions.filter((a) => a.status === "failing").length,
    pending: assertions.filter((a) => a.status === "pending").length,
    blocked: assertions.filter((a) => a.status === "blocked").length
  };
}
function buildProjectStreamSnapshot(session) {
  const project = buildProjectDetail(session);
  const mission = loadMission(session.dir);
  const tasks = loadTasks(session.dir);
  const milestones = mission ? [...mission.milestones].sort((a, b) => a.order - b.order).map((milestone) => {
    const milestoneTasks = tasks.filter((task) => task.milestone === milestone.id);
    return {
      ...milestone,
      taskCount: milestoneTasks.length,
      tasksDone: milestoneTasks.filter((task) => task.status === "done").length
    };
  }) : [];
  return {
    project,
    mission: mission ? { mission, validationSummary: validationSummary(session.dir) } : null,
    milestones,
    goals: project.goals,
    tasks: project.tasks,
    skills: loadSkills(session.dir),
    agents: project.agents,
    events: readEvents(session.dir).slice(-100).reverse()
  };
}
function sandboxResolveDir(rawDir) {
  const trimmed = rawDir.trim();
  if (!trimmed) return { error: "invalid-path", message: "Path must not be empty", status: 400 };
  if (trimmed.includes("\0")) {
    return { error: "invalid-path", message: "Path contains a null byte", status: 400 };
  }
  const home = process.env.TMUX_IDE_HOME_OVERRIDE && process.env.TMUX_IDE_HOME_OVERRIDE.trim().length > 0 ? process.env.TMUX_IDE_HOME_OVERRIDE : homedir12();
  let candidate = trimmed;
  if (candidate === "~") {
    candidate = home;
  } else if (candidate.startsWith("~/")) {
    candidate = `${home.replace(/\/+$/, "")}/${candidate.slice(2)}`;
  }
  if (!isAbsolute7(candidate)) {
    return { error: "invalid-path", message: "Path must be absolute", status: 400 };
  }
  const resolved2 = pathResolve2(candidate);
  let canonical;
  try {
    canonical = realpathSync4(resolved2);
  } catch (err) {
    const code = err.code;
    if (code === "ENOENT" || code === "ENOTDIR") {
      return {
        error: "not-found",
        message: `Path "${resolved2}" does not exist`,
        status: 404
      };
    }
    throw err;
  }
  try {
    assertInsideSandbox(canonical, home);
  } catch (err) {
    if (err instanceof SandboxViolationError) {
      return { error: "outside-sandbox", message: err.message, status: 403 };
    }
    throw err;
  }
  return { canonical };
}
function createApp(options = {}) {
  const authConfig = options.authConfig ?? { method: "none", token_expiry: 86400 };
  const authService = options.authService ?? new AuthService();
  initializeDefaultChatRuntime();
  const app = new Hono();
  app.use("/*", cors());
  const remoteAuth = remoteAccessAuth(options);
  app.use("/api/*", requireAuth(remoteAuth.token, remoteAuth.localBypassToken));
  app.use("/*", authMiddleware(authService, authConfig));
  app.onError((err, c) => {
    console.error("[command-center]", err.message);
    return c.json({ error: err.message }, 500);
  });
  app.post("/api/auth/challenge", async (c) => {
    const body = await c.req.json();
    const userId = body.userId ?? authService.getCurrentUser();
    const challenge = authService.createChallenge(userId);
    return c.json(challenge);
  });
  app.post("/api/auth/verify", async (c) => {
    const body = await c.req.json();
    const result = await authService.authenticateWithSSHKey({
      publicKey: body.publicKey,
      signature: body.signature,
      challengeId: body.challengeId
    });
    if (!result.success) {
      return c.json({ error: result.error }, 401);
    }
    return c.json({ token: result.token, userId: result.userId });
  });
  app.post("/api/auth/token", async (c) => {
    if (authConfig.method !== "none") {
      return c.json({ error: "Direct token generation requires auth method 'none'" }, 403);
    }
    const body = await c.req.json();
    const userId = body.userId ?? authService.getCurrentUser();
    const token = authService.generateToken(userId);
    return c.json({ token, userId });
  });
  app.post("/api/v2/action/:name", createActionDispatcher());
  const providerStore = options.providerStore ?? makeProviderStore();
  function providerErrorStatus(err) {
    if (err.code === "not_found") return 404;
    if (err.code === "duplicate_id") return 409;
    return 400;
  }
  app.get("/api/providers", (c) => {
    return c.json({ providers: providerStore.summaries() });
  });
  app.post("/api/providers", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Body must be valid JSON" }, 400);
    }
    const parsed = ProviderInstanceZ.safeParse(body);
    if (!parsed.success) {
      return c.json(
        {
          error: "Invalid provider instance",
          issues: parsed.error.issues.map((i) => ({
            path: i.path.join("."),
            message: i.message
          }))
        },
        400
      );
    }
    try {
      const created = providerStore.add(parsed.data);
      return c.json(
        {
          provider: created.id,
          summary: providerStore.summaries().find((s) => s.id === created.id)
        },
        201
      );
    } catch (err) {
      if (err instanceof ProviderStoreError) {
        return c.json({ error: err.message, code: err.code }, providerErrorStatus(err));
      }
      throw err;
    }
  });
  app.delete("/api/providers/:id", (c) => {
    const id = c.req.param("id");
    const removed = providerStore.remove(id);
    if (!removed) {
      return c.json({ error: `Provider not found: ${id}` }, 404);
    }
    return c.json({ removed: id });
  });
  app.get("/api/widget/:name/spawn", async (c) => {
    const { resolveWidgetSpawn: resolveWidgetSpawn2, WIDGET_TYPES: WIDGET_TYPES2 } = await Promise.resolve().then(() => (init_resolve(), resolve_exports));
    const name = c.req.param("name");
    if (!WIDGET_TYPES2.includes(name)) {
      return c.json({ error: `unknown widget: ${name}`, available: WIDGET_TYPES2 }, 404);
    }
    const session = c.req.query("session");
    const dir = c.req.query("dir");
    if (!session || !dir) {
      return c.json({ error: "session and dir query params are required" }, 400);
    }
    const target = c.req.query("target") ?? null;
    const themeRaw = c.req.query("theme");
    let theme = null;
    if (themeRaw) {
      try {
        theme = JSON.parse(themeRaw);
      } catch {
        return c.json({ error: "theme must be valid JSON" }, 400);
      }
    }
    try {
      const spec = resolveWidgetSpawn2(name, {
        session,
        dir,
        target,
        theme
      });
      return c.json(spec);
    } catch (err) {
      return c.json({ error: err instanceof Error ? err.message : String(err) }, 500);
    }
  });
  const HEALTHZ_BOOTED_AT = Date.now();
  app.get("/healthz", (c) => {
    return c.json({
      ok: true,
      version: process.env.npm_package_version ?? "dev",
      uptimeMs: Date.now() - HEALTHZ_BOOTED_AT
    });
  });
  app.get("/api/sessions", (c) => {
    const sessions = discoverSessions();
    const overviews = buildOverviews(sessions);
    return c.json({ sessions: overviews });
  });
  app.get("/api/workspaces", (c) => {
    const registry = getDefaultWorkspaceRegistry();
    return c.json({ workspaces: registry.list() });
  });
  app.get("/api/workspaces/:name", (c) => {
    const name = c.req.param("name");
    const registry = getDefaultWorkspaceRegistry();
    const workspace = registry.get(name);
    if (!workspace) return c.json({ error: "Workspace not found" }, 404);
    return c.json({ workspace });
  });
  app.post("/api/workspaces", zValidator2("json", AddWorkspaceRequestSchemaZ), (c) => {
    const body = c.req.valid("json");
    const registry = getDefaultWorkspaceRegistry();
    const name = body.name ?? basename9(body.projectDir);
    if (!name || name.length === 0) {
      return c.json({ error: "Cannot derive workspace name from projectDir" }, 400);
    }
    try {
      const workspace = registry.add({
        name,
        sessionName: body.sessionName,
        projectDir: body.projectDir,
        ideConfigPath: body.ideConfigPath ?? null
      });
      return c.json({ workspace }, 201);
    } catch (err) {
      if (err instanceof WorkspaceAlreadyExistsError) {
        return c.json({ error: err.message, code: err.code }, 409);
      }
      throw err;
    }
  });
  app.get("/api/chat/providers", async (c) => {
    const providers = await discoverProviders(options.providerDiscovery);
    return c.json({ providers });
  });
  app.get("/api/threads/:threadId/plans", (c) => {
    const threadId = c.req.param("threadId");
    if (!threadId) return c.json({ error: "threadId is required" }, 400);
    const plans = getDefaultPlanStore().list(threadId);
    return c.json({ plans });
  });
  app.post(
    "/api/threads/:threadId/plans/:planId/approve",
    zValidator2("json", PlanApproveBodyZ),
    async (c) => {
      const threadId = c.req.param("threadId");
      const planId = c.req.param("planId");
      const body = c.req.valid("json");
      const orchestrator = getDefaultPlanOrchestrator();
      try {
        const result = await orchestrator.approve({
          threadId,
          planId,
          ...body.runtimeMode ? { runtimeMode: body.runtimeMode } : {}
        });
        return c.json({ plan: result.plan, turnId: result.turnId });
      } catch (err) {
        if (err instanceof PlanNotFoundError) {
          return c.json({ error: err.message, code: "plan_not_found" }, 404);
        }
        if (err instanceof PlanAlreadyImplementedError || err instanceof PlanAlreadyRejectedError || err instanceof TurnAlreadyRunningError) {
          return c.json({ error: err.message, code: err.name }, 409);
        }
        throw err;
      }
    }
  );
  app.post(
    "/api/threads/:threadId/plans/:planId/reject",
    zValidator2("json", PlanRejectBodyZ),
    (c) => {
      const threadId = c.req.param("threadId");
      const planId = c.req.param("planId");
      const body = c.req.valid("json");
      const orchestrator = getDefaultPlanOrchestrator();
      try {
        const plan = orchestrator.reject({
          threadId,
          planId,
          ...body.reason !== void 0 ? { reason: body.reason } : {}
        });
        return c.json({ plan });
      } catch (err) {
        if (err instanceof PlanNotFoundError) {
          return c.json({ error: err.message, code: "plan_not_found" }, 404);
        }
        if (err instanceof PlanAlreadyImplementedError || err instanceof PlanAlreadyRejectedError) {
          return c.json({ error: err.message, code: err.name }, 409);
        }
        throw err;
      }
    }
  );
  app.delete("/api/workspaces/:name", (c) => {
    const name = c.req.param("name");
    const registry = getDefaultWorkspaceRegistry();
    try {
      registry.remove(name);
      return c.body(null, 204);
    } catch (err) {
      if (err instanceof WorkspaceNotFoundError) {
        return c.json({ error: err.message, code: err.code }, 404);
      }
      throw err;
    }
  });
  app.get("/api/project/:name", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const detail = buildProjectDetail(session);
    const orchestratorSnapshot = buildOrchestratorSnapshot(session);
    let orchestratorConfig = null;
    try {
      const { config: config2 } = readConfig(session.dir);
      const orch = config2.orchestrator;
      if (orch) {
        orchestratorConfig = {
          enabled: orch.enabled ?? false,
          dispatchMode: orch.dispatch_mode ?? "tasks"
        };
      }
    } catch {
    }
    return c.json({ ...detail, orchestratorSnapshot, orchestratorConfig });
  });
  app.get("/api/project/:name/panes", (c) => {
    const name = c.req.param("name");
    let panes;
    try {
      panes = listSessionPanes(name);
    } catch {
      return c.json({ error: "Session not found" }, 404);
    }
    if (panes.length === 0) {
      const sessions = discoverSessions();
      if (!sessions.find((s) => s.name === name)) {
        return c.json({ error: "Session not found" }, 404);
      }
    }
    return c.json({
      panes: panes.map((p) => ({
        id: p.id,
        index: p.index,
        title: p.title,
        currentCommand: p.currentCommand,
        width: p.width,
        height: p.height,
        active: p.active,
        role: p.role,
        name: p.name,
        type: p.type
      }))
    });
  });
  app.post("/api/project/:name/task/:id", zValidator2("json", updateTaskSchema), async (c) => {
    const name = c.req.param("name");
    const taskId = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const body = c.req.valid("json");
    try {
      const { task } = updateTaskRecord(session.dir, {
        taskId,
        status: body.status,
        assignee: body.assignee,
        title: body.title,
        description: body.description,
        priority: body.priority
      });
      return c.json({ ok: true, task });
    } catch (err) {
      if (err instanceof TaskActionError && err.code === "task_not_found") {
        return c.json({ error: "Task not found" }, 404);
      }
      return c.json({ error: "Task not found" }, 404);
    }
  });
  app.post("/api/project/:name/task", zValidator2("json", createTaskSchema), async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const body = c.req.valid("json");
    const { task } = createTaskRecord(session.dir, {
      title: body.title,
      description: body.description,
      priority: body.priority,
      goalId: body.goal,
      tags: body.tags
    });
    return c.json({ ok: true, task }, 201);
  });
  app.delete("/api/project/:name/task/:id", (c) => {
    const name = c.req.param("name");
    const taskId = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    try {
      deleteTaskRecord(session.dir, taskId);
    } catch (err) {
      if (err instanceof TaskActionError && err.code === "task_not_found") {
        return c.json({ error: "Task not found" }, 404);
      }
      throw err;
    }
    return c.json({ ok: true, deleted: taskId });
  });
  app.get("/api/project/:name/plans", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const plans = loadPlans(session.dir).map((p) => {
      const filePath = join37(session.dir, "plans", `${p.name}.md`);
      const raw = existsSync36(filePath) ? readFileSync26(filePath, "utf-8") : "";
      const frontmatter = parsePlanFrontmatterSummary(raw);
      return {
        name: p.name,
        path: `${p.name}.md`,
        title: p.title,
        status: frontmatter.status ?? p.status,
        effort: p.effort ?? null,
        owner: frontmatter.owner,
        updated: existsSync36(filePath) ? statSync9(filePath).mtime.toISOString() : null,
        completed: p.completed ?? null
      };
    });
    return c.json({ plans });
  });
  app.get("/api/project/:name/plans/:filename", (c) => {
    const name = c.req.param("name");
    const filename = c.req.param("filename");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const safeName = safePlanName(filename);
    const filePath = planFilePath(session.dir, filename);
    if (!existsSync36(filePath)) {
      return c.json({ error: "Plan not found" }, 404);
    }
    const raw = readFileSync26(filePath, "utf-8");
    const { content, marks } = extractMarks(raw);
    const stats = marks ? calculateStats(marks.marks) : null;
    return c.json({
      name: safeName.replace(/\.md$/, ""),
      content,
      marks: marks?.marks ?? null,
      stats,
      mtime: statSync9(filePath).mtimeMs
    });
  });
  app.post("/api/project/:name/plans/:filename", zValidator2("json", savePlanSchema), async (c) => {
    const name = c.req.param("name");
    const filename = c.req.param("filename");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const body = c.req.valid("json");
    const filePath = planFilePath(session.dir, filename);
    const tagged = tagContent(body.content, "human");
    const mtime = writePlanAtomic(filePath, tagged);
    return c.json({ ok: true, mtime });
  });
  app.post(
    "/api/project/:name/plans/:filename/content",
    zValidator2("json", savePlanContentSchema),
    async (c) => {
      const name = c.req.param("name");
      const filename = c.req.param("filename");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) {
        return c.json({ error: "Session not found" }, 404);
      }
      const filePath = planFilePath(session.dir, filename);
      if (!existsSync36(filePath)) {
        return c.json({ error: "Plan not found" }, 404);
      }
      const body = c.req.valid("json");
      const mtime = writePlanAtomic(filePath, body.content);
      return c.json({ ok: true, mtime });
    }
  );
  app.delete("/api/project/:name/plans/:filename", (c) => {
    const name = c.req.param("name");
    const filename = c.req.param("filename");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const safeName = safePlanName(filename);
    const filePath = planFilePath(session.dir, filename);
    if (!existsSync36(filePath)) {
      return c.json({ error: "Plan not found" }, 404);
    }
    unlinkSync4(filePath);
    return c.json({ ok: true, deleted: safeName });
  });
  app.post("/api/project/:name/plans/:filename/done", (c) => {
    const name = c.req.param("name");
    const filename = c.req.param("filename");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const safeName = filename.replace(/[^a-zA-Z0-9_\-. ]/g, "").replace(/\.md$/, "");
    const result = markPlanDone(session.dir, safeName);
    if (!result) {
      return c.json({ error: "Plan not found" }, 404);
    }
    return c.json({ ok: true, plan: result });
  });
  app.post("/api/project/:name/plans/:filename/status", async (c) => {
    const name = c.req.param("name");
    const filename = c.req.param("filename");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const status4 = body?.status;
    if (status4 !== "pending" && status4 !== "in-progress" && status4 !== "done" && status4 !== "archived") {
      return c.json({ error: "Invalid status" }, 400);
    }
    if (status4 === "done") {
      const safeName = safePlanName(filename).replace(/\.md$/, "");
      const result = markPlanDone(session.dir, safeName);
      if (!result) return c.json({ error: "Plan not found" }, 404);
      return c.json({ ok: true, plan: result });
    }
    const filePath = planFilePath(session.dir, filename);
    if (!existsSync36(filePath)) {
      return c.json({ error: "Plan not found" }, 404);
    }
    const patched = patchPlanStatus(readFileSync26(filePath, "utf-8"), status4);
    const mtime = writePlanAtomic(filePath, patched);
    return c.json({ ok: true, mtime });
  });
  app.get("/api/project/:name/checkpoints", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const taskId = c.req.query("task");
    const list = taskId ? loadCheckpointsForTask(session.dir, taskId) : loadCheckpoints(session.dir);
    return c.json({ checkpoints: list });
  });
  app.get("/api/project/:name/checkpoints/:id", (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const cp = loadCheckpoint(session.dir, id);
    if (!cp) return c.json({ error: "Checkpoint not found" }, 404);
    return c.json({ checkpoint: cp });
  });
  app.post("/api/project/:name/checkpoints", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = await c.req.json();
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const id = nextCheckpointId(session.dir);
    const checkpoint = {
      id,
      taskId: body.taskId ?? "",
      title: body.title ?? `Checkpoint ${id}`,
      description: body.description ?? "",
      status: "pending",
      createdBy: body.createdBy ?? "",
      reviewedBy: null,
      created: now,
      updated: now,
      diff: body.diff ?? null,
      files: body.files ?? [],
      comments: []
    };
    saveCheckpoint(session.dir, checkpoint);
    return c.json({ ok: true, checkpoint }, 201);
  });
  app.post("/api/project/:name/checkpoints/:id", async (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const existing = loadCheckpoint(session.dir, id);
    if (!existing) return c.json({ error: "Checkpoint not found" }, 404);
    const body = await c.req.json();
    const updated = {
      ...existing,
      ...body,
      id: existing.id,
      created: existing.created,
      updated: (/* @__PURE__ */ new Date()).toISOString()
    };
    saveCheckpoint(session.dir, updated);
    return c.json({ ok: true, checkpoint: updated });
  });
  app.delete("/api/project/:name/checkpoints/:id", (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    if (!deleteCheckpoint(session.dir, id)) return c.json({ error: "Checkpoint not found" }, 404);
    return c.json({ ok: true, deleted: id });
  });
  const turnDiffProjection = options.turnDiffProjection ?? getDefaultTurnDiffProjection();
  app.get("/api/project/:name/turn-diffs/aggregate", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    if (!sessions.find((s) => s.name === name)) {
      return c.json({ error: "Session not found" }, 404);
    }
    const threadId = c.req.query("threadId");
    if (!threadId) {
      return c.json({ error: "threadId query parameter is required" }, 400);
    }
    return c.json(turnDiffProjection.aggregateForThread(threadId));
  });
  app.get("/api/project/:name/turn-diffs/:turnId", (c) => {
    const name = c.req.param("name");
    const turnId = c.req.param("turnId");
    const sessions = discoverSessions();
    if (!sessions.find((s) => s.name === name)) {
      return c.json({ error: "Session not found" }, 404);
    }
    return c.json({ turnId, entries: turnDiffProjection.listForTurn(turnId) });
  });
  app.get("/api/project/:name/turn-diffs", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    if (!sessions.find((s) => s.name === name)) {
      return c.json({ error: "Session not found" }, 404);
    }
    const threadId = c.req.query("threadId");
    if (!threadId) {
      return c.json({ error: "threadId query parameter is required" }, 400);
    }
    return c.json({ threadId, byTurn: turnDiffProjection.listForThread(threadId) });
  });
  app.get("/api/project/:name/reviews", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const taskId = c.req.query("task");
    const list = taskId ? loadReviewsForTask(session.dir, taskId) : loadReviews(session.dir);
    return c.json({ reviews: list });
  });
  app.get("/api/project/:name/reviews/:id", (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const review = loadReview(session.dir, id);
    if (!review) return c.json({ error: "Review not found" }, 404);
    return c.json({ review });
  });
  app.post("/api/project/:name/reviews", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = await c.req.json();
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const id = nextReviewId(session.dir);
    const review = {
      id,
      taskId: body.taskId ?? "",
      checkpointId: body.checkpointId ?? null,
      title: body.title ?? `Review ${id}`,
      description: body.description ?? "",
      status: "open",
      requestedBy: body.requestedBy ?? "",
      reviewer: body.reviewer ?? null,
      created: now,
      updated: now,
      comments: []
    };
    saveReview(session.dir, review);
    return c.json({ ok: true, review }, 201);
  });
  app.post("/api/project/:name/reviews/:id", async (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const existing = loadReview(session.dir, id);
    if (!existing) return c.json({ error: "Review not found" }, 404);
    const body = await c.req.json();
    const updated = {
      ...existing,
      ...body,
      id: existing.id,
      created: existing.created,
      updated: (/* @__PURE__ */ new Date()).toISOString()
    };
    saveReview(session.dir, updated);
    return c.json({ ok: true, review: updated });
  });
  app.post("/api/project/:name/reviews/:id/comment", async (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const existing = loadReview(session.dir, id);
    if (!existing) return c.json({ error: "Review not found" }, 404);
    const body = await c.req.json();
    const comment = {
      author: body.author ?? "",
      body: body.body ?? "",
      created: (/* @__PURE__ */ new Date()).toISOString()
    };
    existing.comments.push(comment);
    existing.updated = comment.created;
    saveReview(session.dir, existing);
    return c.json({ ok: true, review: existing });
  });
  app.delete("/api/project/:name/reviews/:id", (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    if (!deleteReview(session.dir, id)) return c.json({ error: "Review not found" }, 404);
    return c.json({ ok: true, deleted: id });
  });
  app.get("/api/project/:name/files", async (c) => {
    const { createIgnoreFilter: createIgnoreFilter2, readDirectory: readDirectory2 } = await Promise.resolve().then(() => (init_files(), files_exports));
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const ig = createIgnoreFilter2(session.dir);
    const pathParam = c.req.query("path");
    if (pathParam !== void 0) {
      const relPath = pathParam.replace(/^\/+/g, "");
      const requested = relPath === "" ? session.dir : pathResolve2(session.dir, relPath);
      let resolvedRoot;
      let resolvedTarget;
      try {
        resolvedRoot = realpathSync4(session.dir);
      } catch {
        return c.json({ error: "Session directory not accessible" }, 500);
      }
      try {
        resolvedTarget = realpathSync4(requested);
      } catch {
        return c.json({ tree: [], truncated: false });
      }
      if (!resolvedTarget.startsWith(resolvedRoot + "/") && resolvedTarget !== resolvedRoot) {
        return c.json({ error: "Path outside session" }, 403);
      }
      const entries = readDirectory2(resolvedTarget, session.dir, ig, false);
      const children = entries.map((e) => ({
        path: e.path,
        name: e.name,
        isDirectory: e.isDir
      }));
      return c.json({ tree: children, truncated: false });
    }
    const MAX_DEPTH = 5;
    const MAX_NODES = 5e3;
    let nodeBudget = MAX_NODES;
    const walk = (dir, depth) => {
      if (nodeBudget <= 0) return [];
      const entries = readDirectory2(dir, session.dir, ig, false);
      const out = [];
      for (const e of entries) {
        if (nodeBudget <= 0) break;
        nodeBudget--;
        const node = { path: e.path, name: e.name, isDirectory: e.isDir };
        if (e.isDir) {
          if (depth + 1 < MAX_DEPTH) {
            node.children = walk(e.absolutePath, depth + 1);
          } else {
            node.truncated = true;
          }
        }
        out.push(node);
      }
      return out;
    };
    const tree = walk(session.dir, 0);
    return c.json({ tree, maxDepth: MAX_DEPTH, truncated: nodeBudget <= 0 });
  });
  app.get("/api/project/:name/diff", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const sourceParam = (c.req.query("source") ?? "working").toLowerCase();
    const source = sourceParam === "staged" || sourceParam === "pr" ? sourceParam : "working";
    const DIFF_MAX_BUFFER = 64 * 1024 * 1024;
    function runGit(args) {
      try {
        return execFileSync6("git", args, {
          cwd: session.dir,
          encoding: "utf-8",
          stdio: ["ignore", "pipe", "ignore"],
          maxBuffer: DIFF_MAX_BUFFER
        });
      } catch {
        return null;
      }
    }
    function parseNumstat2(out) {
      if (!out) return [];
      return out.split("\n").filter(Boolean).map((line) => {
        const [added, removed, file] = line.split("	");
        return {
          file,
          additions: parseInt(added, 10) || 0,
          deletions: parseInt(removed, 10) || 0
        };
      });
    }
    function resolvePrBase() {
      let baseBranch2 = null;
      try {
        const out = execFileSync6("gh", ["pr", "view", "--json", "baseRefName"], {
          cwd: session.dir,
          encoding: "utf-8",
          stdio: ["ignore", "pipe", "ignore"],
          maxBuffer: 1024 * 64
        });
        const parsed = JSON.parse(out);
        if (parsed.baseRefName) baseBranch2 = parsed.baseRefName;
      } catch {
      }
      if (!baseBranch2) {
        for (const candidate of ["main", "master", "develop"]) {
          const exists = runGit(["rev-parse", "--verify", candidate]);
          if (exists !== null) {
            baseBranch2 = candidate;
            break;
          }
        }
      }
      if (!baseBranch2) return { baseBranch: null, mergeBase: null };
      const mergeBaseOut = runGit(["merge-base", "HEAD", baseBranch2]);
      const mergeBase = mergeBaseOut?.trim() ?? null;
      return { baseBranch: baseBranch2, mergeBase: mergeBase || null };
    }
    let diff;
    let files;
    let originalRef = "HEAD";
    let modifiedRef;
    let baseBranch = null;
    if (source === "staged") {
      diff = runGit(["diff", "--cached", "HEAD"]) ?? runGit(["diff", "--cached"]) ?? "";
      files = parseNumstat2(
        runGit(["diff", "--cached", "--numstat", "HEAD"]) ?? runGit(["diff", "--cached", "--numstat"])
      );
      modifiedRef = "STAGED";
    } else if (source === "pr") {
      const { baseBranch: base, mergeBase } = resolvePrBase();
      baseBranch = base;
      if (mergeBase) {
        diff = runGit(["diff", `${mergeBase}...HEAD`]) ?? "";
        files = parseNumstat2(runGit(["diff", "--numstat", `${mergeBase}...HEAD`]));
        originalRef = mergeBase;
      } else {
        diff = runGit(["diff", "HEAD"]) ?? "";
        files = parseNumstat2(runGit(["diff", "--numstat", "HEAD"]));
      }
      modifiedRef = "HEAD";
    } else {
      diff = runGit(["diff", "HEAD"]) ?? runGit(["diff"]) ?? "";
      files = parseNumstat2(runGit(["diff", "--numstat", "HEAD"]) ?? runGit(["diff", "--numstat"]));
      modifiedRef = "WORKING";
    }
    return c.json({ diff, files, source, originalRef, modifiedRef, baseBranch });
  });
  app.get("/api/project/:name/diff/:file{.+}", (c) => {
    const name = c.req.param("name");
    const file = c.req.param("file");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const PER_FILE_MAX_BUFFER = 64 * 1024 * 1024;
    let diff = "";
    try {
      diff = execFileSync6("git", ["diff", "HEAD", "--", file], {
        cwd: session.dir,
        encoding: "utf-8",
        stdio: ["ignore", "pipe", "ignore"],
        maxBuffer: PER_FILE_MAX_BUFFER
      });
    } catch {
    }
    if (!diff) {
      try {
        diff = execFileSync6("git", ["diff", "--", file], {
          cwd: session.dir,
          encoding: "utf-8",
          stdio: ["ignore", "pipe", "ignore"],
          maxBuffer: PER_FILE_MAX_BUFFER
        });
      } catch {
      }
    }
    return c.json({ file, diff });
  });
  app.get("/api/project/:name/git/file", (c) => {
    const name = c.req.param("name");
    const path4 = c.req.query("path") ?? "";
    const ref = (c.req.query("ref") ?? "HEAD").trim();
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    if (!path4) return c.json({ error: "Missing ?path=" }, 400);
    if (path4.startsWith("/") || path4.split("/").some((seg) => seg === ".." || seg === ".")) {
      return c.json({ error: "Path escapes workspace" }, 403);
    }
    if (ref !== "STAGED" && ref !== "WORKING" && !/^[A-Za-z0-9_./-]+$/.test(ref)) {
      return c.json({ error: "Invalid ref" }, 400);
    }
    const PER_FILE_MAX_BUFFER = 64 * 1024 * 1024;
    let content;
    let exists = true;
    try {
      if (ref === "WORKING") {
        const requested = pathResolve2(session.dir, path4);
        let resolvedRoot;
        try {
          resolvedRoot = realpathSync4(session.dir);
        } catch {
          return c.json({ error: "Session directory not accessible" }, 500);
        }
        let resolvedTarget;
        try {
          resolvedTarget = realpathSync4(requested);
        } catch {
          return c.json({ path: path4, ref, exists: false, content: "" });
        }
        if (!resolvedTarget.startsWith(resolvedRoot + "/") && resolvedTarget !== resolvedRoot) {
          return c.json({ error: "Path escapes workspace" }, 403);
        }
        try {
          content = readFileSync26(resolvedTarget, "utf-8");
        } catch {
          return c.json({ path: path4, ref, exists: false, content: "" });
        }
      } else {
        const spec = ref === "STAGED" ? `:${path4}` : `${ref}:${path4}`;
        try {
          content = execFileSync6("git", ["show", spec], {
            cwd: session.dir,
            encoding: "utf-8",
            stdio: ["ignore", "pipe", "ignore"],
            maxBuffer: PER_FILE_MAX_BUFFER
          });
        } catch {
          exists = false;
          content = "";
        }
      }
    } catch (err) {
      return c.json({ error: err instanceof Error ? err.message : String(err) }, 500);
    }
    return c.json({ path: path4, ref, exists, content });
  });
  app.put("/api/project/:name/file", async (c) => {
    const name = c.req.param("name");
    const path4 = c.req.query("path") ?? "";
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    if (!path4) return c.json({ error: "Missing ?path=" }, 400);
    if (path4.startsWith("/") || path4.split("/").some((seg) => seg === ".." || seg === ".")) {
      return c.json({ error: "Path escapes workspace" }, 403);
    }
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    if (typeof body?.content !== "string") {
      return c.json({ error: "Body must be `{ content: string }`" }, 400);
    }
    let resolvedRoot;
    try {
      resolvedRoot = realpathSync4(session.dir);
    } catch {
      return c.json({ error: "Session directory not accessible" }, 500);
    }
    const requested = pathResolve2(session.dir, path4);
    const parent = requested.substring(0, requested.lastIndexOf("/"));
    let resolvedParent;
    try {
      resolvedParent = realpathSync4(parent);
    } catch {
      return c.json({ error: "Parent directory not found" }, 404);
    }
    if (!resolvedParent.startsWith(resolvedRoot + "/") && resolvedParent !== resolvedRoot) {
      return c.json({ error: "Path escapes workspace" }, 403);
    }
    const target = `${resolvedParent}/${requested.substring(requested.lastIndexOf("/") + 1)}`;
    try {
      writeFileSync23(target, body.content, "utf-8");
    } catch (err) {
      return c.json({ error: err instanceof Error ? err.message : String(err) }, 500);
    }
    let bytes = 0;
    try {
      bytes = statSync9(target).size;
    } catch {
    }
    return c.json({ ok: true, path: path4, bytes });
  });
  app.get("/api/project/:name/search", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const parsed = parseSearchQuery(c.req.query());
    if (!parsed.ok) return c.json({ error: parsed.error }, 400);
    let searchRoot;
    try {
      searchRoot = realpathSync4(session.dir);
    } catch {
      return c.json({ error: "Session directory not accessible" }, 500);
    }
    const rgPath = await resolveRipgrepPath();
    c.header("Content-Type", "application/x-ndjson");
    c.header("Cache-Control", "no-store");
    c.header("X-Accel-Buffering", "no");
    return streamResponse(c, async (stream) => {
      const controller = new AbortController();
      const onAbort = () => controller.abort();
      stream.onAbort(onAbort);
      try {
        for await (const frame of runSearch({
          rgPath,
          query: parsed.query,
          searchRoot,
          signal: controller.signal
        })) {
          if (controller.signal.aborted) break;
          await stream.writeln(JSON.stringify(frame));
        }
      } catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        await stream.writeln(
          JSON.stringify({ type: "error", message, fatal: true })
        );
      }
    });
  });
  app.post("/api/project/:name/search/replace", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Body must be valid JSON" }, 400);
    }
    const parsed = ReplaceRequestZ.safeParse(body);
    if (!parsed.success) {
      return c.json(
        {
          error: "Invalid replace request",
          issues: parsed.error.issues.map((i) => ({ path: i.path, message: i.message }))
        },
        400
      );
    }
    let searchRoot;
    try {
      searchRoot = realpathSync4(session.dir);
    } catch {
      return c.json({ error: "Session directory not accessible" }, 500);
    }
    const result = executeReplace(searchRoot, parsed.data);
    return c.json(result);
  });
  app.get("/api/project/:name/preview/:file{.+}", (c) => {
    const name = c.req.param("name");
    const file = c.req.param("file");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const requested = pathResolve2(session.dir, file);
    let resolvedRoot;
    let resolvedTarget;
    try {
      resolvedRoot = realpathSync4(session.dir);
    } catch {
      return c.json({ error: "Session directory not accessible" }, 500);
    }
    try {
      resolvedTarget = realpathSync4(requested);
    } catch {
      return c.json({ file, exists: false, content: "" });
    }
    if (!resolvedTarget.startsWith(resolvedRoot + "/") && resolvedTarget !== resolvedRoot) {
      return c.json({ error: "Path outside session" }, 403);
    }
    let stat;
    try {
      stat = statSync9(resolvedTarget);
    } catch {
      return c.json({ file, exists: false, content: "" });
    }
    if (!stat.isFile()) {
      return c.json({ error: "Not a regular file" }, 400);
    }
    const MAX_PREVIEW_BYTES = 1e6;
    let content;
    let truncated = false;
    try {
      if (stat.size > MAX_PREVIEW_BYTES) {
        const fd = openSync(resolvedTarget, "r");
        try {
          const buf = Buffer.alloc(MAX_PREVIEW_BYTES);
          const bytesRead = readSync(fd, buf, 0, MAX_PREVIEW_BYTES, 0);
          content = buf.subarray(0, bytesRead).toString("utf-8");
        } finally {
          closeSync(fd);
        }
        truncated = true;
      } else {
        content = readFileSync26(resolvedTarget, "utf-8");
      }
    } catch (err) {
      return c.json({ error: err instanceof Error ? err.message : "read failed" }, 500);
    }
    return c.json({
      file,
      exists: true,
      content,
      size: stat.size,
      mtimeMs: stat.mtimeMs,
      truncated
    });
  });
  app.get("/api/project/:name/image/:file{.+}", (c) => {
    const name = c.req.param("name");
    const file = c.req.param("file");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const requested = pathResolve2(session.dir, file);
    let resolvedRoot;
    let resolvedTarget;
    try {
      resolvedRoot = realpathSync4(session.dir);
    } catch {
      return c.json({ error: "Session directory not accessible" }, 500);
    }
    try {
      resolvedTarget = realpathSync4(requested);
    } catch {
      return c.json({ error: "Not found" }, 404);
    }
    if (!resolvedTarget.startsWith(resolvedRoot + "/") && resolvedTarget !== resolvedRoot) {
      return c.json({ error: "Path outside session" }, 403);
    }
    let stat;
    try {
      stat = statSync9(resolvedTarget);
    } catch {
      return c.json({ error: "Not found" }, 404);
    }
    if (!stat.isFile()) return c.json({ error: "Not a regular file" }, 400);
    if (stat.size > 25 * 1024 * 1024) {
      return c.json({ error: "Image too large", size: stat.size }, 413);
    }
    const ext = file.split(".").pop()?.toLowerCase() ?? "";
    const mime = ext === "png" ? "image/png" : ext === "jpg" || ext === "jpeg" ? "image/jpeg" : ext === "gif" ? "image/gif" : ext === "webp" ? "image/webp" : ext === "bmp" ? "image/bmp" : ext === "ico" ? "image/x-icon" : ext === "svg" ? "image/svg+xml" : "application/octet-stream";
    let dataUrl;
    try {
      const bytes = readFileSync26(resolvedTarget);
      dataUrl = `data:${mime};base64,${bytes.toString("base64")}`;
    } catch (err) {
      return c.json({ error: err instanceof Error ? err.message : "read failed" }, 500);
    }
    return c.json({ file, dataUrl, mime, size: stat.size });
  });
  app.get("/api/project/:name/milestones", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const mission = loadMission(session.dir);
    if (!mission) return c.json({ milestones: [] });
    const tasks = loadTasks(session.dir);
    const milestones = [...mission.milestones].sort((a, b) => a.order - b.order).map((m) => {
      const mTasks = tasks.filter((t) => t.milestone === m.id);
      return {
        ...m,
        taskCount: mTasks.length,
        tasksDone: mTasks.filter((t) => t.status === "done").length
      };
    });
    return c.json({ milestones });
  });
  app.get("/api/project/:name/milestones/:id", (c) => {
    const name = c.req.param("name");
    const milestoneId = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const mission = loadMission(session.dir);
    if (!mission) return c.json({ error: "No mission" }, 404);
    const milestone = mission.milestones.find((m) => m.id === milestoneId);
    if (!milestone) return c.json({ error: "Milestone not found" }, 404);
    const tasks = loadTasks(session.dir).filter((t) => t.milestone === milestoneId);
    return c.json({ milestone, tasks });
  });
  app.post(
    "/api/project/:name/milestones",
    zValidator2("json", createMilestoneSchema),
    async (c) => {
      const name = c.req.param("name");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const mission = loadMission(session.dir);
      if (!mission) return c.json({ error: "No mission" }, 404);
      const body = c.req.valid("json");
      const id = `M${body.sequence}`;
      if (mission.milestones.find((m) => m.id === id)) {
        return c.json({ error: `Milestone ${id} already exists` }, 409);
      }
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const hasActive = mission.milestones.some(
        (m) => m.status === "active" || m.status === "done"
      );
      const milestone = {
        id,
        title: body.title,
        description: body.description ?? "",
        status: hasActive ? "locked" : "active",
        order: body.sequence,
        created: now,
        updated: now
      };
      mission.milestones.push(milestone);
      mission.milestones.sort((a, b) => a.order - b.order);
      mission.updated = now;
      saveMission(session.dir, mission);
      return c.json({ ok: true, milestone }, 201);
    }
  );
  app.post(
    "/api/project/:name/milestones/:id",
    zValidator2("json", updateMilestoneSchema),
    async (c) => {
      const name = c.req.param("name");
      const milestoneId = c.req.param("id");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const mission = loadMission(session.dir);
      if (!mission) return c.json({ error: "No mission" }, 404);
      const milestone = mission.milestones.find((m) => m.id === milestoneId);
      if (!milestone) return c.json({ error: "Milestone not found" }, 404);
      const body = c.req.valid("json");
      if (body.status && !isValidMilestoneTransition(milestone.status, body.status)) {
        return c.json(
          {
            error: `Invalid milestone status transition: ${milestone.status} -> ${body.status}`
          },
          409
        );
      }
      if (body.status) milestone.status = body.status;
      if (body.title) milestone.title = body.title;
      if (body.description !== void 0) milestone.description = body.description;
      milestone.updated = (/* @__PURE__ */ new Date()).toISOString();
      mission.updated = milestone.updated;
      saveMission(session.dir, mission);
      return c.json({ ok: true, milestone });
    }
  );
  app.get("/api/project/:name/validation", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const state = loadValidationState(session.dir);
    const contract = loadValidationContract(session.dir);
    return c.json({ contract: contract ?? null, state: state ?? null });
  });
  app.get("/api/project/:name/validation/coverage", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return c.json(checkCoverage(session.dir));
  });
  app.post(
    "/api/project/:name/validation/assert/:assertId",
    zValidator2("json", updateAssertionSchema),
    async (c) => {
      const name = c.req.param("name");
      const assertId = c.req.param("assertId");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const body = c.req.valid("json");
      ensureTasksDir(session.dir);
      const state = loadValidationState(session.dir) ?? { assertions: {}, lastVerified: null };
      state.assertions[assertId] = {
        status: body.status,
        verifiedBy: body.verifiedBy ?? null,
        verifiedAt: (/* @__PURE__ */ new Date()).toISOString(),
        evidence: body.evidence ?? null,
        blockedBy: body.status === "blocked" ? body.evidence ?? null : null
      };
      state.lastVerified = (/* @__PURE__ */ new Date()).toISOString();
      saveValidationState(session.dir, state);
      return c.json({ ok: true, assertionId: assertId, ...state.assertions[assertId] });
    }
  );
  app.get("/api/project/:name/research", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const state = loadResearchState(session.dir);
    const tasks = loadTasks(session.dir);
    const activeTask = state.activeResearchTaskId != null ? tasks.find((task) => task.id === state.activeResearchTaskId) ?? null : null;
    const findings = tasks.filter((task) => task.tags.includes("research") && task.status === "done").sort((a, b) => Date.parse(b.updated) - Date.parse(a.updated)).slice(0, 10);
    return c.json({ state, activeTask, findings });
  });
  app.post(
    "/api/project/:name/research/trigger",
    zValidator2("json", triggerResearchSchema),
    async (c) => {
      const name = c.req.param("name");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const body = c.req.valid("json");
      const tasks = loadTasks(session.dir);
      const researchState = loadResearchState(session.dir);
      let maxConcurrentAgents = 10;
      let masterPane = null;
      let researchEnabled = true;
      try {
        const { config: config2 } = readConfig(session.dir);
        maxConcurrentAgents = config2.orchestrator?.max_concurrent_agents ?? 10;
        masterPane = config2.orchestrator?.master_pane ?? null;
        researchEnabled = config2.orchestrator?.research?.enabled ?? true;
      } catch {
      }
      const task = dispatchResearch(
        {
          session: name,
          dir: session.dir,
          masterPane,
          maxConcurrentAgents,
          research: { enabled: researchEnabled }
        },
        {
          lastActivity: /* @__PURE__ */ new Map(),
          previousTasks: /* @__PURE__ */ new Map(),
          claimedTasks: /* @__PURE__ */ new Set(),
          taskClaimTimes: /* @__PURE__ */ new Map(),
          inflightDispatches: /* @__PURE__ */ new Map(),
          completedDispatches: /* @__PURE__ */ new Set(),
          stallNudges: /* @__PURE__ */ new Map(),
          totalDispatched: 0,
          totalCompleted: 0,
          totalFailed: 0,
          lastTickMs: 0,
          ticking: false,
          idleAgents: 0,
          queuedDispatches: 0,
          lastError: null,
          lastReconcileMs: 0
        },
        researchState,
        tasks,
        listSessionPanes(name),
        {
          type: body.type,
          reason: `Manual research trigger: ${body.type}`
        }
      );
      if (!task) {
        return c.json({ error: `Unable to dispatch research trigger "${body.type}"` }, 409);
      }
      return c.json({ ok: true, task }, 201);
    }
  );
  app.get("/api/project/:name/skills", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const skills = loadSkills(session.dir);
    return c.json({ skills });
  });
  app.get("/api/project/:name/skills/:skillName", (c) => {
    const name = c.req.param("name");
    const skillName = c.req.param("skillName");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const skill = loadSkill(session.dir, skillName);
    if (!skill) return c.json({ error: "Skill not found" }, 404);
    return c.json({ skill });
  });
  app.post("/api/project/:name/skill", zValidator2("json", createSkillSchema), (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = c.req.valid("json");
    if (projectSkillExists(session.dir, body.name)) {
      return c.json({ error: `Skill "${body.name}" already exists` }, 409);
    }
    try {
      const skill = writeSkillFromFields(session.dir, body);
      return c.json({ skill }, 201);
    } catch (err) {
      return c.json({ error: err.message }, 400);
    }
  });
  app.put("/api/project/:name/skill/:skillName", zValidator2("json", updateSkillSchema), (c) => {
    const name = c.req.param("name");
    const skillName = c.req.param("skillName");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const existing = loadSkill(session.dir, skillName);
    if (!existing) return c.json({ error: "Skill not found" }, 404);
    const body = c.req.valid("json");
    try {
      const skill = writeSkillFromFields(session.dir, {
        name: skillName,
        role: body.role ?? existing.role,
        description: body.description ?? existing.description,
        specialties: body.specialties ?? [...existing.specialties],
        body: body.body ?? existing.body
      });
      return c.json({ skill });
    } catch (err) {
      return c.json({ error: err.message }, 400);
    }
  });
  app.delete("/api/project/:name/skill/:skillName", (c) => {
    const name = c.req.param("name");
    const skillName = c.req.param("skillName");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    try {
      const removed = deleteSkill(session.dir, skillName);
      if (!removed) return c.json({ error: "Skill not found" }, 404);
      return c.json({ ok: true });
    } catch (err) {
      return c.json({ error: err.message }, 400);
    }
  });
  app.get("/api/project/:name/git/status", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return Effect3.runPromise(
      status2(session.dir).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (s) => c.json({ status: s })
        })
      )
    );
  });
  app.get("/api/project/:name/git/branches", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return Effect3.runPromise(
      branches(session.dir).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (payload) => c.json(payload)
        })
      )
    );
  });
  app.get("/api/project/:name/git/commits", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const base = c.req.query("base")?.trim() || void 0;
    const limitRaw = c.req.query("limit");
    const limit = limitRaw ? Number.parseInt(limitRaw, 10) : void 0;
    return Effect3.runPromise(
      commits(session.dir, {
        ...base ? { base } : {},
        ...limit && Number.isFinite(limit) ? { limit } : {}
      }).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (payload) => c.json(payload)
        })
      )
    );
  });
  app.get("/api/project/:name/git/commit/:sha/diff", async (c) => {
    const name = c.req.param("name");
    const sha = c.req.param("sha");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return Effect3.runPromise(
      commitDiff(session.dir, sha).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (payload) => c.json(payload)
        })
      )
    );
  });
  app.get("/api/project/:name/git/range-diff", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const base = c.req.query("base")?.trim() || "main";
    return Effect3.runPromise(
      rangeDiff(session.dir, base).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (payload) => c.json(payload)
        })
      )
    );
  });
  app.post(
    "/api/project/:name/git/checkout",
    zValidator2("json", checkoutRequestSchema),
    async (c) => {
      const name = c.req.param("name");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const body = c.req.valid("json");
      return Effect3.runPromise(
        checkout(session.dir, body).pipe(
          Effect3.match({
            onFailure: (err) => c.json({ error: toPayload(err) }, 400),
            onSuccess: (r) => c.json({ ok: true, currentBranch: r.currentBranch })
          })
        )
      );
    }
  );
  app.post("/api/project/:name/git/commit", zValidator2("json", commitRequestSchema), async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = c.req.valid("json");
    return Effect3.runPromise(
      commit2(session.dir, body).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (r) => c.json({ ok: true, sha: r.sha })
        })
      )
    );
  });
  app.post("/api/project/:name/git/push", zValidator2("json", pushRequestSchema), async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = c.req.valid("json");
    return Effect3.runPromise(
      push(session.dir, body).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: (r) => c.json({ ok: true, remote: r.remote, branch: r.branch })
        })
      )
    );
  });
  app.post("/api/project/:name/git/stage", zValidator2("json", stageRequestSchema), async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = c.req.valid("json");
    return Effect3.runPromise(
      stage(session.dir, body.paths).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload(err) }, 400),
          onSuccess: () => c.json({ ok: true })
        })
      )
    );
  });
  app.post(
    "/api/project/:name/git/unstage",
    zValidator2("json", unstageRequestSchema),
    async (c) => {
      const name = c.req.param("name");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const body = c.req.valid("json");
      return Effect3.runPromise(
        unstage(session.dir, body.paths).pipe(
          Effect3.match({
            onFailure: (err) => c.json({ error: toPayload(err) }, 400),
            onSuccess: () => c.json({ ok: true })
          })
        )
      );
    }
  );
  app.get("/api/project/:name/git/checks", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const ref = c.req.query("ref") ?? void 0;
    return Effect3.runPromise(
      listChecks(session.dir, ref).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload2(err) }, 400),
          onSuccess: (payload) => c.json(payload)
        })
      )
    );
  });
  app.get("/api/project/:name/git/github-status", async (c) => {
    return Effect3.runPromise(status3().pipe(Effect3.map((status4) => c.json(status4))));
  });
  app.post("/api/project/:name/git/pr", zValidator2("json", createPrRequestSchema), async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const body = c.req.valid("json");
    return Effect3.runPromise(
      createPullRequest(session.dir, body).pipe(
        Effect3.match({
          onFailure: (err) => c.json({ error: toPayload2(err) }, 400),
          onSuccess: (pr) => c.json({ ok: true, pr })
        })
      )
    );
  });
  app.get("/api/project/:name/terminals", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const records = loadTerminals(session.dir);
    const terminals = records.map((t) => {
      const bridge = defaultPtyBridgeRegistry.peek(t.id);
      let runtime = { running: false };
      if (bridge) {
        const cols = typeof bridge.cols === "number" ? bridge.cols : void 0;
        const rows = typeof bridge.rows === "number" ? bridge.rows : void 0;
        const replay = typeof bridge.getReplayBuffer === "function" ? bridge.getReplayBuffer().byteLength : void 0;
        runtime = {
          running: bridge.running !== false,
          ...cols !== void 0 ? { cols } : {},
          ...rows !== void 0 ? { rows } : {},
          ...replay !== void 0 ? { replayBytes: replay } : {}
        };
      }
      return { ...t, runtime };
    });
    return c.json({ terminals });
  });
  app.post(
    "/api/project/:name/terminals",
    zValidator2("json", terminalCreateRequestSchema),
    async (c) => {
      const name = c.req.param("name");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      const body = c.req.valid("json");
      let id = body.id;
      let scripted = false;
      const kind = body.kind ?? "shell";
      if (!id && body.script) {
        id = await createScriptTerminalId({
          projectId: name,
          scopeId: body.scopeId,
          kind,
          script: body.script
        });
        scripted = true;
      }
      if (!id) id = randomUUID11();
      try {
        const upsertInput = {
          id,
          projectId: name,
          scopeId: body.scopeId,
          name: body.name,
          kind
        };
        if (scripted) upsertInput.scripted = true;
        const record = upsertTerminal(session.dir, upsertInput);
        broadcastTerminalsChanged(name);
        return c.json({ ok: true, terminal: record });
      } catch (err) {
        return c.json({ error: err.message }, 400);
      }
    }
  );
  app.post(
    "/api/project/:name/terminals/:id/rename",
    zValidator2("json", terminalRenameRequestSchema),
    async (c) => {
      const name = c.req.param("name");
      const id = c.req.param("id");
      const sessions = discoverSessions();
      const session = sessions.find((s) => s.name === name);
      if (!session) return c.json({ error: "Session not found" }, 404);
      try {
        const record = renameTerminal(session.dir, id, c.req.valid("json").name);
        if (!record) return c.json({ error: "Terminal not found" }, 404);
        broadcastTerminalsChanged(name);
        return c.json({ ok: true, terminal: record });
      } catch (err) {
        return c.json({ error: err.message }, 400);
      }
    }
  );
  app.delete("/api/project/:name/terminals/:id", async (c) => {
    const name = c.req.param("name");
    const id = c.req.param("id");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const removedRecord = deleteTerminal(session.dir, id);
    const killed = defaultPtyBridgeRegistry.delete(id);
    if (!removedRecord && !killed) {
      return c.json({ error: "Terminal not found" }, 404);
    }
    broadcastTerminalsChanged(name);
    return c.json({ ok: true });
  });
  app.get("/api/project/:name/mission", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const mission = loadMission(session.dir);
    if (!mission) return c.json({ error: "No mission" }, 404);
    const valState = loadValidationState(session.dir);
    const assertions = valState ? Object.values(valState.assertions) : [];
    const validationSummary2 = {
      total: assertions.length,
      passing: assertions.filter((a) => a.status === "passing").length,
      failing: assertions.filter((a) => a.status === "failing").length,
      pending: assertions.filter((a) => a.status === "pending").length,
      blocked: assertions.filter((a) => a.status === "blocked").length
    };
    return c.json({ mission, validationSummary: validationSummary2 });
  });
  app.post("/api/project/:name/mission/plan-complete", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    const mission = loadMission(session.dir);
    if (!mission) return c.json({ error: "No mission" }, 404);
    if (mission.status !== "planning") {
      return c.json({ error: `Mission is "${mission.status}", expected "planning"` }, 409);
    }
    mission.status = "active";
    const sorted = [...mission.milestones].sort((a, b) => a.order - b.order);
    const first = sorted.find((m) => m.status === "locked");
    if (first) {
      first.status = "active";
      first.updated = (/* @__PURE__ */ new Date()).toISOString();
    }
    mission.updated = (/* @__PURE__ */ new Date()).toISOString();
    saveMission(session.dir, mission);
    return c.json({ ok: true, mission });
  });
  app.get("/api/project/:name/metrics", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return c.json(computeMetrics(session.dir));
  });
  app.get("/api/project/:name/metrics/agents", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return c.json({ agents: computeMetrics(session.dir).agents });
  });
  app.get("/api/project/:name/metrics/timeline", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return c.json({ timeline: computeMetrics(session.dir).timeline });
  });
  app.get("/api/project/:name/metrics/history", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    return c.json({ history: loadMissionHistory(session.dir) });
  });
  app.get("/api/project/:name/events", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const allEvents = readEvents(session.dir);
    const recent = allEvents.slice(-50).reverse();
    const now = Date.now();
    const withRelative = recent.map((e) => {
      const ms = now - new Date(e.timestamp).getTime();
      let relative4;
      if (ms < 6e4) relative4 = `${Math.floor(ms / 1e3)}s ago`;
      else if (ms < 36e5) relative4 = `${Math.floor(ms / 6e4)}m ago`;
      else if (ms < 864e5) relative4 = `${Math.floor(ms / 36e5)}h ago`;
      else relative4 = `${Math.floor(ms / 864e5)}d ago`;
      return { ...e, relative: relative4 };
    });
    return c.json({ events: withRelative });
  });
  app.get("/api/project/:name/stream", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    return streamSSE(c, async (stream) => {
      projectStreamConnections += 1;
      sseMetrics.connections = projectStreamConnections;
      let closed = false;
      let changeQueued = false;
      let previousSnapshotHash = "";
      let previousTaskHashes = /* @__PURE__ */ new Map();
      let previousGoalHashes = /* @__PURE__ */ new Map();
      let previousMilestoneHashes = /* @__PURE__ */ new Map();
      let previousAgentHashes = /* @__PURE__ */ new Map();
      let previousMissionHash = "";
      let eventCursor = 0;
      let lastPing = Date.now();
      const tasksRoot = join37(session.dir, ".tasks");
      function writeSse(event, payload) {
        sseMetrics.messagesSent += 1;
        void stream.writeSSE({ event, data: JSON.stringify(freezePayload(payload)) });
      }
      function writeSnapshot(currentSession) {
        const snapshot = buildProjectStreamSnapshot(currentSession);
        previousSnapshotHash = JSON.stringify(snapshot);
        previousTaskHashes = new Map(snapshot.tasks.map((task) => [task.id, JSON.stringify(task)]));
        previousGoalHashes = new Map(snapshot.goals.map((goal) => [goal.id, JSON.stringify(goal)]));
        previousMilestoneHashes = new Map(
          snapshot.milestones.map((milestone) => [milestone.id, JSON.stringify(milestone)])
        );
        previousAgentHashes = new Map(
          snapshot.agents.map((agent) => [agent.paneId, JSON.stringify(agent)])
        );
        previousMissionHash = JSON.stringify(snapshot.mission);
        eventCursor = readEvents(currentSession.dir).length;
        writeSse("snapshot", snapshot);
      }
      function writeChanges(currentSession) {
        const snapshot = buildProjectStreamSnapshot(currentSession);
        const snapshotHash = JSON.stringify(snapshot);
        if (!previousSnapshotHash) {
          writeSnapshot(currentSession);
          return;
        }
        const missionHash = JSON.stringify(snapshot.mission);
        if (missionHash !== previousMissionHash) {
          writeSse("mission.changed", {});
          previousMissionHash = missionHash;
        }
        const nextTaskHashes = new Map(
          snapshot.tasks.map((task) => [task.id, JSON.stringify(task)])
        );
        for (const task of snapshot.tasks) {
          if (nextTaskHashes.get(task.id) !== previousTaskHashes.get(task.id)) {
            const op = previousTaskHashes.has(task.id) ? "update" : "create";
            writeSse("task.changed", { id: task.id, op });
          }
        }
        for (const id of previousTaskHashes.keys()) {
          if (!nextTaskHashes.has(id)) {
            writeSse("task.changed", { id, op: "delete" });
          }
        }
        previousTaskHashes = nextTaskHashes;
        const nextGoalHashes = new Map(
          snapshot.goals.map((goal) => [goal.id, JSON.stringify(goal)])
        );
        for (const goal of snapshot.goals) {
          if (nextGoalHashes.get(goal.id) !== previousGoalHashes.get(goal.id)) {
            const op = previousGoalHashes.has(goal.id) ? "update" : "create";
            writeSse("goal.changed", { id: goal.id, op });
          }
        }
        for (const id of previousGoalHashes.keys()) {
          if (!nextGoalHashes.has(id)) {
            writeSse("goal.changed", { id, op: "delete" });
          }
        }
        previousGoalHashes = nextGoalHashes;
        const nextMilestoneHashes = new Map(
          snapshot.milestones.map((milestone) => [milestone.id, JSON.stringify(milestone)])
        );
        for (const milestone of snapshot.milestones) {
          if (nextMilestoneHashes.get(milestone.id) !== previousMilestoneHashes.get(milestone.id)) {
            const op = previousMilestoneHashes.has(milestone.id) ? "update" : "create";
            writeSse("milestone.changed", { id: milestone.id, op });
          }
        }
        for (const id of previousMilestoneHashes.keys()) {
          if (!nextMilestoneHashes.has(id)) {
            writeSse("milestone.changed", { id, op: "delete" });
          }
        }
        previousMilestoneHashes = nextMilestoneHashes;
        const nextAgentHashes = new Map(
          snapshot.agents.map((agent) => [agent.paneId, JSON.stringify(agent)])
        );
        for (const agent of snapshot.agents) {
          if (nextAgentHashes.get(agent.paneId) !== previousAgentHashes.get(agent.paneId)) {
            writeSse("agent.changed", {
              paneId: agent.paneId,
              status: agent.isBusy ? "busy" : "idle"
            });
          }
        }
        previousAgentHashes = nextAgentHashes;
        const events = readEvents(currentSession.dir);
        const effectiveCursor = events.length < eventCursor ? 0 : eventCursor;
        for (const event of events.slice(effectiveCursor)) {
          writeSse("event.appended", event);
        }
        eventCursor = events.length;
        if (snapshotHash !== previousSnapshotHash) {
          writeSse("snapshot", snapshot);
          previousSnapshotHash = snapshotHash;
        }
      }
      function queueChanges() {
        if (closed || changeQueued) return;
        changeQueued = true;
        queueMicrotask(() => {
          changeQueued = false;
          const current = discoverSessions().find((candidate) => candidate.name === name);
          if (current) writeChanges(current);
        });
      }
      const onTaskStoreChange = (change) => {
        if (!isPathInside2(change.path, tasksRoot) && change.path !== null) return;
        queueChanges();
      };
      const onEventAppended = (message) => {
        if (message.dir !== session.dir) return;
        queueChanges();
      };
      try {
        stream.onAbort(() => {
          closed = true;
        });
        taskStore.on("change", onTaskStoreChange);
        eventLogEmitter.on("event", onEventAppended);
        writeSnapshot(session);
        while (!closed) {
          await stream.sleep(250);
          const current = discoverSessions().find((candidate) => candidate.name === name);
          if (!current) break;
          writeChanges(current);
          const now = Date.now();
          if (now - lastPing >= 25e3) {
            writeSse("ping", { at: (/* @__PURE__ */ new Date()).toISOString() });
            lastPing = now;
          }
        }
      } finally {
        closed = true;
        taskStore.off("change", onTaskStoreChange);
        eventLogEmitter.off("event", onEventAppended);
        projectStreamConnections = Math.max(0, projectStreamConnections - 1);
        sseMetrics.connections = projectStreamConnections;
      }
    });
  });
  const resolveLspTarget = (sessionName, file) => {
    if (typeof file !== "string" || !file) {
      return { ok: false, status: 400, error: "Missing `file`" };
    }
    if (file.startsWith("/") || file.split("/").some((seg) => seg === ".." || seg === ".")) {
      return { ok: false, status: 403, error: "Path escapes workspace" };
    }
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === sessionName);
    if (!session) return { ok: false, status: 404, error: "Session not found" };
    let root;
    try {
      root = realpathSync4(session.dir);
    } catch {
      return { ok: false, status: 404, error: "Session directory not accessible" };
    }
    const requested = pathResolve2(root, file);
    let target;
    try {
      target = realpathSync4(requested);
    } catch {
      target = requested;
    }
    if (!target.startsWith(root + "/") && target !== root) {
      return { ok: false, status: 403, error: "Path escapes workspace" };
    }
    return { ok: true, root, target };
  };
  const parseLspPositionBody = (raw) => {
    const body = raw ?? {};
    const { file, line, column } = body;
    if (typeof file !== "string" || !file) {
      return { ok: false, error: "Missing `file`" };
    }
    if (typeof line !== "number" || line < 0 || !Number.isFinite(line)) {
      return { ok: false, error: "Missing or invalid `line`" };
    }
    if (typeof column !== "number" || column < 0 || !Number.isFinite(column)) {
      return { ok: false, error: "Missing or invalid `column`" };
    }
    return { ok: true, file, line, column };
  };
  app.post("/api/project/:name/lsp/hover", async (c) => {
    let raw;
    try {
      raw = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = parseLspPositionBody(raw);
    if (!parsed.ok) return c.json({ error: parsed.error }, 400);
    const sandbox = resolveLspTarget(c.req.param("name"), parsed.file);
    if (!sandbox.ok) return c.json({ error: sandbox.error }, sandbox.status);
    const client = await getLspClientForFile(sandbox.root, sandbox.target).catch(
      (err) => ({
        __error: err instanceof Error ? err.message : String(err)
      })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    if (!client) {
      return c.json({ error: "No LSP server registered for this file type" }, 400);
    }
    const hover = await client.hover(sandbox.target, parsed.line, parsed.column);
    return c.json({ hover });
  });
  app.post("/api/project/:name/lsp/definition", async (c) => {
    let raw;
    try {
      raw = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = parseLspPositionBody(raw);
    if (!parsed.ok) return c.json({ error: parsed.error }, 400);
    const sandbox = resolveLspTarget(c.req.param("name"), parsed.file);
    if (!sandbox.ok) return c.json({ error: sandbox.error }, sandbox.status);
    const client = await getLspClientForFile(sandbox.root, sandbox.target).catch(
      (err) => ({
        __error: err instanceof Error ? err.message : String(err)
      })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    if (!client) {
      return c.json({ error: "No LSP server registered for this file type" }, 400);
    }
    const definition = await client.definition(sandbox.target, parsed.line, parsed.column);
    return c.json({ definition });
  });
  app.post("/api/project/:name/lsp/references", async (c) => {
    let raw;
    try {
      raw = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = parseLspPositionBody(raw);
    if (!parsed.ok) return c.json({ error: parsed.error }, 400);
    const sandbox = resolveLspTarget(c.req.param("name"), parsed.file);
    if (!sandbox.ok) return c.json({ error: sandbox.error }, sandbox.status);
    const client = await getLspClientForFile(sandbox.root, sandbox.target).catch(
      (err) => ({
        __error: err instanceof Error ? err.message : String(err)
      })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    if (!client) {
      return c.json({ error: "No LSP server registered for this file type" }, 400);
    }
    const references = await client.references(sandbox.target, parsed.line, parsed.column);
    return c.json({ references });
  });
  app.post("/api/project/:name/lsp/diagnostics", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const sandbox = resolveLspTarget(c.req.param("name"), body?.file);
    if (!sandbox.ok) return c.json({ error: sandbox.error }, sandbox.status);
    const client = await getLspClientForFile(sandbox.root, sandbox.target).catch(
      (err) => ({ __error: err instanceof Error ? err.message : String(err) })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    if (!client) {
      return c.json({ error: "No LSP server registered for this file type" }, 400);
    }
    await client.ensureOpen(sandbox.target);
    const diagnostics = await client.waitForDiagnostics(sandbox.target, 1500);
    return c.json({ diagnostics });
  });
  app.post("/api/project/:name/lsp/symbols", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const query = typeof body?.query === "string" ? body.query : "";
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    let root;
    try {
      root = realpathSync4(session.dir);
    } catch {
      return c.json({ error: "Session directory not accessible" }, 404);
    }
    const client = await getLspClient(root, "typescript").catch(
      (err) => ({
        __error: err instanceof Error ? err.message : String(err)
      })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    const symbols = await client.workspaceSymbols(query);
    return c.json({ symbols: symbols ?? [] });
  });
  app.post("/api/project/:name/lsp/rename", async (c) => {
    let raw;
    try {
      raw = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = parseLspPositionBody(raw);
    if (!parsed.ok) return c.json({ error: parsed.error }, 400);
    const newName = raw?.newName;
    if (typeof newName !== "string" || !newName.trim()) {
      return c.json({ error: "Missing `newName`" }, 400);
    }
    const sandbox = resolveLspTarget(c.req.param("name"), parsed.file);
    if (!sandbox.ok) return c.json({ error: sandbox.error }, sandbox.status);
    const client = await getLspClientForFile(sandbox.root, sandbox.target).catch(
      (err) => ({
        __error: err instanceof Error ? err.message : String(err)
      })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    if (!client) {
      return c.json({ error: "No LSP server registered for this file type" }, 400);
    }
    const edit = await client.rename(sandbox.target, parsed.line, parsed.column, newName);
    return c.json({ edit });
  });
  app.post("/api/project/:name/lsp/codeActions", async (c) => {
    let raw;
    try {
      raw = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = parseLspPositionBody(raw);
    if (!parsed.ok) return c.json({ error: parsed.error }, 400);
    const body = raw;
    const endLine = typeof body?.endLine === "number" && body.endLine >= 0 ? body.endLine : parsed.line;
    const endColumn = typeof body?.endColumn === "number" && body.endColumn >= 0 ? body.endColumn : parsed.column;
    const sandbox = resolveLspTarget(c.req.param("name"), parsed.file);
    if (!sandbox.ok) return c.json({ error: sandbox.error }, sandbox.status);
    const client = await getLspClientForFile(sandbox.root, sandbox.target).catch(
      (err) => ({
        __error: err instanceof Error ? err.message : String(err)
      })
    );
    if (client && "__error" in client) {
      return c.json({ error: `LSP failed to start: ${client.__error}` }, 500);
    }
    if (!client) {
      return c.json({ error: "No LSP server registered for this file type" }, 400);
    }
    const diagnostics = client.diagnostics(sandbox.target);
    const actions = await client.codeActions(
      sandbox.target,
      {
        start: { line: parsed.line, character: parsed.column },
        end: { line: endLine, character: endColumn }
      },
      diagnostics
    );
    return c.json({ actions: actions ?? [] });
  });
  app.get("/api/daemon/metrics", (c) => {
    const metrics = getTaskStoreMetrics();
    return c.json({
      uptimeMs: metrics.uptimeMs,
      cache: metrics.cache,
      watcher: metrics.watcher,
      reconcile: metrics.reconcile,
      sse: getSseMetrics(),
      writes: metrics.writes,
      paneContentHash: getPaneContentHashMetrics()
    });
  });
  app.get("/api/project/:name/orchestrator/health", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const live = getOrchestratorHealth(name);
    if (live) return c.json(live);
    const tasks = loadTasks(session.dir);
    const panes = (() => {
      try {
        return listSessionPanes(name);
      } catch {
        return [];
      }
    })();
    const inProgress = tasks.filter((task) => task.status === "in-progress");
    const busyAssignees = new Set(inProgress.map((task) => task.assignee).filter(Boolean));
    const idleAgents = panes.filter((pane) => {
      if (pane.role === "lead") return false;
      const agentName = pane.name ?? pane.title;
      return !busyAssignees.has(agentName);
    }).length;
    return c.json({
      ticking: false,
      lastTickMs: 0,
      inflight: inProgress.length,
      idleAgents,
      queuedDispatches: tasks.filter((task) => task.status === "todo").length,
      lastError: null,
      totalDispatched: 0,
      totalCompleted: tasks.filter((task) => task.status === "done").length,
      totalFailed: tasks.filter((task) => task.status === "review").length
    });
  });
  app.post("/api/project/:name/inject", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    if (!body || typeof body !== "object" || Array.isArray(body)) {
      return c.json({ error: "Invalid request body" }, 400);
    }
    const text = body.text;
    const paneId = body.paneId;
    const sendEnter = body.sendEnter;
    if (typeof text !== "string" || text.trim().length === 0) {
      return c.json({ error: "text must be a non-empty string" }, 400);
    }
    if (paneId !== void 0 && (typeof paneId !== "string" || !/^%\d+$/.test(paneId))) {
      return c.json({ error: "paneId must match /^%\\d+$/" }, 400);
    }
    if (sendEnter !== void 0 && typeof sendEnter !== "boolean") {
      return c.json({ error: "sendEnter must be a boolean" }, 400);
    }
    const panes = listSessionPanes(name);
    const pane = paneId ? panes.find((candidate) => candidate.id === paneId) : panes.find((p) => p.active);
    if (!pane) {
      return c.json({ error: "Pane not found" }, 404);
    }
    sendLiteralToPane(name, pane.id, text);
    if (sendEnter) sendEnterToPane(name, pane.id);
    appendEvent(session.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "send",
      target: pane.name ?? pane.title,
      paneId: pane.id,
      message: text.length > 100 ? text.slice(0, 100) + "..." : text
    });
    return c.json({ ok: true });
  });
  app.post("/api/project/:name/send", zValidator2("json", sendCommandSchema), async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const { target, message, noEnter } = c.req.valid("json");
    const panes = listSessionPanes(name);
    const pane = resolvePane(panes, target);
    if (!pane) {
      const available = panes.map((p) => ({
        id: p.id,
        title: p.title,
        name: p.name,
        role: p.role
      }));
      return c.json({ error: "Pane not found", target, available }, 404);
    }
    const busyStatus = getPaneBusyStatus(name, pane.id);
    const prepared = busyStatus === "agent" ? message.replace(/\n+/g, " ").trim() : message;
    if (noEnter) {
      sendText(name, pane.id, prepared);
    } else {
      sendCommand(name, pane.id, prepared);
    }
    appendEvent(session.dir, {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      type: "send",
      target: pane.name ?? pane.title,
      paneId: pane.id,
      message: prepared.length > 100 ? prepared.slice(0, 100) + "..." : prepared
    });
    return c.json({
      ok: true,
      session: name,
      target: {
        paneId: pane.id,
        name: pane.name,
        title: pane.title,
        role: pane.role
      },
      busyStatus
    });
  });
  app.get("/api/project/:name/config", (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    try {
      const { config: config2, configPath } = readConfig(session.dir);
      return c.json({ ok: true, config: config2, configPath });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      return c.json({ error: "Failed to read ide.yml", detail: message }, 500);
    }
  });
  app.post("/api/project/:name/config", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = IdeConfigSchema.safeParse(body);
    if (!parsed.success) {
      return c.json({ error: "Invalid config", details: parsed.error.issues }, 400);
    }
    try {
      const configPath = writeConfig(session.dir, parsed.data);
      return c.json({ ok: true, config: parsed.data, configPath });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      return c.json({ error: "Failed to write ide.yml", detail: message }, 500);
    }
  });
  const execFileAsync4 = promisify3(execFile7);
  app.post("/api/project/:name/restart", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) return c.json({ error: "Session not found" }, 404);
    try {
      await execFileAsync4("tmux-ide", ["restart", "--json"], {
        cwd: session.dir,
        timeout: 3e4,
        env: { ...process.env, TMUX: "" }
      });
      return c.json({ ok: true, session: name, status: "restarted" });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      return c.json({ error: "Restart failed", detail: message }, 500);
    }
  });
  app.post("/api/project/:name/launch", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const state = getSessionState(name);
    if (state.running) {
      return c.json({ ok: true, session: name, status: "already_running" });
    }
    try {
      await execFileAsync4("tmux-ide", ["--json"], {
        cwd: session.dir,
        timeout: 3e4,
        env: { ...process.env, TMUX: "" }
        // Clear TMUX to avoid nesting
      });
      return c.json({ ok: true, session: name, status: "launched" });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      return c.json({ error: "Launch failed", detail: message }, 500);
    }
  });
  app.post("/api/project/:name/stop", async (c) => {
    const name = c.req.param("name");
    const sessions = discoverSessions();
    const session = sessions.find((s) => s.name === name);
    if (!session) {
      return c.json({ error: "Session not found" }, 404);
    }
    const state = getSessionState(name);
    if (!state.running) {
      return c.json({ ok: true, session: name, status: "not_running" });
    }
    stopSessionMonitor(name);
    const result = killSession(name);
    if (result.stopped) {
      return c.json({ ok: true, session: name, status: "stopped" });
    }
    return c.json({ error: "Stop failed", reason: result.reason }, 500);
  });
  app.get("/api/events", (c) => {
    return streamSSE(c, async (stream) => {
      let prevOverviews = [];
      let prevDetails = /* @__PURE__ */ new Map();
      const eventCursors = /* @__PURE__ */ new Map();
      let prevOrchHashes = /* @__PURE__ */ new Map();
      const poll = () => {
        const sessions = discoverSessions();
        const overviews = buildOverviews(sessions);
        const prevNames = new Set(prevOverviews.map((s) => s.name));
        const currNames = new Set(overviews.map((s) => s.name));
        for (const overview of overviews) {
          if (!prevNames.has(overview.name)) {
            stream.writeSSE({
              event: "session_added",
              data: JSON.stringify(overview)
            });
            continue;
          }
          const prev = prevOverviews.find((s) => s.name === overview.name);
          if (prev && (prev.stats.doneTasks !== overview.stats.doneTasks || prev.stats.totalTasks !== overview.stats.totalTasks || prev.stats.activeAgents !== overview.stats.activeAgents)) {
            stream.writeSSE({
              event: "session_update",
              data: JSON.stringify(overview)
            });
          }
        }
        for (const prev of prevOverviews) {
          if (!currNames.has(prev.name)) {
            stream.writeSSE({
              event: "session_removed",
              data: JSON.stringify({ name: prev.name })
            });
          }
        }
        for (const session of sessions) {
          const events = readEvents(session.dir);
          const cursor = eventCursors.get(session.name) ?? 0;
          const effectiveCursor = events.length < cursor ? 0 : cursor;
          const newEvents = events.slice(effectiveCursor);
          for (const evt of newEvents) {
            const eventId = `${evt.timestamp}:${evt.type}:${evt.taskId ?? ""}`;
            stream.writeSSE({
              id: eventId,
              event: "orchestrator_event",
              data: JSON.stringify({ session: session.name, ...evt })
            });
          }
          eventCursors.set(session.name, events.length);
          const orchSnapshot = buildOrchestratorSnapshot(session);
          const orchHash = JSON.stringify(orchSnapshot);
          const prevHash = prevOrchHashes.get(session.name);
          if (orchHash !== prevHash) {
            stream.writeSSE({
              event: "orchestrator_state",
              data: JSON.stringify({ session: session.name, ...orchSnapshot })
            });
            prevOrchHashes.set(session.name, orchHash);
          }
          const detail = buildProjectDetail(session);
          const prevDetail = prevDetails.get(session.name);
          if (prevDetail) {
            const prevTaskMap = new Map(prevDetail.tasks.map((t) => [t.id, t]));
            for (const task of detail.tasks) {
              const prevTask = prevTaskMap.get(task.id);
              if (!prevTask) {
                stream.writeSSE({
                  event: "task_update",
                  data: JSON.stringify({
                    session: session.name,
                    taskId: task.id,
                    status: task.status,
                    title: task.title
                  })
                });
              } else if (prevTask.status !== task.status || prevTask.assignee !== task.assignee) {
                stream.writeSSE({
                  event: "task_update",
                  data: JSON.stringify({
                    session: session.name,
                    taskId: task.id,
                    status: task.status,
                    assignee: task.assignee
                  })
                });
              }
            }
            const prevAgentMap = new Map(prevDetail.agents.map((a) => [a.paneTitle, a]));
            for (const agent of detail.agents) {
              const prevAgent = prevAgentMap.get(agent.paneTitle);
              if (!prevAgent || prevAgent.isBusy !== agent.isBusy) {
                stream.writeSSE({
                  event: "agent_status",
                  data: JSON.stringify({
                    session: session.name,
                    agent: agent.paneTitle,
                    busy: agent.isBusy,
                    taskId: agent.taskId
                  })
                });
              }
            }
          }
          prevDetails.set(session.name, detail);
        }
        prevOverviews = overviews;
      };
      poll();
      while (true) {
        await stream.sleep(2e3);
        poll();
      }
    });
  });
  app.get("/api/logs/:channel", (c) => {
    const channel = c.req.param("channel");
    const match = matchLogChannel(channel);
    if (!match) {
      return c.json({ error: `Unknown log channel: ${channel}` }, 404);
    }
    return streamSSE(c, async (stream) => {
      const backfill = getLogBuffer().filter(match);
      for (const entry of backfill) {
        await stream.writeSSE({ event: "entry", data: JSON.stringify(entry) });
      }
      await stream.writeSSE({ event: "bookmark", data: String(backfill.length) });
      const queue = [];
      let cancelled = false;
      const unsub = subscribeLogs((entry) => {
        if (cancelled) return;
        if (match(entry)) queue.push(entry);
      });
      try {
        while (!cancelled) {
          if (queue.length === 0) {
            await stream.sleep(500);
            continue;
          }
          const drained = queue.splice(0, queue.length);
          for (const entry of drained) {
            await stream.writeSSE({ event: "entry", data: JSON.stringify(entry) });
          }
        }
      } finally {
        cancelled = true;
        unsub();
      }
    });
  });
  const remoteRegistry = options.remoteRegistry ?? null;
  app.post("/api/hq/register", async (c) => {
    if (!remoteRegistry) return c.json({ error: "HQ registry not enabled" }, 501);
    const body = await c.req.json();
    const parsed = RegistrationPayloadSchema.safeParse(body);
    if (!parsed.success)
      return c.json({ error: "Invalid payload", details: parsed.error.issues }, 400);
    try {
      const machine = remoteRegistry.register(parsed.data);
      return c.json({
        ok: true,
        id: machine.id,
        name: machine.name,
        registeredAt: machine.registeredAt.toISOString()
      });
    } catch (err) {
      return c.json({ error: err instanceof Error ? err.message : String(err) }, 409);
    }
  });
  app.get("/api/hq/machines", (c) => {
    if (!remoteRegistry) return c.json({ machines: [] });
    const machines = remoteRegistry.getMachines().map((m) => ({
      id: m.id,
      name: m.name,
      url: m.url,
      registeredAt: m.registeredAt.toISOString(),
      lastHeartbeat: m.lastHeartbeat.toISOString(),
      sessions: Array.from(m.sessionIds)
    }));
    return c.json({ machines });
  });
  app.get("/api/hq/machines/:id", (c) => {
    if (!remoteRegistry) return c.json({ error: "HQ registry not enabled" }, 501);
    const machine = remoteRegistry.getMachine(c.req.param("id"));
    if (!machine) return c.json({ error: "Machine not found" }, 404);
    return c.json({
      id: machine.id,
      name: machine.name,
      url: machine.url,
      registeredAt: machine.registeredAt.toISOString(),
      lastHeartbeat: machine.lastHeartbeat.toISOString(),
      sessions: Array.from(machine.sessionIds)
    });
  });
  app.delete("/api/hq/machines/:id", (c) => {
    if (!remoteRegistry) return c.json({ error: "HQ registry not enabled" }, 501);
    const removed = remoteRegistry.unregister(c.req.param("id"));
    if (!removed) return c.json({ error: "Machine not found" }, 404);
    return c.json({ ok: true });
  });
  function localAgents() {
    const managed = new Set(
      getDefaultWorkspaceRegistry().list().map((w) => w.sessionName)
    );
    const tmuxAgents = discoverTmuxAgents(managed);
    const external = getDefaultExternalAgentRegistry().list();
    return mergeLocalAgents(tmuxAgents, external);
  }
  app.get("/api/agents", (c) => {
    const payload = AgentListSchemaZ.parse({ agents: localAgents() });
    return c.json(payload);
  });
  const MAX_HQ_MACHINES = 50;
  const MAX_HQ_AGENTS = 2e3;
  const isHttpUrl = (u) => {
    try {
      const proto = new URL(u).protocol;
      return proto === "http:" || proto === "https:";
    } catch {
      return false;
    }
  };
  app.get("/api/hq/agents", async (c) => {
    const selfName = options.hqMachineName ?? hostname2();
    const sshSourcesPromise = listSshAgentSources();
    let remotes = [];
    if (remoteRegistry) {
      const candidates = remoteRegistry.getMachines().filter((m) => m.name !== selfName && isHttpUrl(m.url));
      if (candidates.length > MAX_HQ_MACHINES) {
        logger.warn(
          "hq-agents",
          `fan-out capped: ${candidates.length} machines, querying first ${MAX_HQ_MACHINES}`
        );
      }
      const fanout = candidates.slice(0, MAX_HQ_MACHINES).map(async (machine) => {
        try {
          const controller = new AbortController();
          const tid = setTimeout(() => controller.abort(), 5e3);
          const res = await fetch(`${machine.url}/api/agents`, {
            headers: { Authorization: `Bearer ${machine.token}` },
            signal: controller.signal,
            // Don't chase redirects from a (possibly hostile) remote with the
            // machine's bearer attached — treat a 3xx as a failed fetch.
            redirect: "manual"
          });
          clearTimeout(tid);
          if (!res.ok) return null;
          const parsed = AgentListSchemaZ.safeParse(await res.json());
          if (!parsed.success) return null;
          return { machineId: machine.id, machineName: machine.name, agents: parsed.data.agents };
        } catch {
          return null;
        }
      });
      remotes = (await Promise.all(fanout)).filter((r) => r !== null);
    }
    remotes = [...remotes, ...await sshSourcesPromise];
    let agents = aggregateHqAgents(localAgents(), selfName, remotes);
    if (agents.length > MAX_HQ_AGENTS) {
      logger.warn(
        "hq-agents",
        `response capped: ${agents.length} agents, returning first ${MAX_HQ_AGENTS} (local first)`
      );
      agents = agents.slice(0, MAX_HQ_AGENTS);
    }
    return c.json(AgentListSchemaZ.parse({ agents }));
  });
  app.post("/api/agents/send", zValidator2("json", agentSendSchema), async (c) => {
    const { id, machineId, message, noEnter } = c.req.valid("json");
    if (machineId) {
      const sshUrl = await resolveSshMachineUrl(machineId);
      const hqMachine = sshUrl ? null : remoteRegistry?.getMachine(machineId);
      const baseUrl = sshUrl ?? hqMachine?.url;
      if (!baseUrl || !isHttpUrl(baseUrl)) {
        return c.json({ error: `Unknown machine: ${machineId}` }, 404);
      }
      const prefix = `${machineId}:`;
      if (!id.startsWith(prefix)) {
        return c.json({ error: "Agent id does not belong to that machine" }, 400);
      }
      const headers = { "Content-Type": "application/json" };
      if (hqMachine) headers.Authorization = `Bearer ${hqMachine.token}`;
      try {
        const controller = new AbortController();
        const tid = setTimeout(() => controller.abort(), 5e3);
        const res = await fetch(`${baseUrl}/api/agents/send`, {
          method: "POST",
          headers,
          body: JSON.stringify({ id: id.slice(prefix.length), message, noEnter }),
          signal: controller.signal,
          redirect: "manual"
        });
        clearTimeout(tid);
        const body = await res.json().catch(() => ({ error: `HTTP ${res.status}` }));
        if (res.ok) return c.json(body);
        return c.json({ ...body, remoteStatus: res.status }, 502);
      } catch {
        return c.json({ error: `Machine ${machineId} is unreachable` }, 502);
      }
    }
    const agent = localAgents().find((candidate) => candidate.id === id);
    if (!agent) {
      return c.json({ error: `Agent not found: ${id}` }, 404);
    }
    if (agent.kind === "external" || !agent.session || !agent.paneId) {
      return c.json({ error: "External agents are observe-only (no pane to drive)" }, 409);
    }
    const busyStatus = getPaneBusyStatus(agent.session, agent.paneId);
    const prepared = busyStatus === "agent" ? message.replace(/\n+/g, " ").trim() : message;
    if (noEnter) {
      sendText(agent.session, agent.paneId, prepared);
    } else {
      sendCommand(agent.session, agent.paneId, prepared);
    }
    return c.json({
      ok: true,
      agent: { id: agent.id, session: agent.session, paneId: agent.paneId }
    });
  });
  const tunnelManager = options.tunnelManager ?? null;
  app.get("/api/tunnel", async (c) => {
    if (!tunnelManager) return c.json({ running: false, provider: null });
    const status4 = await tunnelManager.status();
    return c.json(status4);
  });
  app.post("/api/tunnel/start", async (c) => {
    if (!tunnelManager) return c.json({ error: "Tunnel manager not configured" }, 501);
    const body = await c.req.json().catch(() => ({}));
    const { tunnelConfigSchema: tunnelConfigSchema2 } = await Promise.resolve().then(() => (init_types3(), types_exports2));
    const parsed = tunnelConfigSchema2.safeParse(body);
    if (!parsed.success)
      return c.json({ error: "Invalid tunnel config", details: parsed.error.issues }, 400);
    const status4 = await tunnelManager.start(parsed.data);
    return c.json(status4);
  });
  app.post("/api/tunnel/stop", async (c) => {
    if (!tunnelManager) return c.json({ error: "Tunnel manager not configured" }, 501);
    await tunnelManager.stop();
    return c.json({ ok: true });
  });
  app.get("/health", (c) => {
    return c.json({
      ok: true,
      uptime: Math.round(process.uptime()),
      version: pkgVersion
    });
  });
  app.get("/api/projects", (c) => {
    return c.json({ projects: listProjects() });
  });
  app.get("/api/projects/templates", (c) => {
    return c.json({ templates: listAvailableTemplates() });
  });
  app.post("/api/projects", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = RegisterProjectRequestSchemaZ.safeParse(body);
    if (!parsed.success) {
      return c.json({ error: "Invalid request", details: parsed.error.issues }, 400);
    }
    try {
      const project = await registerProject({
        dir: parsed.data.dir,
        name: parsed.data.name
      });
      return c.json({ project }, 201);
    } catch (err) {
      if (err instanceof ProjectDirNotFoundError) {
        return c.json({ error: err.message, code: err.code }, 400);
      }
      if (err instanceof ProjectAlreadyRegisteredError) {
        return c.json({ error: err.message, code: err.code, suggestion: err.suggestion }, 409);
      }
      throw err;
    }
  });
  app.delete("/api/projects/:name", (c) => {
    const name = c.req.param("name");
    try {
      unregisterProject(name);
      return c.json({ ok: true });
    } catch (err) {
      if (err instanceof ProjectNotFoundError) {
        return c.json({ error: err.message, code: err.code }, 404);
      }
      throw err;
    }
  });
  app.get("/api/filesystem/browse", (c) => {
    const rawPath = c.req.query("path");
    const showHiddenRaw = c.req.query("showHidden");
    const showHidden = showHiddenRaw === "1" || showHiddenRaw === "true";
    try {
      const result = browseDirectory({
        path: rawPath ?? null,
        showHidden
      });
      return c.json(result);
    } catch (err) {
      if (err instanceof SandboxViolationError) {
        return c.json({ error: "outside-sandbox", message: err.message }, 403);
      }
      if (err instanceof PathNotFoundError) {
        return c.json({ error: "not-found", message: err.message }, 404);
      }
      if (err instanceof InvalidPathError) {
        return c.json({ error: "invalid-path", message: err.message }, 400);
      }
      throw err;
    }
  });
  app.post("/api/projects/:name/probe", async (c) => {
    const name = c.req.param("name");
    if (!getProject(name)) {
      return c.json({ error: `Project "${name}" not found in registry`, code: "NOT_FOUND" }, 404);
    }
    try {
      const project = await refreshProject(name);
      return c.json({ project });
    } catch (err) {
      if (err instanceof ProjectNotFoundError) {
        return c.json({ error: err.message, code: err.code }, 404);
      }
      throw err;
    }
  });
  app.post("/api/projects/init", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = InitProjectRequestSchemaZ.safeParse(body);
    if (!parsed.success) {
      return c.json({ error: "Invalid request", details: parsed.error.issues }, 400);
    }
    if (!existsSync36(parsed.data.dir)) {
      return c.json({ error: `Directory "${parsed.data.dir}" does not exist` }, 400);
    }
    const jobId = randomUUID11();
    const command2 = process.env.TMUX_IDE_INIT_COMMAND ?? "tmux-ide";
    void (async () => {
      try {
        await runInit({
          cwd: parsed.data.dir,
          template: parsed.data.template,
          command: command2,
          onChunk: (chunk) => {
            broadcastInitOutput(jobId, chunk);
          }
        });
        broadcastInitOutput(jobId, "", true);
        try {
          await registerProject({ dir: parsed.data.dir });
        } catch (err) {
          if (!(err instanceof ProjectAlreadyRegisteredError) && !(err instanceof ProjectDirNotFoundError)) {
            broadcastInitError(jobId, err.message);
          }
        }
      } catch (err) {
        if (err instanceof ProjectInitTimeoutError) {
          broadcastInitError(jobId, err.message);
        } else if (err instanceof ProjectInitFailedError) {
          broadcastInitError(jobId, err.message);
        } else {
          broadcastInitError(jobId, err.message);
        }
      }
    })();
    return c.json({ jobId }, 202);
  });
  app.post("/api/filesystem/inspect", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "invalid-path", message: "Invalid JSON body" }, 400);
    }
    const parsed = InspectFilesystemRequestSchemaZ.safeParse(body);
    if (!parsed.success) {
      return c.json({ error: "invalid-path", message: "Invalid request" }, 400);
    }
    const sandboxResult = sandboxResolveDir(parsed.data.dir);
    if ("error" in sandboxResult) {
      return c.json(
        { error: sandboxResult.error, message: sandboxResult.message },
        sandboxResult.status
      );
    }
    try {
      const project = await inspectProject(sandboxResult.canonical);
      return c.json({ project });
    } catch (err) {
      if (err instanceof InspectDirNotFoundError) {
        return c.json({ error: "not-found", message: err.message }, 404);
      }
      throw err;
    }
  });
  app.post("/api/projects/onboard", async (c) => {
    let body;
    try {
      body = await c.req.json();
    } catch {
      return c.json({ error: "Invalid JSON body" }, 400);
    }
    const parsed = OnboardProjectRequestSchemaZ.safeParse(body);
    if (!parsed.success) {
      return c.json({ error: "Invalid request", details: parsed.error.issues }, 400);
    }
    const sandboxResult = sandboxResolveDir(parsed.data.dir);
    if ("error" in sandboxResult) {
      return c.json(
        { error: sandboxResult.error, message: sandboxResult.message },
        sandboxResult.status
      );
    }
    const dir = sandboxResult.canonical;
    let inspect2;
    try {
      inspect2 = await inspectProject(dir);
    } catch (err) {
      if (err instanceof InspectDirNotFoundError) {
        return c.json({ error: "not-found", message: err.message }, 404);
      }
      throw err;
    }
    try {
      assertNoExistingIdeYml(dir);
    } catch (err) {
      if (err instanceof OnboardConflictError) {
        return c.json({ error: err.message, code: err.code }, 409);
      }
      throw err;
    }
    const finalName = parsed.data.name?.trim() || inspect2.name;
    let config2;
    try {
      config2 = composeIdeYmlConfig({
        name: finalName,
        agents: parsed.data.agents,
        agentNames: parsed.data.agentNames,
        devCommand: parsed.data.devCommand ?? null,
        testCommand: parsed.data.testCommand ?? null,
        lintCommand: parsed.data.lintCommand ?? null
      });
    } catch (err) {
      if (err instanceof OnboardInvalidInputError) {
        return c.json({ error: err.message, code: err.code }, 400);
      }
      throw err;
    }
    writeConfig(dir, config2);
    try {
      const project = await registerProject({ dir, name: finalName });
      return c.json({ project }, 201);
    } catch (err) {
      if (err instanceof ProjectAlreadyRegisteredError) {
        return c.json({ error: err.message, code: err.code, suggestion: err.suggestion }, 409);
      }
      if (err instanceof ProjectDirNotFoundError) {
        return c.json({ error: err.message, code: err.code }, 400);
      }
      throw err;
    }
  });
  attachNotesRoutes(app, {
    resolveSession(name) {
      return discoverSessions().find((s) => s.name === name) ?? null;
    }
  });
  app.use("*", serveDashboard());
  return app;
}
function listAvailableTemplates() {
  const __filename = fileURLToPath5(import.meta.url);
  const __dir = dirname16(__filename);
  const templatesDir = join37(__dir, "..", "..", "..", "..", "templates");
  if (!existsSync36(templatesDir)) return [];
  const labels = {
    default: { label: "Default", description: "Single Claude pane + dev/shell row" },
    nextjs: {
      label: "Next.js",
      description: "Two Claude panes + Next.js dev server + shell"
    },
    vite: { label: "Vite", description: "Vite dev server + Claude + shell" },
    convex: {
      label: "Convex",
      description: "Convex dev + Next.js + Claude pane"
    },
    python: { label: "Python", description: "Python project with Claude + tests" },
    go: { label: "Go", description: "Go project with Claude + tests + shell" },
    "agent-team": {
      label: "Agent Team",
      description: "Lead + teammate Claude panes for coordinated multi-agent work"
    },
    "agent-team-nextjs": {
      label: "Agent Team \u2014 Next.js",
      description: "Agent team layout tuned for a Next.js app"
    },
    "agent-team-monorepo": {
      label: "Agent Team \u2014 Monorepo",
      description: "Agent team layout for monorepos with multiple apps"
    },
    missions: {
      label: "Missions",
      description: "Mission-driven layout with planner, validator, and researcher"
    }
  };
  const entries = readdirSync10(templatesDir).filter((f) => f.endsWith(".yml"));
  return entries.map((file) => {
    const id = file.replace(/\.yml$/, "");
    const meta = labels[id];
    return {
      id,
      label: meta?.label ?? id,
      description: meta?.description ?? `Template: ${id}`
    };
  }).sort((a, b) => a.id.localeCompare(b.id));
}
function attachWsEvents(server) {
  const wss = new WebSocketServer({ noServer: true });
  const upgradeListener = (req, socket, head) => {
    const url = req.url ?? "/";
    const pathname = url.split("?")[0];
    if (pathname !== "/ws/events") return;
    wss.handleUpgrade(req, socket, head, (ws) => {
      handleWsEventsConnection(ws);
    });
  };
  server.on("upgrade", upgradeListener);
  return {
    close: () => {
      server.off("upgrade", upgradeListener);
      wss.close();
    }
  };
}
var __dirname3, pkgVersion, projectStreamConnections, ALLOWED_MILESTONE_TRANSITIONS, sseMetrics;
var init_server = __esm({
  "packages/daemon/src/command-center/server.ts"() {
    "use strict";
    init_discovery();
    init_pane_comms();
    init_send();
    init_src();
    init_yaml_io();
    init_ide_config2();
    init_task_store();
    init_task_actions();
    init_event_log();
    init_log();
    init_authorship();
    init_validation();
    init_skill_registry();
    init_metrics();
    init_plan_store2();
    init_workflow_store();
    init_workspace_registry();
    init_agent_discovery();
    init_agent_remotes();
    init_agent_registry();
    init_src2();
    init_src2();
    init_schemas();
    init_git_service();
    init_errors6();
    init_github_service();
    init_src2();
    init_terminals_store();
    init_ws_route();
    init_ws_events();
    init_auth_service();
    init_middleware();
    init_types2();
    init_research();
    init_search();
    init_search_replace();
    init_static();
    init_handlers();
    init_orchestrator();
    init_ws_events();
    init_dispatcher();
    init_project_registry();
    init_project_init_runner();
    init_registry();
    init_inspect();
    init_filesystem_browser();
    init_project_inspect();
    init_project_onboard();
    init_registry3();
    init_defaults();
    init_provider_discovery();
    init_plan_orchestrator();
    init_plan_store();
    init_src2();
    init_provider_store();
    init_src2();
    __dirname3 = dirname16(fileURLToPath5(import.meta.url));
    pkgVersion = resolvePackageVersion();
    projectStreamConnections = 0;
    ALLOWED_MILESTONE_TRANSITIONS = /* @__PURE__ */ new Map([
      ["locked", /* @__PURE__ */ new Set(["active"])],
      ["active", /* @__PURE__ */ new Set(["validating"])],
      ["validating", /* @__PURE__ */ new Set(["done", "active"])],
      ["done", /* @__PURE__ */ new Set()]
    ]);
    sseMetrics = {
      connections: 0,
      messagesSent: 0
    };
  }
});

// packages/daemon/src/lib/auth/types.ts
var types_exports3 = {};
__export(types_exports3, {
  AuthConfigSchema: () => AuthConfigSchema
});
var init_types4 = __esm({
  "packages/daemon/src/lib/auth/types.ts"() {
    "use strict";
    init_src2();
  }
});

// packages/daemon/src/lib/tunnels/tailscale.ts
import { spawn as nodeSpawn } from "node:child_process";
var spawnImpl, testSkipTailscaleFsPaths, TailscaleServeServiceImpl, tailscaleServeService;
var init_tailscale = __esm({
  "packages/daemon/src/lib/tunnels/tailscale.ts"() {
    "use strict";
    init_log();
    spawnImpl = nodeSpawn;
    testSkipTailscaleFsPaths = false;
    TailscaleServeServiceImpl = class {
      serveProcess = null;
      currentPort = null;
      isStarting = false;
      tailscaleExecutable = "tailscale";
      // Default to PATH lookup
      lastError;
      startTime;
      isPermanentlyDisabled = false;
      funnelEnabled = false;
      funnelStartTime;
      desiredFunnel = false;
      // Track what the user requested
      funnelError;
      // Track specific Funnel errors
      async start(port, enableFunnel = false) {
        log.info(
          `\u{1F680} Starting Tailscale Serve on port ${port} ${enableFunnel ? "with Funnel (Public Internet)" : "(Tailnet only - Private)"}`
        );
        if (this.isPermanentlyDisabled) {
          log.warn(`\u274C Cannot start - permanently disabled on this tailnet`);
          throw new Error("Tailscale Serve is permanently disabled on this tailnet");
        }
        if (this.isStarting) {
          log.warn(`\u26A0\uFE0F Already starting, rejecting duplicate request`);
          throw new Error("Tailscale Serve is already starting");
        }
        if (this.serveProcess) {
          log.info("\u{1F504} Serve already running, stopping first...");
          await this.stop();
        }
        this.isStarting = true;
        this.lastError = void 0;
        this.funnelError = void 0;
        this.currentPort = port;
        this.desiredFunnel = enableFunnel;
        log.info(
          `\u{1F30D} ${enableFunnel ? "PUBLIC Internet access (Funnel)" : "PRIVATE Tailnet-only access (no Funnel)"} requested`
        );
        this.funnelEnabled = false;
        try {
          await this.checkTailscaleAvailable();
          try {
            log.debug("Resetting Tailscale Serve configuration...");
            const resetProcess = spawnImpl(this.tailscaleExecutable, ["serve", "reset"], {
              stdio: ["ignore", "pipe", "pipe"]
            });
            await new Promise((resolve35) => {
              resetProcess.on("exit", () => resolve35());
              resetProcess.on("error", () => resolve35());
              setTimeout(resolve35, 1e3);
            });
          } catch {
            log.debug("Failed to reset serve config (this is normal if none exists)");
          }
          const args = ["serve", "--bg", `http://localhost:${port}`];
          log.info(`\u{1F680} Starting Tailscale Serve - HTTPS proxy to localhost:${port}`);
          log.debug(`\u{1F527} Command: ${this.tailscaleExecutable} ${args.join(" ")}`);
          this.currentPort = port;
          this.serveProcess = spawnImpl(this.tailscaleExecutable, args, {
            stdio: ["ignore", "pipe", "pipe"],
            detached: false
            // Keep it attached to our process
          });
          this.serveProcess.on("error", (error) => {
            log.info(`Tailscale Serve process error: ${error.message}`);
            this.lastError = error.message;
            this.cleanup();
          });
          this.serveProcess.on("exit", (code, signal) => {
            log.info(`Tailscale Serve process exited with code ${code}, signal ${signal}`);
            if (code !== 0) {
              if (this.lastError?.includes("Serve is not enabled on your tailnet")) {
                log.info("Tailscale Serve failed due to tailnet permissions");
              } else {
                this.lastError = `Process exited with code ${code}`;
              }
            }
            this.cleanup();
          });
          if (this.serveProcess.stdout) {
            this.serveProcess.stdout.on("data", (data) => {
              log.debug(`Tailscale Serve stdout: ${data.toString().trim()}`);
            });
          }
          if (this.serveProcess.stderr) {
            this.serveProcess.stderr.on("data", (data) => {
              const stderr = data.toString().trim();
              log.debug(`Tailscale Serve stderr: ${stderr}`);
              if (stderr.includes("Serve is not enabled on your tailnet")) {
                log.warn(
                  "Tailscale Serve is not enabled on this tailnet - marking as permanently disabled"
                );
                this.lastError = "Tailscale Serve feature not enabled on your tailnet";
                this.isPermanentlyDisabled = true;
                return;
              }
              if (stderr.includes("error") || stderr.includes("failed")) {
                this.lastError = stderr;
              }
            });
          }
          await new Promise((resolve35, reject) => {
            let settled = false;
            const settlePromise = (isSuccess, error) => {
              if (settled) return;
              settled = true;
              clearTimeout(timeout);
              if (isSuccess) {
                log.info("Tailscale Serve started successfully");
                this.startTime = /* @__PURE__ */ new Date();
                resolve35();
              } else {
                const errorMessage2 = error instanceof Error ? error.message : error || "Tailscale Serve failed to start";
                this.lastError = errorMessage2;
                reject(new Error(errorMessage2));
              }
            };
            const timeout = setTimeout(() => {
              if (this.serveProcess && !this.serveProcess.killed) {
                settlePromise(true);
              } else {
                settlePromise(false, this.lastError);
              }
            }, 3e3);
            if (this.serveProcess) {
              this.serveProcess.once("error", (error) => {
                settlePromise(false, error);
              });
              this.serveProcess.once("exit", (code) => {
                if (code === 0) {
                  log.info("Tailscale Serve configured successfully (exit code 0)");
                  this.serveProcess = null;
                  setTimeout(() => {
                    settlePromise(true);
                  }, 500);
                } else {
                  settlePromise(false, `Tailscale Serve failed with exit code ${code}`);
                }
              });
            }
          });
          if (enableFunnel) {
            log.info(
              `\u{1F30D} Starting Funnel for PUBLIC internet access on HTTPS port 443 (Serve to localhost:${port} completed successfully)`
            );
            try {
              await this.startFunnel(port);
              log.info(
                `\u2705 Funnel started successfully - VibeTunnel now accessible via public internet`
              );
            } catch (funnelError) {
              const errorMsg = funnelError instanceof Error ? funnelError.message : String(funnelError);
              this.funnelError = errorMsg;
              log.info(`\u274C Failed to start Funnel: ${errorMsg}`);
              log.warn(
                `Funnel failed to start, continuing with PRIVATE Tailnet-only access: ${errorMsg}`
              );
            }
          } else {
            log.info(
              `\u{1F512} Running in PRIVATE mode - accessible only within your Tailnet, not from public internet`
            );
          }
        } catch (error) {
          this.lastError = error instanceof Error ? error.message : String(error);
          this.cleanup();
          throw error;
        } finally {
          this.isStarting = false;
        }
      }
      /**
       * Start Tailscale Funnel for public internet access
       */
      async startFunnel(port) {
        const httpsPort = 443;
        log.info(
          `\u{1F30D} Starting Tailscale Funnel on HTTPS port ${httpsPort} for public internet access (proxying to local port ${port})`
        );
        try {
          log.debug("Resetting Funnel configuration before starting...");
          const resetProcess = spawnImpl(this.tailscaleExecutable, ["funnel", "reset"], {
            stdio: ["ignore", "pipe", "pipe"]
          });
          await new Promise((resolve35) => {
            resetProcess.on("exit", () => resolve35());
            resetProcess.on("error", () => resolve35());
            setTimeout(resolve35, 2e3);
          });
          log.debug("Funnel configuration reset completed");
        } catch {
          log.debug("Failed to reset Funnel config (this is normal if none exists)");
        }
        log.debug(
          `\u{1F527} Command: ${this.tailscaleExecutable} funnel --bg --https=${httpsPort} http://localhost:${port}`
        );
        try {
          const funnelProcess = spawnImpl(
            this.tailscaleExecutable,
            ["funnel", "--bg", `--https=${httpsPort}`, `http://localhost:${port}`],
            {
              stdio: ["ignore", "pipe", "pipe"]
            }
          );
          log.debug(`\u{1F4DD} Process spawned with PID: ${funnelProcess.pid}`);
          let stdout = "";
          let stderr = "";
          if (funnelProcess.stdout) {
            funnelProcess.stdout.on("data", (data) => {
              stdout += data.toString();
              log.debug(`\u{1F4E4} Funnel stdout: ${data.toString().trim()}`);
            });
          }
          if (funnelProcess.stderr) {
            funnelProcess.stderr.on("data", (data) => {
              stderr += data.toString();
              log.debug(`\u{1F4E5} Funnel stderr: ${data.toString().trim()}`);
            });
          }
          await new Promise((resolve35, reject) => {
            const timeout = setTimeout(() => {
              reject(new Error("Funnel start timeout"));
            }, 1e4);
            funnelProcess.on("exit", (code) => {
              clearTimeout(timeout);
              log.debug(`\u{1F51A} Funnel process exited with code: ${code}`);
              if (stderr.trim()) {
                log.debug(`\u{1F4E5} Funnel stderr: ${stderr.trim()}`);
              }
              if (stdout.trim()) {
                log.debug(`\u{1F4E4} Funnel stdout: ${stdout.trim()}`);
              }
              if (code === 0) {
                log.info("\u2705 Tailscale Funnel started successfully");
                this.funnelEnabled = true;
                this.funnelError = void 0;
                this.funnelStartTime = /* @__PURE__ */ new Date();
                resolve35();
              } else {
                log.info(`\u274C Funnel failed with exit code ${code}: ${stderr || "No error message"}`);
                reject(new Error(`Funnel failed with exit code ${code}: ${stderr}`));
              }
            });
            funnelProcess.on("error", (error) => {
              clearTimeout(timeout);
              reject(error);
            });
          });
        } catch (error) {
          const errorMsg = error instanceof Error ? error.message : String(error);
          log.info(`Failed to start Tailscale Funnel: ${errorMsg}`);
          throw new Error(`Funnel start failed: ${errorMsg}`, { cause: error });
        }
      }
      /**
       * Stop Tailscale Funnel
       */
      async stopFunnel() {
        if (!this.funnelEnabled) {
          return;
        }
        try {
          log.info("Stopping Tailscale Funnel...");
          const resetProcess = spawnImpl(this.tailscaleExecutable, ["funnel", "reset"], {
            stdio: ["ignore", "pipe", "pipe"]
          });
          await new Promise((resolve35) => {
            resetProcess.on("exit", (code) => {
              if (code === 0) {
                log.info("\u2705 Tailscale Funnel stopped successfully");
              } else {
                log.warn(`Funnel reset exited with code ${code}`);
              }
              resolve35();
            });
            resetProcess.on("error", () => resolve35());
            setTimeout(resolve35, 3e3);
          });
          this.funnelEnabled = false;
          this.funnelStartTime = void 0;
        } catch (error) {
          log.info(`Failed to stop Funnel: ${error}`);
          throw error;
        }
      }
      async stop() {
        if (this.funnelEnabled) {
          try {
            log.info("\u{1F30D} Stopping Tailscale Funnel...");
            await this.stopFunnel();
          } catch (error) {
            log.warn(`Failed to stop Funnel: ${error}`);
          }
        }
        try {
          log.debug("Removing Tailscale Serve configuration...");
          const resetProcess = spawnImpl(this.tailscaleExecutable, ["serve", "reset"], {
            stdio: ["ignore", "pipe", "pipe"]
          });
          await new Promise((resolve35) => {
            resetProcess.on("exit", (code) => {
              if (code === 0) {
                log.debug("Tailscale Serve configuration reset successfully");
              }
              resolve35();
            });
            resetProcess.on("error", () => resolve35());
            setTimeout(resolve35, 2e3);
          });
        } catch {
          log.debug("Failed to reset serve config during stop");
        }
        if (!this.serveProcess) {
          log.debug("No Tailscale Serve process to stop");
          return;
        }
        log.info("Stopping Tailscale Serve process...");
        return new Promise((resolve35) => {
          if (!this.serveProcess) {
            resolve35();
            return;
          }
          const cleanup = () => {
            this.cleanup();
            resolve35();
          };
          const forceKillTimeout = setTimeout(() => {
            if (this.serveProcess && !this.serveProcess.killed) {
              log.warn("Force killing Tailscale Serve process");
              this.serveProcess.kill("SIGKILL");
            }
            cleanup();
          }, 5e3);
          this.serveProcess.once("exit", () => {
            clearTimeout(forceKillTimeout);
            cleanup();
          });
          this.serveProcess.kill("SIGTERM");
        });
      }
      isRunning() {
        return this.currentPort !== null && !this.isStarting;
      }
      isFunnelEnabled() {
        return this.funnelEnabled;
      }
      async getStatus() {
        log.debug("[TAILSCALE STATUS] Getting status", {
          isPermanentlyDisabled: this.isPermanentlyDisabled,
          lastError: this.lastError,
          processActive: !!this.serveProcess,
          currentPort: this.currentPort
        });
        if (process.env.VIBETUNNEL_TAILSCALE_ERROR) {
          return {
            isRunning: false,
            lastError: process.env.VIBETUNNEL_TAILSCALE_ERROR
          };
        }
        if (!this.isPermanentlyDisabled) {
          log.debug("[TAILSCALE STATUS] Checking actual Tailscale Serve configuration");
          const portToCheck2 = this.currentPort || 4020;
          try {
            const isConfigured = await this.verifyServeConfiguration(portToCheck2);
            log.debug(
              `[TAILSCALE STATUS] Serve configured for port ${portToCheck2}: ${isConfigured}`
            );
            if (isConfigured) {
              log.info(
                `\u2705 [TAILSCALE STATUS] Tailscale Serve is configured for port ${portToCheck2} - not permanently disabled`
              );
            } else {
              const checkResult = await this.checkServeAvailability();
              log.debug(`[TAILSCALE STATUS] Serve availability check result: ${checkResult}`);
              if (checkResult.includes("Serve is not enabled") || checkResult.includes("not available") || checkResult.includes("requires admin") || checkResult.includes("unauthorized")) {
                log.debug(
                  "[TAILSCALE STATUS] Tailscale Serve not available on tailnet - marking as permanently disabled"
                );
                this.isPermanentlyDisabled = true;
                this.lastError = "Serve is not enabled on your tailnet";
                return {
                  isRunning: false,
                  port: void 0,
                  lastError: void 0,
                  // No error in fallback mode
                  startTime: this.startTime,
                  isPermanentlyDisabled: true,
                  funnelEnabled: false,
                  funnelStartTime: void 0
                };
              }
            }
          } catch (error) {
            log.debug(`[TAILSCALE STATUS] Failed to check configuration: ${error}`);
          }
        }
        if (this.isPermanentlyDisabled) {
          log.info("[TAILSCALE STATUS] Returning permanently disabled status (no error)");
          return {
            isRunning: false,
            port: void 0,
            // Don't report an error - fallback mode is working fine
            lastError: void 0,
            startTime: this.startTime,
            isPermanentlyDisabled: true,
            funnelEnabled: false,
            funnelStartTime: void 0
          };
        }
        const processRunning = this.isRunning();
        log.info(`[TAILSCALE STATUS] Process running: ${processRunning}`);
        if (!processRunning && this.lastError?.includes("Serve is not enabled on your tailnet")) {
          log.info("[TAILSCALE STATUS] Detected permanent failure, switching to fallback mode");
          this.isPermanentlyDisabled = true;
          return {
            isRunning: false,
            port: void 0,
            lastError: void 0,
            // Don't show error in fallback mode
            startTime: this.startTime,
            isPermanentlyDisabled: true,
            funnelEnabled: false,
            funnelStartTime: void 0
          };
        }
        let actuallyRunning = processRunning;
        let verificationError;
        const portToCheck = this.currentPort || 4020;
        if (!processRunning && !this.isPermanentlyDisabled) {
          log.info(
            `[TAILSCALE STATUS] Process not running, checking for manually configured Tailscale Serve`
          );
          try {
            const isConfigured = await this.verifyServeConfiguration(portToCheck);
            log.info(`[TAILSCALE STATUS] Manual configuration check: ${isConfigured}`);
            if (isConfigured) {
              actuallyRunning = true;
              log.info(
                `\u2705 [TAILSCALE STATUS] Found manually configured Tailscale Serve for port ${portToCheck}`
              );
            } else {
              verificationError = "Tailscale Serve is starting up or needs reconfiguration";
              log.info(
                `[TAILSCALE STATUS] No Serve configuration found for port ${portToCheck} - may need restart`
              );
            }
          } catch (error) {
            log.debug(`Failed to check Tailscale Serve configuration: ${error}`);
          }
        } else if (processRunning && this.currentPort) {
          log.info(`[TAILSCALE STATUS] Verifying configuration for port ${this.currentPort}`);
          try {
            const isConfigured = await this.verifyServeConfiguration(this.currentPort);
            log.info(`[TAILSCALE STATUS] Configuration verified: ${isConfigured}`);
            if (!isConfigured) {
              actuallyRunning = false;
              verificationError = "Tailscale Serve proxy not configured for this port";
              log.info(
                "[TAILSCALE STATUS] Process running but not configured - normal configuration issue"
              );
            }
          } catch (error) {
            log.debug(`Failed to verify Tailscale Serve configuration: ${error}`);
            actuallyRunning = false;
          }
        }
        const desiredMode = this.desiredFunnel ? "public" : "private";
        const actualMode = this.funnelEnabled ? "public" : "private";
        const modesMatch = desiredMode === actualMode;
        const effectivelyRunning = actuallyRunning || modesMatch && this.currentPort !== null && !this.isStarting || this.funnelEnabled && actualMode === "public";
        const result = {
          isRunning: effectivelyRunning,
          port: effectivelyRunning ? this.currentPort ?? void 0 : void 0,
          // Don't show error if modes match and we're configured
          lastError: effectivelyRunning || modesMatch ? void 0 : verificationError || this.lastError,
          startTime: this.startTime,
          isPermanentlyDisabled: this.isPermanentlyDisabled,
          funnelEnabled: this.funnelEnabled,
          funnelStartTime: this.funnelStartTime,
          desiredMode,
          actualMode,
          funnelError: this.funnelError
        };
        log.info("[TAILSCALE STATUS] Returning status:");
        log.info(`  - isRunning: ${result.isRunning}`);
        log.info(`  - lastError: ${result.lastError}`);
        log.info(`  - isPermanentlyDisabled: ${result.isPermanentlyDisabled}`);
        log.info(`  - desiredMode: ${result.desiredMode}`);
        log.info(`  - actualMode: ${result.actualMode}`);
        log.info(`  - funnelError: ${result.funnelError}`);
        return result;
      }
      async status() {
        const s = await this.getStatus();
        return {
          running: s.isRunning,
          port: s.port,
          lastError: s.lastError,
          meta: {
            isPermanentlyDisabled: s.isPermanentlyDisabled,
            funnelEnabled: s.funnelEnabled,
            funnelStartTime: s.funnelStartTime,
            desiredMode: s.desiredMode,
            actualMode: s.actualMode,
            funnelError: s.funnelError
          }
        };
      }
      /**
       * Check if Tailscale Serve is available on this tailnet
       */
      async checkServeAvailability() {
        return new Promise((resolve35) => {
          const statusProcess = spawnImpl(this.tailscaleExecutable, ["serve", "status", "--json"], {
            stdio: ["ignore", "pipe", "pipe"]
          });
          let stdout = "";
          let stderr = "";
          if (statusProcess.stdout) {
            statusProcess.stdout.on("data", (data) => {
              stdout += data.toString();
            });
          }
          if (statusProcess.stderr) {
            statusProcess.stderr.on("data", (data) => {
              stderr += data.toString();
            });
          }
          statusProcess.on("exit", (code) => {
            if (stderr) {
              resolve35(stderr);
            } else if (code === 0) {
              try {
                JSON.parse(stdout);
                resolve35(stdout);
              } catch {
                resolve35(stdout);
              }
            } else {
              resolve35(stdout || "Tailscale Serve status check failed");
            }
          });
          statusProcess.on("error", (error) => {
            resolve35(error.message);
          });
          setTimeout(() => {
            if (!statusProcess.killed) {
              statusProcess.kill("SIGTERM");
              resolve35("Timeout checking Tailscale Serve availability");
            }
          }, 3e3);
        });
      }
      /**
       * Verify that Tailscale Serve is actually configured for the given port
       */
      async verifyServeConfiguration(port) {
        return new Promise((resolve35) => {
          const statusProcess = spawnImpl(this.tailscaleExecutable, ["serve", "status", "--json"], {
            stdio: ["ignore", "pipe", "pipe"]
          });
          let stdout = "";
          let stderr = "";
          if (statusProcess.stdout) {
            statusProcess.stdout.on("data", (data) => {
              stdout += data.toString();
            });
          }
          if (statusProcess.stderr) {
            statusProcess.stderr.on("data", (data) => {
              stderr += data.toString();
            });
          }
          statusProcess.on("exit", (code) => {
            if (code === 0) {
              try {
                const status4 = JSON.parse(stdout);
                const isConfigured = this.parseServeStatusJson(status4, port);
                log.debug(
                  `Tailscale Serve JSON status check: port ${port} configured = ${isConfigured}`
                );
                resolve35(isConfigured);
              } catch {
                log.debug("JSON parsing failed, trying text parsing as fallback");
                const isConfigured = this.parseServeStatus(stdout, port);
                log.debug(
                  `Tailscale Serve text status check: port ${port} configured = ${isConfigured}`
                );
                resolve35(isConfigured);
              }
            } else {
              log.debug(`Tailscale serve status failed with code ${code}: ${stderr}`);
              resolve35(false);
            }
          });
          statusProcess.on("error", (error) => {
            log.debug(`Failed to run tailscale serve status: ${error.message}`);
            resolve35(false);
          });
          setTimeout(() => {
            if (!statusProcess.killed) {
              statusProcess.kill("SIGTERM");
              resolve35(false);
            }
          }, 3e3);
        });
      }
      /**
       * Parse JSON output from 'tailscale serve status --json' to check if our port is configured
       */
      parseServeStatusJson(status4, port) {
        const statusData = status4;
        try {
          log.debug(`Parsing Tailscale serve JSON status for port ${port}:`);
          log.debug(`JSON status: ${JSON.stringify(status4, null, 2)}`);
          const webConfigs = [];
          if (statusData.Web) {
            webConfigs.push(statusData.Web);
          }
          if (statusData.Foreground) {
            for (const nodeId in statusData.Foreground) {
              const nodeConfig = statusData.Foreground[nodeId];
              if (nodeConfig?.Web) {
                webConfigs.push(nodeConfig.Web);
              }
            }
          }
          for (const webConfig of webConfigs) {
            for (const host in webConfig) {
              const handlers = webConfig[host]?.Handlers;
              if (handlers) {
                log.debug(`Checking handlers for host: ${host}`);
                for (const path4 in handlers) {
                  const proxy = handlers[path4]?.Proxy;
                  if (proxy) {
                    log.debug(`Found proxy config: ${path4} -> ${proxy}`);
                    if (proxy.includes(`:${port}`) || proxy.includes(`127.0.0.1:${port}`) || proxy.includes(`localhost:${port}`)) {
                      log.info(`\u2705 Found Tailscale Serve config for port ${port}: ${proxy}`);
                      let funnelEnabled = false;
                      if (statusData.AllowFunnel?.[host]) {
                        funnelEnabled = true;
                        log.info(`\u{1F30D} Funnel is enabled for ${host}`);
                      } else if (statusData.Foreground) {
                        for (const nodeId in statusData.Foreground) {
                          const nodeConfig = statusData.Foreground[nodeId];
                          if (nodeConfig?.AllowFunnel?.[host]) {
                            funnelEnabled = true;
                            log.info(`\u{1F30D} Funnel is enabled for ${host} (node ${nodeId})`);
                            break;
                          }
                        }
                      }
                      this.funnelEnabled = funnelEnabled;
                      if (funnelEnabled && !this.funnelStartTime) {
                        this.funnelStartTime = /* @__PURE__ */ new Date();
                      }
                      return true;
                    }
                  }
                }
              }
            }
          }
          log.warn(`\u274C No proxy configuration found for port ${port} in JSON status`);
          log.debug(`Available proxy configurations:`);
          for (const webConfig of webConfigs) {
            for (const host in webConfig) {
              const handlers = webConfig[host]?.Handlers;
              if (handlers) {
                for (const path4 in handlers) {
                  const proxy = handlers[path4]?.Proxy;
                  if (proxy) {
                    log.debug(`  - ${host}${path4} -> ${proxy}`);
                  }
                }
              }
            }
          }
          return false;
        } catch (error) {
          log.info("Failed to parse JSON status:", error);
          return false;
        }
      }
      /**
       * Parse the output of 'tailscale serve status' to check if our port is configured
       */
      parseServeStatus(output, port) {
        log.debug(`Parsing Tailscale serve status output for port ${port}:`);
        log.debug(`Raw output: ${JSON.stringify(output)}`);
        const lines = output.split("\n");
        log.debug(`Split into ${lines.length} lines`);
        for (const line of lines) {
          const trimmedLine = line.trim();
          if (trimmedLine) {
            log.debug(`Checking line: "${trimmedLine}"`);
          }
          if (line.includes(`127.0.0.1:${port}`) || line.includes(`localhost:${port}`)) {
            log.info(`Found proxy configuration for port ${port} in line: "${line.trim()}"`);
            return true;
          }
        }
        log.warn(`No proxy configuration found for port ${port} in Tailscale serve status`);
        return false;
      }
      cleanup() {
        if (this.serveProcess && !this.serveProcess.killed) {
          log.debug("Terminating orphaned Tailscale Serve process");
          try {
            this.serveProcess.kill("SIGTERM");
            setTimeout(() => {
              if (this.serveProcess && !this.serveProcess.killed) {
                log.warn("Force killing Tailscale Serve process");
                this.serveProcess.kill("SIGKILL");
              }
            }, 1e3);
          } catch (error) {
            log.info("Failed to kill Tailscale Serve process:", error);
          }
        }
        this.serveProcess = null;
        this.currentPort = null;
        this.isStarting = false;
        this.startTime = void 0;
        this.funnelEnabled = false;
        this.funnelStartTime = void 0;
      }
      async checkTailscaleAvailable() {
        const fs2 = await import("node:fs/promises");
        let tailscalePaths = [];
        if (process.platform === "darwin") {
          tailscalePaths = [
            "/Applications/Tailscale.app/Contents/MacOS/Tailscale",
            "/usr/local/bin/tailscale",
            "/opt/homebrew/bin/tailscale"
          ];
        } else if (process.platform === "linux") {
          tailscalePaths = [
            "/usr/bin/tailscale",
            "/usr/local/bin/tailscale",
            "/opt/tailscale/bin/tailscale",
            "/snap/bin/tailscale"
          ];
        }
        if (!testSkipTailscaleFsPaths) {
          for (const path4 of tailscalePaths) {
            try {
              await fs2.access(path4, fs2.constants.X_OK);
              this.tailscaleExecutable = path4;
              log.debug(`Found Tailscale at: ${path4}`);
              return;
            } catch {
            }
          }
        }
        return new Promise((resolve35, reject) => {
          const checkProcess = spawnImpl("which", ["tailscale"], {
            stdio: ["ignore", "pipe", "pipe"]
          });
          checkProcess.on("exit", (code) => {
            if (code === 0) {
              resolve35();
            } else {
              reject(new Error("Tailscale command not found. Please install Tailscale first."));
            }
          });
          checkProcess.on("error", (error) => {
            reject(new Error(`Failed to check Tailscale availability: ${error.message}`));
          });
        });
      }
    };
    tailscaleServeService = new TailscaleServeServiceImpl();
  }
});

// packages/daemon/src/lib/tunnels/ngrok.ts
import { spawn as nodeSpawn2 } from "node:child_process";
import * as os from "node:os";
import * as path2 from "node:path";
var spawnImpl2, NgrokService;
var init_ngrok = __esm({
  "packages/daemon/src/lib/tunnels/ngrok.ts"() {
    "use strict";
    init_log();
    spawnImpl2 = nodeSpawn2;
    NgrokService = class {
      ngrokProcess = null;
      currentTunnel = null;
      isRunning = false;
      config;
      constructor(config2) {
        this.config = config2;
      }
      /**
       * Check if ngrok binary is available
       */
      async checkNgrokBinary() {
        const possiblePaths = [
          "ngrok",
          // Global PATH
          "/usr/local/bin/ngrok",
          "/opt/homebrew/bin/ngrok",
          path2.join(os.homedir(), ".local", "bin", "ngrok"),
          // Windows paths
          "C:\\Program Files\\ngrok\\ngrok.exe",
          path2.join(os.homedir(), "AppData", "Local", "ngrok", "ngrok.exe")
        ];
        for (const ngrokPath of possiblePaths) {
          try {
            const result = await new Promise((resolve35) => {
              const proc = spawnImpl2(ngrokPath, ["version"], { stdio: "ignore" });
              proc.on("close", (code) => resolve35(code === 0));
              proc.on("error", () => resolve35(false));
              setTimeout(() => {
                proc.kill();
                resolve35(false);
              }, 2e3);
            });
            if (result) {
              log.debug(`Found ngrok at: ${ngrokPath}`);
              return ngrokPath;
            }
          } catch {
          }
        }
        return null;
      }
      /**
       * Start ngrok tunnel
       */
      async start() {
        if (this.isRunning) {
          log.warn("Ngrok tunnel is already running");
          if (this.currentTunnel) {
            return this.currentTunnel;
          }
        }
        const ngrokPath = await this.checkNgrokBinary();
        if (!ngrokPath) {
          throw new Error("ngrok binary not found. Please install ngrok: https://ngrok.com/download");
        }
        const args = ["http", String(this.config.port), "--log=stdout", "--log-format=json"];
        if (this.config.authToken) {
          args.push("--authtoken", this.config.authToken);
        }
        if (this.config.domain) {
          args.push("--domain", this.config.domain);
        }
        if (this.config.region) {
          args.push("--region", this.config.region);
        }
        log.log(`Starting ngrok tunnel on port ${this.config.port}...`);
        return new Promise((resolve35, reject) => {
          this.ngrokProcess = spawnImpl2(ngrokPath, args, {
            stdio: ["ignore", "pipe", "pipe"]
          });
          let startupTimeout;
          let resolved2 = false;
          const cleanup = () => {
            if (startupTimeout) clearTimeout(startupTimeout);
          };
          const handleOutput = (data) => {
            const lines = data.toString().split("\n").filter(Boolean);
            for (const line of lines) {
              try {
                const record = JSON.parse(line);
                if (record.msg === "started tunnel" && record.url && !resolved2) {
                  resolved2 = true;
                  this.currentTunnel = {
                    publicUrl: record.url,
                    proto: "http",
                    name: "command_line",
                    uri: `http://localhost:${this.config.port}`
                  };
                  this.isRunning = true;
                  cleanup();
                  log.log(`Ngrok tunnel started: ${record.url}`);
                  resolve35(this.currentTunnel);
                }
                if (record.lvl === "error" || record.err) {
                  log.error("Ngrok error:", record.err || record.msg);
                }
              } catch {
                log.debug("Ngrok output:", line);
              }
            }
          };
          this.ngrokProcess.stdout?.on("data", handleOutput);
          this.ngrokProcess.stderr?.on("data", (data) => {
            log.error("Ngrok stderr:", data.toString());
          });
          this.ngrokProcess.on("error", (error) => {
            if (!resolved2) {
              resolved2 = true;
              this.isRunning = false;
              cleanup();
              reject(new Error(`Failed to start ngrok: ${error.message}`));
            }
          });
          this.ngrokProcess.on("close", (code) => {
            this.isRunning = false;
            this.currentTunnel = null;
            if (code !== 0 && code !== null) {
              log.error(`Ngrok process exited with code ${code}`);
            }
          });
          startupTimeout = setTimeout(() => {
            if (!resolved2) {
              resolved2 = true;
              this.stop().catch(() => {
              });
              reject(new Error("Ngrok startup timeout - tunnel failed to start"));
            }
          }, this.config.startupTimeoutMs ?? 3e4);
        });
      }
      /**
       * Stop ngrok tunnel
       */
      async stop() {
        if (!this.ngrokProcess) {
          return;
        }
        log.log("Stopping ngrok tunnel...");
        return new Promise((resolve35) => {
          if (!this.ngrokProcess) {
            resolve35();
            return;
          }
          const killTimeout = setTimeout(() => {
            if (this.ngrokProcess) {
              log.warn("Ngrok process did not exit gracefully, forcing kill");
              this.ngrokProcess.kill("SIGKILL");
            }
            resolve35();
          }, 5e3);
          this.ngrokProcess.on("close", () => {
            clearTimeout(killTimeout);
            this.ngrokProcess = null;
            this.currentTunnel = null;
            this.isRunning = false;
            log.log("Ngrok tunnel stopped");
            resolve35();
          });
          this.ngrokProcess.kill("SIGTERM");
        });
      }
      /**
       * Get current tunnel information
       */
      getTunnel() {
        return this.currentTunnel;
      }
      /**
       * Check if tunnel is running
       */
      isActive() {
        return this.isRunning;
      }
      /**
       * Get public URL if available
       */
      getPublicUrl() {
        return this.currentTunnel?.publicUrl || null;
      }
      async status() {
        return {
          running: this.isActive(),
          publicUrl: this.getPublicUrl(),
          port: this.config.port
        };
      }
    };
  }
});

// packages/daemon/src/lib/tunnels/cloudflare.ts
import { spawn as nodeSpawn3 } from "node:child_process";
import * as os2 from "node:os";
import * as path3 from "node:path";
var spawnImpl3, CloudflareService;
var init_cloudflare = __esm({
  "packages/daemon/src/lib/tunnels/cloudflare.ts"() {
    "use strict";
    init_log();
    spawnImpl3 = nodeSpawn3;
    CloudflareService = class _CloudflareService {
      cloudflaredProcess = null;
      currentTunnel = null;
      isRunning = false;
      cloudflaredPath = null;
      port;
      options;
      constructor(port, options) {
        this.port = port;
        this.options = options;
      }
      /**
       * Standard paths to check for cloudflared binary
       */
      static cloudflaredPaths = [
        "cloudflared",
        // Global PATH
        "/usr/local/bin/cloudflared",
        "/opt/homebrew/bin/cloudflared",
        "/usr/bin/cloudflared",
        path3.join(os2.homedir(), ".cloudflared", "cloudflared"),
        // Windows paths
        "C:\\Program Files\\Cloudflare\\cloudflared\\cloudflared.exe",
        path3.join(os2.homedir(), "AppData", "Local", "cloudflared", "cloudflared.exe")
      ];
      /**
       * Check if cloudflared binary is available
       */
      async checkCloudflaredBinary() {
        for (const cloudflaredPath of _CloudflareService.cloudflaredPaths) {
          try {
            const result = await new Promise((resolve35) => {
              const proc = spawnImpl3(cloudflaredPath, ["--version"], { stdio: "ignore" });
              proc.on("close", (code) => resolve35(code === 0));
              proc.on("error", () => resolve35(false));
              setTimeout(() => {
                proc.kill();
                resolve35(false);
              }, 2e3);
            });
            if (result) {
              log.debug(`Found cloudflared at: ${cloudflaredPath}`);
              return cloudflaredPath;
            }
          } catch {
          }
        }
        return null;
      }
      /**
       * Start Cloudflare tunnel (Quick Tunnel)
       */
      async start() {
        if (this.isRunning) {
          log.warn("Cloudflare tunnel is already running");
          if (this.currentTunnel) {
            return this.currentTunnel;
          }
        }
        this.cloudflaredPath = await this.checkCloudflaredBinary();
        if (!this.cloudflaredPath) {
          throw new Error(
            "cloudflared binary not found. Please install cloudflared: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/"
          );
        }
        const args = ["tunnel", "--url", `http://localhost:${this.port}`];
        log.log(`Starting Cloudflare tunnel on port ${this.port}...`);
        const cloudflaredPath = this.cloudflaredPath;
        if (!cloudflaredPath) {
          throw new Error(
            "cloudflared binary not found. Please install cloudflared: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/"
          );
        }
        return new Promise((resolve35, reject) => {
          this.cloudflaredProcess = spawnImpl3(cloudflaredPath, args, {
            stdio: ["ignore", "pipe", "pipe"]
          });
          let startupTimeout;
          let resolved2 = false;
          const cleanup = () => {
            if (startupTimeout) clearTimeout(startupTimeout);
          };
          const handleOutput = (data) => {
            const output = data.toString();
            log.debug("Cloudflare output:", output);
            const urlMatch = output.match(/https:\/\/[a-z0-9-]+\.trycloudflare\.com/);
            if (urlMatch && !resolved2) {
              resolved2 = true;
              const publicUrl = urlMatch[0];
              this.currentTunnel = {
                publicUrl,
                proto: "https",
                name: "cloudflare-quick-tunnel",
                uri: `http://localhost:${this.port}`
              };
              this.isRunning = true;
              cleanup();
              log.log(`Cloudflare tunnel started: ${publicUrl}`);
              resolve35(this.currentTunnel);
            }
            if (output.toLowerCase().includes("error") && !resolved2) {
              log.error("Cloudflare error:", output);
            }
          };
          this.cloudflaredProcess.stdout?.on("data", handleOutput);
          this.cloudflaredProcess.stderr?.on("data", handleOutput);
          this.cloudflaredProcess.on("error", (error) => {
            if (!resolved2) {
              resolved2 = true;
              this.isRunning = false;
              cleanup();
              reject(new Error(`Failed to start cloudflared: ${error.message}`));
            }
          });
          this.cloudflaredProcess.on("close", (code) => {
            this.isRunning = false;
            this.currentTunnel = null;
            if (code !== 0 && code !== null) {
              log.error(`Cloudflared process exited with code ${code}`);
            }
          });
          startupTimeout = setTimeout(() => {
            if (!resolved2) {
              resolved2 = true;
              this.stop().catch(() => {
              });
              reject(new Error("Cloudflare tunnel startup timeout - tunnel failed to start"));
            }
          }, this.options?.startupTimeoutMs ?? 3e4);
        });
      }
      /**
       * Stop Cloudflare tunnel
       */
      async stop() {
        if (!this.cloudflaredProcess) {
          return;
        }
        log.log("Stopping Cloudflare tunnel...");
        return new Promise((resolve35) => {
          if (!this.cloudflaredProcess) {
            resolve35();
            return;
          }
          const killTimeout = setTimeout(() => {
            if (this.cloudflaredProcess) {
              log.warn("Cloudflared process did not exit gracefully, forcing kill");
              this.cloudflaredProcess.kill("SIGKILL");
            }
            resolve35();
          }, 5e3);
          this.cloudflaredProcess.on("close", () => {
            clearTimeout(killTimeout);
            this.cloudflaredProcess = null;
            this.currentTunnel = null;
            this.isRunning = false;
            log.log("Cloudflare tunnel stopped");
            resolve35();
          });
          this.cloudflaredProcess.kill("SIGTERM");
        });
      }
      /**
       * Get current tunnel information
       */
      getTunnel() {
        return this.currentTunnel;
      }
      /**
       * Check if tunnel is running
       */
      isActive() {
        return this.isRunning;
      }
      /**
       * Get public URL if available
       */
      getPublicUrl() {
        return this.currentTunnel?.publicUrl || null;
      }
      async status() {
        return {
          running: this.isActive(),
          publicUrl: this.getPublicUrl(),
          port: this.port
        };
      }
      /**
       * Check if cloudflared is installed
       */
      async checkInstallation() {
        this.cloudflaredPath = await this.checkCloudflaredBinary();
        return this.cloudflaredPath !== null;
      }
    };
  }
});

// packages/daemon/src/lib/tunnels/manager.ts
var manager_exports = {};
__export(manager_exports, {
  TunnelManager: () => TunnelManager
});
import { execFileSync as execFileSync7 } from "node:child_process";
var TunnelManager;
var init_manager = __esm({
  "packages/daemon/src/lib/tunnels/manager.ts"() {
    "use strict";
    init_tailscale();
    init_ngrok();
    init_cloudflare();
    init_event_log();
    TunnelManager = class {
      service = null;
      provider = null;
      session;
      dir;
      constructor(opts) {
        this.session = opts.session ?? null;
        this.dir = opts.dir;
      }
      async start(config2) {
        if (this.service) {
          const current = await this.service.status();
          if (current.running) {
            return current;
          }
        }
        this.provider = config2.provider;
        this.service = this.createService(config2);
        try {
          await this.service.start();
          const status4 = await this.service.status();
          if (status4.publicUrl) {
            this.setTmuxVar(status4.publicUrl);
          }
          this.emitEvent("info", `Tunnel started via ${config2.provider}`, status4.publicUrl);
          return status4;
        } catch (err) {
          const msg = err instanceof Error ? err.message : String(err);
          this.emitEvent("error", `Tunnel start failed: ${msg}`, null);
          throw err;
        }
      }
      async stop() {
        if (!this.service) return;
        try {
          await this.service.stop();
          this.clearTmuxVar();
          this.emitEvent("info", `Tunnel stopped (${this.provider})`, null);
        } finally {
          this.service = null;
          this.provider = null;
        }
      }
      async status() {
        if (!this.service) {
          return { running: false, provider: null };
        }
        const s = await this.service.status();
        return { ...s, provider: this.provider };
      }
      async url() {
        if (!this.service) return null;
        const s = await this.service.status();
        return s.publicUrl ?? null;
      }
      createService(config2) {
        switch (config2.provider) {
          case "tailscale":
            return new TailscaleServeServiceImpl();
          case "ngrok":
            return new NgrokService({
              port: config2.port,
              authToken: config2.authToken,
              domain: config2.domain,
              region: config2.region,
              startupTimeoutMs: config2.startupTimeoutMs
            });
          case "cloudflare":
            return new CloudflareService(config2.port, {
              startupTimeoutMs: config2.startupTimeoutMs
            });
        }
      }
      setTmuxVar(url) {
        if (!this.session) return;
        try {
          execFileSync7("tmux", ["set-option", "-t", this.session, "@tunnel_url", url], {
            stdio: "ignore"
          });
        } catch {
        }
      }
      clearTmuxVar() {
        if (!this.session) return;
        try {
          execFileSync7("tmux", ["set-option", "-t", this.session, "-u", "@tunnel_url"], {
            stdio: "ignore"
          });
        } catch {
        }
      }
      emitEvent(level, message, url) {
        try {
          appendEvent(this.dir, {
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            type: level === "error" ? "error" : "notify",
            target: "tunnel",
            message: url ? `${message} \u2014 ${url}` : message
          });
        } catch {
        }
      }
    };
  }
});

// packages/daemon/src/lib/hq/registry.ts
var registry_exports = {};
__export(registry_exports, {
  RemoteRegistry: () => RemoteRegistry
});
var LOG, RemoteRegistry;
var init_registry4 = __esm({
  "packages/daemon/src/lib/hq/registry.ts"() {
    "use strict";
    init_log();
    LOG = "remote-registry";
    RemoteRegistry = class {
      machines = /* @__PURE__ */ new Map();
      machinesByName = /* @__PURE__ */ new Map();
      sessionToMachine = /* @__PURE__ */ new Map();
      healthCheckTimer = null;
      healthInterval;
      healthTimeout;
      maxMachines;
      isShuttingDown;
      constructor(opts) {
        this.healthInterval = opts?.healthInterval ?? 15e3;
        this.healthTimeout = opts?.healthTimeout ?? 5e3;
        this.maxMachines = opts?.maxMachines ?? 256;
        this.isShuttingDown = opts?.isShuttingDown ?? (() => false);
        this.startHealthChecker();
      }
      register(remote) {
        const existing = this.machines.get(remote.id);
        if (existing) {
          if (existing.name !== remote.name) this.machinesByName.delete(existing.name);
          existing.name = remote.name;
          existing.url = remote.url;
          existing.token = remote.token;
          existing.lastHeartbeat = /* @__PURE__ */ new Date();
          this.machinesByName.set(remote.name, existing);
          logger.debug(LOG, `Machine re-registered: ${remote.name} (${remote.id})`);
          return existing;
        }
        if (this.machinesByName.has(remote.name)) {
          throw new Error(`Machine with name '${remote.name}' is already registered`);
        }
        if (this.machines.size >= this.maxMachines) {
          throw new Error(`HQ registry full (${this.maxMachines} machines)`);
        }
        const now = /* @__PURE__ */ new Date();
        const machine = {
          ...remote,
          registeredAt: now,
          lastHeartbeat: now,
          sessionIds: /* @__PURE__ */ new Set()
        };
        this.machines.set(remote.id, machine);
        this.machinesByName.set(remote.name, machine);
        logger.info(LOG, `Machine registered: ${remote.name} (${remote.id}) from ${remote.url}`);
        void this.checkMachineHealth(machine);
        return machine;
      }
      unregister(id) {
        const machine = this.machines.get(id);
        if (!machine) return false;
        for (const sid of machine.sessionIds) {
          this.sessionToMachine.delete(sid);
        }
        this.machinesByName.delete(machine.name);
        this.machines.delete(id);
        logger.info(LOG, `Machine unregistered: ${machine.name} (${id})`);
        return true;
      }
      getMachine(id) {
        return this.machines.get(id);
      }
      getMachines() {
        return Array.from(this.machines.values());
      }
      getMachineBySession(sessionId) {
        const mid = this.sessionToMachine.get(sessionId);
        return mid ? this.machines.get(mid) : void 0;
      }
      updateSessions(machineId, sessionIds) {
        const machine = this.machines.get(machineId);
        if (!machine) return;
        for (const old of machine.sessionIds) {
          this.sessionToMachine.delete(old);
        }
        machine.sessionIds = new Set(sessionIds);
        for (const sid of sessionIds) {
          this.sessionToMachine.set(sid, machineId);
        }
      }
      async checkMachineHealth(machine) {
        if (this.isShuttingDown()) return;
        try {
          const controller = new AbortController();
          const tid = setTimeout(() => controller.abort(), this.healthTimeout);
          const res = await fetch(`${machine.url}/health`, {
            headers: { Authorization: `Bearer ${machine.token}` },
            signal: controller.signal
          });
          clearTimeout(tid);
          if (res.ok) {
            machine.lastHeartbeat = /* @__PURE__ */ new Date();
            logger.debug(LOG, `Health OK: ${machine.name}`);
          } else {
            throw new Error(`HTTP ${res.status}`);
          }
        } catch (err) {
          if (this.isShuttingDown()) return;
          logger.warn(LOG, `Health check failed, pruning: ${machine.name} (${machine.id})`, {
            error: String(err)
          });
          this.unregister(machine.id);
        }
      }
      startHealthChecker() {
        this.healthCheckTimer = setInterval(() => {
          if (this.isShuttingDown()) return;
          const checks = this.getMachines().map((m) => this.checkMachineHealth(m));
          Promise.all(checks).catch(() => {
          });
        }, this.healthInterval);
      }
      destroy() {
        if (this.healthCheckTimer) {
          clearInterval(this.healthCheckTimer);
          this.healthCheckTimer = null;
        }
        logger.info(LOG, "Registry destroyed");
      }
    };
  }
});

// packages/daemon/src/lib/hq/client.ts
var client_exports = {};
__export(client_exports, {
  HQClient: () => HQClient
});
import * as crypto3 from "node:crypto";
var LOG2, HQClient;
var init_client5 = __esm({
  "packages/daemon/src/lib/hq/client.ts"() {
    "use strict";
    init_log();
    LOG2 = "hq-client";
    HQClient = class {
      hqUrl;
      remoteId;
      machineName;
      secret;
      remoteUrl;
      bearerToken;
      heartbeatInterval;
      heartbeatTimer = null;
      backoffMs = 1e3;
      destroyed = false;
      constructor(config2) {
        this.hqUrl = config2.hqUrl;
        this.remoteId = crypto3.randomUUID();
        this.machineName = config2.machineName;
        this.secret = config2.secret;
        this.remoteUrl = config2.remoteUrl;
        this.bearerToken = config2.bearerToken ?? crypto3.randomBytes(32).toString("hex");
        this.heartbeatInterval = config2.heartbeatInterval ?? 15e3;
        logger.debug(LOG2, "HQ client initialized", {
          hqUrl: this.hqUrl,
          machineName: this.machineName,
          remoteId: this.remoteId
        });
      }
      basicAuth() {
        return `Basic ${Buffer.from(this.secret).toString("base64")}`;
      }
      async register() {
        logger.info(LOG2, `Registering with HQ at ${this.hqUrl}`);
        const payload = {
          id: this.remoteId,
          name: this.machineName,
          url: this.remoteUrl,
          token: this.bearerToken
        };
        const response = await fetch(`${this.hqUrl}/api/hq/register`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: this.basicAuth()
          },
          body: JSON.stringify(payload)
        });
        if (!response.ok) {
          const text = await response.text();
          throw new Error(`Registration failed (${response.status}): ${text}`);
        }
        logger.info(LOG2, `Registered: ${this.machineName} (${this.remoteId})`);
        this.backoffMs = 1e3;
        this.startHeartbeat();
      }
      startHeartbeat() {
        if (this.heartbeatTimer) clearInterval(this.heartbeatTimer);
        this.heartbeatTimer = setInterval(() => void this.sendHeartbeat(), this.heartbeatInterval);
      }
      async sendHeartbeat() {
        if (this.destroyed) return;
        try {
          const res = await fetch(`${this.hqUrl}/api/hq/register`, {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              Authorization: this.basicAuth()
            },
            body: JSON.stringify({
              id: this.remoteId,
              name: this.machineName,
              url: this.remoteUrl,
              token: this.bearerToken
            })
          });
          if (res.ok) {
            this.backoffMs = 1e3;
            logger.debug(LOG2, "Heartbeat OK");
          } else {
            throw new Error(`HTTP ${res.status}`);
          }
        } catch (err) {
          logger.warn(LOG2, `Heartbeat failed, backoff ${this.backoffMs}ms`, {
            error: String(err)
          });
          this.backoffMs = Math.min(this.backoffMs * 2, 6e4);
        }
      }
      async destroy() {
        this.destroyed = true;
        if (this.heartbeatTimer) {
          clearInterval(this.heartbeatTimer);
          this.heartbeatTimer = null;
        }
        logger.info(LOG2, `Deregistering from HQ: ${this.machineName} (${this.remoteId})`);
        try {
          await fetch(`${this.hqUrl}/api/hq/machines/${this.remoteId}`, {
            method: "DELETE",
            headers: { Authorization: this.basicAuth() }
          });
        } catch {
        }
      }
      getRemoteId() {
        return this.remoteId;
      }
      getToken() {
        return this.bearerToken;
      }
      getName() {
        return this.machineName;
      }
    };
  }
});

// packages/daemon/src/lib/daemon-embed.ts
var daemon_embed_exports = {};
__export(daemon_embed_exports, {
  startEmbeddedDaemon: () => startEmbeddedDaemon
});
import { execFileSync as execFileSync8 } from "node:child_process";
import { randomBytes as randomBytes5 } from "node:crypto";
import { createServer as createServer2 } from "node:http";
import { createRequire as createRequire3 } from "node:module";
import { existsSync as existsSync37, readdirSync as readdirSync11, statSync as statSync10, unlinkSync as unlinkSync5 } from "node:fs";
import { join as join40 } from "node:path";
import { WebSocket, WebSocketServer as WebSocketServer2 } from "ws";
function resolveOwnVersion() {
  for (const candidate of ["../../package.json", "../package.json"]) {
    try {
      const pkg = requireFromHere(candidate);
      if (typeof pkg.version === "string") return pkg.version;
    } catch {
    }
  }
  return "0.0.0";
}
function tmux3(...args) {
  return execFileSync8("tmux", args, {
    encoding: "utf-8",
    // Pipe stdio explicitly. Inheriting (the default) inherits the parent's
    // file descriptors; when the daemon is launched detached (nohup, disown,
    // launchd, etc.) the controlling terminal's fds can be invalid, and the
    // child spawn fails with EBADF. The visible symptom is sessionExists()
    // returning false → stopSelf → ghost daemon.
    stdio: ["ignore", "pipe", "pipe"]
  }).trim();
}
function tmuxSilent2(...args) {
  try {
    return tmux3(...args);
  } catch {
    return "";
  }
}
function assertTmuxSession(sessionName) {
  try {
    tmux3("has-session", "-t", sessionName);
  } catch (err) {
    throw new DaemonStartupError(
      `tmux session "${sessionName}" does not exist`,
      "tmux_session_missing",
      { cause: err }
    );
  }
}
function validatePort(port) {
  if (!Number.isInteger(port) || port < 1 || port > 65535) {
    throw new DaemonStartupError(`Invalid daemon port: ${port}`, "port_invalid");
  }
}
async function pickFreePort(hostname3) {
  const probe = createServer2();
  return await new Promise((resolve35, reject) => {
    probe.once("error", reject);
    probe.listen(0, hostname3, () => {
      const address = probe.address();
      const port = typeof address === "object" && address ? address.port : null;
      probe.close(() => {
        if (port) resolve35(port);
        else reject(new DaemonStartupError("Could not allocate daemon port", "bind_failed"));
      });
    });
  });
}
function sessionExists(sessionName) {
  try {
    tmux3("has-session", "-t", sessionName);
    return "yes";
  } catch (err) {
    const msg = err.message ?? "";
    const code = err.code;
    if (code === "EBADF" || code === "EAGAIN" || code === "EMFILE" || code === "ENFILE" || msg.includes("EBADF") || msg.includes("EAGAIN")) {
      console.error("[daemon] sessionExists transient spawn error:", msg);
      return "unknown";
    }
    return "no";
  }
}
function hasClients() {
  return tmuxSilent2("list-clients").length > 0;
}
function listPanes2(sessionName) {
  const raw = tmuxSilent2(
    "list-panes",
    "-t",
    sessionName,
    "-F",
    "#{pane_id}	#{pane_pid}	#{pane_current_command}	#{pane_title}	#{@ide_role}	#{@ide_type}	#{@ide_name}"
  );
  if (!raw) return [];
  return raw.split("\n").map((line) => {
    const [id, pid, cmd, title, role, type, name] = line.split("	");
    return {
      id,
      pid,
      cmd,
      title,
      role: role || void 0,
      type: type || void 0,
      name: name || void 0
    };
  });
}
function cleanupDispatchFiles(dir) {
  const dispatchDir = join40(dir, ".tasks", "dispatch");
  if (!existsSync37(dispatchDir)) return;
  const cutoff = Date.now() - 24 * 60 * 60 * 1e3;
  try {
    for (const file of readdirSync11(dispatchDir)) {
      const filePath = join40(dispatchDir, file);
      try {
        const mtime = statSync10(filePath).mtimeMs;
        if (mtime < cutoff) unlinkSync5(filePath);
      } catch {
      }
    }
  } catch {
  }
}
async function startOrchestrator(sessionName, dir) {
  try {
    const { readConfig: readConfig2 } = await Promise.resolve().then(() => (init_yaml_io(), yaml_io_exports));
    const { config: config2 } = readConfig2(dir);
    if (!config2.orchestrator?.enabled) return () => void 0;
    const { createOrchestrator: createOrchestrator2 } = await Promise.resolve().then(() => (init_orchestrator(), orchestrator_exports));
    if (config2.orchestrator.webhooks?.length) {
      const { setWebhookConfig: setWebhookConfig2 } = await Promise.resolve().then(() => (init_event_log(), event_log_exports));
      setWebhookConfig2(config2.orchestrator.webhooks);
    }
    const paneSpecialties = /* @__PURE__ */ new Map();
    for (const row of config2.rows) {
      for (const pane of row.panes) {
        if (pane.specialty && pane.title) {
          paneSpecialties.set(
            pane.title,
            pane.specialty.split(",").map((s) => s.trim().toLowerCase())
          );
        }
      }
    }
    cleanupDispatchFiles(dir);
    const orch = config2.orchestrator;
    return createOrchestrator2({
      session: sessionName,
      dir,
      autoDispatch: orch.auto_dispatch ?? true,
      stallTimeout: orch.stall_timeout ?? 3e5,
      pollInterval: orch.poll_interval ?? 5e3,
      masterPane: orch.master_pane ?? null,
      beforeRun: orch.before_run ?? null,
      afterRun: orch.after_run ?? null,
      maxConcurrentAgents: orch.max_concurrent_agents ?? 10,
      dispatchMode: orch.dispatch_mode ?? "tasks",
      paneSpecialties,
      services: orch.services ?? {},
      research: orch.research
    });
  } catch (err) {
    console.error("[daemon] Orchestrator failed to start:", err);
    return () => void 0;
  }
}
function bearerToken2(authHeader) {
  const value = Array.isArray(authHeader) ? authHeader[0] : authHeader;
  if (!value?.startsWith("Bearer ")) return null;
  return value.slice("Bearer ".length);
}
function requestToken(req) {
  const headerToken = bearerToken2(req.headers.authorization);
  if (headerToken) return headerToken;
  try {
    const url = new URL(req.url ?? "/", "http://localhost");
    return url.searchParams.get("token");
  } catch {
    return null;
  }
}
function isLoopbackRequest(req) {
  const remote = req.socket.remoteAddress ?? "";
  return remote === "127.0.0.1" || remote === "::1" || remote === "::ffff:127.0.0.1" || remote.startsWith("127.");
}
function isLoopbackBind(bindHostname) {
  return bindHostname === "127.0.0.1" || bindHostname === "::1" || bindHostname === "localhost";
}
function isUpgradeAuthorized(req, token, localBypassToken, bindHostname) {
  if (!token) return true;
  if (isLoopbackBind(bindHostname) && isLoopbackRequest(req)) return true;
  const supplied = requestToken(req);
  return supplied === token || localBypassToken != null && supplied === localBypassToken;
}
function rejectUpgradeWithPolicy(wss, req, socket, head) {
  wss.handleUpgrade(req, socket, head, (ws) => {
    ws.close(1008, "Remote access token required");
  });
}
function attachWebSockets(server, opts = {}) {
  const eventsWss = new WebSocketServer2({ noServer: true });
  const ptyWss = new WebSocketServer2({ noServer: true });
  const clients2 = /* @__PURE__ */ new Set();
  const track = (ws) => {
    clients2.add(ws);
    ws.on("close", () => clients2.delete(ws));
    ws.on("error", () => clients2.delete(ws));
  };
  const upgradeListener = (req, socket, head) => {
    const pathname = (req.url ?? "/").split("?")[0] ?? "/";
    if ((pathname === "/ws/events" || pathname.startsWith("/ws/pty/")) && !isUpgradeAuthorized(req, opts.authToken, opts.localBypassToken, opts.bindHostname)) {
      rejectUpgradeWithPolicy(pathname === "/ws/events" ? eventsWss : ptyWss, req, socket, head);
      return;
    }
    if (pathname === "/ws/events") {
      eventsWss.handleUpgrade(req, socket, head, (ws) => {
        track(ws);
        handleWsEventsConnection(ws);
      });
      return;
    }
    const ptyMatch = pathname.match(/^\/ws\/pty\/([^/]+)$/);
    if (ptyMatch) {
      const id = decodeURIComponent(ptyMatch[1]);
      ptyWss.handleUpgrade(req, socket, head, (ws) => {
        track(ws);
        handlePtyWebSocket(ws, id);
      });
      return;
    }
  };
  server.on("upgrade", upgradeListener);
  return {
    closeClients: () => {
      for (const ws of clients2) {
        if (ws.readyState === WebSocket.OPEN || ws.readyState === WebSocket.CONNECTING) {
          closeWsGoingAway(ws);
        }
      }
    },
    closeServers: async () => {
      server.off("upgrade", upgradeListener);
      for (const ws of clients2) {
        if (ws.readyState === WebSocket.OPEN || ws.readyState === WebSocket.CLOSING) {
          ws.terminate();
        }
      }
      const closeWss = (wss) => Promise.race([new Promise((resolve35) => wss.close(() => resolve35())), delay(100)]);
      await Promise.all([closeWss(eventsWss), closeWss(ptyWss)]);
    }
  };
}
function waitForServerClose(server) {
  return new Promise((resolve35, reject) => {
    server.close((err) => {
      if (err) reject(err);
      else resolve35();
    });
  });
}
function delay(ms) {
  return new Promise((resolve35) => setTimeout(resolve35, ms));
}
function generateLocalBypassToken() {
  return randomBytes5(32).toString("base64url");
}
function probeHostname2(bindHostname) {
  return bindHostname === "0.0.0.0" ? "127.0.0.1" : bindHostname;
}
function timeoutSignal2(ms) {
  const controller = new AbortController();
  setTimeout(() => controller.abort(), ms).unref?.();
  return controller.signal;
}
async function healthGone(port, bindHostname) {
  try {
    const res = await fetch(`http://${probeHostname2(bindHostname)}:${port}/health`, {
      signal: timeoutSignal2(500)
    });
    return !res.ok;
  } catch {
    return true;
  }
}
async function requestDaemonShutdown(port, bindHostname) {
  try {
    await fetch(`http://${probeHostname2(bindHostname)}:${port}/api/v2/action/daemon.shutdown`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ reason: "takeover" }),
      signal: timeoutSignal2(1e3)
    });
  } catch {
  }
}
async function takeoverCanonicalDaemon(info) {
  await requestDaemonShutdown(info.port, info.bindHostname);
  const deadline = Date.now() + 1e4;
  while (Date.now() < deadline) {
    const current = readCanonicalDaemonInfo();
    const fileGone = !current || current.pid !== info.pid || current.port !== info.port;
    const serverGone = await healthGone(info.port, info.bindHostname);
    if (fileGone && serverGone) return;
    await delay(150);
  }
  try {
    process.kill(info.pid, "SIGTERM");
  } catch {
  }
  await delay(500);
  try {
    process.kill(info.pid, "SIGKILL");
  } catch {
  }
  clearCanonicalDaemonInfo();
}
function closeWsGoingAway(ws) {
  const reason = Buffer.from("going away");
  const payload = Buffer.allocUnsafe(2 + reason.length);
  payload.writeUInt16BE(1001, 0);
  reason.copy(payload, 2);
  const frame = Buffer.concat([Buffer.from([136, payload.length]), payload]);
  const socket = ws._socket;
  if (socket && !socket.destroyed && socket.writable) {
    socket.end(frame);
    return;
  }
  ws.close(1001, reason);
}
async function startHttpServer({
  sessionName,
  requestedPort,
  bindHostname,
  dir,
  authToken,
  localBypassToken,
  silent
}) {
  const { createApp: createApp3 } = await Promise.resolve().then(() => (init_server(), server_exports));
  const { getRequestListener: getRequestListener3 } = await import(requireFromHere.resolve("@hono/node-server"));
  const { AuthService: AuthService2 } = await Promise.resolve().then(() => (init_auth_service(), auth_service_exports));
  const { AuthConfigSchema: AuthConfigSchema2 } = await Promise.resolve().then(() => (init_types4(), types_exports3));
  const { TunnelManager: TunnelManager2 } = await Promise.resolve().then(() => (init_manager(), manager_exports));
  const { RemoteRegistry: RemoteRegistry2 } = await Promise.resolve().then(() => (init_registry4(), registry_exports));
  const { HQConfigSchema: HQConfigSchema2 } = await Promise.resolve().then(() => (init_types2(), types_exports));
  let authConfig = AuthConfigSchema2.parse({});
  let tunnelConfig;
  let hqConfig;
  try {
    const { readConfig: readConfig2 } = await Promise.resolve().then(() => (init_yaml_io(), yaml_io_exports));
    const { config: config2 } = readConfig2(dir);
    if (config2.auth) authConfig = AuthConfigSchema2.parse(config2.auth);
    if (config2.tunnel) tunnelConfig = config2.tunnel;
    if (config2.hq) hqConfig = HQConfigSchema2.parse(config2.hq);
  } catch {
  }
  const authService = new AuthService2(authConfig.secret);
  const tunnelManager = new TunnelManager2({ session: sessionName, dir });
  let registryShuttingDown = false;
  const remoteRegistry = hqConfig?.enabled && hqConfig.role === "hq" ? new RemoteRegistry2({
    healthInterval: hqConfig.heartbeat_interval,
    isShuttingDown: () => registryShuttingDown
  }) : void 0;
  const { hostname: osHostname } = await import("node:os");
  const app = createApp3({
    authService,
    authConfig,
    tunnelManager,
    remoteRegistry,
    hqMachineName: hqConfig?.machine_name ?? osHostname(),
    remoteAccess: {
      bindHostname,
      token: authToken ?? null,
      localBypassToken: localBypassToken ?? null
    }
  });
  app.get("/api/daemon/health", (c) => {
    const health = getTaskStoreHealth();
    return c.json({ ...health, ok: health.ok, session: sessionName });
  });
  const server = createServer2(getRequestListener3(app.fetch));
  const sockets = /* @__PURE__ */ new Set();
  server.on("connection", (socket) => {
    sockets.add(socket);
    socket.on("close", () => sockets.delete(socket));
  });
  const { closeClients, closeServers: closeWsServers } = attachWebSockets(server, {
    authToken,
    localBypassToken,
    bindHostname
  });
  await new Promise((resolve35, reject) => {
    const onError = (err) => {
      server.off("listening", onListening);
      if (err.code === "EADDRINUSE") {
        reject(
          new DaemonStartupError(`Port ${requestedPort} is already in use`, "port_in_use", {
            cause: err
          })
        );
      } else {
        reject(
          new DaemonStartupError(`Failed to bind daemon on port ${requestedPort}`, "bind_failed", {
            cause: err
          })
        );
      }
    };
    const onListening = () => {
      server.off("error", onError);
      if (!silent) {
        console.log(
          `[daemon] Command Center on http://${bindHostname}:${requestedPort} (session: ${sessionName})`
        );
      }
      resolve35();
    };
    server.once("error", onError);
    server.once("listening", onListening);
    server.listen(requestedPort, bindHostname);
  });
  if (remoteRegistry) {
    console.log("[daemon] HQ registry started");
  }
  if (tunnelConfig?.auto_start) {
    try {
      const { tunnelConfigSchema: tunnelConfigSchema2 } = await Promise.resolve().then(() => (init_types3(), types_exports2));
      const parsed = tunnelConfigSchema2.parse({
        ...tunnelConfig,
        port: tunnelConfig.port ?? requestedPort
      });
      await tunnelManager.start(parsed);
      console.log("[daemon] Tunnel auto-started");
    } catch (err) {
      console.error("[daemon] Tunnel auto-start failed:", err);
    }
  }
  let hqClient;
  if (hqConfig?.enabled && hqConfig.role === "remote" && hqConfig.hq_url) {
    try {
      const { HQClient: HQClient2 } = await Promise.resolve().then(() => (init_client5(), client_exports));
      const os3 = await import("node:os");
      hqClient = new HQClient2({
        hqUrl: hqConfig.hq_url,
        secret: hqConfig.secret ?? "",
        machineName: hqConfig.machine_name ?? os3.hostname(),
        remoteUrl: `http://${bindHostname}:${requestedPort}`,
        heartbeatInterval: hqConfig.heartbeat_interval
      });
      await hqClient.register();
      console.log(`[daemon] Registered with HQ as ${hqClient.getName()}`);
    } catch (err) {
      console.error("[daemon] HQ client registration failed:", err);
    }
  }
  const originalCloseClients = closeClients;
  const originalCloseWsServers = closeWsServers;
  return {
    server,
    sockets,
    closeClients: originalCloseClients,
    closeWsServers: async () => {
      registryShuttingDown = true;
      remoteRegistry?.destroy();
      if (hqClient) await hqClient.destroy();
      await originalCloseWsServers();
    }
  };
}
async function startEmbeddedDaemon(opts) {
  const sessionName = opts.sessionName ?? EMBEDDED_SESSION_NAME;
  const sessionless = opts.sessionName == null;
  const appSettings = readAppSettings();
  const persistedRemoteAccess = appSettings.remoteAccess.enabled && appSettings.remoteAccess.token ? appSettings.remoteAccess : null;
  const bindHostname = opts.bindHostname ?? opts.hostname ?? (persistedRemoteAccess ? "0.0.0.0" : DEFAULT_HOSTNAME);
  const authToken = opts.authToken ?? persistedRemoteAccess?.token ?? null;
  const localBypassToken = opts.localBypassToken ?? generateLocalBypassToken();
  const existingCanonical = readCanonicalDaemonInfo();
  if (existingCanonical) {
    if (await isCanonicalDaemonAlive(existingCanonical)) {
      if (opts.takeoverIfRunning) {
        await takeoverCanonicalDaemon(existingCanonical);
      } else {
        throw new DaemonStartupError(
          `Canonical daemon is already running on port ${existingCanonical.port}`,
          "canonical_already_running"
        );
      }
    } else {
      clearCanonicalDaemonInfo();
    }
  }
  if (!sessionless) assertTmuxSession(sessionName);
  const port = opts.port ?? await pickFreePort(bindHostname);
  validatePort(port);
  const dir = process.cwd();
  const workspaceRegistry = getDefaultWorkspaceRegistry();
  await workspaceRegistry.load();
  const legacySession = process.env.TMUX_IDE_SESSION;
  if (legacySession && !workspaceRegistry.has(legacySession)) {
    try {
      workspaceRegistry.add({
        name: legacySession,
        sessionName: legacySession,
        projectDir: dir
      });
    } catch {
    }
  }
  if (!sessionless && sessionName !== EMBEDDED_SESSION_NAME && !workspaceRegistry.has(sessionName)) {
    try {
      workspaceRegistry.add({
        name: sessionName,
        sessionName,
        projectDir: dir
      });
    } catch {
    }
  }
  const orchestratorStarter = opts.orchestratorStarter ?? startOrchestrator;
  const { server, sockets, closeClients, closeWsServers } = await startHttpServer({
    sessionName,
    requestedPort: port,
    bindHostname,
    dir,
    authToken,
    localBypassToken,
    silent: opts.silent
  });
  writeCanonicalDaemonInfo({
    pid: process.pid,
    port,
    version: resolveOwnVersion(),
    startedAt: (/* @__PURE__ */ new Date()).toISOString(),
    bindHostname,
    authToken
  });
  let stopRootTaskStoreWatcher = null;
  let rootReconcileInterval = null;
  let stopRootOrchestrator = null;
  if (!sessionless) {
    replayTaskStoreWal(dir);
    stopRootOrchestrator = await orchestratorStarter(sessionName, dir);
    stopRootTaskStoreWatcher = startTaskStoreWatcher(dir);
    rootReconcileInterval = setInterval(() => {
      reconcileTaskStore(dir);
    }, RECONCILE_INTERVAL_MS2);
    reconcileTaskStore(dir);
  }
  let lastState = "";
  let stopped = false;
  let stopping = null;
  let stopSelf = null;
  const activeProjectStops = /* @__PURE__ */ new Map();
  const activateProjectOnDaemon = async (projectName, options = {}) => {
    const existing = activeProjectStops.get(projectName);
    if (existing) {
      if (options.orchestrate && !existing.orchestrated) {
        const project2 = getProject(projectName);
        if (project2 && existsSync37(join40(project2.dir, "ide.yml"))) {
          existing.stop = await orchestratorStarter(project2.name, project2.dir);
          existing.orchestrated = true;
        }
      }
      return {
        stop: async () => {
          existing.stop();
        }
      };
    }
    const project = getProject(projectName);
    if (!project) {
      activeProjectStops.set(projectName, { stop: () => void 0, orchestrated: false });
      return {
        stop: async () => {
          const current = activeProjectStops.get(projectName);
          if (!current) return;
          activeProjectStops.delete(projectName);
          current.stop();
        }
      };
    }
    const shouldOrchestrate = options.orchestrate === true && existsSync37(join40(project.dir, "ide.yml"));
    const stopOrchestrator = shouldOrchestrate ? await orchestratorStarter(project.name, project.dir) : () => void 0;
    activeProjectStops.set(project.name, {
      stop: stopOrchestrator,
      orchestrated: shouldOrchestrate
    });
    return {
      stop: async () => {
        const current = activeProjectStops.get(project.name);
        if (!current) return;
        activeProjectStops.delete(project.name);
        current.stop();
      }
    };
  };
  setActivationBackend({
    activateProject: async (name, options) => {
      await activateProjectOnDaemon(name, options);
    },
    deactivateProject: async (name) => {
      const stop2 = activeProjectStops.get(name);
      if (!stop2) return;
      activeProjectStops.delete(name);
      stop2.stop();
    }
  });
  const tick = () => {
    if (sessionless) return;
    const session = sessionExists(sessionName);
    if (session === "no") {
      stopSelf?.();
      return;
    }
    if (session === "unknown") {
      return;
    }
    if (!hasClients()) return;
    const panes = listPanes2(sessionName);
    if (panes.length === 0) return;
    const portPanes = computePortPanes(panes);
    const agentStates = computeAgentStates(panes);
    const stateKey = panes.map((pane) => {
      const portState = portPanes.has(pane.id) ? "1" : "0";
      const agent = agentStates.get(pane.id) ?? "-";
      const titleDrift = pane.name && pane.title !== pane.name ? "d" : "ok";
      return `${pane.id}:${portState}:${agent}:${titleDrift}`;
    }).join("|");
    if (stateKey === lastState) return;
    for (const pane of panes) {
      const hasPort = portPanes.has(pane.id) ? "1" : "0";
      const agent = agentStates.get(pane.id);
      tmuxSilent2("set-option", "-pqt", pane.id, "@has_port", hasPort);
      tmuxSilent2("set-option", "-pqt", pane.id, "@agent_busy", agent === "busy" ? "1" : "0");
      tmuxSilent2("set-option", "-pqt", pane.id, "@agent_idle", agent === "idle" ? "1" : "0");
      if (pane.name && pane.title !== pane.name) {
        tmuxSilent2("select-pane", "-t", pane.id, "-T", pane.name);
      }
    }
    tmuxSilent2("refresh-client", "-S");
    lastState = stateKey;
  };
  const monitorInterval = setInterval(tick, MONITOR_INTERVAL_MS);
  const apiBaseUrl = `http://${bindHostname}:${port}`;
  const wsUrl = `ws://${bindHostname}:${port}/ws/events`;
  const handle = {
    port,
    apiBaseUrl,
    wsUrl,
    localBypassToken,
    stop: async ({ gracefulMs = DEFAULT_GRACEFUL_MS } = {}) => {
      if (stopped) return;
      if (stopping) return stopping;
      stopping = (async () => {
        try {
          stopped = true;
          setActivationBackend(null);
          clearInterval(monitorInterval);
          if (rootReconcileInterval) clearInterval(rootReconcileInterval);
          const closePromise = waitForServerClose(server);
          closeClients();
          for (const stop2 of activeProjectStops.values()) {
            stop2.stop();
          }
          activeProjectStops.clear();
          stopRootOrchestrator?.();
          await shutdownDefaultChatRuntime();
          shutdownPtyBridges();
          await Promise.race([closePromise, delay(gracefulMs)]);
          for (const socket of sockets) socket.destroy();
          await Promise.race([closePromise.catch(() => void 0), delay(100)]);
          await closeWsServers();
          if (stopRootTaskStoreWatcher) await stopRootTaskStoreWatcher();
          flushPendingTaskStore();
          setRemoteAccessRestartBackend(null);
          setDaemonShutdownBackend(null);
        } catch (err) {
          throw new DaemonShutdownError("Daemon shutdown failed", { cause: err });
        } finally {
          clearCanonicalDaemonInfo();
        }
      })();
      return stopping;
    },
    activateProject: activateProjectOnDaemon
  };
  setDaemonShutdownBackend(async () => {
    await handle.stop({ gracefulMs: 500 });
  });
  setRemoteAccessRestartBackend((request) => {
    setTimeout(() => {
      void (async () => {
        const restartPort = request.port ?? port;
        try {
          await handle.stop({ gracefulMs: 500 });
        } catch (err) {
          console.error("[daemon] Remote access stop before restart failed:", err);
        } finally {
          clearCanonicalDaemonInfo();
        }
        for (let attempt = 0; attempt < 2; attempt++) {
          try {
            const nextHandle = await startEmbeddedDaemon({
              sessionName: sessionless ? void 0 : sessionName,
              port: restartPort,
              bindHostname: request.bindHostname,
              authToken: request.token,
              localBypassToken,
              orchestratorStarter
            });
            const mutableHandle = handle;
            mutableHandle.stop = nextHandle.stop;
            mutableHandle.activateProject = nextHandle.activateProject;
            return;
          } catch (err) {
            if (err instanceof DaemonStartupError && err.reason === "port_in_use" && attempt === 0) {
              await delay(150);
              continue;
            }
            throw err;
          }
        }
      })().catch((err) => {
        console.error("[daemon] Remote access restart failed:", err);
        clearCanonicalDaemonInfo();
      });
    }, 50).unref?.();
    return { port };
  });
  stopSelf = () => void handle.stop();
  tick();
  return handle;
}
var requireFromHere, DEFAULT_HOSTNAME, DEFAULT_GRACEFUL_MS, MONITOR_INTERVAL_MS, RECONCILE_INTERVAL_MS2, EMBEDDED_SESSION_NAME;
var init_daemon_embed = __esm({
  "packages/daemon/src/lib/daemon-embed.ts"() {
    "use strict";
    init_session_monitor();
    init_task_store();
    init_errors2();
    init_ws_route();
    init_ws_events();
    init_app_set_remote_access();
    init_daemon_shutdown();
    init_defaults();
    init_app_settings();
    init_project_registry();
    init_workspace_registry();
    init_active_projects();
    init_canonical_daemon();
    requireFromHere = createRequire3(import.meta.url);
    DEFAULT_HOSTNAME = "127.0.0.1";
    DEFAULT_GRACEFUL_MS = 2e3;
    MONITOR_INTERVAL_MS = 1e3;
    RECONCILE_INTERVAL_MS2 = 3e4;
    EMBEDDED_SESSION_NAME = "__embedded__";
  }
});

// packages/daemon/src/lib/cli-action-bridge.ts
import { createRequire as createRequire4 } from "node:module";
import { z as z25 } from "zod";
function timeoutSignal3(ms) {
  const controller = new AbortController();
  setTimeout(() => controller.abort(), ms).unref?.();
  return controller.signal;
}
async function isDaemonAlive(port) {
  try {
    const res = await deps.fetch(`http://127.0.0.1:${port}/health`, {
      signal: timeoutSignal3(500)
    });
    return res.ok;
  } catch {
    return false;
  }
}
function hostnameForClient(bindHostname) {
  return bindHostname === "0.0.0.0" ? "127.0.0.1" : bindHostname;
}
function daemonBaseUrl(info) {
  return `http://${hostnameForClient(info.bindHostname)}:${info.port}`;
}
function expectedDaemonVersion() {
  try {
    for (const candidate of ["../../package.json", "../package.json"]) {
      try {
        const pkg = requireFromHere2(candidate);
        if (typeof pkg.version === "string") return pkg.version;
      } catch {
      }
    }
    return "0.0.0";
  } catch {
    return "0.0.0";
  }
}
async function resolveCanonicalDaemon() {
  const existing = deps.readCanonicalDaemonInfo();
  if (existing) {
    if (await deps.isCanonicalDaemonAlive(existing)) {
      warnOnDaemonVersionSkew(existing, expectedDaemonVersion());
      return { baseUrl: daemonBaseUrl(existing), transientHandle: null, restoreCwd: null };
    }
    deps.clearCanonicalDaemonInfo();
  }
  if (process.env.TMUX_IDE_CLI_NO_AUTOSTART) {
    return null;
  }
  const dir = deps.cwd();
  const previousCwd = process.cwd();
  try {
    process.chdir(dir);
    const handle = await deps.startEmbeddedDaemon({
      sessionName: void 0,
      bindHostname: "127.0.0.1",
      silent: true
    });
    if (!await isDaemonAlive(handle.port)) {
      await handle.stop();
      process.chdir(previousCwd);
      return null;
    }
    return { baseUrl: handle.apiBaseUrl, transientHandle: handle, restoreCwd: previousCwd };
  } catch {
    process.chdir(previousCwd);
    return null;
  }
}
async function stopTransientDaemon(daemon) {
  if (daemon.transientHandle) await daemon.transientHandle.stop().catch(() => void 0);
  if (daemon.restoreCwd) process.chdir(daemon.restoreCwd);
}
async function tryDispatchAction(name, input, options = {}) {
  const dir = options.cwd ?? deps.cwd();
  const previousDeps = deps;
  deps = { ...deps, cwd: () => dir };
  const daemon = await resolveCanonicalDaemon();
  deps = previousDeps;
  if (!daemon) return null;
  const contract = ActionContractsZ[name];
  const parsedInput = contract.input.parse(input);
  let response;
  try {
    response = await deps.fetch(`${daemon.baseUrl}/api/v2/action/${encodeURIComponent(name)}`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(parsedInput),
      signal: timeoutSignal3(2e3)
    });
  } catch {
    await stopTransientDaemon(daemon);
    return null;
  }
  let body;
  try {
    body = await response.json();
  } catch {
    await stopTransientDaemon(daemon);
    return null;
  }
  await stopTransientDaemon(daemon);
  const failure = FailureEnvelopeZ.safeParse(body);
  if (failure.success) {
    throw new CliActionInvocationError({
      code: failure.data.error.code,
      message: failure.data.error.message,
      details: failure.data.error.details
    });
  }
  const success = z25.object({ ok: z25.literal(true), result: contract.result }).safeParse(body);
  if (!success.success) return null;
  return success.data.result;
}
var FailureEnvelopeZ, deps, CliActionInvocationError, requireFromHere2;
var init_cli_action_bridge = __esm({
  "packages/daemon/src/lib/cli-action-bridge.ts"() {
    "use strict";
    init_contract();
    init_canonical_daemon();
    init_daemon_embed();
    FailureEnvelopeZ = z25.object({
      ok: z25.literal(false),
      error: z25.object({
        code: z25.string(),
        message: z25.string(),
        details: z25.unknown().optional()
      })
    });
    deps = {
      fetch,
      cwd: () => process.cwd(),
      readCanonicalDaemonInfo,
      clearCanonicalDaemonInfo,
      isCanonicalDaemonAlive,
      startEmbeddedDaemon
    };
    CliActionInvocationError = class extends Error {
      code;
      details;
      constructor(error) {
        super(error.message);
        this.name = "CliActionInvocationError";
        this.code = error.code;
        this.details = error.details ?? null;
      }
    };
    requireFromHere2 = createRequire4(import.meta.url);
  }
});

// packages/daemon/src/config.ts
import { resolve as resolve23 } from "node:path";
function readConfigSafe(dir) {
  let cfg;
  try {
    ({ config: cfg } = readConfig(dir));
  } catch (e) {
    outputError(`Cannot read ide.yml: ${e.message}`, "READ_ERROR");
    return;
  }
  return cfg;
}
function withConfig(dir, mutator) {
  const cfg = readConfigSafe(dir);
  if (cfg === void 0) return;
  if (!isConfigObject(cfg)) {
    outputError("Invalid ide.yml: config root must be an object", "INVALID_CONFIG");
    return;
  }
  const result = mutator(cfg);
  const validation = IdeConfigSchema.safeParse(cfg);
  if (!validation.success) {
    const issues = validation.error.issues.map((i) => `  ${i.path.join(".")}: ${i.message}`).join("\n");
    outputError(`Invalid config after mutation:
${issues}`, "INVALID_CONFIG");
    return;
  }
  writeConfig(dir, cfg);
  return result;
}
function mutateConfig(dir, mutator) {
  const { config: cfg } = readConfig(dir);
  if (!isConfigObject(cfg)) {
    throw new Error("Invalid ide.yml: config root must be an object");
  }
  const result = mutator(cfg);
  const validation = IdeConfigSchema.safeParse(cfg);
  if (!validation.success) {
    const issues = validation.error.issues.map((i) => `${i.path.join(".")}: ${i.message}`).join("; ");
    throw new Error(`Invalid config after mutation: ${issues}`);
  }
  writeConfig(dir, cfg);
  return { config: cfg, result };
}
function assertDotPath(path4) {
  const parts = path4.split(".");
  if (!path4.trim() || parts.some((part) => !part) || parts.some((part) => part === "__proto__" || part === "prototype" || part === "constructor")) {
    throw new Error(`Invalid config path "${path4}"`);
  }
}
function configSetValue(dir, path4, value) {
  assertDotPath(path4);
  return mutateConfig(dir, (cfg) => {
    setByPath(cfg, path4, value);
  }).config;
}
function configAddPane(dir, rowIndex, pane) {
  return mutateConfig(dir, (cfg) => {
    if (!Array.isArray(cfg.rows)) {
      throw new Error("Invalid ide.yml: 'rows' must be an array");
    }
    if (!cfg.rows[rowIndex]) {
      throw new Error(`Row ${rowIndex} does not exist`);
    }
    if (!Array.isArray(cfg.rows[rowIndex].panes)) {
      throw new Error(`Invalid ide.yml: row ${rowIndex} panes must be an array`);
    }
    cfg.rows[rowIndex].panes.push(pane);
  }).config;
}
function configRemovePane(dir, rowIndex, paneIndex) {
  const updated = mutateConfig(dir, (cfg) => {
    if (!Array.isArray(cfg.rows)) {
      throw new Error("Invalid ide.yml: 'rows' must be an array");
    }
    if (!Array.isArray(cfg.rows[rowIndex]?.panes)) {
      throw new Error(`Invalid ide.yml: row ${rowIndex} panes must be an array`);
    }
    const removed = cfg.rows[rowIndex].panes[paneIndex];
    if (!removed) {
      throw new Error(`Pane ${paneIndex} in row ${rowIndex} does not exist`);
    }
    cfg.rows[rowIndex].panes.splice(paneIndex, 1);
    return removed;
  });
  return { config: updated.config, removed: updated.result };
}
function configAddRow(dir, size) {
  return mutateConfig(dir, (cfg) => {
    if (cfg.rows !== void 0 && !Array.isArray(cfg.rows)) {
      throw new Error("Invalid ide.yml: 'rows' must be an array");
    }
    const row = { panes: [{ title: "Shell" }] };
    if (size) row.size = size;
    cfg.rows = cfg.rows ?? [];
    cfg.rows.push(row);
  }).config;
}
function configEnableTeam(dir, name) {
  return mutateConfig(dir, (cfg) => {
    if (cfg.rows !== void 0 && !Array.isArray(cfg.rows)) {
      throw new Error("Invalid ide.yml: 'rows' must be an array");
    }
    cfg.team = { name: name ?? cfg.name ?? "my-team" };
    let leadAssigned = false;
    for (const row of cfg.rows ?? []) {
      for (const pane of row.panes ?? []) {
        if (pane.command === "claude" || pane.role === "lead" || pane.role === "teammate") {
          pane.role = leadAssigned ? "teammate" : "lead";
          leadAssigned = true;
        }
      }
    }
    if (!leadAssigned) {
      delete cfg.team;
      throw new Error("Cannot enable agent team: no Claude panes found");
    }
  }).config;
}
function configDisableTeam(dir) {
  return mutateConfig(dir, (cfg) => {
    if (cfg.rows !== void 0 && !Array.isArray(cfg.rows)) {
      throw new Error("Invalid ide.yml: 'rows' must be an array");
    }
    delete cfg.team;
    for (const row of cfg.rows ?? []) {
      if (!Array.isArray(row?.panes)) continue;
      for (const pane of row.panes) {
        delete pane.role;
        delete pane.task;
      }
    }
  }).config;
}
async function config(targetDir, { json: json2, action, args } = {}) {
  const dir = resolve23(targetDir ?? ".");
  if (await tryDispatchConfigAction(dir, { json: json2, action, args: args ?? [] })) return;
  switch (action) {
    case "dump":
      return dumpConfig(dir, { json: json2 });
    case "set":
      return setConfig(dir, args ?? [], { json: json2 });
    case "add-pane":
      return addPane(dir, args ?? [], { json: json2 });
    case "remove-pane":
      return removePane(dir, args ?? [], { json: json2 });
    case "add-row":
      return addRow(dir, args ?? [], { json: json2 });
    case "enable-team":
      return enableTeam(dir, args ?? [], { json: json2 });
    case "disable-team":
      return disableTeam(dir, { json: json2 });
    default:
      return dumpConfig(dir, { json: json2 });
  }
}
function dumpConfig(dir, { json: json2 }) {
  const cfg = readConfigSafe(dir);
  if (cfg === void 0) return;
  if (json2) {
    console.log(JSON.stringify(cfg, null, 2));
  } else {
    console.log(JSON.stringify(cfg, null, 2));
  }
}
function printConfigActionError(err) {
  if (err instanceof CliActionInvocationError) {
    outputError(err.message, err.code.toUpperCase());
  }
  throw err;
}
function coerceConfigValue(raw) {
  if (raw === "true") return true;
  if (raw === "false") return false;
  if (/^\d+$/.test(raw)) return parseInt(raw);
  return raw;
}
async function tryDispatchConfigAction(dir, { json: json2, action, args }) {
  try {
    if (action === "set") {
      const [dotpath, ...rest] = args;
      if (!dotpath || rest.length === 0) return false;
      const value = coerceConfigValue(rest.join(" "));
      const result = await tryDispatchAction("config.set", { path: dotpath, value }, { cwd: dir });
      if (!result) return false;
      if (json2) console.log(JSON.stringify({ ok: true, path: dotpath, value }, null, 2));
      else console.log(`Set ${dotpath} = ${JSON.stringify(value)}`);
      return true;
    }
    if (action === "add-pane") {
      const { row, title, command: command2, size } = parseNamedArgs(args);
      if (row === void 0) return false;
      const rowIndex = parseIndex(row);
      if (rowIndex == null) return false;
      const result = await tryDispatchAction(
        "config.addPane",
        { rowIndex, title, command: command2, size },
        { cwd: dir }
      );
      if (!result) return false;
      const pane = { title, command: command2, size };
      if (json2) console.log(JSON.stringify({ ok: true, row: rowIndex, pane }, null, 2));
      else console.log(`Added pane "${title ?? "untitled"}" to row ${rowIndex}`);
      return true;
    }
    if (action === "remove-pane") {
      const { row, pane } = parseNamedArgs(args);
      if (row === void 0 || pane === void 0) return false;
      const rowIndex = parseIndex(row);
      const paneIndex = parseIndex(pane);
      if (rowIndex == null || paneIndex == null) return false;
      const before = readConfigSafe(dir);
      const removed = before?.rows[rowIndex]?.panes[paneIndex] ?? null;
      const result = await tryDispatchAction(
        "config.removePane",
        { rowIndex, paneIndex },
        { cwd: dir }
      );
      if (!result) return false;
      if (json2) {
        console.log(JSON.stringify({ ok: true, row: rowIndex, pane: paneIndex, removed }, null, 2));
      } else {
        console.log(`Removed pane ${paneIndex} from row ${rowIndex}`);
      }
      return true;
    }
    if (action === "add-row") {
      const { size } = parseNamedArgs(args);
      const result = await tryDispatchAction("config.addRow", { size }, { cwd: dir });
      if (!result) return false;
      const row = result.config.rows.length - 1;
      if (json2) console.log(JSON.stringify({ ok: true, row, size: size ?? null }, null, 2));
      else console.log(`Added row ${row}${size ? ` (${size})` : ""}`);
      return true;
    }
    if (action === "enable-team") {
      const { name } = parseNamedArgs(args);
      const result = await tryDispatchAction("config.enableTeam", { name }, { cwd: dir });
      if (!result) return false;
      const teamName = result.config.team?.name ?? name ?? result.config.name ?? "my-team";
      if (json2) console.log(JSON.stringify({ ok: true, team: result.config.team }, null, 2));
      else console.log(`Enabled agent team "${teamName}"`);
      return true;
    }
    if (action === "disable-team") {
      const result = await tryDispatchAction("config.disableTeam", {}, { cwd: dir });
      if (!result) return false;
      if (json2) console.log(JSON.stringify({ ok: true, disabled: true }, null, 2));
      else console.log("Disabled agent team");
      return true;
    }
  } catch (err) {
    printConfigActionError(err);
  }
  return false;
}
function setConfig(dir, args, { json: json2 }) {
  const [dotpath, ...rest] = args;
  if (!dotpath || rest.length === 0) {
    outputError("Usage: tmux-ide config set <dotpath> <value>", "USAGE");
    return;
  }
  const value = coerceConfigValue(rest.join(" "));
  withConfig(dir, (cfg) => {
    setByPath(cfg, dotpath, value);
  });
  if (json2) {
    console.log(JSON.stringify({ ok: true, path: dotpath, value }, null, 2));
  } else {
    console.log(`Set ${dotpath} = ${JSON.stringify(value)}`);
  }
}
function addPane(dir, args, { json: json2 }) {
  const { row, title, command: command2, size } = parseNamedArgs(args);
  if (row === void 0) {
    outputError(
      "Usage: tmux-ide config add-pane --row <N> --title <T> [--command <C>] [--size <S>]",
      "USAGE"
    );
    return;
  }
  const rowIdx = parseIndex(row);
  if (rowIdx == null) {
    outputError(`Invalid row index "${row}"`, "USAGE");
    return;
  }
  const pane = {};
  if (title) pane.title = title;
  if (command2) pane.command = command2;
  if (size) pane.size = size;
  withConfig(dir, (cfg) => {
    if (!Array.isArray(cfg.rows)) {
      outputError("Invalid ide.yml: 'rows' must be an array", "INVALID_CONFIG");
    }
    if (!cfg.rows[rowIdx]) {
      outputError(`Row ${rowIdx} does not exist`, "INVALID_ROW");
    }
    if (!Array.isArray(cfg.rows[rowIdx].panes)) {
      outputError(`Invalid ide.yml: row ${rowIdx} panes must be an array`, "INVALID_CONFIG");
    }
    cfg.rows[rowIdx].panes.push(pane);
  });
  if (json2) {
    console.log(JSON.stringify({ ok: true, row: rowIdx, pane }, null, 2));
  } else {
    console.log(`Added pane "${title ?? "untitled"}" to row ${rowIdx}`);
  }
}
function removePane(dir, args, { json: json2 }) {
  const { row, pane } = parseNamedArgs(args);
  if (row === void 0 || pane === void 0) {
    outputError("Usage: tmux-ide config remove-pane --row <N> --pane <M>", "USAGE");
    return;
  }
  const rowIdx = parseIndex(row);
  const paneIdx = parseIndex(pane);
  if (rowIdx == null || paneIdx == null) {
    outputError("Usage: tmux-ide config remove-pane --row <N> --pane <M>", "USAGE");
    return;
  }
  let removed;
  withConfig(dir, (cfg) => {
    if (!Array.isArray(cfg.rows)) {
      outputError("Invalid ide.yml: 'rows' must be an array", "INVALID_CONFIG");
    }
    if (!Array.isArray(cfg.rows[rowIdx]?.panes)) {
      outputError(`Invalid ide.yml: row ${rowIdx} panes must be an array`, "INVALID_CONFIG");
    }
    if (!cfg.rows[rowIdx].panes[paneIdx]) {
      outputError(`Pane ${paneIdx} in row ${rowIdx} does not exist`, "INVALID_PANE");
    }
    removed = cfg.rows[rowIdx].panes.splice(paneIdx, 1)[0];
  });
  if (json2) {
    console.log(JSON.stringify({ ok: true, row: rowIdx, pane: paneIdx, removed }, null, 2));
  } else {
    console.log(`Removed pane ${paneIdx} ("${removed?.title ?? "untitled"}") from row ${rowIdx}`);
  }
}
function addRow(dir, args, { json: json2 }) {
  const { size } = parseNamedArgs(args);
  let rowIdx;
  withConfig(dir, (cfg) => {
    if (cfg.rows !== void 0 && !Array.isArray(cfg.rows)) {
      outputError("Invalid ide.yml: 'rows' must be an array", "INVALID_CONFIG");
    }
    const row = { panes: [{ title: "Shell" }] };
    if (size) row.size = size;
    cfg.rows = cfg.rows ?? [];
    cfg.rows.push(row);
    rowIdx = cfg.rows.length - 1;
  });
  if (json2) {
    console.log(JSON.stringify({ ok: true, row: rowIdx, size: size ?? null }, null, 2));
  } else {
    console.log(`Added row ${rowIdx}${size ? ` (${size})` : ""}`);
  }
}
function enableTeam(dir, args, { json: json2 }) {
  const { name } = parseNamedArgs(args);
  let teamName;
  let result;
  withConfig(dir, (cfg) => {
    if (cfg.rows !== void 0 && !Array.isArray(cfg.rows)) {
      outputError("Invalid ide.yml: 'rows' must be an array", "INVALID_CONFIG");
    }
    teamName = name ?? cfg.name ?? "my-team";
    cfg.team = { name: teamName };
    let leadAssigned = false;
    for (const row of cfg.rows ?? []) {
      for (const pane of row.panes ?? []) {
        if (pane.command === "claude" || pane.role === "lead" || pane.role === "teammate") {
          if (!leadAssigned) {
            pane.role = "lead";
            leadAssigned = true;
          } else {
            pane.role = "teammate";
          }
        }
      }
    }
    if (!leadAssigned) {
      delete cfg.team;
      outputError("Cannot enable agent team: no Claude panes found", "INVALID_CONFIG");
    }
    result = { team: cfg.team, roles: summarizeRoles(cfg) };
  });
  if (json2) {
    console.log(JSON.stringify({ ok: true, ...result }, null, 2));
  } else {
    console.log(`Enabled agent team "${teamName}"`);
  }
}
function disableTeam(dir, { json: json2 }) {
  withConfig(dir, (cfg) => {
    if (cfg.rows !== void 0 && !Array.isArray(cfg.rows)) {
      outputError("Invalid ide.yml: 'rows' must be an array", "INVALID_CONFIG");
    }
    delete cfg.team;
    for (const row of cfg.rows ?? []) {
      if (!Array.isArray(row?.panes)) continue;
      for (const pane of row.panes) {
        delete pane.role;
        delete pane.task;
      }
    }
  });
  if (json2) {
    console.log(JSON.stringify({ ok: true, disabled: true }, null, 2));
  } else {
    console.log("Disabled agent team");
  }
}
function summarizeRoles(cfg) {
  const roles = [];
  for (let i = 0; i < (cfg.rows ?? []).length; i++) {
    for (let j = 0; j < (cfg.rows[i].panes ?? []).length; j++) {
      const p = cfg.rows[i].panes[j];
      if (p.role) {
        roles.push({ row: i, pane: j, title: p.title ?? null, role: p.role });
      }
    }
  }
  return roles;
}
function parseNamedArgs(args) {
  const result = {};
  for (let i = 0; i < args.length; i++) {
    if (args[i].startsWith("--") && i + 1 < args.length) {
      const key = args[i].slice(2);
      result[key] = args[i + 1];
      i++;
    }
  }
  return result;
}
function isConfigObject(value) {
  return value != null && typeof value === "object" && !Array.isArray(value);
}
function parseIndex(value) {
  if (!/^\d+$/.test(String(value))) return null;
  return Number.parseInt(value, 10);
}
var init_config = __esm({
  "packages/daemon/src/config.ts"() {
    "use strict";
    init_yaml_io();
    init_dot_path();
    init_output();
    init_ide_config2();
    init_cli_action_bridge();
  }
});

// package.json
var require_package = __commonJS({
  "package.json"(exports, module) {
    module.exports = {
      name: "tmux-ide",
      version: "2.5.0",
      description: "Turn any project into a tmux-powered terminal IDE with a simple ide.yml",
      type: "module",
      bin: {
        "tmux-ide": "bin/cli.js"
      },
      files: [
        "bin",
        "scripts",
        "skill",
        "templates",
        "dashboard/dist",
        "packages/daemon/dist"
      ],
      scripts: {
        build: "pnpm build:cli",
        "build:cli": "node scripts/build-cli.mjs",
        "build:dashboard": "cd dashboard && pnpm build",
        prepublishOnly: "pnpm build:cli && pnpm check && pnpm --filter @tmux-ide/dashboard build && node scripts/prepublish-check.mjs",
        typecheck: 'echo "root typecheck deferred to per-package turbo run"',
        dev: "node bin/cli.js",
        test: "vitest run --dir src src/cli.test.ts",
        "test:unit": "vitest run --dir src src/cli.test.ts",
        "test:chat-e2e": "pnpm --filter @tmux-ide/daemon exec vitest run src/chat/__tests__/chat-pipeline.e2e.test.ts",
        lint: "eslint bin scripts packages/contracts/src packages/tmux-bridge/src packages/daemon/src packages/v2-solid-widgets/src packages/chat-solid/src && pnpm run check:silo-mounts",
        "lint:workspace": "turbo run lint && pnpm run check:silo-mounts",
        "check:silo-mounts": "bash scripts/check-silo-mounts.sh",
        format: "prettier --write .",
        "format:check": "prettier --check .",
        "build:workspace": "turbo run build",
        "typecheck:workspace": "turbo run typecheck",
        "docs:build": "turbo run build --filter=@tmux-ide/docs",
        "pack:check": "npm pack --dry-run --cache /tmp/tmux-ide-npm-cache > /dev/null",
        "check:native-deps": "node packages/daemon/scripts/check-native-deps.mjs",
        check: "pnpm run lint:workspace && pnpm run format:check && pnpm run typecheck:workspace && vitest run --dir src src/cli.test.ts && pnpm run docs:build && pnpm run pack:check && pnpm run check:native-deps",
        postinstall: "node scripts/postinstall.js",
        "test:dashboard": "cd dashboard && pnpm test",
        "test:e2e": "playwright test",
        docs: "turbo run dev --filter=@tmux-ide/docs"
      },
      keywords: [
        "tmux",
        "ide",
        "terminal",
        "workspace",
        "developer-tools"
      ],
      engines: {
        node: ">=20"
      },
      repository: {
        type: "git",
        url: "git+https://github.com/wavyrai/tmux-ide.git"
      },
      homepage: "https://github.com/wavyrai/tmux-ide#readme",
      bugs: {
        url: "https://github.com/wavyrai/tmux-ide/issues"
      },
      license: "MIT",
      packageManager: "pnpm@10.21.0",
      dependencies: {
        "@hono/node-server": "^1.19.11",
        "@hono/zod-validator": "^0.7.6",
        "@opentui/core": "^0.1.88",
        "@opentui/core-darwin-arm64": "^0.1.88",
        "@opentui/solid": "^0.1.88",
        "@parcel/watcher": "^2.5.6",
        "@types/ws": "^8.18.1",
        "@vscode/ripgrep": "^1.15.11",
        "better-sqlite3": "^12.4.1",
        chokidar: "^5.0.0",
        effect: "^3.21.2",
        hono: "^4.12.8",
        ignore: "^7.0.5",
        "js-yaml": "^4.1.1",
        "node-pty": "1.2.0-beta.12",
        "solid-js": "^1.9.11",
        "vscode-jsonrpc": "^8.2.1",
        "vscode-languageserver-protocol": "^3.17.5",
        ws: "^8.20.0",
        zod: "^4.3.6"
      },
      pnpm: {
        onlyBuiltDependencies: [
          "@parcel/watcher",
          "better-sqlite3",
          "esbuild",
          "node-pty"
        ]
      },
      devDependencies: {
        "@eslint/js": "^10.0.1",
        "@playwright/test": "^1.59.1",
        "@tsconfig/bun": "^1.0.10",
        "@types/node": "^25.5.0",
        "@typescript-eslint/eslint-plugin": "^8.57.1",
        "@typescript-eslint/parser": "^8.57.1",
        "@vitest/coverage-v8": "^4.1.6",
        esbuild: "0.27.4",
        eslint: "^10.0.3",
        "eslint-plugin-solid": "^0.14.5",
        globals: "^17.4.0",
        prettier: "^3.8.1",
        turbo: "^2.3.3",
        typescript: "^5.9.3",
        vitest: "^4.1.0"
      }
    };
  }
});

// packages/daemon/src/plan.ts
var plan_exports = {};
__export(plan_exports, {
  planCommand: () => planCommand
});
import { resolve as resolve25 } from "node:path";
async function planCommand(targetDir, {
  json: json2 = false,
  sub,
  args = [],
  values: values2 = {}
}) {
  const dir = resolve25(targetDir ?? ".");
  switch (sub) {
    case "list":
    case void 0: {
      const statusFilter = values2.status;
      const plans = listPlans(dir, statusFilter ? { status: statusFilter } : void 0);
      if (json2) {
        console.log(JSON.stringify(plans, null, 2));
        return;
      }
      if (plans.length === 0) {
        console.log("No plans found.");
        return;
      }
      const groups = {};
      for (const p of plans) {
        (groups[p.status] ??= []).push(p);
      }
      for (const status4 of ["in-progress", "pending", "done", "archived"]) {
        const group = groups[status4];
        if (!group?.length) continue;
        const color = STATUS_COLORS[status4];
        console.log(`
${color}${status4.toUpperCase()}${RESET}`);
        for (const p of group) {
          const icon = STATUS_ICONS[p.status];
          const completed = p.completed ? ` (${p.completed})` : "";
          console.log(`  ${color}${icon}${RESET} ${p.name}  ${p.title}${completed}`);
        }
      }
      console.log();
      break;
    }
    case "show": {
      const name = args[0];
      if (!name) outputError("Usage: tmux-ide plan show <name|number>", "USAGE");
      const plan = getPlan(dir, name);
      if (!plan) outputError(`Plan not found: ${name}`, "NOT_FOUND");
      if (json2) {
        console.log(JSON.stringify(plan, null, 2));
        return;
      }
      const icon = STATUS_ICONS[plan.status];
      const color = STATUS_COLORS[plan.status];
      console.log(`${color}${icon} ${plan.title}${RESET}`);
      console.log(`  Status: ${plan.status}`);
      if (plan.effort) console.log(`  Effort: ${plan.effort}`);
      if (plan.gate) console.log(`  Gate: ${plan.gate}`);
      if (plan.completed) console.log(`  Completed: ${plan.completed}`);
      console.log(`  File: ${plan.path}`);
      break;
    }
    case "done": {
      const name = args[0];
      if (!name) outputError("Usage: tmux-ide plan done <name|number>", "USAGE");
      const result = markPlanDone(dir, name);
      if (!result) outputError(`Plan not found: ${name}`, "NOT_FOUND");
      if (json2) {
        console.log(JSON.stringify(result, null, 2));
        return;
      }
      console.log(`\u2713 Plan marked done: ${result.title} (${result.completed})`);
      break;
    }
    default:
      outputError(
        `Unknown plan subcommand: ${sub}
Usage: tmux-ide plan [list|show|done] [--status pending|in-progress|done]`,
        "USAGE"
      );
  }
}
var STATUS_ICONS, STATUS_COLORS, RESET;
var init_plan = __esm({
  "packages/daemon/src/plan.ts"() {
    "use strict";
    init_output();
    init_plan_store2();
    STATUS_ICONS = {
      pending: "\u25CB",
      "in-progress": "\u25CF",
      done: "\u2713",
      archived: "\u25AA"
    };
    STATUS_COLORS = {
      pending: "\x1B[2m",
      // dim
      "in-progress": "\x1B[33m",
      // yellow
      done: "\x1B[32m",
      // green
      archived: "\x1B[2m"
      // dim
    };
    RESET = "\x1B[0m";
  }
});

// packages/daemon/src/skill.ts
var skill_exports = {};
__export(skill_exports, {
  skillCommand: () => skillCommand
});
import { resolve as resolve26, join as join41, dirname as dirname17 } from "node:path";
import { existsSync as existsSync38, mkdirSync as mkdirSync24, readFileSync as readFileSync27, rmSync as rmSync5, writeFileSync as writeFileSync24 } from "node:fs";
import { fileURLToPath as fileURLToPath6 } from "node:url";
function scaffoldSkillContent(name) {
  const templatePath = resolve26(
    __dirname4,
    "..",
    "..",
    "..",
    "templates",
    "skills",
    "general-worker.md"
  );
  if (existsSync38(templatePath)) {
    return readFileSync27(templatePath, "utf-8").replace(/^name: .+/m, `name: ${name}`);
  }
  return `---
name: ${name}
specialties: []
role: teammate
description: ${name} agent
---
You are a ${name} agent.
`;
}
function printSkillActionError(err) {
  if (err instanceof CliActionInvocationError) {
    outputError(err.message, err.code.toUpperCase());
  }
  throw err;
}
async function tryDispatchSkillAction(dir, { json: json2, sub, args }) {
  try {
    if (sub === "create" && args[0]) {
      const name = args[0];
      const content = scaffoldSkillContent(name);
      const result = await tryDispatchAction("skill.create", { name, content }, { cwd: dir });
      if (!result) return false;
      const filePath = join41(dir, ".tmux-ide", "skills", `${name}.md`);
      if (json2) console.log(JSON.stringify({ created: true, path: filePath }));
      else console.log(`Created skill "${result.skill.name}" at ${filePath}`);
      return true;
    }
    if (sub === "update" && args[0] && args[1]) {
      const name = args[0];
      const content = readFileSync27(resolve26(dir, args[1]), "utf-8");
      const result = await tryDispatchAction("skill.update", { name, content }, { cwd: dir });
      if (!result) return false;
      const filePath = join41(dir, ".tmux-ide", "skills", `${name}.md`);
      if (json2) console.log(JSON.stringify({ updated: true, path: filePath }));
      else console.log(`Updated skill "${result.skill.name}" at ${filePath}`);
      return true;
    }
    if (sub === "delete" && args[0]) {
      const name = args[0];
      const result = await tryDispatchAction("skill.delete", { name }, { cwd: dir });
      if (!result) return false;
      if (json2) console.log(JSON.stringify(result));
      else console.log(`Deleted skill "${name}"`);
      return true;
    }
  } catch (err) {
    printSkillActionError(err);
  }
  return false;
}
async function skillCommand(targetDir, {
  json: json2 = false,
  sub,
  args = []
}) {
  const dir = resolve26(targetDir ?? ".");
  if (await tryDispatchSkillAction(dir, { json: json2, sub, args })) return;
  switch (sub) {
    case "list": {
      const skills = loadSkills(dir);
      if (json2) {
        console.log(
          JSON.stringify(
            skills.map((s) => ({
              name: s.name,
              specialties: s.specialties,
              role: s.role,
              description: s.description
            })),
            null,
            2
          )
        );
      } else if (skills.length === 0) {
        console.log("No skills found. Run: tmux-ide skill create <name>");
      } else {
        for (const s of skills) {
          const specs = s.specialties.length > 0 ? ` [${s.specialties.join(", ")}]` : "";
          console.log(`  ${s.name}${specs}  (${s.role}) \u2014 ${s.description}`);
        }
      }
      break;
    }
    case "show": {
      const name = args[0];
      if (!name) outputError("Usage: tmux-ide skill show <name>", "USAGE");
      const skill = loadSkill(dir, name);
      if (!skill) outputError(`Skill "${name}" not found`, "NOT_FOUND");
      if (json2) {
        console.log(JSON.stringify(skill, null, 2));
      } else {
        console.log(`Skill: ${skill.name}`);
        console.log(`  Role: ${skill.role}`);
        console.log(`  Specialties: ${skill.specialties.join(", ") || "none"}`);
        console.log(`  Description: ${skill.description}`);
        if (skill.body) {
          console.log(`
${skill.body}`);
        }
      }
      break;
    }
    case "create": {
      const name = args[0];
      if (!name) outputError("Usage: tmux-ide skill create <name>", "USAGE");
      const skillsDir = join41(dir, ".tmux-ide", "skills");
      const filePath = join41(skillsDir, `${name}.md`);
      if (existsSync38(filePath)) {
        outputError(`Skill "${name}" already exists at ${filePath}`, "EXISTS");
      }
      const content = scaffoldSkillContent(name);
      if (!existsSync38(skillsDir)) mkdirSync24(skillsDir, { recursive: true });
      writeFileSync24(filePath, content);
      if (json2) {
        console.log(JSON.stringify({ created: true, path: filePath }));
      } else {
        console.log(`Created skill "${name}" at ${filePath}`);
      }
      break;
    }
    case "update": {
      const name = args[0];
      const contentPath = args[1];
      if (!name || !contentPath) outputError("Usage: tmux-ide skill update <name> <file>", "USAGE");
      const content = readFileSync27(resolve26(dir, contentPath), "utf-8");
      const skillsDir = join41(dir, ".tmux-ide", "skills");
      const filePath = join41(skillsDir, `${name}.md`);
      if (!existsSync38(skillsDir)) mkdirSync24(skillsDir, { recursive: true });
      writeFileSync24(filePath, content.endsWith("\n") ? content : `${content}
`);
      if (json2) {
        console.log(JSON.stringify({ updated: true, path: filePath }));
      } else {
        console.log(`Updated skill "${name}" at ${filePath}`);
      }
      break;
    }
    case "delete": {
      const name = args[0];
      if (!name) outputError("Usage: tmux-ide skill delete <name>", "USAGE");
      const filePath = join41(dir, ".tmux-ide", "skills", `${name}.md`);
      if (!existsSync38(filePath)) outputError(`Skill "${name}" not found`, "NOT_FOUND");
      rmSync5(filePath);
      if (json2) {
        console.log(JSON.stringify({ deleted: true }));
      } else {
        console.log(`Deleted skill "${name}"`);
      }
      break;
    }
    case "validate": {
      let config2;
      try {
        ({ config: config2 } = readConfig(dir));
      } catch {
        outputError("Cannot read ide.yml", "READ_ERROR");
        return;
      }
      const skills = loadSkills(dir);
      const skillNames = new Set(skills.map((s) => s.name));
      const issues = [];
      for (const row of config2.rows) {
        for (const pane of row.panes) {
          if (pane.skill && !skillNames.has(pane.skill)) {
            issues.push({ pane: pane.title ?? "untitled", skill: pane.skill });
          }
        }
      }
      if (json2) {
        console.log(JSON.stringify({ valid: issues.length === 0, unresolved: issues }, null, 2));
      } else if (issues.length === 0) {
        console.log("All pane skill references resolve.");
      } else {
        console.log(`${issues.length} unresolved skill reference(s):`);
        for (const i of issues) {
          console.log(`  pane "${i.pane}" \u2192 skill "${i.skill}" (not found)`);
        }
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide skill <list|show|create|update|delete|validate>

  list                List all skills (project + personal)
  show <name>         Show full skill detail
  create <name>       Scaffold a new skill file
  update <name> <file> Replace a project skill from a file
  delete <name>       Delete a project skill
  validate            Check pane skill references resolve`);
      break;
    default:
      outputError(
        "Usage: tmux-ide skill <list|show|create|update|delete|validate>\nRun: tmux-ide skill help",
        "USAGE"
      );
  }
}
var __dirname4;
var init_skill = __esm({
  "packages/daemon/src/skill.ts"() {
    "use strict";
    init_output();
    init_skill_registry();
    init_yaml_io();
    init_cli_action_bridge();
    __dirname4 = dirname17(fileURLToPath6(import.meta.url));
  }
});

// packages/daemon/src/metrics-cli.ts
var metrics_cli_exports = {};
__export(metrics_cli_exports, {
  metricsCommand: () => metricsCommand
});
import { resolve as resolve27 } from "node:path";
function fmtDuration(ms) {
  if (ms < 6e4) return `${Math.round(ms / 1e3)}s`;
  if (ms < 36e5) return `${Math.round(ms / 6e4)}m`;
  const h = Math.floor(ms / 36e5);
  const m = Math.round(ms % 36e5 / 6e4);
  return `${h}h ${m}m`;
}
function fmtPct(n) {
  return `${Math.round(n * 100)}%`;
}
async function metricsCommand(targetDir, {
  json: json2 = false,
  sub
}) {
  const dir = resolve27(targetDir ?? ".");
  switch (sub) {
    case "agents": {
      const metrics = computeMetrics(dir);
      if (json2) {
        console.log(JSON.stringify(metrics.agents, null, 2));
        return;
      }
      if (metrics.agents.length === 0) {
        console.log("No agent data yet.");
        return;
      }
      console.log("Agent Performance:");
      for (const a of metrics.agents) {
        const specs = a.specialties.length > 0 ? ` [${a.specialties.join(", ")}]` : "";
        console.log(`  ${a.name}${specs}`);
        console.log(
          `    Tasks: ${a.taskCount}  Retries: ${a.retryCount}  Utilization: ${fmtPct(a.utilization)}`
        );
        console.log(
          `    Active: ${fmtDuration(a.activeTimeMs)}  Idle: ${fmtDuration(a.idleTimeMs)}`
        );
      }
      break;
    }
    case "timeline": {
      const metrics = computeMetrics(dir);
      if (json2) {
        console.log(JSON.stringify(metrics.timeline, null, 2));
        return;
      }
      if (metrics.timeline.length === 0) {
        console.log("No timeline data yet.");
        return;
      }
      console.log("Activity Timeline:");
      for (const t of metrics.timeline.slice(-20)) {
        const ts = new Date(t.timestamp).toLocaleTimeString();
        console.log(
          `  ${ts}  done:${t.completedTasks} active:${t.activeTasks} busy:${t.busyAgents} idle:${t.idleAgents}`
        );
      }
      break;
    }
    case "eval": {
      const metrics = computeMetrics(dir);
      if (json2) {
        const bottlenecks = [...metrics.tasks.byMilestone].sort((a, b) => b.durationMs - a.durationMs).slice(0, 3);
        const highRetryAgents = [...metrics.agents].filter((a) => a.retryCount > 0).sort((a, b) => b.retryCount - a.retryCount);
        const ranking = [...metrics.agents].sort((a, b) => b.utilization - a.utilization);
        console.log(JSON.stringify({ bottlenecks, highRetryAgents, ranking }, null, 2));
        return;
      }
      console.log("Evaluation:");
      const slowest = [...metrics.tasks.byMilestone].filter((m) => m.durationMs > 0).sort((a, b) => b.durationMs - a.durationMs);
      if (slowest.length > 0) {
        console.log("  Slowest milestones:");
        for (const m of slowest.slice(0, 3)) {
          console.log(
            `    ${m.id} ${m.title}: ${fmtDuration(m.durationMs)} (${m.completedCount}/${m.taskCount} tasks)`
          );
        }
      }
      const retryAgents = metrics.agents.filter((a) => a.retryCount > 0);
      if (retryAgents.length > 0) {
        console.log("  High-retry agents:");
        for (const a of retryAgents.sort((x, y) => y.retryCount - x.retryCount)) {
          console.log(`    ${a.name}: ${a.retryCount} retries across ${a.taskCount} tasks`);
        }
      }
      if (metrics.agents.length > 0) {
        console.log("  Agent ranking (by utilization):");
        for (const a of [...metrics.agents].sort((x, y) => y.utilization - x.utilization)) {
          console.log(`    ${a.name}: ${fmtPct(a.utilization)} util, ${a.taskCount} tasks`);
        }
      }
      break;
    }
    case "history": {
      const history = loadMissionHistory(dir);
      if (json2) {
        console.log(JSON.stringify(history, null, 2));
        return;
      }
      if (history.length === 0) {
        console.log("No mission history yet.");
        return;
      }
      console.log("Mission History:");
      for (const m of history) {
        console.log(`  ${m.title} [${m.status}]`);
        console.log(`    Completed: ${m.completedAt}  Duration: ${fmtDuration(m.wallClockMs)}`);
        console.log(
          `    Tasks: ${m.taskCount} (${fmtPct(m.completionRate)} done)  Agents: ${m.agentCount}  Milestones: ${m.milestonesCompleted}`
        );
      }
      break;
    }
    case "help":
      console.log(`Usage: tmux-ide metrics [subcommand] [--json]

  (default)   Session summary \u2014 duration, task rates, agent utilization
  agents      Per-agent table \u2014 tasks, time, utilization, specialties
  timeline    Activity timeline \u2014 sampled every 5 minutes
  eval        Evaluation \u2014 bottleneck milestones, high-retry tasks, agent ranking
  history     Cross-mission comparison from history`);
      break;
    default: {
      const metrics = computeMetrics(dir);
      if (json2) {
        console.log(JSON.stringify(metrics, null, 2));
        return;
      }
      console.log("Session Metrics:");
      console.log(`  Status: ${metrics.session.status}`);
      console.log(`  Duration: ${fmtDuration(metrics.session.durationMs)}`);
      console.log(`  Agents: ${metrics.session.agentCount}`);
      console.log();
      console.log("Tasks:");
      console.log(
        `  Total: ${metrics.tasks.total}  Done: ${metrics.tasks.completed}  Failed: ${metrics.tasks.failed}`
      );
      console.log(
        `  Completion rate: ${fmtPct(metrics.tasks.completionRate)}  Retry rate: ${fmtPct(metrics.tasks.retryRate)}`
      );
      if (metrics.tasks.avgDurationMs > 0) {
        console.log(
          `  Avg duration: ${fmtDuration(metrics.tasks.avgDurationMs)}  Median: ${fmtDuration(metrics.tasks.medianDurationMs)}  P90: ${fmtDuration(metrics.tasks.p90DurationMs)}`
        );
      }
      if (metrics.mission.title) {
        console.log();
        console.log(`Mission: ${metrics.mission.title} [${metrics.mission.status}]`);
        console.log(`  Milestones: ${metrics.mission.milestonesCompleted} completed`);
        console.log(`  Validation: ${fmtPct(metrics.mission.validationPassRate)} pass rate`);
        console.log(`  Wall clock: ${fmtDuration(metrics.mission.wallClockMs)}`);
      }
      break;
    }
  }
}
var init_metrics_cli = __esm({
  "packages/daemon/src/metrics-cli.ts"() {
    "use strict";
    init_metrics();
  }
});

// packages/daemon/src/dispatch.ts
var dispatch_exports = {};
__export(dispatch_exports, {
  dispatch: () => dispatch2
});
import { resolve as resolve28, join as join42 } from "node:path";
import { existsSync as existsSync39, readFileSync as readFileSync28 } from "node:fs";
async function dispatch2(targetDir, opts) {
  const dir = resolve28(targetDir ?? ".");
  const { taskId, json: json2 } = opts;
  if (!taskId) {
    throw new IdeError("Missing task ID. Usage: tmux-ide dispatch <id>", {
      code: "USAGE"
    });
  }
  const dispatchFile = join42(dir, ".tasks", "dispatch", `${taskId}.md`);
  const hasDispatchFile = existsSync39(dispatchFile);
  const task = loadTask(dir, taskId);
  if (!task && !hasDispatchFile) {
    throw new IdeError(`Task "${taskId}" not found and no dispatch file exists`, {
      code: "TASK_NOT_FOUND"
    });
  }
  const mission = loadMission(dir);
  const goal = task?.goal ? loadGoal(dir, task.goal) : null;
  if (json2) {
    const prompt = hasDispatchFile ? readFileSync28(dispatchFile, "utf-8") : buildPrompt(dir, task);
    const result = {
      task: task ?? null,
      mission: mission ?? null,
      goal: goal ?? null,
      prompt
    };
    console.log(JSON.stringify(result, null, 2));
    return;
  }
  if (hasDispatchFile) {
    const content = readFileSync28(dispatchFile, "utf-8");
    console.log(content);
  } else {
    console.log(buildPrompt(dir, task));
  }
  console.log("");
  console.log(`When done: tmux-ide task done ${taskId} --proof "what you did"`);
}
function buildPrompt(dir, task) {
  let prompt = "";
  const mission = loadMission(dir);
  if (mission) {
    prompt += `Mission: ${mission.title}
`;
    if (mission.description) prompt += `${mission.description}
`;
    prompt += "\n";
  }
  if (task.goal) {
    const goal = loadGoal(dir, task.goal);
    if (goal) {
      prompt += `Goal: ${goal.title}
`;
      if (goal.acceptance) prompt += `Acceptance: ${goal.acceptance}
`;
      prompt += "\n";
    }
  }
  prompt += `Your Task: ${task.title}
`;
  if (task.description) prompt += `${task.description}
`;
  prompt += `
Priority: P${task.priority}
`;
  if (task.tags?.length) prompt += `Tags: ${task.tags.join(", ")}
`;
  return prompt.trim();
}
var init_dispatch = __esm({
  "packages/daemon/src/dispatch.ts"() {
    "use strict";
    init_task_store();
    init_errors2();
  }
});

// packages/daemon/src/notify.ts
var notify_exports = {};
__export(notify_exports, {
  notify: () => notify
});
import { resolve as resolve29 } from "node:path";
async function notify(targetDir, opts) {
  const dir = resolve29(targetDir ?? ".");
  const { message, json: json2 } = opts;
  const { name: session } = getSessionName(dir);
  if (!message) {
    throw new IdeError("Missing message. Usage: tmux-ide notify <message>", {
      code: "USAGE"
    });
  }
  const state = getSessionState(session);
  if (!state.running) {
    throw new IdeError(`Session "${session}" is not running`, {
      code: "SESSION_NOT_FOUND"
    });
  }
  const panes = listSessionPanes(session);
  const leadPane = panes.find((p) => p.role === "lead") ?? panes[0];
  if (!leadPane) {
    throw new IdeError("No panes found in session", {
      code: "PANE_NOT_FOUND"
    });
  }
  const short = message.length > 180 ? message.slice(0, 177) + "..." : message;
  sendCommand(session, leadPane.id, short);
  appendEvent(dir, {
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    type: "notify",
    target: leadPane.name ?? leadPane.title,
    paneId: leadPane.id,
    message: short
  });
  if (json2) {
    console.log(
      JSON.stringify({
        ok: true,
        session,
        target: {
          paneId: leadPane.id,
          name: leadPane.name,
          title: leadPane.title,
          role: leadPane.role
        },
        message: short
      })
    );
    return;
  }
  const label = leadPane.name ?? leadPane.title;
  console.log(`Notified "${label}" (${leadPane.id}): ${short}`);
}
var init_notify = __esm({
  "packages/daemon/src/notify.ts"() {
    "use strict";
    init_yaml_io();
    init_src();
    init_pane_comms();
    init_event_log();
    init_errors2();
  }
});

// packages/daemon/src/orchestrator-status.ts
var orchestrator_status_exports = {};
__export(orchestrator_status_exports, {
  orchestratorStatus: () => orchestratorStatus
});
import { resolve as resolve30 } from "node:path";
import { existsSync as existsSync40, readFileSync as readFileSync29 } from "node:fs";
import { join as join43 } from "node:path";
function formatElapsed2(ms) {
  if (ms < 6e4) return `${Math.round(ms / 1e3)}s`;
  if (ms < 36e5) return `${Math.round(ms / 6e4)}m`;
  return `${(ms / 36e5).toFixed(1)}h`;
}
function formatRelative(iso) {
  const ms = Date.now() - new Date(iso).getTime();
  if (ms < 6e4) return `${Math.round(ms / 1e3)}s ago`;
  if (ms < 36e5) return `${Math.round(ms / 6e4)}m ago`;
  return `${(ms / 36e5).toFixed(1)}h ago`;
}
async function orchestratorStatus(targetDir, { json: json2 } = {}) {
  const dir = resolve30(targetDir ?? ".");
  const { name: session } = getSessionName(dir);
  let orchConfig = null;
  try {
    const { config: config2 } = readConfig(dir);
    const orch = config2.orchestrator;
    if (orch) {
      orchConfig = {
        autoDispatch: orch.auto_dispatch ?? true,
        dispatchMode: orch.dispatch_mode ?? "tasks",
        pollInterval: orch.poll_interval ?? 5e3,
        stallTimeout: orch.stall_timeout ?? 3e5,
        maxConcurrentAgents: orch.max_concurrent_agents ?? 10,
        masterPane: orch.master_pane ?? null
      };
    }
  } catch {
  }
  const state = getSessionState(session);
  const running = state.running && orchConfig?.autoDispatch !== void 0;
  const statePath = join43(dir, ".tasks", "orchestrator-state.json");
  let claimedTasks = [];
  let taskClaimTimes = {};
  if (existsSync40(statePath)) {
    try {
      const data2 = JSON.parse(readFileSync29(statePath, "utf-8"));
      claimedTasks = data2.claimedTasks ?? [];
      taskClaimTimes = data2.taskClaimTimes ?? {};
    } catch {
    }
  }
  const tasks = loadTasks(dir);
  const mission = loadMission(dir);
  const agents = [];
  if (state.running) {
    const panes = listSessionPanes(session);
    const agentPanes = panes.filter((p) => isAgentPane(p));
    for (const pane of agentPanes) {
      const name = agentIdentifier(pane);
      const busy = isAgentBusy(pane);
      const assignedTask = tasks.find((t) => t.status === "in-progress" && t.assignee === name);
      let currentTask = null;
      if (assignedTask) {
        const claimTime = taskClaimTimes[assignedTask.id];
        const elapsed = claimTime ? formatElapsed2(Date.now() - claimTime) : formatElapsed2(Date.now() - new Date(assignedTask.updated).getTime());
        currentTask = { id: assignedTask.id, title: assignedTask.title, elapsed };
      }
      agents.push({
        name,
        paneId: pane.id,
        state: busy ? "busy" : "idle",
        currentTask
      });
    }
  }
  const inProgressTasks = tasks.filter((t) => t.status === "in-progress").map((t) => {
    const claimTime = taskClaimTimes[t.id];
    const elapsed = claimTime ? formatElapsed2(Date.now() - claimTime) : formatElapsed2(Date.now() - new Date(t.updated).getTime());
    return { id: t.id, title: t.title, assignee: t.assignee ?? "unassigned", elapsed };
  });
  const doneTaskIds = new Set(tasks.filter((t) => t.status === "done").map((t) => t.id));
  const pendingTasks = tasks.filter((t) => t.status === "todo").sort((a, b) => a.priority - b.priority).map((t) => {
    const blockedBy = t.depends_on.filter((dep) => !doneTaskIds.has(dep));
    return {
      id: t.id,
      title: t.title,
      priority: t.priority,
      blocked: blockedBy.length > 0,
      blockedBy: blockedBy.length > 0 ? blockedBy : void 0
    };
  });
  const recentlyDone = tasks.filter((t) => t.status === "done").sort((a, b) => new Date(b.updated).getTime() - new Date(a.updated).getTime()).slice(0, 5).map((t) => ({ id: t.id, title: t.title, completedAt: t.updated }));
  const allEvents = readEvents(dir);
  const recentEvents = allEvents.slice(-10).reverse();
  const data = {
    running,
    config: orchConfig,
    agents,
    tasks: { inProgress: inProgressTasks, pending: pendingTasks, recentlyDone },
    claims: { count: claimedTasks.length, taskIds: claimedTasks },
    recentEvents
  };
  if (json2) {
    console.log(JSON.stringify(data, null, 2));
    return;
  }
  const noColor2 = "NO_COLOR" in process.env;
  const bold2 = (s) => noColor2 ? s : `\x1B[1m${s}\x1B[22m`;
  const dim2 = (s) => noColor2 ? s : `\x1B[2m${s}\x1B[22m`;
  const green = (s) => noColor2 ? s : `\x1B[32m${s}\x1B[39m`;
  const yellow = (s) => noColor2 ? s : `\x1B[33m${s}\x1B[39m`;
  console.log(`${bold2("Orchestrator:")} ${running ? green("running") : dim2("stopped")}`);
  if (orchConfig) {
    const stallMin = Math.round(orchConfig.stallTimeout / 6e4);
    console.log(
      `  Poll: ${orchConfig.pollInterval}ms | Stall: ${stallMin}m | Mode: ${orchConfig.dispatchMode} | Auto-dispatch: ${orchConfig.autoDispatch ? "on" : "off"}`
    );
    console.log(`  Max concurrent: ${orchConfig.maxConcurrentAgents}`);
  }
  if (mission) {
    console.log(`
${bold2("Mission:")} ${mission.title}`);
  }
  const busyCount = agents.filter((a) => a.state === "busy").length;
  console.log(`
${bold2("Agents")} (${busyCount} busy / ${agents.length} total):`);
  if (agents.length === 0) {
    console.log(`  ${dim2("No agents detected")}`);
  }
  for (const agent of agents) {
    const stateLabel = agent.state === "busy" ? yellow("busy") : green("idle");
    const taskLabel = agent.currentTask ? ` -> ${agent.currentTask.id} "${agent.currentTask.title}" (${agent.currentTask.elapsed})` : "";
    console.log(`  ${agent.name.padEnd(12)} ${agent.paneId.padEnd(5)} ${stateLabel}${taskLabel}`);
  }
  if (inProgressTasks.length > 0) {
    console.log(`
${bold2("In-progress")} (${inProgressTasks.length}):`);
    for (const t of inProgressTasks) {
      console.log(`  ${t.id}  "${t.title}"  @${t.assignee}  ${dim2(t.elapsed)}`);
    }
  }
  if (pendingTasks.length > 0) {
    console.log(`
${bold2("Pending")} (${pendingTasks.length}):`);
    for (const t of pendingTasks.slice(0, 10)) {
      const blocked = t.blocked ? dim2(` (blocked by ${t.blockedBy.join(", ")})`) : "";
      console.log(`  ${t.id}  "${t.title}"  P${t.priority}${blocked}`);
    }
    if (pendingTasks.length > 10) {
      console.log(`  ${dim2(`... and ${pendingTasks.length - 10} more`)}`);
    }
  }
  if (recentEvents.length > 0) {
    console.log(`
${bold2("Recent Events")}:`);
    for (const evt of recentEvents) {
      console.log(`  ${dim2(formatRelative(evt.timestamp))}  ${evt.type.padEnd(15)} ${evt.message}`);
    }
  }
}
var init_orchestrator_status = __esm({
  "packages/daemon/src/orchestrator-status.ts"() {
    "use strict";
    init_yaml_io();
    init_src();
    init_task_store();
    init_event_log();
    init_pane_comms();
    init_orchestrator();
  }
});

// packages/daemon/src/tunnel.ts
var tunnel_exports = {};
__export(tunnel_exports, {
  tunnelCommand: () => tunnelCommand
});
import { resolve as resolve31 } from "node:path";
function getManager(dir, session) {
  if (!manager) {
    manager = new TunnelManager({ session, dir });
  }
  return manager;
}
async function resolveSession() {
  try {
    const { readConfig: readConfig2 } = await Promise.resolve().then(() => (init_yaml_io(), yaml_io_exports));
    const { config: config2 } = readConfig2(process.cwd());
    return config2.name ?? void 0;
  } catch {
    return void 0;
  }
}
async function resolveTunnelConfig(dir, overrides) {
  try {
    const { readConfig: readConfig2 } = await Promise.resolve().then(() => (init_yaml_io(), yaml_io_exports));
    const { config: config2 } = readConfig2(dir);
    if (config2.tunnel) {
      const merged = { ...config2.tunnel, ...overrides };
      return tunnelConfigSchema.parse(merged);
    }
  } catch {
  }
  if (overrides?.provider) {
    return tunnelConfigSchema.parse(overrides);
  }
  throw new IdeError(
    'No tunnel configuration found. Add a "tunnel" section to ide.yml or pass --provider.',
    { code: "CONFIG_MISSING" }
  );
}
async function tunnelCommand(targetDir, opts) {
  const dir = resolve31(targetDir ?? ".");
  const { json: json2, sub } = opts;
  const session = await resolveSession();
  const mgr = getManager(dir, session);
  switch (sub) {
    case "start": {
      const provider = opts.values?.provider ?? void 0;
      const port = opts.values?.port ? parseInt(opts.values.port, 10) : void 0;
      const domain = opts.values?.domain ?? void 0;
      const authtoken = opts.values?.authtoken ?? void 0;
      const config2 = await resolveTunnelConfig(dir, {
        provider,
        port,
        domain,
        authToken: authtoken
      });
      const status4 = await mgr.start(config2);
      if (json2) {
        console.log(JSON.stringify(status4));
      } else {
        if (status4.publicUrl) {
          console.log(`Tunnel running: ${status4.publicUrl}`);
        } else {
          console.log("Tunnel started (no public URL yet)");
        }
      }
      break;
    }
    case "stop": {
      await mgr.stop();
      if (json2) {
        console.log(JSON.stringify({ ok: true }));
      } else {
        console.log("Tunnel stopped");
      }
      break;
    }
    case "status": {
      const status4 = await mgr.status();
      if (json2) {
        console.log(JSON.stringify(status4));
      } else {
        if (status4.running) {
          console.log(`Tunnel: ${status4.provider} \u2014 ${status4.publicUrl ?? "starting..."}`);
        } else {
          console.log("Tunnel: not running");
        }
      }
      break;
    }
    case "url": {
      const url = await mgr.url();
      if (json2) {
        console.log(JSON.stringify({ url }));
      } else {
        console.log(url ?? "No tunnel running");
      }
      break;
    }
    default:
      throw new IdeError(
        `Unknown tunnel subcommand: ${sub ?? "(none)"}
Usage: tmux-ide tunnel start|stop|status|url`,
        { code: "USAGE" }
      );
  }
}
var manager;
var init_tunnel = __esm({
  "packages/daemon/src/tunnel.ts"() {
    "use strict";
    init_errors2();
    init_manager();
    init_types3();
    manager = null;
  }
});

// packages/daemon/src/remote.ts
var remote_exports = {};
__export(remote_exports, {
  remoteCommand: () => remoteCommand
});
import { resolve as resolve32 } from "node:path";
async function loadHQConfig(dir) {
  try {
    const { readConfig: readConfig2 } = await Promise.resolve().then(() => (init_yaml_io(), yaml_io_exports));
    const { config: config2 } = readConfig2(dir);
    if (config2.hq) {
      return HQConfigSchema.parse(config2.hq);
    }
  } catch {
  }
  return null;
}
async function remoteCommand(targetDir, opts) {
  const dir = resolve32(targetDir ?? ".");
  const { json: json2, sub } = opts;
  switch (sub) {
    case "ssh": {
      const { sshRemoteCommand: sshRemoteCommand2 } = await Promise.resolve().then(() => (init_ssh_remote(), ssh_remote_exports));
      await sshRemoteCommand2({ ...opts, args: opts.args ?? [] });
      break;
    }
    case "register": {
      const hqConfig = await loadHQConfig(dir);
      if (!hqConfig || hqConfig.role !== "remote") {
        throw new IdeError(
          'HQ config not found or role is not "remote". Add hq section to ide.yml.',
          {
            code: "CONFIG_MISSING"
          }
        );
      }
      if (!hqConfig.hq_url) {
        throw new IdeError("hq_url is required for remote registration", {
          code: "CONFIG_MISSING"
        });
      }
      const client = new HQClient({
        hqUrl: hqConfig.hq_url,
        secret: hqConfig.secret ?? "",
        machineName: hqConfig.machine_name ?? (await import("node:os")).hostname(),
        remoteUrl: opts.values?.url ?? `http://localhost:4000`,
        heartbeatInterval: hqConfig.heartbeat_interval
      });
      await client.register();
      if (json2) {
        console.log(
          JSON.stringify({
            ok: true,
            remoteId: client.getRemoteId(),
            name: client.getName()
          })
        );
      } else {
        console.log(`Registered with HQ as ${client.getName()} (${client.getRemoteId()})`);
      }
      await new Promise((resolve35) => {
        const shutdown = async () => {
          await client.destroy();
          resolve35();
        };
        process.once("SIGINT", () => void shutdown());
        process.once("SIGTERM", () => void shutdown());
      });
      break;
    }
    case "machines": {
      const hqConfig = await loadHQConfig(dir);
      const hqUrl = opts.values?.["hq-url"] ?? hqConfig?.hq_url ?? "http://localhost:4000";
      try {
        const res = await fetch(`${hqUrl}/api/hq/machines`);
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        const data = await res.json();
        if (json2) {
          console.log(JSON.stringify(data));
        } else {
          const machines = data.machines;
          if (machines.length === 0) {
            console.log("No remote machines registered");
          } else {
            for (const m of machines) {
              console.log(`  ${m.name} \u2014 ${m.url} (${m.id})`);
            }
          }
        }
      } catch (err) {
        throw new IdeError(
          `Failed to query HQ: ${err instanceof Error ? err.message : String(err)}`,
          {
            code: "HQ_ERROR"
          }
        );
      }
      break;
    }
    case "status": {
      const hqConfig = await loadHQConfig(dir);
      if (json2) {
        console.log(
          JSON.stringify({
            configured: !!hqConfig,
            role: hqConfig?.role ?? null,
            hq_url: hqConfig?.hq_url ?? null,
            machine_name: hqConfig?.machine_name ?? null
          })
        );
      } else {
        if (!hqConfig) {
          console.log("HQ: not configured");
        } else {
          console.log(`HQ role: ${hqConfig.role}`);
          if (hqConfig.hq_url) console.log(`HQ URL: ${hqConfig.hq_url}`);
          if (hqConfig.machine_name) console.log(`Machine: ${hqConfig.machine_name}`);
        }
      }
      break;
    }
    default:
      throw new IdeError(
        `Unknown remote subcommand: ${sub ?? "(none)"}
Usage: tmux-ide remote register|machines|status|ssh`,
        { code: "USAGE" }
      );
  }
}
var init_remote = __esm({
  "packages/daemon/src/remote.ts"() {
    "use strict";
    init_errors2();
    init_client5();
    init_types2();
  }
});

// packages/daemon/src/agent-hook.ts
var agent_hook_exports = {};
__export(agent_hook_exports, {
  agentCommand: () => agentCommand,
  buildHookSnippet: () => buildHookSnippet,
  buildPayload: () => buildPayload,
  buildReportRequest: () => buildReportRequest,
  hostnameForClient: () => hostnameForClient2,
  mergeHooks: () => mergeHooks
});
import { readFileSync as readFileSync30, mkdirSync as mkdirSync25, renameSync as renameSync19, existsSync as existsSync41, writeFileSync as writeFileSync25 } from "node:fs";
import { homedir as homedir15 } from "node:os";
import { basename as basename10, dirname as dirname18, join as join44 } from "node:path";
function parseStdin(raw) {
  if (!raw.trim()) return {};
  try {
    const parsed = JSON.parse(raw);
    if (parsed && typeof parsed === "object") return parsed;
  } catch {
  }
  return {};
}
function reportingTool() {
  return process.env.TMUX_IDE_AGENT_TOOL === "codex" ? "codex" : "claude";
}
function buildPayload(event, stdin) {
  const id = typeof stdin.session_id === "string" ? stdin.session_id.trim() : "";
  if (!id) return null;
  if (event === "stop") {
    return { id };
  }
  if (event === "idle") {
    return { id, status: "idle" };
  }
  const tool = reportingTool();
  const cwd = typeof stdin.cwd === "string" && stdin.cwd ? stdin.cwd : void 0;
  const name = cwd ? `${tool}@${basename10(cwd)}` : tool;
  return {
    id,
    tool,
    name,
    cwd,
    session: id,
    pid: typeof process.ppid === "number" ? process.ppid : void 0,
    status: "busy"
  };
}
function timeoutSignal4(ms) {
  const controller = new AbortController();
  setTimeout(() => controller.abort(), ms).unref?.();
  return controller.signal;
}
function hostnameForClient2(bindHostname) {
  return bindHostname === "0.0.0.0" ? "127.0.0.1" : bindHostname;
}
function buildReportRequest(info, action) {
  const baseUrl = `http://${hostnameForClient2(info.bindHostname)}:${info.port}`;
  const headers = { "Content-Type": "application/json" };
  if (info.authToken) headers.Authorization = `Bearer ${info.authToken}`;
  return { url: `${baseUrl}/api/v2/action/${action}`, headers };
}
async function report(event) {
  if (process.env.TMUX) return;
  let raw;
  try {
    raw = readFileSync30(0, "utf-8");
  } catch {
    return;
  }
  if (raw.length > 256 * 1024) return;
  const stdin = parseStdin(raw);
  const payload = buildPayload(event, stdin);
  if (!payload) return;
  const info = readCanonicalDaemonInfo();
  if (!info) return;
  const { url, headers } = buildReportRequest(info, EVENT_TO_ACTION[event]);
  try {
    await fetch(url, {
      method: "POST",
      headers,
      body: JSON.stringify(payload),
      signal: timeoutSignal4(1500)
    });
  } catch {
  }
}
function settingsPath() {
  return process.env.CLAUDE_CONFIG_DIR ? join44(process.env.CLAUDE_CONFIG_DIR, "settings.json") : join44(homedir15(), ".claude", "settings.json");
}
function buildHookSnippet() {
  const snippet = {};
  for (const [hookName, event] of Object.entries(HOOK_EVENTS)) {
    snippet[hookName] = [
      { hooks: [{ type: "command", command: `tmux-ide agent report ${event}` }] }
    ];
  }
  return snippet;
}
function isOurHookEntry(entry) {
  return entry.type === "command" && /\btmux-ide agent report\b/.test(entry.command);
}
function mergeHooks(existing, snippet) {
  const merged = { ...existing ?? {} };
  for (const [hookName, matchers] of Object.entries(snippet)) {
    const current = Array.isArray(merged[hookName]) ? merged[hookName] : [];
    const cleaned = current.map((matcher) => ({
      ...matcher,
      hooks: (matcher.hooks ?? []).filter((entry) => !isOurHookEntry(entry))
    })).filter((matcher) => matcher.hooks.length > 0);
    merged[hookName] = [...cleaned, ...matchers];
  }
  return merged;
}
async function installHooks(opts) {
  const snippet = buildHookSnippet();
  if (opts.print) {
    console.log(JSON.stringify({ hooks: snippet }, null, 2));
    return;
  }
  const path4 = settingsPath();
  let settings = {};
  if (existsSync41(path4)) {
    try {
      settings = JSON.parse(readFileSync30(path4, "utf-8"));
    } catch {
      throw new Error(
        `Could not parse ${path4}. Fix the JSON or run "tmux-ide agent hook install --print" and merge manually.`
      );
    }
  }
  const existingHooks = settings.hooks && typeof settings.hooks === "object" ? settings.hooks : void 0;
  settings.hooks = mergeHooks(existingHooks, snippet);
  mkdirSync25(dirname18(path4), { recursive: true });
  const tmp = `${path4}.${process.pid}.${Date.now()}.tmp`;
  writeFileSync25(tmp, JSON.stringify(settings, null, 2) + "\n", "utf-8");
  renameSync19(tmp, path4);
  if (opts.json) {
    console.log(JSON.stringify({ ok: true, path: path4, events: Object.keys(snippet) }));
  } else {
    console.log(`Installed tmux-ide agent hooks into ${path4}`);
    console.log(`Events: ${Object.keys(snippet).join(", ")}`);
    console.log("Restart your Claude Code sessions for the hooks to take effect.");
  }
}
async function agentCommand(opts) {
  const sub = opts.sub;
  if (sub === "report") {
    const event = opts.args?.[0];
    if (event === "start" || event === "activity" || event === "idle" || event === "stop") {
      try {
        await report(event);
      } catch {
      }
    }
    process.exit(0);
  }
  if (sub === "hook") {
    const hookSub = opts.args?.[0];
    if (hookSub === "install") {
      await installHooks({ print: opts.print, json: opts.json });
      return;
    }
    throw new Error("Usage: tmux-ide agent hook install [--print]");
  }
  throw new Error(
    "Usage:\n  tmux-ide agent report <start|activity|idle|stop>\n  tmux-ide agent hook install [--print]"
  );
}
var EVENT_TO_ACTION, HOOK_EVENTS;
var init_agent_hook = __esm({
  "packages/daemon/src/agent-hook.ts"() {
    "use strict";
    init_canonical_daemon();
    EVENT_TO_ACTION = {
      // start/activity re-register (register is an idempotent upsert) so a session
      // evicted after a long idle reappears on its next prompt or tool call.
      // idle is a lightweight status-only heartbeat fired when a turn ends — the
      // agent stays on the roster (just not "busy"), instead of vanishing between
      // turns. stop is the real teardown (session exit).
      start: "agent.register",
      activity: "agent.register",
      idle: "agent.heartbeat",
      stop: "agent.unregister"
    };
    HOOK_EVENTS = {
      SessionStart: "start",
      UserPromptSubmit: "activity",
      PreToolUse: "activity",
      Stop: "idle",
      SessionEnd: "stop"
    };
  }
});

// packages/daemon/src/agents-cli.ts
var agents_cli_exports = {};
__export(agents_cli_exports, {
  agentsCommand: () => agentsCommand,
  findAgentById: () => findAgentById,
  formatAgentLine: () => formatAgentLine,
  formatAgentList: () => formatAgentList,
  groupAgentsByMachine: () => groupAgentsByMachine,
  resolveSendTarget: () => resolveSendTarget
});
function findAgentById(agents, id) {
  return agents.find((agent) => agent.id === id);
}
function resolveSendTarget(agents, id) {
  const agent = findAgentById(agents, id);
  if (!agent) {
    throw new IdeError(`Agent not found: ${id}. Run \`tmux-ide agents\` to list agent ids.`, {
      code: "AGENT_NOT_FOUND",
      exitCode: 1
    });
  }
  return { agent, machineId: agent.machineId };
}
function groupAgentsByMachine(agents) {
  const groups = /* @__PURE__ */ new Map();
  for (const agent of agents) {
    const key = agent.machineId ?? "";
    let group = groups.get(key);
    if (!group) {
      group = { machineId: agent.machineId, machineName: agent.machineName, agents: [] };
      groups.set(key, group);
    }
    group.agents.push(agent);
  }
  const ordered = [...groups.values()];
  ordered.sort((a, b) => Number(a.machineId !== null) - Number(b.machineId !== null));
  return ordered;
}
function formatAgentLine(agent) {
  const location = agent.session && agent.paneId ? `${agent.session}\xB7${agent.paneId}` : agent.cwd ?? "";
  const parts = [`${STATUS_GLYPHS[agent.status]} ${agent.name}`, agent.tool, agent.kind];
  if (location) parts.push(location);
  if (agent.taskTitle) parts.push(`\u2014 ${agent.taskTitle}`);
  parts.push(`[${agent.id}]`);
  return `  ${parts.join("  ")}`;
}
function formatAgentList(agents) {
  if (agents.length === 0) {
    return "No agents found.";
  }
  const lines = [];
  for (const group of groupAgentsByMachine(agents)) {
    const label = group.machineId === null ? group.machineName ? `${group.machineName} (this machine)` : "this machine" : group.machineName ?? group.machineId;
    lines.push(label);
    for (const agent of group.agents) {
      lines.push(formatAgentLine(agent));
    }
  }
  return lines.join("\n");
}
function requireDaemonInfo() {
  const info = readCanonicalDaemonInfo();
  if (!info) {
    throw new IdeError(NO_DAEMON_MESSAGE, { code: "DAEMON_UNAVAILABLE", exitCode: 1 });
  }
  return info;
}
function daemonRequest(info, path4) {
  const url = `http://${hostnameForClient2(info.bindHostname)}:${info.port}${path4}`;
  const headers = { "Content-Type": "application/json" };
  if (info.authToken) headers.Authorization = `Bearer ${info.authToken}`;
  return { url, headers };
}
function timeoutSignal5(ms) {
  const controller = new AbortController();
  setTimeout(() => controller.abort(), ms).unref?.();
  return controller.signal;
}
async function fetchAgentList(info) {
  const { url, headers } = daemonRequest(info, "/api/hq/agents");
  let res;
  try {
    res = await fetch(url, { headers, signal: timeoutSignal5(15e3) });
  } catch {
    throw new IdeError(NO_DAEMON_MESSAGE, { code: "DAEMON_UNAVAILABLE", exitCode: 1 });
  }
  if (!res.ok) {
    throw new IdeError(`Daemon replied ${res.status} to GET /api/hq/agents`, {
      code: "DAEMON_ERROR",
      exitCode: 1
    });
  }
  const raw = await res.json();
  const parsed = AgentListSchemaZ.safeParse(raw);
  if (!parsed.success) {
    throw new IdeError("Daemon returned an unexpected /api/hq/agents payload", {
      code: "DAEMON_ERROR",
      exitCode: 1
    });
  }
  return { agents: parsed.data.agents, raw };
}
async function listAgents(opts) {
  const info = requireDaemonInfo();
  const { agents, raw } = await fetchAgentList(info);
  if (opts.json) {
    console.log(JSON.stringify(raw));
    return;
  }
  console.log(formatAgentList(agents));
}
async function sendToAgent(opts) {
  const info = requireDaemonInfo();
  const { agents } = await fetchAgentList(info);
  const { agent, machineId } = resolveSendTarget(agents, opts.id);
  const { url, headers } = daemonRequest(info, "/api/agents/send");
  let res;
  try {
    res = await fetch(url, {
      method: "POST",
      headers,
      body: JSON.stringify({
        id: opts.id,
        machineId,
        message: opts.message,
        noEnter: opts.noEnter ? true : void 0
      }),
      signal: timeoutSignal5(15e3)
    });
  } catch {
    throw new IdeError(NO_DAEMON_MESSAGE, { code: "DAEMON_UNAVAILABLE", exitCode: 1 });
  }
  const body = await res.json().catch(() => ({}));
  if (!res.ok) {
    const detail = typeof body.error === "string" ? body.error : `HTTP ${res.status}`;
    const code = res.status === 409 ? "AGENT_OBSERVE_ONLY" : "SEND_FAILED";
    throw new IdeError(`Send failed: ${detail}`, { code, exitCode: 1 });
  }
  if (opts.json) {
    console.log(JSON.stringify(body));
    return;
  }
  const where = machineId === null ? "this machine" : agent.machineName ?? machineId;
  console.log(`Sent to ${agent.name} [${agent.id}] on ${where}.`);
}
async function agentsCommand(opts) {
  const sub = opts.sub;
  if (sub === void 0 || sub === "list") {
    await listAgents({ json: opts.json });
    return;
  }
  if (sub === "send") {
    const [id, ...messageParts] = opts.args ?? [];
    const message = messageParts.join(" ");
    if (!id || !message) {
      throw new IdeError(
        "Usage: tmux-ide agents send <agent-id> <message...> [--no-enter]\nRun `tmux-ide agents` to list agent ids.",
        { code: "USAGE", exitCode: 1 }
      );
    }
    await sendToAgent({ id, message, noEnter: opts.noEnter, json: opts.json });
    return;
  }
  throw new IdeError(
    "Usage:\n  tmux-ide agents [list] [--json]\n  tmux-ide agents send <agent-id> <message...> [--no-enter] [--json]\n(For the plain-terminal hook reporter, see `tmux-ide agent`.)",
    { code: "USAGE", exitCode: 1 }
  );
}
var NO_DAEMON_MESSAGE, STATUS_GLYPHS;
var init_agents_cli = __esm({
  "packages/daemon/src/agents-cli.ts"() {
    "use strict";
    init_src2();
    init_agent_hook();
    init_canonical_daemon();
    init_errors2();
    NO_DAEMON_MESSAGE = "No tmux-ide daemon running \u2014 start one with `tmux-ide`";
    STATUS_GLYPHS = {
      busy: "\u25CF",
      idle: "\u25CB",
      offline: "\u25CC"
    };
  }
});

// packages/daemon/src/command-center/index.ts
var command_center_exports = {};
__export(command_center_exports, {
  startCommandCenter: () => startCommandCenter
});
import { createServer as createServer3 } from "node:http";
import { getRequestListener } from "@hono/node-server";
async function startCommandCenter(options = {}) {
  const port = options.port ?? 6060;
  const hostname3 = options.hostname ?? "0.0.0.0";
  const appOpts = {};
  if (options.authService) appOpts.authService = options.authService;
  if (options.authConfig) appOpts.authConfig = options.authConfig;
  const app = createApp(appOpts);
  const listener = getRequestListener(app.fetch);
  const server = createServer3(listener);
  return new Promise((resolve35) => {
    server.listen(port, hostname3, () => {
      console.log(`Command Center API on http://${hostname3}:${port}`);
      resolve35(server);
    });
  });
}
var init_command_center = __esm({
  "packages/daemon/src/command-center/index.ts"() {
    "use strict";
    init_server();
  }
});

// packages/daemon/src/server/index.ts
var server_exports2 = {};
__export(server_exports2, {
  createApp: () => createApp2,
  resolvePort: () => resolvePort,
  start: () => start
});
import { createServer as createServer4 } from "node:http";
import { parse } from "node:url";
import { Hono as Hono2 } from "hono";
import { getRequestListener as getRequestListener2 } from "@hono/node-server";
import { WebSocketServer as WebSocketServer3 } from "ws";
function resolvePort(port) {
  const raw = port ?? Number.parseInt(process.env.TMUX_IDE_PORT ?? String(DEFAULT_PORT), 10);
  if (!Number.isInteger(raw) || raw <= 0) {
    throw new Error(`Invalid server port: ${String(port ?? process.env.TMUX_IDE_PORT)}`);
  }
  return raw;
}
function createApp2() {
  const app = new Hono2();
  app.get("/", (c) => c.text("tmux-ide server"));
  app.get("/health", (c) => c.json({ ok: true }));
  return app;
}
async function start(port) {
  const resolvedPort = resolvePort(port);
  const app = createApp2();
  const server = createServer4(getRequestListener2(app.fetch));
  const ptyWss = new WebSocketServer3({ noServer: true });
  server.on("upgrade", (req, socket, head) => {
    const { pathname } = parse(req.url ?? "/", true);
    const match = pathname?.match(/^\/ws\/pty\/([^/]+)$/);
    if (!match) {
      socket.destroy();
      return;
    }
    const id = decodeURIComponent(match[1] ?? "");
    ptyWss.handleUpgrade(req, socket, head, (ws) => {
      handlePtyWebSocket(ws, id);
    });
  });
  await new Promise((resolve35, reject) => {
    server.once("error", reject);
    server.listen(resolvedPort, "0.0.0.0", () => {
      server.off("error", reject);
      resolve35();
    });
  });
  console.log(`tmux-ide server listening on http://0.0.0.0:${resolvedPort}`);
  return {
    port: resolvedPort,
    server,
    close: () => new Promise((resolve35, reject) => {
      shutdownPtyBridges();
      ptyWss.close();
      server.close((err) => err ? reject(err) : resolve35());
    })
  };
}
var DEFAULT_PORT;
var init_server2 = __esm({
  "packages/daemon/src/server/index.ts"() {
    "use strict";
    init_ws_route();
    DEFAULT_PORT = 6070;
  }
});

// packages/daemon/src/chat.ts
var chat_exports = {};
__export(chat_exports, {
  chatCommand: () => chatCommand
});
async function chatCommand(opts) {
  if (opts.sub === "events") {
    await chatEventsSubcommand(opts);
    return;
  }
  if (opts.sub !== "session") {
    throw new IdeError(
      `Unknown chat subcommand: ${opts.sub ?? "(none)"}
Usage:
  tmux-ide chat session add <thread-id> --provider <name> --role <role>
  tmux-ide chat events <thread-id> [--json]`,
      { code: "USAGE", exitCode: 1 }
    );
  }
  const action = opts.args[0];
  if (action !== "add") {
    throw new IdeError(
      `Unknown chat session action: ${action ?? "(none)"}
Usage: tmux-ide chat session add <thread-id> --provider <name> --role <role>`,
      { code: "USAGE", exitCode: 1 }
    );
  }
  const threadId = opts.args[1];
  if (!threadId) {
    throw new IdeError(
      `chat session add requires a thread id
Usage: tmux-ide chat session add <thread-id> --provider <name> --role <role>`,
      { code: "USAGE", exitCode: 1 }
    );
  }
  const provider = opts.provider;
  if (!provider) {
    throw new IdeError(`chat session add requires --provider <name>`, {
      code: "USAGE",
      exitCode: 1
    });
  }
  const role = opts.role;
  if (role && !ROLES.has(role)) {
    throw new IdeError(`Unknown --role: ${role}
Known roles: ${[...ROLES].join(", ")}`, {
      code: "USAGE",
      exitCode: 1
    });
  }
  const { getDefaultThreadManager: getDefaultThreadManager2, getDefaultThreadStore: getDefaultThreadStore2 } = await Promise.resolve().then(() => (init_defaults(), defaults_exports));
  const store = getDefaultThreadStore2();
  const thread = await store.get(threadId);
  if (!thread) {
    throw new IdeError(`Chat thread "${threadId}" not found`, {
      code: "NOT_FOUND",
      exitCode: 1
    });
  }
  const manager2 = getDefaultThreadManager2();
  const session = manager2.createSession({
    threadId,
    provider: { kind: provider },
    ...role ? { role } : {},
    ...opts.name ? { displayName: opts.name } : {}
  });
  if (opts.json) {
    process.stdout.write(`${JSON.stringify({ session }, null, 2)}
`);
    return;
  }
  process.stdout.write(
    `Added session ${session.id} (${session.role ?? "unset"}) to thread ${threadId}
`
  );
}
async function chatEventsSubcommand(opts) {
  const threadId = opts.args[0];
  if (!threadId) {
    throw new IdeError(
      `chat events requires a thread id
Usage: tmux-ide chat events <thread-id> [--json]`,
      { code: "USAGE", exitCode: 1 }
    );
  }
  const { getDefaultChatEventStore: getDefaultChatEventStore2 } = await Promise.resolve().then(() => (init_defaults(), defaults_exports));
  const store = getDefaultChatEventStore2();
  const events = store.readByStream(threadId);
  if (opts.json) {
    process.stdout.write(`${JSON.stringify({ threadId, events }, null, 2)}
`);
    return;
  }
  if (events.length === 0) {
    process.stdout.write(`No events for thread ${threadId}
`);
    return;
  }
  for (const e of events) {
    const payload = JSON.stringify(e.event);
    process.stdout.write(
      `#${e.sequence} v${e.streamVersion} ${e.occurredAt} ${e.actorKind} ${e.eventType ?? e.event.type}
  ${payload}
`
    );
  }
}
var ROLES;
var init_chat = __esm({
  "packages/daemon/src/chat.ts"() {
    "use strict";
    init_errors2();
    ROLES = /* @__PURE__ */ new Set([
      "lead",
      "teammate",
      "planner",
      "validator",
      "researcher"
    ]);
  }
});

// packages/daemon/src/chat/checkpoint-engine.ts
import { execFile as execFile8 } from "node:child_process";
import { promisify as promisify4 } from "node:util";
function makeCheckpointEngine(opts = {}) {
  const exec = opts.exec ?? defaultExec2;
  function buildRefName(threadId, turnId) {
    assertSafeRefSegment(threadId, "threadId");
    assertSafeRefSegment(turnId, "turnId");
    return `${REF_PREFIX}/${threadId}/${turnId}`;
  }
  async function ensureGitRepo(cwd) {
    try {
      await exec(["rev-parse", "--git-dir"], cwd);
    } catch (err) {
      throw new CheckpointEngineError(
        `Directory is not a git repository: ${cwd}`,
        "not_a_git_repo",
        err
      );
    }
  }
  async function snapshot(input) {
    await ensureGitRepo(input.workspaceDir);
    let sha = (await exec(["stash", "create"], input.workspaceDir)).trim();
    if (!sha) {
      sha = (await exec(["rev-parse", "HEAD"], input.workspaceDir)).trim();
    }
    if (!sha) {
      throw new CheckpointEngineError(
        "Could not produce a checkpoint SHA (empty repo?)",
        "git_failed"
      );
    }
    const refName = buildRefName(input.threadId, input.turnId);
    try {
      await exec(["update-ref", refName, sha], input.workspaceDir);
    } catch (err) {
      throw new CheckpointEngineError(`git update-ref failed for ${refName}`, "git_failed", err);
    }
    const files = await collectChangedFiles(input.workspaceDir, exec);
    return { checkpointRef: sha, refName, files };
  }
  async function status4(input) {
    try {
      await ensureGitRepo(input.workspaceDir);
    } catch {
      return "error";
    }
    try {
      const resolved2 = (await exec(["rev-parse", "--verify", `${input.checkpointRef}^{commit}`], input.workspaceDir)).trim();
      return resolved2 ? "ready" : "missing";
    } catch (err) {
      const message = errorMessage(err);
      if (/unknown revision|not a valid|bad revision|fatal: Needed/i.test(message)) {
        return "missing";
      }
      return "error";
    }
  }
  async function revert(input) {
    await ensureGitRepo(input.workspaceDir);
    const refStatus = await status4({
      checkpointRef: input.checkpointRef,
      workspaceDir: input.workspaceDir
    });
    if (refStatus !== "ready") {
      throw new CheckpointEngineError(
        `Checkpoint ref not found: ${input.checkpointRef}`,
        "ref_not_found"
      );
    }
    const conflicts = await detectDirtyConflicts(input.workspaceDir, input.checkpointRef, exec);
    if (conflicts.length > 0) {
      throw new CheckpointEngineError(
        `Cannot revert \u2014 working tree has uncommitted changes that conflict with the checkpoint: ${conflicts.join(", ")}`,
        "dirty_conflict"
      );
    }
    try {
      await exec(["checkout", input.checkpointRef, "--", "."], input.workspaceDir);
    } catch (err) {
      throw new CheckpointEngineError(
        `git checkout from ${input.checkpointRef} failed`,
        "git_failed",
        err
      );
    }
  }
  async function listForThread(input) {
    await ensureGitRepo(input.workspaceDir);
    assertSafeRefSegment(input.threadId, "threadId");
    const prefix = `${REF_PREFIX}/${input.threadId}/`;
    const output = await exec(
      ["for-each-ref", "--format=%(refname) %(objectname)", prefix],
      input.workspaceDir
    );
    return output.split("\n").map((line) => line.trim()).filter(Boolean).map((line) => {
      const [refName = "", sha = ""] = line.split(/\s+/, 2);
      const turnId = refName.slice(prefix.length);
      return { turnId, refName, checkpointRef: sha };
    }).filter((row) => row.turnId.length > 0);
  }
  return { snapshot, status: status4, revert, listForThread };
}
async function defaultExec2(args, cwd) {
  try {
    const { stdout } = await execFileAsync3("git", args, {
      cwd,
      encoding: "utf8",
      maxBuffer: 32 * 1024 * 1024
    });
    return stdout;
  } catch (err) {
    const ex = err;
    const stderr = typeof ex.stderr === "string" ? ex.stderr : "";
    const message = stderr.trim() || ex.message || "git command failed";
    const wrapped = new Error(message);
    wrapped.cause = err;
    throw wrapped;
  }
}
function assertSafeRefSegment(value, what) {
  if (!SAFE_REF_SEGMENT.test(value)) {
    throw new CheckpointEngineError(
      `Invalid ${what}: must match ${SAFE_REF_SEGMENT.source}, got "${value}"`,
      "invalid_id"
    );
  }
}
function errorMessage(err) {
  if (err instanceof Error) return err.message;
  return String(err);
}
async function collectChangedFiles(cwd, exec) {
  const [numstatRaw, nameStatusRaw] = await Promise.all([
    exec(["diff", "--numstat", "HEAD"], cwd).catch(() => ""),
    exec(["diff", "--name-status", "HEAD"], cwd).catch(() => "")
  ]);
  const kindByPath = /* @__PURE__ */ new Map();
  for (const line of nameStatusRaw.split("\n")) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    const [code = "", ...rest] = trimmed.split(/\s+/);
    if (rest.length === 0) continue;
    const head = code.charAt(0);
    const kind = NAME_STATUS_KIND_MAP[head] ?? "modified";
    const path4 = rest[rest.length - 1];
    kindByPath.set(path4, kind);
  }
  const files = [];
  for (const line of numstatRaw.split("\n")) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    const [addStr = "0", delStr = "0", ...pathParts] = trimmed.split(/\s+/);
    const path4 = pathParts.join(" ");
    if (!path4) continue;
    const additions = addStr === "-" ? 0 : Math.max(0, Number.parseInt(addStr, 10) || 0);
    const deletions = delStr === "-" ? 0 : Math.max(0, Number.parseInt(delStr, 10) || 0);
    files.push({
      path: path4,
      kind: kindByPath.get(path4) ?? "modified",
      additions,
      deletions
    });
  }
  return files;
}
async function detectDirtyConflicts(cwd, checkpointRef, exec) {
  const refPaths = (await exec(["diff", "--name-only", `${checkpointRef}`], cwd).catch(() => "")).split("\n").map((s) => s.trim()).filter(Boolean);
  if (refPaths.length === 0) return [];
  const dirtyPaths = new Set(
    (await exec(["diff", "--name-only", "HEAD"], cwd).catch(() => "")).split("\n").map((s) => s.trim()).filter(Boolean)
  );
  return refPaths.filter((p) => dirtyPaths.has(p));
}
var execFileAsync3, REF_PREFIX, NAME_STATUS_KIND_MAP, CheckpointEngineError, SAFE_REF_SEGMENT;
var init_checkpoint_engine = __esm({
  "packages/daemon/src/chat/checkpoint-engine.ts"() {
    "use strict";
    execFileAsync3 = promisify4(execFile8);
    REF_PREFIX = "refs/tmux-ide/checkpoints";
    NAME_STATUS_KIND_MAP = {
      M: "modified",
      A: "added",
      D: "deleted",
      R: "renamed",
      C: "modified",
      T: "modified"
    };
    CheckpointEngineError = class extends Error {
      code;
      cause;
      constructor(message, code, cause) {
        super(message);
        this.name = "CheckpointEngineError";
        this.code = code;
        this.cause = cause;
      }
    };
    SAFE_REF_SEGMENT = /^[A-Za-z0-9._-]+$/;
  }
});

// packages/daemon/src/checkpoint.ts
var checkpoint_exports = {};
__export(checkpoint_exports, {
  checkpointCommand: () => checkpointCommand
});
import { resolve as resolve33 } from "node:path";
function fail(message, code, json2) {
  if (json2) {
    process.stderr.write(`${JSON.stringify({ ok: false, code, message })}
`);
  } else {
    process.stderr.write(`${message}
`);
  }
  process.exit(1);
}
async function checkpointCommand(opts) {
  const sub = opts.sub;
  if (!sub) {
    fail("Missing subcommand. Usage: tmux-ide checkpoint <list|revert> ...", "USAGE", opts.json);
  }
  const workspaceDir = resolve33(opts.workspaceDir ?? ".");
  const engine = makeCheckpointEngine();
  switch (sub) {
    case "list": {
      const threadId = opts.args[0];
      if (!threadId) {
        fail("Missing thread id. Usage: tmux-ide checkpoint list <thread-id>", "USAGE", opts.json);
      }
      try {
        const rows = await engine.listForThread({ threadId, workspaceDir });
        if (opts.json) {
          console.log(JSON.stringify({ ok: true, threadId, checkpoints: rows }, null, 2));
          return;
        }
        if (rows.length === 0) {
          console.log(`No checkpoints found for thread "${threadId}".`);
          return;
        }
        for (const row of rows) {
          console.log(`${row.checkpointRef.slice(0, 12)}  ${row.refName}  turn=${row.turnId}`);
        }
      } catch (err) {
        reportFailure(err, "Failed to list checkpoints", opts.json);
      }
      return;
    }
    case "revert": {
      const ref = opts.args[0];
      if (!ref) {
        fail(
          "Missing checkpoint ref. Usage: tmux-ide checkpoint revert <ref-or-sha>",
          "USAGE",
          opts.json
        );
      }
      try {
        await engine.revert({ checkpointRef: ref, workspaceDir });
        if (opts.json) {
          console.log(JSON.stringify({ ok: true, reverted: ref }, null, 2));
          return;
        }
        console.log(`Reverted workspace to ${ref}.`);
      } catch (err) {
        reportFailure(err, "Failed to revert checkpoint", opts.json);
      }
      return;
    }
    default:
      fail(
        `Unknown checkpoint subcommand: ${sub}. Expected "list" or "revert".`,
        "USAGE",
        opts.json
      );
  }
}
function reportFailure(err, fallback, json2) {
  if (err instanceof CheckpointEngineError) {
    fail(err.message, err.code.toUpperCase(), json2);
  }
  const message = err instanceof Error ? err.message : String(err);
  fail(`${fallback}: ${message}`, "CHECKPOINT_ERROR", json2);
}
var init_checkpoint = __esm({
  "packages/daemon/src/checkpoint.ts"() {
    "use strict";
    init_checkpoint_engine();
  }
});

// bin/cli.ts
init_launch();
import { parseArgs } from "node:util";
import { resolve as resolve34, dirname as dirname19 } from "node:path";
import { execFileSync as execFileSync9 } from "node:child_process";
import { existsSync as existsSync42 } from "node:fs";
import { fileURLToPath as fileURLToPath7 } from "node:url";

// packages/daemon/src/init.ts
init_detect();
init_output();
import {
  existsSync as existsSync4,
  readFileSync as readFileSync5,
  writeFileSync as writeFileSync4,
  renameSync as renameSync2,
  mkdirSync as mkdirSync3,
  readdirSync,
  copyFileSync
} from "node:fs";
import { resolve as resolve7, join as join3, basename as basename3, dirname as dirname3 } from "node:path";
import { fileURLToPath as fileURLToPath2 } from "node:url";
var __dirname2 = dirname3(fileURLToPath2(import.meta.url));
function copyTemplateSkills(targetDir) {
  const created = [];
  const templateSkillsDir = resolve7(__dirname2, "..", "..", "..", "templates", "skills");
  if (!existsSync4(templateSkillsDir)) return created;
  mkdirSync3(targetDir, { recursive: true });
  for (const file of readdirSync(templateSkillsDir)) {
    if (!file.endsWith(".md")) continue;
    const destination = join3(targetDir, file);
    copyFileSync(join3(templateSkillsDir, file), destination);
    created.push(destination);
  }
  return created;
}
function scaffoldLibraryStubs(dir) {
  const created = [];
  const libraryDir = join3(dir, ".tmux-ide", "library");
  if (!existsSync4(libraryDir)) {
    mkdirSync3(libraryDir, { recursive: true });
    created.push(libraryDir);
  }
  const archPath = join3(libraryDir, "architecture.md");
  if (!existsSync4(archPath)) {
    writeFileSync4(
      archPath,
      "# Architecture\n\n<!-- Describe your project's architecture here. This context is injected into agent dispatch prompts. -->\n"
    );
    created.push(archPath);
  }
  const learningsPath = join3(libraryDir, "learnings.md");
  if (!existsSync4(learningsPath)) {
    writeFileSync4(
      learningsPath,
      "# Learnings\n\n<!-- Task summaries are automatically appended here by the orchestrator. -->\n"
    );
    created.push(learningsPath);
  }
  return created;
}
function scaffoldValidationContract(dir) {
  const created = [];
  const tasksDir = join3(dir, ".tasks");
  if (!existsSync4(tasksDir)) {
    mkdirSync3(tasksDir, { recursive: true });
  }
  const contractPath = join3(tasksDir, "validation-contract.md");
  if (!existsSync4(contractPath)) {
    writeFileSync4(
      contractPath,
      "# Validation Contract\n\n<!-- Define assertions that the validator agent will verify. Example: -->\n<!-- - VAL-001: All tests pass -->\n<!-- - VAL-002: No TypeScript errors -->\n<!-- - VAL-003: Lint passes with zero warnings -->\n"
    );
    created.push(contractPath);
  }
  return created;
}
function scaffoldAgentsMd(dir, name) {
  const created = [];
  const agentsTemplatePath = resolve7(__dirname2, "..", "..", "..", "templates", "AGENTS.md");
  if (existsSync4(agentsTemplatePath)) {
    const agentsPath = join3(dir, "AGENTS.md");
    if (!existsSync4(agentsPath)) {
      const content = readFileSync5(agentsTemplatePath, "utf-8").replace(/{{name}}/g, name);
      writeFileSync4(agentsPath, content);
      created.push(agentsPath);
    }
  }
  return created;
}
function isTeamTemplate(templateName) {
  return templateName === "missions" || templateName.startsWith("agent-team");
}
function scaffoldTeamWorkspace(dir, name) {
  const created = [];
  created.push(...scaffoldLibraryStubs(dir));
  created.push(...scaffoldValidationContract(dir));
  created.push(...scaffoldAgentsMd(dir, name));
  return created;
}
function scaffoldMissionsWorkspace(dir, name) {
  const created = [];
  const skillsDir = join3(dir, ".tmux-ide", "skills");
  created.push(...copyTemplateSkills(skillsDir));
  created.push(...scaffoldTeamWorkspace(dir, name));
  return created;
}
async function init({
  template,
  json: json2
} = {}) {
  const dir = process.cwd();
  const configPath = resolve7(dir, "ide.yml");
  if (existsSync4(configPath)) {
    outputError("ide.yml already exists in this directory", "EXISTS");
  }
  if (template) {
    const templatePath = resolve7(__dirname2, "..", "..", "..", "templates", `${template}.yml`);
    if (!existsSync4(templatePath)) {
      outputError(`Template "${template}" not found`, "NOT_FOUND");
    }
    let content = readFileSync5(templatePath, "utf-8");
    const name2 = basename3(dir);
    content = content.replace(/^name: .+/m, `name: ${name2}`);
    const tmpPath = configPath + ".tmp";
    writeFileSync4(tmpPath, content);
    renameSync2(tmpPath, configPath);
    let created;
    if (template === "missions") {
      created = scaffoldMissionsWorkspace(dir, name2);
    } else if (isTeamTemplate(template)) {
      created = [
        ...copyTemplateSkills(join3(dir, ".tmux-ide", "skills")),
        ...scaffoldTeamWorkspace(dir, name2)
      ];
    } else {
      created = copyTemplateSkills(join3(dir, ".tmux-ide", "skills"));
    }
    if (json2) {
      console.log(JSON.stringify({ created: true, template, name: name2, paths: created }));
    } else {
      console.log(`Created ide.yml from "${template}" template for "${name2}"`);
      const yaml5 = (await import("js-yaml")).default;
      printLayout(yaml5.load(content));
      for (const createdPath of created) {
        console.log(`Created ${createdPath.replace(dir + "/", "")}`);
      }
    }
    return;
  }
  const detected = detectStack(dir);
  const name = basename3(dir);
  if (detected.frameworks.length > 0) {
    const config2 = suggestConfig(dir, detected);
    const yaml5 = (await import("js-yaml")).default;
    const tmpPath2 = configPath + ".tmp";
    writeFileSync4(tmpPath2, yaml5.dump(config2, { lineWidth: -1, noRefs: true, quotingType: '"' }));
    renameSync2(tmpPath2, configPath);
    const desc = detected.frameworks.join(" + ");
    if (json2) {
      console.log(JSON.stringify({ created: true, detected: detected.frameworks, name }));
    } else {
      console.log(`Detected ${desc}. Created ide.yml for "${name}".`);
      printLayout(config2);
      console.log("Edit it to customize, then run: tmux-ide");
    }
  } else {
    const templatePath = resolve7(__dirname2, "..", "..", "..", "templates", "default.yml");
    let content = readFileSync5(templatePath, "utf-8");
    content = content.replace(/^name: .+/m, `name: ${name}`);
    const tmpPath3 = configPath + ".tmp";
    writeFileSync4(tmpPath3, content);
    renameSync2(tmpPath3, configPath);
    if (json2) {
      console.log(JSON.stringify({ created: true, template: "default", name }));
    } else {
      console.log(`Created ide.yml for "${name}"`);
      const yaml5 = (await import("js-yaml")).default;
      printLayout(yaml5.load(content));
      console.log("Edit it to configure your workspace, then run: tmux-ide");
    }
  }
  const skillsDir = join3(dir, ".tmux-ide", "skills");
  if (!existsSync4(skillsDir)) {
    const created = copyTemplateSkills(skillsDir);
    if (created.length > 0 && !json2) {
      console.log("Copied built-in skill templates to .tmux-ide/skills/");
    }
  }
}

// packages/daemon/src/stop.ts
init_yaml_io();
init_output();
init_src();
import { resolve as resolve8 } from "node:path";
async function stop(targetDir, { json: json2 } = {}) {
  const dir = resolve8(targetDir ?? ".");
  const { name: session } = getSessionName(dir);
  stopSessionMonitor(session);
  const result = killSession(session);
  if (result.stopped) {
    if (json2) {
      console.log(JSON.stringify({ stopped: session }));
    } else {
      console.log(`Stopped session "${session}"`);
    }
    return;
  }
  outputError(`No active session "${session}" found`, "NOT_RUNNING");
}

// packages/daemon/src/attach.ts
init_yaml_io();
init_output();
init_src();
import { resolve as resolve9 } from "node:path";
async function attach(targetDir, { json: _json } = {}) {
  const dir = resolve9(targetDir ?? ".");
  const { name: session } = getSessionName(dir);
  const state = getSessionState(session);
  if (!state.running) {
    outputError(`Session "${session}" is not running. Start it with: tmux-ide`, "NOT_RUNNING");
    return;
  }
  attachSession(session);
}

// packages/daemon/src/ls.ts
import { execSync as execSync2 } from "node:child_process";
async function ls({ json: json2 } = {}) {
  let raw;
  try {
    raw = execSync2(
      'tmux list-sessions -F "#{session_name}|#{session_created}|#{session_attached}"',
      { encoding: "utf-8" }
    ).trim();
  } catch {
    if (json2) {
      console.log(JSON.stringify({ sessions: [] }));
    } else {
      console.log("No tmux sessions running.");
    }
    return;
  }
  const sessions = raw.split("\n").map((line) => {
    const [name, created, attached] = line.split("|");
    return {
      name,
      created: new Date(parseInt(created) * 1e3).toISOString(),
      attached: attached !== "0"
    };
  });
  if (json2) {
    console.log(JSON.stringify({ sessions }, null, 2));
    return;
  }
  console.log("SESSION".padEnd(24) + "CREATED".padEnd(22) + "ATTACHED");
  console.log("\u2500".repeat(54));
  for (const s of sessions) {
    const date = new Date(s.created).toLocaleString();
    console.log(s.name.padEnd(24) + date.padEnd(22) + (s.attached ? "yes" : "no"));
  }
}

// packages/daemon/src/doctor.ts
init_yaml_io();
init_skill_registry();
init_task_store();
import { execSync as execSync3 } from "node:child_process";
import { existsSync as existsSync7 } from "node:fs";
import { resolve as resolve11 } from "node:path";
function check(label, fn, { optional = false } = {}) {
  try {
    const result = fn();
    return { label, pass: true, detail: result, optional };
  } catch (e) {
    return { label, pass: false, detail: e.message, optional };
  }
}
async function doctor({
  json: json2,
  tasks,
  fix
} = {}) {
  const checks = [];
  checks.push(
    check("tmux installed", () => {
      execSync3("which tmux", { stdio: "ignore" });
      return "found";
    })
  );
  checks.push(
    check("tmux version \u2265 3.0", () => {
      const version = execSync3("tmux -V", { encoding: "utf-8" }).trim();
      const num = parseFloat(version.replace(/[^0-9.]/g, ""));
      if (num < 3) throw new Error(`${version} (need \u2265 3.0)`);
      return version;
    })
  );
  checks.push(
    check("Node.js \u2265 18", () => {
      const major = parseInt(process.versions.node.split(".")[0]);
      if (major < 18) throw new Error(`Node ${process.versions.node} (need \u2265 18)`);
      return `v${process.versions.node}`;
    })
  );
  checks.push(
    check(
      "256-color terminal",
      () => {
        const term = process.env.TERM ?? "";
        if (!term.includes("256color") && !term.includes("ghostty") && !term.includes("kitty") && term !== "tmux-256color") {
          throw new Error(`$TERM is "${term}"`);
        }
        return term;
      },
      { optional: true }
    )
  );
  checks.push(
    check("ide.yml exists", () => {
      const path4 = resolve11(".", "ide.yml");
      if (!existsSync7(path4)) throw new Error("not found in current directory");
      return "found";
    })
  );
  checks.push(
    check(
      "Claude Code agent teams",
      () => {
        if (process.env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS !== "1") {
          throw new Error("not set (enable with CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1)");
        }
        return "enabled";
      },
      { optional: true }
    )
  );
  checks.push(
    check(
      "pane skill references",
      () => {
        const dir = resolve11(".");
        const { config: config2 } = readConfig(dir);
        const skills = loadSkills(dir);
        const skillNames = new Set(skills.map((s) => s.name));
        const unresolved = [];
        for (const row of config2.rows) {
          for (const pane of row.panes) {
            if (pane.skill && !skillNames.has(pane.skill)) {
              unresolved.push(`${pane.title ?? "untitled"} \u2192 ${pane.skill}`);
            }
          }
        }
        if (unresolved.length > 0) {
          throw new Error(`${unresolved.length} unresolved: ${unresolved.join(", ")}`);
        }
        const count = config2.rows.flatMap((r) => r.panes).filter((p) => p.skill).length;
        return count > 0 ? `${count} reference(s), all resolved` : "no skill references";
      },
      { optional: true }
    )
  );
  checks.push(
    check(
      "tailscale CLI",
      () => {
        const version = execSync3("tailscale version", { encoding: "utf-8" }).trim().split("\n")[0];
        return version;
      },
      { optional: true }
    )
  );
  checks.push(
    check(
      "ngrok CLI",
      () => {
        const version = execSync3("ngrok version", { encoding: "utf-8" }).trim();
        return version;
      },
      { optional: true }
    )
  );
  checks.push(
    check(
      "cloudflared CLI",
      () => {
        const version = execSync3("cloudflared --version", { encoding: "utf-8" }).trim();
        return version;
      },
      { optional: true }
    )
  );
  let taskReport = null;
  if (tasks) {
    taskReport = validateTasksTree(resolve11("."), { fix });
    checks.push({
      label: "task integrity",
      pass: taskReport.ok,
      detail: taskReport.issues.length === 0 ? "clean" : `${taskReport.issues.length} issue(s)${fix ? ", safe fixes applied where possible" : ""}`,
      optional: false
    });
  }
  const allPass = checks.every((c) => c.pass || c.optional);
  if (json2) {
    console.log(JSON.stringify({ ok: allPass, checks, tasks: taskReport }, null, 2));
    return;
  }
  for (const c of checks) {
    const icon = c.pass ? "\u2713" : c.optional ? "\u25CB" : "\u2717";
    const color = c.pass ? "\x1B[32m" : c.optional ? "\x1B[33m" : "\x1B[31m";
    console.log(`${color}${icon}\x1B[0m ${c.label} \u2014 ${c.detail}`);
  }
  if (taskReport && taskReport.issues.length > 0) {
    console.log("");
    console.log("Task integrity issues:");
    for (const issue of taskReport.issues) {
      console.log(formatTaskIssue(issue));
    }
  }
  if (!allPass) process.exitCode = 1;
}
function formatTaskIssue(issue) {
  const file = issue.file ? `${issue.file}: ` : "";
  const fixed = issue.fixed ? " (fixed)" : "";
  return `  - ${file}${issue.message}${fixed}`;
}

// packages/daemon/src/status.ts
init_yaml_io();
init_src();
init_canonical_daemon();
import { resolve as resolve12 } from "node:path";
import { existsSync as existsSync8 } from "node:fs";
async function status(targetDir, { json: json2 } = {}) {
  const dir = resolve12(targetDir ?? ".");
  const { name: session } = getSessionName(dir);
  const configExists = existsSync8(resolve12(dir, "ide.yml"));
  const state = getSessionState(session);
  const running = state.running;
  let panes = [];
  if (running) panes = listPanes(session);
  const daemonInfo = readCanonicalDaemonInfo();
  const healthy = daemonInfo ? await isCanonicalDaemonAlive(daemonInfo) : false;
  const data = {
    session,
    running,
    configExists,
    panes,
    daemon: {
      pid: daemonInfo?.pid ?? null,
      alive: daemonInfo ? isProcessAlive(daemonInfo.pid) : false,
      port: daemonInfo?.port ?? null,
      healthy
    }
  };
  if (json2) {
    console.log(JSON.stringify(data, null, 2));
    return;
  }
  console.log(`Session: ${session}`);
  console.log(`Running: ${running ? "yes" : "no"}`);
  console.log(`Config:  ${configExists ? "ide.yml found" : "no ide.yml"}`);
  if (running) {
    console.log(
      `Daemon:  ${data.daemon.alive ? "running" : "not running"}${data.daemon.port ? ` (port ${data.daemon.port})` : ""}`
    );
  }
  if (panes.length > 0) {
    console.log(`
Panes:`);
    for (const p of panes) {
      const active2 = p.active ? " (active)" : "";
      console.log(`  ${p.index}: ${p.title} [${p.width}x${p.height}]${active2}`);
    }
  }
}

// packages/daemon/src/inspect.ts
init_yaml_io();
init_validate();
init_output();
init_src();
init_skill_registry();
import { resolve as resolve13, basename as basename5 } from "node:path";
function buildInspection(dir, {
  config: config2,
  configPath,
  running,
  panes
}) {
  const errors = validateConfig(config2);
  const rows = Array.isArray(config2?.rows) ? config2.rows : [];
  const resolvedRows = rows.map((row, rowIndex) => ({
    index: rowIndex,
    size: row.size ?? null,
    panes: (Array.isArray(row?.panes) ? row.panes : []).map((pane, paneIndex) => ({
      index: paneIndex,
      title: pane.title ?? null,
      command: pane.command ?? null,
      dir: pane.dir ?? ".",
      size: pane.size ?? null,
      focus: pane.focus === true,
      role: pane.role ?? null,
      task: pane.task ?? null,
      env: pane.env ?? {}
    }))
  }));
  const focusPane = resolvedRows.flatMap((row) => row.panes.map((pane) => ({ row: row.index, pane }))).find(({ pane }) => pane.focus) ?? null;
  const session = config2?.name ?? basename5(dir);
  const allSkills = loadSkills(dir);
  const skillNames = new Set(allSkills.map((s) => s.name));
  const skills = allSkills.map((s) => ({ name: s.name, specialties: s.specialties }));
  const paneSkillMap = [];
  for (const row of rows) {
    for (const pane of row.panes ?? []) {
      if (pane.skill) {
        paneSkillMap.push({
          pane: pane.title ?? "untitled",
          skill: pane.skill,
          resolved: skillNames.has(pane.skill)
        });
      }
    }
  }
  return {
    dir,
    configPath,
    valid: errors.length === 0,
    errors,
    session,
    before: config2?.before ?? null,
    summary: {
      rows: resolvedRows.length,
      panes: resolvedRows.reduce((sum, row) => sum + row.panes.length, 0),
      focus: focusPane ? `rows.${focusPane.row}.panes.${focusPane.pane.index}` : null
    },
    team: config2?.team ?? null,
    theme: config2?.theme ?? null,
    focus: focusPane ? {
      row: focusPane.row,
      pane: focusPane.pane.index,
      title: focusPane.pane.title
    } : null,
    rows: resolvedRows,
    rawConfig: config2,
    tmux: {
      running,
      panes
    },
    skills,
    paneSkillMap
  };
}
async function inspect(targetDir, { json: json2 } = {}) {
  const dir = resolve13(targetDir ?? ".");
  let config2;
  let configPath;
  try {
    ({ config: config2, configPath } = readConfig(dir));
  } catch (error) {
    outputError(`Cannot read ide.yml: ${error.message}`, "READ_ERROR");
    return;
  }
  const session = config2?.name ?? basename5(dir);
  const state = getSessionState(session);
  const panes = state.running ? listPanes(session) : [];
  const data = buildInspection(dir, { config: config2, configPath, running: state.running, panes });
  if (json2) {
    console.log(JSON.stringify(data, null, 2));
    return;
  }
  console.log(`Directory: ${data.dir}`);
  console.log(`Config:    ${data.configPath}`);
  console.log(`Valid:     ${data.valid ? "yes" : "no"}`);
  console.log(`Session:   ${data.session}`);
  console.log(`Running:   ${data.tmux.running ? "yes" : "no"}`);
  console.log(`Rows:      ${data.summary.rows}`);
  console.log(`Panes:     ${data.summary.panes}`);
  console.log(`Team:      ${data.team ? data.team.name : "disabled"}`);
  if (data.focus) {
    console.log(
      `Focus:     row ${data.focus.row}, pane ${data.focus.pane}${data.focus.title ? ` (${data.focus.title})` : ""}`
    );
  }
  if (!data.valid) {
    console.log("\nValidation Errors:");
    for (const error of data.errors) {
      console.log(`  - ${error}`);
    }
  }
  console.log("\nResolved Layout:");
  for (const row of data.rows) {
    console.log(`  Row ${row.index}${row.size ? ` (${row.size})` : ""}`);
    for (const pane of row.panes) {
      const parts = [];
      if (pane.title) parts.push(pane.title);
      if (pane.command) parts.push(`cmd=${pane.command}`);
      if (pane.dir && pane.dir !== ".") parts.push(`dir=${pane.dir}`);
      if (pane.role) parts.push(`role=${pane.role}`);
      if (pane.focus) parts.push("focus");
      console.log(`    - pane ${pane.index}: ${parts.join(" | ") || "shell"}`);
    }
  }
  if (data.tmux.running && data.tmux.panes.length > 0) {
    console.log("\nLive Panes:");
    for (const pane of data.tmux.panes) {
      const active2 = pane.active ? " (active)" : "";
      console.log(`  ${pane.index}: ${pane.title} [${pane.width}x${pane.height}]${active2}`);
    }
  }
}

// bin/cli.ts
init_validate();
init_detect();
init_config();
init_restart();

// packages/daemon/src/task.ts
init_output();
init_task_store();
init_task_actions();
init_github_pr();
init_validation();
init_yaml_io();
init_src();
init_pane_comms();
init_research();
init_cli_action_bridge();
import { resolve as resolve24 } from "node:path";
function splitList(raw) {
  return raw?.split(",").map((value) => value.trim()).filter(Boolean);
}
function parsePriority(raw) {
  if (raw === void 0) return void 0;
  const value = parseInt(raw, 10);
  return Number.isFinite(value) ? value : void 0;
}
function printActionError(err) {
  if (err instanceof CliActionInvocationError) {
    outputError(err.message, err.code.toUpperCase());
  }
  throw err;
}
async function tryDispatchTaskAction(dir, {
  json: json2,
  action,
  sub,
  args,
  values: values2
}) {
  try {
    if (action === "task") {
      const id = args[0];
      if (sub === "create" && id) {
        const result = await tryDispatchAction(
          "task.create",
          {
            title: id,
            description: values2.description,
            goalId: values2.goal,
            priority: parsePriority(values2.priority),
            assign: values2.assign,
            tags: splitList(values2.tags),
            depends: splitList(values2.depends),
            milestone: values2.milestone,
            specialty: values2.specialty,
            fulfills: splitList(values2.fulfills)
          },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.task, null, 2));
        else console.log(`Created task ${result.taskId}: ${result.task.title}`);
        return true;
      }
      if (sub === "update" && id) {
        const result = await tryDispatchAction(
          "task.update",
          {
            taskId: id,
            status: values2.status,
            assign: values2.assign,
            description: values2.description,
            priority: parsePriority(values2.priority),
            tags: splitList(values2.tags),
            goalId: values2.goal,
            depends: splitList(values2.depends),
            proof: values2.proof,
            milestone: values2.milestone,
            specialty: values2.specialty,
            fulfills: splitList(values2.fulfills)
          },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.task, null, 2));
        else console.log(`Updated task ${id}`);
        return true;
      }
      if (sub === "claim" && id && values2.assign) {
        const result = await tryDispatchAction(
          "task.claim",
          { taskId: id, assign: values2.assign },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.task, null, 2));
        else console.log(`Task ${id} claimed by ${values2.assign}: ${result.task.title}`);
        return true;
      }
      if (sub === "done" && id) {
        const result = await tryDispatchAction(
          "task.done",
          { taskId: id, proof: values2.proof },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.task, null, 2));
        else console.log(`Task ${id} done: ${result.task.title}`);
        return true;
      }
      if (sub === "delete" && id) {
        const result = await tryDispatchAction("task.delete", { taskId: id }, { cwd: dir });
        if (!result) return false;
        if (json2) console.log(JSON.stringify({ deleted: id }));
        else console.log(`Deleted task ${id}`);
        return true;
      }
    }
    if (action === "goal") {
      const id = args[0];
      if (sub === "create" && id) {
        const result = await tryDispatchAction(
          "goal.create",
          {
            title: id,
            priority: parsePriority(values2.priority),
            acceptance: values2.acceptance,
            description: values2.description,
            milestone: values2.milestone,
            specialty: values2.specialty
          },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.goal, null, 2));
        else console.log(`Created goal ${result.goalId}: ${result.goal.title}`);
        return true;
      }
      if (sub === "update" && id) {
        const result = await tryDispatchAction(
          "goal.update",
          {
            goalId: id,
            status: values2.status,
            description: values2.description,
            acceptance: values2.acceptance,
            priority: parsePriority(values2.priority),
            milestone: values2.milestone
          },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.goal, null, 2));
        else console.log(`Updated goal ${id}`);
        return true;
      }
      if (sub === "done" && id) {
        const result = await tryDispatchAction("goal.done", { goalId: id }, { cwd: dir });
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.goal, null, 2));
        else console.log(`Goal ${id} marked done: ${result.goal.title}`);
        return true;
      }
      if (sub === "delete" && id) {
        const result = await tryDispatchAction("goal.delete", { goalId: id }, { cwd: dir });
        if (!result) return false;
        if (json2) console.log(JSON.stringify({ deleted: id }));
        else console.log(`Deleted goal ${id}`);
        return true;
      }
    }
    if (action === "milestone") {
      const id = args[0];
      if (sub === "create" && id) {
        const sequence = parsePriority(values2.sequence);
        const result = await tryDispatchAction(
          "milestone.create",
          { title: id, sequence, description: values2.description },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.milestone, null, 2));
        else
          console.log(
            `Created milestone ${result.milestoneId}: ${id} [${result.milestone.status}]`
          );
        return true;
      }
      if (sub === "update" && id) {
        const result = await tryDispatchAction(
          "milestone.update",
          {
            milestoneId: id,
            status: values2.status
          },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.milestone, null, 2));
        else console.log(`Updated milestone ${id}`);
        return true;
      }
    }
    if (action === "mission") {
      if (sub === "set" && args[0]) {
        const result = await tryDispatchAction(
          "mission.set",
          { title: args[0], description: values2.description },
          { cwd: dir }
        );
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result.mission, null, 2));
        else console.log(`Mission set: ${result.mission.title}`);
        return true;
      }
      if (sub === "plan-complete") {
        const result = await tryDispatchAction("mission.planComplete", {}, { cwd: dir });
        if (!result) return false;
        saveMission(dir, result.mission);
        if (json2) console.log(JSON.stringify(result.mission, null, 2));
        else console.log(`Mission "${result.mission.title}" planning complete \u2014 now active`);
        return true;
      }
      if (sub === "clear") {
        const result = await tryDispatchAction("mission.clear", {}, { cwd: dir });
        if (!result) return false;
        if (json2) console.log(JSON.stringify(result));
        else console.log("Mission cleared");
        return true;
      }
    }
    if (action === "validate" && sub === "assert" && args[0] && values2.status) {
      const result = await tryDispatchAction(
        "validation.assert",
        {
          assertId: args[0],
          status: values2.status,
          evidence: values2.evidence
        },
        { cwd: dir }
      );
      if (!result) return false;
      if (json2) console.log(JSON.stringify(result.assertion, null, 2));
      else console.log(`Assertion ${args[0]}: ${result.assertion.status}`);
      return true;
    }
  } catch (err) {
    printActionError(err);
  }
  return false;
}
async function taskCommand(targetDir, {
  json: json2 = false,
  action,
  sub,
  args = [],
  values: values2 = {}
}) {
  const dir = resolve24(targetDir ?? ".");
  if (await tryDispatchTaskAction(dir, { json: json2, action, sub, args, values: values2 })) return;
  switch (action) {
    case "mission":
      return handleMission(dir, sub, args, values2, json2);
    case "milestone":
      return handleMilestone(dir, sub, args, values2, json2);
    case "validate":
      return handleValidation(dir, sub, args, values2, json2);
    case "research":
      return handleResearch(dir, sub, args, json2);
    case "goal":
      return handleGoal(dir, sub, args, values2, json2);
    case "task":
      return handleTask(dir, sub, args, values2, json2);
    default:
      outputError(`Unknown action: ${action}`, "USAGE");
  }
}
function emptyOrchestratorState() {
  return {
    lastActivity: /* @__PURE__ */ new Map(),
    previousTasks: /* @__PURE__ */ new Map(),
    claimedTasks: /* @__PURE__ */ new Set(),
    taskClaimTimes: /* @__PURE__ */ new Map(),
    inflightDispatches: /* @__PURE__ */ new Map(),
    completedDispatches: /* @__PURE__ */ new Set(),
    stallNudges: /* @__PURE__ */ new Map(),
    totalDispatched: 0,
    totalCompleted: 0,
    totalFailed: 0,
    lastTickMs: 0,
    ticking: false,
    idleAgents: 0,
    queuedDispatches: 0,
    lastError: null,
    lastReconcileMs: 0
  };
}
function handleResearch(dir, sub, args, json2) {
  switch (sub) {
    case "status": {
      const researchState = loadResearchState(dir);
      const tasks = loadTasks(dir);
      const activeTask = researchState.activeResearchTaskId != null ? tasks.find((task) => task.id === researchState.activeResearchTaskId) ?? null : null;
      const recentFindings = tasks.filter((task) => task.tags.includes("research") && task.status === "done").sort((a, b) => Date.parse(b.updated) - Date.parse(a.updated)).slice(0, 5).map((task) => ({
        id: task.id,
        title: task.title,
        updated: task.updated,
        type: task.tags.find((tag) => tag !== "research") ?? "research",
        summary: task.salientSummary
      }));
      const payload = {
        activeTask,
        recentFindings,
        cooldowns: researchState.lastResearchAt,
        missionStartAnalyzed: researchState.missionStartAnalyzed,
        retryWindow: researchState.retryWindow
      };
      if (json2) {
        console.log(JSON.stringify(payload, null, 2));
      } else {
        console.log(
          `Active research task: ${activeTask ? `${activeTask.id} \u2014 ${activeTask.title}` : "none"}`
        );
        console.log(`Mission start analyzed: ${researchState.missionStartAnalyzed ? "yes" : "no"}`);
        console.log(`Recent findings: ${recentFindings.length}`);
      }
      break;
    }
    case "trigger": {
      const type = args[0];
      if (!type) outputError("Usage: tmux-ide research trigger <type>", "USAGE");
      const { name: session } = getSessionName(dir);
      const sessionState = getSessionState(session);
      if (!sessionState.running) {
        outputError(`Session "${session}" is not running`, "SESSION_NOT_FOUND");
      }
      const panes = listSessionPanes(session);
      const tasks = loadTasks(dir);
      const researchState = loadResearchState(dir);
      let maxConcurrentAgents = 10;
      let masterPane = null;
      let researchEnabled = true;
      try {
        const { config: config2 } = readConfig(dir);
        maxConcurrentAgents = config2.orchestrator?.max_concurrent_agents ?? 10;
        masterPane = config2.orchestrator?.master_pane ?? null;
        researchEnabled = config2.orchestrator?.research?.enabled ?? true;
      } catch {
      }
      const task = dispatchResearch(
        {
          session,
          dir,
          masterPane,
          maxConcurrentAgents,
          research: { enabled: researchEnabled }
        },
        emptyOrchestratorState(),
        researchState,
        tasks,
        panes,
        {
          type,
          reason: `Manual research trigger: ${type}`
        }
      );
      if (!task) {
        outputError(`Unable to dispatch research trigger "${type}"`, "CONFLICT");
      }
      if (json2) {
        console.log(JSON.stringify(task, null, 2));
      } else {
        console.log(`Dispatched research task ${task.id}: ${task.title}`);
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide research <status|trigger>

  status                         Show research agent state
  trigger <type>                 Manually dispatch a research task`);
      break;
    default:
      outputError(
        "Usage: tmux-ide research <status|trigger>\nRun: tmux-ide research help",
        "USAGE"
      );
  }
}
function handleMission(dir, sub, args, values2, json2) {
  switch (sub) {
    case "set": {
      const title = args[0];
      if (!title) outputError('Usage: tmux-ide mission set "title"', "USAGE");
      const { mission } = setMissionRecord(dir, { title, description: values2.description });
      if (json2) {
        console.log(JSON.stringify(mission, null, 2));
      } else {
        console.log(`Mission set: ${title}`);
      }
      break;
    }
    case "create": {
      const title = args[0];
      if (!title) outputError('Usage: tmux-ide mission create "title" [-d "description"]', "USAGE");
      ensureTasksDir(dir);
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const mission = {
        title,
        description: values2.description ?? "",
        status: "planning",
        branch: null,
        milestones: [],
        created: now,
        updated: now
      };
      saveMission(dir, mission);
      if (json2) {
        console.log(JSON.stringify(mission, null, 2));
      } else {
        console.log(`Mission created (planning): ${title}`);
      }
      break;
    }
    case "plan-complete": {
      const { mission, coverageGaps } = completeMissionPlanRecord(dir);
      const first = [...mission.milestones].sort((a, b) => a.order - b.order)[0] ?? null;
      if (json2) {
        console.log(JSON.stringify({ ...mission, coverageGaps }, null, 2));
      } else {
        console.log(`Mission "${mission.title}" planning complete \u2014 now active`);
        if (first) console.log(`  Activated milestone: ${first.id} \u2014 ${first.title}`);
        if (coverageGaps.length > 0) {
          console.log(
            `  Warning: ${coverageGaps.length} assertion(s) not claimed by any task: ${coverageGaps.join(", ")}`
          );
        }
      }
      break;
    }
    case "show": {
      const mission = loadMission(dir);
      if (!mission) outputError('No mission set. Run: tmux-ide mission set "..."', "NOT_FOUND");
      if (json2) {
        console.log(JSON.stringify(mission, null, 2));
      } else {
        console.log(`Mission: ${mission.title}`);
        if (mission.description) console.log(`  ${mission.description}`);
      }
      break;
    }
    case "status": {
      const mission = loadMission(dir);
      if (!mission) outputError("No mission set", "NOT_FOUND");
      const tasks = loadTasks(dir);
      const doneMilestones = mission.milestones.filter((m) => m.status === "done").length;
      const totalMilestones = mission.milestones.length;
      const doneTasks = tasks.filter((t) => t.status === "done").length;
      const totalTasks = tasks.length;
      const valState = loadValidationState(dir);
      const assertions = valState ? Object.values(valState.assertions) : [];
      const passingCount = assertions.filter((a) => a.status === "passing").length;
      const failingCount = assertions.filter((a) => a.status === "failing").length;
      const pendingCount = assertions.filter((a) => a.status === "pending").length;
      if (json2) {
        console.log(
          JSON.stringify(
            {
              title: mission.title,
              status: mission.status,
              milestones: { done: doneMilestones, total: totalMilestones },
              tasks: { done: doneTasks, total: totalTasks },
              validation: { passing: passingCount, failing: failingCount, pending: pendingCount }
            },
            null,
            2
          )
        );
      } else {
        console.log(`Mission: ${mission.title} [${mission.status}]`);
        console.log(`  Milestones: ${doneMilestones}/${totalMilestones} done`);
        console.log(`  Tasks: ${doneTasks}/${totalTasks} done`);
        if (assertions.length > 0) {
          console.log(
            `  Validation: ${passingCount} passing, ${failingCount} failing, ${pendingCount} pending`
          );
        }
      }
      break;
    }
    case "clear": {
      clearMissionRecord(dir);
      if (json2) {
        console.log(JSON.stringify({ cleared: true }));
      } else {
        console.log("Mission cleared");
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide mission <set|create|show|status|plan-complete|clear>

  set "title"  [-d "description"]   Set mission (status: active)
  create "title" [-d "description"] Create mission (status: planning)
  show                              Show current mission
  status                            Show mission progress
  plan-complete                     Signal planning is done
  clear                             Clear the mission`);
      break;
    default:
      outputError(
        "Usage: tmux-ide mission <set|create|show|status|plan-complete|clear>\nRun: tmux-ide mission help",
        "USAGE"
      );
  }
}
function handleMilestone(dir, sub, args, values2, json2) {
  switch (sub) {
    case "create": {
      const title = args[0];
      if (!title) outputError('Usage: tmux-ide milestone create "title" --sequence N', "USAGE");
      const sequence = parseInt(values2.sequence ?? "0", 10) || void 0;
      const { milestone } = createMilestoneRecord(dir, {
        title,
        description: values2.description ?? "",
        sequence
      });
      if (json2) {
        console.log(JSON.stringify(milestone, null, 2));
      } else {
        console.log(`Created milestone ${milestone.id}: ${title} [${milestone.status}]`);
      }
      break;
    }
    case "list": {
      const mission = loadMission(dir);
      if (!mission) outputError("No mission set", "NOT_FOUND");
      const tasks = loadTasks(dir);
      const milestones = [...mission.milestones].sort((a, b) => a.order - b.order);
      if (json2) {
        const data = milestones.map((m) => {
          const mTasks = tasks.filter((t) => t.milestone === m.id);
          const done = mTasks.filter((t) => t.status === "done").length;
          return { ...m, taskCount: mTasks.length, tasksDone: done };
        });
        console.log(JSON.stringify(data, null, 2));
      } else if (milestones.length === 0) {
        console.log('No milestones. Run: tmux-ide milestone create "title" --sequence N');
      } else {
        for (const m of milestones) {
          const mTasks = tasks.filter((t) => t.milestone === m.id);
          const done = mTasks.filter((t) => t.status === "done").length;
          console.log(`  ${m.id}  [${m.status}]  ${m.title}  (${done}/${mTasks.length} tasks)`);
        }
      }
      break;
    }
    case "show": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide milestone show <id>", "USAGE");
      const mission = loadMission(dir);
      if (!mission) outputError("No mission set", "NOT_FOUND");
      const milestone = mission.milestones.find((m) => m.id === id);
      if (!milestone) outputError(`Milestone ${id} not found`, "NOT_FOUND");
      const tasks = loadTasks(dir).filter((t) => t.milestone === id);
      if (json2) {
        console.log(JSON.stringify({ milestone, tasks }, null, 2));
      } else {
        console.log(`Milestone ${milestone.id}: ${milestone.title}`);
        console.log(`  Status: ${milestone.status}`);
        console.log(`  Order: ${milestone.order}`);
        if (milestone.description) console.log(`  Description: ${milestone.description}`);
        if (tasks.length > 0) {
          console.log(`  Tasks:`);
          for (const t of tasks) {
            console.log(`    ${t.id}  [${t.status}]  ${t.title}`);
          }
        }
      }
      break;
    }
    case "update": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide milestone update <id> --status <status>", "USAGE");
      const { milestone } = updateMilestoneRecord(dir, {
        milestoneId: id,
        status: values2.status,
        description: values2.description
      });
      if (json2) {
        console.log(JSON.stringify(milestone, null, 2));
      } else {
        console.log(`Updated milestone ${id}`);
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide milestone <create|list|show|update>

  create "title" [--sequence N] [-d "desc"]   Create a milestone
  list                                         List milestones
  show <id>                                    Show milestone with tasks
  update <id> [--status active|done|locked]    Update milestone`);
      break;
    default:
      outputError(
        "Usage: tmux-ide milestone <create|list|show|update>\nRun: tmux-ide milestone help",
        "USAGE"
      );
  }
}
function handleValidation(dir, sub, args, values2, json2) {
  switch (sub) {
    case "show": {
      const contract = loadValidationContract(dir);
      const state = loadValidationState(dir);
      if (json2) {
        console.log(JSON.stringify({ contract: contract ?? null, state: state ?? null }, null, 2));
      } else {
        if (contract) {
          console.log("Validation Contract:");
          console.log(contract);
          console.log();
        } else {
          console.log("No validation contract found (.tasks/validation-contract.md)");
        }
        if (state && Object.keys(state.assertions).length > 0) {
          console.log("Assertion Status:");
          for (const [id, entry] of Object.entries(state.assertions)) {
            const evidence = entry.evidence ? ` \u2014 ${entry.evidence}` : "";
            console.log(`  ${id}  [${entry.status}]${evidence}`);
          }
        } else {
          console.log("No assertion state yet.");
        }
      }
      break;
    }
    case "assert": {
      const assertionId = args[0];
      if (!assertionId || !values2.status) {
        outputError(
          'Usage: tmux-ide validate assert <ASSERT_ID> --status passing|failing [--evidence "..."]',
          "USAGE"
        );
      }
      const status4 = values2.status;
      if (status4 !== "passing" && status4 !== "failing" && status4 !== "blocked") {
        outputError("Status must be 'passing', 'failing', or 'blocked'", "USAGE");
      }
      ensureTasksDir(dir);
      const state = loadValidationState(dir) ?? { assertions: {}, lastVerified: null };
      state.assertions[assertionId] = {
        status: status4,
        verifiedBy: values2.assign ?? null,
        verifiedAt: (/* @__PURE__ */ new Date()).toISOString(),
        evidence: values2.evidence ?? null,
        blockedBy: status4 === "blocked" ? values2.evidence ?? null : null
      };
      state.lastVerified = (/* @__PURE__ */ new Date()).toISOString();
      saveValidationState(dir, state);
      if (json2) {
        console.log(JSON.stringify({ assertionId, ...state.assertions[assertionId] }, null, 2));
      } else {
        console.log(
          `Assertion ${assertionId}: ${status4}${values2.evidence ? ` \u2014 ${values2.evidence}` : ""}`
        );
      }
      break;
    }
    case "report": {
      const state = loadValidationState(dir);
      if (!state || Object.keys(state.assertions).length === 0) {
        if (json2) {
          console.log(JSON.stringify({ total: 0, passing: 0, failing: 0, pending: 0, blocked: 0 }));
        } else {
          console.log("No assertions to report.");
        }
        break;
      }
      const entries = Object.values(state.assertions);
      const report2 = {
        total: entries.length,
        passing: entries.filter((e) => e.status === "passing").length,
        failing: entries.filter((e) => e.status === "failing").length,
        pending: entries.filter((e) => e.status === "pending").length,
        blocked: entries.filter((e) => e.status === "blocked").length
      };
      if (json2) {
        console.log(JSON.stringify(report2, null, 2));
      } else {
        console.log(`Validation Report: ${report2.total} assertions`);
        console.log(`  Passing: ${report2.passing}`);
        console.log(`  Failing: ${report2.failing}`);
        console.log(`  Pending: ${report2.pending}`);
        if (report2.blocked > 0) console.log(`  Blocked: ${report2.blocked}`);
      }
      break;
    }
    case "coverage": {
      const result = checkCoverage(dir);
      const contract = loadValidationContract(dir);
      const allIds = contract ? parseAssertionIds2(contract) : [];
      const claimed = allIds.length - result.unclaimed.length;
      if (json2) {
        console.log(
          JSON.stringify(
            {
              total: allIds.length,
              claimed,
              unclaimed: result.unclaimed,
              duplicates: result.duplicates
            },
            null,
            2
          )
        );
      } else {
        console.log(`Assertion Coverage: ${claimed}/${allIds.length} claimed`);
        if (result.unclaimed.length > 0) {
          console.log(`  Unclaimed: ${result.unclaimed.join(", ")}`);
        }
        if (Object.keys(result.duplicates).length > 0) {
          console.log(`  Duplicate claims:`);
          for (const [id, taskIds] of Object.entries(result.duplicates)) {
            console.log(`    ${id}: tasks ${taskIds.join(", ")}`);
          }
        }
        if (result.unclaimed.length === 0 && Object.keys(result.duplicates).length === 0) {
          console.log("  Full coverage \u2014 all assertions claimed.");
        }
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide validate <show|assert|report|coverage>

  show                                                       Show contract + assertion state
  assert <ID> --status passing|failing|blocked [--evidence]  Update assertion status
  report                                                     Summary of assertion results
  coverage                                                   Check assertion coverage`);
      break;
    default:
      outputError(
        "Usage: tmux-ide validate <show|assert|report|coverage>\nRun: tmux-ide validate help",
        "USAGE"
      );
  }
}
function handleGoal(dir, sub, args, values2, json2) {
  switch (sub) {
    case "list": {
      const goals = loadGoals(dir);
      if (json2) {
        console.log(JSON.stringify(goals, null, 2));
      } else if (goals.length === 0) {
        console.log('No goals. Run: tmux-ide goal create "..."');
      } else {
        for (const g of goals) {
          const tasks = loadTasksForGoal(dir, g.id);
          const done = tasks.filter((t) => t.status === "done").length;
          console.log(`  ${g.id}  [${g.status}]  ${g.title}  (${done}/${tasks.length} tasks)`);
        }
      }
      break;
    }
    case "create": {
      const title = args[0];
      if (!title) outputError('Usage: tmux-ide goal create "title"', "USAGE");
      const { goal, goalId } = createGoalRecord(dir, {
        title,
        description: values2.description ?? "",
        acceptance: values2.acceptance ?? "",
        priority: parseInt(values2.priority ?? "2", 10),
        specialty: values2.specialty ?? null,
        milestone: values2.milestone ?? null
      });
      if (json2) {
        console.log(JSON.stringify(goal, null, 2));
      } else {
        console.log(`Created goal ${goalId}: ${title}`);
      }
      break;
    }
    case "update": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide goal update <id>", "USAGE");
      const { goal } = updateGoalRecord(dir, {
        goalId: id,
        status: values2.status,
        description: values2.description,
        acceptance: values2.acceptance,
        priority: values2.priority ? parseInt(values2.priority, 10) : void 0,
        milestone: values2.milestone
      });
      if (json2) {
        console.log(JSON.stringify(goal, null, 2));
      } else {
        console.log(`Updated goal ${id}`);
      }
      break;
    }
    case "done": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide goal done <id>", "USAGE");
      const { goal } = doneGoalRecord(dir, id);
      if (json2) {
        console.log(JSON.stringify(goal, null, 2));
      } else {
        console.log(`Goal ${id} marked done: ${goal.title}`);
      }
      break;
    }
    case "show": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide goal show <id>", "USAGE");
      const goal = loadGoal(dir, id);
      if (!goal) outputError(`Goal ${id} not found`, "NOT_FOUND");
      const tasks = loadTasksForGoal(dir, id);
      if (json2) {
        console.log(JSON.stringify({ goal, tasks }, null, 2));
      } else {
        console.log(`Goal ${goal.id}: ${goal.title}`);
        console.log(`  Status: ${goal.status}`);
        if (goal.acceptance) console.log(`  Acceptance: ${goal.acceptance}`);
        if (tasks.length > 0) {
          console.log(`  Tasks:`);
          for (const t of tasks) {
            console.log(`    ${t.id}  [${t.status}]  ${t.title}`);
          }
        }
      }
      break;
    }
    case "delete": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide goal delete <id>", "USAGE");
      deleteGoalRecord(dir, id);
      if (json2) {
        console.log(JSON.stringify({ deleted: id }));
      } else {
        console.log(`Deleted goal ${id}`);
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide goal <list|create|update|done|show|delete>

  list                                 List all goals
  create "title" [-p N] [-d "desc"]    Create a goal
  update <id> [-s status] [-p N]       Update a goal
  done <id>                            Mark goal complete
  show <id>                            Show goal with tasks
  delete <id>                          Delete a goal`);
      break;
    default:
      outputError(
        "Usage: tmux-ide goal <list|create|update|done|show|delete>\nRun: tmux-ide goal help",
        "USAGE"
      );
  }
}
function handleTask(dir, sub, args, values2, json2) {
  switch (sub) {
    case "init": {
      ensureTasksDir(dir);
      if (json2) {
        console.log(JSON.stringify({ initialized: true }));
      } else {
        console.log("Initialized .tasks/ directory");
      }
      break;
    }
    case "list": {
      let tasks = loadTasks(dir);
      const allTasks = tasks;
      if (values2.status) tasks = tasks.filter((t) => t.status === values2.status);
      if (values2.goal) tasks = tasks.filter((t) => t.goal === values2.goal);
      tasks.sort((a, b) => a.priority - b.priority);
      if (json2) {
        console.log(JSON.stringify(tasks, null, 2));
      } else if (tasks.length === 0) {
        console.log('No tasks. Run: tmux-ide task create "title"');
      } else {
        const goals = loadGoals(dir);
        const goalNames = new Map(goals.map((g) => [g.id, g.title]));
        for (const t of tasks) {
          const blocked = t.depends_on.length > 0 && t.depends_on.some((depId) => {
            const dep = allTasks.find((d) => d.id === depId);
            return !dep || dep.status !== "done";
          });
          const status4 = blocked ? "blocked" : t.status;
          const assignee = t.assignee ? ` @${t.assignee}` : "";
          const goalLabel = t.goal ? ` (${goalNames.get(t.goal) ?? `goal ${t.goal}`})` : "";
          const deps2 = blocked ? ` (depends: ${t.depends_on.join(", ")})` : "";
          console.log(`  ${t.id}  [${status4}]  ${t.title}${assignee}${goalLabel}${deps2}`);
        }
      }
      break;
    }
    case "create": {
      const title = args[0];
      if (!title)
        outputError(
          'Missing title. Usage:\n  tmux-ide task create "Fix the auth bug" -g 01 -p 1 -d "Token storage issue"',
          "USAGE"
        );
      const { task, taskId } = createTaskRecord(dir, {
        title,
        description: values2.description ?? "",
        goalId: values2.goal ?? null,
        assign: values2.assign ?? null,
        priority: parseInt(values2.priority ?? "2", 10),
        tags: values2.tags,
        depends: values2.depends,
        milestone: values2.milestone ?? null,
        specialty: values2.specialty ?? null,
        fulfills: values2.fulfills
      });
      if (json2) {
        console.log(JSON.stringify(task, null, 2));
      } else {
        console.log(`Created task ${taskId}: ${title}`);
      }
      break;
    }
    case "update": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide task update <id>", "USAGE");
      const { task } = updateTaskRecord(dir, {
        taskId: id,
        status: values2.status,
        assign: values2.assign,
        description: values2.description,
        priority: values2.priority ? parseInt(values2.priority, 10) : void 0,
        tags: values2.tags,
        goalId: values2.goal,
        depends: values2.depends,
        proof: values2.proof,
        milestone: values2.milestone,
        specialty: values2.specialty,
        fulfills: values2.fulfills
      });
      if (json2) {
        console.log(JSON.stringify(task, null, 2));
      } else {
        console.log(`Updated task ${id}`);
      }
      break;
    }
    case "done": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide task done <id>", "USAGE");
      const { task } = doneTaskRecord(dir, id, values2.proof);
      if (values2.summary) {
        updateTaskRecord(dir, { taskId: id, summary: values2.summary });
        task.salientSummary = values2.summary;
      }
      let prWarning;
      if (values2.pr) {
        if (isGhAvailable()) {
          const pr = createTaskPr(task, dir);
          if (pr) {
            if (!task.proof) task.proof = {};
            task.proof.pr = {
              number: pr.number,
              url: pr.url,
              status: "open"
            };
            if (!json2) console.log(`PR created: ${pr.url}`);
          } else {
            prWarning = "PR creation failed (gh error)";
            console.error(`Warning: ${prWarning}`);
          }
        } else {
          prWarning = "gh CLI not found, skipping PR creation";
          console.error(`Warning: ${prWarning}`);
        }
      }
      saveTask(dir, task);
      if (json2) {
        const output = { ...task };
        if (prWarning) output.prError = prWarning;
        console.log(JSON.stringify(output, null, 2));
      } else {
        console.log(`Task ${id} done: ${task.title}`);
      }
      if (task.goal && areGoalTasksDone(dir, task.goal)) {
        if (!json2) {
          console.log(
            `All tasks for goal ${task.goal} are done. Run: tmux-ide goal done ${task.goal}`
          );
        }
      }
      break;
    }
    case "claim": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide task claim <id>", "USAGE");
      const { task } = claimTaskRecord(dir, id, values2.assign ?? "");
      if (json2) {
        console.log(JSON.stringify(task, null, 2));
      } else {
        const who = task.assignee ? ` by ${task.assignee}` : "";
        console.log(`Task ${id} claimed${who}: ${task.title}`);
      }
      break;
    }
    case "show": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide task show <id>", "USAGE");
      const task = loadTask(dir, id);
      if (!task) outputError(`Task ${id} not found. Run: tmux-ide task list`, "NOT_FOUND");
      const mission = loadMission(dir);
      const goal = task.goal ? loadGoal(dir, task.goal) : null;
      if (json2) {
        console.log(
          JSON.stringify({ mission: mission ?? null, goal: goal ?? null, task }, null, 2)
        );
      } else {
        if (mission) console.log(`Mission: ${mission.title}`);
        if (goal) {
          console.log(`Goal ${goal.id}: ${goal.title}`);
          if (goal.acceptance) console.log(`  Acceptance: ${goal.acceptance}`);
        }
        console.log(`Task ${task.id}: ${task.title}`);
        console.log(`  Status: ${task.status}`);
        if (task.description) console.log(`  Description: ${task.description}`);
        if (task.assignee) console.log(`  Assignee: ${task.assignee}`);
        if (task.tags.length > 0) console.log(`  Tags: ${task.tags.join(", ")}`);
        if (task.depends_on.length > 0) console.log(`  Depends on: ${task.depends_on.join(", ")}`);
        if (task.proof) console.log(`  Proof: ${JSON.stringify(task.proof)}`);
      }
      break;
    }
    case "edit": {
      const id = args[0];
      if (!id)
        outputError("Usage: tmux-ide task edit <id> --title 'new title' -d 'new desc'", "USAGE");
      const task = loadTask(dir, id);
      if (!task) outputError(`Task ${id} not found. Run: tmux-ide task list`, "NOT_FOUND");
      if (values2.title) task.title = values2.title;
      if (values2.description) task.description = values2.description;
      if (values2.priority) task.priority = parseInt(values2.priority, 10);
      if (values2.tags) task.tags = values2.tags.split(",").map((t) => t.trim());
      if (values2.goal) task.goal = values2.goal;
      if (values2.depends) {
        const newDeps = values2.depends.split(",").map((d) => d.trim());
        const cycle = detectCycle(dir, task.id, newDeps);
        if (cycle) {
          outputError(`Dependency cycle detected: ${cycle.join(" -> ")}`, "CYCLE");
          return;
        }
        task.depends_on = newDeps;
      }
      task.updated = (/* @__PURE__ */ new Date()).toISOString();
      saveTask(dir, task);
      if (json2) {
        console.log(JSON.stringify(task, null, 2));
      } else {
        console.log(`Edited task ${id}: ${task.title}`);
      }
      break;
    }
    case "unassign": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide task unassign <id>", "USAGE");
      const task = loadTask(dir, id);
      if (!task) outputError(`Task ${id} not found. Run: tmux-ide task list`, "NOT_FOUND");
      task.assignee = null;
      task.status = "todo";
      task.updated = (/* @__PURE__ */ new Date()).toISOString();
      saveTask(dir, task);
      if (json2) {
        console.log(JSON.stringify(task, null, 2));
      } else {
        console.log(`Task ${id} unassigned, status reset to todo: ${task.title}`);
      }
      break;
    }
    case "delete": {
      const id = args[0];
      if (!id) outputError("Usage: tmux-ide task delete <id>", "USAGE");
      deleteTaskRecord(dir, id);
      if (json2) {
        console.log(JSON.stringify({ deleted: id }));
      } else {
        console.log(`Deleted task ${id}`);
      }
      break;
    }
    case "help":
    case void 0:
      console.log(`Usage: tmux-ide task <subcommand>

  init                                     Initialize .tasks/ directory
  list [-s status] [-g goalId] [--json]    List tasks
  create "title" [-g id] [-p N] [-d ...]   Create a task
  edit <id> [--title ...] [-d ...] [-p N]  Edit task fields
  update <id> [-s status] [-a name]        Update task status/assignee
  done <id> [--proof "..."]                Mark task complete
  claim <id> [-a name]                     Claim and start a task
  unassign <id>                            Unassign and reset to todo
  show <id> [--json]                       Show task with full context
  delete <id>                              Delete a task

Short flags: -p priority, -g goal, -d description, -a assign,
             -s status, -t tags, -b branch`);
      break;
    default:
      outputError(`Unknown subcommand: ${sub}
Run: tmux-ide task help`, "USAGE");
  }
}

// bin/cli.ts
init_send();

// packages/daemon/src/dashboard.ts
init_canonical_daemon();
init_errors2();
import { spawn as spawn10 } from "node:child_process";
function openInBrowser2(url) {
  const platform = process.platform;
  const cmd = platform === "darwin" ? "open" : platform === "win32" ? "start" : "xdg-open";
  try {
    const child = spawn10(cmd, [url], { stdio: "ignore", detached: true });
    child.unref();
  } catch {
  }
}
async function dashboard(opts = {}) {
  const info = readCanonicalDaemonInfo();
  if (!info) {
    throw new IdeError(
      "No tmux-ide daemon is running.\nStart one by running `tmux-ide` inside a project directory, then re-run this command.",
      { code: "DAEMON_NOT_RUNNING", exitCode: 1 }
    );
  }
  const url = `http://${info.bindHostname}:${info.port}/`;
  if (opts.json) {
    process.stdout.write(JSON.stringify({ url, port: info.port, pid: info.pid }) + "\n");
  } else {
    process.stdout.write(`Dashboard: ${url}
`);
  }
  if (opts.open !== false) {
    openInBrowser2(url);
  }
}

// bin/cli.ts
init_errors2();
init_output();
var __dirname5 = dirname19(fileURLToPath7(import.meta.url));
var { positionals, values } = parseArgs({
  allowPositionals: true,
  strict: false,
  options: {
    json: { type: "boolean" },
    tasks: { type: "boolean" },
    fix: { type: "boolean" },
    row: { type: "string" },
    pane: { type: "string" },
    title: { type: "string" },
    command: { type: "string" },
    size: { type: "string" },
    write: { type: "boolean" },
    template: { type: "string" },
    name: { type: "string" },
    verbose: { type: "boolean", default: false },
    help: { type: "boolean", short: "h" },
    version: { type: "boolean", short: "v" },
    // task command flags
    description: { type: "string", short: "d" },
    acceptance: { type: "string" },
    priority: { type: "string", short: "p" },
    status: { type: "string", short: "s" },
    assign: { type: "string", short: "a" },
    goal: { type: "string", short: "g" },
    tags: { type: "string", short: "t" },
    proof: { type: "string" },
    depends: { type: "string" },
    pr: { type: "boolean" },
    specialty: { type: "string" },
    milestone: { type: "string" },
    fulfills: { type: "string" },
    summary: { type: "string" },
    sequence: { type: "string" },
    evidence: { type: "string" },
    port: { type: "string" },
    // tunnel command flags
    provider: { type: "string" },
    domain: { type: "string" },
    authtoken: { type: "string" },
    // setup command flags
    edit: { type: "boolean" },
    wizard: { type: "boolean" },
    // remote command flags
    url: { type: "string" },
    "hq-url": { type: "string" },
    host: { type: "string" },
    path: { type: "string" },
    "local-port": { type: "string" },
    "remote-port": { type: "string" },
    // send command flags
    to: { type: "string" },
    "no-enter": { type: "boolean" },
    // dashboard command flags
    open: { type: "boolean" },
    "no-open": { type: "boolean" },
    // agent hook command flags
    print: { type: "boolean" },
    // chat command flags (T078)
    role: { type: "string" }
  }
});
var knownCommands = /* @__PURE__ */ new Set([
  "start",
  "init",
  "stop",
  "attach",
  "restart",
  "ls",
  "doctor",
  "status",
  "inspect",
  "validate",
  "detect",
  "config",
  "mission",
  "milestone",
  "goal",
  "task",
  "research",
  "plan",
  "skill",
  "metrics",
  "setup",
  "send",
  "dispatch",
  "notify",
  "orchestrator",
  "settings",
  "command-center",
  "dashboard",
  "server",
  "tunnel",
  "remote",
  "checkpoint",
  "chat",
  "agent",
  "agents",
  "__remote-serve",
  "help"
]);
if (values.version) {
  const pkg = await Promise.resolve().then(() => __toESM(require_package(), 1));
  console.log(`tmux-ide v${pkg.version}`);
  process.exit(0);
}
if (values.verbose) {
  globalThis.__tmuxIdeVerbose = true;
}
var ALIASES = {
  t: "task",
  g: "goal",
  m: "mission",
  ms: "milestone",
  orch: "orchestrator",
  o: "orchestrator",
  met: "metrics"
};
var firstPositional = positionals[0];
var resolved = ALIASES[firstPositional] ?? firstPositional;
var hasKnownCommand = resolved ? knownCommands.has(resolved) : false;
var command = hasKnownCommand ? resolved : "start";
var startTargetDir = hasKnownCommand ? positionals[1] : firstPositional;
var json = values.json ?? false;
var noColor = "NO_COLOR" in process.env;
var bold = (s) => noColor ? s : `\x1B[1m${s}\x1B[22m`;
var cyan = (s) => noColor ? s : `\x1B[36m${s}\x1B[39m`;
var dim = (s) => noColor ? s : `\x1B[2m${s}\x1B[22m`;
if (values.help) {
  printHelp();
  process.exit(0);
}
function printHelp() {
  console.log(`${bold("tmux-ide")} \u2014 Terminal IDE powered by tmux

${bold("Usage:")}
  ${cyan("tmux-ide")}                    ${dim("Launch IDE from ide.yml")}
  ${cyan("tmux-ide <path>")}             ${dim("Launch from a specific directory")}
  ${cyan("tmux-ide setup")}              ${dim("Interactive TUI setup wizard")}
  ${cyan("tmux-ide setup --edit")}       ${dim("Open config tree editor")}
  ${cyan("tmux-ide settings")}           ${dim("Interactive TUI config manager")}
  ${cyan("tmux-ide init")} [--template]  ${dim("Scaffold a new ide.yml (auto-detects stack)")}
  ${cyan("tmux-ide stop")}               ${dim("Kill the current IDE session")}
  ${cyan("tmux-ide restart")}            ${dim("Stop and relaunch the IDE session")}
  ${cyan("tmux-ide attach")}             ${dim("Reattach to a running session")}
  ${cyan("tmux-ide ls")}                 ${dim("List all tmux sessions")}
  ${cyan("tmux-ide status")} [--json]    ${dim("Show session status")}
  ${cyan("tmux-ide inspect")} [--json]   ${dim("Show effective config and runtime state")}
  ${cyan("tmux-ide doctor")}             ${dim("Check system requirements")}
  ${cyan("tmux-ide validate")} [--json]  ${dim("Validate ide.yml")}
  ${cyan("tmux-ide detect")} [--json]    ${dim("Detect project stack")}
  ${cyan("tmux-ide detect --write")}     ${dim("Detect and write ide.yml")}
  ${cyan("tmux-ide config")} [--json]    ${dim("Dump config as JSON")}
  ${cyan("tmux-ide config set")} <path> <value>
  ${cyan("tmux-ide config add-pane")} --row <N> --title <T> [--command <C>]
  ${cyan("tmux-ide config remove-pane")} --row <N> --pane <M>
  ${cyan("tmux-ide config add-row")} [--size <percent>]
  ${cyan("tmux-ide config enable-team")} [--name <N>]   ${dim("Enable agent teams")}
  ${cyan("tmux-ide config disable-team")}               ${dim("Disable agent teams")}

${bold("Pane Messaging:")}
  ${cyan("tmux-ide send")} <target> <message>     ${dim("Send message to a pane")}
  ${cyan("tmux-ide send")} --to <name> <message>   ${dim("Target by name, title, role, or ID")}
  ${cyan("tmux-ide send")} <target> --no-enter msg  ${dim("Send text without pressing Enter")}

${bold("Dispatch:")}
  ${cyan("tmux-ide dispatch")} <id> [--json]        ${dim("Print task context to stdout")}
  ${cyan("tmux-ide notify")} <message> [--json]     ${dim("Send notification to lead pane")}

${bold("Orchestrator:")}
  ${cyan("tmux-ide orchestrator")} [--json]         ${dim("Show orchestrator status")}
  ${cyan("tmux-ide orch")}                          ${dim("Alias for orchestrator")}
  ${cyan("tmux-ide server")} [--port N]             ${dim("Start v2.5 HTTP + PTY WebSocket server")}

${bold("Multi-agent Chat:")}
  ${cyan("tmux-ide chat session add")} <thread-id> --provider <name> [--role <role>]
                                  ${dim("Register a Session on a Thread (lead|teammate|planner|validator|researcher)")}

${bold("SSH Remotes:")}
  ${cyan("tmux-ide remote ssh hosts")} [--json]       ${dim("List concrete Host aliases from ~/.ssh/config")}
  ${cyan("tmux-ide remote ssh add")} <name> --host <ssh-host> --path <dir>
                                  ${dim("Save a tunnel-only remote project")}
  ${cyan("tmux-ide remote ssh launch")} <name> [--no-open]
                                  ${dim("Open a local SSH tunnel to a remote tmux-ide dashboard")}

${bold("Agent Fleet:")}
  ${cyan("tmux-ide agents")} [list] [--json]          ${dim("Every agent across machines (see also: agent)")}
  ${cyan("tmux-ide agents send")} <agent-id> <msg...> ${dim("Send input to any agent, local or remote")}
  ${cyan("tmux-ide agent hook install")} [--print]    ${dim("Self-report hooks for plain-terminal sessions")}
  ${cyan("tmux-ide agent report")} <event>            ${dim("Invoked by Claude Code hooks (see also: agents)")}

${bold("Task Management:")}
  ${cyan("tmux-ide mission set")} "title"              ${dim("Set the project mission")}
  ${cyan("tmux-ide mission show")}                     ${dim("Show current mission")}
  ${cyan("tmux-ide goal list")}                        ${dim("List goals")}
  ${cyan("tmux-ide goal create")} "title"              ${dim("Create a goal")}
  ${cyan("tmux-ide goal show")} <id>                   ${dim("Show goal with tasks")}
  ${cyan("tmux-ide task list")} [--status X --goal Y]  ${dim("List tasks")}
  ${cyan("tmux-ide task create")} "title" [--goal id]  ${dim("Create a task")}
  ${cyan("tmux-ide task claim")} <id> [--assign name]  ${dim("Claim a task")}
  ${cyan("tmux-ide task done")} <id> [--proof "..."]   ${dim("Complete a task")}
  ${cyan("tmux-ide task show")} <id>                   ${dim("Show task with full context")}
  ${cyan("tmux-ide research status")}                  ${dim("Show research agent state")}
  ${cyan("tmux-ide research trigger")} <type>          ${dim("Manually dispatch a research task")}

${bold("Flags:")}
  ${cyan("--json")}                      ${dim("Output as JSON (all commands)")}
  ${cyan("--template <name>")}           ${dim("Use specific template for init")}
  ${cyan("--write")}                     ${dim("Write detected config to ide.yml")}
  ${cyan("--verbose")}                   ${dim("Log all tmux commands (or set TMUX_IDE_DEBUG=1)")}
  ${cyan("-h, --help")}                  ${dim("Show usage")}
  ${cyan("-v, --version")}               ${dim("Show version number")}`);
}
function execBunWidget(scriptPath, args, commandLabel) {
  const widgetMissing = !existsSync42(scriptPath);
  let bunMissing = false;
  try {
    execFileSync9("bun", ["--version"], { stdio: "ignore" });
  } catch {
    bunMissing = true;
  }
  if (widgetMissing || bunMissing) {
    const reasons = [];
    if (bunMissing) reasons.push("the `bun` runtime is not installed (https://bun.sh)");
    if (widgetMissing)
      reasons.push(
        "the TUI widget sources are absent (they ship only in a cloned tmux-ide checkout, not the npm package)"
      );
    throw new IdeError(
      `\`tmux-ide ${commandLabel}\` is unavailable because ${reasons.join(" and ")}.
Run it from a cloned tmux-ide checkout with bun installed.`,
      { code: "USAGE", exitCode: 1 }
    );
  }
  execFileSync9("bun", [scriptPath, ...args], { stdio: "inherit" });
}
try {
  switch (command) {
    case "start":
      await launch(startTargetDir, { json });
      break;
    case "init":
      await init({ template: values.template, json });
      break;
    case "stop":
      await stop(positionals[1], { json });
      break;
    case "attach":
      await attach(positionals[1], { json });
      break;
    case "restart":
      await restart(positionals[1], { json });
      break;
    case "ls":
      await ls({ json });
      break;
    case "doctor":
      await doctor({ json, tasks: values.tasks, fix: values.fix });
      break;
    case "status":
      await status(positionals[1], { json });
      break;
    case "inspect":
      await inspect(positionals[1], { json });
      break;
    case "validate": {
      const valSub = positionals[1];
      if (valSub === "assert" || valSub === "show" || valSub === "report" || valSub === "coverage" || valSub === "help") {
        await taskCommand(null, {
          json,
          action: "validate",
          sub: valSub,
          args: positionals.slice(2),
          values: {
            status: values.status,
            evidence: values.evidence,
            assign: values.assign
          }
        });
      } else {
        await validate(valSub, { json });
      }
      break;
    }
    case "detect":
      await detect(positionals[1], { json, write: values.write });
      break;
    case "config": {
      const sub = positionals[1];
      let action = "dump";
      let configArgs = [];
      if (sub === "set") {
        action = "set";
        configArgs = positionals.slice(2);
      } else if (sub === "add-pane") {
        action = "add-pane";
        configArgs = [];
        if (values.row !== void 0) configArgs.push("--row", values.row);
        if (values.title !== void 0) configArgs.push("--title", values.title);
        if (values.command !== void 0) configArgs.push("--command", values.command);
        if (values.size !== void 0) configArgs.push("--size", values.size);
      } else if (sub === "remove-pane") {
        action = "remove-pane";
        configArgs = [];
        if (values.row !== void 0) configArgs.push("--row", values.row);
        if (values.pane !== void 0) configArgs.push("--pane", values.pane);
      } else if (sub === "add-row") {
        action = "add-row";
        configArgs = [];
        if (values.size !== void 0) configArgs.push("--size", values.size);
      } else if (sub === "enable-team") {
        action = "enable-team";
        configArgs = [];
        if (values.name !== void 0) configArgs.push("--name", values.name);
      } else if (sub === "disable-team") {
        action = "disable-team";
        configArgs = [];
      } else if (sub === "edit") {
        const scriptPath = resolve34(__dirname5, "../packages/daemon/src/widgets/setup/index.tsx");
        execBunWidget(
          scriptPath,
          ["--dir=" + resolve34(startTargetDir || "."), "--edit"],
          "config edit"
        );
        break;
      }
      await config(null, { json, action, args: configArgs });
      break;
    }
    case "mission": {
      const sub = positionals[1];
      await taskCommand(null, {
        json,
        action: "mission",
        sub,
        args: positionals.slice(2),
        values: { description: values.description }
      });
      break;
    }
    case "milestone": {
      const sub = positionals[1];
      await taskCommand(null, {
        json,
        action: "milestone",
        sub,
        args: positionals.slice(2),
        values: {
          description: values.description,
          status: values.status,
          sequence: values.sequence
        }
      });
      break;
    }
    case "goal": {
      const sub = positionals[1];
      await taskCommand(null, {
        json,
        action: "goal",
        sub,
        args: positionals.slice(2),
        values: {
          description: values.description,
          acceptance: values.acceptance,
          priority: values.priority,
          status: values.status,
          specialty: values.specialty
        }
      });
      break;
    }
    case "task": {
      const sub = positionals[1];
      await taskCommand(null, {
        json,
        action: "task",
        sub,
        args: positionals.slice(2),
        values: {
          title: values.title,
          description: values.description,
          priority: values.priority,
          status: values.status,
          assign: values.assign,
          goal: values.goal,
          tags: values.tags,
          proof: values.proof,
          depends: values.depends,
          pr: values.pr,
          specialty: values.specialty,
          milestone: values.milestone,
          fulfills: values.fulfills,
          summary: values.summary
        }
      });
      break;
    }
    case "research": {
      const sub = positionals[1];
      await taskCommand(null, {
        json,
        action: "research",
        sub,
        args: positionals.slice(2),
        values: {}
      });
      break;
    }
    case "plan": {
      const { planCommand: planCommand2 } = await Promise.resolve().then(() => (init_plan(), plan_exports));
      await planCommand2(null, {
        json,
        sub: positionals[1],
        args: positionals.slice(2),
        values: { status: values.status }
      });
      break;
    }
    case "skill": {
      const { skillCommand: skillCommand2 } = await Promise.resolve().then(() => (init_skill(), skill_exports));
      await skillCommand2(null, {
        json,
        sub: positionals[1],
        args: positionals.slice(2)
      });
      break;
    }
    case "metrics": {
      const { metricsCommand: metricsCommand2 } = await Promise.resolve().then(() => (init_metrics_cli(), metrics_cli_exports));
      await metricsCommand2(null, {
        json,
        sub: positionals[1]
      });
      break;
    }
    case "setup": {
      const scriptPath = resolve34(__dirname5, "../packages/daemon/src/widgets/setup/index.tsx");
      const setupArgs = ["--dir=" + resolve34(startTargetDir || ".")];
      if (positionals[1] === "--edit" || values.edit) setupArgs.push("--edit");
      if (positionals[1] === "--wizard" || values.wizard) setupArgs.push("--wizard");
      execBunWidget(scriptPath, setupArgs, "setup");
      break;
    }
    case "send": {
      const target = values.to ?? positionals[1];
      const messageStart = values.to ? 1 : 2;
      let message = positionals.slice(messageStart).join(" ");
      if (!message && !process.stdin.isTTY) {
        const { readFileSync: readFileSync31 } = await import("node:fs");
        message = readFileSync31(0, "utf-8").trim();
      }
      await send(null, { json, to: target, message, noEnter: values["no-enter"] });
      break;
    }
    case "dispatch": {
      const { dispatch: dispatchCmd } = await Promise.resolve().then(() => (init_dispatch(), dispatch_exports));
      const taskId = positionals[1];
      await dispatchCmd(null, { taskId, json });
      break;
    }
    case "notify": {
      const { notify: notifyCmd } = await Promise.resolve().then(() => (init_notify(), notify_exports));
      const notifyMessage = positionals.slice(1).join(" ");
      await notifyCmd(null, { message: notifyMessage || void 0, json });
      break;
    }
    case "orchestrator": {
      const { orchestratorStatus: orchestratorStatus2 } = await Promise.resolve().then(() => (init_orchestrator_status(), orchestrator_status_exports));
      await orchestratorStatus2(positionals[1], { json });
      break;
    }
    case "settings": {
      const scriptPath = resolve34(__dirname5, "../packages/daemon/src/widgets/config/index.tsx");
      execBunWidget(scriptPath, ["--dir=" + resolve34(startTargetDir || ".")], "settings");
      break;
    }
    case "tunnel": {
      const { tunnelCommand: tunnelCommand2 } = await Promise.resolve().then(() => (init_tunnel(), tunnel_exports));
      await tunnelCommand2(null, {
        json,
        sub: positionals[1],
        args: positionals.slice(2),
        values: {
          provider: values.provider,
          port: values.port,
          domain: values.domain,
          authtoken: values.authtoken
        }
      });
      break;
    }
    case "remote": {
      const { remoteCommand: remoteCommand2 } = await Promise.resolve().then(() => (init_remote(), remote_exports));
      await remoteCommand2(null, {
        json,
        sub: positionals[1],
        args: positionals.slice(2),
        values: {
          url: values.url,
          "hq-url": values["hq-url"],
          host: values.host,
          path: values.path,
          "local-port": values["local-port"],
          "remote-port": values["remote-port"],
          "no-open": values["no-open"]
        }
      });
      break;
    }
    case "agent": {
      const { agentCommand: agentCommand2 } = await Promise.resolve().then(() => (init_agent_hook(), agent_hook_exports));
      await agentCommand2({
        sub: positionals[1],
        args: positionals.slice(2),
        json,
        print: values.print === true
      });
      break;
    }
    case "agents": {
      const { agentsCommand: agentsCommand2 } = await Promise.resolve().then(() => (init_agents_cli(), agents_cli_exports));
      await agentsCommand2({
        sub: positionals[1],
        args: positionals.slice(2),
        json,
        noEnter: values["no-enter"] === true
      });
      break;
    }
    case "__remote-serve": {
      const { remoteServeCommand: remoteServeCommand2 } = await Promise.resolve().then(() => (init_ssh_remote(), ssh_remote_exports));
      await remoteServeCommand2({ port: values.port });
      break;
    }
    case "command-center": {
      const { startCommandCenter: startCommandCenter2 } = await Promise.resolve().then(() => (init_command_center(), command_center_exports));
      await startCommandCenter2({ port: parseInt(values.port ?? "4000") });
      break;
    }
    case "dashboard": {
      const noOpen = values["no-open"] === true || values.open === false;
      await dashboard({ json, open: !noOpen });
      break;
    }
    case "server": {
      if ("bun" in process.versions) {
        const scriptPath = resolve34(__dirname5, "../packages/daemon/src/server/standalone.ts");
        const serverArgs = ["--experimental-strip-types", scriptPath];
        if (values.port) serverArgs.push("--port", values.port);
        execFileSync9("node", serverArgs, { stdio: "inherit" });
      } else {
        const { start: start2 } = await Promise.resolve().then(() => (init_server2(), server_exports2));
        await start2(values.port ? parseInt(values.port, 10) : void 0);
      }
      break;
    }
    case "chat": {
      const { chatCommand: chatCommand2 } = await Promise.resolve().then(() => (init_chat(), chat_exports));
      await chatCommand2({
        sub: positionals[1],
        args: positionals.slice(2),
        json,
        provider: values.provider,
        role: values.role,
        name: values.name
      });
      break;
    }
    case "checkpoint": {
      const { checkpointCommand: checkpointCommand2 } = await Promise.resolve().then(() => (init_checkpoint(), checkpoint_exports));
      await checkpointCommand2({
        sub: positionals[1],
        args: positionals.slice(2),
        json
      });
      break;
    }
    case "help":
      printHelp();
      break;
    default:
      throw new IdeError(`Unknown command: ${command}
Run "tmux-ide help" for usage.`, {
        code: "USAGE",
        exitCode: 1
      });
  }
} catch (error) {
  if (error instanceof IdeError) {
    printCommandError(error, { json });
  } else {
    throw error;
  }
}
